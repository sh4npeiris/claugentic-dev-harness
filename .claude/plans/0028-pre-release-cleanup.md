# 0028 — Pre-0.3.0 cleanup (clear the plan-deferrals + release-machinery debt)

- **Status:** Plan-gate PASSED-with-changes — all 8 required changes FOLDED (2026-06-25). Ready for approval → implement. _(This plan file was recreated after an app crash lost the uncommitted draft; it is committed now so it survives. The committed 0027 work was safe throughout.)_ The 0.3.0 `release` force-push is HELD until this lands.
- **Branch state:** `origin/main` advanced to `bdf7068` while offline (the ruff chip merged as **PR #4** — lint debt now cleared on main); this branch was fast-forwarded onto it. So 0028 builds on `bdf7068`. The stale local `release` (461a47d, from 3cd1e57) will be rebuilt at Land.
- **Resumable from:** S1 renderOnly → S2 build_release test → S3 scanner gate → S4 seed-migration → 0028 Land (batched DECISIONS entry + condense) → rebuild `release` from the new main + hand the force-push to the user.
- **Blockers:** none.
- **Flags (reversible judgment-calls, decided from the research + plan-gate — surface at the approval gate):**
  - S3: Pass A.b (uncaveated harness-self-gate mention) ships **WARN-only** (heuristic precision); A.a + B are hard exit-1 (exact literals). Reversible — can harden A.b later once the marker-set is tuned.
  - S3: the VALID set for Pass B = agent basenames ∪ skill basenames ∪ `{update}` — **FS-derived** (not a hardcoded OLD_NAMES list); covers all future renames, duplicates nothing.
  - S3: the namespace pass scans **all shipped markdown** (not just SKILLs + the plan template) — strictly more robust at the same cost.
  - S4: seed files use the **underscore-prefix** convention (`docs/claugentic-_DECISIONS.md`), mirroring `docs/claugentic-standards/_TEMPLATE.md`; kept OUT of the managed-set table (a separate create-if-absent step). _(User may veto the naming at approval.)_
- **References:** the 0028 research workflow output (4 structured designs, run `wf_958b5e59`) · `scripts/build_release.py` (`classify`/`DEV_ONLY_*`) · `scripts/check_versions_synced.py` + `check_doc_budgets.py` (the run-gate mould) · `tests/test_build_release.py` (`TestBaseAncestryGuard`, `TestReleaseInitContract`) · `tests/workflows/agent-namespace.test.mjs` · `skills/init/SKILL.md` (step 7 + managed-set) · `engine/audit.js:1137` (renderOnly) · `docs/claugentic-INVARIANTS.md` (release/init contract + the SELECT re-render honesty rule).

## Problem
Plans 0025/0026/0027 each landed complete but deliberately deferred follow-ups, and the §C/0.3.0 release prep surfaced one untested release-machinery path. The user wants 0.3.0 to ship **genuinely complete + debt-free** — so we clear the *real* deferrals/debt now, while keeping the two **genuinely YAGNI** items deferred (audit synthesis-ceiling auto-sizing; Stage-2b engine orchestration — both wait on a measured trigger; building them now is the over-engineering the harness audits *against*).

Open items being cleared:
1. **renderOnly SELECT log line** (0025 S5) — the agent-free re-render path returns before the run's `log()` trail, so a stuck/empty re-render is undiagnosable.
2. **`build_release.py --apply` regression test** (§C/0.3.0 release prep) — the git-side `_apply` (incl. the `--no-verify` hook-bypass, commit `dfd2fea`) is untested; only the pure `classify` core is. It broke once at release.
3. **Unified shipped-content scanner** = the **content-grep guard** (0027 S3) + the **namespace presence-scan** (0026 §C3a) — *the same shape* (a scan over shipped-file TEXT for a forbidden literal), so built as ONE DRY gate. Today the release/init contract is pinned only at *membership* level; a roster rename can strand a live `claugentic-dev-harness:<old-role>` spawn literal in a shipped SKILL with all tests green (the §C3a review caught one by hand).
4. **Seed-migration** (0027) — the one-time seeds (DECISIONS/ROADMAP) are abstract prose inside `init` step 7; an init agent must *invent* the seed bytes. Migrate to shipped pristine `_template` files init copies, standardizing the three-kinds managed-file convention (added to WORKFLOW by 0027).

## Goals / Non-goals
- **Goal:** clear the four items above to production standard (each lands complete, no debt), then re-release 0.3.0 clean.
- **Non-goal (stay deferred — YAGNI):** audit synthesis-ceiling auto-sizing (0025); Stage-2b advisory engine orchestration (0026). Both wait on a measured trigger.
- **Non-goal (now DONE elsewhere):** the ruff E722/F401 in `tests/test_eval_manifest.py` — completed independently as **PR #4** (`bdf7068`, now on main), so it's off the board, not in 0028.
- **Non-goal:** migrating the step-6 Current-scope / detected-tooling blocks — those are *generated per-repo* from live detection, not static seeds; they stay generated.

## Approach (per item; condensed from the research + plan-gate folds marked ★)

**S1 — renderOnly log (engine/audit.js).** Insert 2 lines in the `renderOnly` fast-path (`:1137-1139`) before the `return`: a guarded `selectedCount = Array.isArray(args.renderOnly.items) ? args.renderOnly.items.length : 0` and ONE `log()` mirroring the run-header style (`audit renderOnly — re-rendering the backlog fence over N selected item(s); no FIND/PRUNE/VERIFY this pass.`). The trailing clause is the **honesty guard** (INVARIANTS "SELECT re-render … never a false terminal signal") — states a fact, never "sound/verified." Purely additive: no behavior/output/schema change.

**S2 — `_apply` regression test (tests/test_build_release.py).** Add `@pytest.mark.integration class TestApplyHookBypass` (the `integration` marker already exists in `pyproject.toml`) — a hermetic real-git fixture under `tmp_path` reusing the isolation recipe at `tests/test_check_architecture_tree.py:678-687` (empty `GIT_CONFIG_GLOBAL/SYSTEM`, `-c commit.gpgsign=false`): init repo → tracked `ARCHITECTURE_TREE.md` (the "missing" file) + a shipped file → commit → install an **always-fail pre-commit hook via `core.hooksPath=<absdir>`** → `git update-ref refs/remotes/origin/main HEAD` (satisfies the base-ancestry guard with zero network) → `monkeypatch.setattr(br, "_repo_root", lambda: tmp)` + `monkeypatch.chdir(tmp)` (★ plan-gate correction: of `_apply`'s git calls only `_tracked_files()` runs without `-C` and depends on cwd; status/rev-parse DO pass `-C` — so `chdir` is still required, just fix the *why*). `git update-ref refs/remotes/origin/main HEAD` is confirmed sufficient for the base guard. Assert `br._apply() == 0` + `git rev-parse --verify release` resolves + (the strip ran) `ls-tree -r release` excludes ARCHITECTURE_TREE.md, includes the shipped file. **CONTROL** (makes the assertion non-hollow): a plain `git commit` ★ run with the hook ARMED — NOT under the isolation recipe's `core.hooksPath=` empty pin; the control must use the repo's real hooksPath so the hook actually fires — returns non-zero, proving `_apply`'s success is attributable to `--no-verify`, not a dead hook. Does NOT re-test the base-ancestry refusal (already pinned at `TestBaseAncestryGuard`).

**S3 — unified shipped-content scanner (`scripts/check_shipped_content.py`).** ONE harness-self Python **run-gate** (sibling to `check_versions_synced.py`/`check_doc_budgets.py`, honoring the DECISIONS "one gate, one invariant — sibling not extension" rule). It `import build_release as br` and takes `SHIP = set(br.classify(br._tracked_files())[0])` as the single ship source-of-truth (legitimate — the gate is harness-self, reasons about the shipped tree, and is itself stripped, so the "copied gate can't import" rule doesn't apply). Pure cores over the shipped-file texts:
- **Pass B (namespace, HARD exit-1):** regex `claugentic-dev-harness:([a-z-]+)` over **all shipped markdown**; flag any captured token not in the **VALID set**. ★ plan-gate catch: the `claugentic-dev-harness:` prefix names BOTH agent-spawn ids AND **slash-commands** (`:audit`, `:build`, `:doctor`, `:init`, `:product`, `:update`) — a roster-only valid set would HARD-fail on ~29 legit command tokens today. So **VALID = agent basenames (`.claude/agents/*.md`) ∪ skill basenames (`skills/*/`) ∪ `{update}`** (the last is prose-only — no `skills/update/` dir, not FS-derivable — so a single documented hardcoded addition). Keying on the full `claugentic-dev-harness:` prefix structurally excludes the `<!-- product-critic:rejected-proposals -->` memory-fence tokens (the false-positive land-mine). FS-derived ⇒ duplicates neither the node test's `CUSTOM_AGENTS` nor an OLD_NAMES list, and survives all future agent/skill renames. Scan corpus MUST include `docs/claugentic-PLAN_TEMPLATE.md` (the exact §C3a near-miss surface).
- **Pass A.a (dangling-path literals, HARD exit-1):** flag any shipped-file reference to a **stripped-AND-init-uncreated** path. ★ plan-gate catch: `.claude/plans/TEMPLATE.md` is DEAD (0027 moved it to the now-shipping `docs/claugentic-PLAN_TEMPLATE.md`) — do NOT use it. The live forbidden set: `RELEASE_CHECKLIST.md`, the harness-self scripts as *paths* (`build_release.py`, `check_versions_synced.py`, `check_doc_budgets.py`, `check_shipped_content.py`), and a *specific* harness plan-file ref (`.claude/plans/<NNNN>-*.md`). Derive it as `DEV_ONLY_* minus the init-creates set` rather than hardcoding where practical. **Small documented allow-list** for legit shipped mentions of stripped-NAMED tokens (e.g. `init/SKILL.md`'s bare `.claude/plans/` *dir* = the adopter's own in-flight-plans dir — a real reference, not a dangle). The implementer MUST run the gate and tune the allow-list until it is GREEN on the current tree before committing (the allow-list is an explicit commented seam, not the gate guessing).
- **Pass A.b (uncaveated harness-self-gate mention, WARN-only):** for each shipped mention of a harness-self gate script, WARN if no adopter-caveat marker ("harness-self", "skip in an adopter", "N-A in an adopter", "not shipped", "stripped from the release") appears within a bounded window. Honestly heuristic (false pos/neg at margins) → WARN band (exit 0 + `WARN:` line, like `check_doc_budgets`), never a hard fail.

The gate must be **GREEN on the current tree at commit** — but ★ note the plan-gate's correction: green is NOT free (the slash-command false-positive in Pass B and the allow-list tuning in A.a are exactly what the implementer must get right). The implementer runs the gate and tunes VALID-set + allow-list until green; S3 stays purely additive (no content-fix on the shipped tree — research verified zero stranded *agent* literals + every shipped gate-mention already caveated). Wiring: add to `ci.yml` gates job + WORKFLOW DoD (harness-self / adopter-N-A framing) + doctor's harness-self gate list **with the 3-way `green/WARN/breach` band** (like `check_doc_budgets`, because of A.b's WARN — NOT the 2-way version-sync shape) + `build_release.py` DEV_ONLY_FILES (the scanner itself never ships) + a `test_build_release.py` `is_dev_only` strip assertion + the ARCHITECTURE_TREE entry (script is in `scripts/**/*.py` INCLUDE_GLOBS) + the INVARIANTS "Live gate" line. **Honesty (the #1 rule):** the Live-gate line + the WORKFLOW/doctor/gate-header copy state the contract is mechanically pinned for the **exact-literal** cases (A.a dangling paths + B stranded roster) and **WARN-heuristic** for A.b — they must NOT imply the contract is now *fully mechanically content-enforced*, and the gate is a **run-gate, not hook-enforced**. Hermetic characterization tests in `tests/test_check_shipped_content.py` (inject `{path: text}` maps; load-bearing regression pins = the memory-fence token NOT flagged AND a `claugentic-dev-harness:audit` command token NOT flagged).

**S4 — seed-migration (`docs/claugentic-_DECISIONS.md`, `_ROADMAP.md`, `skills/init/SKILL.md`).** Add two NEW pristine seed files (underscore-prefix convention, mirroring `standards/_TEMPLATE.md`) authored fresh as the adopter seed — NOT copies of the harness's filled ledgers: `_DECISIONS.md` = the "append newest at top; consult before re-litigating" header; `_ROADMAP.md` = a one-line intro + an empty Later/Ideas section, NO audit/product fences (they self-create on first audit/product run). They **ship** (not DEV_ONLY); the harness's own filled `DECISIONS.md`/`ROADMAP.md` stay **stripped**. `init` step 7 (★ plan-gate: the anchor is `skills/init/SKILL.md:661-671`) changes from "create a seed (prose-described)" to "copy `${SOURCE}/docs/claugentic-_X.md` → `docs/claugentic-X.md` if-absent, stripping the leading underscore" — **create-if-absent only, never refresh** (never-clobber a filled ledger). Seeds stay **OUT** of the managed-set table (a separate step-7 list) so the four-verdict REFRESH upsert can never match a filled ledger — make-invalid-states-unrepresentable. Backward-compat is automatic (create-if-absent + not-in-managed-set both protect existing adopters' filled ledgers). ★ plan-gate: the seed *output* (`docs/claugentic-DECISIONS.md` etc.) is already covered by solo-mode's `.git/info/exclude` `docs/claugentic-*` glob — do NOT add a redundant exclude line; the source `_X.md` files live in the plugin, never written to the adopter repo. Document the underscore-prefix = one-time-seed convention in step 7 + cross-reference the WORKFLOW three-kinds taxonomy.

## Architecture & holistic fit
- **Codebase fit.** S2/S3/S4 sit in the established **run-gate + characterization-test** pattern (`scripts/check_*.py` + `tests/test_*`); S3 honors the explicit "sibling gate, not an extension" SRP rule and the "self-anchored `_repo_root`" convention. S1 is a localized engine observability edit. No new layer, no new directory.
- **DRY.** S3 reuses `classify()` (one ship source-of-truth) and FS-derives the roster (no duplicated list); the content-grep + namespace deferrals collapse into ONE gate (one CI line, one DoD bullet). S4 makes the seed bytes a single source-of-truth file instead of re-invented prose.
- **Quality dimensions to uphold** (→ `docs/claugentic-standards/` modules): **reliability-resilience** (S2 pins the release machinery; S3 fail-loud on git/read failure — never false-green) · **maintainability-structure** (SRP sibling gate; seeds physically separated from the refresh path) · **api-and-contracts** (S1 honesty clause; S3 honest mechanical-vs-heuristic framing). The spec names the in-scope dimensions per slice.
- **Honesty (the #1 rule).** Only the architecture-tree gate is hook-enforced; S3 is a **run-gate** (CI + DoD), and its A.b pass is **heuristic/WARN** — the gate header, the WORKFLOW/doctor/INVARIANTS copy must say so plainly and must NOT claim the contract is now fully mechanically content-enforced.
- **Future-proofing.** FS-derived roster + `classify()` reuse mean S3 stays correct through future renames/strip-rule changes without edits. Keep the A.b marker-set + allow-list as documented, tunable seams.

## Affected files
**S1:** `engine/audit.js` (renderOnly branch).
**S2:** `tests/test_build_release.py` (new `TestApplyHookBypass`).
**S3:** `scripts/check_shipped_content.py` (NEW) · `tests/test_check_shipped_content.py` (NEW) · `.github/workflows/ci.yml` · `docs/claugentic-WORKFLOW.md` (DoD bullet) · `skills/doctor/SKILL.md` (gate list, 3-way band) · `scripts/build_release.py` (DEV_ONLY_FILES) · `tests/test_build_release.py` (strip assertion) · `docs/claugentic-ARCHITECTURE_TREE.md` (script entry) · `docs/claugentic-INVARIANTS.md` (Live-gate line).
**S4:** `docs/claugentic-_DECISIONS.md` (NEW) · `docs/claugentic-_ROADMAP.md` (NEW) · `skills/init/SKILL.md` (step 7, :661-671) · `docs/claugentic-ARCHITECTURE_TREE.md` (2 seed entries) · `docs/claugentic-WORKFLOW.md` (three-kinds refinement, optional) · `tests/test_build_release.py` (seed ship + filled-ledger strip assertions).
**Land:** `docs/claugentic-DECISIONS.md` (ONE batched 0028 entry — recording S3's unified-scanner + S4's seed convention; **condense-on-WARN**, it's at ~89% post-ruff-PR) · `docs/claugentic-ROADMAP.md` (remove the 4 done items; keep the 2 YAGNI) · delete this plan file.

## Research / grounding
- **Done:** 4 parallel design agents (`Plan` agentType) read each touchpoint and returned current-state (file:line), approach, affected-files, slice-scope, tests, risks, harness-impact, open-questions. All open questions resolved (see Flags) from the agents' recommendations; the plan-gate then caught the slash-command + dead-path issues (folded above, marked ★).
- **Verified facts:** the `integration` pytest marker already exists (`pyproject.toml`); `tests/conftest.py` puts `scripts/` on `sys.path` (so `import build_release` + `import check_shipped_content` work); zero stranded `claugentic-dev-harness:<old-role>` *agent* literals today (S3 green on adoption once the slash-command VALID-set fix is in); the `<!-- product-critic:… -->` memory-fence tokens are a real false-positive class (excluded by full-prefix keying); `DEV_ONLY_FILES` strips the harness's own DECISIONS/ROADMAP (so seeds must be fresh files); `git update-ref refs/remotes/origin/main HEAD` satisfies `_dropped_merges`.

## Risks & mitigations
- **S2 platform fragility (win32 + Git Bash):** hook executability/line-endings — write the hook with LF + an explicit `#!/bin/sh` shebang; use an absolute `core.hooksPath`. Forgetting `monkeypatch.chdir` would make `_apply`'s `_tracked_files()` hit the real repo — the fixture pins both `_repo_root` + chdir. The control commit must run with the hook armed (not the empty-hooksPath isolation pin) or it passes for the wrong reason.
- **S3 caveat heuristic (A.b):** false pos/neg → WARN-only + documented marker-set + allow-list seam; A.a + B stay hard (exact literals, near-zero ambiguity). Pass B's slash-command false-positive is the headline plan-gate catch — VALID = agents ∪ skills ∪ {update}. DRY-coupling to `build_release.classify()` is intended; pinned by `TestClassify`.
- **S4 never-clobber:** a future maintainer adding seeds to the managed-set table would turn create-if-absent into a clobbering REFRESH → mitigated by physical separation (step-7 list) + the DECISIONS line + the membership test. Backward-compat for existing adopters: automatic (create-if-absent + not-in-set).
- **DECISIONS budget:** at ~89% (the ruff PR #4 added a line); per-slice appends would re-trip WARN → batch ALL 0028 decisions into ONE entry at Land + condense then.
- **Tree gate:** S3/S4 add files under/around the globs → the affected-files include the ARCHITECTURE_TREE entries so each commit stays green.
- **Crash/persistence:** the plan was lost once as an uncommitted untracked file → commit each plan + slice promptly; don't leave durable artifacts uncommitted across long gaps.

## Test strategy
- **S1:** no new assertion practical (the top-level body isn't executed under the node helper-extraction harness); `renderOnlyResult` behavior already pinned at `audit.test.mjs:1158-1220` (unchanged → regression pin that output didn't move). Manual: a renderOnly run now emits one progress line.
- **S2:** the test IS the deliverable; mutation-check (drop `--no-verify` ⇒ the test must fail) validated mentally by the implementer.
- **S3:** hermetic `tests/test_check_shipped_content.py` (inject `{path:text}`): dangling-ref hit/miss, caveated-vs-uncaveated WARN, stranded `:plan-reviewer`/`:implementer-architect` flagged, valid `:lens-reviewer` clean, **the memory-fence token NOT flagged**, **a `:audit` slash-command token NOT flagged** (the two load-bearing pins), `valid_roster()`/VALID-set correctness, `main()` exit 0 clean / 1 dirty / fail-loud on git error. Plus the `is_dev_only('scripts/check_shipped_content.py')` strip pin.
- **S4:** extend `TestReleaseInitContract` — seeds SHIP (`is_dev_only` False), filled ledgers STRIP (True); optional seed-shape test (mirrors `test_product_spec_template.py`) pinning `_ROADMAP.md` has NO audit/product fence markers + `_DECISIONS.md` has the header.
- All slices: full suite (`node --test tests/workflows/*.test.mjs` + `python -m pytest -q`) + tree + version-sync + doc-budget + the new scanner + ruff/mypy green before each commit.

## Decomposition (slices)
- [x] **S1 — renderOnly log line.** ✅ LANDED 2026-06-25 (2 additive lines in `engine/audit.js`; honesty clause "no FIND/PRUNE/VERIFY this pass"; 372 node + 161 pytest + tree green; no behavior/schema change).
- [x] **S2 — `build_release._apply` regression test.** ✅ LANDED 2026-06-25 (synthesizer-gate PASS — mutation-check independently re-run: fails without `--no-verify`, passes with; control proves the hook armed; hermetic, no real-repo leakage; 163 pytest + 372 node green). `TestApplyHookBypass`. Additive test only; closes the ROADMAP build_release item.
- [ ] **S3 — unified shipped-content scanner gate.** NEW `check_shipped_content.py` (Pass A.a + B hard, A.b WARN; VALID = agents ∪ skills ∪ {update}) + hermetic tests + wiring (ci/WORKFLOW/doctor/build_release/tree/INVARIANTS). Green on current tree (purely additive). Closes ROADMAP content-grep + namespace items.
- [ ] **S4 — seed-migration.** NEW `_DECISIONS.md`/`_ROADMAP.md` seeds + `init` step-7 copy-and-rename (create-if-absent, out of managed-set) + tests + tree. Closes the ROADMAP seed item.

(No hard inter-slice dependencies; built sequentially in this worktree to avoid shared-file contention — `test_build_release.py`, `ARCHITECTURE_TREE.md` are touched by multiple slices.)

## Land (after S4)
ONE batched `DECISIONS.md` entry (unified-scanner choice + seed convention; condense-on-WARN) · prune the 4 done ROADMAP items (keep the 2 YAGNI) · update the INVARIANTS Live-gate line (done in S3) · delete this plan · Stage-9 harvest · then rebuild `release` from the new main + drop-check + hand the force-push to the user.

---

## Review  _(synthesizer-gate, plan-gate altitude, Stage 3 — 2026-06-25)_

**Verdict: CHANGES REQUIRED → all changes FOLDED (see ★ markers in Approach).** The four directions are architecturally sound and correctly sized; the YAGNI deferrals were correctly kept deferred (not relitigated). Two blockers + six should-fixes, all now incorporated:

**Blockers (folded):**
1. **S3 Pass B would HARD-fail on the current tree** — `claugentic-dev-harness:<name>` names both agent ids AND slash-commands (`:audit`/`:build`/`:doctor`/`:init`/`:product`/`:update`, ~29 legit tokens). Fix folded: VALID = agents ∪ skills ∪ `{update}`.
2. **S3 Pass A.a cited the dead `.claude/plans/TEMPLATE.md`** (0027 moved it) — fix folded: live forbidden set (`RELEASE_CHECKLIST.md` + harness-self script paths + specific plan-file refs), and `docs/claugentic-PLAN_TEMPLATE.md` is confirmed in Pass B's corpus.

**Should-fixes (folded):** (3) doctor wiring uses the 3-way `green/WARN/breach` band (A.b WARN); (4) S2 control commit must run with the hook ARMED (not the empty-hooksPath pin); (5) S2 `-C` rationale corrected (only `_tracked_files` lacks `-C`; chdir still required); (6) S4 init step-7 anchor is `:661-671`; (7) S4 seed output already covered by the solo-mode exclude (no redundant line); (8) honesty — shipped copy must not imply full mechanical content-enforcement.

**Confirmed sound:** sizing/sequencing (sequential build justified by shared-file contention); S1 cannot break the SELECT terminal-signal invariant; S3 importing `build_release` is DRY-correct and permitted (the copied-gate-can't-import rule scopes only to the adopter gate); S4 never-clobber + ship/strip + no-glob-collision; `update-ref` satisfies the base guard; DECISIONS batched-at-Land + condense is genuinely required at ~89%.
