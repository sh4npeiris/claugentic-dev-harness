# 0044 — The leanness pass, eval-gated

- **Status:** Slices 1a + 1b + 2a + 2b + 3 DONE — Slice 4 (skills cut) next, then 5, then 0.6.0 release
- **Resumable from:** Slice 4: skills cut, full appetite (groundwork `groundwork/skills-merged.md` — safe-cut band ~34–36 KB / 21 regions, zero contract lost, ~14 KB in init; the bigger structural breaks (init split −45K context-load, wrapper de-dup) are a separate risk class to surface for the user's call). **S3 LANDED** (roster 91,314 → 85,583 B, −6.3%, conservative-by-design; 3 direct-candidate probes all passed — plan-gate CHANGES_REQUIRED, detection found all seeds, refuter Verified+Refuted; verify-verdict altitude eval-blind, cut narration-only). Standards deep cut also landed (−30.8%). Then **Slice 5** (WORKFLOW cap ratchet) → **release 0.6.0** (user tags).
- **Blockers:** none
- **Flags:** drift-mode cadence settled as on-demand-only (reversible) · A-vs-A calibration settled as skip (reversible; pull-in condition recorded) · H pinned at 13 with ΔF ≥ 2, together, in `BUILD_BASELINE.md` · **`/simplify` deliberately skipped on 1a** — the panel's yagni + maintainability lenses performed the simplification review with measurements; an auto-apply pass over a calibration-locked instrument risks silent un-calibration · **the Stage-5 size estimate was superseded** (~30–40 KB approved → 249,558 B measured, all release-stripped; surfaced to the user at Land, recorded in `BUILD_ENTRIES.md` #1) · `/code-review` discharged via its completed Angle A (all six candidates dispositioned across rounds 1–3); its cross-file tracer died unreported
- **Disposition at close:** per `docs/claugentic-WORKFLOW.md` → Plan file lifecycle.
- **Roadmap item:** none (owner-directed arc, 2026-08-20 handover prompt; the three user answers below are its charter)
- **References:** `docs/claugentic-ARCHITECTURE_TREE.md` · `docs/claugentic-DECISIONS.md` · plan 0041 (thinning precedent, git history) · `eval/BASELINE.md`

## The user's binding answers (2026-08-20)

1. **v0.5.4 tagged and published first** — DONE (Release live 17:28:35Z, `release`=`08faae2`). The pass runs on a released base.
2. **Skills cut = FULL APPETITE** — breaking, adopter-visible changes allowed; version accordingly (next release **0.6.0**).
3. **BUILD-path eval FIRST** — it must exist and have a baseline before the standards second pass; the cut is gated on its result.
4. (later that day) **NO EXTERNAL USERS EXIST YET** — the plugin is unadvertised; compatibility is NOT a constraint. Optimize for the best design, publish when done; the single-source contract map still binds for internal coherence (engine↔skill↔test wiring), never for migration. 0.6.0 versioning kept as hygiene.

## Problem

The harness is 0.74 MB of shipped prose+code after the −28% thinning. The names-only ablation (2026-08-20, recorded in the session handover — **not an `eval/BASELINE.md` entry**; the ledger's 2026-08-20 entry is the separate post-thinning run, 9/10 with the 130K catalog) measured audit recall **10/10 with the catalog reduced to dimension NAMES** — one run each, so evidence, not proof, that the catalog's *audit* product is **boundaries + enumeration, not knowledge**. But that measured **detection only**. The catalog is also the implementer's authoring checklist (`.claude/agents/implementer.md` — "the catalog is an authoring checklist, not only a review checklist"), and **output quality — the owner's actual goal — is asserted, not measured**: five of six skills have no eval at all. Meanwhile the corpus carries measurable slack: agents doing the same job class span 8× (yagni-sentinel 3,066 B vs synthesizer-gate 24,505 B, both untested), skills are 200 KB (init 80.5 KB), standards 130 KB against a ~60 KB target.

North star (falsifiable, `docs/claugentic-PRODUCT_SPEC.md` → Why this exists): every byte is **team-function or compensation**; compensations are deleted, not tuned. As models improve the harness gets thinner.

## Goals / Non-goals

- **Goal 1:** a build-path eval measuring the implement path's output-quality sensitivity to the standards catalog — bounded cost, honest about small-N noise, its own instrument calibration-verified before any verdict.
- **Goal 2:** standards second pass 130K → ~60K, landing only on the eval's **"no regression detected at this K"** verdict per the pre-registered rule — keeping dimension enumeration, un-inferable thresholds, incidents, `[D]`/`[J]` tags, output contracts.
- **Goal 3:** roster bytes cut (synthesizer-gate 24.5K is the prime target), gated on one audit-eval run over the candidate roster + one plan-gate probe; every posture, mode list and output contract kept.
- **Goal 4:** skills cut at full appetite (breaking allowed → 0.6.0) — contracts inventoried before cutting; narration and compensation deleted.
- **Goal 5:** WORKFLOW cap ratcheted down to the post-arc size + orphaned-pointer cleanup (no free-standing re-cut — it was thinned 2026-08-19; no new evidence implicates it).
- **Non-goal:** measuring all six skills' output quality — the eval targets the implement path; extensions are one roadmap line, not built (YAGNI).
- **Non-goal:** any new harness feature, and any re-proposal of a `docs/claugentic-PRODUCT_SPEC.md` → Deliberate non-goal (the bar is evidence, never argument).
- **Non-goal:** cutting the bones (plan → review → spec → approve → implement → verify → land) or the FRAME ceremony — owner-valued structure, already effort-dialled.
- **Non-goal (hard rule):** compressing away any mechanical-vs-model-upheld distinction — that inverts the product's one invariant.

## Approach

Eval-first sequencing per the user's answers. Slice 1a builds the measurement instrument (design synthesized by a 3-design/3-judge/1-synthesis panel, then hardened by the 2b advisory round and the Stage-3 gate — all folded in below) and calibration-verifies it; Slice 1b runs a K=3 arm-A **shakedown** against the current 130K catalog — it validates the instrument end-to-end with real builders, records the first spread/flap noise yardstick, and discharges binding answer 3's "have a baseline", but is **explicitly demoted: never a decision input** (Stage-3 R1). The decision comparison happens entirely inside Slice 2b: **all six runs — arm A re-derived + arm B — one sitting, one base commit, interleaved**, so Δ never confounds the cut with tier drift or intervening commits; the +3 re-derived arm-A builds (~50% more comparison spend) are the stated price of that control. Slice 2a produces the candidate-cut catalog on a branch; 2b measures and lands only on a "no regression detected at this K" verdict per the pre-registered rule. Verification honesty per slice: Slices 2 and 3 have existing instruments (the audit eval; plus Slice 1's build eval for Slice 2); Slice 4's verification is a per-skill contract inventory written before cutting (**model-upheld**) plus the hygiene gates incl. `claude plugin validate --strict`; Slice 5's is the byte-pinned budget test; anchor integrity is a **named manual sweep per cut slice, assisted by a resolver script** (built in Slice 2, reused after — the script catches the parseable citation forms; the sweep judgment stays model-upheld).

Alternatives rejected: cutting standards on audit-recall evidence alone (over-generalises from the detection path — the handover's named trap); sharding WORKFLOW instead of thinning (declined 2026-08-19, recorded); a full six-skill eval suite before any cut (YAGNI — the implement path is the one the standards cut touches); a free-standing WORKFLOW re-cut (2b: no evidence implicates a file cut yesterday — a ROADMAP watch line instead).

## Architecture & holistic fit

- **Codebase fit** — the eval mirrors the existing `eval/fixture-defects/` pattern: fixture inside the scoped path, answer key outside it (no-peeking, canary, integrity + containment tests in `tests/`), procedure and entries under `eval/`. **Day-one split, avoiding the sibling's recorded killable-half residual:** `eval/BUILD_BASELINE.md` carries procedure + pre-registered rule + standing rules (no answers); `eval/BUILD_ENTRIES.md` carries the append-only answer-bearing entries. Shared chassis mechanics (worktree hygiene, canary handling, filename-residual precedent, human-stamps-the-date) are **pointed at in `eval/BASELINE.md`, never copied**. Cuts are edits to shipped surfaces governed by the release/init contract and the shipped-content scan; nothing new is load-bearing at runtime.
- **Product fit** — PS-5 (honest disclosure) governs every copy change; the pass serves the north star (thinner as models improve) with the owner's addractive bar intact.
- **Felt/visual craft bar** — the one user-facing surface is skill/README copy an adopter reads; the bar is the existing voice: dense, honest, no over-claim. No new UI.
- **Quality dimensions to uphold** — `docs-traceability` (anchor integrity on every section cut), `testing` (calibration-verified instrument; mutation-verified pins), `maintainability-structure` (single source of truth for contracts the cuts touch), `product-ux` (skill copy). Spec refines per slice.
- **Future-proofing** — the build eval is re-runnable for future cuts and model upgrades; an advisory K=1 drift variant is *derivable from the procedure* and noted in one line (advisory only — it can trigger a full K=3 comparison, never issue a pass/block verdict), not built. Cut decisions are recorded per shard so they are never re-litigated.

## Affected files

- `eval/fixture-build/` (new) · `eval/BUILD_BASELINE.md` + `eval/BUILD_ENTRIES.md` (new) · `tests/` integrity + containment extensions · `docs/claugentic-decisions/evals.md` (new shard) + its index line. (Slice 1a; 1b appends an entry)
- `docs/claugentic-standards/*.md` + `scripts/` anchor-sweep resolver (dev-only). (Slice 2a; 2b ratchets the standards glob cap + pin)
- `.claude/agents/*.md`. (Slice 3)
- `skills/*/SKILL.md`. (Slice 4)
- `docs/claugentic-WORKFLOW.md` pointer cleanup + `.claude/claugentic-doc-budgets.json` + `tests/test_check_doc_budgets.py` (cap pin, same commit). (Slice 5)
- Throughout: `docs/claugentic-ARCHITECTURE_TREE.md`, `docs/claugentic-decisions/` shards, `CHANGELOG.md` (0.6.0 section), `docs/claugentic-ROADMAP.md` (the deferred watch lines land with their deferring slice).

## Research / grounding

- **Files reviewed:** `eval/BASELINE.md` (whole procedure + newest 3 entries) · `eval/fixture-defects/SEED_MANIFEST.md` · `.claude/agents/implementer.md` · `docs/claugentic-standards/README.md` · `docs/claugentic-WORKFLOW.md` (whole) · `docs/claugentic-PRODUCT_SPEC.md` (whole) · `docs/RELEASE_CHECKLIST.md` · README mission ¶ · byte inventories (skills 199,932 B · agents 90,126 B · standards 130,019 B · engine 234,901 B).
- **2b advisory round (2026-08-20, prose-convened per WORKFLOW):** lens-reviewer(testing) — 9 findings, all adopted · lens-reviewer(docs-traceability) — 5 findings, all adopted (grounded: the repo's inbound citations live in Python docstrings, wrap across lines, use two arrow glyphs; `tests/test_eval_key_containment.py` imports from a file the deletion set removes) · yagni-sentinel — OVER-BUILT at the edges: Slice 5 shrunk, Slice 3 single-run, drift mode and A-vs-A dropped to lines, DRY between baseline files · honesty-reviewer — OVERCLAIMS: 1 blocking (the evidence mis-citation, fixed above) + 5 should-fix, all reworded in place. Kept-after-interrogation: K=3, blind grading, fault-injection fakes, ten traps two-per-module, the deletion set, the no-coaching lint.
- **Findings:** the audit eval's chassis is directly reusable; `eval/fixture-app/` is the runtime-QA fixture, not a build fixture; plan number 0043 is spent.

## Risks & mitigations

- **The instrument itself is unverified (green-pinning-nothing relocated into the eval)** → the **calibration pair** (below) is a hard Slice-1a acceptance criterion: a clean reference implementation must score **H/H** held-out + 10/10 AVOIDED (H is the authoring-time-pinned held-out count), and per-trap fell-in mutants must each flip exactly their probe — run before any arm, recorded in the first `BUILD_ENTRIES.md` entry. This also discharges every `[D]` tag by resolution, not vocabulary.
- **Eval verdict is noise, not signal** → pre-registered thresholds fixed before any run; spread-beside-Δ; an ambiguous verdict defers the cut (recorded), never forces it.
- **Judge circularity** → outcome-anchored checks; harm-line admissibility; blind `[J]` grading; standing no-same-release-tuning rule. Honest residual stated, never smoothed.
- **Anchor breakage from section cuts** → the per-slice sweep as specified in Slice 2 (corpus = `git ls-files` all extensions incl. frontmatter `description:` lines; both arrow glyphs `→` and `->`; quoted-heading-no-arrow forms; wrap-normalized before matching; the pattern validated against a known positive — `docs/RELEASE_CHECKLIST.md:7` and one `->` docstring — before any zero is trusted), assisted by the resolver script.
- **Full-appetite skills cut silently breaks an adopter contract** → per-skill contract inventory (verdicts, engine args, fence markers, `renderOnly` seam, upsert semantics) before cutting; every intentional break in the 0.6.0 CHANGELOG; `claude plugin validate --strict` **in Slice 4's own verify** (the v0.4.0–v0.5.1 unparseable-frontmatter incident is this exact class), not only at release.
- **A new pin that pins nothing** → mutation-verify every new pin, including the eval's probes (the calibration mutants ARE that verification) and the no-coaching lint (insert one denied token, confirm red).
- **Cap edits drift from their test pins** → cap change + `tests/test_check_doc_budgets.py` pin in the same commit, byte-exact.
- **Windows byte traps** → LF via `write_bytes`; ASCII-only for `engine/*.js`; re-measure budgeted files after write.

## Test strategy

Per slice: the 8 gates run as **individual commands, exit codes read directly**. Slice 1a adds the fixture integrity test (pattern: `tests/test_eval_manifest.py`, plus the [D]-resolution and no-coaching assertions below), extends the containment scan to the new fixture's vocabulary, and calibration-verifies the whole instrument. Slice 1b shakes the instrument down end-to-end with real builders. Slice 2b runs the six-run build-eval comparison AND the audit eval on the candidate catalog. Slice 3 runs the audit eval once on the candidate roster + one plan-gate probe. Slice 4 runs `claude plugin validate --strict`. The two cap ratchets (2b standards glob, 5 WORKFLOW) update their byte pins in the same commit.

## Decomposition (slices)

- [x] **Slice 1a — the instrument, calibration-verified.** LANDED 2026-08-20 after 4 review rounds (2 full panels + targeted confirm + diff-confirm; every probe defect found is fixed and pinned by a mutant that flips). Calibration: reference 13/13 + 10/10 AVOIDED; **15 mutants over 10 traps, zero cross-trips**; H=13, ΔF ≥ 2 pinned together. *(Measured cost 249,558 B repo-side — supersedes the approved ~30–40 KB estimate, 6–8× over; all release-stripped, zero payload impact; recorded in `BUILD_ENTRIES.md` #1 and surfaced to the user. The arc's retirement ledger is slices 2–5's byte deltas.)*
- [x] **Slice 1b — the shakedown arm.** DONE 2026-08-20 (`BUILD_ENTRIES.md` entry 2): K=3 arm A at `2ce66df`, three Opus builders + Fable blind grader (cross-family, self-reported both sides). **M(A)=10 · F=13/13 ×3 · flap=0 · spread=0 · S: one drift (A1's optional kwarg) · contamination fully clean** (no canary, no id, no answer-key filename in ~3M transcript chars). Demoted by design — never a decision input; binding answer 3's "have a baseline" discharged. Saturation risk recorded before any arm-B number exists.
- [x] **Slice 2a — the candidate catalog cut.** DONE 2026-08-20. Target ≤65K REFUTED twice by measured floor; user amended the spec to the deep cut (per-check-bullet rule dropped). Delivered **132,203 → 91,478 B (−30.8%)** on branch `0044-2a-standards-cut`; anchor-resolver `scripts/check_standards_anchors.py` built + self-tested (24 tests) + mutation-verified; every hard keep-rule held (124 headings text-identical, 23 incidents, thresholds, 4 registers, the [D]/[J] discipline; hedged-tag brackets split into explicit clauses). −10% fallback preserved at `dec7059`.
- [x] **Slice 2b — measure + land.** DONE 2026-08-20 (`eval/BUILD_ENTRIES.md` entry 3). Six-run one-sitting comparison: **Δ=0, ΔF=0, floors clear → "no regression detected at this K"**; independent detection eval **10/10 recall** on the 91K catalog. Build exam SATURATED (both arms 10/10) → re-sharpen allowance user-DEFERRED to ROADMAP; cut landed on rule + detection. Standards glob cap ratcheted **60,000 → 20,000** (largest module 15,216 B ≈ 76%), byte pin same commit. One run discarded + re-run on a caught contamination hit (root-caused: shared-scratchpad leak, 566 files evacuated).
- [x] **Slice 3 — roster cut.** DONE 2026-08-21 (merge on main). **91,314 → 85,583 B (−6.3%)**, deliberately conservative: the implementer refused to cut synthesizer-gate's eval-blind verify-verdict altitude (63%) on faith; every mode name, caller contract, output-contract string diff-verified preserved; de-correlation prohibition untouched. **Gate: 3 direct-candidate probes** (the audit engine's sub-agents may resolve to the installed roster, so candidate bytes were probed directly) — plan-gate → CHANGES_REQUIRED on every planted flaw; audit-scope → all seeded security+reliability defects surfaced with tags; refuter → Verified real SQLi + Refuted plausible-wrong leak by measurement. Honest limit recorded: verify-verdict altitude eval-blind, cut narration-only.
- [x] **Slice 4 — skills cut (safe-cut band).** DONE 2026-08-21. **199,932 → 190,336 B (−4.80%)**, all 18 danger-list contracts grep-confirmed surviving, no test pin altered, tag-glyph drift fixed (ASCII), `plugin validate --strict` green. Conservative by the safe-band discipline; the 15–18% estimate lives in the deferred structural band (init split → 0.7.0). Criterion refuted, slice complete.
- [x] **Slice 5 — WORKFLOW verification.** DONE 2026-08-21. **No cap ratchet and no orphan cleanup were warranted:** slices 2–4 removed no section WORKFLOW cites (standards headings text-identical; roster/skills added pointers, removed none) — the standards anchor gate reports every inbound citation resolves; WORKFLOW didn't shrink this arc (deliberately not re-cut per the 2b yagni finding) and sits at 82% of its 77,500 cap, in-band. The one finding — the anchor resolver's 11 bold-landmark false-positives on WORKFLOW — is a ROADMAP item (extend it to resolve bold landmarks; every cited landmark verified present).

## Slice 1 design — "Trap-Gauntlet" build-path eval (panel synthesis + 2b hardening)

_Chassis from a 3-design / 3-judge / 1-synthesis panel (unanimous, 27–28/30); hardened by the 2b round. Settled — the rejected alternatives are listed at the end so they are not re-proposed._

**Home:** `eval/fixture-build/` + `eval/BUILD_BASELINE.md` (procedure) + `eval/BUILD_ENTRIES.md` (entries). LF, ASCII. Model-upheld procedure — never a CI gate. One question per run: *did swapping catalog variant A for variant B change what the implement path actually ships?*

### Fixture artifacts

1. **`TASK_SPEC.md`** — builder-visible, byte-identical in both arms. Domain: **"spendlog"**, a small expense tracker in stdlib Python + sqlite3 — a fresh domain (not the audit fixture's task tracker) so fixture shape can't prime a builder. Quality-blind PM-voiced requirements R1–R9 (CSV budget import creating budget+expense rows in one call · expense listing with budget names · merchant search from a raw query-string term · operator-configured service token check · query-param-driven monthly HTML report · category validation at add AND grouping in report · dashboard running total · over-budget webhook notify where the endpoint "may be slow or down" — the one reliability sentence allowed · tests covering the import write path and the renderer). It **pins the public surface only** — files `out/{db,importer,handlers,report,notify,test_spendlog}.py`, function signatures, the two-table sqlite schema — so held-out checks can import any faithful implementation; internal structure is never pinned (the MAINT traps live in function bodies and cross-file duplication, where pins dictate nothing). Names the five deep standards modules as in-scope dimensions — identical text in both arms; only module CONTENT varies.
2. **`plan-slice.md`** — a frozen, template-shaped approved-plan slice pointing at TASK_SPEC, copied into each arm worktree's `.claude/plans/` so the **shipped `implementer.md` contract is exercised verbatim** ("the plan + spec live in a `.claude/plans/` file"). **It carries the run's scoping in-contract (Stage-3 R5): writes scoped to `out/`, architecture tree out of scope for this slice** — scoping delivered through the contract's own channel (scoping, not coaching), so "no wrapper-prompt departure" holds. **Builder-visible, so inside the no-coaching lint's corpus.** *(Enabling fact, noted so a future change doesn't silently break the eval: `out/*.py` is unwatched by the tree gate — `INCLUDE_GLOBS` is `scripts/**/*.py` + `engine/**/*.js` today; widening it would put the tree check in tension with the frozen plan slice.)*
3. **`TRAP_MANIFEST.md`** — the hidden answer key: ten traps, exactly two per deep module. **The trap CLASSES predate the cut question** (they are the audit eval's frozen seed classes); **the concrete instantiations are authored now** — neutrality is upheld by the harm-line admissibility rule, blind grading, and standing rule (a), not by provenance alone. One integrity-tested table `| id | module | spec req | harm line | check | tag |`; the **harm line** is the admissibility rule (the user-visible bug a person would file — a trap defensible only as "the catalog says so" is inadmissible). Eight `[D]`, at most two `[J]` (both MAINT — kept deliberately: MAINT is the ablation's one post-cut miss, precisely the prose-sensitive dimension a build eval must not drop). Traps: SQL interpolation in search · hardcoded token (**probe: reconfigure the operator token, assert the check follows config, not a baked literal**) · assert-nothing test (mutation probe: gut the write path, arm's own tests stay green) · self-patching test (**AST scan + `mutation_probe.py`**) · one function parsing+querying+rendering (`[J]`, rule stated in the row) · category set defined twice (`[D]` grep, `[J]` fallback) · partial write on mid-import failure (fault injection) · N+1 in the listing (sqlite `trace_callback` query counter) · DB error swallowed as success-shaped 0 (corrupt-DB probe) · **webhook hazard probe: count connection attempts on a never-responding fake and require the call to return/raise within a generous bound (60s), FELL-IN only on "still blocked / still retrying with no backoff at the bound"** — never a tight wall-clock that punishes a correct 10s timeout. Own canary: `the seeded-trap crimson-giraffe canary has leaked into the run`.
4. **`checks/`** — the measurement instrument, **catalog-free by construction: no reference to the catalog path (grep-checked), and the integrity test asserts `checks/` reads no file outside `eval/fixture-build/` and the target worktree's `out/`** — the stronger it-would-compute-identical-results claim rests on that read-scope assertion (static scan of file-access calls; its limits stated in the test), not on the grep. Contents: `test_heldout.py` (H ≈ 12 behavioral happy-path pytest tests — proves a WORKING artifact; no style assertions; **H and the ΔF threshold — default ΔF ≥ 2 — pinned together** in BUILD_BASELINE.md by the Slice-1a implementer, re-pinnable only together, before any run, recorded) · `fakes.py` (blocking `urlopen`, raise-on-Nth-write connection, corrupt-DB handle, injection payloads) · `run_sweep.py` (held-out tests + spec-compliance + ten trap probes against a worktree's `out/`; **computes facts, never scores**; builds the blind `[J]` pack — the sitting's `out/` dirs (three for a shakedown, six for a decision comparison) shuffled under opaque names, comment-line-only redaction, every redaction logged, mapping sealed) · `mutation_probe.py` (the vacuous-test detector). Binary-safe LF writes throughout.
5. **The calibration pair (2b, the instrument's own verification — a hard acceptance criterion):** `eval/fixture-build/calibration/` holds (a) a clean reference spendlog that must score H/H held-out and 10/10 AVOIDED, and (b) ten per-trap fell-in mutants, each of which must flip **exactly its own probe** to FELL-IN. Run before the baseline arm; recorded as the first `BUILD_ENTRIES.md` entry. Outside builder scope (in the deletion set). This is what makes every `[D]` tag resolution-verified, not vocabulary-verified.
6. **`tests/test_eval_trap_manifest.py`** — fixture integrity on main: ten rows two-per-module; **every `[D]` row's check cell resolves to an importable callable in `checks/` (import + `getattr`, fail loud on miss)**; every `[J]` states its rule; **the no-coaching lint runs over every builder-visible artifact (TASK_SPEC.md AND plan-slice.md)** with a per-trap → denylist-entry mapping assertion (denylist extended to cover all ten traps — incl. cohesion/separate/layer and swallow/bare-except vocabulary — or a deliberate residual recorded in the manifest row); the lint itself mutation-verified (insert one denied token, confirm red). Allowed exception: the one "slow or down" sentence.
7. **Containment (2b):** extend the `tests/test_eval_key_containment.py` scan with a second vocabulary derived from `TRAP_MANIFEST.md` (allowlisting `eval/BUILD_BASELINE.md` + `eval/BUILD_ENTRIES.md` with justification), and extend the architecture-tree do-not-revert note + its pin to cover the `fixture-build/` entries (the 0041 S11 incident class: tree entries paraphrasing answers).
8. **`eval/BUILD_BASELINE.md`** — procedure + pre-registered decision rule + standing rules, **no answers**; **points at `eval/BASELINE.md` for every shared chassis mechanic** (scratch-worktree hygiene, canary handling, the filename-only-disclosure precedent, human-stamps-the-date) instead of carrying a second copy. The small-N caveat is stated **once** here; entries cite it. Standing rules: **(a) after its initial landing, the trap manifest never changes in a release that cuts the catalog** (no cut author tunes the exam they sit) — **with one carve-out (Stage-3 R2): the recorded both-arms-saturation calibration allowance may fire even in a cut release; its trigger is symmetric so it cannot favor the cut, and a re-sharpened manifest voids all prior runs — both arms re-run against it**; **(b) the decision rule is pre-registered** — thresholds fixed before any run (H and ΔF pinned together at authoring time). One-line extension note: *"a K=1 single-arm advisory run is derivable from this procedure — advisory only: it can trigger a full K=3 comparison, never issue a pass/block verdict."*
9. **`eval/BUILD_ENTRIES.md`** — append-only, newest first, human-stamped; answer-bearing (per-run trap tables, mappings). The day-one procedure/entries split avoids the killable-half residual recorded against `eval/BASELINE.md`.

### Arms & materialization

Arm A = the base commit's `docs/claugentic-standards/` (130,019 B today). Arm B = any variant (the ~60K candidate, names-only, absent) as a wholesale directory swap — the names-only ablation's own mechanism, so a modified catalog is supported by construction. **K=3 runs per arm**, one scratch worktree per run at the sitting's one base commit (Slice 1b: three arm-A worktrees; Slice 2b: all six); copy `plan-slice.md` → `.claude/plans/`; apply the **deletion set — derived as a class, not a hand list: the named answer-side artifacts** (`TRAP_MANIFEST.md` · `checks/` · `calibration/` · `eval/BASELINE.md` · `eval/BUILD_BASELINE.md` · `eval/BUILD_ENTRIES.md` · `eval/fixture-defects/` wholesale — SEED_MANIFEST is a near-answer-key: the trap classes ARE the seed classes) **plus every test whose corpus or imports NAME a deleted file, found by grep at run time — the grep is the authority; any printed list is illustrative-at-print-time** (Stage-3 R4: e.g. `tests/test_build_release.py` names `eval/BASELINE.md` as a string literal and is swept in by the class even though it reads no file). Deletions verified with `ls`; **post-deletion `pytest` must be green before spawn** (satisfiable only because the deletion set is class-derived). **The post-deletion gate is pytest-only by design:** `tests/workflows/*.mjs` sits outside it, and `qa.test.mjs` touches only `eval/fixture-app/`, which is not in the deletion set. **Held constant:** the sitting's base commit, spec, plan slice, unmodified `implementer.md` (its "self-apply the Auditor checks" step IS the treatment), spawn prompt, session tier — **a decision comparison is always all six runs in one sitting, interleaved A1 B1 A2 B2 A3 B3 so a mid-session tier shift is spread across both arms rather than confounded with one** (a cross-sitting or cross-commit comparison is invalid by this design — the R1 rule). **The only difference between arms is the bytes inside `docs/claugentic-standards/`.**

### Metrics & the pre-registered decision rule

- **Functional pass rate** `F(X,k)` over the held-out tests — **floor rule leads the verdict:** mean `F < 0.8` → that arm "did not reliably produce a working artifact," stated FIRST, before any trap arithmetic. **Completed rule (2b): candidate arm fails the floor while baseline passes → BLOCK; both arms fail → instrument invalid, cut deferred, calibration allowance considered.**
- **Spec compliance** `S(X,k)` scored separately — an interface-naming drift can never masquerade as a quality delta. UNCHECKABLE (probe can't bind) counts as FELL-IN with the raw evidence printed; human may overrule with a recorded judgment, never silently.
- **Per-trap 2-of-3 majority** → arm score `M(X)` ∈ 0..10; **decision figures `Δ = M(A) − M(B)`** and `ΔF` (held-out test counts).
- **BLOCK the cut iff `Δ ≥ 2` OR `ΔF ≥ 2`** (the ΔF threshold as pinned together with H at authoring time — ≥2 is the default; mirrors the audit eval's ≥2-seed rule) **or the floor clause above fires.** `Δ ∈ {0,1}` and floors clear → **"no regression detected at this K"** — the cut may proceed; **never phrased as equivalence shown, and never predicated of the cut itself** ("non-regressing cut" is the forbidden reading).
- **Recorded, not gated:** `flap(X)` (traps not unanimous within an arm) · **intra-arm `spread(X)` printed beside Δ** — a delta inside the measured spread is called noise by the entry itself · **catalog-read attribution** per transcript ("catalog unread" is never recorded as "catalog unneeded").
- The procedure carries the caveat once, entries cite it: *"K=3 per arm is a tripwire, not a proof: it can catch a gross regression (≥2 traps) but cannot rule out a subtle one… A null result means no regression was detected at this K — never that the cut is safe in general."*

### Procedure (condensed; BUILD_BASELINE.md carries the full numbered form)

Fix + record arm identities → **run the calibration pair; both halves must pass** → build the sitting's worktrees (three for a shakedown, six for a decision comparison), swap B's catalog, apply + verify the class-derived deletion set, post-deletion pytest green → spawn fresh clean-context `implementer` per run (scoping carried by `plan-slice.md`, no commits, transcripts + `RUNNING AS` retained), interleaved one sitting → `run_sweep.py` per worktree from the MAIN checkout → blind `[J]` grading (one grader, shuffled sealed pack, file:line citations required; unsupported citation = discounted) → grade + attribution → **contamination sweep**: grep all transcripts+outputs for crimson-giraffe, purple-elephant, TRAP_MANIFEST content lines AND SEED_MANIFEST content lines (filename-only sightings disclosed per the v0.5.3 precedent; a content hit discards that run). **Honest limit, stated in the procedure: the sweep catches verbatim leakage only — the main-checkout read path remains open to a builder, and paraphrase contamination is undetectable at this layer.** → floor rule → decision rule → append the entry to `BUILD_ENTRIES.md` → remove worktrees, `ROADMAP.md` byte-untouched. **One calibration allowance** (mirrors the audit eval): if the first outing saturates in either direction for BOTH arms, the spec/traps (never the catalog, never the implementer) may be re-sharpened once, recorded.

### Cost & circularity

~**8 agents / ~1.5–2.5M tokens** per full two-arm comparison (6 implementers — the only heavy spend — + 1 blind grader + orchestrator; the sweep and calibration are scripts) vs the audit eval's ~31 agents. The arc pays one extra K=3 shakedown arm in Slice 1b (+3 builders + a grader pass) — the stated price of keeping every decision comparison same-sitting/same-commit (R1(i)). Arm A's builders read ~2× arm B's catalog bytes — noted so token counts are never misread as an efficiency finding. Circularity defense, five layers: outcome-anchored checks (every failure is user-visible harm) · the harm-line admissibility rule · frozen trap-class provenance + the no-same-release-tuning rule · blind `[J]` grading · symmetric held-out tests. **Honest residual, stated never smoothed:** traps and catalog share subject matter; the defense is that checks measure the FAILURE, not the vocabulary — a cut that keeps the teeth and sheds the prose can score 10/10.

### Settled — do not re-propose

By the design panel: audit-as-instrument corroboration layer (~20–40 agents, zero gate power — one-line extension only) · 20-agent blind pairwise judge panel · K=1 with immediate block (decision-on-noise both directions given the coin-flip record of the historically flakiest seed (its id lives only in `eval/BASELINE.md`, per the containment pin)) · convergent-deficit rule (structural false-PASS bias) · dropping MAINT traps · task-tracker domain reuse · wrapper-prompt spec delivery · whole-run sealed arm blinding · auto-editing code lines in redaction (comment lines only; code flagged for human review). By the 2b round: a standing K=1 drift MODE built into the procedure (reduced to the advisory one-line extension above) · a separate A-vs-A calibration run (skip — the K=3 intra-arm spread is the noise yardstick; **pull-in condition, recorded: a real Δ landing ambiguously inside the measured spread**) · a free-standing Slice-5 WORKFLOW re-cut · a second full copy of the shared eval mechanics. **These land in a NEW `docs/claugentic-decisions/evals.md` shard at Slice-1a close** (the index's filing rule: no fit → create one, growth is horizontal — `audit.md` has only 2,327 B usable to its WARN, and `deterministic-gates.md` sits 90 B under its own; never file there). Shape: **one dense entry per decision cluster with clauses, not N bulleted entries**; the index gains its one-line route in the same commit; the shard is auto-covered by the existing `decisions/*.md` per-file cap. (The plan file is deleted at close; a plan-resident decision is no record — 0041 S10b.)

---

## Review  _(filled by synthesizer-gate in its plan-gate altitude, Stage 3)_

_Round 1, 2026-08-20. RUNNING AS: Fable 5 — same family as the planner; this is a clean-context pass, a reduction of rubber-stamping risk, never model-independence. Scope honored: the panel/2b settled list was not re-litigated; findings below are what the merge introduced or the rounds missed. Re-measured this round (all exact): standards 130,019 B · agents 90,126 B · synthesizer-gate 24,505 B · yagni 3,066 B · skills 199,932 B · init 80,556 B · WORKFLOW 62,780 B / cap 77,500 · trap classes = the ten frozen seed classes 1:1 (`eval/fixture-defects/SEED_MANIFEST.md:25-34`, incl. N+1 = data-and-persistence, so two-per-module is exact) · `implementer.md` quotes verbatim · the killable-half residual is real (`tests/test_eval_key_containment.py:93`) · `eval/` is release-stripped (`scripts/build_release.py:157`) · v0.5.4 is live (tag → `431e791`, `origin/release` = `08faae2`) · plan 0043 spent (`c17a402`). Binding answers: **1 honored (verified live) · 2 honored (Slice 4 + 0.6.0 CHANGELOG) · 3 honored in sequencing, modulo R1 below.**_

- **Verdict:** CHANGES REQUIRED

- **Required changes:**
  1. **Reconcile Slice 1's baseline arm with the procedure's own confound controls — the merge's biggest contradiction.** Slice 1 runs the K=3 arm A alone; §Arms holds constant "RC commit … session tier — all six runs one sitting, interleaved A1 B1 A2 B2 A3 B3". A Slice-1 arm A compared against a Slice-2 arm B is a cross-**sitting**, cross-**commit** comparison the design itself forbids: Δ confounds the catalog cut with tier/model drift AND with every commit landed between (Slice 1 itself lands on main before Slice 2 branches). Pick one, in text: **(i)** the decision comparison re-derives arm A inside Slice 2's one-sitting six-run block; Slice 1's arm-A outing is kept (it is what discharges binding answer 3's "have a baseline") but explicitly demoted to shakedown / instrument-validation, never a decision input — cost +3 builder runs (~50% more comparison spend), state it; or **(ii)** Slice 1's baseline is decision-bearing and the one-sitting/one-commit control is downgraded to "same base commit + tier-matched via `RUNNING AS`, else re-run" — a weaker control `BUILD_BASELINE.md` must state, never silently carry. Dropping the Slice-1 arm entirely would re-read binding answer 3 → Stage-5 surfacing. Recommend **(i)**.
  2. **Carve the calibration allowance out of standing rule (a) — they contradict as written.** The first outing happens in the release that cuts the catalog (0.6.0), and the allowance permits re-sharpening the traps exactly then, which rule (a) forbids. Add to rule (a): "…except the one recorded both-arms-saturation allowance; a re-sharpened manifest voids all prior runs — both arms re-run against it." (The carve-out is safe: the trigger is symmetric, so it cannot favor the cut.)
  3. **Un-hard-code the implementer-pinned quantities.** "12/12 held-out" is hard-coded in §Risks bullet 1 and design item 5 while the Flags line pins H (~12) at authoring time — write **H/H** in both (0041: a quantitative AC must be feasible under its own spec). Same class: §Metrics pre-registers "ΔF ≥ 2" while Flags says the ΔF threshold is the Slice-1 implementer's call — state ΔF ≥ 2 is the default, re-pinnable only together with H, before any run, recorded.
  4. **The deletion-set "today:" list is measurably incomplete.** `tests/test_build_release.py:115,271` NAMES `eval/BASELINE.md` (string literals into `classify()`; it reads no file, so it stays green post-deletion), and the stated class — "whose corpus or imports **name** a deleted file, found by grep" — sweeps it in while the list omits it. Keep the conservative class + the run-time grep as the authority and mark any list illustrative-at-print-time (prefer the locator to the count). Also state the post-deletion gate is **pytest-only by design**: `tests/workflows/*.mjs` sits outside it, and `qa.test.mjs` touches only `eval/fixture-app/`, which is not in the deletion set.
  5. **Say where the `out/` write-scope instruction lives, or "no wrapper-prompt departure" overclaims.** The shipped contract mandates ARCHITECTURE_TREE updates on file adds plus the gate runs (`.claude/agents/implementer.md` → Working rules). It reconciles with "writes scoped to `out/`" only because (a) `INCLUDE_GLOBS` is `scripts/**/*.py` + `engine/**/*.js` (measured — `out/*.py` is unwatched by the tree gate), and (b) the scoping can ride the frozen `plan-slice.md`, the contract's own delivery channel. Pin (b): plan-slice.md carries the write scope + "architecture tree out of scope for this slice" (builder-visible, so inside the lint corpus — scoping, not coaching). Note (a) in the design so a future glob widening doesn't silently break the eval.
  6. **Price the plan's own recording obligations (measured).** The settled list files to a decisions shard at Slice-1 close; slices 2–4 add cut-decision records; Slice 5 adds watch lines. Likely home `docs/claugentic-decisions/audit.md` = 10,272 B against WARN at 12,600 (cap 14,000) → 2,327 B usable (the WARN band is inclusive); `deterministic-gates.md` is 90 B under its WARN — never file there. Name the target shard(s) and the addition SHAPE (one entry with clauses, not N bulleted entries), or open a new eval shard per the index's filing rule.
  7. **Ratchet the standards cap, not only WORKFLOW's.** `.claude/claugentic-doc-budgets.json` → `docs/claugentic-standards/*.md: 60000` fans out **per file** (the gate's glob-by-key semantics), so post-cut regrowth of the whole catalog back to 130K is mechanically silent. After Slice 2 lands, ratchet that glob cap to fit the post-cut largest module — byte-exact test pin in the same commit, i.e. the plan's own Slice-5 rule applied to the very surface this plan halves. One line, in Slice 2 or 5.
  8. **Split slices 1 and 2** — per the Sizing field below; fold the chosen R1 branch into the split.

- **Sizing/completeness:**
  - **Slice 1 — SPLIT NEEDED.** Honest count: TASK_SPEC + plan-slice + TRAP_MANIFEST + 4 `checks/` modules + a full trap-avoiding reference implementation (~6 files, 12/12 + 10/10 by construction) + **ten** single-probe-flip mutants (a mutation-verification loop each) + `test_eval_trap_manifest.py` + 2 test extensions (containment vocabulary, tree-note pin) + 2 procedure docs + tree entries + decisions filing + the calibration RUN + a K=3 arm (3 heavy spawned builders + blind `[J]` grading + entry). Over the one-specialist-one-session bar. → **1a** instrument + calibration pair verified (lands complete; the calibration entry is `BUILD_ENTRIES.md` #1) · **1b** the baseline arm + entry — or 1b dissolves into Slice 2's sitting under R1(i).
  - **Slice 2 — SPLIT NEEDED.** A −70K judgment cut across 13 modules + resolver build/self-test + anchor sweep + the build-eval comparison + an audit-eval re-run (~31 agents) + land + records will not fit one session. → **2a** candidate + resolver + sweep (the branch is the durable artifact; main untouched, so nothing half-done lands) · **2b** measure both evals + land-or-defer per the pre-registered rule.
  - **Slice 3 — OK**, with the re-slice trigger stated: synthesizer-gate first, siblings as reached; if the roster-wide cut outgrows the session, land what is measured and re-slice the rest.
  - **Slice 4 — OK-borderline:** init-first is right; same per-skill re-slice trigger.
  - **Slice 5 — OK** (small; cap + pin same commit).

- **Harness impact:** No new agent, no new standards module. Lands (all harness-self, so directly here — no upstream staging): the build-eval instrument, dev-only (`eval/` is release-stripped — verified) + tree entries + the containment/tree-pin extensions; decisions-shard filings (priced per R6); **two** doc-budget cap edits, each with its byte-exact `tests/test_check_doc_budgets.py` pin in the same commit (WORKFLOW ratchet, Slice 5; standards glob ratchet, R7); the 0.6.0 CHANGELOG breaking-changes section (binding answer 2); ROADMAP watch lines (K=1 advisory extension · WORKFLOW re-cut pull-in), landed with their deferring slice as stated.

_Convergence: R2–R7 are sentence-to-line edits — apply as written. R1 is a fork the planner must pick (Stage-5 surfacing only if the Slice-1 arm is dropped); R8 restructures the slice table. Round 2 = confirm R1's chosen branch + diff-confirm the rest; no third round expected._

### Re-review verdict (Stage 3, round 2)

_2026-08-20, commit `3d73ecf` re-read from disk. RUNNING AS: Fable 5 — same caveat as round 1, and this round reviews bytes round 1 prescribed, so my own prescriptions were hunted first._

- **Verdict: PASS — conditional on residuals R2-a and R2-b below; no further gate round.** Applying the two named line edits closes the gate; the check that they landed is a diff glance at Spec time, not a re-review.
- **R1 branch (i) — CONFIRMED, applied consistently.** Verified at all five surfaces: Approach (1b "explicitly demoted: never a decision input"), Slice 1b, Slice 2b ("all six runs — arm A re-derived + arm B — one sitting, one base commit, interleaved"), §Arms (held-constant now reads "the sitting's base commit"; "a cross-sitting or cross-commit comparison is invalid by this design"), §Cost (+3 builders + a grader pass stated). Binding answer 3 stays discharged by 1b's shakedown. No contradiction remains between the slice table and the procedure.
- **R2–R8 — diff-confirmed applied:** R2 carve-out verbatim in rule (a) incl. voids-all-prior-runs; R3 in Flags + Risks + checks item 4 + §Metrics (see R2-b for the one missed spot); R4 grep-is-the-authority + illustrative-lists + the pytest-only-by-design note, all in §Arms — the "(today: …)" count is gone, replaced by the locator; R5 in-contract scoping in item 2 with the `INCLUDE_GLOBS` enabling fact and the future-widening warning; R6 the new `evals.md` shard — premise re-verified this round: the index's own filing rule permits it (`docs/claugentic-DECISIONS.md:6`, "no fit → create one — growth is horizontal"), and the index line fits (measured: index 2,609 B vs WARN 3,150 → 540 B usable; a route line is ~⅕ of that); R7 in Slice 2b + Test strategy; R8 splits 1a/1b/2a/2b + re-slice triggers on 3 and 4.
- **Residuals (the last two stale counts; both already contradicted by corrected adjacent text, so no new rule is needed — only the stale figure removed):**
  - **R2-a** — design item 4 (`run_sweep.py`) still hard-codes "six `out/` dirs shuffled" in the blind-pack build; my own R1(i) prescription created the three-worktree shakedown mode that invalidates it (round 1 missed this because the mode did not exist yet — my error to name). Edit to: "the sitting's `out/` dirs (three for a shakedown, six for a decision comparison) shuffled under opaque names". The durable pin already exists — the Procedure line carries the sitting-sized rule.
  - **R2-b** — design item 5 (the calibration pair) still says "12/12 held-out"; round-1 R3 named this exact spot and it was applied everywhere else but here. Edit to "H/H". The durable pin already exists — Flags, Risks bullet 1, Slice 1a, and item 4 all carry H pinned-together.
- **Sizing/completeness (round 2):** 1a OK · 1b OK · 2a OK · 2b OK (heavy but single-sitting by design — the measurement is the session) · 3, 4 OK with their stated re-slice triggers · 5 OK. All land complete; nothing half-done reaches main (2a's candidate lives on its branch).
- **Harness impact:** unchanged from round 1, now fully reflected in the plan (evals.md shard + index line, two cap ratchets each with byte-exact pins same commit, 0.6.0 CHANGELOG, watch lines with their deferring slices).
---

## Spec  _(per slice, after Review passes — Stage 4)_

### Slice 1a — the instrument, calibration-verified

- **In plain English (shown first at the approval gate):**
  - *What this builds:* a repeatable exam for the harness's **build** path. A small fixed programming task (an expense tracker) that an implementer agent will build from a written plan; a hidden answer key of ten realistic mistakes the task invites (two per deep standards area); and scripts that mechanically check whether a build fell into each trap and whether the program actually works. Before the exam is ever used, the exam itself is proven: a known-good solution must pass everything, and ten deliberately-broken variants must each trip exactly their own alarm — so a broken alarm can't silently wave a bad catalog cut through.
  - *What "done" means for you:* the exam exists under `eval/` (dev-side only — adopters never download it), its self-checks are green in the test suite, the proof-of-the-exam run is recorded as the ledger's first entry, and the pass/block thresholds are written down before any real measurement.
  - *What you're accepting:* new repo-side files that retire nothing by themselves (the cuts in slices 2–5 are the retirement) — **approved at "~30–40 KB"; measured at land: 249,558 B (6–8× the estimate; the mutants and reference implementation are the bulk), all release-stripped, superseded-and-surfaced per 0041's published-figure rule**; the exam is a **tripwire** — it catches gross quality regressions (≥2 traps), it can never prove a cut is safe; one trap is graded by a blinded judgment call rather than a script, and every entry says so.
- **Files & changes:** `eval/fixture-build/TASK_SPEC.md` · `plan-slice.md` (carries write-scope + tree-out-of-scope in-contract) · `TRAP_MANIFEST.md` (own canary) · `checks/{test_heldout.py, fakes.py, run_sweep.py, mutation_probe.py}` · `calibration/` (reference impl ~6 files + `mutants/` ten single-trap variants) — all per the design section above. `eval/BUILD_BASELINE.md` (procedure · pre-registered rule with the floor clauses · standing rules (a)+carve-out and (b) · H + ΔF pinned together · the one-line K=1 advisory extension · pointers-not-copies to `eval/BASELINE.md` shared mechanics · the small-N caveat stated once). `eval/BUILD_ENTRIES.md` (entry #1 = the calibration record). `tests/test_eval_trap_manifest.py` (new: table shape · every `[D]` check cell resolves import+`getattr` to a callable in `checks/` · no-coaching lint over TASK_SPEC **and** plan-slice with a per-trap→denylist mapping). `tests/test_eval_key_containment.py` (extend: TRAP_MANIFEST-derived vocabulary, allowlists, tree do-not-revert note + pin extension). `docs/claugentic-ARCHITECTURE_TREE.md` entries (containment-aware). `docs/claugentic-decisions/evals.md` (new shard, dense-entry shape) + its `docs/claugentic-DECISIONS.md` index line. One `docs/claugentic-ROADMAP.md` Later line (the other-paths/second-task extension, with pull-in evidence named).
- **In-scope standards dimensions:** `testing` (the instrument's own verification is the slice's core) · `docs-traceability` (two new docs, shard + index, pointers-not-copies) · `maintainability-structure` (single source of truth: procedure vs entries vs shared mechanics). Trust surface → the Verify panel convenes `honesty-reviewer` (the diverse-panel rule); scope includes every claim in both new docs and the entry format.
- **Tests to add:** `test_eval_trap_manifest.py` (mutation-verified: one denied token in TASK_SPEC → red; a `[D]` cell naming a missing callable → red) · the containment extension (mutation-verified: a planted trap-vocabulary line in a tree entry → red) · the calibration pair run itself (the instrument's verification — both halves must pass).
- **Acceptance criteria:** (1) full suite + all gates green, each **new** pin's red observed and recorded in the slice report; (2) calibration: reference scores H/H + 10/10 AVOIDED, each mutant flips exactly its own probe, recorded as `BUILD_ENTRIES.md` #1; (3) no-coaching lint green over both builder-visible artifacts, denylist covering all ten traps or residuals recorded in manifest rows; (4) `checks/` catalog-free: zero-path grep + the read-scope static assertion both green; (5) `BUILD_BASELINE.md` carries everything listed above, H and ΔF pinned; (6) shard + index line + ROADMAP line + tree entries landed, all doc budgets green; (7) no builder-visible artifact coaches a remedy (lint + reviewer judgment).

### Slice 1b — the shakedown arm

- **In plain English (shown first at the approval gate):**
  - *What this builds:* the exam's first real sitting. Three independent builder agents each build the task with today's full 130 KB catalog; their outputs are mechanically swept, blind-graded on the two judgment traps, and recorded as the shakedown entry — the first real measurement of how much run-to-run noise the exam carries.
  - *What "done" means for you:* entry #2 in the ledger with per-run trap tables, working-artifact and spec-compliance figures, the flap and spread noise yardsticks, catalog-read attribution, and a clean contamination sweep; scratch worktrees removed; nothing lands on main but the entry.
  - *What you're accepting:* three heavy builder runs (~order 0.6–1.2M tokens); this outing is **explicitly not the decision baseline** — the Slice-2b comparison re-runs arm A in the same sitting as arm B, which is what keeps the verdict honest; if the shakedown exposes an instrument defect, the fix rides a recorded spec-amendment and the calibration re-runs.
- **Files & changes:** `eval/BUILD_ENTRIES.md` (+entry #2). Any instrument defect the shakedown exposes → the WORKFLOW spec-amendment path (recorded in this plan; calibration re-run; `BUILD_BASELINE.md` amended if procedure text was the defect).
- **In-scope standards dimensions:** `testing`. Trust surface (the entry's claims) → `honesty-reviewer` on the Verify panel.
- **Tests to add:** none (the instrument is 1a's; this slice *runs* it).
- **Acceptance criteria:** (1) K=3 arm-A runs, one sitting, at the 1a-landed base commit; (2) per worktree: class-derived deletion set applied, `ls`-verified, post-deletion pytest green before spawn; (3) blind `[J]` grading with file:line citations, discounts recorded; (4) contamination sweep: zero content hits (crimson-giraffe · purple-elephant · TRAP_MANIFEST · SEED_MANIFEST lines); filename-only residuals disclosed; (5) entry #2 records the full entry format incl. spread/flap and cites the caveat; (6) worktrees removed, `docs/claugentic-ROADMAP.md` byte-untouched, transcripts + `RUNNING AS` retained.

### Slice 2a — the standards candidate cut (130K → ~60K), on a branch

- **In plain English (shown first at the approval gate):**
  - *What this builds:* the catalog cut itself, on a branch main never sees until it's measured. Everything that does work survives: **every dimension heading, every auditor check with its [D]/[J] tag, every threshold and number, every recorded incident, the module contracts**. What goes: prose explaining concepts to a model that already knows them — concept essays, Good-looks-like lines that only restate their heading, repeated preambles, cross-module restatements.
  - *What "done" means for you:* a candidate branch with the catalog at ~60–65 KB, every inbound citation into it still resolving (a new resolver script + the wrap-normalized manual sweep), all gates green on the branch — and **nothing landed**: the branch waits for 2b's measurement.
  - *What you're accepting:* the cut is judgment; the protection is that it cannot land without the six-run build-eval comparison plus an audit-eval run saying "no regression detected at this K". If 2b blocks, the branch is re-cut or dropped — main never carries an unmeasured cut.
- **Files & changes:** `docs/claugentic-standards/*.md` (13 files, per-module cut passes guided by the citation map in the session groundwork); `scripts/check_standards_anchors.py` (new, dev-only run-gate: enumerates inbound file+section citations across `git ls-files` — both arrow glyphs, quoted-name forms, wrap-normalized — and resolves each against live headings; self-tested against the known positives before any zero is trusted); tree entry for the script.
- **Hard keep-rules (spec lines, not judgment):** all dimension headings survive (count pinned before/after); every Auditor-check bullet + its tag survives; every numeric threshold survives; every Incident survives; frontmatter (`load_scope`, `status`) survives; the 2-day-old verdict-totality dimension survives (live incident).
- **Spec amendment (2026-08-20, USER-APPROVED re-scope at the fork):** the first cut measured the ≤65,000 target REFUTED under "every Auditor-check bullet survives" (the four kept classes alone = 89,642 B; honest floor ~115K; delivered 118,992 B at commit `dec7059` — preserved on the branch as the fallback). **Amended rule set for the DEEP cut:** the per-check bullet-survival rule is DROPPED; kept verbatim: every `##` heading (124, text-identical) · every `**Incident` block (23) · every numeric token · frontmatter · the 4 honesty registers · the `[D]`/`[J]` discipline on every check clause that survives (one tag each; the mechanical-vs-model-upheld distinction is never compressed away — the arc's hard-rule non-goal). Checks collapse to dense per-dimension clauses keeping thresholds, named primitives, and house preferences; elaborations restating a heading go. `_TEMPLATE.md` is amended in the same commit to DESCRIBE the new shape (the catalog's own authoring contract may not lag its modules). Target ≤65,000 B; a second refutation is reported ceiling-style, never absorbed. The tag COUNT pin (494) is superseded by this amendment; the tag-discipline pin replaces it.
- **In-scope standards dimensions:** `docs-traceability` (the sweep is the slice's core risk) · `maintainability-structure` · `honesty` via the 2b panel (the catalog's [D]/[J] tags are a trust surface). The full Stage-7 panel + eval measurement happen in **2b** — 2a's own verify is the branch gates + the anchor sweep + heading/check/incident count-pins.
- **Acceptance criteria:** (1) catalog total ≤ 65,000 B (target ~60K; the ceiling-refute rule applies if keep-rules make it unreachable); (2) heading count unchanged; (3) tagged-check count unchanged; (4) incident count unchanged; (5) resolver + manual sweep: zero dead inbound citations; (6) all gates green on the branch; (7) zero commits to main.

### Slice 2b — measure and land-or-defer (spec'd with 2a; runs after it)

Per `eval/BUILD_BASELINE.md` verbatim: the six-run one-sitting comparison (arm A re-derived + arm B = the 2a branch's catalog) + one audit-eval run on the candidate; **land only on "no regression detected at this K"** by the pre-registered rule, else defer (recorded). With the land: ratchet the `docs/claugentic-standards/*.md` glob cap to fit the post-cut largest module, byte-exact test pin same commit. **Known going in (entry 2): the both-arms-saturation shape is live** — if it fires, the verdict stands by rule and the allowance question (re-sharpen the exam once, voiding prior runs) comes back to the user as a recorded decision, never auto-taken.

### Slice 3 — the agents roster cut (spec'd 2026-08-21)

- **In plain English:** trim the specialist role prompts to their working density — the same move as the standards cut, one level down. Every posture, mode, caller contract and output contract stays verbatim; what goes is narration, rationale a capable model doesn't need, and rules restated from WORKFLOW/standards (single-sourced with a pointer). One incident per rule is kept where a model would otherwise get the rule backwards; the rest of the provenance goes. Two files are already at working density and are left untouched.
- **What "done" means:** the roster is smaller with no lost contract, the cut is gated by an audit-eval run (which exercises the finder/refuter/synthesis roles) plus a plan-gate probe, and the one altitude neither instrument sees is stated as an honest limit in the entry.
- **Files & changes (per `groundwork/roster-*.md`):** cut `synthesizer-gate.md` (24,505 — prime target; keep all three modes' caller+output contracts, cut the verify-verdict narration and the WORKFLOW-restated Stage-3 gate list) · `honesty-reviewer.md` (14,261) · `lens-reviewer.md` (12,304) · `product-designer.md` (12,275) · `implementer.md` (8,233) · `retrospect-harvester.md` (6,192). **Untouched:** `runtime-qa.md` (near-minimal), `finding-verifier.md` (near-minimal AND invariant-protected — INVARIANTS → scrubbed-file-set names it), `yagni-sentinel.md` (the 3K control).
- **Hard keep-rules:** every mode name + its caller contract · every output contract incl. engine-parsed lines (`RUNNING AS:`, `crossModel`, verdict strings) · the `[D]`/`[J]` + proof-vs-attempt discipline · the de-correlation-claim prohibition (protected by leaving those files alone) · one incident per rule a model would misapply.
- **In-scope dimensions:** `docs-traceability` (single-source restated content with pointers) · `maintainability-structure`; trust surface → `honesty-reviewer` on Verify.
- **Gate:** all deterministic gates green (shipped-content catches a stranded namespace token from a bad cut); **one audit-eval run** on the seeded fixture with the candidate roster (exercises `lens-reviewer` audit-scope, `finding-verifier`, `synthesizer-gate` audit-synthesis) — recall ≥ baseline; **one plan-gate probe** (planted must-block flaw → assert CHANGES_REQUIRED). **Honest limit (entry):** the `verify-verdict` altitude (63% of synthesizer-gate) is exercised by neither — model-upheld review only there.
- **Acceptance criteria:** (1) roster down materially, no mode/contract lost (diff-checked keep-list); (2) gates green; (3) audit-eval recall ≥ baseline; (4) plan-gate probe = CHANGES_REQUIRED; (5) verify-verdict honest limit recorded; (6) branch-only until gated, then land.

### Slice 4 — skills cut, the safe-cut band (spec'd 2026-08-21)

- **In plain English:** trim the six skill files to their operative content — the same move again, one surface up. Cut narration, worked-example anecdotes, and copy restated from WORKFLOW/agents/other skills (single-sourced with a pointer); keep every contract, marker, verdict string, engine arg, and user-decision gate. This is the *safe-cut band* the groundwork proved touches no test and loses no contract (~34–36 KB, ~18%); the structural breaks (init split, wrapper de-dup) are held for a fast-follow (0.7.0).
- **Files & changes (per `groundwork/skills-merged.md` §3, 21 ranked regions with exact carve-outs):** `init` (~14 KB recoverable — Step 9 report narration, the idempotency/NOT-do epilogues, scattered anecdotes; **carve-outs**: the readiness-summary shape, the 8 group names, the reportOnly/condense sentence, the compute-the-write-predicate-directly warning, the 6 husky preconditions, the managed-set table, the 4 upsert verdicts, the stamp/fence exact strings) · `build` (sub-line rationale + pitch/echo; **keep** the decline frame, approval triad, tag strings, engine arg-assembly, resume-contract section name) · `audit` (preamble/phase-1/prose-fallback narration; **keep** the *Application source present* predicate L49, the *Prose-orchestrated fallback* heading product cites, the empty-repo message, the renderOnly/SELECT guards) · `doctor` (report-table + rationale halves; **keep** the caps-config contract, the pre-commit health verdict, `$LASTEXITCODE`/never-`command -v` rules, status vocabulary) · `product` (elevate-input duplicate + deep-research explainer; **keep** the `product-critic:declined` marker, the two-fence rule, criteria-mode arg wiring) · `condense` (ownership preamble + provenance parentheticals; **keep** the 5-lever order, the anti-footgun rule, the reportOnly-clearing step, ⚠ the UNREACHABILITY×DOC_BUDGET test-window: deleting is safe, rewording toward "cannot produce that WARN" turns the suite RED).
- **DANGER LIST (never cut — 18 single-source contracts, `groundwork/skills-merged.md` §4):** the INCLUDE_GLOBS REFRESH re-injection · doctor's chain-aware re-wire refusal · the managed-set table · reportOnly clearing · the *Application source present* predicate · the settings.json self-reference shape · the SELECT keep-none guard · the `- Run the app:` format (engine/qa.js reads it) · the teammate bootstrap block · Solo mode · `product-critic:declined` · the caps-config reader-contract · the 5-lever meaning · the 4 upsert verdicts · the full managed-stamp write-forms · the pre-commit HEALTH verdict · the engine arg-assembly · the build-to-green DECLINE.
- **Plus one correctness fix (break candidate F):** the verification-tag glyph drift — `build` L196 uses `—`/`⚠` where audit/engine use ASCII `--`/`!`; normalize to the ASCII form engine/audit.js emits (a published rendered string is a claim; two spellings is a real defect).
- **In-scope dimensions:** `docs-traceability` (single-source with pointers; every carve-out preserved) · `maintainability-structure`. Trust surface → `honesty-reviewer` (skills carry the harness's honesty claims + the `[D]`/`[J]` copy).
- **Gate:** all deterministic gates green — **`check_shipped_content` and the skill-frontmatter/test pins are the real instruments** (a cut that strands a namespace token, breaks frontmatter YAML, or drops a pinned substring fails loud); `claude plugin validate --strict` in-slice (the v0.4.0–v0.5.1 unparseable-frontmatter class); a diff-checked DANGER-LIST survival check.
- **Acceptance criteria:** (1) skills corpus down ~15–18% with zero danger-list contract lost (named survival check); (2) all gates green incl. `plugin validate --strict`; (3) glyph drift fixed and swept (build/qa/audit agree on the ASCII tag); (4) any touched test pin updated to the new canonical form + mutation-verified; (5) branch-only until gated, then land. Structural breaks (init split, wrapper de-dup) → ROADMAP for 0.7.0.

### Slice 5 — WORKFLOW cap ratchet + orphan cleanup (spec'd JIT)
