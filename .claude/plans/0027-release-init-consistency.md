# 0027 — Release/init consistency (the adopter actually gets a working harness)

- **Status:** Implementing — **S1 LANDED** (2026-06-25, verified); next = S2. **Stage-3 plan-gate: APPROVE WITH CHANGES — all 6 required changes FOLDED into S1/S2/S3.** Forks RESOLVED: Fork A → MOVE the plan template to `docs/claugentic-PLAN_TEMPLATE.md`; Fork B → strip `check_doc_budgets.py` + make doctor/WORKFLOW/implementer adopter-aware. The 0.3.0 release is **PAUSED** at the force-push for this. The roster work + the `build_release --no-verify` fix are already on `origin/main` (safe).
- **Resumable from:** **implement S2** (harness-self gates adopter-clean — full scope in S2) → S3 re-audit+guard → S4 re-release 0.3.0 clean (force-push stays the user's). Bootstrapping note: §C is LANDED + on main, so the build machinery is the NEW roster (the session resolves `synthesizer-gate`/`implementer` from repo-local `.claude/agents/`); use them.
- **References:** `scripts/build_release.py` (the strip-set) · `skills/init/SKILL.md` (what init scaffolds) · `skills/doctor/SKILL.md` · `docs/claugentic-WORKFLOW.md` DoD · `.claude/agents/implementer.md` · `scripts/check_doc_budgets.py`.

## Problem — the release/init contract is violated

**The principle (a new INVARIANT, user-stated 2026-06-25):** *"Whatever the release strips, init must (re)create so the harness works in the adopter's project context."* Refined: the release strips **two classes** —
- **Class A — harness-DEV tooling** (`build_release.py`, `check_versions_synced.py`, `check_doc_budgets.py`, `tests/`, `eval/`, `RELEASE_CHECKLIST.md`, `.github/`, `pyproject.toml`, configs, the harness's OWN plans): about building the *plugin*. Adopters correctly never get these.
- **Class B — adopter-relevant state/templates** (`DECISIONS.md`, `ROADMAP.md`, `ARCHITECTURE_TREE.md`, `INVARIANTS.md`, the plan `TEMPLATE.md`, `PRODUCT*`): the workflow produces/uses these in ANY repo. **init must recreate fresh ones, and nothing shipped may reference a Class-B file that isn't created — nor a Class-A (harness-self) gate without adopter-awareness.**

**Confirmed violations (systematic audit, 2026-06-25):**
1. **★ Plan `TEMPLATE.md` — Class B, stripped, NOT init-created, but referenced by shipped docs.** It lives under the `.claude/plans/` DEV_ONLY_DIR (stripped wholesale) and init never creates it — yet the **shipped** `docs/claugentic-WORKFLOW.md` (`:54,:93,:97,:184,:241`) + `skills/build/SKILL.md:294` say a plan is *"structured by `TEMPLATE.md`."* → an adopter's plan-drafting points at a file that doesn't exist. (Degraded, not a crash — the agent can fall back to WORKFLOW's inline structure — but a real dangling ref.)
2. **★ `check_doc_budgets.py` — Class-A (harness-self) gate leaking, with a NEW 0.3.0 fail-loud.** Its own docstring: *"same register as `check_versions_synced.py`"* (which IS stripped). It budgets the harness's OWN ledgers incl. `INVARIANTS.md`, and **fail-louds if a budgeted ledger is missing** — but init doesn't create `INVARIANTS.md` (lazy). 0024 added the INVARIANTS budget row, so a fresh 0.3.0 adopter who runs it hits *"INVARIANTS.md is missing"* where v0.2.4's 3-ledger version didn't. Plus the budgets (6 KB CLAUDE / 60 KB DECISIONS) are harness-tuned. **Referenced by:** `skills/doctor/SKILL.md:50` (doctor RUNS it), `docs/claugentic-WORKFLOW.md:135` (DoD lists it, NO skip-caveat — unlike version-sync at `:134`).
3. **Harness-self gates leak into adopter surfaces without adopter-awareness (the pattern).** `skills/doctor/SKILL.md:48,50` runs `check_versions_synced.py` + `check_doc_budgets.py` (both harness-self — version-sync checks the PLUGIN's two manifests an adopter doesn't have; doc-budgets as above). `.claude/agents/implementer.md:18` tells the adopter's implementer to run `check_versions_synced.py`. WORKFLOW handles version-sync (`:123,:134` skip-caveat) but NOT doc-budgets (`:135`). So `doctor` + `implementer` would run harness-self gates that error for an adopter.
4. **`INVARIANTS.md` — Class B, correctly lazy** (Stage-9: "create lazily, don't seed empty"). NOT a gap on its own — only via #2 (the doc-budgets gate hard-requiring it). Resolved once #2 is.

**Verified OK (no gap):** `DECISIONS.md` / `ROADMAP.md` (init step 7) · `ARCHITECTURE_TREE.md` (init generates) · **`CLAUDE.md`** (init manages a `<!-- harness:managed -->` fence inside the adopter's own file — append-if-no-fence / refresh-inside-fence / create-if-absent; the fence's pointers all resolve) · `.claude/settings.json` (init seeds the plugin self-ref) · `PRODUCT*` (create-on-use via the `product` skill).

## Design — RESOLVED + refined (2026-06-25 conversation)

**FORK A — plan TEMPLATE → MOVE to `/docs/` as a managed template (NOT the carve-out).** Rather than teach `build_release` to "strip `.claude/plans/` EXCEPT TEMPLATE.md" (an exception that rots), **move `.claude/plans/TEMPLATE.md` → `docs/claugentic-PLAN_TEMPLATE.md`** (a repeated-use managed template, like `docs/claugentic-PRODUCT_SPEC_TEMPLATE.md` + `docs/claugentic-standards/_TEMPLATE.md`). It then ships normally (no strip special-case), init copies it like any managed doc, and the WORKFLOW/build-SKILL refs point at `docs/claugentic-PLAN_TEMPLATE.md`. The harness's own *plans* stay in `.claude/plans/` (all stripped); only the TEMPLATE moves out. The adopter reads the `/docs/` template and writes plans into their own `.claude/plans/` (no copy of the template into `.claude/plans/` needed).

**FORK B — `check_doc_budgets.py` is harness-self → strip it + make doctor/WORKFLOW/implementer adopter-aware.** ★ Correction: **`doctor` runs ALL THREE gates** — `claugentic-check_architecture_tree.py` (adopter-relevant — KEEP) **+** `check_versions_synced.py` **+** `check_doc_budgets.py` (both harness-self). Adopter-aware fix: doctor **detects an adopter repo** (simple signal: `scripts/check_versions_synced.py` is absent — it's stripped) and **runs only the tree gate**, marking version-sync + doc-budgets harness-self/N-A. Plus: strip `check_doc_budgets.py` from the release; add the WORKFLOW DoD doc-budget skip-caveat (mirror version-sync `:134`); reframe `implementer.md:18`'s version-sync line as harness-self.

**The managed-file model (the conversation's organizing principle — guides S1 + the follow-up):**
- **Full docs copied verbatim** (WORKFLOW, PLAYBOOK, ENGINEERING_STANDARDS, standards/*): source = ship = adopter copy. **No `_template` needed** (they're not seeds; renaming them would churn + force the harness to copy its own template to use it).
- **Templates/seeds where the harness's OWN copy diverges from the adopter's** → the `_template` source-of-truth pattern. Two sub-kinds, copied differently:
  - **Repeated-use templates** (plan template, standards `_TEMPLATE.md`, `PRODUCT_SPEC_TEMPLATE.md`): adopter KEEPS them as templates, copies one per use → stay `*_TEMPLATE.md`, **no rename.** ← the plan template joins this set (S1).
  - **One-time seeds** (DECISIONS, ROADMAP): adopter gets ONE file filled over time → `_X.md` **renamed to** `X.md` on copy. Currently these seeds are **prose buried in the init skill**, not files.

**FOLLOW-UP (NOT in 0027 — a separate, reviewed refactor):** migrate the one-time seeds (DECISIONS/ROADMAP, + the init-prose seed content generally) to `_template` FILES, standardize the naming convention, so init copies from files instead of embedding seed prose. A real consistency win, but it touches init's seeding logic + the ledgers + needs its own tests, and does NOT block the release. KISS: 0027 unblocks the release; this refactor earns its own slice. _(Roadmap it at 0027 Land.)_

## Slices
- [x] **S1 — Plan TEMPLATE → `/docs/` managed template.** ✅ LANDED 2026-06-25 (synthesizer-gate verify-verdict PASS · honesty-reviewer CLEAN · 372 node + 158 pytest + all 5 gates green · grep clean). `git mv .claude/plans/TEMPLATE.md docs/claugentic-PLAN_TEMPLATE.md`. **Complete ref inventory** (plan-gate 2026-06-25 found S1's first list missed 3; refs appear in BOTH the full-path `.claude/plans/TEMPLATE.md` form AND the bare-prose `TEMPLATE.md` form — update both forms, not a path-only find/replace):
  - **SHIPPED — dangling-ref-critical:** `docs/claugentic-WORKFLOW.md:54,93,97,105,184,241` · `skills/build/SKILL.md:294` · `scripts/claugentic-advisor.py` (SHIPS — see advisor decision).
  - **Stripped/dev — consistency (not ship-critical):** `CLAUDE.md:26` ("start from `TEMPLATE.md`" → `docs/claugentic-PLAN_TEMPLATE.md`; "Active plans live in `.claude/plans/`" stays true) · `docs/claugentic-ARCHITECTURE_TREE.md:64` (rename the index-entry path) · `docs/claugentic-ROADMAP.md:44` (a **LIVE** forward item — update its scan-target path so it stays actionable).
  - **Leave as-is — historical, append-only:** `docs/claugentic-DECISIONS.md:61` records what was true at plan 0025; the 0027 Land DECISIONS line records the move (honest append-only treatment, not history-rewrite).
  - **Template self-refs: NONE** (verified — the body points at other docs, never its own filename; the §C3a `plan-reviewer`→`synthesizer-gate` strand is already clean at `TEMPLATE.md:54`).
  - **Advisor decision — DROP the vestigial skip:** remove `scripts/claugentic-advisor.py`'s `TEMPLATE_PLAN_NAME` constant (`:82`) + the `_read_plans()` skip block (`:282`) + the two doc-comments referencing it (`:30,:268`). After the move `.claude/plans/` never holds a `TEMPLATE.md` (harness OR adopter — adopters read the /docs/ template and write their own plans), so the skip is dead and shipping a comment about a layout that no longer holds is "silently mismatched" debt (honest/DRY → remove). `test_advisor.py` does not pin the TEMPLATE skip (grep-clean) → test-safe; implementer re-runs pytest to confirm.
  - **init managed-set:** add ONE row to the table at `skills/init/SKILL.md:139-144`: `| docs/claugentic-PLAN_TEMPLATE.md | the plan-file contract template (verbatim copy; adopters copy one per plan into their own .claude/plans/) |`. The upsert/predicate/stamp logic is generic over the set — the row wires it. **Source ships UNSTAMPED** (line 1 stays `# NNNN — <Title>`, like every managed source incl. `PRODUCT_SPEC_TEMPLATE.md`); init stamps the adopter's TARGET line 1 + body-compares target-minus-line-1 (`init/SKILL.md:190-196`) — do **NOT** add a stamp to the source `docs/` file (it would break the body-compare).
  - **tests:** fix the stale `test_build_release.py:59` fixture (`test_dev_directories_are_stripped_recursively`, `:52-64`) — drop `.claude/plans/TEMPLATE.md` from its input list + adjust the `len(strip)` count, AND add a SHIP assertion that `docs/claugentic-PLAN_TEMPLATE.md` ships to `test_managed_docs_and_runtime_files_ship` (`:32-50`).
  - No `build_release` strip carve-out (the move makes `.claude/plans/` cleanly all-stripped). **Verify** (grep): no SHIPPED file references a `.claude/plans/TEMPLATE.md` path or a bare-prose plan-template `TEMPLATE.md` anymore.
- [ ] **S2 — Harness-self gates adopter-clean.** Add `scripts/check_doc_budgets.py` to `build_release` `DEV_ONLY_FILES` (strip it). `doctor` (`skills/doctor/SKILL.md:45-53`): detect adopter repo (absence of `scripts/check_versions_synced.py` at the path init would place it) → run ONLY the tree gate; mark version-sync + doc-budgets harness-self/**N-A** in an adopter (don't error). `WORKFLOW.md:135` DoD doc-budget → add the "harness-self only — skip for adopters" caveat (mirror version-sync `:134`). `implementer.md:18` → reframe the version-sync line as harness-self (adopter's implementer skips it). **Plus the two surfaces the plan-gate flagged (low-effort wording, in-scope here):** `skills/build/SKILL.md:400` names "version-sync" inline in the close-out with no adopter caveat (build delegates the canonical list to WORKFLOW DoD `:352`, but `:400` is inline) — add a caveat or point at the DoD; `WORKFLOW.md:123` adopter-note already calls out `check_versions_synced.py` as harness-self but NOT doc-budgets — make it consistent.
- [ ] **S3 — Re-audit + a ship-list guard.** Re-run the systematic audit (grep every shipped file for refs to any stripped-uncreated Class-B file or an adopter-inapplicable gate — must be clean; explicitly confirm the advisor no longer carries a stale `.claude/plans/TEMPLATE.md` assumption). **Honest assertion scope:** a pure-membership `test_build_release` assertion can only pin ship/strip *set membership* (TEMPLATE ships · check_doc_budgets strips), NOT "no shipped file's *text* references a stripped path" — word the test + its docstring honestly (no over-claim). If a content-grep guard is wanted it's a heavier test (grep shipped file contents) — decide in S3 whether the membership guard suffices (likely yes: the membership pins + the S1/S2 manual grep are the contract; a content-grep test is a candidate ROADMAP item, not a blocker).
- [ ] **S4 — Re-release 0.3.0 clean.** `build_release.py --apply` (the `--no-verify` fix is already on main) → `git range-diff`/diff drop-check → **the `release` force-push stays the USER's** (irreversible/outward) → `/plugin update` → post-publish: the `eval/BASELINE.md` drift-check + adopter validation.

## Affected files
**S1:** `.claude/plans/TEMPLATE.md` → `docs/claugentic-PLAN_TEMPLATE.md` (git mv) · `docs/claugentic-WORKFLOW.md` (refs :54,93,97,105,184,241) · `skills/build/SKILL.md` (ref :294) · `scripts/claugentic-advisor.py` (drop the vestigial skip) · `CLAUDE.md` (ref :26) · `docs/claugentic-ARCHITECTURE_TREE.md` (index entry :64) · `docs/claugentic-ROADMAP.md` (live item :44) · `skills/init/SKILL.md` (managed-set table row) · `tests/test_build_release.py` (strip-fixture fix + ship assertion).
**S2:** `scripts/build_release.py` (add check_doc_budgets to DEV_ONLY_FILES) · `skills/doctor/SKILL.md` (adopter-awareness) · `docs/claugentic-WORKFLOW.md` (DoD doc-budget caveat :135; adopter-note :123) · `skills/build/SKILL.md` (inline version-sync caveat :400) · `.claude/agents/implementer.md` (version-sync framing :18) · `scripts/check_doc_budgets.py` (stays in-repo, just stripped from release).
**S3:** `tests/test_build_release.py` (membership guard) · the systematic grep.
**Land:** `docs/claugentic-DECISIONS.md` (the release/init-contract decision) · `docs/claugentic-INVARIANTS.md` (the contract as a load-bearing invariant) · `docs/claugentic-ROADMAP.md` (the deferred seed-migration follow-up).

## Review _(plan-reviewer, Stage 3 — after the forks resolve)_

`RUNNING AS: Opus 4.x` — separate clean-context plan-gate pass on the most-capable model (a reduction of rubber-stamping risk, not a model-independent oracle).

**Verdict: APPROVE WITH CHANGES.** The chosen direction (both forks) is sound and correctly motivated; the violations audit is accurate. But the **reference inventory in S1 is incomplete** — three live `TEMPLATE.md` references are not in the plan's cited set, and one cited line is mislabeled. An incomplete grep here is the exact failure mode S3 exists to catch, so it must be fixed in S1 (don't lean on S3 to discover it). The required changes below are all in-scope and small.

### Required changes (numbered, each actionable)

1. **S1 ref inventory is incomplete — add the three missed `TEMPLATE.md` references.** The plan cites `WORKFLOW.md:54,93,97,184,241` + `build/SKILL.md:294` + "the template's own self-refs" + ARCHITECTURE_TREE. A full grep (`rg "TEMPLATE\.md"`) of the shipped tree finds **three more** that S1 must update:
   - **`CLAUDE.md:26`** — *"Active plans live in `.claude/plans/` (start from `TEMPLATE.md`)."* CLAUDE.md is **Class A (stripped)**, so this is not an adopter-dangling-ref — but it is a shipped-in-DEV doc that will point at a moved file, and CLAUDE.md ↔ WORKFLOW.md must stay consistent. Update it (and re-word: the adopter reads `docs/claugentic-PLAN_TEMPLATE.md`, writes plans into their own `.claude/plans/`).
   - **`WORKFLOW.md:105`** — *"(The plan `TEMPLATE.md` carries a matching section.)"* (the plain-English-layer note). **SHIPPED** — must be updated to `docs/claugentic-PLAN_TEMPLATE.md`.
   - **`scripts/claugentic-advisor.py:30,82,268`** — the SessionStart advisor SKIPs `TEMPLATE.md` when deriving in-flight plans from `.claude/plans/*.md` (`TEMPLATE_PLAN_NAME = "TEMPLATE.md"` at :82). This script **SHIPS** (not in `DEV_ONLY_*`). After the move, **`.claude/plans/` no longer contains `TEMPLATE.md`** — so the skip is dead code that no longer matches anything, but it is *also harmless* (nothing to skip). **Decide explicitly in S1:** either (a) drop the now-vestigial `TEMPLATE.md`-skip from the advisor (cleaner, no-dead-code per CLAUDE.md), or (b) leave it with a one-line comment that the template moved to `/docs/` and the guard is retained defensively. Do **not** leave it silently mismatched. (Confirm no test pins the advisor's skip behavior before removing it.)

2. **Fix the mislabeled citation `WORKFLOW.md:54`.** `:54` is *"the `TEMPLATE.md` architect-pass forcing function"* (the "native capability" paragraph) — it names `TEMPLATE.md` twice as a **bare filename in prose**, not as a path `.claude/plans/TEMPLATE.md`. Same for `:93` (*"the plan `TEMPLATE.md` carries a required … section"*) and `:97` (*"structured by `TEMPLATE.md`"*). These are **bare-filename prose references**, whereas `:184` and `:241` and `build/SKILL.md:294` use the **full path** `.claude/plans/TEMPLATE.md`. S1's instruction *"update EVERY ref `.claude/plans/TEMPLATE.md`→`docs/claugentic-PLAN_TEMPLATE.md`"* is a **path-only find/replace that will MISS the bare-filename prose refs** (`:54,:93,:97,:105` + `CLAUDE.md:26`). Re-state S1's edit as *"update every reference to the plan template — both the full-path form and the bare `TEMPLATE.md` prose form"* so the bare-filename mentions are caught. This is the single highest-risk gap in the plan.

3. **Clarify the managed-stamp mechanic in S1 (source stays unstamped).** S1 says init copies the template *"with the managed stamp."* Per `skills/init/SKILL.md:190-196`, **managed sources carry NO stamp** — init stamps the *target copy* on line 1 and body-compares `target-minus-line-1` against the pristine source. So `docs/claugentic-PLAN_TEMPLATE.md` must ship **unstamped** (line 1 stays `# NNNN — <Title>`), exactly like `docs/claugentic-PRODUCT_SPEC_TEMPLATE.md` (line 1 `# PRODUCT_SPEC …`) and `docs/claugentic-standards/_TEMPLATE.md`. Reword S1 to *"init stamps the adopter's copy (source stays pristine/unstamped, like the other managed templates)."* — otherwise an implementer may wrongly add a stamp to the source and break the body-compare.

4. **S1 must add the new doc to init's managed-set TABLE explicitly.** The managed set is the markdown table at `skills/init/SKILL.md:137-144` (a clean insertion point — confirmed). S1 says "add it to init's managed-copy set" — make it concrete: add a row `| docs/claugentic-PLAN_TEMPLATE.md | the plan-file contract template (verbatim copy; adopters copy one per plan into their own .claude/plans/) |`. (Sibling note: `init` step 7 seeds DECISIONS/ROADMAP but the *plan template* belongs in this verbatim-managed table, not step-7 seeding — the plan has this right; just name the exact edit.)

5. **S2: name the adopter-facing version-sync wording the doc-budget fix doesn't cover.** S2 reframes `implementer.md:18` and the WORKFLOW DoD `:135` caveat. Two more shipped surfaces name a harness-self gate without an adopter caveat — confirm S3's grep catches them, and decide in S2 whether to fix now: **`build/SKILL.md:400`** lists *"version-sync"* in the deterministic-gate close-out (build delegates the canonical list to WORKFLOW DoD at `:352`, so the DoD :135/:134 caveats mostly cover it, but `:400` names version-sync inline with no caveat); and **WORKFLOW.md:123** (the adopter-note block) already handles version-sync but **not doc-budgets** — S2's `:135` caveat is necessary but verify the adopter-note at `:123` is consistent (it currently calls out only `check_versions_synced.py` as harness-self). These are low-effort wording fixes squarely in S2's "harness-self gates adopter-clean" scope.

6. **S1: state the test-file consequence for `test_build_release.py:59`.** `test_dev_directories_are_stripped_recursively` (`:52-64`) hardcodes `".claude/plans/TEMPLATE.md"` in its input list and asserts `len(strip) == 5`. The move does **NOT break this test** (it's a pure `classify()` unit test on a literal path string — that string still classifies as stripped under the `.claude/plans/` dir-prefix). **But the test becomes stale/misleading** (it pins a path that no longer exists post-move). S1 must update that fixture: drop `.claude/plans/TEMPLATE.md` from the list (and adjust the count to 4) or replace it with a still-stripped real plan path, AND add the new SHIP assertion for `docs/claugentic-PLAN_TEMPLATE.md` to `test_managed_docs_and_runtime_files_ship` (`:32-50`). The plan's S1 mentions a ship assertion but not the stale-fixture update — name both.

### Sizing / completeness check (per slice)

- **S1 (template move) — OK, sized, but underspecified (fix via Required changes 1-4,6).** One vertical, session-sized change: `git mv`, ~9 ref updates (after the inventory fix), one init-table row, two test edits. Lands complete. The omitted refs are spec gaps, not sizing problems.
- **S2 (harness-self gates adopter-clean) — OK.** Strip one file, one doctor adopter-detection branch, two-three wording edits. Session-sized, vertically complete. **Soundness of the doctor adopter-signal: HOLDS** — `scripts/check_versions_synced.py` is in `DEV_ONLY_FILES` (`build_release.py:52`) and so is absent in every adopter tree but present in-repo; its absence is a clean, stable adopter discriminator. (Minor: state the signal is "absent at the path init would have placed it," and that doctor in an adopter then marks version-sync + doc-budgets N-A rather than erroring — matches `doctor/SKILL.md:45-53`.)
- **S3 (re-audit + ship-list guard) — OK, and load-bearing.** Keep it as the backstop, but it must not be the *first* place the missed refs (change 1) surface — fix them in S1. The `test_build_release` no-dangling-ref assertion is a good durable guard; scope it precisely (it can only assert ship/strip membership, not "no shipped doc text references a stripped path" — a true dangling-ref test would need to grep shipped file *contents*, which is a heavier test; decide which guarantee S3's test actually gives and word the assertion honestly, no over-claim).
- **S4 (re-release) — OK.** Correctly keeps the `release` force-push as the user's irreversible action (class-(c) hard stop per WORKFLOW Decision-gated autonomy). Correct sequencing.
- **Deferred follow-up (seed migration to `_template` files) — correctly deferred.** Genuinely separate, touches init seeding logic + ledgers + its own tests, does not block the release. YAGNI-correct to roadmap it at Land, not fold it in. Good call.

### Harness impact / risks

- **No test pins TEMPLATE.md's old path destructively.** `test_build_release.py:59` references it but as a `classify()` input literal, not a filesystem assertion — it won't fail on the move, but must be updated to avoid staleness (change 6). No other test references the plan template path (the `test_product_spec_template.py` / `_TEMPLATE.md` matches are the *standards* + *product-spec* templates, unrelated).
- **Stripping `check_doc_budgets.py` does NOT break the harness's own doc-budget gate.** The script stays in `main` and runs in-repo (CI `.github/workflows/ci.yml:68`, the DoD run-gate, doctor); only the *release tree* loses it. Its test `tests/test_check_doc_budgets.py` is hermetic (monkeypatches `DOC_BUDGETS`, tmp files) and `tests/` doesn't ship — no orphaning, no in-repo regression. Plan's harness-impact reasoning is correct.
- **Violation #2 is real and worth fixing now:** `check_doc_budgets.py:76-77` fail-louds on a missing budgeted ledger, and `:55` budgets `docs/claugentic-INVARIANTS.md` (added 0024), which init creates lazily — so a fresh 0.3.0 adopter running it hits *"INVARIANTS.md is missing"*. Plus the 6 KB `CLAUDE.md` cap (`:48`) is harness-tuned and would wrongly breach on an adopter's own CLAUDE.md. Stripping is the right call.
- **DECISIONS + INVARIANTS entries (Affected files line):** good — the "release strips ⇒ init recreates ⇒ nothing shipped references a stripped-uncreated Class-B file or runs a harness-self gate without adopter-awareness" rule is a genuine load-bearing invariant (sibling to the two-manifest invariant at `INVARIANTS.md:14`). Record it with provenance. **No new STANDARD or agent** is implied; this is a build/init-contract fix, not a standards-catalog change. Harness-impact is correctly scoped.
- **One residual risk to name in S3:** the advisor (`claugentic-advisor.py`) SHIPS and its `TEMPLATE.md`-skip assumes the template lives in `.claude/plans/`. After the move that assumption is silently void. Change 1c forces an explicit decision; flag it so S3's sweep verifies the advisor no longer carries a stale assumption about the template's location.
