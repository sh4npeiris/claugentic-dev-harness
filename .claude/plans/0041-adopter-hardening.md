# 0041 — Adopter hardening: make the harness's promises real in installed repos

- **Status (2026-08-17, latest):** Implementing — **Slices 1–10 LANDED-or-LANDING.** S10b Stage-7 PASS; land branch `land/0041-s10b` = feat (`07ad09a` + `12a9754` + `c05d79f`) + `harvest/0041-s10a` + the ROADMAP discharge; PR pending user merge. **Remaining: Slice 11 (eval leak — grounded), Slice 12a/12b (WORKFLOW relocation + condensation to ≤ 56,000 B, carrying the whole-feature closing pass, which has never run), then plan close.** Next after merge: housekeeping + S10b Stage-9 harvest → Slice 11 spec. **Ledger standings to respect:** ROADMAP 12,580 (20 B) · `plugin-distribution` 12,594 (**6 B — effectively at its condensation threshold**) · INVARIANTS 17,976 (24 B) · CLAUDE.md 5,364 (36 B). **The doc-budget gate compares `>=`, so the safe margin is one byte less than the distance.**
- _(superseded header line, kept for the arc)_ **Slices 1–9 + 10a LANDED-or-LANDING.** S9 merged (PR #15). **Slice 10a: Stage-7 PASS round 2**; land branch `land/0041-s10a` = feat (`120606e` + `dfd4154` + `9928e4f`, already carrying `harvest/0041-s9`) + the land commit; PR pending user merge. **Remaining: Slice 10b (D4+D6), Slice 11 (eval leak), Slice 12 (WORKFLOW weight + the whole-feature closing pass, which has never run).** Next after merge: housekeeping + S10a Stage-9 harvest -> Stage 4 spec for Slice 10b.
- **Resumable from:** Land Slice 10a (PR merge) -> housekeeping + S10a harvest -> Stage 4 spec for Slice 10b (bases on 10a)
- **Blockers:** none. S9-L1/L4/L5 and S10-L1/L2 + S10a-L3 all discharged. **The ledger squeeze is EASED but not solved, and the shape changed:** 10a's Stage-7 fix *freed* 82 B, so ROADMAP now sits at **12,449 B / 151 B under WARN** (it was 69). Four ledgers remain at 88.9–89.9% of cap against `doc-lifecycle.md:7`'s own "~80%" design rule — **INVARIANTS 17,976 B / 24 B is the tightest and the likeliest next stall**, and its own *Ledger hygiene* ROADMAP entry names rung 2 of the sharding ladder as the probable answer. S10a-L2 is deliberately routed to the Stage-9 harvest rather than ROADMAP for exactly this reason.
- **Flags:**
  - **S12 grew 63% and nothing gates it.** WORKFLOW is **94,471 B** today vs the plan-era measured base of 83,383 B. Post-relocation the density work is **28,678 B**, not the Slice-12 bullet's 17,590 B. Structural cause: WORKFLOW is the one large managed doc **absent from `.claude/claugentic-doc-budgets.json`**, so no gate bounds its growth. Recompute the 12a/12b split at S12's own spec; the ≤ 56,000 B AC stands.
  - **NOT release-ready, and a naive tag BURNS the version.** `CHANGELOG.md:12` is still `## Unreleased`, but `release.yml:168-170` extracts `## <version>` and hard-errors when absent — the gates job passes, then the **publish** job dies *after* the tag exists. Manifests are still `0.5.1` while main is ~96 commits past `v0.5.1`. Release prerequisites: rename the heading + `build_release.py --apply --bump`, an eval-drift baseline row (`eval/BASELINE.md` newest entry is v0.5.0 / 2026-07-30 — nothing for v0.5.1), and the two GitHub admin settings, **verified NOT applied**: `branches/main/protection` returns `required_status_checks: null`, `branches/release` 404s, `rulesets: []` — so "the workflow is the only publisher" is a contract, not a mechanism.
  - **A newly-authored false mechanical claim is queued to ship.** `CHANGELOG.md:39-40` says the gates run "at every commit" / "**Both gates run every time**" — measurably false for conflict-free merge commits (`pre-merge-commit` unwired; tracked at ROADMAP Bugs). CHANGELOG is in the SHIP set, so unlike the pre-existing `init/SKILL.md` siblings this instance is **new copy this release would author**. A 3-line copy fix, not a slice — but it should land before any tag.
  - **ROADMAP contradicts itself.** `:14` ("No open items — the harness is feature-complete for v1") and `:39` ("there is no open backlog") sit ~14 lines above `:53`, which tracks plan 0041 as ACTIVE with 3 unbuilt slices. Harness-self only (ROADMAP is in the STRIP set), but it is exactly the class S8 swept.
  - **Unowned residual:** `docs/claugentic-decisions/deterministic-gates.md:11` restates the five cap numbers the config owns (a second source of truth), parked for "the next doc-lifecycle condensation" that **no slice schedules**. Assign it to 12b or a ROADMAP line.
  - Already discharged early, do not redo: the S2 and S3 follow-on migrations are in ROADMAP `:52`/`:49`.
- **Disposition at close:** per template — deferred remainders → `docs/claugentic-ROADMAP.md`.
- **Roadmap item:** `docs/claugentic-ROADMAP.md` → Later/Ideas → "Plan 0041 — adopter hardening (ACTIVE 2026-08-12)".
- **References:** `docs/claugentic-DECISIONS.md` (deterministic-gates · plugin-distribution · release-contract · doc-lifecycle shards) · 12-agent holistic review (wf_b334694f, 2026-08-12) · 3-adopter scout (wf_ee5f24f7, 2026-08-12)

## Problem

Evidence from the harness's first three real deployments (data-insights-hub docs@0.4.1 · DistrictSync docs@0.3.0/plugin 0.4.1 · AskBase docs@0.4.0) plus a 12-agent holistic review of this repo (both 2026-08-12):

1. **Ledger weight is unbounded in every adopter** — the #1 systemic failure, confirmed ×3. DECISIONS: 193KB+278KB archive (DIH, re-snowballed to 193KB within ~4 weeks of its own split) · 300KB monolith (DS) · 648KB monolith (AB, ~13× the size that triggered sharding here). "Optional, leave empty" CHARTER: 60KB / 8KB / 202KB. Uncapped split-extensions everywhere (DIH `AUDIT_BACKLOG.md` 391KB; AB `BACKLOG.md`+companions). CLAUDE.md always-loaded at 28KB / 63KB / 23KB. Root cause: `check_doc_budgets.py` is release-**stripped**, the caps-config is "adopter-created" (nobody creates it), and split-created extension files inherit **no cap** — so the exact condense machinery this repo built for itself protects only itself.
2. **Version skew is universal.** All three deployments run stale managed docs (0.3.0/0.4.0/0.4.1 vs 0.5.1); DistrictSync hand-documents "cite the plugin, not that file" instead of re-running init. Nothing surfaces "your stamped docs < installed plugin".
3. **Plan-lifecycle convention dropped in all three** (AB: 31 live + 50 archived plans, ~1MB; DS: landed 137KB plan retained; DIH: manual sweep needed). Delete-at-land is model-upheld and universally not upheld.
4. **Releases aren't gated on green CI.** v0.5.1 shipped inside an 11-day red-CI window (pytest collection dead — `import yaml` vs no pyyaml; fixed PR #7). `claude plugin validate --strict` is human-remembered (a MEMORY note), not mechanized.
5. **Five front-door over-claims** vs the honesty bar: README:3 "never changes your code without your sign-off" (lightweight path has no sign-off; stop discipline model-upheld, WORKFLOW:193) · "zero side effects" aborted-run claim (CLAUDE.md/CHANGELOG vs build_release.py:26-28) · README:57 "blocks *done* until *every file* documented" (commit-time, glob-scoped, conditionally wired) · "deterministic architecture enforcement" unqualified in both manifests · /build frontmatter "stops before anything irreversible" stated as fact.
6. **The Stage-9 learning loop doesn't close in adopters:** harvest destinations `.claude/agents/` + editable WORKFLOW exist only here (retrospect-harvester.md:18-19 vs WORKFLOW:151); honesty-reviewer:14 + product-designer:47 read a CLAUDE.md "honesty positioning" section only this repo has; no upstream contribution path exists in shipped docs.
7. **Team wiring is fragile:** husky repos (`core.hooksPath=.husky`) leave the tree gate written-but-inactive (init:499-506); a teammate without Python has every commit blocked by a cryptic error (wrapper can't distinguish missing interpreter from failing gate); wiring never propagates (git deliberately doesn't auto-activate hooks on clone).
8. **Engine defects:** audit.js:1137 checks `args.renderOnly` BEFORE parseArgs though scriptPath delivery passes args as a JSON string (documented at :756-768) → SELECT re-render seam throws · build-item.js:472-475 filters the explicit `null` "same-model" signals (:822,:854) before folding → a mixed run over-reports "cross-model confirmed" · qa.js:816-818 driverPrompt preamble malformed + :658/:730 trim only `/` (build-item trims both) · verify.js:484-523 fan-out unguarded where audit.js uses guardedAgent (:1203-1214) · build-item.js:790-792 silently defaults verify dimensions/trustSurface vs verify.js:141's "never defaulted" stance. Plus the 0040-banked standing fix: `nsAgent()` hard-namespaces spawns with no bare-name fallback (hit twice).
9. **Eval answer-key leak:** `docs/claugentic-ARCHITECTURE_TREE.md:141-145` describes each fixture file's planted seeds in the read-first doc; the canary grep can't detect absorption from there.
10. **Adopter incident to record:** harness doc content crashed DIH's Tailwind build (poison string scanned from docs/; commits a8248b3f/e315dcf6/3eb2e62d).

## Goals / Non-goals

- Goal: adopter doc budgets **mechanical where wired and an interpreter is present; seeding and wiring stay model-upheld (`init`, a prose skill) — the same honesty shape as the tree gate.** Adopter enforcement is **commit-time ONLY** (chained after the tree gate in the same pre-commit; `deterministic-gates.md:7` "No adopter CI" stands — the CI legs of this plan are harness-self). **PostToolUse/per-action hooks REJECTED** (user constraint 2026-08-12; consistent with 0024-S4a).
- Goal: release pipeline to industry standard — the human gate becomes the tag push; a tag-triggered workflow runs every gate (incl. `plugin validate --strict`) and only then publishes.
- Goal: every shipped sentence matches wiring; the learning loop closes in adopter repos; team wiring survives husky + python-less teammates; the six engine defects fixed with regression pins; eval leak sealed.
- **Delivery dependency (stated per gate #11):** nothing in the doc/skill slices reaches the three live adopters until a release ships **and they re-run `init`** — except the plugin-resident SessionStart advisor (Slice 3), which arrives on plugin update alone; that is why it is sequenced early.
- Non-goal: remediating the three existing deployments (their re-init + condense runs happen in those repos — ROADMAP line records this as follow-on).
- Non-goal: telemetry of any kind (privacy — declined 2026-08-12).
- Non-goal: the mechanical approval land-gate + secret-scan (stay honestly "not built yet" — existing DECISIONS line).
- Non-goal: standards-catalog gen-1 upgrade pass (own plan; ROADMAP line at close).
- Non-goal: WORKFLOW.md **semantic** rewrite — Slice 12 is relocation + DoD renumbering + a **content-preserving condensation** to ~80% of its cap (scope expanded by the user 2026-08-12 resolving R2; meaning never changes, only density).

## Approach

Ship the leanness machinery instead of keeping it harness-self (generalize `check_doc_budgets.py` to a per-repo caps-config; init seeds defaults incl. a heritable glob so split-extensions are born capped — **for flat siblings matching the seeded pattern**; managed full-copy docs excluded). Surface staleness at session start (advisor) and in doctor, never per-tool. Formalize release-on-green with GitHub-native mechanisms. Fix copy to match mechanics.

**Forks resolved (user, 2026-08-12):**
- **0038's "NO doc-budget hook" don't-re-break is SUPERSEDED** — the user directed commit-time enforcement "like the tree gate." A dated superseding DECISIONS line + the enforcement-story copy sweep land **with the slice that falsifies each sentence** (Stage-4 repair, 2026-08-14 — this parenthetical predated the 3a/3b/3c split and said "(Slice 6)" for the whole list): the superseding line + every **ship**-falsified site ride **Slice 6**; the sentences only the **chaining** falsifies (`CLAUDE.md`'s "the one mechanically enforced gate" · README's blocks-done [shared surface with S8] · `honesty.md`/`PRODUCT_SPEC` enforcement claims · WORKFLOW/INVARIANTS hook-enforcement wording) ride **Slice 7** with the chaining — those sentences stay TRUE until the chain is wired, so there is still no interim false-copy window.
- **Grace posture = per-file report-only flag** (option A): init seeds the recommended cap; an already-over file gets a visible `reportOnly` flag — the gate prints the breach and passes until the file first comes under cap — clearing the flag is `/condense` + `/doctor` judgment, nothing mechanical clears or "self-arms" it (wording corrected per the implemented semantics, S7 round-1 L4). Avoids both rung-2's forbidden mechanical ceiling-raise and day-one repo-global blocking. Honoring the shrink is `/condense` + model-upheld — nothing mechanical shrinks anything. **Visibility is only real once Slice 5's wrapper surfaces non-empty advisory output on exit 0** — today's wrapper discards success output (R6), so the breach line must be plumbed through, and Slice 7's e2e asserts it actually appears in commit output.
- **WORKFLOW cap = relocate + condense to a real cap** (R2, user 2026-08-12): measured 83,383 B − 9,793 B relocatable body ≈ 73.6 KB — so Slice 12 also runs a content-preserving condensation landing WORKFLOW at **≤ ~56 KB (80% of the 70,000 B cap)**; the cap then genuinely bites without permanent-WARN parking.
- **Release shape = CI publishes** (option A): `build_release.py` prepares + validates locally and STOPS before any push; the human act is pushing the tag; the tag-triggered workflow re-runs all gates + `plugin validate --strict` + built-tree validation, and only then pushes the `release` branch + creates the GitHub Release. The installable plugin **content** publishes only on green; the catalog's advertised version (`marketplace.json` on main) is written at prepare time — see §Risks for that window and its recovery (R9). Offline publish is retired (recorded trade-off). The local preflight red-CI check becomes advisory (warn), not the load-bearing gate.
- **Loop closure = fix the agent pointers** (no managed-fence contract change).

Alternatives rejected: PostToolUse enforcement (user — overhead) · model-upheld budgets (observed failing ×3) · fail-closed hook on missing python (user chose warn-and-pass) · cap-at-current-size (rung-2 violation, gate #4) · verify-after-publish release shape (gates nothing adopters consume).

## Architecture & holistic fit

- **Codebase fit:** one-gate-one-invariant holds — doc-budgets stays a sibling script, **chained (run-both-and-report: a budget failure never masks the tree gate's message)** after the tree gate; `_repo_root()`/UTF-8 parity with siblings; init remains the sole writer of adopter wiring; the release workflow invokes the same gate scripts + `build_release.py` build path rather than forking logic.
- **Caps-config schema (spec'd once, gate #13 — no `version` field, no non-path keys at all):** flat map `{"<relpath-or-glob>": <max_bytes> | {"max": <bytes>, "reportOnly": true}}`. The extended object form exists ONLY for the grace flag. **Canonical schema statement = `/doctor`'s reader-contract**; the other three documentation sites (`WORKFLOW.md:175/:177` · `doc-lifecycle.md:6` · `condense/SKILL.md:140`) become pointers to it (swept in Slices 4/6 per R7b). **`REQUIRED_SHARDS` is DELETED, not config-keyed** (R7a, shape corrected per R10): **the glob stays the SOLE cap** — never per-file shard entries beside it (double measurement, two cap homes — the exact "never a second cap" rule the script documents); **shard existence moves to the 0040-banked index↔shards agreement pytest**, which Slice 4 absorbs (one cap source, one message, one banked item cleared).
- **Adopter config semantics:** absent config ⇒ **no-op exit 0** (a chained gate must never block a repo that hasn't opted in) · seeded glob matching nothing ⇒ **skip** · adopter globs are non-recursive and the flat-shape assertion stays harness-self (gate #8).
- **Product fit:** the adopter (possibly non-engineer) should never wake to a 648KB ledger or stale docs — the harness keeps itself lean and current without being asked.
- **Craft bar:** non-user-facing tooling — skipped per effort-dial.
- **Quality dimensions:** `maintainability-structure` · `docs-traceability` · `reliability-resilience` · `testing` · `security` (release workflow provenance).
- **Future-proofing:** release workflow steps single-purpose so signing/attestation slots in later; the object-form cap value is the extension point if a future flag is ever needed.

## Affected files

`scripts/check_doc_budgets.py` (generalize; caps migrate to config) · **`.claude/claugentic-doc-budgets.json` (NEW — this repo's own caps; one cap source per repo)** · `scripts/build_release.py` (stop-before-push shape, advisory preflight) · `.github/workflows/ci.yml` + **new `release.yml`** · `pyproject.toml` · `skills/init/SKILL.md` (caps seed, husky chain, wrapper, docs-scanner caution) · `skills/condense/SKILL.md` + `skills/doctor/SKILL.md` (reader-contract, gate table, skew advisory) · `scripts/claugentic-session-advisor.py` · `.githooks/pre-commit` · `engine/{audit,build-item,qa,verify}.js` · `README.md` · `CLAUDE.md` · `CHANGELOG.md` · `.claude-plugin/{plugin,marketplace}.json` · `.claude/agents/{retrospect-harvester,honesty-reviewer,product-designer}.md` · `docs/claugentic-WORKFLOW.md` · `docs/claugentic-INVARIANTS.md` · `docs/claugentic-PRODUCT_SPEC.md` · decisions shards (`doc-lifecycle` · `deterministic-gates` · `release-contract`) · `docs/claugentic-ARCHITECTURE_TREE.md` · `tests/*` per slice.

## Research / grounding

- **Files reviewed:** all cited `file:line` above, first-hand-verified for the CI incident (`gh run list`, ci.yml:26, test_frontmatter_parses.py:26); adopter facts from the bounded 3-repo scout (journal: wf_ee5f24f7); collision set from the Stage-3 gate's own verification pass.
- **Harness docs consulted:** deterministic-gates · plugin-distribution · release-contract · doc-lifecycle DECISIONS shards; CLAUDE.md; WORKFLOW DoD; RELEASE_CHECKLIST.
- **Findings:** the condense/budget machinery exists and works — the gap is distribution + seeding + heritability, not invention. The advisor is plugin-resident (`${CLAUDE_PLUGIN_ROOT}`-rooted) so it reaches adopters on plugin update without re-init. Husky-chaining gives teammate propagation for free via npm. The publish order (`release` branch pushed before the tag) is why a naive tag-triggered workflow gates nothing.
- **0040-banked items absorbed:** Slice 4 absorbs `check_doc_budgets.py`'s missing `_repo_root()` anchor + the CWD-coupled `TestClosurePassD` + **the index↔shards agreement pytest (R10 — it becomes the shard-existence guard)**; Slice 10 absorbs the `nsAgent()` bare-name fallback. Only the non-`.md`-in-shard-dir stance **stays banked**.

## Risks & mitigations

- Chained gate blocking innocents → absent-config no-op · glob-no-match skip · report-only grace for day-one-over files · warn-and-pass wrapper lands **before** chaining (sequencing fixed, gate #5).
- Ship-class change breaking release contracts → Slice 6 updates the derived sets, Pass D, the byte-identical shipped-set test, and doctor's per-script-presence rule **together** (gate #3).
- Release workflow double-publish vs `build_release.py` → the script never pushes; the workflow is the only publisher (single writer).
- Dry-run tag poisoning the version-increase guard → dry-run uses a **non-`v*`** tag (`release-dryrun-*`); pushing even that is user-gated (class (c) outward action).
- Copy edits breaking frontmatter/manifest parse → `claude plugin validate --strict` + frontmatter test in each copy-touching slice's AC (model-upheld run until Slice 2 wires it into CI; per-slice AC says which).
- WORKFLOW relocation/condensation losing content → content-preserving diff, git history the archive, verify-panel review.
- **Version-guard inversion under CI-publishes (R4):** the tag now precedes publish, so a red gate run burns the version and leaves a tag — Slice 2 documents the recovery (default: **bump-forward patch + new tag; a tag is never reused**; deleting the failed tag is the exception, itself outward + user-gated), updates the `_version_increase_error` docstring/tests, and owns the "zero side effects" sentence (moved out of Slice 8 — its truth conditions change here).
- **Marketplace-catalog window (R5):** `marketplace.json` lives on main and is bumped at prepare, so a red run leaves an advertised-but-unserved catalog version until re-run — Slice 2 words this honestly ("plugin **content** publishes only on green") and names the recovery.
- Shared surfaces: `skills/init/SKILL.md` → Slices 5 → 7 (that order); **`docs/claugentic-WORKFLOW.md` is the real four-slice surface** — Slices 4 (:175 copy fix) → 6 (DoD gate 4 + adopter note) → 9 (adopter note → intro) → 12 (relocation + renumber + condensation), landed strictly in that order, each rebasing on the prior's landed text (R8).

## Test strategy

Per-fix regression pins (renderOnly string-args · crossModelClaim mixed-run · qa trim/prompt shape · config-driven caps incl. object-form + no-op/skip semantics · wrapper missing-interpreter via PATH-stripped subprocess) in the existing suites; full `python -m pytest` + `node --test` + gate scripts green per slice. **Adopter end-to-end (gate #10):** Slice 7's AC runs `init` against a scratch repo (and one real adopter, read-only inspection) asserting caps seeded · hook chained · husky-repo case · PATH-stripped-python case · report-only grace behavior · skew nudge fires · `/condense` reachable. **Whole-feature closing pass** (WORKFLOW:130) runs on the final slice against the Stage-1 job-to-be-done: "an adopter's harness stays lean, current, and honest without being asked."

## Decomposition (slices) — execution order

**Standing rule for the remaining slices (0041 S5 L8):** any sentence describing a capability a LATER slice wires rides the **future tense** AND carries a **strict-xfail tripwire** naming that slice — this class fired at review in S3, S4, and S5; the authoring rule is what was missing (harvest candidate: a docs-traceability dimension, *forward promises are dated and carry a falsifier*).

- [x] **Slice 1 — CI restored** · **landed, pending merge** (PR #7, green ×5). Slice 2 depends on the merge.
- [x] **Slice 2 — Release formalization (CI-publishes)** *(built e056938 + fixes 850f20d + residual b88d027; Stage-7 PASS round 2; spec's workflow-level `contents: write` AMENDED to read + publish-job-scoped write per the security lens; follow-ons recorded in the verdict block migrate to ROADMAP at plan close)* · test deps → pyproject (CI installs from it) · `release.yml` on `v*` tag: gates + `plugin validate --strict` + built-tree validation → push `release` branch + GitHub Release · `build_release.py` stops before any push; advisory red-CI preflight · **version-guard semantic update + documented recovery: bump-forward patch, never reuse a tag; tag deletion = exception, user-gated (R4)** · **owns the "zero side effects" sentence + the R3 copy sweep: CLAUDE.md release block · RELEASE_CHECKLIST:3/34/47 · release-contract.md:9/:11 · INVARIANTS release wording · CHANGELOG** · **R5 honest catalog wording + recovery** · action-pin Node-runtime bumps (checkout/setup-python) · **publish mechanics named (R11): the workflow publishes by invoking `build_release.py --apply` at the tagged commit — never hand-rolled push logic; `permissions: contents: write`; a lease-equivalent fetch-and-compare before the branch push** · RELEASE_CHECKLIST rewrite · required-checks branch protection documented (user applies) · **the non-`v*` dry-run tag is a post-land, user-gated verification step OUTSIDE this slice's AC** (the slice self-completes without it; R11b).
- [x] **Slice 3 — Currency nudges** *(built ecaa1f4 + fixes aab6822 + R3-a pin; Stage-7 PASS round 2; spec's "truncated honestly" line AMENDED — clauses preserved whole, tightening register, see §Spec-amendment note in the S3 verdict block. Land obligations discharged in the land commit: plugin-distribution audience-split bullet amended in place · CHANGELOG Added entry · CLAUDE.md:23 completeness. Deferred to plan close with the S2 follow-ons (ROADMAP at its budget edge): the advisor-hardening line — git subprocess timeout + matching except · off-switch short-circuit · call-count pin · lead sorted-order pin. PLAYBOOK advisor mention: DISMISSED — each clause names its exact command, self-explanatory)* · advisor + doctor: stamped-docs < installed-plugin → "re-run init"; landed/cold-plan nudge · audience-split honored (nudges = user-facing `systemMessage` only; `additionalContext` stays the resume branch).
- [x] **Slice 4 — Budget script generalization (3a)** *(built b6f17f0 + fixes b2db616/31f690e + R4-a a8c33ef; Stage-7 PASS round 2; spec amendments SA1/SA2 recorded; land: cap bump 12K→14K rung-2 recorded · rationales re-homed · ROADMAP batches landed · plan repairs E1/E2/D-i applied)* · config-driven from `.claude/claugentic-doc-budgets.json` · **this repo's caps migrate to its own config** (shard glob = the sole cap; shard existence = the absorbed index↔shards pytest, R10) · **the new config is classed `init-gen` in DEV_ONLY_PATH_CLASSES in THIS slice + shipped-set test + Pass D confirmation** (R1 — else it ships by default-include at exactly the path init seeds; `init-gen` not `config`, since shipped docs point adopters at the path) · no-op/skip/non-recursive semantics per §Architecture · report-only object form · **WORKFLOW:175 "caps live in the script" copy fix (R7b — this slice falsifies it)** · `_repo_root()` + CWD fix (0040-banked) · tests.
- [x] **Slice 5 — Team wiring** *(built b5db261 + fixes fec4995/ccb4213 + R5-a/b 3926607; Stage-7 PASS round 2; L1-L9 discharged at land — L7 in-delta, L8 the standing rule above, L9 into Slice 6's bullet)* · warn-and-pass wrapper (loud one-line skip on missing interpreter) · **wrapper surfaces non-empty advisory output on exit 0 for both chained gates, quiet on clean runs (R6 — else report-only grace is a silent no-op)** · husky-chain detection in init · teammate bootstrap one-liner · docs-scanner-exclusion caution + DIH Tailwind incident → DECISIONS.
- [x] **Slice 6 — Ship-class + contract sweep (3b)** *(built 40e1540 + fix 2c4e825; Stage-7 PASS round 2; SA1 "payload ≠ delivery" + SA2 recorded; L1–L8 discharged at land — L6 filed in release-contract beside the ship/strip entry, not plugin-distribution [289 B from WARN]; L8 removed `until 0041` from HISTORICAL_RE with its red-first pin)* · reclass to shipped: derived sets + Pass D + byte-identical shipped-set test + doctor gate-table/applicability + reader-contract (**ALL THREE cap forms: int · object/reportOnly · glob-by-key** — doctor's “no glob support” sentence and :87/:89/:95 dead-symbol lines corrected; 0041 S4 round-1 E2) + condense rewiring · **0038-superseding DECISIONS line + the copy sweep grown per R7b — CONTENT ANCHORS, not line numbers (they drifted twice): WORKFLOW's “adopter-owned caps config” rung-2 bullet + its “ships no live copy” parenthetical · doc-lifecycle's DRY-one-cap-source sentence (carries a 0041-S4 SUPERSEDED interim marker to remove) · condense SKILL's schema pointer — mechanics and copy land together** · INVARIANTS candidate: "exactly one cap source per repo" · **doctor rows (0041 S5 L9): an interpreter-health row (run the hook's own probe; report SKIPPED as a flag) + a husky-aware hook-wiring row (the chained-`.husky` third wiring — today filed as a hooksPath conflict)** · split guidance: if the spec exceeds ~15 files, split the doctor/condense rewiring off — never the copy sweep from the mechanics.
- [x] **Slice 7 — init seeding + chaining (3c)** *(built 61822c7 + fix 9e9470c + R1-a 6e51d24; Stage-7 PASS round 2; SA1 solo-anchor + SA2 released-shape recorded; all three tripwires discharged to positive pins — suite 640/0 xfailed; L1–L9 discharged at land)* · seed caps (CLAUDE.md · DECISIONS+extensions · ROADMAP · CHARTER) + heritable glob **excluding managed full-copy docs** + report-only flags for over files · chain into pre-commit after the tree gate (run-both-and-report) · **adopter e2e battery incl. asserting the report-only breach line appears in real commit output (R6)** · **the enforcement-story copy sweep the chaining falsifies** (content anchors: CLAUDE.md's "the one **mechanically enforced** gate is the architecture-tree check" sentence · WORKFLOW's gate-4/adopter-note "run-gate, not hook-wired" clauses · INVARIANTS' only-the-tree-check-is-hook-enforced wording · honesty.md / PRODUCT_SPEC / README enforcement sentences [README's blocks-done shared with S8]) — mechanics and copy land together (relocated from §Approach's pre-split "(Slice 6)" parenthetical, Stage-4 repair 2026-08-14) · both strict-xfail tripwires (hook-chain · init-config-path) flip RED here and are discharged by removing their xfail annotations in the same change · **the hook-wired SET grows here — add the floor-parse pin (0041 S5 harvest, 2026-08-14): assert every gate the wrapper invokes parses under `ast.parse(src, feature_version=<the wrapper's floor>)`. Measured: both hook-wired gates parse clean at 3.7; the repo's one 3.8-syntax script is never hook-wired — that scoping IS the test's contract.** · **DELIVERY (SA2, S6 Stage-7 verdict — measured): init must DELIVER the gate script into the adopter repo — chaining without delivery prints "claugentic gate SKIPPED: <gate> is not in this checkout" on every commit forever; the adopter path must be born prefixed (`scripts/claugentic-check_doc_budgets.py`, per plugin-distribution's born-prefixed rule), so copy-with-rename vs a `--root` override is an S7 design call FOR THE USER (plugin-root invocation is refuted by construction — `_repo_root()` anchors on `__file__`); the S6-created delivery tripwire XPASSes here and is discharged with the other two** · **the seeder caps only files it creates in the same run — a budgeted absent file exits 1 even under `reportOnly`, and INVARIANTS is `recreate-on-demand` (S6 verdict, reliability AC)**
- [x] **Slice 8 — Copy honesty pass** *(built 2df0279 + fix 64ae022; Stage-7 PASS round 2; F1 spec amendment recorded as S8-L3; S8-L1/L2/L3 discharged at land)* · the pre-existing over-claims minus "zero side effects" (re-homed to Slice 2 per R4): README:3 headline · README:57 blocks-done · both manifests' "deterministic architecture enforcement" · /build frontmatter stop claim · validate + frontmatter tests.
- [x] **Slice 9 — Adopter loop closure (fix-pointers)** *(built `386b8bd` after red-first pins `e8e0a06` + fixes `42e21c3` (R1–R12) + `c8eb304` (R13); Stage-7 PASS round 2, conditional on R13 applied as written; amendments A1/A2/A3 recorded as S9-L3; S9-L2/L3 discharged at land, **S9-L1/L4/L5 land OPEN as a stated user decision** — see the discharge block)* · Stage-9 destinations branch by repo type · honesty-reviewer/product-designer carry their premise inline · upstream contribution path named in shipped docs · WORKFLOW adopter-note moved to intro.
- [x] **Slice 10a — the four bounded engine defects** *(tests `bd048d9` red-first + build `120606e` + ROADMAP `e2a6e7c` + Stage-7 fixes `dfd4154` (R1–R10) + `9928e4f` (S10a-L3); round 1 CHANGES REQUIRED **BLOCKING** — the DRY collapse shipped a non-idempotent normalizer that made the QA driver save to one dir while the report cited another, 8 shapes, all created by the slice; round 2 **PASS** on 301,287 measured pairs, 0 divergent. Node suite 372 → **402**. The fix FREED 82 B of ROADMAP. Spec amendment recorded: **a normalizer N call sites collapse onto must be a FIXED POINT**. S10-L1/L2 + S10a-L3 discharged; S10a-L1/L2 open)*
- [x] **Slice 10b — the shared-seam pair (D4 + D6)** *(5 test commits red-first + build `07ad09a` + `12a9754` + Stage-7 fixes `c05d79f` (R1–R12); round 1 CHANGES REQUIRED, applied set measured end-to-end before prescription. Node suite **402 → 461**. The panel fan-out is now bounded — 200 duplicate dimensions produced 203 agent calls, now 4. The blocking finding was COPY: four newly-authored places claimed the honesty judge's two-failure throw "stays loud"; measured, `parallel()` swallows it at HEAD **and at base**, so a trust-surface panel can report PASS having never received the honesty review — and the stated rationale was **inverted** (a guard is what would make it visible). Routed as D7. S10b-L1/L2/L4–L10 discharged; L5/L6/L7 open as ROADMAP items)*
- [x] ~~**Slice 10 — Engine fixes**~~ *(spec'd 2026-08-17, grounded by a 3-reader fan-out; **SPLIT APPROVED 10a/10b** — see the two rows above)* · the five defects + `nsAgent` bare-name fallback · ASCII-only + `|` / ` - ` delimiter constraints carried into spec · regression pins. **10a** = D1 `audit.js` renderOnly-pre-parse · D2 `build-item.js` crossModelClaim · D3 `qa.js` prompt malform + the dead `artifactBase` / 5 inconsistent copies · D5 silent defaults. **10b** = D4 unguarded fan-out + D6 `nsAgent` fallback — **one seam, and D6's failure mode IS D4's trigger**; the only remaining change that can silently corrupt a trust claim (`spawnJudge`'s respawn contract + the same-model disclosure). Five of six defects sit BELOW the testable helpers block, so the spec names the pin technique per defect.
- [ ] **Slice 11 — Eval leak** · neutral tree entries + grep-for-seed-ids test.
- [ ] **Slice 12 — WORKFLOW weight (after the 3-series; scope expanded per R2)** · relocate the 9,793 B condensation body to `/condense` + pointer sweep (`doc-lifecycle.md:6` canonical-home line · DoD · condense/doctor skills) · DoD dual-numbering fix · **content-preserving condensation pass with a hard AC of ≤ 56,000 B** (80% of the 70,000 B cap — measured base 83,383 B, so ~17.6 KB of density work beyond the relocation; the 90% WARN line sits at 63,000 B) · cap at 70,000 B in this repo's config · **whole-feature closing pass** · if one session can't land both halves complete, split 12a relocation / 12b condensation — the cap AND the closing pass land only with 12b.

---

## Review  _(filled by synthesizer-gate in its plan-gate altitude, Stage 3)_

RUNNING AS: Opus 5 (1M context) — a separate clean-context pass on the most capable model; a reduction of rubber-stamping risk, never model-family independence.

- **Verdict:** **CHANGES REQUIRED** — the problem set is real and first-hand-grounded, the path (full pipeline) is right, and the approach (ship the machinery, don't re-invent it) is sound. But **Slice 3 breaks a recorded don't-re-break** and three shipped contracts it doesn't name; **Slice 2's release gate fires after the thing it gates is already published**; the **grace posture hides a user-decision fork**; and **Slice 3 is oversized** for one session. Fix 1–8 before Spec; 9–13 are landable as plan edits.

_(Revision 2 addresses items 1–13: forks resolved by the user 2026-08-12 — 0038 superseded with same-slice sweep · report-only grace · CI-publishes · fix-pointers; Slice 3 split into Slices 4/6/7; sequencing reordered (nudges early, wiring before chaining, WORKFLOW last); honesty rewordings applied; adopter e2e + closing pass added; caps-config version field dropped; ROADMAP line added; 0040-banked absorption named. Full original findings: git history of this file at the Stage-3 commit.)_

_(Revision 3 addresses R1–R8: the caps-config dev-only classing moved into Slice 4 · Slice 12 recomputed from measured bytes and expanded per the user's R2 choice (relocate + condense to 80% of 70 KB) · Slice 2 gains the release copy sweep, the version-guard recovery (bump-forward, never reuse a tag), the re-homed "zero side effects" sentence, and the honest catalog-window wording · the wrapper surfaces advisory output on exit 0 with an e2e assertion · REQUIRED_SHARDS deleted in favor of per-file entries · the four-site schema documentation converges on doctor's reader-contract · shared-surface risk line corrected to the WORKFLOW 4→6→9→12 order.)_

### Re-review verdict (Stage 3, round 2)

RUNNING AS: Opus 5 (1M context) — same clean-context gate role, round 2.

- **Verdict:** **CHANGES REQUIRED** (close). All 13 round-1 items are addressed in intent and the four user forks are resolved coherently — the sequencing fix, the same-slice copy sweep, the 3a/3b/3c split, and the CI-publishes shape are all genuine improvements. But **two of the resolutions introduce new, mechanically-verifiable defects** (R1, R2 — both block a slice from landing green), **the release-shape change repeats the exact copy/contract gap that item #1 fixed for budgets** (R3–R5), and **one grace-posture claim is false as wired** (R6). Every residual is a plan edit plus at most one small user-visible choice (R2); no re-architecture. Round 3 should be short.

**R1 — BLOCKING. Slice 4 ships the harness's own caps by default-include.** `.claude/` is *not* a stripped subtree: only `.claude/settings.json` is classed `config` and `.claude/plans/` is a `DEV_ONLY_DIRS` prefix (`scripts/build_release.py:106,118-120`), and the release's stated policy is **default-include** (`:12-15` — "a forgotten new file ships"). So the NEW `.claude/claugentic-doc-budgets.json` that Slice 4 creates **ships to adopters at exactly the path `init` seeds** — putting the harness's caps (`docs/claugentic-DECISIONS.md` = 3,500 B, the *index* cap) into adopter repos and colliding with Slice 7's seeding. Slice 4 is advertised as "no ship, zero adopter surface"; as written it has the largest adopter surface in the plan. Fix **in Slice 4** (the slice that creates the file): add it to `DEV_ONLY_PATH_CLASSES` with its class (`config`), update the byte-identical shipped-set test, and confirm Pass D treats it as stripped-and-not-adopter-relevant (init creates the adopter's own).

**R2 — BLOCKING. Slice 12's cap arithmetic doesn't hold; the AC is unachievable.** Measured: `docs/claugentic-WORKFLOW.md` = **83,383 B**; the condensation body (DoD gate-4 sub-bullets, lines 166-179) = **9,793 B** (10,484 B including the trailing bullet). Post-relocation ≈ **73,590 B**, not the "~68 KB" the slice asserts — so the **70,000 B cap breaches on landing** and the slice cannot go green. And any cap that clears it (≥ 74 KB) sits the file at 97-99% of budget → **permanent WARN from day one**, i.e. every later WORKFLOW edit is a forced condensation, which is the ceiling-blessing anti-pattern rejected in #4(B) wearing the other face. Pick and state one: **(a)** drop the cap — relocation + pointer sweep only, ROADMAP a later condensation (YAGNI-clean, and it keeps the "no rewrite" non-goal intact); or **(b)** expand Slice 12 to relocation **plus** a real condensation pass landing ~80% of a chosen cap — which changes the stated non-goal and is therefore a **user-visible choice at Stage 5**. Recompute from the measured bytes either way; "the cap bites" (Slice 12) is currently false in the wrong direction.

**R3 — Slice 2 has no copy sweep — the same gap item #1 fixed for Slice 6.** The CI-publishes shape falsifies, at minimum: `CLAUDE.md`'s release block ("ONE command up to a single human-gated push … prints the gated `git tag && push --force-with-lease && push tag`; the tool never tags/pushes") · `docs/RELEASE_CHECKLIST.md:3,34,47` · `docs/claugentic-decisions/release-contract.md:9` ("the `vX.Y.Z` tag is created ONLY at publish … an aborted/declined run leaves **zero side effects**") and `:11` ("force-push stays checklist-gated") · the release-shape wording riding `docs/claugentic-INVARIANTS.md:96-127` · CHANGELOG. Name that list **in Slice 2's bullet** so mechanics and copy land together — the principle the plan already accepted for Slice 6 applies unchanged here.

**R4 — the version-guard semantic inverts, unstated.** `_version_increase_error` is documented as *"tag-anchored — the tag is the last thing actually PUBLISHED … the tag is created only at PUBLISH (never in-build), so a not-yet-published version is > every tag and passes"* (`scripts/build_release.py:287-293`). Under CI-publishes the tag is pushed **before** publish, so the anchor now means *attempted*: a red gate run **burns the version** (`new == latest tag -> REFUSE`) and leaves a tag that is precisely the side effect the old shape guaranteed against. Slice 2 must state the recovery (delete the failed tag — itself an outward, user-gated act — or bump the patch), update the docstring/comment, and update any test pinning that semantic. This also **re-homes the "zero side effects" sentence from Slice 8 to Slice 2** (Slice 8 owns the five *pre-existing* over-claims; this one's truth conditions are changed by Slice 2, so Slice 2 owns it).

**R5 — "Nothing adopters consume exists until CI is green" over-claims by one artifact.** `.claude-plugin/marketplace.json` — the catalog entry `/plugin marketplace add|update` reads — lives on **main** and carries `"version"` (0.5.1 today) while `source.ref: "release"` serves the *content* from the release branch. The local prepare step bumps and commits it to main **pre-tag**, so a red gate leaves the catalog advertising a version the release branch doesn't serve. Either move the manifest bump into the workflow (bump-on-green) or reword honestly — *"the installable plugin **content** publishes only on green; the advertised catalog version is written at prepare time, so a failed run leaves an advertised-but-unserved version until re-run"* — and name the recovery.

**R6 — the report-only grace is silently swallowed by the wrapper as wired.** `.githooks/pre-commit` captures the gate's output (`out=$(…)`) and `[ $? -eq 0 ] && exit 0` — **success output is discarded**. A `reportOnly` breach printed at exit 0 therefore prints *nothing*, making *"the flag makes the debt loudly visible every commit"* (§Approach) false. Slice 5's wrapper work must explicitly **surface non-empty advisory output on exit 0** (for both chained gates, without making the tree gate chatty on a clean run), and Slice 7's e2e battery must assert the report-only line actually appears in real commit output — otherwise the whole grace posture degrades to a silent no-op and the day-one-over ledgers never get their signal.

**R7 — schema + sweep completeness (two items).** **(a)** `REQUIRED_SHARDS` *"driven by a harness-config-only key"* reintroduces the **non-path key** that #13 rejected for `version` — `/doctor`'s reader must then special-case it, and the plan asserts a flat path-keyed map two lines above. State the concrete shape, or delete `REQUIRED_SHARDS` and express shard existence through the **existing per-file cap semantics**: `_check_one` (`scripts/check_doc_budgets.py:202-203`) already fails loud on a missing budgeted file, so listing the ten shards as plain entries in this repo's own config buys the existence guard for free (and the glob entry keeps the cap). **(b)** The 8-location sweep misses the two places that document the **cap source itself**: `docs/claugentic-WORKFLOW.md:175` (*"the harness's OWN caps live in `scripts/check_doc_budgets.py`'s `DOC_BUDGETS` … an adopter has no such script"*) and `:177` (*"the harness ships no live copy"* + the flat-map schema), plus `doc-lifecycle.md:6` (same DRY sentence — only `:5` is listed). Note the split: **Slice 4 alone falsifies the "harness caps live in the script" half**, so the "zero adopter surface" slice owns a copy fix; the "adopter has no such script" half belongs to Slice 6.

**R8 — minor: the stale shared-surface line.** §Risks says *"Slices 5/7/9 all edit `skills/init/SKILL.md`"*, but after the fix-pointers resolution Slice 9 no longer touches `init` (agents + WORKFLOW + shipped docs). The real shared surface is **`docs/claugentic-WORKFLOW.md`, touched by Slices 4 (per R7b), 6 (DoD gate 4 + adopter note :152/:165), 9 (moves that same adopter note to the intro) and 12 (relocates the gate-4 body + renumbers the DoD)** — three of them in the *same two regions*. Sequential landing handles it, but name WORKFLOW as the shared surface with the order 4 → 6 → 9 → 12 and the rebase requirement, and correct the init trio to 5 → 7.

**Answers to the six round-2 checks.** **(1) Split:** yes — Slice 4/6/7 matches the 3a/3b/3c intent; 4 is clean once R1/R7 land, 7 (seeding + chaining + e2e) is right-sized now that 5 carries the wrapper, and **6 is the largest remaining slice** — acceptable for one session, but it is the split-risk one: if its spec exceeds ~15 files, split the `/doctor`+`/condense` rewiring off, **never** the copy sweep from the mechanics. **(2) Interleaving:** the init collision is fixed (5 → 7, non-adjacent); the *unaddressed* collision is WORKFLOW.md — see R8. **(3) Honesty 9a–9e:** 9a, 9c, 9d, 9e are cleanly fixed; **9b is not** (R5), and the revision introduced one new false claim (R6) plus one false quantitative claim (R2's "the cap bites"). **(4) Object form:** updating `/doctor`'s reader-contract in Slice 6 is sufficient *for the reader*, but the schema is documented in **four** places (doctor SKILL:85 · WORKFLOW:175,177 · `doc-lifecycle.md:6` · `condense/SKILL.md:140`) — sweep all of them and point three at one canonical statement (the two-implementation drift surface just doubled); `REQUIRED_SHARDS`-stays-harness-self is **not** coherent as spec'd — see R7a. **(5) 70,000 B:** no — it breaches on landing (R2). **(6) New debt:** R1 (shipped harness caps), R2 (un-landable AC), R4 (inverted guard semantics + a burned version with no documented recovery), R6 (a grace signal that never prints) — each fixable in-plan.

**Nothing in this round re-opens the user's four resolutions** — they are sound; the residuals are their unpropagated consequences. Honesty register: this is a clean-context gate pass on the most capable model, not an independent oracle — R1/R2/R6 are mechanically checkable from the cited `file:line` and byte counts; the sizing judgments are `[J]`.

### Re-review verdict (Stage 3, round 3)

RUNNING AS: Opus 5 (1M context) — same clean-context gate role, round 3.

- **Verdict:** **CHANGES REQUIRED (minor — three sentence-level edits; no forks, no user decisions, no re-architecture).** Six of the eight residuals are genuinely closed, not merely mentioned; **R5 is half-closed** (the fix is in §Risks but the refuted sentence still stands verbatim in §Approach) and **R7a's fold picked a shape that breaks an invariant of the file it edits** — partly my own round-2 phrasing, corrected below. Slice 2 survives its growth. **Conditional pass:** applying R9–R11 as written closes the Stage-3 gate — round 4 needs only a diff confirmation, not another full re-gate.

**Closure check (verified against the repo, not just the plan's own claims).** **R1 CLOSED** — the dev-only classing, shipped-set test and Pass D confirmation now sit in Slice 4, the slice that creates the file; the title's honest downgrade to *"no shipped behavior change"* is correct. **R2 CLOSED** — arithmetic re-derived and it holds: 83,383 − 9,793 = **73,590 B** post-relocation, target 56,000 B = exactly 80% of 70,000, so *"~17 KB of density work beyond the relocation"* is right (17,590 B). The **12a/12b clause is consistent**: 12a alone leaves 73,590 B, *above* the cap, so "the cap lands only with 12b" is the only correct rule and the plan states it. **R3 CLOSED** — Slice 2 names all six copy surfaces. **R4 CLOSED** — the recovery is stated (bump-forward, never reuse a tag; deletion the user-gated exception), the docstring/tests are in scope, and Slice 8 correctly *loses* the re-homed sentence (checked — Slice 8 now lists four, not five). **R6 CLOSED** — the caveat rides §Approach, the wrapper change rides Slice 5, and Slice 7's e2e asserts the line actually appears in commit output. **R7b CLOSED** — four sites converge on `/doctor`'s reader-contract, split correctly across Slices 4 (:175) and 6 (:177 · `doc-lifecycle.md:6` · `condense/SKILL.md:140`). **R8 CLOSED** — both orders corrected and they match the slice bullets.

**R9 — R5 is only half-closed: the refuted sentence is still in §Approach.** §Risks:81 states the honest version, but §Approach's release-shape bullet still asserts verbatim *"Nothing adopters consume exists until CI is green."* The plan therefore contradicts itself, and the false half sits in the section a Stage-4 spec reads first — in a plan whose subject is copy that outruns mechanics. Reword **in place** (e.g. *"the installable plugin **content** publishes only on green; the catalog's advertised version is written at prepare time — see §Risks for the window and its recovery"*), and keep §Goals:29 as is (it already says "only then publishes," which is accurate).

**R10 — R7a's fold creates double measurement and a second cap source (my round-2 wording was the proximate cause; here is the clean shape).** §Architecture:54 keeps **both** the ten shards as per-file entries **and** the glob entry — so every shard is resolved twice, and `check_doc_budgets.py:83-90` documents precisely this as the thing not to do: *"an EXISTENCE guard ONLY — **never a second cap** (the glob entry is the one and only home of the shard budget), so a deleted shard produces **exactly one message, not two**."* As spec'd, an over-cap shard prints twice and its cap has two hand-maintained homes — the DRY breach the same slice's INVARIANT candidate ("exactly one cap source per repo") forbids. **Resolution: the glob stays the sole cap; shard *existence* moves to a pytest** — which is exactly the **0040-banked "index↔`REQUIRED_SHARDS` agreement test"** that §Research:70 currently leaves banked. Absorb that banked item into Slice 4, assert the index's routing lines all resolve to existing files, delete `REQUIRED_SHARDS` **and** the per-file shard entries. Net effect: one cap source, one message, existence still guarded, one banked item cleared, less code than the current plan.

**R11 — Slice 2: name the publisher and settle whether the dry-run proof is in-slice.** Two completeness gaps in the grown bullet. **(a)** *"the workflow is the only publisher"* never says **how** it publishes: state that `release.yml` invokes **`build_release.py --apply` (no `--bump`) at the tagged commit** and pushes the resulting branch — with `permissions: contents: write` and a lease-equivalent guard (CI cannot hold the human's `--force-with-lease`, so fetch-and-compare `origin/release` before pushing). Without this line the implementer may hand-roll `git push --force` in YAML, forking the build logic §Architecture:53 explicitly forbids. **(b)** The dry-run tag is a class-(c) outward action needing the user: say whether it is **inside** the slice's acceptance criteria (a legitimate mid-slice stop the implementer must announce, not stall on) or a **post-land** user-gated verification. As written, "lands complete in one session" is ambiguous for the one slice that depends on a human act.

**Advisory (not blocking — carry into Spec, no plan edit required).** (i) The caps-config's dev-only **class should be `init-gen`, not `config`**: `build_release.py:89` defines `config` as *"repo machinery **no shipped doc points an adopter at**"*, and after Slice 6 three shipped docs point adopters at that exact path; `init-gen` (init generates the adopter's own) is the accurate attestation — Slice 7 is what makes it true, so note the ordering. Pass D would catch a wrong pick, which is why this is advisory. (ii) Slice 12b's AC should carry a **hard byte number (≤ 56,000 B)**, not "~56 KB" — and note the WARN line at **63,000 B** so a 12b that lands at, say, 64 KB isn't green-but-instantly-warning. (iii) If 12 splits, the **whole-feature closing pass rides 12b** (the final sub-slice), not 12a.

**Sizing (round-3 re-check).** **Slice 2 still lands in one session** — ~11 files, but only one is a behavior refactor (`build_release.py` stop-before-push + guard semantics + its tests); the rest are a new 60-line workflow and bounded copy edits with named line anchors. It is now the second-largest slice behind Slice 6, and both stay within a 1M-context session. The R11(b) answer is what actually determines whether it can *self*-complete. **Slice 4 shrinks** under R10 (fewer entries, one absorbed test). No other slice moved.

**Honesty register:** R9/R10 are mechanically checkable from the cited `file:line` and the byte arithmetic above (re-derived this round, not carried over); the sizing calls are `[J]`. This remains a clean-context gate pass on the most capable model — a reduction of rubber-stamping risk, never an independent oracle.

### Re-review verdict (Stage 3, round 4 — diff confirmation)

RUNNING AS: Opus 5 (1M context) — same clean-context gate role, round 4.

- **Verdict:** **PASS.** Diff `002c711..5a7f696` confirmed line-by-line against the round-3 prescriptions: R9, R10, R11(a)+(b) and all three advisories are applied as written — no scope drift, nothing else touched.

**Confirmation, item by item.**

- **R9 ✔** — §Approach's release bullet now reads *"The installable plugin **content** publishes only on green; the catalog's advertised version (`marketplace.json` on main) is written at prepare time — see §Risks for that window and its recovery."* The refuted absolute is gone, the content-vs-advertised split matches §Risks, and §Goals:29 is correctly untouched.
- **R10 ✔ — and coherent in all three places it had to change:** §Architecture (*"the glob stays the SOLE cap … never per-file shard entries beside it"*), §Research (the index↔shards pytest moves from *banked* to *absorbed*; only the non-`.md`-in-shard-dir stance stays banked), and Slice 4's bullet (*"shard glob = the sole cap; shard existence = the absorbed index↔shards pytest"*). One cap source, one message per file, existence still guarded, one banked item cleared.
- **R11(a) ✔** — Slice 2 names the publish mechanics exactly: `build_release.py --apply` at the tagged commit, never hand-rolled push logic, `permissions: contents: write`, lease-equivalent fetch-and-compare before the branch push. **R11(b) ✔** — the non-`v*` dry-run tag is now explicitly a **post-land, user-gated step outside the slice AC**, so Slice 2 self-completes in one session.
- **Advisories ✔** — (i) the caps config is classed **`init-gen`** with the rationale inline (correct against `build_release.py:89`, which reserves `config` for machinery *no shipped doc points an adopter at*); (ii) Slice 12 carries the hard **≤ 56,000 B** AC and notes the **63,000 B** WARN line, and its "~17.6 KB of density work" matches the measured delta (73,590 − 56,000 = 17,590 B); (iii) the closing pass now rides **12b** on a split, alongside the cap.

**Housekeeping applied in this edit (no content changed).** My round-3 block had been appended directly after round-2's R5, wedging it between R5 and R6 — my own anchoring error, not the planner's. The Review section is now back in chronological order: round 2 complete, then round 3, then this round.

**Stage-3 gate, final.** Correct & sound (SOLID/patterns respected; no fight with established conventions) · every slice **session-sized and landing complete** — Slices 2 and 6 are the ceiling and both fit, with Slice 6 carrying its own split guidance · full-pipeline path was the right call · risks and test strategy adequate, including the adopter end-to-end battery and the whole-feature closing pass · *Architecture & holistic fit* genuinely reasoned rather than hand-waved or gold-plated · harness impact named (the superseding DECISIONS lines, the INVARIANTS candidate, the managed-doc sweep). **The plan may proceed to Stage 4 (Spec).**

**Honesty register (final).** What this gate did: four clean-context adversarial passes on the most capable model, each **re-deriving the load-bearing facts from the repo** — byte counts, ship classes, publish order, wrapper semantics — rather than trusting the plan's own account of them. That is a **reduction of rubber-stamping risk, never an independent oracle**, and never model-family independence. A PASS means *the plan clears the Stage-3 bar on the dimensions reviewed*; it is not a prediction that the slices implement cleanly — Verify remains the gate on the code. Two things stay explicitly **model-upheld** and must not be re-described as mechanical downstream: the caps **seeding and wiring** (a prose `init`, exactly like the tree gate) and the **shrink** that the report-only flag only makes *visible*.

---

## Spec  _(per slice, after Review passes — Stage 4)_

### Slice 2 — Release formalization (CI-publishes)

- **In plain English (shown first at the approval gate):**
  - **What this builds:** releasing becomes *prepare locally → you push one tag → CI does everything else*. `build_release.py --apply --bump X.Y.Z` still prepares and validates on your machine and STOPS; your single human act shrinks to `git tag vX.Y.Z && git push origin main vX.Y.Z`. The tag triggers a workflow that re-runs every gate (pytest both OSes · node tests · the four gate scripts · `claude plugin validate --strict`), rebuilds the stripped release tree at the tagged commit, pushes the `release` branch, and creates a GitHub Release. **If any gate is red, nothing publishes.** Test dependencies move into `pyproject.toml` so the pyyaml class of silent CI break is structurally closed.
  - **What "done" means for you:** a release can only reach adopters through a green workflow run; the release docs/copy tell exactly that story; the whole thing is pinned by tests.
  - **What you're accepting (risks / trade-offs):** offline publishing is retired (CI is the only publisher) · a failed run burns a version number — recovery is bump-forward to the next patch, a tag is never reused (deleting the failed tag is a documented, user-gated exception) · the catalog version on main is written at prepare time, so a failed run leaves an advertised-but-unserved version until you re-run (documented honestly) · the workflow gets `contents: write` (minimum needed to push the branch + create the Release) · required-checks branch protection is a GitHub-settings step only you can apply (documented for you).
- **Files & changes:**
  - `pyproject.toml` — PEP-735 `[dependency-groups] test = ["pytest", "pyyaml"]`.
  - `.github/workflows/ci.yml` — both pytest jobs install via the dependency group (single source of truth); action pins bumped off the deprecated Node-20 runtime (implementer verifies current majors).
  - `.github/workflows/release.yml` (NEW) — `on: push: tags: ['v*']` · `permissions: contents: write` · job `gates`: checkout tagged commit (`fetch-depth: 0` — ancestry + tag anchors need history), full suite + gate scripts + `claude plugin validate --strict` + assert tag == `v` + plugin.json version at that commit · job `publish` (`needs: gates`): `python scripts/build_release.py --apply` at the tagged commit, lease-equivalent fetch-and-compare, push `release`, `gh release create` with notes from the CHANGELOG entry. Never hand-rolled build logic.
  - `scripts/build_release.py` — docstring rewritten to the new flow · printed gated command becomes tag-push only · `_version_increase_error`: **equal is allowed iff the `v<new>` tag points at HEAD** (the CI-time case; prepare-time semantics unchanged), docstring documents the burned-version recovery · advisory red-CI preflight (`gh` check on origin/main; warn-only, silent-skip when `gh`/network absent — labeled advisory) · "zero side effects" wording corrected (aborted prepare = no tag/no push; bumped manifests + local branch remain).
  - `docs/RELEASE_CHECKLIST.md` — rewritten to the new ritual; Honest-scope section updated (what CI now guarantees vs what stays model-upheld: the eval-drift check, the catalog window).
  - Copy sweep (R3): `CLAUDE.md` release block · `docs/claugentic-decisions/release-contract.md:9/:11` · `docs/claugentic-INVARIANTS.md` release wording · `CHANGELOG.md` unreleased entry describing the shape change.
  - `docs/claugentic-ARCHITECTURE_TREE.md` — `release.yml` (+ new test file) entries.
- **In-scope standards dimensions:** `reliability-resilience` (red-run / absent-`gh` / lease-race postures) · `security` (minimal workflow permissions, pinned actions) · `docs-traceability` (claims match wiring after the sweep) · `testing` (semantic-change pins).
- **Tests to add:** version-guard pins for equal-allowed-iff-tag-points-at-HEAD (+ prepare-time unchanged) · printed-command pin updated · a static `release.yml` sanity test (YAML parses; invokes `build_release.py --apply`; `needs: gates`; permissions exactly `contents: write`) · CI-install-from-group asserted by the existing collection behavior (removing pyyaml from the group must fail loud).
- **Acceptance criteria:** full pytest + node + four gate scripts green · `claude plugin validate --strict` green (model-upheld this slice; mechanized for every future release by the workflow) · grep finds zero occurrences of the swept release-story phrases · tree + doc budgets green · dry-run tag and branch protection explicitly OUT (post-land, user-gated / user-applied).

### Slice 2 — Verify verdict (Stage 7)

**RUNNING AS: Opus 5** (separate clean-context pass on the most capable model — a reduction of rubber-stamping risk, never a guarantee; same model family as the builder, so model blind spots are NOT independent).

**Verdict: round 1 — CHANGES REQUIRED · round 2 (delta, `e056938..850f20d`) — PASS, conditional on one single-clause doc sweep (see *Round 2* at the end of this block).**

**Round 1 — CHANGES REQUIRED.** Reviewed `5374e72..feat/0041-s2-ci-publishes` (e056938) as synthesizer over a six-lens panel (yagni · security · honesty · docs-traceability · reliability-resilience · testing; all six ran). The load-bearing core is right — one build path at two call sites, `needs: gates`, the leased push, the equal-at-HEAD guard inversion, the advisory that provably cannot block, gate parity in intent, the validate-the-BUILT-tree split. Three clusters block landing: (a) the workflow **grants** more authorization than the copy's story assumes and than the gates job needs; (b) the **honest register** is wrong on the two claims this slice exists to make ("only publisher", "a red run publishes nothing"); (c) four of the new YAML/advisory pins assert **string presence, not the refusal they name** (mutation-evidenced: 9 of 13 hand-mutants survived).

**Spec amendment (the lens is right, the spec was wrong):** the spec pinned "`permissions` exactly `contents: write`" and the implementation matched it faithfully — but workflow-level write also arms the `gates` job, which executes the full suite plus an unpinned global npm install with a write-capable token. The spec line is amended to: **workflow-level `contents: read`; `contents: write` only on `publish`.** Also amended: "Tests to add" gains the scoped-to-the-owning-step requirement (a presence assertion is not a pin), and `security`'s in-scope note gains *tag-commit authorization*, which the spec did not name.

#### Required changes (one pass, in priority order)

**Tier 1 — authorization (the slice's whole point).**
1. **Least privilege by job** (`.github/workflows/release.yml:25-26`). Workflow-level `permissions: contents: read`; add `permissions: contents: write` to the `publish` job only. Retarget the two tests that currently *forbid* the correct shape: `tests/test_release_workflow.py::TestPermissions::test_exactly_contents_write` → workflow-level `{"contents": "read"}`; `::test_no_job_widens_the_token` → `publish` is `{"contents": "write"}` and `gates` declares no `permissions` key. Optional, not required: `persist-credentials: false` on the gates checkout — with a read-only token on a PUBLIC repo it buys little.
2. **Require the tagged commit to be on `main`** (`release.yml`, gates job). The existing guard is *reverse* inclusion (`_missing_upstream_commits`: `origin/main` ⊆ HEAD, `scripts/build_release.py:446-461`) — a never-merged descendant passes every gate and publishes. Add, before anything can publish: fetch `main`, then `git merge-base --is-ancestor "${GITHUB_SHA}" refs/remotes/origin/main || { echo "::error::…"; exit 1; }` (`shell: bash`, Linux leg is enough). Equivalent placement in `publish` immediately after the existing origin/main fetch is acceptable — the invariant is *fails loud before any remote write*. Add one scoped shape test (per #7's style). This is what makes RELEASE_CHECKLIST:31's "`main` rides along" mechanical instead of habitual.

**Tier 2 — the honest register (verified against live GitHub state).**
3. **The "only publisher" claim is human-upheld, not mechanical.** Confirmed this session: `branches/release/protection` → *404 Branch not protected*; `rulesets` → `[]`. Fix all four sites in one pass — `docs/RELEASE_CHECKLIST.md:45` (move out of "What CI now guarantees" / qualify + add to the model-upheld list) · `docs/claugentic-decisions/release-contract.md:11` ("never a human's" → "no human command *in the flow* pushes it — model-upheld, since `release` is not branch-protected") · `CHANGELOG.md:26,:37` · `docs/claugentic-INVARIANTS.md:143` (resolve the dangling conditional: "…the gate is advisory — **model-upheld until `release` is branch-protected**"). Add a *restrict pushes to `release`* bullet to RELEASE_CHECKLIST's Branch-protection section — that bullet is what would convert the claim into a real guarantee.
4. **"A red run publishes nothing" is false in one window.** The branch push (`release.yml:166-179`) is the second-to-last step; `gh release create` (:180-183) is the last — a run that fails *at* the Release step has already served the content. Fix `docs/RELEASE_CHECKLIST.md:31,:37` + `docs/claugentic-INVARIANTS.md:143`, and repair the recovery section that is built on the false form: (a) **re-run the failed run FIRST** — it is safe by construction for the whole transient class (checkout resolves `github.sha`, the equal-at-HEAD guard admits the rebuild, the lease re-snapshots), so a registry blip must not cost a version; (b) if the run died *after* the branch push, re-run or `gh release create` by hand — do **not** bump forward; (c) make the last step idempotent so (a) is true end-to-end: `gh release view "$GITHUB_REF_NAME" >/dev/null 2>&1 || gh release create …`.
5. **Two precision fixes in the same pass.** `CHANGELOG.md:39` "every CI job installs from it" → "every job that runs pytest" (only that job does). And the aborted-prepare line's "**both** `git checkout`-able" (`scripts/build_release.py` module docstring + `CHANGELOG.md`) is false for the local `release` branch — its ref is force-reset, not restorable by `git checkout`; say the manifests are `git checkout`-able and the branch is simply rebuilt next run.

**Tier 3 — pins that do not pin (all mutation-evidenced).**
6. **Scope the two YAML assertions to the owning step and assert the refusal, not the vocabulary.** Deleting the lease comparison+refusal (`release.yml:171-174`) leaves the suite 26/26 green (the snapshot step still contains `git ls-remote …` and `RELEASE_LEASE_SHA`); deleting the tag↔manifest refusal (`:78-81`) likewise (the surviving `echo` carries both asserted strings). Add a `_step_run(job, name_fragment)` helper and assert, inside the owning step, both the comparison **and** its `exit 1`.
7. **Derive gate parity instead of hand-typing it** (`tests/test_release_workflow.py:175-188`) — convergent finding (yagni TRIM-3 + testing F2, opposite motivations). Today a fifth gate added to `ci.yml` only ships silently. Extract `gate_scripts(job)` by regex and assert `gate_scripts(ci.gates) ⊆ gate_scripts(release.gates)`; keep the current four as an explicit floor.
8. **Pin the CHANGELOG↔awk heading contract** (testing F3): assert the workflow builds the heading `## ${version}` **and** that `CHANGELOG.md`'s release headings match `^## \d+\.\d+\.\d+$`. A heading-style drift is otherwise found only after the tag is spent. (Portable — no `awk` on the Windows leg.)
9. **Advisory tests: fewer, stronger** (`tests/test_build_release.py:812-883`) — the reconciliation of testing F4 (add) with yagni TRIM-2 (cut); they are not in conflict. **Add:** have the fake capture the call and assert `timeout=_CI_ADVISORY_TIMEOUT_S` and `--branch main` (both currently deletable with the suite green), plus a non-list payload and a non-dict-first-element case (today an API error dict raises `KeyError` *inside* `_apply` — crashing the build the advisory is documented never to affect). **Cut:** `test_it_never_blocks_it_only_returns_a_string` (tautological) and collapse the five one-bit silent-skip cases into one parametrized case. Net: fewer tests, more killed mutants.
10. **One negative assertion** (testing F5): `if pending:` → `if True:` survives — the UNCOMMITTED banner is never proven *absent* on the clean-tree publish path, where it would be false instruction in CI logs. Add `assert "UNCOMMITTED" not in out` to the publish-time rebuild test (the suite's own convention).
11. **Restore hermeticity** (testing F7): 14 of 15 `_apply` integration invocations now shell out to ambient `gh` (this slice introduced the call) — with `GH_REPO` exported that is real API traffic at 20s per call, against a fixture that claims hermeticity. Add an autouse `gh`-patch fixture on the `_apply` integration classes; keep the one explicit override.
12. **Exercise the annotated-tag peel** (testing F6 + reliability R-6): the `^{commit}` peel is the mechanism the equal-at-HEAD relaxation rests on and every real-git test uses lightweight tags — add one integration case with `git tag -a`. In the same edit widen `_tag_points_at_head`'s `except subprocess.CalledProcessError` to `(subprocess.CalledProcessError, OSError)`, which makes its "any git error → False" docstring true for free (`scripts/build_release.py:302-316`).

**Tier 4 — leanness, cheap robustness, doc accuracy.**
13. **Drop the action-pin registry; state the accepted risk instead** (yagni CUT-1 + security S-4 + testing F8, one coherent act). Delete `MIN_ACTION_MAJORS` and both `TestActionPins` tests (`tests/test_release_workflow.py:41-45,220-244`): they can only fail on an act nobody performs, the floor goes stale silently, `assert floor is not None` turns the map into a mandatory registry, and `dict(findall)` collapses repeated actions so the half-bump test is half-blind anyway. Keep the v7 bumps. Add **one line** recording that first-party `actions/*@vN` mutable majors are an accepted risk (not SHA-pinned) — Honest scope or `docs/claugentic-DECISIONS.md`. `.github/dependabot.yml` (`github-actions` ecosystem) → ROADMAP. Update the `tests/test_release_workflow.py` tree entry, which currently advertises "action-major floors".
14. **Adjudicated (reliability R-4 vs yagni): add the prepare-time CHANGELOG check — as a PRINT, not a refusal.** yagni is right that a hard gate here is disproportionate and would fight the flow (a `## Unreleased` heading is legitimately unrenamed when prepare runs, and renaming it dirties the tree the clean-tree precondition guards). Reliability is right that the likeliest human omission must not be detected only *after* the version is spent. Both are satisfied by the shape this diff already invented: extend the `_uncommitted_manifests` pre-tag print block with "BEFORE YOU TAG: `CHANGELOG.md` has no non-empty `## <version>` section — the publish job will refuse." ~8 lines, two tests (missing → message, present → silent), zero flow surgery; the workflow keeps the hard fail-loud.
15. **Bound the workflows' wall clock** (reliability R-5): no `timeout-minutes` anywhere, so unbounded network I/O rides GitHub's 6-hour default — while the Python half of this same slice bounded its call at 20s. Add `timeout-minutes: 25` (gates) / `15` (publish). No retry, no pin test.
16. **One `_dirty_paths(root)` helper** (yagni TRIM-5): `_bump_only_dirty` and the new `_uncommitted_manifests` are now two `git status --porcelain` parsers in one file (`scripts/build_release.py:526`, `:560`). The new behavior is earned; the second parser is not.
17. **Two deterministic doc corrections** — the first is an AC item, not hygiene ("grep finds zero occurrences of the swept release-story phrases" is currently **not** met): `docs/claugentic-ARCHITECTURE_TREE.md:31` still indexes the *pre-slice* RELEASE_CHECKLIST ("the human push" + "the one-time `v0.3.1` bootstrap tag" — a section this diff deleted; `grep -c bootstrap` → 0), misdirecting the read-first index into the retired story. And `docs/claugentic-INVARIANTS.md:112-113` misattributes provenance: `check_shipped_content.py` has run in `ci.yml` since 0028 S3 (verified at the base commit) — only the *tagged-commit* half is new; re-wrap the orphaned ":114 mechanically pins" line.
18. **State the floating CLI install** (security S-3): `npm install -g @anthropic-ai/claude-code` (`release.yml:87,:154`) resolves latest at runtime and runs lifecycle scripts — at `:154` inside the job holding the publish credential — and this is stated nowhere (grep-proved), while RELEASE_CHECKLIST:45 lists strict-validate under "What CI now guarantees". One line in Honest scope + a ROADMAP line. **Not** pinning it is the deliberate choice (a stale validator pin is worse validation than a floating one) — so say so.

#### Explicitly dropped (with reasons)

- **yagni CUT-4 (`fetch-depth: 0` on gates)** — superseded: required change #2 puts an ancestry check in that job, which genuinely needs history. Keep the all-jobs `test_full_history_is_fetched` assertion.
- **yagni TRIM-6 (`test_pyproject_declares_the_group` hard-codes "pyyaml")** — the assertion isn't wrong, and removing pyyaml from the group *should* trip a test. Not the same class as TRIM-3, whose list cannot catch the drift it exists for.
- **yagni OPTIONAL-7 (duplicate CLI install/validate site)** — failing before the `publish` job even starts is worth the runner minute; the two sites validate two different artifacts.
- **reliability R-7 (remove the validation worktree)** — ephemeral GitHub runners are the deployment; self-hosted is not.
- **security's `git ls-remote … | head -n1` hardening** — spot-checked: both failure modes of a shadowing ref are LOUD (a malformed multi-line `GITHUB_ENV` write, or a lease mismatch that rejects the push). `head -n1` trades one loud failure for another and could silently select the shadow ref. No path to a bad publish.
- **security S-3's *pinning* half + version bounds on `test = ["pytest", "pyyaml"]`** — pin treadmill costs more than it buys here; a broken dep fails loud in `ci.yml` on `main` first, and the advisory surfaces it before a tag. Required change #18 keeps the honesty half.
- **honesty HF6, broad half ("worst case as the only case")** — the copy errs *conservative* (it over-states its own side effects), which is not an overclaim. Only the false `git checkout`-able clause is required (#5).
- **testing F8's `_patch_gh` process-wide patch / `_run` seam** — tidiness refactor; #11 closes the live risk.
- **reliability R-5's npm retry + a `timeout-minutes` pin test, and the publish-job advisory guard** — YAGNI; the advisory is already proven unable to block.
- **The `concurrency:` group** — the lease makes two parallel tag runs safe-and-loud, which is the property that matters; serializing changes semantics for no demonstrated need. ROADMAP at most.
- **`.github/dependabot.yml` itself, and the tag/environment ruleset** — real, but separate work: ROADMAP + a user-applied GitHub setting, not this slice.
- **docs-traceability DT1's optional half (naming the new test groups at tree `:119`)** — nothing dangles; #13 already forces the adjacent `:120` edit.

#### Definition of Done

- **Acceptance criteria:** suites green on the branch (110/110 pytest, 26/26 node — panel-run) ✔ · `claude plugin validate --strict .` green (re-run this session against an equivalent tree; this diff touches no manifest, skill, or agent frontmatter) ✔ · tree + doc budgets green ✔ · dry-run tag + branch protection correctly OUT ✔ · **"zero occurrences of the swept release-story phrases" — NOT met** (required change #17, tree `:31`).
- **In-scope dimensions:** `security` GAPS (#1, #2, #18) · `docs-traceability` GAPS (#17, and #3/#4 as claim-vs-wiring) · `reliability-resilience` GAPS (#4, #12, #14, #15) · `testing` GAPS (#6–#12). `yagni` PROPORTIONATE (~14 of 52 test units are padding; #9/#13 remove it).
- **Dimension the spec missed:** tag-commit authorization ("what makes this commit releasable") — no gate covered it; folded into `security` above.
- **Test-first audit:** N/A — this work did not choose test-first (no charter in this repo, and the slice did not record a per-work choice), so no independent test-author spawn is expected and none is claimed.
- **Whole-feature pass:** N/A — Slice 2 of 8; the cross-slice end-to-end check belongs to the closing slice.
- **New tech debt:** none accepted. Everything above is fixed in-slice or explicitly routed to ROADMAP (#13 Dependabot, #18's pin question, branch-protection settings).

**Plain-English summary (round 1).** The machinery works and the shape is right — a tag triggers a run, the gates run before anything ships, and the one build path is the tested one. Three things stop it landing. **First, the workflow hands a write-capable token to the job that runs the test suite and installs a package from the internet** — nothing exploits that today, but the whole point of this slice is that publishing is trustworthy, and it's a two-line fix. **Second, nothing checks that the commit you tagged is actually on `main`** — tag any branch, anywhere, and it publishes; the docs treat "push main with the tag" as the safeguard, but it's only a habit. **Third, the docs claim two things that aren't true yet:** the `release` branch has no protection (verified live — anyone with push rights can still write it by hand), and "a red run publishes nothing" is false in the narrow window where the branch push succeeded and the GitHub Release step failed — where the documented recovery (bump forward) is also the wrong advice. Underneath, several of the new tests check that the workflow *mentions* a safety check rather than that it *performs* one: delete the concurrent-publish guard or the tag-versus-version refusal and the suite still passes green. None of this is architectural rework — it is roughly a half-day of small, surgical edits, most of them one or two lines.

#### Round 2 (delta) — `e056938..850f20d`, 10 files, +426/−166

**Round 2 (delta): PASS** — all 18 required changes verified met in the diff, not merely reported. Conditional on **one single-clause doc sweep** (R2-a below), which needs no further gate round.

**Spot-checked rather than taken on report** (the four I said I would doubt, plus the leanness cut): **#1** — `release.yml:26` is now `contents: read`, `publish` carries `permissions: contents: write`, and both tests were genuinely *inverted*, not deleted (`test_workflow_default_is_read_only` asserts equality on `{"contents": "read"}`; `test_only_publish_holds_the_write_grant` asserts the grant on `publish` **and** its absence on `gates` — the old form forbade exactly this shape). **#6** — the new `_step_run(job, name_fragment)` fails loud on 0-or-2 matches, and the three scoped assertions name the literal construct a mutant would delete (`if [ "${now}" != "${RELEASE_LEASE_SHA}" ]`, `if [ "v${version}" != "${GITHUB_REF_NAME}" ]`, `git merge-base --is-ancestor "${GITHUB_SHA}" refs/remotes/origin/main`) **plus** `exit 1` — a warn-instead-of-refuse mutation now fails. **#9** — the payload guard reads `payload[0] if isinstance(payload, list) and payload else None`, with the API-error-object and list-of-non-objects cases now parametrized; *correction to my round-1 wording*: the shipped code was already guarded, the `KeyError` was the panel's **mutant**, not a live defect — the real gap was the missing test, which is what landed. **#3/#4** — all four honest-register sites land on the right register: the one-publisher claim is **out of** "What CI now guarantees" and into the model-upheld list with the live evidence inline (`404 Not protected`, `rulesets → []`), `release-contract.md:11` reads "no human command *in the flow* pushes it — model-upheld, since `release` is not branch-protected", `CHANGELOG` says "retired as a practice, in the documented flow … a maintainer with push rights can still write it directly", and INVARIANTS gained an **"Enforcement, stated exactly"** clause splitting the tooling half (pinned) from the branch half (model-upheld). **#13** — `MIN_ACTION_MAJORS`, `_USES_RE` and both `TestActionPins` cases are gone, the v7 pins stayed, and the accepted risk is recorded in Honest scope beside the floating-validator line. Independently re-verified: `docs/claugentic-INVARIANTS.md` is 12,732 B against its 20K cap, and `CLAUDE.md:22` now states the flow, the re-run-first recovery, the after-the-last-step exception, and the unprotected-branch caveat accurately in one line.

**The four flagged judgment calls — all four sustained.** (1) *Subset `ci ⊆ release` + `GATE_FLOOR`, not equality* — correct, and what the requirement asked for: equality would be **wrong** (release legitimately runs `claude plugin validate`, the on-main check and the tag↔version check that `ci` does not), and the frozen four close the reverse-loss hole the subset alone leaves. (2) *On-main check on the Linux leg only* — sound: the fact is OS-independent and `publish` needs **every** matrix leg green, so the Linux leg alone is sufficient; same posture as the pre-existing Linux-only `claude plugin validate` step. (3) *`git worktree add --detach` still allowed* — correct scoping: that site builds the **validation** worktree, and the forbidden list still catches every way a YAML re-implementation could actually publish (`git rm`, `--force`, and `git commit -` which prefix-matches `-m`/`--message`; a flagless `git commit` needs an editor and dies in CI). (4) *Heading non-vacuity via "contains a semver"* — correct and non-vacuous in both directions: `## Unreleased` / `## Prior versions` are properly excluded, every drift form that names a version (`## [0.5.2] - 2026-08-12`, `## v0.5.2`) fails the `fullmatch`, and a wholesale drift to version-free headings trips the `assert versioned` guard instead of passing silently. Declining `persist-credentials: false` is within what round 1 allowed (public repo + a now read-only token).

**Residuals.**
- **R2-a (must sweep before land, one clause).** `docs/claugentic-decisions/release-contract.md:12` still describes the workflow as "`permissions: contents: write`" — the pre-fix, spec-level shape. Fix #1 made that inaccurate, and the sibling bullet at `:11` in the same file was updated while this one was not. Reword to "workflow-level `contents: read`, `contents: write` on the `publish` job alone". This is the ledger, so claim-vs-wiring matters more here than anywhere; it is a one-clause edit and needs no re-review.
- **R2-b (optional, ROADMAP-able).** Neither the on-main check nor the Linux-only `claude plugin validate` step is protected against the matrix losing `ubuntu-latest` — both would silently vanish with the suite green. One assertion that `gates`' `matrix.os` contains `ubuntu-latest` would close it. Pre-existing exposure for the validate step; not introduced by this round.

**Definition of Done (round 2).** Acceptance criteria now all met — including the previously-failing "zero occurrences of the swept release-story phrases" (`ARCHITECTURE_TREE.md:31` rewritten to the CI-publishes ritual, `:120` re-described). In-scope dimensions: `security` MET · `docs-traceability` MET but for R2-a · `reliability-resilience` MET · `testing` MET (every required pin now asserts the refusal inside its owning step) · `yagni` MET (net test count fell where it should and rose only where mutants survived). No new tech debt; the round-1 drop list was honored with nothing implemented from it.

### Slice 3 — Currency nudges (the plugin-resident vector)

- **In plain English (shown first at the approval gate):**
  - **What this builds:** the SessionStart advisor — which already ships inside the plugin and reaches your three live repos on a mere `/plugin update`, no re-init needed — learns two new things to tell you (never the agent): **"your harness docs are behind the installed plugin — re-run init"** (the version-skew all three of your deployments have right now, which DistrictSync hand-documents workarounds for instead of fixing), and **"N landed/cold plans are sitting in .claude/plans/ — run doctor to sweep"** (the lifecycle drift all three repos show, up to AskBase's 81 plan files). `/doctor` gains the same skew check in its diagnose report.
  - **What "done" means for you:** open any adopter repo after updating the plugin and the session's first line tells you the docs are stale and how to fix it — the answer to "how do I keep all my projects current" becomes something the harness volunteers instead of something you have to remember.
  - **What you're accepting:** two more one-line nudges in the session banner (still capped, still mute-able with `CLAUDE_HARNESS_ADVISOR=off`; appended to the existing single line, truncated honestly if over the cap) · "cold" is defined as an in-flight plan untouched for 30+ days (a constant, not configurable — YAGNI) · the skew read is best-effort: a missing/malformed plugin.json or fence silently skips (advisor stays fail-safe-to-silent).
- **Files & changes:**
  - `scripts/claugentic-session-advisor.py` — `_read_installed_version()`: the plugin's own `.claude-plugin/plugin.json` located relative to `__file__` (works identically in-source and installed; any failure → None) · `_version_lt()`: tolerant numeric-tuple semver compare (malformed → None → skip) · `AdvisorState` gains `installed_version`, landed-plan count, cold-plan count (`COLD_DAYS = 30`, via `git log -1 --format=%ct`; git absent → not cold) · `build_output`: skew clause + housekeeping clause appended to the `systemMessage` (each ~40 chars; alone they form the message when nothing else fires) — **`additionalContext` untouched: resume-branch only, audience-split preserved (0024 S3)**.
  - `skills/doctor/SKILL.md` — the diagnose report gains a stamped-fence-vs-installed-plugin version row (`[D]` where both are readable, N-A otherwise); one row in the existing table, pointing at re-run-init as the treat (already an init concern, not a new treat class).
  - `tests/test_session_advisor.py` — skew fires / equal is silent / malformed-semver is silent / missing plugin.json is silent · landed + cold counting (incl. git-absent) · clause composition + cap truncation · audience-split regression: the new clauses NEVER appear in `additionalContext`.
  - `docs/claugentic-ARCHITECTURE_TREE.md` — advisor + doctor entries refreshed.
  - DECISIONS line at land (deterministic-gates or plugin-distribution shard: the advisor grows the two currency nudges; systemMessage-only rationale).
- **In-scope standards dimensions:** `reliability-resilience` (every new read degrades, never blocks a session) · `docs-traceability` (doctor row + tree entries) · `testing` (the audience-split regression pin is the load-bearing one).
- **Tests to add:** listed above; the mutation-shaped ones: delete the skew clause → a test fails; widen the clauses into `additionalContext` → the audience-split pin fails.
- **Acceptance criteria:** full pytest green incl. new cases · advisor smoke run on THIS repo stays silent-or-resume (no skew here: no managed fence) · a synthetic adopter fixture (fence@0.4.1 + plugin.json@0.5.1 + one Done plan + one 40-day-old in-flight plan) produces the exact combined systemMessage and an UNCHANGED additionalContext · tree + doc budgets green.

### Slice 3 — Verify verdict (Stage 7)

**Round 1: CHANGES REQUIRED** — `main..feat/0041-s3-currency-nudges` (`ecaa1f4`, 4 files, +668/−58). Four-lens panel (reliability-resilience · docs-traceability · testing · honesty; `yagni` skipped by the effort dial and re-checked here — the diff is proportionate: no speculative abstraction, `COLD_DAYS` is a constant as the user chose, the clause set is two, and the one new dataclass `PlansScan` earns itself by keeping a single definition of "in flight"). The shape is right and the load-bearing audience split is genuinely wired; **eight changes are required in one pass**, and two of them are defects the current suite cannot see.

**Independently re-executed by this gate (not taken on report).** A hermetic copy of the branch's advisor + suite was run in a scratch tree: baseline **79 passed**, then eight mutants. Corrections and confirmations below are from that run, not from the lens reports.

#### Required changes (one implementer pass, in this order)

1. **`_version_lt` must be total — it can blank the ENTIRE banner** (`scripts/claugentic-session-advisor.py:522`; reliability GAP-1 · testing T-3 · docs DT-1). `segment.isdigit()` is True for characters `int()` rejects (`'²'`), and the left-hand input is arbitrary user text captured by `MANAGED_VERSION_RE = r"claugentic-dev-harness@(\S+)"` from a hand-editable CLAUDE.md fence. **Reproduced end-to-end through `main()`**: with a fence stamped `@1.²`, the process prints *nothing at all* — not the payload, not even `{}` — so the resume `systemMessage` **and** the `additionalContext` vanish, silently and permanently, until a human notices. This slice created the exposure (first arithmetic consumption of the stamp) and it violates the module's own `:70` invariant ("a single bad input never blanks the rest"). **Fix:** `.isdecimal()` **and** a `try/except ValueError: return None` around the `int()` conversion. **The `try` is the load-bearing half, not the belt** — mutation-verified: `.isdecimal()` alone still dies on a >4300-digit segment (Python ≥3.11's int-conversion limit, also a `ValueError`), while `try` alone survives both. **Tests:** two rows on the `test_version_lt_table` (`tests/test_session_advisor.py:624`) — `("1.²", "1.3", None)` and `("1." + "9"*5000, "1.3", None)`. Both verified to fail on the current code and pass on the fix.
2. **Widen the too-narrow excepts, and make the docstrings true** (reliability GAP-2/GAP-3 · docs DT-1). `_read_installed_version`'s read (`:394`) catches `OSError` only, so a non-UTF-8 manifest raises `UnicodeDecodeError` (a `ValueError`) straight past it — while `:388` promises "**ANY** failure → None … must not cost the user their session banner". Fix `:394` → `except (OSError, ValueError):`, and reword the docstring to state what is actually caught rather than "ANY". Same one-token fix at `:323` (`epoch.isdigit()` → `.isdecimal()`, and it sits outside the `try`). **Also sweep the two pre-existing siblings** — `_read_managed_version:420` and `_read_roadmap:252` — in the same edit: **verified** that a non-UTF-8 `CLAUDE.md` blanks the whole banner today by exactly this route. Those two are pre-existing, not this slice's regression; they are folded in because it is the identical two-character change in the same file, and leaving a fixed reader beside two unfixed siblings is what re-breeds the bug. `except Exception` is **not** the fix here — name the classes.
3. **Fix the truncation ORDER — mechanism, not copy** (reliability GAP-4 · honesty H-1/H-2; `:617-618`). **Measured on the branch:** with both clauses firing, the housekeeping nudge is truncated away at **4** in-flight plans and both clauses are gone at **8** — and because cold plans *are* in-flight plans, the nudge is most certainly lost exactly in the repo shape that most needs it (the AskBase-shaped repo this plan cites). Compose-then-cap makes the clauses the first casualty. **Adjudication — reserve the clause budget on the `systemMessage` side only** (see *Adjudications* below for why this shape and not the plan-list elision). Required properties: (a) `additionalContext` stays **byte-identical** to the pre-slice form for every input — it is built from `recommendation` alone and must never pass through the composer; (b) when the composed line overflows, the **clauses survive whole** and the ellipsis lands inside the recommendation; (c) the ceiling still always wins, with a floor guard so a pathological clause length can never produce a negative slice; (d) with no clause firing, the line is byte-identical to pre-slice. **Validated in the scratch tree** (a ~12-line `_compose_user_line` + a `limit` parameter on `_cap`): 87 passed, and reverting it to compose-then-cap is killed by the rewritten test. **Test:** rewrite `test_composed_line_is_capped_as_a_whole` (`tests/test_session_advisor.py:749`) — it currently asserts `endswith("…")`, which is precisely the wrong guarantee. New assertions: `len(msg) <= MAX_LINE_CHARS` · the line **ends with the housekeeping clause verbatim** · the skew clause is present verbatim · `"…" in msg.split(CLAUSE_SEP)[0]`. The `TestSyntheticAdopter` goldens and the audience-split pin are **unaffected** (verified) — exactly one test changes.
4. **Make the anchor pin non-vacuous — the slice's central structural claim currently has no oracle** (testing T-1; `tests/test_session_advisor.py:532`+`:542`). **Reproduced:** replacing `PLUGIN_MANIFEST_PATH` with the exact forbidden form `Path(".claude-plugin/plugin.json").resolve()` leaves **79/79 passing**, because the test recomputes the same `__file__` expression and the suite runs from the repo root where both anchors coincide; the partner test reads the constant on both sides. **Fix:** a foreign-CWD pin — spawn the module in a subprocess with `cwd=tmp_path` where `tmp_path` holds a **decoy** `.claude-plugin/plugin.json`, and assert the resolved path is unmoved, `tmp_path` is not in it, and the decoy version is not what `_read_installed_version()` returns. **Validated:** passes on the branch code, **fails** on the CWD mutant. Acceptance is the mutant-kill, not the exact snippet.
5. **Pin `_is_cold` directly — four mutants survive today** (testing T-2). **Confirmed surviving:** `>` → `>=` · `COLD_DAYS` 30 → 7 · `abs(now - epoch)`. **Correction to the panel:** `COLD_DAYS` 30 → **45 is already killed** (4 failures — the 40-day fixtures bound it from above); only the lower bound, the boundary and the sign are unpinned. Root cause: `_is_cold`'s injectable `now` has no direct test, and the boundary test (`:673`) feeds boundary **+5s** (younger than the boundary), so it cannot distinguish `>` from `>=`. **Fix:** a 4-row table against a **fixed** epoch — `(29, False) (30, False) (31, True) (-31, False)` plus the `None` row. **Validated:** kills all three survivors and removes the suite's last wall-clock dependency.
6. **Truth up the module's own register** (honesty H-3 · docs DT-2). `:17` still reads "reports what the fences SAY and asserts nothing new" — now false three ways: it reads a non-fence input (the plugin manifest), asserts a relationship neither input states (`managed < installed`), and applies an advisor-owned 30-day threshold to a classification doctor labels `[J]`. Reword to name the two derivations it now owns. And `:58` — "SILENT path — nothing actionable (fresh repo / no fences / no plans)" — is falsified by this slice's own headline scenario (no plans, no fences, behind-stamp still emits); `build_output`'s docstring was updated and the module contract was not. Reword to "nothing actionable **AND no currency clause**".
7. **Rework the doctor row — it promises a treat doctor does not have** (docs DT-3 · honesty H-4; `skills/doctor/SKILL.md:152-154` + the table row at `:192`). Verified: doctor's treat set is enumerated as exactly **four** in *both* the frontmatter description and the Treat section, and re-run-init is in neither; an operator ticking the skew finding at SELECT hits a dead end, and init's blast radius (repo-wide upsert, can stop-and-report) fails the treat boundary anyway. **KISS reword, no fifth treat class** — "doctor **REPORTS** it; the remedy is for **you** to re-run `/claugentic-dev-harness:init` … **not** one of doctor's applied treats (see *Treat*)". Same edit fixes the `[D]` shape: the legend says "a `[D]` row states the gate's exact exit result" and this is the only bare-`[D]` row that is not a gate exit — follow the doc-budget row's precedent and append "(read-only — not a gate)". The N-A degradation copy is exactly right; change nothing there.
8. **Delete the mislabelled test row** (testing T-4; `tests/test_session_advisor.py:575`). The `("", "0.5.1")  # blank stamp` row writes **no fence at all** (the `if managed else` guard), so it is a duplicate of `test_absent_managed_fence_is_silent_even_with_a_readable_plugin` under a false label; a truly blank stamp is unreachable (`\S+` cannot match empty), and the empty-string branch is already covered by the `_version_lt` table. Drop the row and record the unreachability in one comment.

#### Spec amendment (register: **tightening** — rides the Verify loop, no re-approval)

Per `docs/claugentic-WORKFLOW.md` → Stage 7 (*when the SPEC is what's wrong*, landed on `harvest/0041-s2`): the spec's *What you're accepting* line — "appended to the existing single line, **truncated honestly if over the cap**" — is the defect the lenses refuted, and a spec-conformant diff is no defense. **As spec'd:** truncation is accepted, order unspecified. **Corrected:** *the two clauses are preserved whole; when the composed line overflows, the ellipsis falls inside the resume summary, never on a nudge.* This **tightens** what the user accepted (a strictly better guarantee, same scope, same trade-offs) so it stays inside the Verify loop. **The copy-only alternative would not:** it would weaken the Stage-5 plain-English promise "open **any** adopter repo … the session's first line tells you" to "usually", which is a change to what the approval block promised and therefore goes back to the user. Fixing the mechanism is both the cheaper and the register-safe path. Acceptance criterion restated: the synthetic-adopter golden is unchanged, and the cap test now pins clause survival rather than `endswith("…")`.

#### Adjudications

- **Truncation order — why *reserve*, not *elide the plan list*.** Both were on the table. Elision would have to happen **only** on the `systemMessage` side (an elision inside `_in_flight_summary` changes `recommendation`, which changes `additionalContext`, which breaks the slice's own verified byte-identity guarantee *and* its goldens) — meaning two renderings of the same fact and a user line that disagrees with the agent line about how many plans are named. Reserve keeps **one** rendering, keeps `additionalContext` byte-identical **by construction** (it never enters the composer), and cost exactly one test rewrite when measured. The ordering it produces is also self-correcting: the clause that survives the overflow is the one telling the user to sweep the plans that caused the overflow. What gets eaten is the tail of the plan list and the `Lead:` fragment — the agent still receives the full lead through `additionalContext`.
- **Pre-existing reliability notes — where each lands.** The **narrow-except siblings** are folded into required change #2 (same token, same file). The **git subprocess timeout** is **ROADMAP, not this pass** — exposure is unchanged-to-improved by this slice (fewer calls than before), and taking it here would drag in a second correctness requirement: `subprocess.TimeoutExpired` is **not** an `OSError` or `ValueError`, so a `timeout=` without the matching `except` re-creates GAP-2 in a new place. If a future pass takes it, the timeout and the widened except must land together, with a test. The **off-switch short-circuit**, the **one-subprocess call-count pin** and the **lead sorted-order pin** are likewise ROADMAP: all three are pre-existing and none is a correctness defect.
- **YAGNI re-check (the skipped lens).** Proportionate. No speculative surface: `COLD_DAYS` is a constant per the user's decision, the clause set is two, `PlansScan` prevents a second definition of "in flight" rather than anticipating one, and the required changes add ~15 lines of production code and no new concepts. The required composer is the minimum that satisfies a promise already made.

#### Explicitly dropped (do not implement)

- **H-5 (the "skew first" ordering sentence at `:559`).** Moot once #3 lands — the cap no longer discards clauses, so the comment implies no protection the code lacks.
- **H-6 (reword the clause noun "Harness docs stamped 0.4.1").** Premise doesn't hold: init's own contract makes the CLAUDE.md fence *the authoritative repo-version readout* (per-file stamps varying is exactly why the fence is the readout), so the clause is accurate as written. The reword would churn four golden strings and spend cap budget for no accuracy gain. The lens itself grades the core wording GOOD.
- **H-7 ("landed" implies a merge check; advisor-cold is wider than doctor-cold).** Self-labelled nit. "Landed" is this repo's own published term, and the clause's whole job is to hand off to `/doctor`, whose `[J]` call is the authority — a count that is wider than what doctor later flags is the correct direction for a pointer.
- **Reliability GAP-5 (import-time `Path(__file__).resolve()` at `:103`).** Verified out: the shipped hook command is `python3 "${CLAUDE_PLUGIN_ROOT}/scripts/claugentic-session-advisor.py"` — an **absolute** path, so `__file__` is absolute and `.resolve()` never consults the CWD; with `strict=False` it does not raise on missing components. Every available "fix" is worse than the risk — dropping `.resolve()` *reintroduces* CWD-dependence (the exact category error the constant exists to prevent), and making it lazy churns the constant that #4's pin and the whole anchoring claim are built on. Re-openable only if that hook command ever becomes relative.
- **The two mutants the panel marked non-actionable** — `agent_relevant`'s added conjunct (provably equivalent: `recommend_next` is non-None whenever `state.plans` is truthy; defensive and unkillable) and the pre-existing `sorted(reverse=True)` lead-order survivor. Honored, not re-litigated.

#### Land-commit obligations (NOT implementer fixes — carry these into the land commit)

1. **DECISIONS — AMEND, don't append** (docs DT-4): edit the existing audience-split bullet in the `plugin-distribution` shard **in place** (one contract, one line; the shard is at ~80% of cap) to carry all four facts — clauses are `systemMessage`-ONLY and `additionalContext` is built from the recommendation alone · the plugin-version read is `__file__`-anchored, never CWD · `COLD_DAYS = 30` is a constant, not config (YAGNI) · the compare **skips, never guesses**. Reference it only via `docs/claugentic-DECISIONS.md` (the index), never the shard directly.
2. **CHANGELOG** — one line for the two currency nudges.
3. **`CLAUDE.md:23`** — incomplete, not false ("the backlog nudges are user-facing `systemMessage` only" → the backlog **and currency** nudges). Minimal clause edit.
4. **Commit narrative** — the round-1 commit body repeats the "Any failure → None" phrasing that required change #2 exists to correct; don't carry it forward into the land/squash message.
5. **PLAYBOOK** — it has never mentioned the advisor at all. Dismiss explicitly or take one ROADMAP line; do not silently leave it open.
6. **ROADMAP** — one combined advisor-hardening line: git `timeout=` **plus** the matching `SubprocessError`/`TimeoutExpired` except · off-switch short-circuit in `main()` (off still pays every read, including one `git log` per in-flight plan) · a call-count pin for the one-subprocess-per-plan property (currently pinned only by proxy) · the lead sorted-order pin.

#### Definition of Done (round 1)

- **Acceptance criteria:** full pytest green ✔ (79/79 re-run independently) · advisor smoke on THIS repo stays silent-or-resume ✔ (re-run: resume-only, no clauses — one in-flight plan, no landed plans, no managed fence) · synthetic-adopter exact `systemMessage` + unchanged `additionalContext` ✔ (the `TestSyntheticAdopter` goldens are tight and correct) · tree + doc budgets green ✔. **But "green" is not sufficient here** — required changes #4 and #5 show the suite is green partly because two of its central pins assert nothing.
- **In-scope dimensions:** `reliability-resilience` **GAPS** (#1, #2, #3 — "every new read degrades, never blocks" is not yet true: two inputs blank the whole banner) · `docs-traceability` **GAPS** (#2, #6, #7) · `testing` **GAPS** (#4, #5, #8 — 5 actionable survivors from an independent 28-mutant sweep, four of them re-confirmed here). `yagni` (out-of-spec, re-checked): **PROPORTIONATE**.
- **Dimension the spec missed:** none material. The spec named the three right dimensions, and `honesty` — added by the panel because the advisor is a trust surface — found the register drift (#6) that the three would have missed.
- **Test-first audit:** N/A — this slice did not choose test-first (no charter, no per-work choice recorded), so no independent test-author spawn is expected and none is claimed.
- **Whole-feature pass:** N/A — Slice 3 of 8; the end-to-end check belongs to the closing slice.
- **New tech debt:** none accepted. Everything above is fixed in-slice, amended in the spec, or explicitly routed to ROADMAP with a reason.

**Plain-English summary (round 1).** The feature works and the one thing it most had to get right — never whispering these nudges into the agent's ear, only the user's — is genuinely wired and provably so. Three things stop it landing. **First, a typo in a hand-edited line of your CLAUDE.md can silently kill the whole session banner** — not just the new nudge, the resume line too, and with no error anywhere; the fix is two lines. **Second, the nudge disappears exactly where you need it most**: the more stale plans a repo has, the longer the first line gets, and the "run doctor to sweep" nudge is the first thing cut — gone at four in-flight plans, both nudges gone at eight, which is the shape of the very repos this slice was written for. **Third, two of the tests that are supposed to guarantee the headline claims don't actually test them** — the one proving the plugin version is read from the plugin (not from your repo) still passes if you break it exactly the way it warns against, and the "30 days means cold" boundary passes if you change 30 to 7. Nothing here is architectural; it is roughly half a day of small, surgical edits, and each fix has been run and verified in an isolated copy of the branch before being required.

#### Round 2 (delta) — `ecaa1f4..aab6822`, 4 files, +353/−85

**Round 2 (delta): PASS** — all eight required changes verified **met in the diff, not taken on report**. Conditional on **one single-assertion test addition** (R3-a below), which needs no further gate round.

**Independently re-executed again.** A hermetic copy of `aab6822` was rebuilt in a scratch tree — **93 passed** (advisor file 79 → 93) — then run against **14 mutants of this gate's own choosing**, including the three the coordinator flagged as the ones to doubt. Every result below is from that run.

**The three I said I would doubt.**
- **The composer's byte-identity claim for `additionalContext`** — **holds, verified directly rather than inferred.** Probed the shipped `build_output` in the regime where the reserve is actually active (10 in-flight cold plans + both clauses firing): `additionalContext == _cap(ADVISORY_PREFIX + recommend_next(state))` on the **full** recommendation — byte-identical to the pre-slice form — while the user line lands at exactly 320 with both clauses whole and the housekeeping clause last. The no-clause path is likewise byte-identical to a bare `_cap`. `_compose_user_line` is never on the `additionalContext` path, as the docstring's property 3 claims.
- **The changed golden's new assertion shape** — **stronger than prescribed.** The old `endswith("…")` (the wrong guarantee) is gone; `test_overflow_truncates_the_recommendation_never_a_clause` splits on `CLAUSE_SEP` and asserts the skew clause **verbatim and whole**, the housekeeping clause last, `"…" in head`, and `"…"` in **neither** clause. Reverting the reserve to compose-then-cap fails **2** tests. Two pins I did not ask for and should have: `test_both_clauses_survive_at_ten_in_flight_plans` (the panel's measured worst case) and `test_with_no_clause_firing_the_line_is_the_bare_capped_recommendation` (property (d) as an equality, not a length check). The floor guard has its own pin, and deleting the guard fails it.
- **The foreign-CWD pin** — **kills the mutant.** Re-ran `PLUGIN_MANIFEST_PATH = Path(".claude-plugin/plugin.json").resolve()` against the new suite: **1 failed** (it left 79/79 green before). The two deleted tests are not a weakening — the replacement asserts both of their properties (the path is unmoved *and* the real shipped version is read) plus the two neither could: the decoy in the cwd never wins, and `tmp_path` never appears in the resolved path.

**The rest of the battery.** `>`→`>=` **killed** · `COLD_DAYS` 30→7 **killed** · `abs(now-epoch)` **killed** · dropping the `try` in `_version_lt` **killed** · dropping `RecursionError` **killed** · narrowing each of the **four widened excepts** back to `OSError` **killed individually** (roadmap · per-plan read · manifest read · CLAUDE.md read) · counting an undecodable plan as *landed* **killed**. The one surviving `except` arm is `_plan_git_meta`'s `ValueError` on `subprocess.run` (`:327`) — **pre-existing, unreachable by construction** with a literal argv, and pinning it would require monkeypatching `subprocess.run` to raise, which pins nothing real. Not a finding; don't chase it.

**The two judgment extensions — both accepted, both inside fix #2's stated rationale.**
- **(a) `_read_plans`' per-plan read.** Correct and in scope, not scope creep: this slice *rewrote* that reader (it became the landed/cold counter), so it is the slice's own third `OSError`-only sibling, not a bystander. The pin is better than what I asked for — it asserts the undecodable plan is skipped **and** that `(landed, cold) == (0, 0)`, i.e. that a mojibake'd file is never silently miscounted **into the very nudge this slice adds**. Mutating the skip into `landed += 1` fails it.
- **(b) `RecursionError` on `json.loads`.** Accepted. It was named in the panel's GAP-2 citation, it sits inside the requirement's own wording ("named **wide** enough to cover what this read can meet"), the docstring now **enumerates** the classes instead of promising "ANY failure", it is still a named-class list rather than a blanket `except Exception`, and it is pinned by a real 100k-deep nesting case.

**The equivalence proof — accepted, and independently strengthened.** The surviving `.isdecimal()` → `.isdigit()` mutant is genuinely equivalent *given the `try`*, and I verified it beyond execution of a few cases: an exhaustive sweep of all 1,114,112 code points found **zero** characters where `isdigit()` is True, `isdecimal()` is False, and `int()` succeeds — so on every input where the two predicates disagree, the conversion raises and the `try` returns the same `None`. Same category as the `agent_relevant` conjunct. The docstring records it *and* warns that the `try` is the killable half — which is the right way to leave an equivalent mutant behind.

**Residual (must land, no re-review needed).**
- **R3-a.** The composer's property 3 is pinned only in the **non-overflow** regime — the two byte-identity assertions use short fixtures where the reserve never bites. Verified: a mutant that leaks the reserved budget into the **agent-facing** string (`_cap(ADVISORY_PREFIX + _cap(recommendation, head_limit))` — shortening `additionalContext` on overflow without adding any clause) survives all 93 tests. The **code is correct**; the promise just has no oracle exactly where the composer is active. One line inside the existing `plans=10` (or `plans=6`) overflow test: `assert payload["additionalContext"] == adv._cap(adv.ADVISORY_PREFIX + adv.recommend_next(state))`. Same shape and standing as Slice 2's R2-a.

**Also verified.** Doctor: the reword is exactly the KISS shape — "Doctor **REPORTS** it; the remedy is for **YOU** to re-run init … **not** one of doctor's applied treats (that set is exactly four)" — with `(read-only — not a gate)` on **both** the bullet and the table row, matching the doc-budget precedent; **no fifth treat class**, and the frontmatter's four-treat enumeration is untouched. Both register truth-ups landed verbatim, plus an unrequested `SIZE-CAPPED` sentence documenting the reserve. The mislabelled row is replaced by a `("1.²", "0.5.1")` row carrying the unreachability note. The `Readers —` section comment states the widened-except rule **once**, for the whole block, instead of repeating it per function. Tree entry re-measured at **428/450** — under the ceiling the implementer flagged as briefly breached.

**Definition of Done (round 2).** Acceptance criteria all met, and now met **in substance**: the two vacuous pins from round 1 have real oracles, and the suite's last wall-clock dependency is gone. In-scope dimensions: `reliability-resilience` **MET** (every reader degrades to "this one input is absent"; the end-to-end typo'd-fence case proves the banner survives) · `docs-traceability` **MET** (register, output contract, doctor row, both tree entries) · `testing` **MET but for R3-a** · `yagni` **MET** (net production code ~+40 lines, one new constant, no new concepts; the dropped list was honored with nothing implemented from it). No new tech debt. The six land-commit obligations from round 1 stand unchanged and are still outstanding.

**Plain-English summary (round 2).** Everything I asked for is there, and in a few places it is better than what I asked for. The typo-in-your-CLAUDE.md bug that used to silently kill the whole session banner is fixed and now has a test that reproduces the original failure end to end; the nudges no longer get cut off in cluttered repos — they survive at ten stale plans, which is past the point where both used to vanish; and the two tests that were quietly asserting nothing now genuinely fail when you break the thing they name. The two extra fixes the implementer made on their own judgment are the right ones for the right reason. One small thing is left: the promise that these nudges can never alter what the *agent* sees is true in the code and proven by hand, but the test that guards it only exercises the easy case — I broke the guarantee in a way the suite doesn't notice. That's one line of test to add before this merges, not another review round.

### Slice 4 — Budget script generalization (3a — no shipped behavior change)

- **In plain English (shown first at the approval gate):**
  - **What this builds:** the doc-budget gate becomes **config-driven** — the caps move out of the script's hardcoded dict into `.claude/claugentic-doc-budgets.json`, and this repo gets its own config carrying exactly today's five entries (byte-equivalent behavior: same caps, same messages, same WARN band). The script itself learns the shapes the later slices need — the `reportOnly` grace form, no-op-when-no-config, glob-entries-by-key — and gains the `_repo_root()` anchor its sibling gates already have, so it works from any directory. Shard existence moves from the hand-maintained `REQUIRED_SHARDS` list into a two-direction agreement test (every index-routed shard exists · every shard file is index-routed — strictly stronger, and it clears a 0040-banked item).
  - **What "done" means for you:** gates stay green and identical in behavior; the machinery is now shaped so Slice 6 can ship it and Slice 7 can seed adopter configs — **no shipped *behaviour* change reaches adopters from this slice; the one shipped-*content* change is WORKFLOW's rung-2 paragraph** (the config file itself is classed dev-only in the same commit, confirmed by the shipped-set test and Pass D).
  - **What you're accepting:** absent config = **silent no-op exit 0** (the gate enforces only where a config exists — the not-opted-in posture) · a present-but-malformed config **fails loud** (your own config is your signal — unparseable JSON or a non-numeric cap is a problem line, exit 1) · a glob entry matching nothing is **skipped** (existence guarding is the agreement test's job now) · subdirectories under a globbed dir produce a **WARN naming the unbudgeted subtree**, never an error · a `reportOnly` breach prints and passes; **nothing mechanical clears the flag** (condense + doctor own that, model-upheld — stated plainly).
- **Files & changes:**
  - `scripts/check_doc_budgets.py` — `DOC_BUDGETS`/`REQUIRED_SHARDS` deleted · `_load_config()` reads `<repo-root>/.claude/claugentic-doc-budgets.json`: flat map `{"<relpath-or-glob>": <max_bytes> | {"max": <bytes>, "reportOnly": true}}`; a key containing `*` is a glob entry (unambiguous — `*` is invalid in filenames; the docstring records this as the deliberate successor to the old declared-kind marker) · absent ⇒ no-op exit 0 with a one-line note; malformed ⇒ problem line exit 1 · glob-no-match ⇒ skip · subdir-under-globbed-dir ⇒ WARN (was: error) · `reportOnly` breach ⇒ warn-level line with an explicit report-only tag · `_repo_root()` + `_force_utf8_output()` parity with the sibling gates (CWD-independent).
  - **`.claude/claugentic-doc-budgets.json` (NEW)** — this repo's five migrated entries verbatim (CLAUDE.md 6000 · DECISIONS index 3500 · `docs/claugentic-decisions/*.md` 14000 · ROADMAP 12000 · INVARIANTS 20000). No WORKFLOW cap (that lands only with Slice 12b).
  - `scripts/build_release.py` — the config classed **`init-gen`** in `DEV_ONLY_PATH_CLASSES` with the rationale inline (shipped docs point adopters at the path; init generates the adopter's own).
  - `docs/claugentic-WORKFLOW.md` — the `:175` "the harness's OWN caps live in `check_doc_budgets.py`'s `DOC_BUDGETS`" sentence corrected to the config + a pointer at the canonical schema home (doctor's reader-contract; full four-site convergence is Slice 6's).
  - `tests/` — config-form tests (int · object · malformed · absent · glob-by-key · no-match skip · subdir WARN · reportOnly wording) · CWD-independence (run from a subdir; absorbs the 0040-banked `TestClosurePassD` CWD-coupling fix) · **the index↔shards agreement test, both directions** · existing hermetic fixtures migrated from monkeypatched dicts to config-file fixtures (a forced seam edit, named per the S3 precedent) · shipped-set byte-identical test updated for the `init-gen` entry.
  - `docs/claugentic-ARCHITECTURE_TREE.md` — new config entry + refreshed script/test entries.
- **In-scope standards dimensions:** `maintainability-structure` (one cap source per repo — the invariant candidate hardens in Slice 6) · `reliability-resilience` (the absent/malformed/skip/WARN boundary table) · `testing` (migration with no assertion weakening; the agreement test; declarative-artifact discipline for the JSON config) · `docs-traceability` (`:175` correction).
- **Tests to add:** per above; the mutation-shaped ones: absent-config no-op vs malformed-config fail-loud must be distinguishable (a mutant collapsing them fails); deleting a shard fails the agreement test in the existence direction; an unrouted shard file fails it in the other.
- **Acceptance criteria:** full battery green · this repo's gate output **byte-equivalent** pre/post migration (modulo the REQUIRED_SHARDS message class, now the pytest's) · `check_shipped_content.py` green with the config classed dev-only · the script green when invoked from a subdirectory · doc budgets green (the config file itself is not a budgeted ledger).

### Slice 4 — Verify verdict (Stage 7)

**RUNNING AS: Opus 5** — same model family as the builder/planner. The independence here is **role + clean context only** (a separate clean-context pass on the most capable model, a reduction of rubber-stamping risk); model blind spots are **not** de-correlated. Five lenses ran (`maintainability-structure` · `reliability-resilience` · `testing` · `docs-traceability` · `honesty`); this is the synthesis, with every load-bearing claim **re-executed against `b6f17f0` in a scratch worktree** rather than taken on report.

**VERDICT: CHANGES REQUIRED** — one implementer pass. The slice's *architecture* is sound and its central claim (one cap source per repo) is genuinely achieved **in code**; what fails is the **boundary totality** of the new reader, the **key-side** validation, and a **blocking honesty defect** in the summary. Next round is a **targeted re-verify of the changed surfaces**, not a re-panel: confirm each item below in the diff, re-run the mutants named in C1–C3, and confirm 505+ green **from both the repo root and `tests/`**.

**What I re-measured myself (not taken on report).** `WARN_RATIO 0.9→0.5` survives all 85 module tests · `_repo_root`'s `Path(__file__)→Path.cwd()` survives the **full 505-test suite** · rewording the summary prefix+separator survives · `docs/**/*.md` resolves to `[]` and prints `(0 files)` under `OK:` at exit 0 · `docs/claugentic-decisions/**` resolves to **10 files on local 3.13 and 0 on CI's 3.12** (`ci.yml:29,73`) · a non-UTF-8 byte escapes `_load_config` as `UnicodeDecodeError` and deep JSON as `RecursionError` · a `PermissionError` from `iterdir()` propagates out of `evaluate()` **with a real CLAUDE.md breach already queued and never printed** · duplicate keys silently last-wins (`{"CLAUDE.md":6000,"CLAUDE.md":999999}` → the tighter cap vanishes) · the graced summary renders verbatim `OK: all managed ledgers within budget - CLAUDE.md <= 100 bytes` for a 5,200-byte file · both T-F6 one-liners applied ⇒ 505 green from **both** cwds, and the chdir-leak fix **alone** ⇒ 504 passed / **1 failed**.

#### Required changes (one pass, in this order)

**A — fail-open / totality in the gate itself (`scripts/check_doc_budgets.py`)**

1. **A1 (R1 + H-F1) — `_load_config` totality.** Read with `encoding="utf-8-sig"` (also parses PowerShell-BOM'd adopter configs) and catch **named** excepts — `OSError`, `json.JSONDecodeError`, `UnicodeDecodeError`, `RecursionError` — **never a bare `ValueError`**. This makes the already-written claims true (`:33/:97/:162/:230` "two DISTINCT boundary verdicts" / "never a swallowed exception" / "no traceback"), so fix the tuple, not the words. Oracles: a non-UTF-8 byte and a deeply-nested JSON each → **one** problem line + exit 1 + no traceback.
2. **A2 (M-GAP-2 = R3) — key-side boundary validation.** In `_parse_rule` (or a `_validate_key` beside it): reject `*` **outside the final path component** and reject `**` **outright** — "this entry measures a FLAT directory" becomes unrepresentable instead of merely documented. Today `docs/**/*.md` — the natural "everything under docs" — measures **zero** files and exits 0 under the OK banner: the module's own forbidden fail-open, reachable through supported-looking syntax that the docstring's cited precedents (`.gitignore`, tsconfig) actively invite. After this, "a dead glob is skipped" honestly means *no shards yet*. Oracles in `TestMalformedConfigFailsLoud`; assert the production key `docs/claugentic-decisions/*.md` still validates.
3. **A3 (R3 rider — CONFIRMED, adjudication 4).** Same edit, same file: `json.loads(..., object_pairs_hook=...)` making **duplicate keys fatal**. Backed only because A2 lands anyway — it does; it is one line at the call site A1 already touches, with one oracle.
4. **A4 (R2) — the survey escape masks sibling breaches.** `_unbudgeted_subtrees`'s `Path.iterdir()` is unguarded (`is_dir`/`glob` swallow `OSError`; `iterdir` does not), so one unreadable globbed directory **discards every breach queued before it** — falsifying the module's own `:89` "one file must never mask a breach in another". Wrap in `except OSError` and degrade to a WARN line **naming the entry**. Oracle: a queued breach + a raising `iterdir` ⇒ the breach still prints and the exit code is still 1.

**B — the blocking honesty defect**

5. **B1 (H-F2 BLOCKING, + H-F3, + the M-GAP-5 rider) — the report-only summary launders a live breach.** With a grace firing, the gate prints the WARN and then `OK: all managed ledgers within budget - CLAUDE.md <= 100 bytes` for a file at **52×** its cap: both the headline and the clause are false, anyone tailing CI or grepping `OK:` reads a clean pass, and a test currently **pins it as intended**. The author fixed exactly this laundering for the empty-config case (`:150`) and gave the graced case the treatment they had already rejected. **Exact shape (do not improvise):** collect the graced entry keys in `evaluate()`'s loop where the grace is applied; then (a) when that list is non-empty the headline becomes a distinct rendering — `f"OK: {n} report-only breach(es) NOT within budget (see WARN above) - "` (keep the `OK:` token: the run genuinely passes; the falsehood was "all … within budget") — and (b) `_summary_clause` gains the graced fact and renders that entry as **measurement-over-cap, never cap-satisfied**: `f"{rel_path} OVER budget {max} bytes [report-only]"` (glob variant: `f"{rel_path} ({matched} files) OVER budget {max} bytes each [report-only]"`). Update the pinning test to assert the new contract. `_summary_clause`'s docstring (H-F3) currently implies a distinction it cannot make — this fix makes that sentence **true**; keep it and say so. Also `:87` "it says so on every run" → "**re-prints the breach**". **M-GAP-5 rider:** this adds a third consumer of the suffix-sniffed `size_verdict`, so extend the existing ordering comment to name graced-tracking as depending on it (no signature change — see the dropped list).

**C — test oracles (the pins that currently assert nothing)**

6. **C1 (T-F1) — the `_repo_root` anchor claim is VACUOUS.** `here = Path(__file__).resolve().parent → Path.cwd()` survives the **full 505-test suite** (both test cwds sit inside the same repo, so `rev-parse` returns the same root either way). Add a third case to `TestInvokedFromASubdirectory`: `cwd=tmp_path` holding a **git-init'ed decoy repo** with its own config and a breaching `CLAUDE.md`; assert `rc == 0` and `stdout == at_root.stdout`.
7. **C2 (T-F2) — the WARN band is pinned from one side only.** `WARN_RATIO → 0.8` and `→ 0.5` both survive (verified). Add a **fixed-reference table** against `cap=100`: `89 → nothing · 90 → warn · 100 → warn, not a breach · 101 → error`. Live stakes remain real after the approved ROADMAP bump: `CLAUDE.md` sits at **5,286/6,000 = 88.10%**, 114 bytes below the trip.
8. **C3 (T-F3) — the byte-equivalence AC is demonstrated but not PINNED.** Rewording the summary prefix and separator survives. Add a **hermetic** whole-line equality (a known 2-entry `budget_repo` config, `summary == "OK: all managed ledgers within budget - A.md <= 100 bytes, B.md <= 200 bytes"`) — hermetic on purpose, so the envelope pin does not re-import the live-size coupling C4 removes. The cap half is already pinned (4 drift mutants killed by the migration test) — leave it.
9. **C4 (T-F4) — decouple the CWD test from live ledger sizes.** In `test_output_is_identical_from_the_repo_root_and_from_a_subdirectory`, drop the absolute `assert at_root.returncode == 0` (a docs commit breaching any ledger turns the **unit suite** red for a reason that has nothing to do with the property under test); keep the cross-cwd equality of `returncode` **and** `stdout`, and add a size-independent non-vacuity guard (e.g. `assert "CLAUDE.md" in at_root.stdout` — present in both the OK summary and any breach line, and red if the gate no-ops or the config vanishes). For `test_the_summary_names_every_configured_entry`, keep the coupling but **state it deliberately at the site** in one comment.
10. **C5 (T-F5) — align the agreement test's oracles.** The hermetic refusal cases use a different expression than the live assertion, so the hermetic half can stay green while the live expression rots. One shared expression, used by both.
11. **C6 (T-F6) — FOLD BOTH one-liners (adjudication 3, see reasoning below).** (a) `tests/test_check_versions_synced.py` — the `manifests` fixture must restore the CWD (`monkeypatch.chdir(tmp_path.cwd())` beside the two `setattr`s, with a comment naming *why*: `cvs.main()` chdirs to the repo root and never restores). (b) `tests/test_shipped_condensation_trigger.py` — an `at_repo_root` autouse fixture (`monkeypatch.chdir(br._repo_root())`) on `TestShippedSetHasNoOrphanedTrigger`. **They must land together**: applying (a) alone takes the suite to **504 passed / 1 FAILED** from `tests/`, because the leak is what currently masks the red.
12. **C7 (T-F7 + H-F5) — give the forward registration a tripwire.** `INIT_GEN_OUTPUTS` registers `.claude/claugentic-doc-budgets.json` **ahead of its writer**; Pass D passes on the promise and nothing goes red if Slice 7 is dropped. Add `@pytest.mark.xfail(strict=True)` on a case asserting the path appears in `skills/init/SKILL.md` — it flips **red the moment Slice 7 lands**, forcing the annotation to be removed. The comment's own instruction ("if Slice 7 is ever abandoned, re-annotate as `recreate-on-demand` — never leave this claim standing") has no mechanism today; this is it, in three lines.

**D — copy that this diff made false (all in-slice)**

13. **D-a (D1) — `WORKFLOW.md` current **:177** is self-contradictory within one line.** Delete the seven words `**adopter-owned, adopter-created** — ` : the surviving clause sits beside this diff's own new "the harness's own caps live there too". **Owned by no other slice** (see E1).
14. **D-b (M-GAP-1 + D3) — `WORKFLOW.md` current **:179**, IN-SLICE (adjudication 2).** Correct "*the harness's own caps are `DOC_BUDGETS` in `scripts/check_doc_budgets.py`*" and "*a second config here would be a duplicate cap source*" — both falsified **by this slice**, which deleted the symbol. Same file, same deleted token, no second correctness requirement ⇒ the absorption rule folds it. **Bound it:** fix only the `DOC_BUDGETS` clause and the adopter-only framing; leave the paragraph's flat-int schema example alone — the three-form statement is Slice 6's.
15. **D-c (D2) — `docs/claugentic-decisions/doc-lifecycle.md:6` gets an interim SUPERSEDED clause.** It is the consult-before-re-litigating ledger and it actively instructs that a live config here would be a second cap source and "never a tracked file" — an agent obeying it **deletes the config**, which is the silent-disarm fail-open (absent config ⇒ quiet exit 0, green locally, disarmed in CI). One appended clause: *"SUPERSEDED for the harness itself by 0041 S4 (the harness's own caps now live in the tracked config); full sweep S6."* This doubles as the in-slice DECISIONS record.
16. **D-d (D4) — `skills/doctor/SKILL.md:99`**, one clause: "the adopter default, **and the harness's own repo**" is false as of this commit → "(the adopter default)". Shipped content. `:87`/`:89`/`:95` stay Slice 6's (see E2).
17. **D-e (M-GAP-3 = H-F6) — stop pointing at a doc that contradicts you.** `WORKFLOW:177`'s "the schema is stated **once**, in `skills/doctor/SKILL.md`" **ships**, and the named source states neither new form and affirmatively says the map has **no glob support** — while this repo's own config uses a glob key. Qualify both that sentence and the module docstring's mirror: canonical **home**, convergence in progress (Slice 6), and where the forms live today.
18. **D-f (M-GAP-4) — trim the tree entry to a LOCATOR.** `docs/claugentic-ARCHITECTURE_TREE.md` restates all five cap values with no gate comparing entry↔file. The approved ROADMAP bump is the live proof: it forces edits in the config, the tree entry, `deterministic-gates.md:11`, the migration pin **and** a DECISIONS line — so the same diff's `WORKFLOW:177` promise that a bump is "*the same one-line edit … in a harness-self repo as in an adopter's*" is **false**. Trim the entry to "this repo's ledger byte caps (the one cap source); dev-only (`init-gen`)" **and** qualify that WORKFLOW sentence to name the harness-self extra (the byte-exact migration pin, which is deliberate drift-detection and stays).
19. **D-g (H-F4) — `scripts/build_release.py:114-119` states the Slice-7 init writer in the PRESENT tense** ("init generates the adopter's OWN caps") with zero budget mentions in `skills/init/SKILL.md`. The sibling registration carries an HONEST STATUS note; this one — in the one place ship/strip semantics are declared — must too. Reword to *will generate* + cross-reference the `INIT_GEN_OUTPUTS` note.
20. **D-h (H-F5) — `scripts/check_shipped_content.py:295` "with no release in between"** asserts a plan-order convention as a property; nothing gates it and Pass D accepts unconditionally. The stated fallback covers *abandonment* but not *release-before-S7*, the likelier break. Reword to a stated intention with the release-before-S7 case named. (C7 is its mechanical complement.)
21. **D-i (H-F7) — this plan's own line ~383:** "nothing reaches adopters from this slice" is false — `WORKFLOW.md` **ships** (verified through `br.classify`) and this slice rewrites its rung-2 paragraph. Honest form: *"no shipped **behavior** change; the one shipped-**content** change is WORKFLOW's rung-2 paragraph."*
22. **D-j (M-GAP-6, trivial)** — `tests/test_build_release.py`: the allow-delta sets cross-reference one direction only. One comment line.

**E — plan repairs that ride this pass (adjudication 1)**

23. **E1 — replace Slice 6's line-number references with CONTENT ANCHORS.** The plan's numbers have **drifted by 2** (plan `:175` = current `:177`, plan `:177` = current `:179`), so Slice 6's checklist sweeps current `:179` and leaves current `:177`'s residual **unassigned** — which is exactly how the falsified sibling sentence nearly shipped permanently. In the Slice 6 bullet, change `WORKFLOW:177` to *"WORKFLOW → the escape-valve ladder rung 2 + the 'Creating the caps config (adopter procedure)' paragraph"*, and `doc-lifecycle.md:6` to *"doc-lifecycle → 'Escape-valve ladder + per-repo caps config (0038 S3)'"*.
24. **E2 — add the THIRD schema form to Slice 6's scope.** Slice 6 says "reader-contract (**both** cap forms…)" = int + object. There are now **three**: int · object · **glob-by-key**. `skills/doctor/SKILL.md:95` says the map has "no glob support" — grep-invisible (no `DOC_BUDGETS` token), owned by nobody. Change to *"all three cap forms (int · object · glob-by-key)"* and add `doctor SKILL:95` to its sweep list beside `:87`/`:89`.

#### Spec amendments (the finding refutes the SPEC, not the diff — see `docs/claugentic-WORKFLOW.md` → Stage 7)

- **SA1 — §Files & changes, the `docs/claugentic-WORKFLOW.md` line.** *As spec'd:* "the `:175` … sentence corrected". *Corrected:* **name both rung-2 sites by content anchor** — the escape-valve rung-2 paragraph **and** the "Creating the caps config + a recorded cap-bump (adopter procedure)" paragraph — i.e. every WORKFLOW sentence this slice falsifies, never a line number. *Lens:* maintainability + docs-traceability. *Reason:* the diff faithfully obeyed a line-scoped file list, and the list was wrong; a spec-conformant diff is no defense. This is what D-a/D-b are correcting.
- **SA2 — the "byte-equivalent" acceptance criterion, read for its spirit.** The AC means **behavior preservation on this repo**. B1 changes the summary **only in the graced case**, and this repo's config declares **zero** `reportOnly` entries (asserted by `test_the_config_is_loadable_by_the_gates_own_reader`; the live summary I rendered is unchanged). **The AC survives B1 intact** — it is not a reason to soften the blocking fix.

#### Adjudication outcomes (the seven the orchestrator asked for)

1. **Stale-doc/temporal family — docs-trace's per-site grading RATIFIED, with one amendment.** In-slice: D-a · D-b (amended up from *defer*) · D-c · D-d · D-e · D-f · D-g · D-h · D-i. Land: L2 · L3 · L4. Slice 6: `doctor:87/:89/:95` + the four-site convergence + the third schema form. Both structural repairs ride: **E1** (content anchors) and **E2** (glob form added to Slice 6's scope).
2. **`WORKFLOW:179` → FIX IN-SLICE** (amending docs-trace's *defer-iff-D1-fixed*). Four reasons: it is the **same file and the same deleted token** the slice already edits, so the absorption rule folds it (a corrected `:177` beside an uncorrected `:179` is the canonical re-breeding shape); the plan's own **R7b assigned "the falsified half" to Slice 4**, and `:179` *is* the half this slice falsified — it only looks deferred because of the 2-line drift; the edit carries **no second correctness requirement**; and D3's deferral rests on "no release before S6", which is a **schedule assumption, not a property** — precisely the class H-F5 flags one item away. Bounded: the schema statement stays Slice 6's.
3. **T-F6 → FOLD BOTH (honest application of the absorption rule).** The same-token-same-file test **fails** on its face (different files), so I am folding on the rule's *stated purpose* rather than its letter, and saying so plainly: (i) both are **one-line test fixtures with no second correctness requirement** — the exact thing the rule folds; (ii) the slice's spec **explicitly absorbs the 0040-banked CWD-coupling item**, and the implementer fixed one instance (`TestClosurePassD`) of a class whose sibling instance is still red from `tests/` — a fixed reader beside an unfixed sibling; (iii) **decisive and measured**: the chdir leak is an **order dependency masking a genuinely red test** — apply (a) alone and the full suite goes **504/1 FAILED** from `tests/`; apply both and it is **505 green from both cwds**. Routing them to follow-up would mean knowingly leaving a red test masked. *(A meta-guard — running the suite once from a non-root cwd in CI — is routed to ROADMAP, not required here.)*
4. **Duplicate-keys rider → CONFIRMED (fold).** A2 lands, so the rider's precondition is met; verified silent last-wins discards the tighter cap; it is one line at the call site A1 already edits, plus one oracle. Folded as A3.
5. **R4 (`_repo_root` sibling-wide) → ROADMAP, routing CONFIRMED, with the don't-split constraint.** The function is **byte-identical across 5 gate scripts** (recorded deliberate duplication, and the maintainability lens verified this copy is AST-hash-identical to the three correct siblings). A Slice-4-only fix would both leave four unfixed siblings **and break the byte-identity that makes the duplication safe** — actively worse than not fixing it. Route as **one 5-file sweep**, and it **must carry the `timeout=` pairing**: `TimeoutExpired` is neither `OSError` nor `ValueError`, so a bare `timeout=` re-creates the very bug the widened `except` is added to prevent. **Do not split the widened `except` from the timeout, and do not split the sweep by file.** (C1's decoy-repo pin is test-only and stays in-slice — it does not touch `_repo_root`.)
6. **H-F2 fix shape → specified exactly in B1** (headline rendering + graced clause rendering + the docstring becoming true), so nothing is left to guess. **Reliability's `NO_ENTRIES` "OK:"-prefix nit → NOT folded** (see the dropped list): the message text is *accurate*, only the grep token is at issue, and the case is closed for this repo by the migration pin.
7. **T-F7 + H-F5 → TAKE BOTH.** They are the mechanical and the verbal halves of the same forward-registration honesty problem: H-F5 stops asserting an ungated plan-order convention as a property, and T-F7 makes the promise **falsifiable** for three lines. Taking only the wording would leave the promise unfalsifiable; taking only the xfail would leave the false property standing.

#### Explicitly dropped (with reasons — not oversights)

- **M-GAP-5 — `_check_one` returning `(level, kind, message)`.** The lens itself graded it minor and "fix only if the seam reopens"; the negative cases **do** pin today's behavior; and changing the return arity fights `TestCheckOneStaysUnchanged`, which pins that contract deliberately. Absorbed as the one-comment rider in B1 instead.
- **Reliability's `NO_ENTRIES_NOTE` "OK:"-prefix nit.** The text ("declares no budget entries; nothing measured") is accurate — this is not laundering, unlike H-F2. The truncated-to-`{}` risk is closed for this repo by the migration pin. Judgment call, deprioritized rather than refuted; revisit with the adopter-facing work in Slices 6/7.
- **The glob-whose-parent-is-a-FILE case (`CLAUDE.md/*.md`).** Catching it needs filesystem state inside what A2 keeps a **pure** validator. Once `**` is banned, the surviving zero-match shapes are "no shards yet" (legitimate) and a plain typo, and the summary's `(0 files)` clause keeps it on screen.
- **`doctor SKILL:87/:89/:95`** — routed to Slice 6, not dropped (`:95` requires the three-form reader-contract rewrite = a second correctness requirement).
- **A `.gitignore` pattern un-ignore (`!.claude/claugentic-*.json`)** — the class-level "any future top-level `.claude/` file is double-invisible" observation. Closed for *this* file by the explicit un-ignore + the tracked-by-git test. → ROADMAP.

#### Land-commit obligations (NOT this fix pass)

- **L1 — the approved ROADMAP cap bump 12,000 → 14,000** (user-approved; rides the land commit). It touches **five** places: `.claude/claugentic-doc-budgets.json` · the migration pin `test_the_migrated_caps_are_exactly_the_five_entries` · the tree entry (unless D-f already trimmed it — do D-f first and this drops to four) · `deterministic-gates.md:11`'s restated "ROADMAP 12K" · a dated DECISIONS line recording *why*. Post-bump ROADMAP sits at **10,792/14,000 = 77.09%** (WARN at 12,600), which defuses the 8-bytes-from-WARN concern **for that file only** — `CLAUDE.md` remains at 88.10%, so C2's finding stands in general.
- **L2 (D5) — re-home the per-cap WHY** into `docs/claugentic-decisions/deterministic-gates.md:11` (the caps' decision home): the 3,500 B index-cap backstop rationale ("slack is where a mis-routed entry would hide") and the 20 K INVARIANTS reasoning now live **only in git history**, and rung 2 requires a recorded *why* to bump. Same edit: **strip the restated cap NUMBERS** there and point at the config — a pointer, not a copy.
- **L3 (D6) — the commit narrative must enumerate the deferral list** (repo precedent: `0516d13`).
- **L4 (D7) — rewrite `docs/claugentic-ROADMAP.md:45`**, which banks work this slice completed (the CWD coupling + the index↔shards agreement test) and names the now-deleted `REQUIRED_SHARDS`; carry forward explicitly so nobody hunts for a `_repo_root` that already exists. Note that C6 clears the *second* CWD instance too.
- **L5 — the DECISIONS line** for the config-driven caps + the glob-by-key doctrine (per repo convention, DECISIONS lands at land).
- **ROADMAP additions:** R4's 5-file `_repo_root` sweep **with** its `timeout=` pairing (don't split) · the `.gitignore` pattern un-ignore · run the pytest suite once from a non-root cwd in CI.

#### Definition of Done check

| | |
|---|---|
| **Acceptance criteria** | Full battery green ✔ (505) · byte-equivalence **demonstrated (261 bytes identical) but not pinned** → C3 · shipped-content green with the config stripped ✔ (verified through `br.classify`) · green from a subdirectory ✔ **but the anchor claim is vacuous** → C1 · doc budgets green ✔ |
| **`maintainability-structure`** | **GAP** — the single cap source is genuinely achieved in code (grep-proven) and the seam decomposition is clean, but key-side validation is absent (A2) and three doc sites still name the deleted symbol (D-b/D-c/D-d) |
| **`reliability-resilience`** | **GAP** — the five-row boundary table is driven end-to-end and `_check_one`'s `read_bytes` is decode-free by construction; the escapes are A1, A2/A3 and A4 |
| **`testing`** | **GAP** — the three inversions, the declarative-artifact discipline and the allow-delta pattern are strong (6/6, 9/9); the vacuous or unpinned oracles are C1–C5, plus C6/C7 |
| **`docs-traceability`** | **GAP** — tree entries accurate and net-trimmed, the docstring contract clauses checked truthful; D-a…D-i outstanding |
| **`honesty` (not spec'd, correctly added)** | **BLOCKING GAP** — B1 |
| **No new tech debt** | **Not yet** — B1 is pinned by a test *as intended behavior*, and C6 shows a red test masked by an order dependency. Both clear in this pass. |
| **Missed dimension** | The spec named four; `honesty` was the right addition (a gate that prints `OK:` is a trust surface). No further dimension is missing. |

#### Plain-English summary (two layers)

**What's wrong.** The rebuilt budget gate is well-designed and the big idea works — the caps really do live in exactly one file now, and the gate really is one edit away from any repo tuning its own budgets. But three things are not yet true. **First and worst:** if you ever mark a ledger "report only" (the whole point of the new grace flag, which Slices 6 and 7 are built on), the gate prints the breach as a warning and then prints `OK: all managed ledgers within budget` on the very next line — for a file **52 times** over its cap. Anyone reading CI, or grepping for `OK:`, sees a clean pass. A test currently locks that in as correct. **Second:** the gate's own front door isn't sealed — a config saved in the wrong encoding, or a deeply-nested one, crashes with a raw Python traceback instead of the plain message the file promises three times over; a `docs/**/*.md` entry (the obvious way to say "everything under docs") silently measures **nothing** and reports OK; the same key measures **ten files on a developer's machine and zero in CI**; and one unreadable folder can throw away a real breach found seconds earlier in the same run. **Third:** several of the tests that vouch for this work don't actually test it — I broke the "works from any directory" guarantee and the "warn at 90%" threshold and all 505 tests stayed green.

**How bad, and what could break.** Nothing here is shipping to a user today: the new config is correctly stripped from the release, and the one thing that *does* reach adopters is a paragraph of documentation. The report-only bug can't fire in this repo yet either — no ledger is marked report-only. That's precisely why now is the moment to fix it: Slice 7 seeds adopter configs **with report-only flags turned on for files that are already over budget**, which means the first time a real adopter uses this feature, their gate would tell them everything is within budget while it isn't. The encoding and glob problems are one or two slices away from being wrapped in a "warn and pass" hook — the exact place where a crash turns into a silently skipped gate. And the documentation left behind still tells a future agent that the harness's caps live in a variable that this change deleted, in one place actually instructing that a live config file "would be a second cap source" and should never be tracked — an agent that obeys it deletes the config, and a deleted config makes this gate exit 0 in silence. Every item above is a small, bounded edit; none of them require rethinking the design.

#### Round 2 (delta) — `b6f17f0..31f690e`

**RUNNING AS: Opus 5.** Re-executed, not read: a fresh scratch worktree at `31f690e`, **18 mutants** (my round-1 survivors + 10 new ones aimed at everything round 2 added), all four deterministic gates, and an independent reproduction of the C6 must-land-together property. Source restoration verified (`git status` clean after each run).

**VERDICT: PASS — conditional on R4-a below. No further gate round.** Every A–D item landed, and several landed *better* than specified: A1 fixed the tuple so the docstring's existing claims became true rather than reworded (which is the standing preference), B threaded `graced` through `_summary_clause` rather than re-deriving it, and `REPORT_ONLY_MARK`/`_TAG` now derive from one token so the WARN line and the summary clause **cannot** disagree about what fired.

**Re-execution results.**

- **18/18 mutants KILLED**, including every round-1 survivor: `WARN_RATIO 0.9→0.5` · `→0.8` · `→0.95` (now dead in **both** directions) · `_repo_root` `Path(__file__)→Path.cwd()` (dead — it had survived the full 505-test suite) · summary-prefix reword. New round-2 mutants also all dead: `utf-8-sig→utf-8` · drop `object_pairs_hook` · `_validate_key`→no-op · drop the `**` refusal · drop the `*`-outside-final refusal · graced headline→OK headline · `entry_graced`→always-`False` · graced clause→`<= max` · drop the `iterdir` guard · drop the `UnicodeDecodeError` except · drop the `RecursionError` except · breach `>`→`>=` · `REPORT_ONLY_MARK` reword.
- **B rendering, both paths.** Graced: `OK: 1 report-only breach(es) NOT within budget (see WARN above) - CLAUDE.md OVER budget 100 bytes [report-only], OTHER.md <= 5000 bytes` — matches the specified form exactly, and a clean sibling entry keeps its `<=` clause. Glob-graced: `OK: 2 report-only breach(es) NOT within budget (see WARN above) - sh/*.md (5 files) OVER budget 100 bytes each [report-only]` — the headline counts **breaching files**, which is the accurate number, and the WARN lines enumerate which. Non-graced: byte-identical to the pre-round-2 form (asserted char-for-char).
- **SA2 confirmed end-to-end.** `main` (pre-slice, `DOC_BUDGETS` era) and `31f690e` produce the **identical** live summary line. The AC is intact; B changed the summary only in a regime this repo never enters.
- **A2 validator.** Refuses `docs/**/*.md` · `docs/deep/**` · `a**b/*.md` · `docs/*/x.md` · empty/whitespace keys; accepts `CLAUDE.md` · `*.md` · `docs/a/b/c.md` · and the production `docs/claugentic-decisions/*.md`. Messages name the entry, the reason **and** the corrected shape (`use "<dir>/*.<ext>"`, `put the * in the filename part`) — actionable, not just refusing. `_validate_key` is called first in `_parse_rule`, so a bad key is refused before its cap is read (pinned).
- **C2 boundary at exactly the cap.** Measured against `_check_one`: `0→None · 89→None · 90→warn · 99→warn · 100→warn · 101→error`, with `int(100*WARN_RATIO)==90`. At-budget is a **warn, not a breach** — exactly matching the `measured > max_bytes` semantics. The derived companion is **not** tautological: the fixed table is the anchor and the companion only pins that the constant is what moves the threshold (verified — the ratio mutants die on the table).
- **C6 reproduced independently.** Reverting only the trigger fixture (chdir fix alone) → **540 passed / 1 FAILED** from `tests/`; both applied → **541 passed / 1 xfailed** from `tests/` **and** from the repo root. Matches the implementer's report.
- **C7.** `strict=True` xfail with a bidirectional rationale, and the `check_shipped_content` note now names it as the mechanism. The promise is falsifiable.
- **Gates at `31f690e`:** doc-budgets · shipped-content (52 files, closure holds) · version-sync · architecture-tree — **all exit 0**.
- **D-c scope check (asked).** The "**Do not delete that file on the strength of the sentence above**" rider stays **inside** the superseded clause: its antecedent resolves unambiguously (the very next clause names the config's `.gitignore` un-ignore and its tracked-by-git pin), it addresses precisely the hazard the interim marker creates, and it introduces no new doctrine and no second cap-source claim. In scope, correctly bounded.
- **Also checked and clean.** D-f's tree entry now says "the file IS the list — no values restated here" and names the migration pin (removing the second cap copy **and** mapping L1) · WORKFLOW rung 2 states the harness-self extra (the byte-exact pin) so the "one-line bump" claim is no longer false · the shipped rung-2 pointer at the harness-self module docstring is explicitly caveated (`an adopter has no such script`) and passes the dangling-ref gate · C5's two named oracles are genuinely shared by the live and hermetic halves · C4's remaining coupling is stated at the site.

**R4-a (the one residual — the flagged judgment, and my own round-1 error).** In `TestInvokedFromASubdirectory.test_a_decoy_repo_in_the_cwd_cannot_redirect_the_gate`, replace `assert decoy.returncode == 0, decoy.stdout + decoy.stderr` with **`assert decoy.returncode == at_root.returncode`**, and keep the size-independent restatement (`assert "budget 10" not in decoy.stdout`) and the stdout equality. **Judgment: drop the absolute `rc == 0`, keep the restatement.** Three reasons. (1) `rc == 0` re-imports the exact live-size coupling C4 removed **one test above** — the file would state the principle in one test's comment and violate it in the next. (2) It buys **no** kill: under the anchor mutant the decoy's stdout diverges, so `stdout == at_root.stdout` and the `"budget 10"` restatement each already kill it — verified, `P4` stays **KILLED** with the residual applied and the module stays green (121 passed). (3) The relative form preserves the "same verdict from both cwds" pin and matches the sibling test's shape. **This was my error, not the implementer's:** I specified `assert rc == 0` in C1 in the same verdict where C4 struck that coupling — the implementer was right to flag it rather than apply it blind, and right to add the restatement in the meantime. Apply R4-a and this gate is closed; **no further gate round**.

**Still outstanding, unchanged (deferred to the orchestrator, as reported).** **D-i** (plan line ~383's "nothing reaches adopters from this slice" → "no shipped **behavior** change; the one shipped-**content** change is WORKFLOW's rung-2 paragraph") — wording supplied, applies at land. **E1** (Slice 6's `WORKFLOW:177` → content anchors; `doc-lifecycle.md:6` → its heading anchor) and **E2** (Slice 6's "both cap forms" → "all three cap forms (int · object · glob-by-key)" + add `doctor SKILL:95` to its sweep) — both verified still un-applied on `main` at plan lines 95 and 383. **L1–L5 and the ROADMAP routings stand unchanged**; note L1's touch-list drops to four places now that D-f trimmed the tree entry.

**Plain-English (round 2).** Everything I asked for is in, and the three things that mattered most are now actually true rather than merely written down: the gate can no longer print "all managed ledgers within budget" over a file it just measured at 52× its cap; it can no longer be handed a config it silently refuses to measure; and the tests that vouch for all of this now fail when I break the thing they name — I re-broke the same four things that slipped past last time and every one of them turned red. The one thing left is a single line in one test, and it is my mistake, not the implementer's: I asked for a check that quietly re-couples a unit test to how big this repo's documents happen to be today, one test after I had removed exactly that coupling elsewhere. The implementer spotted the contradiction and asked instead of applying it. Swap that line for the relative form beside it — I have already run it, and the test still catches the bug it exists to catch — and this slice is done.

### Slice 5 — Team wiring (before any chaining)

- **In plain English (shown first at the approval gate):**
  - **What this builds:** the pre-commit wrapper stops punishing teammates and starts surfacing what matters. Today a teammate without a working Python gets **every commit blocked by a cryptic error**, and any advisory output a gate prints on success (a WARN band, a report-only breach) is **silently discarded** — the R6 hole that would make Slice 7's grace flag a no-op. After this slice: a missing/broken interpreter prints ONE loud line and **passes** (your decision — infrastructure failure never blocks a commit); gate **warnings flow to the terminal on every commit** via a clean stream contract (warnings move to stderr, which the wrapper always lets through; OK stays silent); and the wrapper is shaped so Slice 7 chains the budget gate in with one line. `init` learns to **chain into husky** when a repo already uses it (propagation is usually free — **if** the repo's own `package.json` carries husky's `prepare` script, husky's default; the harness neither wires nor checks it), and the fence gains the one-line teammate bootstrap for non-husky repos. The DIH Tailwind incident (harness docs crashing an adopter's build via a broad content scanner) gets its caution in init's report and its DECISIONS record.
  - **What "done" means for you:** a python-less teammate commits with a visible skip-notice instead of a wall; the wrapper's advisory channel is live from this slice (the first gate that uses it is the doc-budget gate, chained in Slice 7); a husky repo gets a *live* gate instead of a written-but-inactive one.
  - **What you're accepting:** warn-and-pass means the gate is **advisory on interpreter-less machines** (loud at every commit; doctor has no interpreter-health row today — Slice 6 adds it, L9 — so noticing and fixing it stays human-upheld) · the husky chain is an **append with idempotency markers** into `.husky/pre-commit`, offered via a question with chain as the default, decline recorded — never an overwrite · warnings moving to stderr is a **stream-contract change** to `check_doc_budgets.py` (CI logs merge streams; local `2>/dev/null` users would newly hide warnings — accepted) · no mechanical detection of content scanners (the Tailwind caution is prose — YAGNI on config-sniffing).
- **Files & changes:**
  - `.githooks/pre-commit` — rework: pick interpreter → **probe it** (`"$PY" -c ""`; catches both absent and the Windows-Store stub that exists-but-exits-nonzero) → probe fails ⇒ one stderr line ("claugentic tree gate SKIPPED: no working python3/python — install Python or re-run /claugentic-dev-harness:init") + exit 0 · run the gate with **stdout captured, stderr flowing through** (advisory channel always visible; no more `2>&1`) · exit 0 ⇒ silent pass; exit ≠0 ⇒ print captured stdout, exit 1 · a small `run_gate <script>` function so Slice 7's chaining is one added line · git-failure-passes and `--staged` unchanged.
  - `scripts/check_doc_budgets.py` — `main()` prints WARN lines to **stderr** (the advisory channel the wrapper surfaces); problem lines and the OK summary stay on stdout. Existing capsys assertions updated (forced seam edits, named).
  - `skills/init/SKILL.md` — the wrapper template block updated to the same logic (its "byte-identical run-logic" claim becomes **test-pinned**, see below) · the never-clobber section gains the **husky-chain offer**: when `core.hooksPath` is `.husky`/a husky install is detected, ask (default: chain) and append a marker-guarded `sh "$(git rev-parse --show-toplevel)/.githooks/pre-commit"` block to `.husky/pre-commit`; idempotent on the marker; decline recorded like the gate-off choice · the fence content gains the non-husky teammate bootstrap line ("hooks never auto-activate on clone — run `git config core.hooksPath .githooks` once, or re-run init"; husky path: nothing needed, say so) · the docs-scanner caution sentence in the report step (adopter-aware wording — name the incident class, no stripped paths).
  - `tests/` — **the wrapper battery** (sh subprocess; a guard that hard-fails when `CI` is set and `sh` is absent, skips locally): missing interpreter → exit 0 + the stderr notice · stub interpreter (fake python exiting nonzero) → same · gate red → exit 1 + output shown · gate green with stderr WARN → exit 0 + WARN visible · green clean → silent · git absent → exit 0 · **the template-parity pin**: extract the wrapper block from `skills/init/SKILL.md` and assert its run-logic matches `.githooks/pre-commit` (normalized) — the shipped claim "byte-identical to the shared wrapper" stops being prose · WARN-to-stderr assertions in the budget-gate suite.
  - `docs/claugentic-ARCHITECTURE_TREE.md` — refreshed entries; DECISIONS lines at land (warn-and-pass posture + the Tailwind incident).
- **In-scope standards dimensions:** `reliability-resilience` (the probe's totality; the stream contract; failure modes) · `testing` (the battery incl. the parity pin — no weakening) · `docs-traceability` (init prose + fence + the shipped-content cleanliness of every edit) · honesty on the wrapper's printed claims.
- **Tests to add:** per above; mutation-shaped: probe removed ⇒ the stub case fails; stderr-flow removed ⇒ the WARN-visible case fails; marker-idempotency removed ⇒ the double-append case fails (hermetic husky fixture).
- **Acceptance criteria:** full battery green · this repo's own commit flow: green-clean commits stay silent, a synthetic WARN is visible at commit time (demonstrated) · shipped-content green (init SKILL edits adopter-aware) · the parity pin red if either wrapper home drifts · doctor's hook-wiring row is unchanged and still correct for the two wirings it knows; the new husky-chained wiring is reported as a `core.hooksPath` conflict until Slice 6 teaches it the third (L9).

### Slice 5 — Verify verdict (Stage 7)

**RUNNING AS: Opus 5 (1M context)** — the same model family as the panel and (per the orchestrator) the builder. The independence here is **role + clean context**, never model: a reduction of rubber-stamping risk, not a guarantee.

**VERDICT: CHANGES REQUIRED** — one implementer pass. Four lenses ran (reliability · honesty · docs-traceability · testing); no yagni-sentinel, so I applied the YAGNI weighing myself and cut/narrowed three prescriptions below. **Two blocking defects reproduced in scratch repos by me, not read** — and **one panel-prescribed fix re-creates the very bug it fixes** (H6). The slice's core is sound: the stream contract is correct and end-to-end proven, the parity pin is a real mechanism, the battery is honest about its own environment. What fails is **totality** — the wrapper's promise ("infrastructure failure never blocks a commit") is false in two reachable shapes, and the husky chain, which lands in a *tracked* file, can block **every teammate's** commit.

**What I re-executed (evidence, not report).** Full suite on `b5db261`: **563 passed / 1 xfailed**; all four gates exit 0. Then, in scratch repos through real `sh`:

- **R-1 reproduced verbatim.** PATH = stub `python3` (exits 9) + **working** `python` + `git` ⇒ the wrapper printed `claugentic tree gate SKIPPED: no working python3/python` and exited 0, while `python scripts/…tree.py --staged` in the same environment printed `OK`. The gate is permanently disarmed on the *most common Windows shape*, with a factually false message. The battery cannot see it — `env_with_stub_python` never contains a working sibling (`tests/test_precommit_wrapper.py:147-159`).
- **R-2 reproduced.** Gate script absent, real Python present ⇒ **rc 1**, stdout empty, stderr = `python.exe: can't open file '…tree.py': [Errno 2]`. A commit rejected by infrastructure, with the raw interpreter error as its only explanation — the exact inversion of `.githooks/pre-commit:5-6`.
- **The prescribed fixes, validated end-to-end** (five scenarios, all green): stub+working ⇒ **gate RAN**; stub+working with a RED gate ⇒ still **rc 1** (the fallback does not weaken the gate); no Python ⇒ **exactly one** stderr line (the shell's own "command not found" *is* suppressed by the probe's redirection — so G6's `len(splitlines()) == 1` is satisfiable without a `command -v` pre-check); stub-only ⇒ one line; missing script ⇒ loud skip, rc 0.
- **H6's correction, measured.** `[ -f "$hook" ] && { sh "$hook" || exit 1; }` as the **last** line of `.husky/pre-commit` (which append-at-EOF guarantees) returns **1** when the wrapper is absent ⇒ the husky hook exits 1 ⇒ **every commit blocked** — the panel's R-3 fix reintroduces R-3. The `if … then … fi` form returns 0. Both measured.
- **Bounds measured, not reasoned.** The WARN-emitter sweep across `scripts/` returns exactly **two** sites — `check_doc_budgets.py` (fixed here) and `check_shipped_content.py:650` (not) — so R-5's sibling set is closed at one. Live ledger headroom for the land commit: `plugin-distribution.md` 11,777 B of 14,000 (WARN at 12,600 ⇒ **823 B** before the band); `INVARIANTS.md` 12,910 of 20,000. The land obligations fit, but L1 is the one to keep tight.
- **G9's vacuity confirmed by running the gate:** today `check_doc_budgets.py` emits `OK: all managed ledgers within budget` with **empty stderr**, so the two new `stderr == stderr` assertions compare `"" == ""`.

---

## Required changes (one pass) — disposition: **fix-in-this-pass**

Every wrapper edit lands in **both homes** (`.githooks/pre-commit` + the template in `skills/init/SKILL.md:492-527`); `TestTemplateParity` makes that mechanical, so drift is not a risk — forgetting is.

### A. Wrapper totality (blocking)

**W1 [blocking, R-1] — probe *candidates*, not the pre-picked one.** Replace the `command -v` selection + `"$PY" -c ""` probe with the loop below. **Version floor `(3, 7)`, not the panel's `(3, 8)` — my correction:** the two hook-wired gates record their own floor at `scripts/claugentic-check_architecture_tree.py:446` and `scripts/check_doc_budgets.py:579` (`stream.reconfigure(…)  # Python 3.7+`), both use `from __future__ import annotations` (3.7+), and **neither uses a 3.8-only construct** (the walrus is at `check_shipped_content.py:437`, which is harness-self and never hook-wired). A `(3, 8)` floor would loudly skip on an interpreter both gates run on *and* would plant a second, undocumented version claim that disagrees with the code's own comments — the duplicate-source-of-truth failure this plan has already fixed twice.

```sh
# PROBE each candidate, never merely its presence. The Windows-Store `python3` STUB exists on
# PATH and exits non-zero, and it commonly sits BESIDE a working `python` — picking first and
# probing second disarms the gate on the most ordinary Windows machine. Python 2 answers `-c ""`
# happily and then dies on the gate with a SyntaxError, so the probe asserts the VERSION the
# gate scripts record for themselves (`# Python 3.7+` in each gate's `_force_utf8_output`).
# Raise this floor only when a hook-wired gate raises its own.
PY=
for cand in python3 python; do
  if "$cand" -c 'import sys; sys.exit(0 if sys.version_info >= (3, 7) else 1)' >/dev/null 2>&1; then
    PY=$cand
    break
  fi
done
if [ -z "$PY" ]; then
  printf '%s\n' "claugentic tree gate SKIPPED: no working Python 3.7+ on PATH (tried python3, python) - install Python 3 and the gate resumes on your next commit (no re-init needed)" >&2
  exit 0
fi
```

That notice text also discharges **F3**: `re-run /claugentic-dev-harness:init` is inert at this surface (init does not bake the interpreter into the file — `SKILL.md:529-532` says so; the reader is a teammate at `git commit` who may not have the plugin; and init accepts `py`, which the wrapper never tries). Drop the re-init remedy in **both** homes.

**W2 [blocking, R-2] — an existence guard inside `run_gate`, before invocation.**

```sh
if [ ! -f "$root/$gate" ]; then
  printf '%s\n' "claugentic gate SKIPPED: $gate is not in this checkout" >&2
  return 0
fi
```

Reachable via keep-mine-gate-off re-init leaving an old hook, a gitignored `scripts/`, sparse checkout, or a mid-`init` clone.

**W3 [R-6] — per-gate args. IN-SLICE (adjudication 5: reliability is right, it is cheaper now).** It is the *same function* W2 already rewrites, it costs two tokens, and the existing `test_the_gate_is_scoped_to_the_staged_set` keeps passing unchanged: `run_gate() { gate=$1; shift; … "$PY" "$root/$gate" "$@" … }` with the call site `run_gate scripts/claugentic-check_architecture_tree.py --staged`. Left as-is, Slice 7 chains an argv-ignoring gate behind a hardcoded `--staged` and the header's own scoping claim becomes false the moment an unstaged over-cap edit blocks an unrelated commit.

**W4 [blocking, F1] — "passes LOUDLY" is false for half its subject; sweep all four sites.** The git-failure branch passes **silently** (`root=$(… 2>/dev/null) || exit 0`) — and its own test is named `test_a_failing_git_passes_without_a_word`, asserting `stderr == ""`. Un-hedged sites (swept; `SKILL.md:473-476`'s prose is the correctly-split sibling and stays): `.githooks/pre-commit:6` · `skills/init/SKILL.md:497` · `docs/claugentic-ARCHITECTURE_TREE.md:79` · `:120`. Honest split: **a broken git passes silently (there is no repo to report into); a missing/broken Python and a missing gate script pass loudly.**

**W5 [F2] — the header's totality claim, resolved jointly with W2. The residual, decided (adjudication 1):** after the existence guard, *missing* skips — but a gate script that **exists and crashes on import** still exits non-zero and still blocks, and **that is the correct posture**: a broken gate script is a repo defect the whole team should see, not a teammate's machine. Do not extend fail-open to it. Header text:

> INFRASTRUCTURE THAT CANNOT BE REACHED NEVER BLOCKS A COMMIT — a broken git, no working Python 3.7+, a gate script that is not in this checkout. Anything a gate ITSELF exits non-zero on aborts the commit, **including a gate that crashes on import**: a gate present-but-broken is a repo defect, not a teammate's machine.

### B. The husky section + the fence (blocking)

Rewrite `skills/init/SKILL.md:550-584` as one ordered procedure — **this order** (adjudication 2), because each step is a precondition of the next:

**H1 [DT-3] — scope first.** The husky bullet currently sits as a **peer of Gate OFF** (`:535` vs `:550`), so a literal reader on a gate-off repo chains to a wrapper that was never written. Open with *"Only when the gate is ON (the wrapper was written in this run or already exists)"*. (Pre-existing peer ambiguity; this diff is what makes it consequential.)

**H2 [DT-2] — read the record before asking.** `:576-579` promises "a re-run never re-asks" with **no reader**. Prepend: *"Read the `- Husky chain:` line in the detected-tooling block FIRST; if present, honor it and skip the offer (report the recorded outcome)."* Every sibling recorded-choice line in this skill states its reader explicitly.

**H3 [R-4] — trackability precondition; decided: REFUSE-to-chain, not warn-and-chain.** Before offering, run `git check-ignore -v .githooks/pre-commit`. If ignored/untrackable: **do not append**, report the reason + the one-line fix, and **record nothing** (so a fixed repo is re-offered). Rationale: this mirrors init's own precedent — it makes `.claude/settings.json` trackable *before* wiring it (`SKILL.md:556-562`) and has a fail-loud `git check-ignore` guard at `:886`. With H6 in place a missing wrapper degrades to a silent no-gate rather than a block, so warn-and-chain would be *safe* — but a silent disarm is precisely the class this slice exists to kill, and failing at `init` in front of the user beats failing at every teammate's commit. Report it like the settings.json step.

**H4 [F6] — reachability, and report "appended", not "chained".** The block is appended at EOF into a file init never parses; an existing `exit 0` above it makes it dead while the report and the fence both say chained. Add: *"scan the existing file for an unconditional `exit` above the append point — if one exists, say so and do not call the gate live."* Report register: **"appended"**.

**H5 [R-9] — a failed read STOPS.** An unreadable `.husky/pre-commit` currently degrades to "marker absent" ⇒ duplicate chain. One clause: *"if the file cannot be read, STOP and report — never append."*

**H6 [blocking, R-3 — with my correction; the panel's own form re-creates the bug].** The block goes into a **tracked** file, so `sh <missing wrapper>` (exit **127**) blocks **every teammate**. But the panel's guard `[ -f "$hook" ] && { sh "$hook" || exit 1; }` **returns 1 when the file is absent**, and append-at-EOF guarantees it is the hook's last command ⇒ husky exits 1 ⇒ every commit blocked anyway. **Measured both ways.** Use the `if`/`fi` form:

```sh
# >>> claugentic-dev-harness tree gate (managed marker — do not duplicate)
# A MISSING wrapper must not block anyone: `if`/`fi` (never `[ -f … ] && { … }`, which returns
# 1 when the test fails and would abort the commit from the hook's last line). A wrapper that
# RUNS and fails still blocks — that is the gate doing its job.
hook="$(git rev-parse --show-toplevel 2>/dev/null)/.githooks/pre-commit"
if [ -f "$hook" ]; then sh "$hook" || exit 1; fi
# <<< claugentic-dev-harness tree gate
```

**H7 [F5] — honest propagation register, 3 sites** (`SKILL.md:580` + the step-9 report + plan `:524`): detection matches a bare `.husky/` dir with **no `prepare` script**, and propagation is entirely the adopter's `package.json`. → *"usually free — **if** the repo's own `package.json` carries husky's `prepare` script (husky's default). The harness neither wires nor checks it."*

**H8 [blocking, F4 + DT-1] — rewrite the fence's bootstrap line; it covers TWO hazards.** `SKILL.md:719-723` is written byte-identical into every teammate's always-loaded `CLAUDE.md`. (a) *"there is nothing to do — the harness check is chained"* is **false in the recorded, supported DECLINED state**; (b) *"If this repo wires them via `.githooks/`, run `git config core.hooksPath .githooks`"* invites a husky-repo reader — who has no way to self-classify — to point git **away from `.husky`** and **disable the adopter's own lint-staged hooks**. Replace with a **check-first, conditional-per-branch** block:

> **New clone? Hooks never activate automatically** (git's design) — check before you commit: run `git config --get core.hooksPath`.
> - prints `.githooks` → wired; nothing to do.
> - prints nothing → run `git config core.hooksPath .githooks` **once per clone** (or re-run `/claugentic-dev-harness:init`).
> - prints `.husky` or `.husky/_` → **do not change it** (that would disable this repo's husky hooks). Run `npm install` so husky installs its hooks; the harness check runs too **only if** `.husky/pre-commit` contains the `claugentic-dev-harness tree gate` marker — grep it, and if it is absent re-run `/claugentic-dev-harness:init`, which can chain it.

And fix the meta-claim at `:718`: *"(it states facts, so it stays true whichever way this repo is wired)"* is the laundering sentence — it is what let an unconditional falsehood through. → *"one fixed block, byte-identical every run: it tells the reader how to **check**, so it needs no per-repo variation."*

**H9 [DT-1 minor] — the step-9 report (`SKILL.md:1022-1029`) lacks the fourth, solo outcome:** solo mode skips the offer entirely (`:582-584`), so a solo husky repo must report the `core.hooksPath` conflict per solo divergence (b).

### C. The Slice-7 present-tense family (adjudication 3)

**S1 [F7 + DT-4] — `scripts/check_doc_budgets.py`'s new docstring present-tenses S7's wiring.** *"a report-only breach is visible at every commit"* — the gate is **not chained** (its own tree entry hedges correctly: *"Not hook-wired"*). Recast as the one-clause future form (*"…so that, once Slice 7 chains this gate in, a report-only breach is visible at every commit"*). Same nit in the wrapper header, both homes: its two named examples (WARN band, report-only breach) **both live only in the unchained gate** — name the tree gate's verdict output as today's live case.

**S2 [G3] — the tripwire.** The stream-contract payoff clause has no falsifier; the only strict xfail in the repo is S4's (`tests/test_check_shipped_content.py:329` — verified). Add the house-pattern 3-liner to `tests/test_precommit_wrapper.py`: `test_the_budget_gate_is_chained_into_the_wrapper`, `strict=True`, reason naming Slice 7, asserting `check_doc_budgets` appears in `.githooks/pre-commit`. Red today, flips the suite red the instant S7 lands — and write the abandonment branch into the reason (if S7 is dropped, the stream contract is re-registered as tree-gate-only).

**Standing instruction (adjudication 3 — this is the THIRD consecutive slice for this class; I am naming it rather than leaving it to harvest).** See **L8**: one line into the plan's remaining-slice preamble, plus a Stage-9 harvest candidate. The S4 riders caught it at review all three times, which means the *review* mechanism works and the *authoring* rule is missing.

### D. Tests (the oracle half)

**T1 [G1]** — the missing-gate-script case (pins W2's posture: rc 0 + the notice on stderr + the fake gate proven never to have run).
**T2 [R-1's new case, the one the battery structurally cannot see today]** — stub `python3` **plus a working `python`** ⇒ assert the **gate RAN** (fake gate writes a sentinel) and that a RED gate under that PATH still returns rc 1.
**T3 [G5]** — the stub must log its own invocation and the test must assert the log; today `PY=python` (never select `python3`) survives in **both** homes, 19/19 green, while the test name claims the probe caught the stub.
**T4 [G6]** — `assert len(stderr.splitlines()) == 1` in both notice cases (measured achievable with W1's loop).
**T5 [G7]** — one from-a-subdirectory case; the cwd-anchored mutant survives 19/19 today while three sites assert worktree-safety.
**T6 [G4, deterministic half]** — extract `_shell_or_fail(found, ci)` from the `sh` fixture and unit-test **both** branches; today `if CI:` → `if False:` leaves 19/19 green, i.e. the anti-vacuity guard is the one thing in the battery with no test.
**T7 [G2]** — the husky idempotency **rule** is unpinned: deleting the SKILL's "Idempotent on the OPEN marker" bullet, or inverting it, leaves all four husky tests green — only the test-local `_chain` reimplementation is proven. Add a SKILL-anchored assertion (marker-as-idempotency-key + the refusal wording + check-BEFORE-append present in the section text) and rename the class to say what it actually pins. **Same edit replaces the literal `|| exit 1` pin at `:447` with H6's semantic**: *a missing wrapper does not block; a failing wrapper does* — exercised through `sh`, not asserted as a substring.
**T8 [G8 — narrowed, my correction]** — the parity normalizer (`_run_logic`) is shell-context-blind, but **do not** add the odd-quote-count check: the skip notice is prose and one reworded apostrophe ("doesn't") would turn the parity pin red for a non-behavioral reason. Add the **heredoc precondition only** (`assert "<<" not in script`) plus one docstring line naming the residual limitation (line-based; assumes no line-spanning quotes). Latent, not live — this is the cheap honest guard, not a shell parser.
**T9 [G9 — honesty-note, keep the assertions]** — measured: this repo's live gate emits an empty stderr today, so both new `stderr == stderr` lines compare `"" == ""`. Keep them (they arm themselves the moment any ledger enters the WARN band, and they cost nothing) and correct the comment, which currently claims restored strength: *"today both sides are empty — no ledger is in the WARN band; this arms itself the moment one is."*

### E. In-slice code/doc (the sibling rule)

**E1 [R-5, reverse absorption] — `scripts/check_shipped_content.py:650` prints `WARN:` on stdout.** The slice's own fix (`check_doc_budgets.py` WARN → stderr) names the class; the sweep shows exactly **one** unfixed instance, in a file whose sibling ERROR lines already use stderr (`:627`, `:646`). One token + one capsys seam edit, no second correctness requirement ⇒ folded in. Payoff at land: **DT-7's invariant lands with zero known non-conformers** instead of shipping with one named in its own text.

**E2 [DT-5, ships] — `docs/claugentic-PLAYBOOK.md:85`** *"the same result on every machine and in CI"* is now false in exactly the new case → *"…on every machine with a working Python — without one the gate skips loudly and the commit passes."*

**E3 [R-8, confirmed in-slice (adjudication 6) — with one addition of mine]** — init step 5b appends `.githooks/** text eol=lf` to `.gitattributes` (append-if-absent). This repo self-protects via a global `* text=auto eol=lf`; do **not** impose that global form on an adopter. **Shared mode ONLY** — `.gitattributes` is a *tracked* file, so editing it in solo mode would violate the solo invariant, and the solo hook lives at `.git/hooks/pre-commit`, which git never normalizes. Honest register: I could not execute `dash` on this platform, so I ratify this on cost (one line) and blast radius (a CRLF-committed multi-line wrapper is a parse error for every `/bin/sh`-is-dash teammate), not on a reproduction — keep that caveat out of shipped copy; the line itself needs no claim.

---

## Land-commit obligations (owner: the orchestrator, not the implementer)

**L1 — DECISIONS repair (a):** `docs/claugentic-decisions/plugin-distribution.md:22` — the *"resolve-once, exit-code-preserving"* rationale now reads as the opposite of this slice's headline (fail-open is no longer advisor-only), and *"written with the DETECTED interpreter"* was **already false on `main`**. Amend to the form drafted in the docs-trace report. **Budget note: this shard has 823 B before its WARN band — keep the amendment tight or condense in the same commit.**
**L2 — DECISIONS repair (b):** `docs/claugentic-decisions/deterministic-gates.md:7` — *"never-clobbers (reports + continues)"* is incomplete for the new husky branch; add the offer clause.
**L3 — the two originally-planned DECISIONS lines:** the warn-and-pass posture (user-directed, approved) · the DIH Tailwind content-scanner incident.
**L4 — a new INVARIANTS entry (DT-7):** *a chained gate's advisory output rides stderr — the wrapper eats stdout*, with the enforcement split stated honestly (wrapper half test-pinned; per-gate half model-upheld). With E1 landed it names **no** non-conformer. (`INVARIANTS.md` is at 12,910/20,000 — ample room.)
**L5 — CHANGELOG `## Unreleased`:** S5 ships adopter-visible behavior (init's husky question, an append into a tracked file, the wrapper's warn-and-pass + stream contract, the fence bootstrap line). S4 correctly had none; S5 differs.
**L6 — plan-copy amendments** (see the Spec-amendment block below): `:525` (F8, dated) · `:526` (the doctor claim struck) · `:535` (the AC reworded).
**L7 — `docs/claugentic-ARCHITECTURE_TREE.md:79` locator trim:** drop the restated literal `"$PY" -c ""` command per the freshly-harvested locator clause (the entry locates; the file states).
**L8 — the standing instruction + harvest candidate:** one line into the plan's remaining-slice preamble — *"any sentence describing a capability a later slice wires rides the future tense AND a strict-xfail tripwire naming that slice"* — and register the general rule as a **Stage-9 harvest candidate** with a proposed home (`docs/claugentic-standards/docs-traceability.md`, a new dimension: *forward promises are dated and carry a falsifier*). There is no `honesty.md` standards module; honesty lives in the reviewer role + INVARIANTS, which is why this keeps landing at review instead of at authoring time. **Do not invent the module in this pass** — route it.
**L9 — Slice 6 scope additions (adjudication 4, "strike vs build" → strike now, build there):** add to the Slice 6 bullet (a) a **doctor interpreter-health row** (run the hook's own probe; report SKIPPED as a flag) and (b) a **husky-aware hook-wiring row** (a chained `.husky` repo is a supported third wiring today, and doctor `SKILL.md:143-146` files it as a foreign-hooks-path conflict). Slice 6 already opens that file; a ROADMAP routing would outlive the plan file that promises it.

---

## Spec amendments (Stage 7 procedure: `docs/claugentic-WORKFLOW.md` → Stage 7)

The implementation is faithful to these lines; **the lines are wrong** — a spec-conformant diff is no defense.

1. **`:525` (F8) — WORDING amendment; it DATES the promise, it does not change what the user accepted.** *"a repo at a WARN band sees it at every commit instead of never"* is Slice 7's outcome: the only chained gate is the tree gate, and it writes **everything** to stdout (verified — `claugentic-check_architecture_tree.py` has no stderr emission anywhere), so at land **no commit surfaces any WARN**. The mechanism-fix *is* Slice 7, and the user accepted the wrapper + the stream contract, which both land here in full. → *"the wrapper's advisory channel is live from this slice; the first gate that uses it is the doc-budget gate, chained in Slice 7."* **No return to the user required — but the tense correction is not cosmetic**: without it, the stream contract ships with zero live consumers while the copy says otherwise.
2. **`:526` (R-7 / F9) — MATERIAL; surface to the user at land.** *"warn-and-pass means the gate is advisory on interpreter-less machines (loud, **and doctor flags it**…)"* names a compensating control that **does not exist**: doctor has no interpreter-health row (it checks file presence + `hooksPath`, and runs gates with the *agent's* interpreter — a different resolution path entirely), and the wrapper's own notice is stderr-on-exit-0, which GUI clients that show output only on failure will drop. **The posture itself is approved and is not re-litigated** — only its stated mitigation is corrected. Strike "and doctor flags it"; state the residual honestly (*the only signal is one stderr line at commit time; a doctor row is routed to Slice 6*), and tell the user in one line at land.
3. **`:535` (F10) — AC reword.** *"doctor's existing hook-wiring row still true (no contract change to what doctor asserts)"* is technically satisfiable and misleading: the row's text is unchanged, but this slice creates a **third** wiring (husky-chained, `hooksPath=.husky`) that doctor reports as a conflict. → *"doctor's hook-wiring row is unchanged and still correct for the two wirings it knows; the new husky-chained wiring is reported as a `core.hooksPath` conflict until Slice 6 teaches it the third (L9)."*

---

## ROADMAP (genuinely separate future work)

- **R-6's successor:** nothing. (Folded in — see W3.)
- **F11 — the Tailwind caution's trigger is never armed.** Step 8's scan list (eslint / tsconfig / test-runners / CI) contains **no content-scanner-class tool**, so on the motivating repo the new caution at `SKILL.md:1044-1055` would never fire. Two honest options; **take the cheap one in-slice if it is one clause** (make the caution name its own sources — Tailwind/UnoCSS config, docs indexer, SSG/codegen with broad globs — so it is self-triggering prose rather than a dependent on step 8); **route widening step 8's detection to ROADMAP** (that is a detection-surface change with its own test burden, and the plan explicitly chose "no mechanical config-sniffing — YAGNI").
- **No timeout on the probe or the gate invocation** (noted by reliability, not a finding): there is no portable POSIX fix. One honest clause in the wrapper header is the right treatment; a `timeout(1)` dependency is not.

## Watch-item (not a fix — adjudication 9)

- **G4's Windows-CI `sh` risk.** `tests/test_precommit_wrapper.py` is the repo's **first** `sh`-dependent test, and the fixture **hard-fails** (by design) when `CI` is set and `sh` is absent. GitHub's Windows runners ship Git Bash, but its presence on the default `PATH` for the `python -m pytest` step is not something I can verify from here. **The land PR's CI run is the check — read the Windows legs specifically before merge.** If it fails there, the fix is PATH/shell setup in the workflow, never loosening the guard to a skip (that would re-create exactly the false green the guard exists to prevent).

## Explicitly dropped (with reason — nothing silent)

- **X1 — `(3, 8)` as the version floor (R-1's second half).** Dropped in favor of `(3, 7)`; evidence and reasoning in W1. A floor above the code's own recorded requirement is a second source of truth and a false skip.
- **X2 — the odd-quote-count half of G8.** Dropped: it would turn the parity pin red on an apostrophe in prose (a reword to "doesn't"), trading a latent false-green for a live false-red. The heredoc guard (T8) covers the realistic blind spot.
- **X3 — "extend fail-open to a gate script that crashes on import" (a possible reading of F2).** Dropped deliberately: a present-but-broken gate is a repo defect the whole team should see. W5 states that boundary instead of widening it.
- **X4 — the cosmetic `not in out+err` concatenation nit** (testing lens): dropped, no behavioral or clarity gain.
- **X5 — re-litigating warn-and-pass, commit-time-only enforcement, or default-chain.** Not dropped-as-refuted — **out of bounds**: user decisions. Every finding above refines the *wiring and the register* of that posture; none questions it.

---

## Definition of Done check

| | |
|---|---|
| **Acceptance criteria** | ✗ — the battery is green (563 passed / 1 xfailed) and the parity pin is real, but AC-5 is falsified as worded (F10 → amendment 3), and "a synthetic WARN is visible at commit time" is demonstrable only with a synthetic gate, never with a chained one (F8 → amendment 1). |
| **In-scope dimensions** | `reliability-resilience` ✗ (W1, W2, W3, H3, H6, E3) · `testing` ✗ (T1–T9) · `docs-traceability` ✗ (H1–H9, E2, L1–L7) · honesty ✗ (W4, W5, S1, H7, H8, amendments 1–3). |
| **Missed dimension (spec scoping)** | The spec scoped honesty to *"the wrapper's printed claims"* — but the findings that bit hardest are **documentation** claims (the fence written into every teammate's `CLAUDE.md`, the SKILL prose, two tree entries, the plan's own approval-gate copy). **Honesty is in-scope for every surface a slice writes, not only its runtime output** — carry that into the remaining slices' specs. `maintainability-structure` was unnamed but is met: the two-home duplication has a real DRY mechanism (`TestTemplateParity`). |
| **All gates green** | ✓ at `b5db261` — tree · version-sync · shipped-content · doc-budgets all exit 0; suite green. |
| **No new debt** | ✗ until the above lands: two reachable commit-blocking paths (one team-wide, in a tracked file), one factually false runtime message, four un-hedged claims in shipped/always-loaded copy, one unfixed WARN-stream sibling, and six mutants surviving the new battery. |

## Plain English (what's wrong · how bad · what could break)

The good half is genuinely good: warnings now come out of a separate pipe so a passing commit can stay silent without swallowing them, and the copy that tells adopters how to write the hook is now checked by a test instead of trusted — that is the thing this repo keeps getting wrong, done right.

The bad half is that the hook's headline promise — *"a broken setup never stops you committing"* — is false in two ordinary situations, and I reproduced both rather than reading them. On a very common Windows setup (a fake `python3` shortcut sitting next to a real `python`), the hook decides there is no Python at all, says so untruthfully, and quietly stops checking anything **forever** — the repo's guard is off and nobody is told. And if the checking script is missing from someone's checkout, their commit is **rejected** with a raw Python error and nothing else — the exact wall this slice was written to remove.

The most dangerous item is the husky feature, because its wiring goes into a **shared, committed** file: as written, if the harness's own hook file is missing on a teammate's machine, **every commit for every teammate on the project fails**. The panel's proposed fix has the same defect through a different door — I ran both forms and measured it — so take the corrected shape here, not the one in the report.

Everything else is truthfulness in writing: four places say a broken git "passes loudly" when it passes in total silence; a line that will be copied into every teammate's always-loaded instructions tells husky users "there is nothing to do" even when the user declined to wire anything, and tells them to run a command that would **switch off their own project's hooks**; and the plan promises the user two things that only arrive one slice later, plus one safety net (`/doctor` noticing a skipped gate) that does not exist at all. The posture the user chose is not in question anywhere here — only whether the code and the copy tell the truth about it.

#### Round 2 (delta): `b5db261..ccb4213` — **PASS, conditional on R5-a + R5-b. No further gate round.**

**RUNNING AS: Opus 5 (1M context).** Re-executed, not read: every disposition below was checked against the diff and, where it has runtime behavior, run.

**Re-execution.** Suite at `ccb4213`: **581 passed / 2 xfailed**; all four gates exit 0; LF confirmed on the touched files. **My own round-2 mutants: 10 of 12 killed**, both survivors dispositioned below.

| mutant | result |
|---|---|
| revert to pick-then-probe (undo R-1) | **killed** (3 tests) |
| delete the existence guard inside `run_gate` | **killed** (2) |
| floor `(3, 7)` → `(3, 6)` | **killed** (2) |
| husky block `if`/`fi` → `[ -f … ] && { … }` | **killed** (2) |
| revert per-gate args to a hardcoded `--staged` | **killed** (1) |
| invert "the marker **is** the idempotency key" | **killed** (1) |
| revert `check_shipped_content` WARN to stdout | **killed** (1) |
| swap loop order to `python python3` | **killed** (1) |
| drop the loop's `break` | **killed** (1) |
| delete husky step 4 (trackability) wholesale | **killed** (1) |
| substitute the `git check-ignore -v` **command** (leave the cross-reference) | **SURVIVED** → R5-b |
| both tripwires `strict=True` → `False` | **SURVIVED** → N2 (dropped, with reason) |

**Direct measurements (the questions you asked me to spot-check).**

- **W1 loop when `python3` works.** Instrumented both interpreters and ran the shipped hook: `python3` invoked exactly **twice** (probe, then gate), the `python` log **empty**. The `break` holds, order is python3-first, and no needless probe happens. Both properties are *pinned*, not merely true — the order-swap and `break`-drop mutants each turn a test red.
- **H6 as landed, through real `sh`, using the block text extracted from the shipped SKILL** (not the test helper): wrapper **absent** ⇒ rc 0 (no team-wide block) · wrapper **failing** ⇒ rc 1 with its message · wrapper **passing** ⇒ rc 0 · **outside a git repo** (rev-parse fails, empty substitution) ⇒ rc 0. Matches my prescription exactly, including the inline `never [ -f … ] && { … }` comment that records *why*.
- **H3's record-nothing choice genuinely re-offers.** Traced the loop: step 4 refuses and writes no record → step 2's reader finds no `- Husky chain:` line on the next run → step 3 re-detects → step 4 re-checks → a fixed repo reaches step 5's ask. No stale decline can strand it. Deleting step 4 entirely is caught.
- **T7 anchors on the SECTION, not the file.** `_husky_section()` slices from the husky heading bullet to `> **Solo divergence (b)`, so a rule deleted from the procedure but surviving elsewhere in the 1,400-line skill still turns it red — and a reworded anchor raises `ValueError` (fails loud) rather than silently matching nothing. The strong form.
- **Both tripwires are `strict=True`**, and the new one writes its **abandonment branch** into the `reason` (if S7 is dropped, re-register the stream contract as tree-gate-only and reword both homes) — the falsifier is honest in both directions.
- **The four "passes LOUDLY" sites: all swept**, and the sweep is clean repo-wide — `.githooks/pre-commit:11`, `SKILL.md:517` (template header), `tree:79`, `tree:120` each now split silent-git from loud-Python/loud-missing-script. `SKILL.md:478` and `:574` were already correct.

**Landed better than prescribed (recorded, not just credited).** `test_the_version_floor_matches_what_the_gate_scripts_record` turns my W1 *comment* about drift into a **mechanism**, pinning the wrapper's `(3, 7)` against the `# Python 3.7+` marker in both hook-wired gates — that is the DRY guard the floor needed and I did not require. `test_a_gate_that_crashes_on_import_still_aborts` pins W5's boundary rather than restating it. The parity non-vacuity guard went from `>= 8` lines to `>= 12` **plus five property anchors** (candidate loop · version floor · existence guard · call-site args · both notices) — strictly stronger. And T8's rejected half is argued **in the code** (`_run_logic`'s HONEST LIMITATION block: a live false-red traded for a latent false-green), which is where an adjudication has to live once the plan file is deleted.

**Self-catch (1) verified and correctly fixed.** The anti-vacuity oracle's first cut did let the mutant's `Skipped` escape as a skip — the landed form asserts the outcome **type** (`isinstance(excinfo.value, pytest.fail.Exception)`), and the comment records the vacuity it had ("measured: the mutant survived that way"). I re-ran both branches. This is the right artifact: a fixed oracle that says what it used to miss.

**Test-diff review (no weakening).** +70 / −11 assertion lines. Every removal accounted for: the two `stderr == stderr` lines were **re-emitted with the measured honest-status comment**, not dropped (G9 handled exactly as prescribed, on both sites); the stub-fixture asserts moved into `_stub_python3`; `-c ""`, the re-init remedy and the literal `|| exit 1` lost their subjects and each gained a stronger replacement (the last one, the semantics-through-`sh` husky class, is the one I asked for). No new skip/xfail/tolerance beyond the intended tripwire.

**Reverse absorption on this round's fixes.** Three classes swept: WARN-to-stderr — **closed** (both emitters in `scripts/` now on stderr) · `[ -f … ] && { … }` as a last command — **closed** (the only remaining occurrence in the repo is the comment warning against it; the wrapper's own `[ -n "$gate_out" ] && printf` is followed by `return 1`, safe) · **exec-bit on a created hook file — OPEN → R5-a.**

**R5-a [the one conditional] — the husky create-if-absent path has no exec-bit clause.** Step 6 says "create it only if it is **absent**" and stops there, while the skill states the requirement at **both** sibling hook-write sites (`skills/init/SKILL.md:571` for `.githooks/pre-commit`, `:680` for the solo `.git/hooks/pre-commit`) and the battery's own comment calls a bitless hook "a hook git silently never runs". Create-if-absent is reachable only via the two `core.hooksPath` detection shapes — and under **husky v8** (`core.hooksPath=.husky`) git executes that file **directly**, so without the bit the block is appended, reported, and never runs: a silent disarm in the one wiring path this slice adds. (Init's "appended, not chained" register keeps the *report* honest — this is an outcome gap, not a lie.) **Fix — one clause in step 6:** *"If you create the file, set its exec bit (`chmod +x .husky/pre-commit`; git tracks it, so a clone inherits it). Under husky v8 (`core.hooksPath=.husky`) git runs this file directly and skips a non-executable hook silently; under v9 `.husky/_` sources it and the bit is irrelevant — set it either way."* **Honest register: I could not reproduce the silent skip on this Windows checkout** (the exec bit is not removable on this filesystem — `chmod -x` was a no-op and the hook ran). The citation is the project's own twice-stated rule, not my measurement.

**R5-b [free, bundle with R5-a] — one string in `test_the_preconditions_are_stated_in_order`.** Measured: substituting the actual instruction (`git check-ignore -v` → `git status --porcelain`) **survives**, because the assertion's substring `"git check-ignore"` is also satisfied by step 4's closing cross-reference to the solo block's guard. Anchor on `git check-ignore -v .githooks/pre-commit` instead. (Deleting the step wholesale *is* caught, which is why this is a tightening and not a blocker.)

**Dispositions for everything else in this round.**

- **fix-in-this-pass:** R5-a · R5-b. Nothing else. Applying these two closes the gate — **no further gate round.**
- **land-commit obligation (orchestrator):** L1 · L2 · L3 · L4 · L5 · L8 · L9 stand unchanged from round 1. **L6 grows by one site — plan `:524`** (H7's propagation wording; the implementer was right not to edit the branch's plan copy, which predates this verdict), so L6 is now `:524` · `:525` · `:526` · `:535`. **L7 is DISCHARGED** — the `tree:79` locator trim landed in this delta (the restated literal probe command is gone); drop it from the land list.
- **ROADMAP:** unchanged — step-8 detection widening (the cheap half of F11 landed in-slice and lands well: the caution now names its own sources and says *"raise it whenever one of those is present, even if step 8 reported nothing"*) · the probe/gate timeout, now discharged as an honest header clause rather than a dependency, exactly as prescribed.
- **explicitly dropped (N2), with the reason:** flipping either tripwire's `strict=True` to `False` survives the suite (measured: 85 passed / 2 xfailed). This is structural to the xfail pattern, not a gap in this slice: catching it needs a meta-test that reads its own source for a decorator argument, which buys a guard against deliberate vandalism rather than against drift. The value of the tripwire is the XPASS at Slice 7, and that is intact and proven.
- **watch-item, unchanged:** G4's Windows-CI `sh` leg on the land PR. If it is red there, fix the workflow's shell/PATH — never loosen the guard to a skip.

**Definition of Done (round 2):** acceptance criteria met as amended (the three round-1 spec amendments stand and are land obligations) · all four in-scope dimensions now met, plus the honesty-scoping correction carried forward to the remaining slices' specs · deterministic gates green · **no new debt** once R5-a/R5-b land.

**Plain English (round 2).** Everything I asked for is in, and three things landed better than I specified — the version floor is now checked against the gate scripts' own recorded requirement instead of trusting a comment, the parity pin got materially harder to fool, and the reasoning behind the one check I told them *not* to add is written into the test file rather than into this plan, which gets deleted. I re-broke twelve things on purpose; ten turned red, including every defect this round existed to fix. Of the two that didn't, one is a substitution nobody would make by accident and costs one string to close, and the other is a known dead-end of the pattern that I am recording rather than pretending to solve. The single real gap left is small and of a piece with the rest of the slice: when init has to create a husky hook file from scratch it never says to make it executable — and a hook file without that bit is one git ignores without a word, which is precisely the silent-disarm failure this whole slice was written to eliminate. The skill already says exactly that in the two other places it writes a hook. Add the same sentence in the third, tighten one test string, and this slice is done.

### Slice 6 — Ship-class + contract sweep (3b)

_Grounded 2026-08-14 by a 7-reader fan-out over every touched surface (wf_f1399ff5); all anchors below verified by quote at `main` f546b6a — line numbers are locators only, the quotes are the anchors._

- **In plain English (shown first at the approval gate):**
  - **What this builds:** the doc-budget gate **ships to adopters**. Today `scripts/check_doc_budgets.py` is stripped from the release under a rationale Slice 4 already falsified ("budgets the harness's OWN ledgers (harness-tuned caps) — irrelevant to adopters"; the caps are per-repo data now). The reclass is one manifest-entry **deletion** (default-include: deletion IS the ship, and every derived set — shipped set, Pass D's loops, the harness-self script list — re-derives with zero gate-code edits), plus the mirror-image **removals delta** in both shipped-set test files (the additions-delta pattern that already exists there, in reverse, restated in both files per its own rule). Every shipped sentence built on the old story is swept **by content anchor** in the same change: doctor's harness-self/N-A applicability sites and its reader-contract — rewritten to state **all three cap forms** (plain int · `{"max": N, "reportOnly": true}` · glob-by-key), with the dead `DOC_BUDGETS` sentence, its false "stripped / fail-louds" rationale, and the "no glob support" line gone — plus WORKFLOW's adopter note / DoD gate 4 / condensation-trigger story / rung-2 parenthetical, the condense skill's dual-trigger framing and cap-bump pointer, and doc-lifecycle's 0038 entry, which gets the dated **superseding line** (the chaining itself stays Slice 7's: future tense, already strict-xfail-tripwired). Doctor also gains the two rows S5 flagged (L9): **interpreter-health** — runs the hook's own probe (EXECUTE `python3` then `python` with the ≥3.7 assertion; never `command -v`, the Windows-Store-stub trap) and reports a dead interpreter as a **flag** in the hook's own remedy register — and **husky-aware hook-wiring** — the chained-`.husky` third wiring becomes a recognized-HEALTHY shape instead of a hooksPath conflict, with sub-flags (marker unreachable · exec bit missing on a created hook · wrapper git-ignored), and the re-wire treat learns to never clobber a healthy chain (the treat count stays exactly four).
  - **What "done" means for you:** the next release carries the gate; until Slice 7 seeds a config and chains the hook, it quietly no-ops in an adopter repo (absent config = exit 0 — the designed not-opted-in posture), so nothing changes behavior anywhere that hasn't opted in. Doctor tells the truth about the gate's ship-class, the full schema, the interpreter, and husky wiring; the decision ledger records the supersession instead of contradicting the ship.
  - **What you're accepting:** the ship lands one slice before seeding/chaining — in that window an adopter has the script and no config, which is exit-0 by design **[SA1, Stage-7 round 1: AMENDED — measured false. The script is in the plugin PAYLOAD, not the adopter's repo; nothing repo-local exists to no-op until S7 DELIVERS it. The shipped copy must be presence-scoped (R1); see the verdict below.]** (the "chains in S7" promise rides future tense + the existing tripwire) · once shipped, the script's basename **leaves the shipped-content caveat scan** (Pass A.b) — after this slice no gate flags stale "harness-self/stripped" copy about it; this sweep is the one shot, future drift is model-upheld · `tests/test_shipped_condensation_trigger.py` is retired/inverted (its premise — "an adopter can never see the WARN" — inverts by design; the condense rewiring would trip its regex) · doctor's interpreter row is a doctor-run-time `[D]` check, not proof of every teammate's commit-time PATH (probed through `sh` where available so its PATH view matches the hook's) · INVARIANTS sits at 82.7% of its own cap — its edits are net-tight by AC, not by hope.
- **Files & changes:**
  - `scripts/build_release.py` — DELETE the `"scripts/check_doc_budgets.py": "self-gate"` manifest entry. Deletion, never a class rename: any class value still strips, and an unrecognized class falls through to the dangling-basename scan, turning every shipped doc that mentions the script into a hard exit-1. The caps-config entry (`init-gen`) and its HONEST-STATUS note are untouched.
  - `scripts/check_doc_budgets.py` — docstring only: the "WHERE THE SCHEMA IS DEFINED" two-homes section (which defers by name to "Slice 6 converges the two statements") becomes the converged statement naming doctor's reader-contract canonical; the line-7 `check_versions_synced.py` mention gains a caveat marker (an uncaveated still-stripped basename in newly-shipped text = a fresh Pass A.b WARN on every scan).
  - `scripts/check_shipped_content.py` — zero code edits (the single-source design: all sets derive from the manifest); docstring/example touch-ups only where the departed script served as the A.b example.
  - `tests/test_build_release.py` — a removals-delta frozenset (mirror of `POST_MIGRATION_ADDITIONS`, 0041-S6 rationale inline; frozen snapshots stay frozen — deltas are the mechanism) wired into the exact-equality, the full-historical-membership subset pin, and the byte-identical reconstruction predicate (`(PRE | ADDITIONS) - REMOVALS`) · the TestClassify strip-list entry removed (count 3→2, comment reworded) · `is_dev_only(...) is True` flips + the script asserted in the ships-test · NEW `recreate_class("scripts/check_doc_budgets.py") is None` pin (the seam is otherwise untested — no existing test asserts this script's class) · class-docstring/comment sweep ("the three .py gates strip" et al.).
  - `tests/test_check_shipped_content.py` — the sibling removals delta (deltas are deliberately restated in BOTH files, never imported) subtracted in the `HARNESS_SELF_SCRIPTS == _OLD_HARNESS_SELF_SCRIPTS` equality; the A.b WARN-only example test re-pointed at a remaining self-gate script (its current example basename leaves the scan set).
  - `tests/test_shipped_condensation_trigger.py` — retired/inverted WITH the condense-skill rewiring, same commit (the reclass alone leaves it green; the rewired copy legitimately pointing adopters at the WARN trips its orphaned-trigger regex).
  - `skills/doctor/SKILL.md` — **(a) applicability:** doc-budgets leaves the harness-self trio at every site — frontmatter description · "The rule" trio · the diagnose bullet (incl. the stale "the INVARIANTS cap already lives in this script" sentence, false since S4) · the gate-table row · the condense-ramp "harness-self" label · "the three harness-self gates" count; the presence rule itself SURVIVES for version-sync + shipped-content. **(b) reader-contract rewritten:** all three cap forms + exact edge semantics (at-cap = WARN, breach = strictly over, band inclusive at 90% · absent-config note vs empty-config "OK:" note are distinct verdicts · `reportOnly` graces ONLY the size verdict — a missing/unreadable budgeted file still exits 1, and nothing mechanical clears a grace · every key IS a path or final-component glob — no reserved keys exist · `**` refused, `*` final-component-only, duplicate keys fatal · WARN→stderr, verdict→stdout) — in substance identical to deterministic-gates' mechanics entry; the dead-`DOC_BUDGETS` sentence, the "no glob support" sentence, and the advisory-exists-because-stripped section framing all replaced. **(c) interpreter-health row** (beside the init post-condition row): `[D]` — replicate the hook's probe exactly (candidate order python3→python, executed with the ≥3.7 assertion, through `sh` where available so the PATH view matches the hook's; "doctor runs Python therefore the hook has Python" is a false inference); SKIPPED = **flag**, remedy in the hook's own register ("install Python 3 — the gate resumes on the next commit; no re-init needed" — never the slash command); remedy is not a treat (the stamped-fence row is the precedent). **(d) husky-aware wiring row:** third HEALTHY shape = husky detected (init's detection rule, incl. a bare `.husky/` containing a `pre-commit` with hooksPath unset) + the managed marker in `.husky/pre-commit` + the wrapper on disk; **sub-flags, not conflicts:** marker unreachable (unconditional `exit` above it — report "appended", never "chained") · missing exec bit (git index mode `100755`, never fs stat — Windows lies) · wrapper git-ignored; a recorded `- Husky chain: declined` = known-inactive (informational), and an ABSENT record proves nothing (the refused-because-ignored case records nothing by design, and solo-mode records live in CLAUDE.local.md); the re-wire treat becomes chain-aware (refuses to re-point `core.hooksPath` away from a recognized-healthy chain) — **modified, never a fifth treat**.
  - `skills/condense/SKILL.md` — trigger framing (frontmatter description + both body mentions): the WARN is adopter-reachable wherever a config opts in (the WARN/advisory dual-trigger framing encoded the stripped-script world); the cap-bump pointer becomes cap-form-aware (int: replace the number · object: edit `"max"` · glob: the bump raises EVERY matched file — the glob stays the sole cap for its matches; a per-file exception beside a glob would be a second cap source and is not offered).
  - `docs/claugentic-WORKFLOW.md` (content anchors, never line numbers) — adopter note: the "both are **harness-self and irrelevant to an adopter**" coupling splits (version-sync keeps its caveat; doc-budgets = shipped, no-ops without a config; "the script isn't even present in your repo" deleted) · DoD gate 4: "*harness-self only*", "stripped from the release", "Not applicable to adopter repos — skip it" go; bytes-not-chars, run-gate status (not hook-wired until S7 — future tense, tripwired), WARN semantics stay · the condensation-pass trigger story rewritten (the WARN CAN fire in an adopter) · rung-2 parenthetical: "an adopter has no such script" deleted; the "until the two statements converge" schema IOU discharged into a statement of convergence pointing at doctor · the "ships no live copy" sentence **SURVIVES** (it is about the caps CONFIG, which stays dev-only) with only its "harness-self gate" labeling fixed.
  - `docs/claugentic-INVARIANTS.md` — the EXISTING "exactly ONE source per repo" entry is **extended in place** (S5 already landed it — never a duplicate entry); its "Slice 6 extends the one-cap-source contract to adopters" IOU discharged; doc-budgets leaves the Class-A list in the release/init-contract entry; the `self-gate` taxonomy narrative reconciled with the departed member. **Net-tight, with the measured baseline: the S5 harvest entry (floor↔syntax coupling, on `harvest/0041-s5`) puts the file at 17,995 B — 5 B under the 18,000-B WARN line — so this slice's INVARIANTS edits must be net-negative-or-zero after the harvest merge** (the discharged IOU sentence and the Class-A trim are the offsets); if the combined land result still trips the WARN, stop and surface it — the honest fallback is the pre-registered INVARIANTS condense/shard ROADMAP trigger, a user call, never a silent WARN.
  - `docs/claugentic-decisions/doc-lifecycle.md` — the 0038 entry **rewritten in place to newest truth** (shards are not append-only) with the dated superseding line: **SUPERSEDED by 0041 S6** (2026-08-14) — the gate SHIPS, killing the "replacing the stripped `check_doc_budgets.py` WARN an adopter's repo can't produce" premise; the "NO doc-budget hook" don't-re-break superseded per the user's 2026-08-12 commit-time decision, with the chaining named as **S7, future tense, tripwired** · the DRY-one-cap-source sentence rewritten: the interim "SUPERSEDED for the harness itself by 0041 S4" marker, the false "ships NO live copy"/"`DOC_BUDGETS` in `check_doc_budgets.py`" claims, and the discharged "Full four-site sweep: 0041 S6" pointer all go; the **"Do not delete that file" fail-open warning STAYS** (a live constraint, not part of the interim patch).
  - `docs/claugentic-decisions/deterministic-gates.md` — the "doctor's interpreter-health row = Slice 6" forward pointer discharged (the row lands here; 3,132 B of WARN headroom — net-lean edit).
  - `docs/claugentic-ARCHITECTURE_TREE.md` + `CHANGELOG.md` — refreshed entries; the CHANGELOG records the ship-class change + the two doctor rows.
  - **Negative space (load-bearing, in-spec):** `.githooks/pre-commit` untouched — the chain tripwire matches the raw substring `check_doc_budgets` ANYWHERE in the hook text; even a comment flips it · `skills/init/SKILL.md` untouched — writing the literal config path there XPASSes the Slice-7 tripwire and the suite goes red · `.claude/claugentic-doc-budgets.json` untouched (byte-exact pin; never a `reportOnly` on the harness's own ledgers — the object form is exercised by synthetic fixtures only) · the DECISIONS index untouched (never-append rule; 520 B of headroom) · `docs/claugentic-decisions/plugin-distribution.md` untouched (289 B from its WARN band) · named sweep false-positives that must SURVIVE: the byte-exact-pin "harness-self extra" clauses (WORKFLOW rung 2 + INVARIANTS) · gate-3 (version-sync) and gate-5 (shipped-content) harness-self labels · deterministic-gates' tree-gate-altitude entry (its "commit-time"/"PostToolUse" text is 0024 S4a, not the 0038 line) · workflow-process's charter "(0038)" · plugin-distribution's "adopter-owned" INCLUDE_GLOBS line · the DoD dual numbering (Slice 12's, reviewer-visible — do not "fix" it).
- **In-scope standards dimensions:** `docs-traceability` (the content-anchored sweep; forward promises future-tense + tripwired) · `maintainability-structure` (deletion-derives-everything single-source discipline; one cap source per repo) · `reliability-resilience` (probe semantics; wiring-shape totality incl. the sub-flag states) · `honesty` (`[D]`/`[J]` integrity on the new rows; claims match wiring — the slice's whole point) · `testing` (the delta-set pattern in both files; no assertion weakening; the new seam pin).
- **Tests to add:** the removals delta in BOTH test files (mutation-shaped: the manifest deletion without either delta = red, either delta without the deletion = red) · `recreate_class("scripts/check_doc_budgets.py") is None` · the script asserted present in the ships-test · the retired/inverted condensation-trigger replacement · the suite run itself asserts both strict-xfail tripwires are still xfail (they must NOT flip in this slice).
- **Acceptance criteria:** full battery green from the repo root AND from `tests/` · all four gate scripts green with ZERO new shipped-content WARNs (the line-7 caveat lands with the reclass) · `claude plugin validate --strict` green (doctor + condense frontmatter edited — the v0.5.1 lesson) · the release-built tree CONTAINS `scripts/check_doc_budgets.py` (asserted via classify/ships-test) · both strict-xfail tripwires still xfail · doc budgets green with INVARIANTS under its 18,000-B WARN line · doctor's treat count stays exactly four and every new row carries an honest `[D]`/`[J]` tag · the reader-contract agrees in substance with deterministic-gates' mechanics entry (verify-panel checks it) · grep over shipped docs finds zero live "harness-self / stripped from the release / N-A in an adopter / no glob support / `DOC_BUDGETS`" claims about this gate.
- **Sizing:** 13 surfaces — inside the ~15-file split line, so the doctor/condense rewiring stays in-slice (the split guidance stands if the build proves otherwise; the copy sweep never splits from the mechanics).

### Slice 6 — Verify verdict (Stage 7)

RUNNING AS: **Opus 4.x** (claude-opus-5[1m] — the "Opus 4.x-generation" family; **same model family as the builder and the panel**). Honest independence register: this is a **separate clean-context pass on the most capable available model** — role + context independence only, **not** model-family independence, so model blind spots are *not* de-correlated. A reduction of rubber-stamping risk, never a guarantee.

---

# Stage-7 verdict — plan 0041 Slice 6 (ship-class + contract sweep), round 1

## VERDICT: **CHANGES REQUIRED**

The mechanical core is the best-shaped work in this plan so far: one manifest line deleted, every derived set re-deriving with zero gate-code edits, and I broke it on purpose to confirm. What fails the gate is the slice's *own thesis* — claims match wiring. **Shipping the script into the release payload does not put it in an adopter's repo, and eight shipped sentences say it does.** I proved that by execution, not by reading. Two further stale ship-class claims survived the one-shot sweep in shipped files, and the new scan that is now the *sole* mechanical guard on this copy has two measured false-positive paths and a scope note that understates its residual.

*Test-first audit: **N/A** — this slice did not choose test-first (no independent test-author spawn claimed, none required). Audited normally. Not the closing slice, so no whole-feature pass.*

---

## What I re-executed (scratch worktrees at `40e1540` and `6b0af70`, both removed; the implementer's worktree and `main` untouched)

**Confirmed as reported.** Battery **589 passed / 2 xfailed** from the repo root *and* from `tests/` · `node --test tests/workflows/*.test.mjs` → **372 pass / 0 fail** · all four gates **exit 0, zero `WARN:` on either stream** (I read exit codes without a pipe) · `ruff` clean · dry-run **53 ship / 64 strip**, gate in SHIP · both tripwires **xfail by name**, both `strict=True` (`tests/test_check_shipped_content.py:349`, `tests/test_precommit_wrapper.py:500`) · **negative space 5/5 byte-identical by blob hash** (`.githooks/pre-commit`, `skills/init/SKILL.md`, `.claude/claugentic-doc-budgets.json`, `docs/claugentic-decisions/plugin-distribution.md`, `docs/claugentic-DECISIONS.md`).

**INVARIANTS byte math, re-derived independently.** `main` 16,542 → branch **16,454** (−88 B); `harvest/0041-s5` 17,995 B; `git merge-tree --write-tree` clean; **merged blob = 17,907 B**, cap 20,000 × 0.9 = **18,000 WARN → 93 B headroom**. The implementer's arithmetic is exact.

**Stronger than reported.** `claude plugin validate --strict .` green; the `.claude-plugin/plugin.json` form fails on exactly one pre-existing warning (*"CLAUDE.md at the plugin root is not loaded as project context"*), which `release.yml:193` sidesteps by validating the **built** tree — so I materialized the 53-file ship set and ran the CI-equivalent form: **`✔ Validation passed`**. That is the form the v0.5.1 lesson actually demands, and it is green.

**Mutants I ran myself.** Class-rename instead of deletion (`"scripts/check_doc_budgets.py": "ships"`) → **9 test failures** *and* `check_shipped_content.py` **exit 1** naming three shipped files as dangling paths. J1's "deletion, never rename" rule is enforced twice over, independently.

**Reader-contract spot-checks by execution** (not inspection): at-cap = `WARN:` on **stderr** + verdict on stdout + exit 0 · `reportOnly` on a **missing** budgeted file → **exit 1** · a `**` key → **exit 1** with the flat-directory explanation. Doctor's rewritten contract matches the code on all three.

**The decisive probe (nobody ran this one).** In a scratch adopter repo *that has written a caps config*:
- `python scripts/check_doc_budgets.py` — the literal command shipped `WORKFLOW.md:154` tells an adopter to run — → **exit 2, `can't open file`**.
- the plugin's own copy invoked from that repo's root → **exit 0, `OK: all managed ledgers within budget - CLAUDE.md <= 6000 bytes, docs/claugentic-DECISIONS.md <= 3500 …`** — a **green verdict about the harness's ledgers**, printed inside someone else's project. `_repo_root()` anchors on `__file__` (`scripts/check_doc_budgets.py:551-572`), then `main()` `os.chdir`s to it (`:592`).
- `skills/init/SKILL.md` contains **0** occurrences of `check_doc_budgets`; its managed set copies exactly one script (`:145`).

---

## (1) Required changes — one fix pass, priority order

### R1 — **BLOCKING.** Presence-scope every shipped "it ships ⇒ you can run it" claim
`skills/doctor/SKILL.md:46-48`, `:72-73`, `:280` · `docs/claugentic-WORKFLOW.md:154`, `:171` · `skills/condense/SKILL.md:3`, `:16-19` · `CHANGELOG.md:16-18`

Shipping ≠ repo-resident. The tree gate runs in any repo **because `init` copies it**; this one has no delivery path, and none is planned in S7's decomposition line. Three specific harms, all measured above: shipped copy prints a command that does not exist; `doctor:73` / `:280` attribute the absence to *"an install predating this gate's ship"* — false for **every** adopter, and it points them at a plugin update that changes nothing; and `condense:3`/`:16-19` tells them a WARN trigger is reachable when only `/doctor`'s advisory is.

Sharpen the framing, because it is the crux: **this slice retires the 0038 orphaned-trigger guard on the premise that the trigger is now reachable, then writes shipped copy pointing adopters at that still-orphaned trigger.** The inversion is one slice early. The fix is not to restore the old guard — it is to make the copy presence-scoped, at which point the new scan's premise is honest and the orphaned-trigger risk is worded away.

**Exact shape:** adopt the register the diff already got right at `skills/doctor/SKILL.md:2` — *"architecture-tree and doc-budgets both ship and **run wherever present**"*. Everywhere: **(a)** payload membership and repo-local presence are stated as two different facts; **(b)** the repo-local copy "arrives with 0041 Slice 7's `init` step" (future tense, per the S5 L8 standing rule); **(c)** doctor's N-A cause becomes *"absence is the norm outside this repo today"*, with the pre-ship install as the secondary case; **(d)** one new sentence in doctor: **never substitute the plugin's own copy — it anchors to its own checkout and reports a green about the wrong repo** (measured verbatim above).

**PIN:** a new `strict=True` xfail beside `tests/test_check_shipped_content.py:348`, asserting `"check_doc_budgets" in skills/init/SKILL.md`. Measured: **0 occurrences today → xfails**; it XPASSes the instant S7 delivers, turning the suite red and forcing this copy out of the future tense. The plan's own standing rule (`:90`) *requires* a falsifier for this promise and there is none today — the existing `test_init_documents_the_caps_config` pins *config seeding*, a different capability.

### R2 — the same claim in the dev-only ledgers
`docs/claugentic-decisions/doc-lifecycle.md:5` — *"the **gate** emits the ≥90% WARN in any repo whose config opts in"* → *"wherever it is **present** and a config opts in; adopter presence lands in 0041 S7."* This is the superseding record; leaving it hardens the false premise into the ledger. Shard has room (5,495 / 14,000 B). `docs/claugentic-INVARIANTS.md:183` — same correction, **under R5's byte ceiling**. **PIN:** R1's tripwire.

### R3 — the two stale ship-class claims that survived the one-shot sweep (AC violation)
Both in **shipped** files; both invisible to **every** gate — I ran the branch's own scan cores over the real shipped corpus: `find_unreachability_claims` → `[]`, `find_ship_class_denials` → `[]`, four gates exit 0 with zero WARNs.

- **(a) `skills/build/SKILL.md:401-404`** — *"plus, in the harness's own repo only, the harness-self gates version-sync + doc-budgets; an adopter close-out names neither"*. Two clauses now false, in a file that ships. **Shape: delete the restatement, don't patch it** — point at the WORKFLOW DoD, which the same sentence already calls canonical. (Patching it also re-adds a roster that omits `shipped-content`, a second pre-existing inaccuracy.)
  **PIN — structural, and I measured why nothing else works.** The restated roster is deleted, so there is nothing left to drift. A mechanical pin is **refuted by measurement**: the offending phrase wraps across `:402`/`:403`, so *no* line-scoped scan sees it (including T1's clause-scoped prototype — 0 hits on the post-state); the paragraph-scoped alternatives that *do* catch it produce **3 false positives at window 0 and 11 at ±1** on honest post-state copy. After this slice, this class of copy is model-upheld. Say so; do not invent a scan.
- **(b) `CHANGELOG.md:189-191`** (the `0.4.0` section) — *"the harness's internal byte-budget gate, **which is stripped from the release and never runs in an adopter repo**"*, present tense, in a shipped file. Neither scan matches it (`byte-budget` ≠ `doc[- ]budget`; *"which is stripped"* ≠ the `(that|the|this) gate is stripped` construction). **Shape: the house convention two paragraphs down** (`CHANGELOG.md:203-208`) — keep the history, append a bracketed dated correction. Do not rewrite the release section.

### R4 — the new trigger test's two false-positive paths and its scope note
`tests/test_shipped_condensation_trigger.py`

- **(a) The past-tense guard — measured both ways.** Reflowing `CHANGELOG.md:16-17` into one line (a **pure re-wrap, zero wording change**) turns `test_no_shipped_file_caveats_this_gate_away_from_adopters` **RED**, accusing honest release history of denying the ship. I implemented the minimal guard (a `HISTORICAL_RE` skip — `used to be|no longer|formerly|stopped being|was stripped|until 0041` — applied to both scans, 5 lines) and measured: **post-state stays 0/0 · the reflow false positive is closed · 12/12 tests still pass**. Adopt that shape.
  **PIN:** a unit test carrying the reflowed one-line historical sentence, asserting `find_ship_class_denials(...) == []`. Measured red without the guard.
- **(b) The honest-scope note (`:17-21`, `:66-72`) understates the residual and must be corrected.** I ran the landed scans over the **base** shipped corpus — the exact copy this slice deleted: **10 candidate lines, 4 caught.** Every miss is the dominant `harness-self` phrasing (`doctor:2`, `:79`, `:116`, `:189`), which is invisible because `harness-self` is the one excluded marker. The docstring characterizes the residual as **one** shared-line case; the true residual is *any claim that does not name the basename, plus every `harness-self` phrasing, plus anything wrapped across lines*. Also record the second, different reason the exclusion is needed (`WORKFLOW:180`'s byte-exact-pin clause) and the fact that the shared-line shape is a **false-positive** path in the sibling scan (measured: `find_unreachability_claims` returns a hit on it). This matters because Pass A.b stopped scanning this basename **in the same commit** — this file is now sole custody.
  **PIN:** none needed — it is a scope statement; its falsifier is R4(a)'s test plus the ROADMAP rewrite.

### R5 — INVARIANTS edits are **byte-capped**; two lens prescriptions breach the WARN as written
**Measured, and this is the finding no single lens could see.** Post-merge headroom is **93 B**. Applied verbatim: the honesty lens's `:183` reword is **+106 B → 18,013 B (breach)**; the maintainability lens's `:100` "derive the roster" reword is **+98 B → 18,005 B (breach)**; **both → 18,111 B**. Either would trip the WARN the slice's own AC forbids and fire the plan's pre-registered stop-and-surface (a user call).

**Shape:** reword `:183` in a form measured at **≤ +33 B** (a tightened version lands at 17,940 B). Complete the `:100` roster (it omits `shipped-content`, a genuine harness-self gate a shipped file runs — self-gate class is `{check_versions_synced, check_shipped_content, build_release}`) **only if the merged file still lands ≤ 17,995 B**, the S5 harvest baseline; otherwise route the roster restructure to ROADMAP and leave `:100` alone. Re-run `git merge-tree` and quote the number.
**PIN:** mechanical — `python scripts/check_doc_budgets.py` on the merged tree.

### R6 — four corrections to doctor's new rows (all prose, all in copy this slice authored)
- **(a) `:191-195`, table `:286` — a husky chain is called HEALTHY with `core.hooksPath` unset.** Init's *offer-time* detection predicate ("a bare `.husky/` counts, even with hooksPath unset") is inherited as a *health verdict*. With hooksPath unset git runs `.git/hooks/`, so the marker block never runs — the common pre-`npm install` state. Require, for GREEN, that hooksPath resolves to husky (`.husky` or `.husky/_`); add a **fourth sub-flag**: *husky present but not installed in this clone*. (Sub-flags are not treats — the "exactly four" AC is untouched.)
- **(b) `:202-207` and the treat bullet `:318` — drop the "that `init` CREATED" provenance clause.** Measured: `init` records only `- Husky chain: <appended | declined (tree gate written but inactive)>` (`skills/init/SKILL.md:665`, `:957`) — never "created". Doctor cannot evaluate the predicate, so the check is skipped on every *appended* chain, where a `100644` hook is silently ignored by git under husky v8. Read `git ls-files -s .husky/pre-commit` unconditionally.
- **(c) `:316-322` — widen the re-wire treat's refusal.** It refuses only for a *recognized-healthy* chain. A `Husky chain: declined` repo has `core.hooksPath=.husky` and no marker → not recognized-healthy → the treat's stated action re-points hooksPath and **silently switches off every hook that repo owns**. One clause: *refuses to re-point a non-default `core.hooksPath` it did not itself establish*; on an unmarked husky repo the offer is to **chain**, never to re-point. Treat count stays four.
- **(d) `:225-230` — one clause on the no-`sh` fallback. Measured on this machine:** in PowerShell, after a `CommandNotFoundException`, `$LASTEXITCODE` **retained its previous value (0)**. An exit-code read therefore reports a missing `python3` as working — a false green on the one row whose purpose is not lying about the interpreter. Add: *treat "command not found" as a FAILED candidate; never read a stale exit code.*

**PIN for R6:** none mechanical, honestly — these are skill-doc rows; the interpreter *remedy wording* is test-pinned (`tests/test_precommit_wrapper.py:332-347`) but the new sub-flags are not. Model-upheld; do not claim otherwise.

### R7 — honesty tags and closure (in-scope AC: *every new row carries an honest `[D]`/`[J]` tag*)
- `skills/doctor/SKILL.md:127-130` — *"the gate exits 1 on **exactly these**"* claims a closure the enumeration lacks (`RecursionError`, non-boolean `reportOnly`, empty key also exit 1). Add *"and on any other structural defect"*; the bullet itself forbids reading softer than the gate.
- `:286` — append `[J]` for the marker-**reachability** read (whether an early `exit` is unconditional is a control-flow judgment, not a measurement); the other three sub-checks are genuinely `[D]`.
- `CHANGELOG.md:28` / `skills/doctor/SKILL.md:237` — the treat **count** is four (verified), but the re-wire treat gained an *un-ignore the wrapper* action `init` itself refuses to take. One clause, so "still exactly four" doesn't read as "the boundary is unchanged".
- `tests/test_shipped_condensation_trigger.py:131` — rename to `test_the_marker_vocabulary_stays_in_step_with_the_scanner`. Measured: a hand-typed literal tuple keeps the file 12/12 green, so the name over-claims; the property it *does* pin (staying in step) is real and valuable.

---

## Spec amendments (WORKFLOW → Stage 7 procedure; point, don't restate)

- **SA1 — the accepted-cost bullet is factually wrong.** The spec says *"until Slice 7 seeds a config and chains the hook, it quietly no-ops in an adopter repo (absent config = exit 0 — the designed not-opted-in posture)"*. **Measured false:** there is no repo-local script to no-op; the adopter gets `exit 2, can't open file`, and the plugin's copy reports green about the harness's repo. The implementer faithfully implemented the spec's premise — **this refutes the SPEC, not the diff**, and R1 is the correction of both.
- **SA2 — Slice 7's decomposition line is incomplete.** It names *seed caps* + *chain into pre-commit*, not **delivery**. Two consequences it must absorb, both measured: (i) chaining without delivery gives `claugentic gate SKIPPED: <gate> is not in this checkout` on every commit, forever (`.githooks/pre-commit` run_gate); (ii) delivery collides with `docs/claugentic-decisions/plugin-distribution.md:7` — *"any new managed file must be born prefixed"* — so the adopter path is `scripts/claugentic-check_doc_budgets.py`, which makes today's shipped `python scripts/check_doc_budgets.py` instruction wrong even *after* S7. Plugin-root invocation is refuted by construction (`_repo_root()` anchors on `__file__`), so the choice is copy-with-rename or a `--root` override — an S7 design call for the user, not this slice.

---

## (2) Land-commit obligations (orchestrator, not the fix pass)

- **L1** — record SA1 in the plan's S6 block with the measured evidence.
- **L2** — amend Slice 7's decomposition bullet per SA2 (delivery + prefix consequence + the invocation path the shipped copy will then name), and add the reliability lens's S7 AC: **the seeder caps only files it creates in the same run** — measured, a budgeted file that does not exist exits 1 *even under `reportOnly`*, and `INVARIANTS.md` is `recreate-on-demand`.
- **L3** — `docs/claugentic-ARCHITECTURE_TREE.md` + `CHANGELOG.md` entries for whatever the fix pass touches.
- **L4** — re-run `git merge-tree` after the harvest merge and quote the measured merged INVARIANTS size in the land commit (R5's ceiling is only real if it is re-measured at land).
- **L5** — **harness impact / harvest candidate (this round's class):** *a prescribed doc edit against a budgeted ledger is byte-measured before it enters a verdict.* Two independent lenses prescribed INVARIANTS rewrites that each breach the WARN; neither could see it, and only synthesis caught it. This belongs in the `synthesizer-gate` posture beside the existing "a lens's prescribed FIX is a hypothesis" rule.
- **L6** — DECISIONS line (route via the index; `plugin-distribution` is the topical shard): **ship-class ≠ delivery.** A script in the release payload is not in an adopter's repo; adopter-facing invocation requires an `init` delivery step **and** the `claugentic-` prefix, and a plugin-root copy is refuted by `_repo_root()`'s `__file__` anchoring. This is the durable rule the whole round turns on.

---

## (3) ROADMAP routings

- **The rewording-surviving stale-claim scan** (the implementer's own candidate, now measured). Clause-scoping both scans — strip emphasis, split on `[.;:·()]`/spaced em-dashes, require context + marker in the *same clause*, skip negation/past-tense clauses, drop the `harness-self` exclusion, replace the basename gate with `DOC_BUDGET_CONTEXT_RE`. **I prototyped and measured it: 0 hits on the post-state, +3 true catches on the base corpus** (the lens measured +4 with a different splitter — same direction). It ships with T2's fixture re-point, which **cannot land without it** (measured: the shared-line shape hits today). Routed rather than required because it does **not** close the residual that actually bit this slice — `build/SKILL.md:403` is cross-line — and a ±3 window that *would* catch it produces 11 hits on honest copy.
- Doctor's `### 2. Adopter doc-budget advisory` heading now names a section that binds **both** readers; four call sites point at it by name. Rename + 4 pointer updates, or a one-line scope sentence under the heading.
- The `INVARIANTS:100` Class-A roster restructure (derive, don't restate) **if** R5's byte ceiling forbids it in-slice.
- `docs/claugentic-decisions/deterministic-gates.md:11` restates the five cap numbers the config owns — pre-existing, out of diff; flag for the next doc-lifecycle condensation.
- `tests/test_check_shipped_content.py:416`, `:427` — `scan_gate_caveats(...) == []` goes vacuous if `check_versions_synced.py` ever ships; one `!= []` each, whenever that day comes.
- Doctor's stream-capture rule is prose-only (the implementer's candidate).

---

## (4) Explicitly dropped, with reasons

- **A mechanical guard for the `build/SKILL.md` class of stale copy — dropped, refuted by measurement.** Every candidate predicate either misses it (line/clause-scoped: 0 hits) or fires on honest copy (window 0: 3 FPs; ±1: 11 FPs). The honest answer is R3(a)'s structural fix plus writing the residual down; manufacturing a noisy scan would be worse than the gap.
- **Delivering the script in this slice — dropped.** It would edit `skills/init/SKILL.md`, XPASS the S7 tripwire, and take the suite red; it also needs the prefix decision (SA2). Copy correction is the right move now, exactly as the honesty lens scoped it.
- **The lens-verbatim INVARIANTS rewordings — dropped in their prescribed form** (measured: each breaches the WARN). Adopted only in byte-bounded form under R5.
- **Reversing J1, J2 or J3 — dropped; all three endorsed, and two are better than the spec's letter.** J1: a class *definition* does not go stale when a member leaves; only a *roster* does, and the roster is what got edited — plus `:107` was reading as an argument against this very slice. J2: the index routes ship/strip to `release-contract`; filing elsewhere would hide it from the `rg` a future agent runs. J3: verified across the whole gate set — both WARN-emitting gates print to stderr, the other two emit no `WARN:` at all — and it is load-bearing *because of this slice*, since a stdout-only capture would newly read a real WARN as silent green.
- **`docs/claugentic-WORKFLOW.md:182`'s int-only "the schema" example — not raised.** It defers explicitly and rung 2 states all three forms two lines above.
- **The delta-set "bogus extra entry" symmetry assert — dropped (YAGNI).** The additions set is self-validating; a typo'd *removal* is a silent no-op on both sides. Low value against a `git diff` that shows the line.

---

## Definition of Done

| Dimension / criterion | Status | Evidence |
|---|---|---|
| **docs-traceability** | **GAP** | Tree gate green, 4 refreshed entries under 450 chars; decisions dated in the right shard with the accepted cost stated; supersession format mirrors the shard's own. **Fails on:** two stale shipped claims (R3) and a scope note that understates its own residual (R4b); the new S7 *delivery* promise has no falsifier (R1's PIN). |
| **maintainability-structure** | **MET** | Manifest deletion + zero derived-set hand-edits, verified: `check_shipped_content.py` absent from the diff; rename mutant → 9 failures + gate exit 1. One canonical schema home, three deferring pointers. J1/J2 endorsed. One shape residual → ROADMAP. |
| **reliability-resilience** | **GAP** | Interpreter probe replicates the hook exactly; absent/empty/broken config contract verified by execution. **Fails on:** the reachability disarm (R1, measured), husky-green without hooksPath (R6a), the unevaluable exec-bit predicate (R6b), the treat's narrow refusal (R6c), the PowerShell stale-exit-code false green (R6d, measured). |
| **honesty** | **GAP** | Interpreter `[D]` scoping, husky record asymmetry, temporal S7 hedging and the treat count are all honest and unusually careful. **Fails on:** the "ships ⇒ runnable" family (R1/R2) and three tag/closure nits (R7). |
| **testing** | **MET** *(with fixes)* | Delta-set wiring mutation-proof in every direction I tested; two new seam pins die under both mutants; both strict tripwires intact; zero assertion weakening. **Fixes required, not a re-do:** two measured false-positive paths in the new scan (R4a) and one over-claiming test name (R7d). |
| **Acceptance criteria** | **Mostly met** | Battery both cwds ✔ · four gates zero-WARN ✔ · `validate --strict` green, incl. the built-tree form ✔ · gate in SHIP (53/64) ✔ · both tripwires xfail ✔ · INVARIANTS post-merge 17,907 < 18,000 ✔ · treat count four ✔ · reader-contract agrees, spot-verified by execution ✔. **NOT met:** the "grep finds zero live harness-self / stripped claims" AC (two residuals, R3) — and the AC's *spirit*, claims-match-wiring (R1). |
| **No new debt** | **NOT YET** | R1–R4 land as debt if shipped as-is; R1 ships a false instruction to every adopter, and R3 is unguarded by construction after this slice. |

---

## Plain English

**What's wrong.** The engineering half of this slice is excellent — one line removed from a list, and everything downstream figured itself out with no hand-editing, which is the design working exactly as designed. I deliberately broke it four different ways and it went red every time, loudly and with useful messages.

The problem is the *story*. "The gate now ships" turns out to mean it is inside the plugin you install — **not** inside your project. Nothing copies it there yet; that is next slice's job. But eight sentences in the files adopters actually read now say, in effect, "you've got it, go run it." I tried it in a throwaway project: the command they're told to run doesn't exist (a plain error, harmless but wrong), and if anyone reaches for the plugin's own copy instead, it prints a cheerful "all budgets fine" — **about the harness's files, not theirs**. A green about the wrong project is the worst kind of green. There's a sharper version of the same point: this slice retired an old safety net whose job was to stop the docs promising a signal adopters can't get — and then wrote copy promising exactly that. One slice early. The fix is wording, not engineering: say "it's in the plugin; a copy reaches your project next slice; run it wherever the script is present." The skill's own front-matter already words it correctly — copy that.

Two pages also slipped through the sweep and still call the gate "harness-only": the `/build` skill's close-out and an old changelog entry. Both ship. And this is the slice that deliberately spent the last automatic check on this gate, so nothing will ever flag them — I confirmed that by running the checks myself. One sentence each.

**How bad, and what could break.** Nobody's build breaks and no data is at risk. The realistic damage is an adopter following an instruction that fails, or quietly waiting for a warning that can't arrive. The one thing that could actually mislead is that false green from the plugin's copy, which is why doctor should say plainly: never substitute it.

Three smaller things worth doing while the file is open: `/doctor` will currently call a husky setup "healthy" when husky isn't switched on in that clone (green, checking nothing); its "hook isn't executable" check is written to apply only to files it can't tell it created, so the common case goes unchecked; and on a Windows machine without a Unix shell, the interpreter probe can report "Python is fine" when it isn't — I reproduced that one on this machine.

**And one thing only the synthesis could catch.** Two reviewers independently prescribed edits to the invariants file. Each is right on the merits — and **either one, written as prescribed, pushes that file past its size warning** once it merges with the pending branch (93 bytes of room; the two fixes want 204). So those two corrections have to be written tight, or one of them waits. That is now a standing rule for this role.

---

## What the next round needs — bounded

**Targeted delta re-verify, solo. No re-panel.** Every finding above is either measured or a one-clause wording change; a second five-lens fan-out would re-derive facts already executed. The next pass checks exactly six things: (1) the eight R1 sites read presence-scoped and the new S7 delivery tripwire **xfails by name**; (2) `skills/build/SKILL.md` no longer restates a gate roster and `CHANGELOG.md:189` carries the bracketed correction; (3) the past-tense guard's new unit test is red without the guard (re-run the reflow mutant); (4) `git merge-tree` INVARIANTS **≤ 17,995 B**, re-measured; (5) the four doctor rows + four honesty nits landed; (6) the full battery from both cwds, four gates zero-WARN, `validate --strict` on the built tree, both original tripwires still xfail. **Applying R1–R7 closes this gate — no third round.**

#### Round 2 (delta) — `40e1540..2c4e825`

RUNNING AS: **Opus 4.x** (claude-opus-5[1m]) — same model family as the builder; role + clean-context independence only, **not** model-family independence. Round-2 pass is a **bounded targeted delta re-verify, solo** (no re-panel), exactly as scoped at the end of round 1.

**VERDICT: PASS.** All seven required changes landed, three of them in a stronger form than prescribed. The six checks named at round 1 are green **by my own execution**, not by report. Nothing outstanding blocks the land.

**What I re-executed (scratch worktree at `2c4e825`, removed).**

| Check | Result |
|---|---|
| **1. R1 sites + new tripwire** | **PASS.** `test_init_delivers_the_budget_gate_script` **xfails by name**, `strict=True`, reads-never-edits `skills/init/SKILL.md`, and its reason names *both* the S7 trigger and the abandonment branch. All nine copy sites read presence-scoped. |
| **2. build/SKILL + CHANGELOG** | **PASS.** The gate roster is **deleted**, not patched — replaced by a pointer plus the reason. The 0.4.0 correction matches the house form: dated, bracketed, history preserved, and it names the still-live half (delivery gap). |
| **3. `HISTORICAL_RE` PIN** | **PASS, measured.** Guard stripped from both scans: the reflow test and the superseding-ledger sibling both go **RED**. Non-vacuous in both directions — the `…does_not_excuse_a_live_claim` test correctly stays green under that mutant. |
| **4. INVARIANTS bytes** | **PASS, re-derived.** Branch 16,487 B; `git merge-tree` with `harvest/0041-s5` clean; **merged = 17,940 B** — inside the ≤ 17,995 allowance, 60 B under the WARN. Both edits fit (`:183` reword **and** the `:100` Class-A completion), more than R5 required. |
| **5. R6 ×4 + R7 ×4** | **PASS.** Husky-green requires hooksPath resolving to husky, 4th sub-flag added with the "don't inherit an offer-time predicate as a health verdict" reason · exec-bit provenance clause gone at **both** sites with the `appended \| declined` evidence inline · the treat refusal is a proper superset ("ANY non-default `core.hooksPath` it did not itself establish") with the declined-chain hole named · the PowerShell clause carries its measurement · `[J]` on marker-reachability (body **and** table row) · "exactly these" → illustrative-not-closed · treat **count vs boundary** split at both sites · test renamed with the reason. |
| **6. Full battery** | **PASS.** **592 passed / 3 xfailed** from the repo root **and** from `tests/` · four gates **exit 0 with 0 bytes on stderr** · node **372/372** · ruff clean · `claude plugin validate --strict .` green **and** the CI-equivalent built-tree form green (53 ship / 64 strip; gate shipped) · negative space still **5/5 byte-identical** to `6b0af70`, including `skills/init/SKILL.md`, which all three tripwires are armed on. |

**Adjudication of the three flagged deviations.** **D1 (the ninth site, doctor §2 intro): ACCEPT** — R1 named a *class*, not a quota of eight; correcting it is the finding. **D2 (WORKFLOW DoD gate 4 left untouched): ACCEPT, with one land obligation** — gate 4's requirement genuinely binds (so gate 3's "skip it" register would be wrong), but gates 3 and 5 carry inline scoping and gate 4 does not, and a DoD is read by jumping to it → **L7**, one clause, **written in the presence register, never the caveat register** (measured: presence wording is scan-clean; an "N-A in an adopter" wording on that line trips the denial scan, correctly). **D3 (`DOC_BUDGET_CONTEXT_RE` not widened): ACCEPT — the measurement sharpens the reason.** Ran it both ways: widening alone is a **no-op (0 hits** — "which is stripped" never matches `UNREACHABILITY_RE`); the version that would catch it turns **two honest lines red**, including the correction this very fix pass wrote and `doctor:2`'s accurate version-sync/shipped-content clause. The ROADMAP rewrite inherits this: it must clear *both* honest shapes.

**One residual found while probing D2 — routed, with its PIN (L8).** `HISTORICAL_RE` contained `until 0041` — a **forward** clause inside a guard whose contract is "history is not a denial". Measured: 14 lines exempted on the shipped corpus — 13 past-tense, exactly 1 by `until 0041` alone, 0 in doc-budget context — so the live footprint today is nil; but a caveat-marker + `until 0041` line is **silently exempted** (measured), and the presence-scoped copy this pass wrote uses that phrasing everywhere. Not a blocker; **L8:** one test case (red as measured) forcing `until 0041` out of the alternation or scoped.

**Land obligations — delta on round 1.** L1–L6 stand; **L4 discharged in-delta** (merge arithmetic re-run; re-confirm after the real harvest merge). New: **L7** (the gate-4 presence clause) · **L8** (the guard test case).

**Definition of Done — updated.** docs-traceability **MET** (both stale shipped claims closed, one by deleting the drift surface; the scope note states the measured 10/4 reach and all three residual shapes; the new forward promise has a strict falsifier) · maintainability-structure **MET** · reliability-resilience **MET** (R6a–d each with its measurement or unevaluable-predicate reasoning written into the doc) · honesty **MET** (payload-vs-delivery two facts at all nine sites; "never substitute the plugin's copy" explicit; four tag/closure nits closed) · testing **MET** (the guard pinned in both directions — broken on purpose, two tests red) · acceptance criteria **MET** including the grep AC and the built-tree `validate --strict` · no new debt **MET** (L7/L8 are land-commit hygiene).

**Plain English.** Everything asked for is in, and three things came back better than specified: the `/build` skill's stale gate list was deleted and replaced with a pointer to the one place that list lives, so it can't go stale a third time; the invariants file absorbed both byte-tight edits and still landed 60 bytes under its warning line where only one was budgeted; and the "history isn't a lie" guard came with an unrequested test proving it doesn't excuse a real false claim sitting near honest history. I re-broke the guard on purpose and two tests went red. I tested the one thing the implementer declined to do, both ways, and they were right to decline — the change would either have done nothing or turned the build red on two perfectly honest sentences, including the correction this fix pass itself wrote. One small find of my own: the guard's word list included a phrase that points forward sitting in a list otherwise all about the past — today it hides nothing (counted), but one extra test case at land closes it.

**Loop closed.** Round 2 is the last gate round for this slice, as committed at round 1. No round 3, no re-panel. Clear to land once L1–L3, L5–L8 are discharged in the land commit and the INVARIANTS merge figure is re-confirmed against the real harvest merge.

#### Code-review round at land (user-invoked `/code-review`, 2026-08-15 — after the round-2 PASS, before the PR merge)

Ten verified findings (8 finder angles, 13 verifier verdicts: 17 confirmed / 1 plausible / 2 refuted; capped at 10). **All ten adjudicated ACCEPT in bounded form and applied on `land/0041-s6`:** F1 the "plugin's copy prints a green about the harness's ledgers" symptom was only true of a DEV checkout — a real install strips the caps config, so its copy prints the not-configured no-op; all four sites now state both shapes as wrong-repo verdicts · F2 INVARIANTS' "the failure mode is closed" tightened (absent-CONFIG closed; a cap on an ABSENT file still exits 1 even under `reportOnly` — S7's seeder AC restated; lands at 17,990 B, 10 B under the WARN) · F3 `HISTORICAL_RE`'s bare `no longer` was a present-state negation exempting a live denial — narrowed to honest collocations (`no longer stripped|true|the case`), pinned both directions (measured: the superseding-ledger fixture required `no longer true`) · F4+F7 the delivery tripwire's predicate now keys on the **born-prefixed delivered path** (`scripts/claugentic-check_doc_budgets.py`) so it measures DELIVERY, not mention, and its reason instructs the path respell at discharge; doctor's probe row names both path shapes · F5 doctor's restated probe/floor literals are now test-pinned to the hook (`test_doctors_restated_probe_stays_in_step_with_the_hook`) · F6 WORKFLOW gate 4's "nothing runs it for you" scoped to commit time (CI runs it on push + tags) · F8 the CHANGELOG exec-bit sub-flag description de-conditioned (unconditional, matching doctor) · F9 scan-vocabulary rot closed (`TestScanVocabularyIsExercised`: identity pin + every regex branch and denial marker positively exercised) · F10 the reader-contract states the gate's `int(cap*0.9)` floor truncation exactly. **Routed to plan close (ROADMAP):** pinning doctor's prose band numbers to `WARN_RATIO` · an invariant for the S6→S7 presence-scoping obligation (byte-capped out of INVARIANTS this round).

### Slice 7 — init seeding + delivery + chaining (3c)

_Grounded 2026-08-15 by a 6-reader fan-out (wf_cc8109fb) over every touched surface; anchors verified by quote at `main` 2534004. Delivery mechanism resolved by the user 2026-08-15: **copy-with-rename**._

- **In plain English (shown first at the approval gate):**
  - **What this builds:** the snowball fix completes. `init` learns three things: **deliver** the budget gate into the adopter's repo, **seed** a caps config with recommended values, and **chain** the gate into the shared pre-commit wrapper after the tree gate — so an adopter's ledgers get the same commit-time WARN/breach signal this repo has. **One path everywhere (the completion your copy-with-rename choice forces):** the wrapper template and the shipped hook are pinned run-logic-identical, so ONE chain line must name ONE path in both repos — the harness's own copy is therefore **renamed** to the born-prefixed `scripts/claugentic-check_doc_budgets.py` (`git mv`; the alternatives are measured worse: divergent lines per home break the parity pin; a prefixed line over an unprefixed repo copy prints a loud SKIPPED notice on every harness commit forever; a try-both fallback would execute an adopter's own unrelated script as a commit gate). Delivery then becomes a plain stamped copy — a new managed-set table row, the exact tree-gate precedent. The seeder writes `.claude/claugentic-doc-budgets.json` **create-if-absent only** (an existing config is adopter-owned data — skipped byte-untouched on re-run) capping exactly the files init itself ensures exist in the same run **[SA1, Stage-7 round 1: AMENDED — measured false in SOLO mode: divergence (d) writes `CLAUDE.local.md` and leaves `CLAUDE.md` untouched, so the anchor key is MODE-AWARE (solo seeds `CLAUDE.local.md`; drop any non-glob key whose target does not exist on disk at the end of the run — a cap on an absent file blocks every commit, `reportOnly` included). See the verdict.]**: CLAUDE.md **6,000** · DECISIONS index **3,500** · the heritable `docs/claugentic-decisions/*.md` glob **14,000** (zero-match-safe; structurally excludes every managed full-copy doc) · ROADMAP **14,000** · CHARTER **8,000** (new precedent — an optional on-demand ledger the 3-adopter scout found at 60/8/202 KB; the number is a spec choice, flagged at approval) — with **`reportOnly` for any day-one-over file, never a bigger number** (the rung rule), and **never** INVARIANTS or WORKFLOW (a cap on an absent file exits 1 even under `reportOnly`; INVARIANTS is recreate-on-demand). The chain line lands in BOTH wrapper homes at the same relative position; the no-Python skip notice generalizes off "tree gate" wording; and the three strict-xfail tripwires that have guarded every forward promise since S4/S6 **flip and are discharged** — each test survives as a permanent positive pin. The **enforcement story** is swept everywhere the chaining falsifies it ("the one mechanically enforced gate is the tree check" becomes the hook-chained pair, where wired), the ~15 "arrives with Slice 7" tense sites flip to present, and every adopter-facing invocation names the one delivered path.
  - **What "done" means for you:** a fresh `init` in an adopter repo leaves a delivered gate, a seeded tracked config, and a chained hook — the first over-budget commit prints the WARN (report-only files pass loudly; a strict breach blocks); a python-less teammate still commits (warn-and-pass, both gates); re-running init never touches an adopter's tuned caps. The e2e battery proves the report-only breach line appears in **real `git commit` output** (R6, stderr).
  - **What you're accepting:** the rename touches every reference to the old path (tests, CI workflows, docs, the tree) — mechanical but wide; the scans keep matching the bare `check_doc_budgets.py` substring so released-history copy stays scanned · **existing adopters' wrappers are presence-keyed** — init gains a bounded reconciliation rule (an UNMODIFIED wrapper — run-logic exactly the prior shipped shape — is refreshed in place with the chain line; ANY divergence = never clobber, report the exact one-line addition) — a modified wrapper stays un-chained until its owner acts, stated honestly in the report · gate-off repos have no wrapper, so no commit-time budget signal there (reported, never silently skipped) · the seeder's recommended numbers are this repo's load-profile values, not adopter telemetry (none exists — by design) · "run init against a scratch repo + a real adopter read-only" is an **agent-run Verify step** (init is prose; pytest exercises the ARTIFACTS init writes, never the prose — stated honestly in the battery's docstring) · nothing mechanical ever clears a `reportOnly` flag (condense + doctor own that — the plan's old "self-arms" wording is superseded by the implemented semantics).
- **Files & changes:**
  - **The rename (first; everything else follows):** `git mv scripts/check_doc_budgets.py scripts/claugentic-check_doc_budgets.py`. No manifest entry before or after (ships by absence; the do-not-re-add comment at the deletion site updates its named path). Churn swept in the same commit: `tests/test_precommit_wrapper.py` gate tuple (~:620) · `tests/test_shipped_condensation_trigger.py` `GATE_PATH` (the scans must keep firing on the bare `check_doc_budgets.py` substring — released CHANGELOG history uses it; hermetic fixtures keep their pinned semantics) · `tests/test_build_release.py` `recreate_class(...) is None` pin path + ships-test (frozen snapshots stay frozen — they are history) · `tests/test_check_doc_budgets.py` path references · `.github/workflows/ci.yml` + `release.yml` invocations · `docs/claugentic-ARCHITECTURE_TREE.md` entry · every doc naming the old path (the sweep below unifies on the one name; doctor's "whichever of the two names" probe simplifies to one).
  - `skills/init/SKILL.md` — **(a) delivery:** a new step-3 managed-set table row for `scripts/claugentic-check_doc_budgets.py` (stamped Python copy — stamp line 1, shebang line 2; no exec bit, interpreter-invoked); generalize the "strip only line 1" one-file wording and the "four of the five managed files" count copy. **(b) seeding:** a new step beside step 7 (CREATE-IF-ABSENT ONLY; `skipped (present)` on re-run; stays OUT of the step-3 table — the `:887` guard's exact reason; the seed JSON written as a literal block in the skill); shared-mode trackability via the `!.claude/claugentic-doc-budgets.json` negation precedent (`:728`) + the S5 check-ignore-refuse posture; solo mode adds the config + delivered script to `.git/info/exclude`; a `- Doc budgets: <seeded | skipped (present)>` record line (label-keyed, append-if-line-absent, with a reader); report groups gain the Created/Wired rows. **(c) chaining:** the template's run_gate region gains `run_gate scripts/claugentic-check_doc_budgets.py || rc=1` after the tree line (no args — the gate ignores argv; the call-site-args rule); the SKIP notice generalizes off "tree gate" (one stderr line, both homes, test constant updated); the wrapper-header "Today's chained gate (the tree check)" comment flips plural-present in both homes (comments exempt from parity; the tripwire reason demands it). **(d) wrapper reconciliation (the `:1198`/`:1234` gap):** the wired-check learns one bounded rule — an existing wrapper whose run-logic exactly matches the PRIOR shipped shape (current template minus the budget line) is refreshed in place, reported "wrapper: refreshed (chained the budget gate)"; any other divergence = never clobber, report the exact one-line addition. **[SA2, Stage-7 round 1: AMENDED — measured false: NO release ever shipped the `run_gate` wrapper (v0.4.1–v0.5.1 ship the 7-line pre-S5 shape), and even `main` differs by the reworded skip notice — the refresh branch as spec'd can never fire, and the one-line remediation pasted into a released-era wrapper DISARMS the tree gate (measured, exit 1→0). The rule is R1's three-branch, shape-aware form. See the verdict.]** **(e)** step-1's "the tree-check gate needs it" Python wording covers both gates; the fence's one commit-time sentence (`:806`) gains the second gate (fence stays static). Husky block untouched (it runs the whole wrapper; the marker string is the idempotency key — never rename it). Gate-off repos: no wrapper, no chain; reported.
  - `.githooks/pre-commit` — the chain line + the header/notice edits above, mirrored exactly (ordered run-logic parity).
  - **Tripwire discharges (each atomic with its predicate):** remove the three `@pytest.mark.xfail` decorators — config-seeding (`test_check_shipped_content.py:348`), delivery (`:365`), hook-chain (`test_precommit_wrapper.py:499`) — each test survives as a positive pin · delete BOTH HONEST-STATUS comment blocks (`build_release.py:120-123` to a present-tense one-liner; `check_shipped_content.py:293-304`) per their own written instructions.
  - **Copy sweep (the chaining-falsified enforcement story + tense + the one path), by content anchor:** CLAUDE.md's "the one **mechanically enforced** gate" sentence → the hook-chained pair, where wired · `honesty.md:5`'s only-hook-enforced sentence (the budget gate moves lists) · `PRODUCT_SPEC:42` + `PRODUCT.md:27/:246` + `honesty-reviewer.md:14` (same exclusivity class — the grounding surfaced the last three beyond the plan's anchor list) · doctor SKILL ×5 (`:50` rule · `:68-69` "the one hook-enforced gate" · `:74-77` probe row, present-tense one-name · `:85-86` absent-branch · `:110` advisory clause · `:325` table row) · the gate script's own docstring ×3 (`:11-12`, `:129`, `:596`) · `check_shipped_content.py:104` trailing exclusivity clause ONLY (its "NOT hook-wired" stays true) · WORKFLOW `:156` adopter note + `:172` gate 4 (incl. the "Scope today" clause and "nothing runs it for you at commit time") + `:173` trigger scoping + the discharged tripwire-promise sentence · `condense` SKILL frontmatter `:3` + `:20` (frontmatter stays parseable YAML — validate) · CHANGELOG **Unreleased** `:19-20`/`:28-29` amended in place (+ the S7 entry; released sections never edited) · `ARCHITECTURE_TREE:111` · `doc-lifecycle.md:5` + `release-contract.md:8` (record the resolved user call: copy-with-rename + the repo-side rename completion, dated) · INVARIANTS `:111`/`:184`/`:200-201` — the `:200` "routed to S7 … a read, not a proof" sentence becomes the test-pinned statement; **net-negative target** (tense scaffolding removal shortens; the file sits at 17,990/18,000 — if any honest form would trip the WARN, stop and surface, never land a silent WARN). **Survivors (must NOT be swept):** version-sync's and shipped-content's "run-gate, not hook-wired" clauses everywhere · `workflow-process.md:12` (superseded FOR THIS GATE by the recorded 2026-08-12 decision — cite, never edit) · the L8 fixture at trigger-test `:268` · released CHANGELOG history. **S8's, untouched:** README:3 headline · README:57 blocks-done · both manifests' "deterministic architecture enforcement" · /build frontmatter stop claim.
  - `tests/` — **battery second-gate cases:** budget-red (exit 1, report shown, tree gate still ran — run-both-and-report) · budget-green-with-stderr-WARN (the R6 case at the second call-site) · budget-missing SKIPPED (notice names the exact chained path; tree still gates) · empty-argv pin · repair the three single-gate assumptions (`:414` silent-clean, `:357` one-stderr-line, `:483` no-missing-notice) by planting a benign second gate in the fixtures, never by weakening assertions · a sibling non-vacuity pin naming the budget chain line (the `:603` pattern) · **the floor-parse ast pin:** the gate set DERIVED from the hook's `run_gate scripts/…` call-site lines (grows with the chain), the floor derived via the existing `:633` regex, `ast.parse(src, feature_version=floor)` per gate + assert the set is exactly the expected two and excludes `check_shipped_content.py` (its walrus is the scoping contract) — discharges INVARIANTS `:200` · **the seed-block parity pin:** extract the seed JSON from init's SKILL and validate it through the gate's own `_load_config`/`_parse_rule` (loadable · all keys valid · contains NO INVARIANTS/WORKFLOW key) · **the R6 e2e:** a scratch repo with the REAL wrapper + REAL delivered gate bytes + a seeded `reportOnly` config over an over-cap file, `core.hooksPath` wired, `user.name`/`email` set, then a real `git commit` subprocess — assert exit 0 AND HEAD advanced AND stderr contains the `WARN: [report-only] … vs budget` line (utf-8 capture — em-dashes; git-side `sh` executes the hook) · husky-shape + PATH-stripped/stub-python cases extended to the two-gate wrapper.
  - **Agent-run Verify step (not pytest, stated honestly):** run `init` against a scratch repo end-to-end (caps seeded · gate delivered · hook chained · skew nudge fires via a low-stamped fence · `/condense` reachable) + one real-adopter read-only inspection — the spec's gate-#10 battery half that exercises the PROSE.
  - `docs/claugentic-ARCHITECTURE_TREE.md` + `CHANGELOG.md` — refreshed entries; the CHANGELOG records delivery/seeding/chaining + the rename.
  - **Negative space (load-bearing):** the husky marker string · the L8/F3 hermetic fixtures' semantics · exactly ONE `#!/bin/sh` fence in init's SKILL · no second `gate_out=$(` capture line · the wrapper floor stays `(3, 7)` (both gates parse clean — raising it would false-skip working interpreters and red the doctor parity pin) · the seeded config must NOT be a copy of the harness's file (no INVARIANTS entry) · `plugin-distribution.md` untouched · the DECISIONS index untouched.
- **In-scope standards dimensions:** `reliability-resilience` (the seeder/absent-file interaction; wrapper-reconciliation totality; silent-disarm sweep of every new wiring) · `docs-traceability` (the tense+path+enforcement sweep; **Reach, not residence** — the new S6-harvest dimension, applied to every sentence this slice writes; forward promises: none should remain) · `testing` (the tripwire discharges leave positive pins; **Exemptions under test** for any scan the slice touches; no assertion weakening; the derived-set floor pin) · `honesty` (the enforcement story never over-claims — "where wired/chained" qualifiers; the reconciliation report's register) · `maintainability-structure` (one path, one seed block, one wrapper shape; the rename leaves zero stale references).
- **Tests to add:** per above; mutation-shaped: the chain line removed from ONE home = parity red · the budget line's `|| rc=1` dropped = the run-both-and-report case red · a seeded `**` glob or an INVARIANTS key = the seed-block parity pin red · the ast pin's floor moved either direction = red.
- **Acceptance criteria:** full battery green from the repo root AND from `tests/` with **0 xfailed** (all three tripwires discharged to positive pins) · four gates zero-WARN · `claude plugin validate --strict` green (init + condense frontmatter edited — the v0.5.1 lesson) · the release-built tree ships the renamed script and the built tree still validates · template parity green · INVARIANTS ≤ 17,990 B (net ≤ 0; stop-and-surface if any honest form trips 18,000) · repo-wide grep finds zero "arrives with … Slice 7" / "is a later change" forwards and zero references to the old unprefixed path outside frozen snapshots + released history · the R6 e2e's real-commit stderr assertion green · ruff clean.
- **Sizing:** ~22 surfaces — the plan's biggest slice, mostly mechanical rename churn; one session at 1M context, with the pre-authorized fallback split if the build proves otherwise: **7a** = rename + delivery + chaining + tripwires 2/3 + the full copy sweep (the wrapper template's chain line contains the delivery tripwire's predicate string, so delivery and chaining CANNOT be split apart) · **7b** = seeding + tripwire 1 + the HONEST-STATUS deletions + the seeder pins + the R6 e2e (needs the config). The copy sweep never splits from the mechanics; 7a's init-SKILL edits must not contain the config-path literal.

### Slice 7 — Verify verdict (Stage 7)

RUNNING AS: **Opus 4.x** (`claude-opus-5[1m]` — the Opus-generation family; **same model family as the builder and as all five lenses**). Honest independence register: this is a **separate clean-context pass on the most capable available model** — role + context independence only, **not** model-family independence, so model blind spots are *not* de-correlated. A reduction of rubber-stamping risk, never a guarantee.

---

# Stage-7 verdict — plan 0041 Slice 7 (init seeding + delivery + chaining), round 1

## VERDICT: **CHANGES REQUIRED**

The mechanics are the strongest in this plan. The rename is complete, the hook-wired *set* is now derived from the hook itself (a third gate joins the floor and syntax pins for free), all three tripwires are genuinely discharged, no assertion anywhere was weakened, and I re-executed the composite end-to-end in a scratch repo: a fresh shared-mode adopter gets a silent first commit with both gates running and a blocked second commit naming the breach.

What fails the gate is the same class as S6 — **claims measured against the author's checkout instead of the reader's** — except this round it is versions, not deployment shapes. **No released version of this plugin has ever shipped the `run_gate` wrapper.** The reconciliation this slice adds for "adopters who predate the chain" is written against a shape that exists only on `main`, so it can never fire for a single real adopter — and the fallback advice it prints to them, executed literally through real `sh`, **turns off the tree gate they already had**. Plus: a fresh **solo** adopter cannot commit at all, the delivery pin proves nothing, and the land merge trips a doc-budget WARN.

*Test-first audit: **N/A** — this slice did not choose test-first (no independent test-author spawn claimed, none required). Audited normally. Not flagged as the closing slice (S8 pending), so no whole-feature pass.*

---

## What I re-executed
*Scratch worktree at `61822c7` + five scratch repos; **all removed** (`git worktree list` shows only the implementer's; the repo is clean). Read-only on tracked files throughout.*

**Confirmed as reported.** `638 passed` from the repo root **and** from `tests/`, 0 xfailed · four gates exit 0, **zero WARN** (`claugentic-check_doc_budgets.py` prints the five-key OK line) · `claude plugin validate --strict` → `✔ Validation passed` · INVARIANTS **17,979 B** · CLAUDE.md **5,364 B** · ROADMAP **12,564 B**.

**Executed measurements that changed the verdict:**

1. **The released wrapper shape (nobody measured this).** `v0.4.1`, `v0.5.0`, `v0.5.1` — every published release, and the branch's own manifest is still `0.5.1` — ship a **7-run-logic-line** wrapper: `command -v python3`, one `out=$(...)`, `[ $? -eq 0 ] && exit 0`. **No `run_gate` function, no `rc`, no floor probe.** The 31-line wrapper (0041 S5) has never been released. So *every* pre-S7 adopter's wrapper differs from "current template minus the budget line" by **24+ run-logic lines**, not one.
2. **Even against `main` the claim is false.** Running the SKILL's own normalization (drop blanks + whole-line comments) over `81d77a6` vs the branch, minus the budget line: **not equal** — the executable `printf` skip-notice was reworded in the same commit (`claugentic tree gate SKIPPED` → `claugentic gates SKIPPED`). Confirmed on both homes (SKILL template and `.githooks/pre-commit`), 31 vs 31 lines, one differing.
3. **The fallback remediation disarms the gate — measured through real `sh`.** v0.5.1 wrapper + a red tree gate → **exit 1** (commit blocked). Insert `run_gate scripts/claugentic-check_doc_budgets.py || rc=1` "immediately after the tree-gate line", exactly as init's report instructs → `run_gate: command not found`, and the following `[ $? -eq 0 ] && exit 0` now reads *that* line's status → **exit 0**, breach output discarded.
4. **Solo mode, end-to-end.** Gate against an absent budgeted file: exit 1 under a plain cap **and** under `{"max":6000,"reportOnly":true}`. Then through the real wrapper in a scratch repo shaped like a solo init (CLAUDE.md absent, cap seeded): `git commit` → `CLAUDE.md is missing — a budgeted ledger must exist`, **HEAD unmoved**. The repo cannot make its first commit.
5. **Delivery mutant M1, independently.** Deleted `skills/init/SKILL.md:147` (the managed-set delivery row) → **638/638 green**, whole suite, both the delivery pin and its sibling.
6. **The prescribed delivery fix, both ways.** Region-scoped row regex: anchors `"The managed set is exactly:"` and `"**Per file"` each occur **exactly once**; under M1 → no match (red), post-state → match (green). Adopt.
7. **GAP-2 both ways.** `GIT_CONFIG_GLOBAL` with `commit.gpgsign=true` → `test_a_report_only_breach_...` fails, `returncode 128`. My one-line fix (repo-local `commit.gpgsign=false` in the fixture) → passes under the hostile global **and** clean.
8. **Merge arithmetic (`git merge-tree --write-tree 61822c7 harvest/0041-s6`, materialized with `git archive` and measured with the real gate).** Exactly two conflicts (WORKFLOW, release-contract). Auto-merged: **`docs/claugentic-decisions/deterministic-gates.md` = 12,610 B vs the shard WARN band 12,600** → **the land merge fires a WARN.** ROADMAP merges to **12,470** (harvest shrinks it — the branch-only 12,564 figure is not the land number).
9. **Bounded agent-run init e2e** (see the dedicated section below).

---

## (1) Required changes — one fix pass, priority order

### R1 — **BLOCKING.** The wrapper reconciliation is written against a shape no release ever shipped, and its fallback advice disarms the gate the adopter already has
`skills/init/SKILL.md:605-625` (+ `:627-634`, `:1251`, `:1298-1304`, `:1330-1334`, `:1375`) · `CHANGELOG.md:49-51`
*(merges reliability G2+G3+G4, honesty #1+#2, maintainability G2+G4 — one edit region closes all six)*

Measurements 1-3 above. Four defects, one root cause:

- **(i) The REFRESH branch cannot fire for its named population.** `:609-613` defines the comparable as "the template above minus its budget line, **which is exactly the PRIOR shipped shape**." It is not — off by one executable line against `main`, off by the whole `run_gate` refactor against every release. The block names "a repo `init`'d before the budget gate was chained"; **100 % of that population lands in the never-clobber branch.**
- **(ii) The fallback makes a false statement about the user.** `:616` — "The adopter … **has edited that file**". False for every adopter who edited nothing.
- **(iii) The fallback's one-line fix is destructive on the wrapper those adopters actually have** (measured: exit 1 → exit 0, a silent disarm of the *pre-existing* gate — this is worse than the gap it repairs).
- **(iv) A third state is unenumerated.** A wrapper equal to the **current** template (the normal state after any fresh S7 init) is not equal to template-minus-budget-line, so a settled re-run reports "you edited this" + "add a line" that is already there; applying it chains the gate twice. `:1330-1334` promises the exact opposite ("every later run is a no-op again"). **(v)** And the husky branch at `:627-634` still writes `.githooks/pre-commit` unconditionally — no compare, no report — two bullets after the never-clobber promise.

**Exact shape** (one edit region; no new `#!/bin/sh` fence — `tests/test_precommit_wrapper.py:684` asserts exactly one, and a literal prior-shape block would become a second source of wrapper truth):

- **(a)** Add the missing branch **first**: *"Run logic equals the current template → **already chained**; write nothing, report 'pre-commit hook already wired (budget gate chained)'."*
- **(b)** Keep REFRESH for *current-template-minus-the-budget-line*, described honestly: *"this version's shape without the budget line"* — **delete** "which is exactly the PRIOR shipped shape".
- **(c)** Fallback stops asserting authorship: *"the on-disk run logic is not this version's shape — it may be an older harness wrapper or your own edit; either way it is never rewritten."*
- **(d)** Make the reported fix **shape-aware**: report the one-line addition **only when the on-disk wrapper defines a `run_gate` function**. Otherwise report: *"your wrapper predates the `run_gate` shape (v0.5.1 and earlier) — that one-line addition does not apply to it and must not be pasted in: `run_gate` is undefined there, and the inserted line's exit status can mask the tree gate's (measured). The safe repair is to move the wrapper aside and re-run `init`, or replace it with the template above after diffing your own changes."*
- **(e)** Husky branch `:627-634`: run the same compare before writing — refresh only the recognized shape, otherwise leave it and report.
- **(f)** `CHANGELOG.md:49-51`: replace "byte-for-byte the previously shipped one" with run-logic scoping (comments/blank lines ignored, so a comment-only edit **can** be rewritten), **and state plainly** that a wrapper from **v0.5.1 or earlier is never auto-chained** — with the shape-aware instruction, not the one-liner.

**PIN** (the residual this round cannot leave unpinned): extend `TestTemplateParity` with (1) a whitespace-normalized assertion that the SKILL's **prose remediation line** matches the template's chain line — the `tests/test_check_doc_budgets.py:1300-1310` technique, needed because `:619-620` hard-wraps mid-line — and (2) a required-phrase assertion naming all three branches (already-chained / refresh / never-clobber), so deleting a branch turns the suite red. *(maintainability G4 supplies the technique; this makes the prose rule falsifiable rather than deletable.)*

### R2 — **BLOCKING.** Solo mode seeds a cap on a file solo `init` never creates → the fresh solo adopter cannot commit at all
`skills/init/SKILL.md:951-965` · *(reliability G1 — and it **refutes the spec**, see SA1)*

Measurement 4. Chain: solo divergence (d) (`:790-799`) writes everything step 6 writes into `CLAUDE.local.md` and leaves the committed `CLAUDE.md` **byte-untouched** — in a repo that has none, it does not exist. Solo divergence (b) (`:714`) writes a **run-logic-identical** wrapper to `.git/hooks/pre-commit`, so it carries the budget chain line. The seed caps `CLAUDE.md` unconditionally. `_check_one` → `("error", "… is missing")` → exit 1, and the `reportOnly` grace applies to the **size** verdict only (`:531-535`). Verified end-to-end: HEAD never advances. Scope is exactly this one key — the three step-7a ledger seeds are written to disk in solo mode too (divergence (a) only excludes them from git), so they measure fine.

**Exact shape** — prose bullet beside the seed block (the fix **must** be prose: I verified `_seed_text` asserts exactly one ```` ```json ```` fence and `test_the_seed_caps_exactly_what_that_init_run_creates` asserts the exact five-key set):

> **Solo mode:** the harness anchor is `CLAUDE.local.md` (solo divergence (d)) and the committed `CLAUDE.md` is left untouched — so seed **`CLAUDE.local.md`** in its place. General safety clause: before writing, **drop any non-glob key whose target does not exist on disk at the end of this run** — a cap on an absent file is a hard exit 1 that `reportOnly` cannot grace, and it blocks every commit.

Also correct the false premise in the pin's own comment (`tests/test_check_doc_budgets.py`, `test_the_seed_caps_exactly_what_that_init_run_creates`: *"CLAUDE.md (step 6)"* — true in shared mode only).

**Do NOT widen `docs/claugentic-INVARIANTS.md:111`** to carry the solo clause: INVARIANTS is at 17,979 / WARN 18,000 → **21 B of headroom** (24 B after R7's −3 B). The rule's home is init's SKILL, which is unbudgeted.

**PIN:** extend `test_the_skill_states_the_grace_rule_and_the_never_raise_rule` (whitespace-normalized, same technique) with the solo clause — e.g. `assert "CLAUDE.local.md" in text` — so deleting the bullet turns red.

### R3 — the delivery pin proves nothing; the slice's headline capability is mechanically unguarded
`tests/test_check_shipped_content.py:362-376` (and `:351-360`) · *(testing GAP-1 + Minor-1)*

Measurement 5, re-executed independently: **delete the delivery row and the entire 638-test suite stays green.** The prefixed path now occurs 8× in the SKILL, so a whole-file substring is satisfied by exactly the "prose that merely discusses the gate" the test's own comment (`:369`) says cannot satisfy it. It was true at S6 (zero occurrences); this slice's own edits at `:579`/`:1181` retired it. **`init` is prose — pytest is delivery's only mechanical custody.**

**Exact shape** (I measured it both ways — see measurement 6):

```python
text = init_skill.read_text(encoding="utf-8")
assert text.count("The managed set is exactly:") == 1   # anchored by UNIQUENESS, never by ordinal
table = text.split("The managed set is exactly:", 1)[1].split("**Per file", 1)[0]
assert re.search(r"^\|\s*`scripts/claugentic-check_doc_budgets\.py`\s*\|", table, re.M), \
    "the step-3 managed-set row is what DELIVERS the gate — a mention elsewhere is not delivery"
```

Same edit for the sibling config pin: anchor on the step-7b heading (`**(b) Seed the doc-budget caps config`) rather than the bare path (M2 measured: the path survives at `:283`, `:1230`, `:1364`).

### R4 — three shipped, adopter-facing sites missed the presence-scope sweep; one is now false for the *entire existing adopter base*
*(honesty #3/#4/#7/#8, docs G3 — the absorption test in reverse: the slice hedged eight siblings and left these)*

- **`skills/condense/SKILL.md:3` (frontmatter) + `:20-22`** — *"init delivers a copy **and chains it into your pre-commit hook**"* / *"in an `init`'d repo the WARN reaches you **at every commit**"*. Unconditional, on a shipped surface. False for gate-off, false with no working Python, and — per R1 — **false for every pre-v0.5.2 adopter, whose wrapper is never auto-chained even after they upgrade and re-run `init`**. Use the sibling register ("where your pre-commit wrapper is wired"). **Re-run `claude plugin validate --strict` after touching frontmatter** (the v0.5.1 lesson; it is green today, I checked).
- **`docs/claugentic-PLAYBOOK.md:85`** — new sentence *"Two run there, chained into one hook"* on the shipped plain-English guide, which carries **no** gate-off caveat anywhere. Append the one-clause scope. PLAYBOOK is uncapped — no byte risk.
- **`skills/doctor/SKILL.md:325`** — *"N-A means init has not run here"* upgrades a presence fact into a false claim about the user's history. The same file's probe branch at `:85-86` already words it correctly ("or one adopted before the delivery step existed") — copy that up.
- **`docs/claugentic-WORKFLOW.md:156`** (nit, fold while open) — "and chains it into your pre-commit hook" → "and, where a pre-commit wrapper is wired, chains it into that hook."

### R5 — reader-shape provenance + two failure-path messages
- **`skills/init/SKILL.md:971`** — *"recommendations from **this repo's** load profile"*. `init` executes **in the adopter's repo** — an agent writing the report resolves "this repo" to theirs, claiming the caps were derived from *their* profile. → *"the **harness's own** load-profile recommendations — not measurements of your repo, and not telemetry (there is none, by design)."* (honesty #5)
- **`scripts/claugentic-check_doc_budgets.py:479`** — a deliberately deleted optional ledger (the charter, which `:904-907` calls optional) now hard-blocks **every commit**, with a message that never names the caps file. Append: *"— if you removed it deliberately, delete its entry from `.claude/claugentic-doc-budgets.json`."* Verified test-safe: all five assertions on that message are the substring `"is missing"` (`tests/test_check_doc_budgets.py:169,217,370,1039,1150`). `scripts/` is unbudgeted. (reliability G5)
- **`skills/init/SKILL.md:977`** — day-one-over says "measure each seeded **path that already exists**"; the glob key is a shape, and the gate applies the cap **per matched file**. An adopter who sharded before upgrading gets a strict breach on their first commit — the exact hard block the rung rule exists to prevent. Add: *"…and for the glob entry, expand it and measure **every** match — any match over cap ⇒ seed that entry `reportOnly`."* (reliability G6)

### R6 — the R6 e2e leaks the ambient git environment (false-RED only — I overrule the lens's second half)
`tests/test_precommit_wrapper.py:1103-1105` · *(testing GAP-2)*

Re-executed: with `commit.gpgsign=true` in the ambient global config, `test_a_report_only_breach_...` fails `returncode 128, gpg: signing failed`. Real defect — this is the suite's first real `git commit` and the only real-git test that doesn't neutralize ambient config.

**Overruled, in my own name, with the measurement:** the lens's stronger claim — that a report-but-don't-block mutant would *survive* in that environment — is **refuted**. I applied that mutant (`return 1` → `return 0` at gate `:604`) and `test_a_strict_breach_blocks_the_commit` went **RED in both environments**; the "the reason is on stderr" assertion keeps its teeth (under gpgsign the stderr carries only the gpg failure). So this is a false-RED, never a false-green — severity low, fix still worth one line.

**Exact shape (measured both ways, and simpler than prescribed):** add `("commit.gpgsign", "false")` to the fixture's existing repo-local config loop. Repo-local beats a hostile global, `core.hooksPath` stays repo-local (it is the thing under test), and no `GIT_CONFIG_GLOBAL`/`SYSTEM` env plumbing is needed. The lens's fuller idiom is **dropped** as gold-plating for this test.

### R7 — currency, comments and one delivered-file leak (all in copy this slice authored)
- **`docs/claugentic-INVARIANTS.md:107`** — still spells the old unprefixed `check_doc_budgets.py` in a sentence this slice edited two lines later, while the same diff de-named it at `.gitignore:7` and `:184`. → "the doc-budget gate shipped". **−3 B → 17,976** (keeps the ≤17,990 AC and buys 3 B of the 21 B headroom back). (docs G2)
- **`docs/claugentic-ARCHITECTURE_TREE.md:119/:120`** — name the new cross-file couplings: `:119` "+ the init seed-block parity pin (parses `skills/init/SKILL.md`)"; `:120` "(fake gate — plus a REAL-gate real-`git commit` e2e) + the floor/ast pin derived from the hook's own call sites". Both land ~347/~410 chars, under `MAX_ENTRY_CHARS=450`. (docs G4)
- **`scripts/claugentic-check_doc_budgets.py:11`, `:130`, `:598`** — three in-body `plan 0041 Slice 7` refs in a file now **delivered into adopter repos**, where `.claude/plans/` does not exist. Its sibling delivered script cites zero. Drop the refs, keep the (now true, present-tense) statements. (docs G7)
- **`tests/test_shipped_condensation_trigger.py:19-22`** — move the S7 paragraph below the two-bullet list it currently splits. Byte-neutral. (docs G6 / maintainability G3)
- **`tests/test_precommit_wrapper.py:754-756` + `:849`** — hoist `FLOOR_RE` to module level beside `RUN_GATE_CALL_RE`; the comment currently asserts a single-sourcing the code doesn't have. (maintainability G1)
- **`:585-591`** — comment over-claim: measured (M5), dropping `|| rc=1` loses the **exit code**, not a message. Reword; no assertion change. **`:67`** — add `assert SKIP_NOTICE not in MISSING_GATE_NOTICE and MISSING_GATE_NOTICE not in SKIP_NOTICE`; two negative assertions at `:466`/`:561` depend on a property nothing checks. (testing Minor-2/3)

### R8 — fold harvest's two-shape symptom into `release-contract.md:8` **on the branch**, so take-S7 becomes lossless
`docs/claugentic-decisions/release-contract.md:8` · *(docs G1; adjudicates F5)*

Measured across three revisions: `main` and **S7** both say *"and prints a green about the wrong repo"*; **`harvest/0041-s6`** carries the corrected *"from an **install** (caps config stripped) a *not configured* no-op, from a **dev checkout** a green about the harness's ledgers; both are verdicts about the wrong repo."* The planned take-S7 resolution would silently revert the S6 code-review correction on the exact page a future agent reads before re-proposing "point the plugin's copy at their tree" — and would leave the ledger asserting the one-shape symptom the new *Reach, not residence* corollary forbids.

Fixing it **on the branch** (rather than at the merge) is what makes the implementer's own F5 claim true. **Byte-measured:** branch 12,316 B; the fold is **+135 B → 12,451**, WARN 12,600 → **149 B clear** ✓. **Do NOT also fold harvest's trailing parenthetical** (+153 B → 12,602 → **trips the WARN**); its lesson is recorded verbatim in the harvest's own `docs/claugentic-standards/docs-traceability.md`, which lands in the same merge.

---

## Spec amendments (procedure: `docs/claugentic-WORKFLOW.md` → Stage 7 — point, don't restate)

- **SA1 — the seed-list premise is wrong for solo mode.** The spec says the seeder caps *"exactly the files init itself ensures exist in the same run: **CLAUDE.md 6,000** …"* (§Slice 7 *In plain English*; echoed in the checklist at `:98`). **Measured false:** solo divergence (d) writes `CLAUDE.local.md` and leaves `CLAUDE.md` untouched, so a solo `init` guarantees no such file — and a cap on it blocks every commit, `reportOnly` included. The implementer implemented the spec faithfully; **this refutes the SPEC, not the diff.** Amend the spec line to the mode-aware anchor rule in R2.
- **SA2 — the reconciliation's comparable is spec'd wrong.** The spec says *"an existing wrapper whose run-logic exactly matches the PRIOR shipped shape **(current template minus the budget line)** is refreshed in place"* (§Files & changes (d); echoed in *What you're accepting*). **Measured false against every release** (v0.4.1/v0.5.0/v0.5.1 ship a `run_gate`-less 7-line wrapper) **and against `main`** (one extra reworded executable line). Amend to R1's three-branch rule, and add the consequence the spec never anticipated: the remediation line **must be shape-aware**, because pasting it into a released-era wrapper disarms the tree gate (measured).

---

## (2) Land-commit obligations (orchestrator, not the fix pass)

- **L1 — the land merge fires a doc-budget WARN; it must be condensed in the merge commit.** Measured on the materialized merge tree with the real gate: `docs/claugentic-decisions/deterministic-gates.md` = **12,610 B vs WARN 12,600** (both branches append to that shard: `main` 9,684 → harvest 10,754 → S7 11,540). **Remedy:** apply the docs lens's G5 at the merge — strip the five restated cap numerals from `:11` (the config owns them; nothing compares the two; the fix is byte-negative and only **−11 B** is required), keeping every rationale. Re-run the gate on the merged tree and quote the number in the land commit. *This also corrects the reliability lens's F4 residual, which measured branch-only and predicted a silent first post-land commit: it will **not** be silent.*
- **L2 — resolve the two merge conflicts and verify losslessness.** `git merge-tree` reports exactly two: `docs/claugentic-WORKFLOW.md` and `docs/claugentic-decisions/release-contract.md`. **F5 adjudicated HALF TRUE by measurement:** WORKFLOW's tail is byte-identical to harvest's (take-S7 lossless ✓); release-contract was not — R8 fixes that on the branch, after which take-S7 is lossless there too. Re-verify both after R8 lands.
- **L3 — quote the post-merge budgeted-ledger table** in the land commit (these are the *land* numbers, not the branch numbers): `deterministic-gates.md` 12,610 → must land < 12,600 after L1 · `release-contract.md` 12,451 (post-R8) / 12,600 · ROADMAP **12,470** / 12,600 (**130 B** headroom — the branch-only 12,564 is not the land number) · CLAUDE.md 5,364 / 5,400 (**36 B**) · INVARIANTS 17,976 post-R7 / 18,000 (**24 B**) · DECISIONS index 2,630 / 3,150.
- **L4 — plan repair:** `.claude/plans/0041-adopter-hardening.md:44` (§Approach) still says *"then enforcement **self-arms**"* — a mechanical transition the implemented semantics explicitly refuse (`skills/init/SKILL.md:983-985`). The slice contract records it superseded, but §Approach is what a reader of the still-active plan (S8 pending) reads first. (honesty #6)
- **L5 —** record SA1 and SA2 in the plan's S7 block with the measured evidence.
- **L6 —** `docs/claugentic-ARCHITECTURE_TREE.md` + `CHANGELOG.md` entries for whatever the fix pass touches.
- **L7 — DECISIONS line** (route via the index; `deterministic-gates` is the topical shard — **so it rides inside L1's byte budget**): *a wrapper/artifact reconciliation must be keyed on shapes that were actually **RELEASED**; the harness's own `main` is not any adopter's baseline.*
- **L8 — harness impact / harvest candidate (this round's class).** S6 harvested *"measure in the shape the READER will be in (install vs dev checkout)."* This round shows the same rule has a **version axis**: *the reader's baseline is the last **published** version, not your `main` — before writing an upgrade/reconciliation rule, diff against the release tags.* Belongs beside the existing rule in the `synthesizer-gate` posture, and as an upgrade-path check in `docs/claugentic-standards/reliability-resilience.md`. Second, smaller candidate: *"never ship a copy-pasteable remediation line without executing it against the artifact the reader actually has."*

---

## (3) ROADMAP routings — **byte-bounded: 130 B of post-merge headroom (12,470 / 12,600)**

- **One line only:** an upgrade path that can genuinely re-chain a pre-v0.5.2 adopter's wrapper (a released-shape catalogue, or an `init --rewrite-wrapper` confirm-and-replace). R1 only makes the *report* honest; it does not restore the repair.
- **If (and only if) L1's condensation frees room**, these may ride along; otherwise they are recorded here and nowhere else: the `.claude/` **directory-form** negation instruction at `skills/init/SKILL.md:989` is a git no-op (a negation cannot re-include under an excluded directory — `.claude/` → `.claude/*`; the slice's own new `check-ignore` verify catches it loudly, so it is inherited, not charged); the declared-twin comment drift at `.githooks/pre-commit:41` vs `skills/init/SKILL.md:559` ("while **still** passing" vs "while passing"); the redundant third `(3, 7)` assertion at `tests/test_precommit_wrapper.py:839`.

---

## (4) Explicitly dropped, with reasons

- **The lens's `GIT_CONFIG_GLOBAL`/`GIT_CONFIG_SYSTEM` plumbing for R6 — dropped.** Measured equivalent for this test via one repo-local config line; the fuller idiom would also strip `user.name`/`email` and add a fixture file for no gain here (KISS).
- **"A report-but-don't-block mutant survives under ambient gpgsign" — refuted by measurement.** Killed in both environments; the lens's own parenthetical contradicts its sentence. GAP-2 is false-RED only.
- **docs G5 as a branch edit — moved, not dropped.** It becomes load-bearing at L1, where it is what buys the merged tree back under WARN.
- **Carrying the prior released wrapper shape as a literal block in init's SKILL — dropped.** `tests/test_precommit_wrapper.py:684` asserts exactly one `#!/bin/sh` fence, and a literal would become a second source of wrapper truth. R1's shape-aware branch needs no literal.
- **Pinning the seeded cap *numbers* — dropped (change-detector).** The seed's *shape* is pinned through the gate's own reader, which is the real seam; the values are a stated spec choice.
- **Rewriting `deterministic-gates.md:11` inside the fix pass — dropped as gold-plating there**; it is the land act (L1).
- **A re-panel next round — dropped.** See the bounded round below.
- **F1 (conftest `_load_hyphenated`) and F3 ("Three properties" → "Four") — endorsed, no change.** F1 is a genuine unstated prerequisite discharged at the one file that already owns hyphenated-name mapping, with its docstring claim kept true; F3 is a verified pre-existing miscount inside a sentence being rewritten anyway — leaving a known-false count in a line you are editing would be the worse call.
- **F6 — endorsed as scoped.** Git forwards hook output to its own stderr, so both streams legitimately arrive there at the `git commit` boundary; the discriminating half stays on the wrapper tests. The comment under-claims rather than over-reads.

---

## The agent-run init e2e (the spec assigns it to Stage 7)

**I ran a bounded version myself** — three scratch repos built from init's **own** SKILL (wrapper template and seed JSON extracted from the file, not hand-typed), all removed:

| Scenario | Result |
|---|---|
| Fresh shared-mode: delivered gate + seeded caps + chained wrapper + `core.hooksPath` | first `git commit` **lands silently**; both gates ran (tree gate blocked an unregistered file until the tree was updated — correct) |
| Same repo, CLAUDE.md pushed to 6,100 B | commit **blocked**, `CLAUDE.md: 6100 bytes vs budget 6000 — over budget`, HEAD unmoved |
| Solo shape (CLAUDE.md absent, cap seeded) | **first commit impossible** — R2 |
| Pre-S7 adopter (v0.5.1 wrapper) + init's reported remediation | tree gate **disarmed**, exit 1 → 0 — R1 |

**The remaining half is assigned to the fix-pass round, with its shape** — and deliberately not run now, because R1/R2 rewrite the very prose it exercises. Required shape: three scratch repos plus one read-only real-adopter inspection — **(a)** fresh **shared** init (caps seeded · gate delivered · hook chained · skew nudge fires via a low-stamped fence · `/condense` reachable), **(b)** fresh **solo** init on a repo with no `CLAUDE.md` (must produce a committable repo — the R2 falsifier), **(c)** a repo pre-seeded with the **v0.5.1** wrapper, re-run init (must report the shape-aware message, must not claim the adopter edited it, must not emit the `run_gate` one-liner), **(d)** an immediate **second** init run on (a) (must report "already wired", zero further instructions — the R1(a) falsifier). Report each outcome verbatim.

---

## Definition of Done

| Dimension / criterion | Status | Evidence |
|---|---|---|
| **reliability-resilience** | **GAP** | Run-both-and-report verified (`\|\| rc=1` both homes, red-first-gate does not mask the second); loud missing-gate skip; probe-not-pick and exec-bit posture preserved; husky marker correctly left unrenamed; create-if-absent seeding with a genuine reader for the deleted-config opt-out. **Fails on:** solo seeding blocks every commit (R2, executed); the reconciliation cannot fire and its remediation disarms the tree gate (R1, executed); unenumerated already-chained state (R1 iv); husky sibling still clobbers (R1 v); two failure-path messages (R5). |
| **docs-traceability** | **GAP** | 23 gate-path sites, 22 prefixed; payload-vs-presence stated as two facts at every actionable site; exclusivity class closed (only released history survives); decisions dated in the right shards; index and `plugin-distribution` untouched; the five cap values live in exactly one place. **Fails on:** `release-contract.md:8` loses the S6 correction at merge (R8, measured); `INVARIANTS:107` stale path (R7); PLAYBOOK's unhedged commit-time claim (R4); two tree entries silent on new cross-file readers (R7); plan-slice refs in a delivered file (R7). |
| **testing** | **GAP** | 638/638 both cwds, **0 xfailed** — all three tripwires genuinely discharged; zero assertions weakened anywhere in the diff (three single-gate assumptions repaired by planting a real second gate, not by loosening); the derived hook-wired set + `ast.parse` floor pin + both-spellings scan token are exemplary *exemption-under-test* work; 11 of 12 panel mutants killed and my own M1/M5/M9-class probes agree. **Fails on:** the delivery pin is vacuous (R3, re-executed: 638 green with delivery deleted) and its sibling comment names the wrong custodian; ambient-env leak in the new e2e (R6). |
| **honesty** | **GAP** | The enforcement rewrite carries a real scope qualifier at eight sites; the R6 class docstring's "HONEST SCOPE" and the F6 stream comment are the register I would ask for; the discharged tripwires state what they do and do not prove; no "self-arms" in shipped copy. **Fails on:** the prior-shape claim and the "has edited that file" statement (R1); CHANGELOG's stricter-than-true never-clobber (R1f); condense/PLAYBOOK/doctor unconditional claims (R4); "this repo's load profile" read in the adopter's repo (R5). |
| **maintainability-structure** | **MET** *(with fixes)* | One path, one seed block, one chain line in the two parity-pinned homes; `git mv` at 96 % similarity with mode `100644` preserved; the tree gate's stamp-keyed detection meant the rename broke nothing implicit; `_chained_gates()` converts a hand-typed tuple into a derivation so a third gate joins the pins for free; ledger facts each have exactly one home. Four cheap fixes (R1f, R7 ×3) — none structural. |
| **Acceptance criteria** | **Mostly met** | Battery both cwds, 0 xfailed ✔ · four gates zero-WARN **on the branch** ✔ (**but the land merge WARNs — L1**) · `validate --strict` ✔ · INVARIANTS 17,979 ≤ 17,990 ✔ · zero forward promises, zero stale unprefixed paths outside frozen snapshots/history ✔ · R6 real-commit stderr assertion ✔ · ruff ✔. **NOT met:** the AC's *spirit* on delivery — the discharged tripwire pins nothing (R3) — and the reconciliation AC is spec'd against a shape no release shipped (SA2). |
| **No new debt** | **NOT YET** | Shipped as-is, R1 hands every existing adopter a false accusation plus a gate-disarming instruction, and R2 hands every fresh solo adopter an uncommittable repo. R3 leaves the headline capability with no mechanical custody. |

---

## Plain English

**What's good.** The plumbing works, and I proved it rather than read it: I built a throwaway project with exactly the files `init` says it writes, and a real `git commit` ran both checks, stayed silent when everything was in budget, and refused the commit with a clear reason when a file went over. The rename is complete and the tests now work out *which* checks the hook runs by reading the hook — so the next check added gets its safety pins automatically. Nothing was quietly weakened to make anything pass.

**What's wrong.** Three things, and the first two are the kind you cannot ship.

1. **The "repair an old user's hook" feature is aimed at a hook that doesn't exist.** The published versions of this plugin (0.4.1, 0.5.0, 0.5.1) install a *short, old-style* hook. The repair compares against a *new-style* hook that has only ever existed on the development branch. So for **every real user**, the repair never fires — they're told "you must have edited this file" (they didn't), and handed a line to paste in by hand. I pasted it in, exactly as instructed, into the hook they actually have: **their existing check stopped working and the commit sailed through.** We would be shipping an instruction that breaks the one thing that was already protecting them.
2. **A solo user can't commit at all.** In solo mode the harness writes its notes to `CLAUDE.local.md` and deliberately leaves `CLAUDE.md` alone — but the new size-limit list still watches `CLAUDE.md`. A limit on a file that isn't there is a hard stop, and the "just warn me" setting can't soften it. I set this up and tried: the very first commit is refused, permanently, until the user hand-edits a file they were just told is theirs to own. One sentence in the setup instructions fixes it.
3. **The test that's supposed to prove the gate gets installed doesn't.** I deleted the single line that installs it and all 638 tests still passed — the test just searches the whole document for a filename that now appears eight times. This is the slice's headline feature and that test is its only automatic guard.

**How bad, and what could break.** Nothing here risks data. The realistic damage is: an existing user upgrades, follows our own printed advice, and silently loses the check they had; a new solo user hits a wall on their first commit and concludes the tool is broken. Both are first-five-minutes experiences, which is the worst place to spend trust.

**And two things only the synthesis could catch.** First, **the merge that lands this trips the very warning this slice wires up**: both branches appended to the same decisions file and it merges to 12,610 bytes against a 12,600 warning line — ten bytes over. There is already a byte-negative cleanup identified for that file; it should be part of the merge, not deferred. Second, the "merging is lossless" claim is **half true**: one of the two conflicting files would quietly revert a correction made last week, on exactly the page a future agent reads before re-proposing an idea we already rejected. Measured; +135 bytes fixes it and stays clear of the limit.

---

## What the next round needs — bounded

**Targeted delta re-verify, solo. No re-panel.** Every finding above is measured or a bounded wording change; a second five-lens fan-out would re-derive facts already executed. The next pass checks exactly seven things:

1. **R1** — the three branches read as specified; the fallback is shape-aware; **re-run my `sh` probe**: the released 7-line wrapper + whatever init now instructs must leave a red tree gate at **exit 1**, never 0. The new parity/required-phrase PIN goes red when a branch is deleted.
2. **R2** — the solo clause is present and its PIN goes red when deleted; and a solo-shaped scratch repo (no `CLAUDE.md`, seeded caps) **commits successfully**.
3. **R3** — delete `skills/init/SKILL.md:147` and confirm the suite goes **RED** (it is 638-green today).
4. **R6** — `GIT_CONFIG_GLOBAL` with `commit.gpgsign=true` and the R6 pair still passes.
5. **R4/R5/R7/R8** — sites read as prescribed; `validate --strict` green after the condense-frontmatter edit; `release-contract.md` measured **≤ 12,600**; INVARIANTS **≤ 17,990**.
6. **The agent-run init e2e**, all four scenarios above, reported verbatim.
7. Full battery both cwds, **0 xfailed**, four gates zero-WARN, plus `git merge-tree` re-run with the merged `deterministic-gates.md` measured **< 12,600** (or L1 explicitly scheduled for the land commit).

**Applying R1–R8 closes this gate — no third round.**

#### Round 2 (delta) — `61822c7..9e9470c`

RUNNING AS: **Opus 4.x** (`claude-opus-5[1m]`) — same model family as the builder and as round 1's panel. Role + clean-context independence only, **not** model-family independence. Bounded **targeted delta re-verify, solo** (no re-panel), exactly as scoped at the end of round 1.

**VERDICT: PASS — conditional on R1-a**, the single residual below. **No third gate round**: apply R1-a, confirm the one mutant goes red, land.

Seven of eight required changes discharge cleanly, several better than prescribed. The one thing round 2 cannot wave through is that **R1's own new pin is vacuous in exactly the way R3 was** — the synthesizer removed the remediation quote the pin exists to guard and all 640 tests stayed green.

**What I re-executed** *(scratch worktree at `9e9470c` + two scratch repos; all removed)*: battery and gates first-hand (`640 passed` both cwds · **0 `mark.xfail` occurrences** by grep · node 372/0 · four gates exit 0 with 0 `WARN:` lines counted on the merged stream · ruff · `validate --strict` ✔ post-frontmatter) · **the R1 `sh` probe against the real v0.5.1 tag bytes** — the released wrapper defines no `run_gate`, branch (3b) fires, the wrapper is untouched, a red tree gate exits **1** ✔; the paste-hazard still reproduces exactly, which is why init now refuses to emit it · R1 branch-deletion mutant → the three-branch pin **FAILED** ✔ · R2 first-hand: a solo-shaped scratch repo **commits successfully** (HEAD advanced, status clean) and the solo-clause pin is red-when-deleted ✔ · R3 both mutants **FAILED** where round 1 measured 638-green ✔ · **R6 hostile-global probe run in my own environment** (`GIT_CONFIG_GLOBAL` with `commit.gpgsign=true`): both e2e cases pass at `9e9470c`, failed 128 at `61822c7` ✔ · R4/R5/R7/R8 sites as prescribed; INVARIANTS **17,976** ≤ 17,990 ✔; release-contract **12,449** < 12,600 with the harvest fold **byte-identically present and lossless** ✔; tree entries 365/446 chars under the 450 ceiling ✔ · merge arithmetic re-run: two conflicts, both take-S7 lossless; merged `deterministic-gates.md` **12,610** → **L1 must shed ≥ 11 B at the merge commit** (confirmed as check 7's "or" branch — scheduling satisfies it).

**The residual — R1-a** (`tests/test_precommit_wrapper.py:832-843`): `test_the_prose_remediation_quotes_the_real_chain_line` searches the WHOLE skill, which contains the template fence, so the template's own chain line satisfies it regardless of the remediation prose. Measured: quote deleted → GREEN (640 passed); quote drifted to `--staged || rc=1` → GREEN. This is the R3 class reintroduced inside the fix for R1 — and it guards the literal text an adopter is told to paste into a commit hook, the one string in this slice whose drift is destructive. **Exact edit** (both anchors measured unique, both mutants red under it): scope the search to branch (3)'s region between `**(3) Anything else → NEVER CLOBBER` and `**A repo with NO wrapper gets NO chain` (uniqueness asserted, never ordinal), whitespace-normalized. **PIN:** the quote-removal mutant itself — delete the chain-line quote from branch (3a), template untouched; must go RED.

**Adjudication of the three flagged deviations.** (1) R6 sandbox deviation ACCEPTED and superseded — the synthesizer ran the real hostile-global probe itself. (2) The 456→446-char tree-entry trim and the retained `Slice 6` schema-provenance ref BOTH ACCEPTED (R7 was scoped to refs decorating run-time statements an adopter acts on; `:32` misleads no adopter action). (3) The real-adopter inspection is NOT covered by scenario (c) — (c) is the *stronger* evidence for the reconciliation question (the whole released population), but only a real repo can answer whether the day-one-over `reportOnly` path + the new glob-expansion rule behave sanely against a genuinely oversized ledger → **L9**, a land obligation with a named purpose, blocking nothing in the diff.

**Land obligations — deltas on round 1:** L1 sharpened (merged `deterministic-gates.md` = 12,610; strip the five restated cap numerals from entry `:11` — the config owns them — keep every rationale; re-run the gate on the merged tree and quote the number) · L2 discharged in advance (both conflicts take-S7 lossless, re-verified post-fix) · L7 rides inside L1's byte budget · **L9 (new):** one read-only real-adopter inspection — do the seeder's day-one-over path and the glob-expansion rule behave sanely against a genuinely oversized, possibly-sharded ledger? Report measured sizes.

**Updated DoD.** reliability-resilience **MET** (three ordered branches; already-chained enumerated first; no authorship claim; shape-aware remedy with the destructive form refused by name; husky sibling reconciles; solo verified by a real commit) · docs-traceability **MET** · testing **GAP → R1-a only** (R3's pins now have teeth; R2/three-branch pins red-when-deleted; R6 closed under a hostile global; one vacuous pin remains, measured) · honesty **MET** (the reconciliation volunteers that no release ever shipped the refresh shape) · maintainability-structure **MET** · acceptance criteria **MET** (post-merge WARN is an explicitly scheduled land act, not an unmet AC) · no new debt **MET on land** (R1-a is a three-line test edit with a measured falsifier; L1/L9 scheduled and owned).

**Plain English.** The fix pass did what it was asked, and in two places did better. The hook-repair advice is now aimed at the hook people actually have: the synthesizer fetched the real v0.5.1 bytes and confirmed the harness now leaves that wrapper alone and prints a "here's how to move to the current one" message instead of the paste-this-line instruction that, last round, switched their existing check off — and the skill writes down *why* (the shape it can auto-refresh was never released). A solo user can now make their first commit. The test that proves the gate gets installed genuinely fails when the installing line is deleted. One thing didn't carry over: the new test guarding the exact line an adopter is told to paste searches the whole document — which also contains the hook template with that same line — so it passes no matter what the advice says. The repair is three lines (scope to the never-overwrite section), measured both ways. Two things wait for the merge, both known and scheduled: the merge pushes one decisions file ten bytes past its warning line (a trim of five duplicated numbers that file shouldn't restate anyway), and one read-only look at a real adopter repo, because the "your file is already too big on day one" path has never met a genuinely huge ledger.

**Applying R1-a closes this gate. Confirm the one mutant goes red, then land.**

##### L9 discharge (at land, 2026-08-15) — the real-adopter read-only inspection

Run against the locally reachable **AskBase** checkout (the 3-adopter scout's worst ledger case), read-only. Measured: `CLAUDE.md` **23,117 B** · `docs/claugentic-DECISIONS.md` **648,642 B** · `docs/claugentic-ROADMAP.md` **384,716 B** · `docs/claugentic-CHARTER.md` **202,467 B** · `docs/claugentic-decisions/` **ABSENT** (unsharded) · caps config **ABSENT**. Against the S7 seeder's rules: all four existing files are day-one-over → every one takes the `{"max": N, "reportOnly": true}` grace (loud WARN each commit, never a block — the designed snowball signal, and R5's glob-expansion rule has no matches to measure since the decisions dir is absent, exercising the zero-match skip); the config would be created fresh; nothing hard-blocks. **The day-one-over path behaves as designed against the worst real ledger.** (The R1-a falsifier and the four e2e scenarios are in the round-2 block above.)

### Slice 8 — Copy honesty pass

_Grounded 2026-08-15 by direct reads at `main` 3dcf073 (a four-site copy slice — no fan-out needed; the Verify panel is right-sized to the trust surface). All four sites re-read in the POST-S7 world: the truth conditions of the manifests' claim SHIFTED (two gates now chain where wired), so the corrections are grounded fresh, never reused from the plan-era wording._

- **In plain English (shown first at the approval gate):**
  - **What this builds:** the four PRE-EXISTING over-claims — the ones every earlier slice was told not to touch — get their honest form. (1) **README:3 headline** "never changes your code without your sign-off": true only on the full pipeline (the Approve gate); the lightweight path for small changes has no sign-off gate and the stop discipline is model-upheld → the headline scopes itself to substantial changes without losing its punch. (2) **README's "Mechanical (a real gate)" bullet** "blocks 'done' until every file is documented": it is a COMMIT-TIME hook, glob-scoped, wired only where init wired it — and post-S7 it is a PAIR (the codebase-map check + the doc-budget check chain in one wrapper, where wired) → rewritten in the presence-scoped register the rest of the repo now speaks, scan-safe. (3) **Both manifests' "deterministic architecture enforcement"** (plugin.json:4 · marketplace.json:13): unqualified "enforcement" implies always-on → "deterministic commit-time gates (codebase-map + doc-budget, wired at init)" register; both manifests move in ONE commit. (4) **/build frontmatter** "it stops before anything irreversible" stated as bare fact → gains the honest register ("a discipline the skill instructs — model-upheld, never a mechanical gate"), consistent with the body's existing "Honest about its limits" framing.
  - **What "done" means for you:** every front-door sentence a prospective adopter reads survives the harness's own honesty bar — nothing claims mechanics it doesn't have, and the one real pair of gates is described exactly as wired.
  - **What you're accepting:** the headline gets slightly longer to get honest (marketing punch preserved, scoped claim) · marketplace.json's description is the catalog text adopters see at install — rewording it is outward-facing copy (it ships at the next release, not before) · no new tests beyond the existing guards (the frontmatter-parses pin, `claude plugin validate --strict`, and the shipped-text denial/unreachability scans already police this class — adding a prose-pin per sentence would be change-detector testing).
- **Files & changes:** `README.md` (:3 headline · the ":57" Mechanical bullet — presence-scoped pair register, scan-safe wording: never pair a gate name with a caveat marker on one line) · `.claude-plugin/plugin.json` + `.claude-plugin/marketplace.json` (the "deterministic architecture enforcement" phrase in both, one commit; the "six live skills" count re-verified — still six) · `skills/build/SKILL.md` frontmatter (the stop claim's register; YAML must stay parseable — the v0.5.1 lesson) · `CHANGELOG.md` Unreleased entry · tree entries only if descriptions drift.
- **In-scope standards dimensions:** `honesty` (the whole slice — verb discipline, [D]/[J] integrity, claims match wiring) · `docs-traceability` (Reach-not-residence for the README's new gate copy — presence-scoped, reader-world paths).
- **Tests:** existing suites green (640/0 xfailed) · `tests/test_frontmatter_parses.py` green · `claude plugin validate --strict` green (frontmatter edited) · the shipped-text scans green (the new README wording must not trip the denial scan) · four gates zero-WARN · no new tests (rationale above).
- **Acceptance criteria:** full battery green both cwds · validate green · repo-wide grep finds zero remaining instances of the four over-claim phrasings in live copy (released CHANGELOG history exempt) · both manifests changed together · the Verify panel is **right-sized: honesty-reviewer + docs-traceability lens → synthesizer verdict** (three agents — the slice is pure trust-copy; the effort dial exists for exactly this).
- **Sizing:** 4 content files + CHANGELOG — the plan's smallest slice; one session, no split.
- **Pre-registered land obligations (from the S7 Stage-9 harvest, 2026-08-17 — ID'd per the new synthesizer-gate rule):** **S8-L1** — the `pre-merge-commit` gating-gap Bug entry lands in ROADMAP's Bugs section WITH its paired condensation (the harvest names the levers: the *Multi-charter* and *Restrict pushes to `release`* entries hold ~224 B; the merged file must stay under the 12,600 WARN, measured on the MERGE tree per the new Land rule). The durable rule already landed (Silent-disarm's merge-path shape + WORKFLOW's Land merge-tree bullet, harvest/0041-s7); S8-L1 is the WORK-ITEM filing. **S8-L2** — one-clause precision candidate flagged by the harvest: the swept "mechanically enforced … where wired" register is marginally over-scoped (merge commits are not hook-gated); a one-clause scope on the WORKFLOW/honesty-reviewer sentences ONLY (never CLAUDE.md — 36 B headroom), adjudicated by the S8 verify panel.

### Slice 8 — Verify verdict (Stage 7)

RUNNING AS: Fable 5 (`claude-fable-5`) — the session model, overriding the agent definition's opus pin (529 Overloaded at launch). Same family as the builder and both panel lenses: this is a clean-context role-independent pass, **not** a model-independent one — tag it a same-model run. (Both panel lenses ran the same override and state it in their reports.)

# Stage-7 verdict — plan 0041 Slice 8 (copy honesty pass) · `feat/0041-s8-copy-honesty` @ `2df0279` (base `0a5eea7`)

## VERDICT: CHANGES REQUIRED — four bounded edits (R2-a…R2-d); the next round is a **diff confirmation, not a re-gate**

## What I re-executed (scratch worktree at `2df0279`, removed after)

| Check | Result |
|---|---|
| Normalized wrap-tolerant scan, four over-claim classes, full branch tree | **[D]** C1/C2/C3 → ZERO in live copy (survivors: CHANGELOG past-tense + plan file, both exempt per spec). C4 survivors: the registered frontmatter `skills/build/SKILL.md:3` + the DECISIONS line quoting it. Matches both lenses. |
| Trigger-scan cores + shipped-content | **[D]** `scripts/check_shipped_content.py` exit 0, zero WARN; `tests/test_shipped_condensation_trigger.py` + `tests/test_frontmatter_parses.py` → 54 passed. |
| Both commit gates | **[D]** tree gate exit 0 · doc-budget gate exit 0, **zero WARN confirmed with stderr visible**. Note for the record: my briefing's "caps" (5,400 / 12,600 / 18,000) are the **90% WARN bands**; the repo's actual caps are 6,000 / 14,000-per-shard / 14,000 / 20,000. |
| YAML/JSON parse | **[D]** both manifests `json.tool`-clean, versions untouched at 0.5.1; build-SKILL frontmatter `yaml.safe_load`-clean with the register present in `description`. |
| Full pytest battery | **[D]** 640 passed in the worktree (one cwd; the both-cwds + 372-node claims re-run in round 2 anyway, see R2-c). |
| "rc accumulator" trace | **[D]** `.githooks/pre-commit` ends `rc=0; run_gate …tree… || rc=1; run_gate …budgets… || rc=1; exit $rc` — both gates always run; the README clause is true. |
| "wired by init" trace | **[D]** init SKILL step 5 writes the wrapper with **both** gates chained; gate-off writes no hook and no `core.hooksPath`. The manifests' phrasing is the honest attribution. |
| Merge-commit gap | **[D]** `.githooks/` contains **only** `pre-commit` — no `pre-merge-commit`; the commit a clean `git merge` creates is not gated, and `--no-verify` bypasses. The S8-L2 fact base holds. |
| Land-merge losslessness | **[D]** `git merge-tree --write-tree feat/0041-s8-copy-honesty harvest/0041-s7` → clean (tree `434a402`), rc 0; the two branches touch **disjoint** file sets. Merge-tree byte measures: CLAUDE.md 5,364 · deterministic-gates 12,506 · ROADMAP 12,452 · release-contract 12,449 · INVARIANTS 17,976 · honesty shard 4,001 — all at stated baselines, all zero-WARN. |
| Pin exposure of the round-2 edits | **[D]** no test pins "run on every commit"; the `engine/audit.js:125` pitch is pinned verbatim exactly once (`tests/workflows/audit.test.mjs:42`); `CAVEAT_MARKERS` is a fixed six-phrase set and the denial scan is gate-basename-gated — every prescribed edit below is scan-safe. |

## Required changes (fix-in-this-pass; round 2 = confirm these four diffs + re-run the named batteries)

- **R2-a — `README.md:57`: "run on every commit" → "run at commit time".** Both lenses found the same real, measured gap; they prescribed different fixes, and I adopt the honesty lens's minimal form **in my own name, with the measurement**: (1) the fact base is the unwired `pre-merge-commit` + `--no-verify`, confirmed above; (2) the manifests' "commit-time gates" was adjudicated honest this very round on the ground that *commit-time names when, not coverage* — R2-a puts the README bullet in the same register, one front-door voice; (3) the docs-traceability lens's explicit ~55 B merge caveat would create exactly the drift it warned about — one surface saying "every commit" beside another saying "every non-merge commit" — across the sibling quantifier sites (`skills/init/SKILL.md:597`, `:740`, `:998`, `docs/claugentic-WORKFLOW.md:173`, init wrapper spec ~`:499`) that S8-L2's pre-registered scope does **not** sweep. Removing the quantifier creates no drift: a non-quantified surface cannot contradict a scoped one. The explicit caveat lands where S8-L2 put it plus the S8-L1 Bug entry. Deletion-safe, no pin needed (removing a claim cannot over-claim).
- **R2-b — same bullet, honesty lens's nit, folded because the line is already open:** "the one-line bootstrap `init` prints" → "the one-line bootstrap `init` leaves in your CLAUDE.md". **[D]** init *writes* the check-first block into the committed CLAUDE.md fence; the teammate's reach-path is the file, not a transcript.
- **R2-c — `engine/audit.js:125` + its verbatim pin `tests/workflows/audit.test.mjs:42`: register the rendered pitch's irreversible claim.** Found by the docs lens via the reverse-absorption rule: the engine's rendered pitch ("…pausing only at the spec (before any code) and before anything irreversible") is the last unregistered **asserting** member of the class in adopter-facing copy — the new DECISIONS doctrine's "do not re-sweep" fence covers commitments/imperatives, not this. I **upgrade the lens's "follow-up note" to fix-in-this-pass, in my own name**: the round is reopening anyway, the edit is bounded to one string + its lockstep pin, and a copy-honesty slice leaving the loudest surviving member of its own class live is the auditability failure this plan exists to close. **Wording constraint the lens's draft misses:** in engine mode the pause *points* are mechanical (engine returns — `skills/build/SKILL.md:82`); only the *detection* of "irreversible" is model judgment — the register must credit the first while marking the second (ASCII-only, e.g. `(pause points the engine enforces; judging "irreversible" is model-upheld)`), because a blanket model-upheld register would under-claim the engine's real returns and this repo's bar cuts both ways. Update the pin in the same commit; re-run the node workflows suite + `check_shipped_content.py`.
- **R2-d — `README.md:71` under-inclusion, docs lens's fold-now, endorsed:** "Requires … Python 3 (for the codebase-map check; …)" credits one member of the pair line 57 just registered as two; the doc-budget check equally needs Python and equally skip-degrades **[D]**. One clause: "(for the two commit-time checks; without them the agent maintains the map by hand and the caps go unchecked)". Scan-safe, README uncapped.

**Round-2 battery:** pytest both cwds · node workflows suite · `claude plugin validate --strict` · `check_shipped_content.py` · both gates · the four-class scan. Nothing else may drift.

## Spec amendment (F1 — the finding refutes the spec, not the diff)

- **As spec'd** (site 4): register "consistent with the body's existing **'Honest about its limits' framing**" — implying a body section. **Corrected:** the phrase exists **only in the frontmatter** (**[D]** grep: `skills/build/SKILL.md:3`, nowhere in the body); the register was correctly attached at the frontmatter claim + the body's authoritative Guardrails guardrail, whose WORKFLOW anchor resolves (**[D]**). Both lenses adjudicated the implementer correct; recorded as **S8-L3**.

## Flag adjudications

- **F1 — implementer CORRECT** (spec amended, S8-L3 records it). **F2 — CORRECT, both lenses concur, I concur** — the commitment/imperative sites enact rather than assert; the class's one genuinely *asserting* stray is `engine/audit.js:125` → R2-c; the rest are D-1/D-2. **F3 — CORRECT** (no unapproved file; test-first audit conditional and not triggered). **The should-fix — upheld, resolved as R2-a.**

## Dispositions (numbered; discharge BY NAME at Land)

**Fix-in-this-pass:** R2-a · R2-b · R2-c · R2-d.

**Land obligations (owner: orchestrator):**
- **S8-L1** (pre-registered) — ROADMAP Bug entry for the `pre-merge-commit` gating gap, **with its paired condensation, measured on the MERGE tree** (merge-tree ROADMAP = 12,452 B, WARN 12,600 → 148 B headroom; the ~224 B condensation levers at *Multi-charter* / *Restrict-pushes* are **load-bearing, not optional** — execute first, then measure). The Bug entry carries **one clause** naming the sibling "every commit" quantifier sites (init SKILL:597/:740/:998 · WORKFLOW:173 · init wrapper spec ~:499) so the eventual hook-fix sweep is findable; budget its bytes in the same measurement.
- **S8-L2** (pre-registered, **adjudicated: LAND IT**) — the one-clause merge-commit scope on the WORKFLOW/honesty-reviewer "mechanically enforced … where wired" sentences **only**; never CLAUDE.md (**[D]** 36 B to WARN). Neither target is budget-managed. The README's inheritance is discharged by R2-a, not this.
- **S8-L3** (new) — one line in the plan's S8 section recording the F1 spec amendment.

**Explicitly dropped, with reasons:** **D-1** `PRODUCT.md:230-231` · `PRODUCT_SPEC.md:96` (design docs stating *intended* behavior — weakest assert-class, internal surfaces). **D-2** `PLAYBOOK.md:48` (commitment class, referent registered; sweeping would violate the slice's own DECISIONS fence). **D-3** `README.md:64` "9 specialist sub-agents" — **currently true [D]**; enumeration is the pitch's value; the count-restatement defect this slice fixed was in the *index*, where DRY forbids it. Known drift risk, accepted. **D-4** the bullet's benign understatements (the omitted glob-drift check; the `reportOnly` grace) — understatement is not over-claim.

## Definition of Done

| Gate | Status |
|---|---|
| AC: four-class grep zero in live copy | **Met [D]** (re-executed, wrap-tolerant) |
| AC: both manifests one commit, JSON valid, versions untouched | **Met [D]** |
| AC: battery green | **Met [D]** as re-executed; node + validate re-confirmed in round 2 |
| AC: right-sized panel | **Met** |
| Honesty | **Met after R2-a/R2-b** — everything else traced true against the wiring |
| Docs-traceability | **Met after R2-c/R2-d + S8-L3** — deictics, reach facts, tree-locator fix, DECISIONS filing, CHANGELOG register all verified |
| No new debt | **Met once R2-a…d land** — every defer carries an ID |
| Land-merge with `harvest/0041-s7` | **Clean [D]** — conflict-free, disjoint files, all ledgers at baseline zero-WARN on the merge tree |

## Plain English

This slice is a large net honesty gain: three of the four rewritten front-door sentences now say exactly what the harness does — I traced every clause to the hook, the two gate scripts, and init's wiring myself, and they hold. Two things must change before it merges. First, the new README bullet promises the gates run on *every* commit, but a merge commit git creates on its own skips the hook entirely — a reader could trust a merged tree the gates never saw; deleting the quantifier ("run at commit time") fixes it without burying the front door in caveats, and the detailed gap gets its permanent homes at land. Second, the audit skill's closing pitch — rendered to real adopters — still asserts the pipeline "pauses before anything irreversible" with no register, the exact sentence-shape this slice exists to retire; one string plus its test pin fixes it. Two more one-clause touch-ups ride along in the already-open file. Worst case if shipped as-is: an adopter extends slightly more trust to merges and to unwatched pauses than the machinery warrants — small, but this slice's entire product is trust.

**Bounded next round:** confirm the four R2 diffs line-by-line, re-run the named battery, verify nothing else moved — then PASS with S8-L1/S8-L2/S8-L3 discharged by name at Land. No third gate round.


#### Round 2 (diff confirmation) — `2df0279..64ae022`

RUNNING AS: Fable 5 (`claude-fable-5`) — the session model, overriding the agent definition's opus pin (529 Overloaded); same family as the builder and round-1 lenses — a clean-context pass, not a model-independent one.

**VERDICT: PASS — no residual, no further gate round.**

**What I re-executed (fresh scratch worktree at `64ae022`, removed after).** The four R2 diffs line-by-line — **all exact [D]** (R2-a "run at commit time", quantifier gone repo-wide · R2-b "leaves in your CLAUDE.md" · R2-c the pitch tail's register, byte-identical in source and pin, both files `str.isascii()` True, crediting the mechanical returns while marking the detection as judgment · R2-d the two-check Python requirement with the map's specific maintainer still named). **Nothing else drifted [D]:** `git diff 2df0279..64ae022 --stat` = exactly 3 files, 4 insertions / 4 deletions. **Four-class wrap-tolerant scan [D]:** C1/C2/C3 zero in live copy; C4 survivors exactly the adjudicated set (the registered frontmatter + the DECISIONS quote); the C4b asserting class gained no new member and `engine/audit.js`'s sole "anything irreversible" now carries its register (verified by grep, not report). **Node workflows suite [D]:** 372 pass / 0 fail — the updated pin holds. **Gates + scans [D]:** shipped-content OK (the new js string passed the shipped ASCII scan) · tree gate OK · doc-budget gate OK, zero WARN, stderr visible · trigger + frontmatter suites 54 passed. **Land-merge re-check at the new tip [D]:** `git merge-tree --write-tree 64ae022 harvest/0041-s7` clean, rc 0 — disjoint files, losslessness holds.

**Updated DoD:** all acceptance criteria **met [D]** · honesty and docs-traceability **met in full** (R2-a…d closed the only gaps) · **no new debt** · every deferral ID'd — **S8-L1 / S8-L2 / S8-L3 discharge at Land, by name** (S8-L1's merge-tree byte bound re-affirmed unchanged at the new tip).

**Plain English.** All four prescribed fixes landed exactly as written and nothing else moved — each line confirmed in the diff, every scan and suite re-run in a clean checkout of the new commit, and the branch still merges losslessly with the harvest branch. The front door now claims precisely what the machinery does: the gates run at commit time where init wired them, the bootstrap lives where a teammate can actually find it, the Python requirement names both checks, and the engine's own pitch tells adopters which pauses are mechanical and which are the model's judgment. This slice is done; the three named land obligations are the orchestrator's to discharge at merge.

##### S8-L3 discharge (at land) — the F1 spec amendment, recorded

The spec's site-4 instruction referenced "the body's 'Honest about its limits' section" — **no such body section exists**; the phrase lives only in the frontmatter (`skills/build/SKILL.md:3`). The register was correctly attached at the frontmatter claim + the body's authoritative Guardrails guardrail (whose WORKFLOW → *Decision-gated autonomy* anchor resolves). Adjudicated correct by both lenses and the synthesizer; recorded here so the spec's next reader doesn't hunt for a section that never existed.

##### S8-L1/S8-L2 discharge (at land)

**S8-L1:** the `pre-merge-commit` gating-gap Bug entry landed in ROADMAP's Bugs section (with the sibling "every commit" quantifier sites named for the eventual fix sweep), paired with THREE condensation levers (Multi-charter · Restrict-pushes · the S2 follow-ons batch — the harvester's two levers under-shed by ~100 B, measured, so the synthesizer-named fifth lever was applied): **ROADMAP = 12,491 B, 109 B under the 12,600 WARN, gate zero-WARN** — measured on the LAND BRANCH after both merges, per the Land merge-tree rule. **S8-L2:** the one-clause merge-scope landed on WORKFLOW gate 4 ("on the commits the `pre-commit` hook sees; a conflict-free `git merge` commits via the unwired `pre-merge-commit` — a tracked Bug") and honesty-reviewer:14 ("and only on the commits that hook sees") — CLAUDE.md untouched; guard scans green (91 passed).

### Slice 9 — Adopter loop closure (fix-pointers)

_Grounded 2026-08-17 by a 4-reader fan-out at `main` 284e0e0 (journal `wf_974393f6`): the full nine-file `.claude/agents/` pointer sweep (not just the three named suspects) · the WORKFLOW surface · ship mechanics + upstream-path absence · adopter-side homes + repo-type precedent. Plan-era anchors had drifted again — **WORKFLOW is 92,327 B at 284e0e0 and 93,271 B after the S8 harvest** (83,383 at Stage 3; the S12 density-work figure re-derives at S12's own spec — its ≤ 56,000 B AC stands); the adopter note sits at `:154-159`; honesty-reviewer's suspect line is still `:14` post-S8-L2. All anchors below are current at 284e0e0 AND hold at `harvest/0041-s8` @ `7b3acf0` — the harvest's two WORKFLOW edits are intra-line (no line shift) and its honesty-reviewer insertions land below `:14`._

- **In plain English (shown first at the approval gate):**
  - **What this builds:** the Stage-9 learning loop stops being a dead end in adopter repos. Today the shipped WORKFLOW §9 (`:317-322`) and `retrospect-harvester` (`:16-21`) tell every repo to "promote to STANDARDS / CLAUDE.md", "fold into the `.claude/agents/` role file", and "edit this `WORKFLOW.md`" — but in an adopter there IS no `.claude/agents/` (agents are plugin-resident, measured: init creates none, the plugin manifest resolves all nine from the install), the standards catalog and WORKFLOW are managed copies re-`init` clobbers, and the one "promote upstream" sentence (`:328`) names **no channel** — a full shipped-set grep finds no URL, no issue/PR path, nothing (the repo URL lives only in the two manifests' `repository` fields). Four moves: **(1)** the harvest destinations **branch by repo type** in both homes (WORKFLOW §9 + the harvester), in the file's own established register (the `:172`/`:188` "harness-self … adopter" shape; doctor already branches a *destination* this way at its roadmap-routing rule) — adopter branch: universal lesson → stage in `docs/claugentic-standards/CANDIDATES.md` (the born-on-first-use local buffer the standards README already defines) + the upstream path; repo-specific lesson → the adopter-owned homes (CLAUDE.md harness area · DECISIONS · ROADMAP · INVARIANTS — already valid today). **(2)** `honesty-reviewer` and `product-designer` carry their premise **inline**: their "CLAUDE.md → Honesty positioning" read-first pointers dangle in an adopter AND in this repo (no such heading exists anywhere — the real home is the DECISIONS index → honesty shard, and the adopter DECISIONS seed is test-pinned to carry no honesty positioning); the fix keeps each agent's already-inline premise text and deletes the dead pointer wrapper, de-deicticizing "this repo's #1 risk" while keeping the register. **(3)** the upstream contribution channel is named **once** — WORKFLOW `:328`, the canonical learning-loop home — as issue-or-PR against `https://github.com/sh4npeiris/claugentic-dev-harness` (the manifests' `repository` URL); the other promotion mentions (standards README `:25`/`:27` · the harvester's adopter branch) stay pointers to it, never copies. **(4)** the adopter note (`:154-159`, 3,515 B) moves **verbatim** to the intro — inserted after the "New here?" blockquote whose `:7`/`:9` lines are exactly what it corrects — gaining ONE clause: growing the harness (new agents · standards edits · workflow changes) happens upstream in the plugin, not by editing your managed copies (this clause is what covers `:37`/`:71`/`:150` without per-line edits).
  - **What "done" means for you:** an adopter running a Stage-9 harvest gets destinations that exist in their repo, a named path for lessons that belong upstream, and two reviewer agents whose premises don't point at text their repo can't have; the first thing an adopter reads in WORKFLOW now tells them how references resolve, instead of finding out at line 154.
  - **What you're accepting:** WORKFLOW grows a bounded net ≤ ~1.2 KB (branch clauses + channel sentence + one note clause — every byte added is a byte S12 must condense, so the cap is part of the AC) · the GitHub URL is written into shipped prose (the manifests stay authoritative; a repo move now touches three sites — accepted, an indirection would be useless to the human reader) · same-class minor fixes ride along because the slice IS the pointer-integrity sweep: `synthesizer-gate:27` ("new STANDARD, agent" gains the adopter branch clause) · `yagni-sentinel:18` (bare `claugentic-ROADMAP.md` → `docs/claugentic-ROADMAP.md`) · doctor's harvest-skipped surface list gains `CANDIDATES.md` (an adopter harvest staging there must not read as "skipped") · honesty-reviewer `:14`'s "a tracked Bug" deictic resolves to the reader's frame ("tracked in the plugin repo") — and its WORKFLOW gate-4 twin from S8-L2 gets the same one-word resolution, `[J]` panel-adjudicated.
- **Files & changes:** `docs/claugentic-WORKFLOW.md` (§9 destination lines `:317`/`:319`/`:320` gain the repo-type branch · `:326` reframed to speak to both audiences · `:328` gains the channel · the adopter-note block `:154-159` relocates to the intro between the blockquote and the purpose paragraph, + one new clause; **content-preserving — the diff must show relocation, not rewrite**) · `.claude/agents/retrospect-harvester.md` (`:3`/`:8` harness-self framing → both-repo framing · the destination rows `:16-21` + escape hatch `:23` branch, pointing at WORKFLOW §9 as canonical rather than restating) · `.claude/agents/honesty-reviewer.md` (`:8` de-deicticized · `:14` pointer wrapper deleted, inline premise kept, "tracked Bug" resolved) · `.claude/agents/product-designer.md` (`:47` dangling pointer dropped · `:98` header premise inlined; `:100-104` already inline — untouched) · `.claude/agents/synthesizer-gate.md` (`:27` one clause) · `.claude/agents/yagni-sentinel.md` (`:18` path) · `skills/doctor/SKILL.md` (`:301-312` surface list + CANDIDATES.md) · `docs/claugentic-standards/README.md` (`:25` "edit the plugin" gains the pointer to the channel) · one dated DECISIONS line (plugin-distribution shard beside its `:19` adopter-aware entry — **wc -c before/after**, per-shard cap 14,000) · `CHANGELOG.md` Unreleased · tree descriptions if drifted.
- **In-scope standards dimensions:** `docs-traceability` (the slice IS its Reach-not-residence + deictics dimensions — the motivating incident at its `:83` is this exact failure class) · `honesty` (premise/claims register on the two reviewer agents; the embedded bar).
- **Tests:** existing suites green (640 + 372 node, 0 xfailed) · `claude plugin validate --strict` (agent frontmatter untouched but agents edited — validate anyway) · `check_shipped_content.py` green (the edits *reduce* its uncaveated-harness-self class, never add) · `tests/test_seed_templates.py:63-73` stays green (the DECISIONS seed still carries no honesty positioning — the fix direction is delete-the-pointer, not add-the-section) · both gates zero-WARN. **New pins (red-first):** (1) no `.claude/agents/*.md` carries a `CLAUDE.md`-anchored "Honesty positioning" pointer form (the dangling-pointer regression; premise-prose mentions stay legal) · (2) WORKFLOW's adopter-note heading appears **before** the first stage heading (`## 0.`) · (3) the shipped WORKFLOW names the plugin repo URL (guards the channel against the S12 condensation dropping it).
- **Acceptance criteria:** full battery green both cwds · the repo-type branch present in BOTH §9 homes in the in-file precedent register, zero unbranched broken-destination lines remaining at the named sites · the channel named exactly once with pointers elsewhere (grep-verifiable: one URL occurrence in WORKFLOW, none added in other prose) · adopter-note relocation byte-preserving apart from the one named clause · WORKFLOW net growth ≤ ~1.2 KB measured (`wc -c` before/after in the implementer's report) · Verify panel **right-sized: honesty-reviewer + docs-traceability lens → synthesizer** (three agents — trust-copy slice, same shape as S8; panel reads standards from `harvest/0041-s8`; note honesty-reviewer reviews a diff that edits its own definition — the panel spawns from the pre-edit definition on main, no action needed).
- **Sizing:** 8 content files + CHANGELOG + a DECISIONS line + 1-2 test files — comparable to S8. One session, no split.
- **Build base (shared-surface rule):** the implementer branches from **`harvest/0041-s8` @ `7b3acf0`**, not main — the harvest's home-selection sentence lands inside the same `:326` paragraph S9 reframes, and its 529-override rule inside the Model-tiers line. Basing on the post-harvest text removes the land-merge conflict; the land branch then merges feat alone (harvest already in its history). The harvest also strengthens S9's own frame: WORKFLOW `:326` now carries the "pick the HOME by ship-class" test the destination branches must respect.
- **Pre-registered land obligations (from the S8 Stage-9 harvest, 2026-08-17):** **S9-L1 — ROADMAP condensation posture.** Three consecutive lands have paid a micro-shave tax to fit one entry (S8-L1 burned three levers; the S8 harvest burned four more for +81 B — ROADMAP now 12,568 B, 32 B under WARN) and the harvester states the shave levers are exhausted. If S9's land or verdict routings need ROADMAP bytes, the discharge is a **genuine condensation pass (the `/condense` procedure — user approves the diff) or the sharding-ladder decision** (ROADMAP `:44` tracks the INVARIANTS twin) — a fifth micro-shave is not an acceptable discharge; if nothing needs bytes, discharge by stating that. **S9-L2 — merge-tree measurement, thinnest-yet edition.** ROADMAP's 32 B and INVARIANTS' 24 B headroom leave no room for a wrong-ref measurement: every ledger number at land is measured on the MERGE tree (`git merge-tree --write-tree` + materialize + the real gate), per the standing Land rule — named here because the margin is the smallest the plan has carried.

### Slice 9 — Verify verdict (Stage 7)

RUNNING AS: Opus 5 (`claude-opus-5[1m]`) — both panel lenses and the synthesizer ran the opus tier (the S8 529s did not recur; no model override needed this slice). Same family as the builder, which ran the session model: a clean-context **role**-independent gate, **not** model-independent and not independent verification.

Panel right-sized per the spec: `honesty-reviewer` + a `docs-traceability` lens → synthesizer. Round 1 **CHANGES REQUIRED** (R1–R12 + amendments A1/A2/A3); round 2 **conditional PASS** on **R13**, applied as written — gate closed, no round 3.

**Round 1 — the four moves are sound; twelve sentence-level prescriptions.** The synthesizer applied the whole set in a byte-faithful scratch tree before prescribing it, and **re-measured every lens estimate — all three were low** (docs-F3 +18→+29 · honesty-F4 +0→+45 · docs-F4 ~+48→+61). Applying the panel's arithmetic on trust would have landed +1,222 B against a user-approved ~1.2 KB AC. Ruling: no cap raise, no paired trim; R2 uses the slice's own coined `plugin-side` register to kill the deictic at +7 B. Headline finds: the relocated note's own "**below**" excluded the two references the relocation existed to correct (A1) · a new `here` deictic in a managed copy · **four sites asserting "an adopter has no `.claude/agents/` dir"** — a Claude Code project directory any repo may have, and `/doctor` was told to discount it, so a genuine adopter harvest into their own project agent still read as *"harvest likely skipped"* · the newly-canonical inline premise dropped the **deterministic run-gate** class `honesty.md:5` names · pin 3's docstring claimed one-canonical-home while scanning WORKFLOW alone.

**Two lens prescriptions overruled, in the synthesizer's own name.** **docs-F8 REFUTED BY EXECUTION** — its proposed anchor (first plugin-resident path) turns pin 2 RED on HEAD, because the greeting blockquote names `.claude/agents/` above the note; the *finding* was right, the *fix* was never run. **docs-F4 DROPPED on DRY grounds, not budget** — CANDIDATES.md's born-on-first-use fact is a write-destination whose canonical home is the standards README; copying it into `WORKFLOW:329` would breach the one-canonical-home rule stated two lines above it.

**Round 2 — R13, the orchestrator's own defect, caught by the gate.** The orchestrator's R7 implementation enumerated the URL corpus with `Path.rglob`, which walks **on-disk** material: this repo parks agent worktrees under the gitignored `.claude/worktrees/` **inside the main checkout**, each holding its own `WORKFLOW.md`. Measured: main checkout `rglob` = **129 `.md`, 61 worktree leaks**; `git ls-files` = **60, zero**. The pin was green in a worktree and green in CI (fresh clone) and **RED in the maintainer's checkout — where Land runs**. Worse, the orchestrator's own non-vacuity validation ran *in the worktree*, the one shape where the defect is invisible, so it proved nothing about the failing case. R13 swaps in the `git ls-files` convention the repo's three other corpus scanners already use, with `check=True` for the same fail-loud posture and a **do-not-revert-to-rglob** comment as the pin (neither CI nor a worktree would catch the regression).

**Measured at the gate:** WORKFLOW **93,271 → 94,471 = exactly +1,200 B** · `workflow-process` 7,356 · `plugin-distribution` 12,524 (76 B to WARN). Battery at `c8eb304`: 653 passed · node 372/0 · shipped-content 53 files · tree · budgets zero-WARN · version-sync · `validate --strict`.

#### Spec amendments (S9-L3 — the findings refute the SPEC, not the diff)

- **A1 — the relocation constraints were jointly unsatisfiable.** The spec demanded the adopter note move **"verbatim"** AND be **"inserted after the 'New here?' blockquote whose `:7`/`:9` lines are exactly what it corrects."** Together those necessarily strand the note's own word "below" so it excludes those very two lines. The diff was faithful; **the spec line was wrong.** Resolved: the placement stands (a note above *"New here? Read this first"* is self-defeating — the greeting is deliberately first) and a **second, one-word deviation from byte-identity is authorized** (R1: "below" → "in this doc").
- **A2 — the spec's "no such heading exists anywhere" is false.** `docs/claugentic-decisions/honesty.md:1` is titled verbatim *"Honesty positioning (the #1 rule)"*. The correct scope is *no such heading in **either repo's `CLAUDE.md`***, which is what the diff and the pin actually say. The fix direction (delete-the-pointer, never add-the-section) is unaffected.
- **A3 — the spec's DECISIONS filing instruction was wrong; the implementer's deviation was right.** Spec'd: file the dated line on `plugin-distribution.md` beside its `:19` entry. Measured by both lenses and the synthesizer independently: that shard was 12,311 B, the line is 837 B, so the spec's filing lands **13,148 B ≈ 93.9% — tripping the 12,600 WARN**, whose only S9-L1-legal discharge is a condensation pass. The ledger index (`docs/claugentic-DECISIONS.md:5`) mandates filing into the *fitting* shard and the Stage-9 sibling already lives at `workflow-process.md:11`. Upheld: the loop-closure/channel decision on `workflow-process`, the role-files-are-adopter-aware half on `plugin-distribution`.

##### S9-L1…S9-L5 discharge (at land, 2026-08-17)

- **S9-L2 — DISCHARGED.** `git merge-tree --write-tree main feat/0041-s9-loop-closure` materialized and the **real** doc-budget gate run on it: **exit 0, zero WARN**. Merged margins to WARN: INVARIANTS 17,976 (**+24**) · ROADMAP 12,568 (**+32**) · CLAUDE.md 5,364 (**+36**) · plugin-distribution 12,524 (+76) · deterministic-gates 12,506 (+94) · release-contract 12,449 (+151) · DECISIONS index 2,630 (+520); all other shards ≤56% of cap. R6's and R12's shard moves are absorbed. Valid at `main = 930aee3` (merge-base *is* the tip, so the merge is a fast-forward today) — re-measure if main advances before the PR merges.
- **S9-L3 — DISCHARGED.** Amendments A1/A2/A3 recorded above, per the S8-L3 precedent.
- **S9-L1 — DISCHARGED (2026-08-17, user-authorized condensation, `/condense` procedure).** Not a fifth micro-shave: a real pass, classify-first. **ROADMAP 12,568 → 11,776 B = 84.1% of cap, 824 B under WARN** (was 32 B — 26x the headroom), *including* the two new entries below. This ledger holds **no `00NN — LANDED` build-records** (it is a forward backlog), so STEP 2's usual primary target was absent and the absorbable bulk was duplicative sibling prose plus re-derivable locators inside live entries. Merges: 4 gate-items → 1 · 3 `0041` verify-deferred batches → 1 · **the fence contract, which the preamble stated three times (`:5`, `:7`, `:29`) plus twice more in the section headings → one block** (the single largest win, ~850 B) · INVARIANTS-sharding + the unowned `deterministic-gates` cap restatement → one *Ledger hygiene* line. Every `00NN` anchor was grepped first and every one is referenced outside ROADMAP, so all anchors were kept and only the bodies condensed. Accepted at 84.1% per the skill's own accept test — every absorbable record is absorbed and the remainder is genuinely all-live deferred work; cutting further would cut live force to chase a number.
- **S9-L4 — DISCHARGED.** Filed as *"Shipped pointers that don't resolve in an adopter (0041 S9 residual)"* — both classes named (8 role-file locators in shipped skills/README; the bare-ledger-filename class at ~50–75 sites, pattern-dependent) with the instruction to **re-derive by grep before fixing, never work from a stale site list**, and the note that neither is claimed swept anywhere, so no copy over-claims.
- **S9-L5 — DISCHARGED.** Filed as *"Repo-wide mechanical-vs-run-gate register sweep (0041 S9)"* — `CLAUDE.md:3` collapses *hook-enforced* into *mechanically enforced*, dropping the deterministic run-gate class that the honesty shard names and that R3 made canonical in `honesty-reviewer`, so the always-loaded anchor under-describes its own wiring. (CLAUDE.md sits at 5,364 B / 36 B to WARN, so that sweep is not free — noted in the entry's home.)
- **Not fixed, deliberately — the generated fence.** `docs/claugentic-ROADMAP.md`'s audit fence still reads *"No open items — the harness is feature-complete for v1"*, which is false while this plan has three unbuilt slices. It sits **inside** `<!-- harness-audit:backlog -->`, and the fence contract is that a re-run overwrites hand edits — so the honest repair is re-running `/claugentic-dev-harness:audit`, not editing it here. Harness-self only (ROADMAP is in the STRIP set), so it is not adopter-facing.

### Slice 10 — Engine fixes  ·  **SPLIT APPROVED (user, 2026-08-17): 10a / 10b**

**S10-L3 resolved at approval, as the spec required — the D6 constraint does NOT go to INVARIANTS.** The candidate rule (*"a namespace retry must never touch the respawn counter or set `forcedSameModel`"*) is **mechanically pinnable**, and INVARIANTS is explicitly *"user-owned documentation, not a gate… nothing mechanically verifies the invariants hold"* — its value is for a constraint **no gate can catch**. This one a test catches. So: a red-first pin plus a do-not-revert comment at the call site is the stronger guard **and** costs none of the 24 B. Escalation is bounded and stated: **only if the 10b panel judges the constraint real AND not coverable by a pin** does it become a land obligation, and then the discharge is the sharding-ladder decision, never an append. This is the *"is each entry genuinely load-bearing… not a restatement"* test in `docs-traceability` applied honestly rather than reflexively.

**Sequencing (not parallel — measured):** 10a and 10b cannot run concurrently. D6's fallback must land **byte-identically in all four engine files** (`cross-script.test.mjs` pins that), so 10b touches `audit.js`, `build-item.js` and `qa.js` — the same three files 10a edits. **10a lands first; 10b bases on it.**

_Grounded 2026-08-17 by a 3-reader fan-out at `main` `21d3ca8` (journal `wf_84f7a9b6`). One reader read at the stale ref `b5efe48`; **verified harmless — `git diff b5efe48 HEAD -- engine/ tests/workflows/` is EMPTY**, so every anchor below holds. Anchors were re-derived from code, not carried from the plan; all six still resolve._

**The structural fact that shapes this whole slice.** `tests/workflows/_load-helpers.mjs` extracts a line-anchored `// --- helpers ---` … `// --- end helpers ---` block and evals it via `new Function`, so **only that block is testable, and only if it closes over no tool primitive** (`agent`/`parallel`/`phase`/`log`/`workflow`). Blocks: `audit.js` 30–1092 (of 1430) · `build-item.js` 58–624 (of 961) · `qa.js` 42–744 · `verify.js` 22–444. **Five of the six defects sit BELOW their block.** There is no integration runner anywhere in `tests/`. So "add a regression pin" is not a free acceptance criterion — for each defect the spec must name the technique, or the pin silently doesn't happen. That is exactly why `audit.js`'s existing `guardedAgent` (`:1207`) has stood **uncovered**.

- **In plain English (shown first at the approval gate):**
  - **What this builds:** the six known engine defects, fixed with real pins. **D1** (`audit.js:1137`) — the `renderOnly` fast-path tests `args.renderOnly` on the **raw** args, but a `scriptPath` invocation delivers args as a **JSON string** (the only shape `/audit` uses), so the branch never fires: either the documented call throws `audit args invalid` (the SELECT seam is dead on the real path) or, if the caller re-passes the original args, the engine **silently re-runs a full second audit** with the whole agent fan-out. Fix = move the check after `parseArgs`, still before `validateArgs` (which it bypasses by design). **D2** (`build-item.js:472-475`) — `crossModelClaim` filters null same-model signals and over-reports "confirmed". **D3** (`qa.js:815-818`) — the driver prompt emits its narrowing STATES-SCOPE constraint *before* the role framing and glues them with **no separator** (`…entry.Runtime QA —`); this line has been patched twice already and the ordering survived both. Same defect family: `artifactBase` is **dead code** (0 call sites) while four sites re-implement it *inconsistently*, and its `/`-only trim leaves a trailing backslash on Windows-style paths. **D5** (`build-item.js:790-792`) — silent defaults. **D4** (`verify.js:484-523`) — the panel fan-out is unguarded: one thrown lens rejects the whole `parallel()`, and `dimensions` is neither de-duplicated nor capped, so a caller can fan out 200 agents over one module. **D6** — `nsAgent()` hard-namespaces every spawn; in a project-local dogfood session nothing resolves, which is what **aborted an eval run** and forced the published v0.5.0 baseline to be measured through a **source-shimmed engine** — so the repo's own baseline does not measure the shipped agent-resolution path.
  - **What "done" means for you:** the engine stops carrying six known-and-unfixed defects into every adopter's `/audit`, `/build` and QA run; the SELECT seam actually works; a single failed panel spawn degrades to one honest coverage gap instead of killing the round; and the next eval baseline can measure the real shipped path.
  - **What you're accepting:** **D4 and D6 share one seam** (both must live in the spawn wrappers below the helpers block) and D6's only implementable form — try-namespaced, retry-bare-on-spawn-failure — sits **directly on the repo's most protected trust surface**, `spawnJudge`'s one-respawn contract and the `forcedSameModel` flag that feeds the same-model disclosure. A careless retry silently corrupts that disclosure. Hence the split below · three engine files gain a helper extraction purely to make a fix pinnable (behavior-preserving, but it moves code) · `docs/claugentic-decisions/plugin-distribution.md:9` states the one-way namespacing rule as settled doctrine and **must be amended in the same change as D6**, or it becomes a stale over-claim the moment the fallback ships.
- **Sizing — SPLIT, and the split is by SEAM and RISK, not by count.**
  - **Slice 10a — the four bounded defects (D1 · D2 · D3 · D5).** Independent of each other, no shared seam, no trust-surface interaction. ~6 files.
  - **Slice 10b — the shared-seam pair (D4 + D6) + the copy they falsify.** Both fixes live in the same spawn wrappers; D6's failure mode *is* D4's trigger (an unresolvable agent id throws in the unguarded fan-out), so fixing D4 alone converts a crash into a total-coverage-gap `CHANGES_REQUIRED` rather than a working run — they are one incident seen from two ends. ~7 files.
  - **10b is the riskiest work in the entire plan** — it is the only remaining change that can silently corrupt a trust claim. It gets its own Verify panel including `honesty-reviewer`.
- **Files & changes (10a):** `engine/audit.js` (D1 reorder + extract the branch decision into a pure helper inside the block so it can be pinned) · `engine/build-item.js` (D2 in-block fix; D5 + a pure-helper extraction) · `engine/qa.js` (D3: **relocate `driverPrompt` above line 744** — it is already pure, takes only `runConfig`/`criterion`/`runLabel`/`artifactDir`, calls only `sanitizeForPath` + a const, and closes over no primitive, so the move is behavior-free and makes it pinnable; fix the ordering + the missing `\n\n`; route all five artifact-dir sites through the dead-but-tested `artifactBase` and widen its trim to `/[\\/]+$/`) · `tests/workflows/{audit,build-item,qa}.test.mjs` · `CHANGELOG.md`.
- **Files & changes (10b):** `engine/verify.js` (D4 guard as a **transparent per-thunk wrapper returning `null`**; dedupe `dimensions`; correct the `:12` header comment *"Call count is structurally bounded by the roster — no loops"*, which is false — there is a `.map()` and the roster is caller-sized) · `engine/{audit,qa,build-item}.js` (the D6 fallback in their spawn wrappers, byte-identical) · `tests/workflows/{verify,cross-script,agent-namespace}.test.mjs` · `docs/claugentic-decisions/plugin-distribution.md` (amend `:9`; **`wc -c` before/after — 12,524 B, 76 B to WARN**) · `CHANGELOG.md`.
- **THREE test-pin traps the grounding found — the spec names them so the implementer cannot walk into them:**
  1. **Rewriting `nsAgent` as a multi-statement arrow silently guts the drift pin.** `cross-script.test.mjs`'s extractor matches `^const <name> =[ \n][\s\S]*?;$` — **non-greedy to the first line ending in `;`**. A multi-line arrow body extracts only its first line, so the byte-identity test across all four engine files **still passes while pinning almost nothing**. Keep `nsAgent` a single-expression `const`, or convert it to `function nsAgent(...)` (the `^function` branch matches to the closing brace and is safe). Not hypothetical — it is live.
  2. **`agent-namespace.test.mjs` forbids the naive D6 fix by construction.** It strips `nsAgent("<name>")` then asserts no bare `"<name>"` survives, over a 9-name list. So the fallback name must be **DERIVED at runtime** (strip the `claugentic-dev-harness:` prefix from the namespaced id) and must **never appear as a bare double-quoted literal** in engine source. A `FALLBACK_AGENTS = [...]` array turns the suite red.
  3. **`agentType` must stay a plain string.** `verify.test.mjs:412` asserts `honesty.agentType === "claugentic-dev-harness:honesty-reviewer"`. Returning a candidate array breaks both the pin and the tool contract. Note two call sites pass the id **positionally** into `spawnJudge` (`verify.js:513`, `:566`), so an `agentType:`-keyed grep is insufficient — the existing test says so explicitly.
- **Hard constraints carried in (all re-verified):** **ASCII-only** in `engine/*.js` — `check_shipped_content.py` `scan_non_ascii_js` (`:375`, wired Pass C `:547`), **hard exit 1**; all four files are 0 non-ASCII bytes today. The repo's habitual `—`, `→`, `·` and smart quotes would fail it: use `--` and `->`. · **The cell-key delimiter INVARIANT** (`INVARIANTS.md:162-175`): `|` is name-safe and round-trips through the **persisted resume contract**; changing it requires producer + parser + serializer + the SKILL resume re-parse + fixtures **atomically**. The status-line `" - "` separator is load-bearing for the same reason — it is the resume contract, not display. **10a/10b touch neither; the spec forbids reshaping `doneCells`/`pendingCells` as a convenience.** · **Sandbox:** no imports, no filesystem, no `Date.now()`/`Math.random()` — so D6 **cannot detect which resolution world it is in**; only try-then-retry is implementable, and the guard must be plain `try`/`catch` + `log()`.
- **The single sharpest break risk (10b), stated so the panel can check it:** `splitPanelResults` slices `parallel()` results **by index** (`slice(0, lensCount)`, `[lensCount]`, `[lensCount+1]`). The D4 guard must preserve **position and arity** — any filtering, chunking or settled-results reshape misaligns `lensReturns` ↔ `modulesAudited` and corrupts `coverageGaps`'s parallel-array assumption. And `spawnJudge` must **not** be double-wrapped: it already owns a one-respawn contract whose two-failure throw is pinned (*"failed twice — never a silent partial PASS"*), and blanket-guarding `panelTasks` would swallow it. **A D6 namespace-retry is NOT a model respawn and must not touch the respawn counter or set `forcedSameModel`** — that flag feeds the same-model disclosure.
- **In-scope standards dimensions:** `reliability-resilience` (D4 degradation, D6 retry) · `maintainability-structure` (the DRY collapse of five artifact-dir copies onto `artifactBase`; the helper extractions) · `testing` (every fix pinned, and the three traps) · `honesty` (**10b only** — the `verify.js:12` over-claim, the `plugin-distribution:9` amendment, and the same-model disclosure).
- **Tests:** existing suites stay green (**653 pytest · 372 node**, 0 xfailed). Every defect gets a **red-first** pin, and the spec names the technique per defect rather than leaving it to the implementer: D1/D5 → extract the decision into a pure in-block helper, then pin it · D2 → pinnable in place today · D3 → pin `driverPrompt` after relocating it, plus the existing `artifactBase`/`screenshotPath` tests must stay green while five sites are routed through them (note `:959` currently applies **no trim** and `:1188` applies **no default** — verify before consolidating, do not assume equivalence) · D4 → extract a pure `guardFailure(err, label) -> null` into the block and unit-test it, plus a source-level pin on the wrapper (the `agent-namespace.test.mjs` precedent) · D6 → source-level pins for derivation-not-literal, plus the drift-pin trap check.
- **Acceptance criteria:** full battery green both cwds · `node --test tests/workflows/*.test.mjs` (explicit glob) · `check_shipped_content.py` exit 0 zero WARN (**the ASCII gate is the one that bites here**) · both commit gates zero-WARN · `claude plugin validate --strict` · every fix has a pin that was **observed RED first**, with the failure quoted in the implementer's report · **D1 proven on the real call shape** — a JSON-**string** args payload carrying `renderOnly` returns the re-render and never reaches `validateArgs` · **10b additionally:** `splitPanelResults` index arithmetic unchanged (pinned) · `spawnJudge`'s two-failure throw still fires · the same-model disclosure is unchanged by a namespace retry · `plugin-distribution:9` amended in the same commit and re-measured.
- **A standing benefit worth stating as an AC for 10b:** the published eval baseline currently carries a recorded deviation — `nsAgent` was **source-shimmed to bare names** to make the run complete, so *"this run measures the shipped pipeline through project-local agent definitions, not through the installed-plugin registry."* If D6 lands, the next baseline can drop the shim and measure the real path. Record that in the 10b land commit.
- **Pre-registered land obligations (from the S9 Stage-9 harvest, applied on `harvest/0041-s9` and riding this slice's land PR):** **S10-L1 — ROADMAP is the thin one again; measure on the MERGE tree, not the branch.** The S9 harvest spent 55% of the condensation's gain (11,776 → **12,226 B = 87.3%, 374 B under WARN**) filing the corpus gate-item and the tree-gate-scope over-claim. If S10 appends anything to ROADMAP, 374 B is not a margin to assume from a branch-local number — `git merge-tree --write-tree` + materialize + the real gate, per the standing Land rule. If it breaches, the discharge is the **sharding-ladder decision already tracked in the *Ledger hygiene* entry**, not a shave. **S10-L2 — the harvest's own new rules now bind this slice.** Three of them apply directly to engine work and the panel should audit against them, not just the pre-existing bar: `testing.md` → *A scan's CORPUS is part of its contract* (any new pin that sweeps files uses `git ls-files`, never `rglob`, with a floor assertion) · `testing.md`'s widened ambient-vacuity axis (**run a new pin's non-vacuity check in the shape the defect inhabits** — for D6 that means the resolution world it actually fails in, not the one you are standing in) · `synthesizer-gate`'s *BYTE-BOUNDED surface* rule (10a/10b set no byte AC, so this binds only if the spec grows one). **S10-L3 — no INVARIANTS entry is affordable.** INVARIANTS sits at 17,976 B / **24 B to WARN** and the S9 harvest deliberately added nothing. If 10b's D6 fallback establishes a genuine load-bearing invariant — plausibly *"the namespace retry must never touch the respawn counter or set `forcedSameModel`"* — it **cannot** simply be appended: the land owes either a paired condensation or the sharding-ladder decision. Decide at spec-approval time, not at land.

### Slice 10a — Verify verdict (Stage 7)

RUNNING AS: Opus 5 (`claude-opus-5[1m]`) — all three lenses, the yagni sentinel and the gate ran opus; the builder ran the session model. Clean-context **role** independence, not model independence. **Round 2 carries weaker independence still and says so in its own words:** the round-2 bytes ARE the gate's own prescription patch, so it was confirming its own work — it compensated by hunting for errors in its own prescriptions, **and found one** (S10a-L3).

Round 1 **CHANGES REQUIRED (BLOCKING)** → round 2 **PASS**, conditional on nothing.

**The blocking finding: the DRY collapse shipped a total claim it had not earned.** `artifactBase` is applied **twice** on the live path (boundary, then inside `driverPrompt`) and was **not idempotent** — an all-separator dir trimmed to `""` and the second application substituted the default, so the QA driver **saved** screenshots to one directory while the report **cited** another. Measured: base diverged on **0** of 23 dir shapes, HEAD on **8 — all created by this slice**; on 200,000 random pairs the divergence rate was **95.4%**. Aggravating, and the reason the severity is blocking rather than should-fix: **the same commit authored a comment asserting the two "cannot diverge"** while its own ROADMAP line filed the exception.

**The gate overturned its own panel three times, by measurement — this is the round's most important lesson.** (1) All three lenses blamed the widened `[\\/]` trim; **wrong** — `/`, `//`, `///` regress too and were already inside base's `/`-only class, so the cause is *double application of a non-idempotent normalizer* and narrowing would not have fixed it. (2) Two lenses converged on a prescribed fix — `(dir == null ? "" : String(dir))` — that is **itself defective**: measured, it changes 9 inputs, 3 unintended (`0`, `false`, `NaN` stop defaulting and become path segments). The gate's variant preserves the falsy test and changes exactly the 6 defect inputs. (3) The reliability lens filed a mutation survivor (R5) that **is killed** — narrowing `DEFAULT_VERIFY_DIMENSIONS` survives only if you drop `testing`; dropping the other entry is caught by a region anchor.

**The `runLabel` disagreement, adjudicated against the majority.** Two lenses said defer to ROADMAP, one said fold in. The gate folded it in and named a trigger no lens found: not a hostile caller and not the boot agent, but **`engine/build-item.js`'s own `build-<id>-iter<n>`** — the harness's own `/build` → QA path. The repo's own fixture ids (`T1`, `AC-1`, `BL-3`, `Tier1-item2`) **all diverged**. It is the same expression on a line this diff rewrote: the diff normalized the token left of the slash and left the one on the right raw. `sanitizeForPath` was measured **idempotent**, so `driverPrompt`'s second application is a proven no-op. **And the fix FREED 82 B** (ROADMAP 12,531 → 12,449; headroom 69 → **151**) — cheaper than the deferral.

**Test strength — three mutants that survived the shipped slice are now killed.** The sharpest **re-created D5's own defect**: the pin asserted the *vocabulary* (`for…of .defaulted` exists) rather than the *effect* (something is logged), so deleting the `log()` passed a test whose failure message names that defect. `DEFAULT_VERIFY_DIMENSIONS` was pinned only by **tautology** (compared against itself), so the default Verify panel could have silently lost its `testing` lens. Five paragraph-deletion mutants on `driverPrompt` all survived. **Where `testing` and `yagni` were in direct tension the gate resolved by measurement, not compromise:** it took testing's findings (measured survivors) but yagni's cheaper remedy (one section-order pin kills all five deletions plus two reorders — no golden-master snapshot), and **rejected yagni's cuts wherever a mutant disagreed**, including one the plan's own Slice 10b would have made dangerous (10b is spec'd to "dedupe `dimensions`"; an in-place dedupe on an aliased module constant corrupts the default panel for every later item in a build-to-green loop — the `.slice()` copy pin yagni wanted cut is what catches it).

**Verified at round 2:** all five files **byte-identical** to the prescription patch by blob hash (zero hand-editing) · red-first held for the gate's own prescriptions (tests-only → exactly 3 RED, then 402/402) · every already-green pin proven non-vacuous by running its mutant **both ways** (survived before, killed after) · **301,287 measured (dir, label) pairs → 0 divergent**, against 290,746/300,000 divergent pre-fix · ROADMAP re-measured on the **materialized merge tree** with the real gate: **12,449 B, 151 B headroom, exit 0**.

**S10-L1 DISCHARGED** (measured on the merge tree, not the branch — main 11,776 → base 12,226 → head 12,449; the 755 B delta is exactly why the merge-tree rule exists). **S10-L2 DISCHARGED** (no file sweep added, so the corpus rule does not fire; every source pin reads one named file; red-first verified independently at 25 red of 399). **S10-L3 n/a to 10a** — resolved at approval; INVARIANTS untouched at 17,976 B.

#### Spec amendment (the finding refutes the SPEC, not the diff) — carry into the 10b spec

**When a slice collapses N call sites onto one normalizer, the normalizer must be a FIXED POINT.** The S10a spec asked for *"one source of the artifact-dir shape"* and got it — but never asked whether that one source is **idempotent**. Single-sourcing is not enough when the live path applies the normalizer more than once: **DRY-by-collapse silently converts a latent non-idempotence into a live divergence**, and a spec-conformant diff was no defense. 10b must state the property explicitly for any normalizer it collapses onto and carry a fixed-point pin (`f(f(x)) === f(x)`) as an **acceptance criterion**, not reviewer discretion.

**Generalization flagged for Stage 9 (cheap, mechanical):** the *comment* was the tell. A commit that authors a **total claim** in the same breath as a **ROADMAP line filing its exception** is asserting a property it has not earned — that pairing is a mechanically detectable smell.

##### S10a-L1…L3 discharge (at land, 2026-08-17)

- **S10a-L3 — DISCHARGED.** `four` → `five` at the ROADMAP `screenshotPath` line. **The gate caught this in its own prescription:** its R7 pin composes `screenshotPath()`, adding the fifth call site **in the same patch that wrote "four."** Byte-neutral (both words are 4 bytes — the 12,449 B / 151 B measurement stands, re-confirmed). Substance re-verified: **zero engine call sites**, referenced only in two comments. A textbook instance of the gate's own standing rule *never carry forward a prior round's numbers, including your own*.
- **S10a-L1 — OPEN, carried to plan close** with the other ROADMAP routings: `screenshotPath` is dead code — delete it with its pins, or wire it as the report's oracle. Already written into ROADMAP `:57`, **no new bytes** (the rewrite freed 82).
- **S10a-L2 — OPEN, owner = orchestrator at Stage 9** (harvest, **not** ROADMAP — 151 B will not hold it, and `docs/claugentic-standards/*.md` is unbudgeted): when a below-the-block **source pin** suffices versus when **extraction** is warranted. 10a moved production code twice "so it can be pinned"; the gate's own R7 then showed a source-string pin covers the below-the-block case at zero relocation risk. `driverPrompt` clearly earned its move; `auditEntry`/`verifyChildArgs` each cost ~24 lines of engine surface for fixes a one-line regex already guards.

### Slice 10b — the shared-seam pair (D4 + D6)

_Re-grounded 2026-08-17 at `main` `97bbe18` (post-10a). **Every anchor re-derived; the plan's were stale** — 10a moved three helpers-block end-markers (`audit.js` 1092→**1112**, `build-item.js` 624→**679**, `qa.js` 744→**810**), and `guardedAgent` moved `:1207`→**`:1231`**. `engine/verify.js` was untouched by 10a (22–444, 619 lines), so its anchors are original-vintage._

#### ⚠ SPEC CORRECTION, before any code — the plan's D4 premise is FALSE

The plan says *"one thrown lens rejects the whole `parallel()`."* **It does not.** The Workflow tool's own contract states: *a thunk that throws (or whose agent errors) resolves to `null` in the result array — the call itself never rejects.* `engine/audit.js:1227-1228` already records this correctly; the plan's framing contradicts the repo and the platform. **A build started from the plan's premise would have "fixed" a crash that cannot happen.**

**What this changes:** D4 is **not** a crash-prevention fix. Its honest justification is the one `audit.js` already wrote for `guardedAgent` — *make the never-crash property LOCAL and auditable instead of resting on an out-of-repo tool contract* — plus the **genuine** defect the plan under-stated: **the fan-out is unbounded by caller input.** `validateArgs` (`verify.js:107-147`) checks `dimensions` membership only — **no de-duplication, no length cap** (verified) — so `dimensions: Array(200).fill("security")` validates clean and spawns **200 lens agents over one module**. The severity moves from "a crash" to "a silent cost/latency blow-up and an unauditable safety property." Slice 10b's AC changes accordingly.

- **In plain English (shown first at the approval gate):**
  - **What this builds:** the two remaining engine defects, which are **one seam**. **D4** — the Verify panel's fan-out has no local guard and no bound: a lens that errors resolves to `null` (which the existing `coverageGaps` → `finalVerdict` machinery already converts into an honest forced `CHANGES_REQUIRED`), but nothing in this repo *says* so, and nothing stops a caller from spawning the same lens 200 times. **D6** — `nsAgent` hard-namespaces all 20 spawn sites; in a project-local dogfood session none resolve, which **aborted an eval run** and forced the published v0.5.0 baseline to be measured through a **source-shimmed engine**, so the repo's own baseline does not measure the shipped agent-resolution path.
  - **What "done" means for you:** the panel's degradation is provable from inside the repo instead of inherited from a tool contract; a caller cannot multiply agent spawns without bound; and the next eval baseline can drop its shim and measure the real thing.
  - **What you're accepting:** a retry path lands next to the harness's most protected trust surface (`spawnJudge`'s one-respawn contract and the `forcedSameModel` flag that feeds the same-model disclosure) · `docs/claugentic-decisions/plugin-distribution.md:9` states the one-way namespacing rule as settled doctrine and **must be amended in the same commit** (12,524 B, **76 B to WARN** — measure it) · a false header comment gets corrected.
- **Files & changes:** `engine/verify.js` (the guard; the dedupe; the `:11-12` header correction) · `engine/{audit,qa,build-item}.js` (the D6 fallback, **byte-identical across all four**) · `tests/workflows/{verify,cross-script,agent-namespace}.test.mjs` · `docs/claugentic-decisions/plugin-distribution.md` · `CHANGELOG.md`.

- **THE FIVE TRAPS — each measured, each fatal if ignored:**
  1. **A blanket guard over `panelTasks` ERASES the fail-loud honesty contract.** The honesty thunk *is* a `spawnJudge` call inside the same `parallel()`. `judgeOutcome` **deliberately throws** on two failures (*"failed twice — never a silent partial PASS"*, pinned at `verify.test.mjs:453`). A uniform guard catches that throw → `honestyJudge = null` → `honesty = null` → `reportedFamily = null` → the floor tag, **and `coverageGaps` does not cover it** (it walks `lensReturns` only, `:305-325`). The honesty lens would silently read as "ran, nothing to disclose." **The guard applies to the lens and yagni thunks ONLY, or is spawnJudge-aware.**
  2. **`splitPanelResults` slices by INDEX** (`:341-350`): `slice(0, lensCount)`, `[lensCount]`, `[lensCount+1]`, and `lensReturns[i] ↔ modulesAudited[i]` is a parallel-array invariant `coverageGaps` consumes. Any guard that filters, compacts, chunks, or returns settled-result objects **misaligns the pairing and makes `coverageGaps` name the wrong module as unrun.** Return `null` **in position**, preserving arity.
  3. **The dedupe must be NON-MUTATING and applied ONCE, feeding BOTH consumers.** `panelRoster` (`:476`) and `modulesAudited` (`:477`) derive **independently** from `input.dimensions` — dedupe one and not the other and the index pairing silently breaks. `validateArgs` is currently a **pure predicate**; do not make it mutate. **New hazard 10a did NOT close:** `verifyChildArgs`'s *named* branch is `dimensions = named` with **no copy** (`build-item.js:~633`), so an in-place dedupe mutates the caller's item object across build-to-green iterations. The default branch is protected only by `.slice()` (`:637`) — the pin the 10a panel wanted cut.
  4. **Rewriting `nsAgent` as a multi-statement arrow silently guts the drift pin.** `cross-script.test.mjs:36-42` matches `^const <name> =[ \n][\s\S]*?;$` — **lazy, to the first line ending in `;`** — so a multi-line body compares a truncated fragment across all four files and **passes while pinning almost nothing.** Keep it a single-expression `const`, or convert to `function nsAgent(...)` (the `^function` branch matches to the closing brace and is safe). Note the drift pin covers **only the names in its array** — a new `spawnWithFallback`/`bareName` helper is unpinned unless added.
  5. **`agent-namespace.test.mjs` forbids the naive fallback by construction** — it strips `nsAgent("<name>")` then asserts no bare `"<name>"` survives, over a 9-name list. The fallback name must be **DERIVED at runtime** from the namespaced id, never written as a literal. And `agentType` must stay a **plain string** (`verify.test.mjs:412` asserts the concrete namespaced value); two sites pass the id **positionally** into `spawnJudge` (`:513`, `:566`), so an `agentType:`-keyed grep is insufficient.
- **What a namespace retry must NOT do (the trust surface):** not set or influence **`forcedSameModel`** — that flag means *"ran without the `model:` pin"*, and mis-setting it either fabricates a false cross-model claim or silently downgrades a true one · not **consume the one respawn** (`judgeOutcome`'s `second === undefined` IS the state machine; threading a namespace retry through it leaves a genuine model failure with no attempt left) · not **swallow the two-failure throw** · not **reuse the `:respawn` label**, or the run log cannot distinguish a namespace retry from a model-forced respawn — the same disclosure surface one layer out.
- **The inherited fixed-point AC (10a's spec amendment) — and it BITES here.** Any normalizer this slice collapses onto must satisfy `f(f(x)) === f(x)` **as an acceptance criterion, not reviewer discretion**. The dimensions dedupe is trivially idempotent; **the D6 derivation is not automatic** — `nsAgent(nsAgent(x))` yields `claugentic-dev-harness:claugentic-dev-harness:x`, and a prefix-strip applied to an id that legitimately contains the prefix is the same class. Pin it both ways. Live precedent to copy: `qa.test.mjs:903` (`artifactBase: IDEMPOTENT`).
- **In-scope standards dimensions:** `reliability-resilience` (the guard, the retry, the bound) · `testing` (every fix pinned; the trap-4 drift-pin check) · `honesty` (**the same-model disclosure, the `verify.js:11-12` false claim, and the `plugin-distribution:9` amendment** — this is a trust surface, so the panel convenes `honesty-reviewer`).
- **The false claim to correct:** `engine/verify.js:11-12` — *"Call count is structurally bounded by the roster — no loops."* **Both halves are false:** `:484` is a `.map()` (a loop), and the roster is caller-sized (`panelRoster` iterates `modulesFor(args.dimensions)`). After the dedupe the bound becomes real (`≤ 11`, `KNOWN_MODULES.length`), but "no loops" stays false unless reworded.
- **Tests — red-first, technique named per defect:** the guard and the fallback both live **below** the helpers block and close over `agent()`/`log()`, so **neither is extractable** — extract the *pure* parts (a `guardFailure(err, label) -> null` message builder; the bare-name derivation) into the block and unit-test those, plus source-level pins on the wrappers (`agent-namespace.test.mjs` is the precedent; note `verify.test.mjs` has **no** source-level pins today, so that is new machinery in that file). Dedupe: pin that `panelRoster` and `modulesAudited` agree on a duplicated input. Trap 4: a pin asserting `nsAgent`'s declaration still matches the drift-pin extractor's `const` shape. **Any new sweeping pin uses `git ls-files` with a floor assertion** (the harvest's corpus rule).
- **Acceptance criteria:** full battery green both cwds · node suite (baseline **402**) · `check_shipped_content.py` exit 0 zero WARN (**ASCII: all four engine files are 0 non-ASCII today — use `--` and `->`**) · both commit gates zero-WARN · `validate --strict` · every fix pinned RED-first with the failure quoted · **`f(f(x)) === f(x)` pinned for every normalizer touched** · `splitPanelResults` index arithmetic unchanged and still pinned · `judgeOutcome`'s two-failure throw still fires · **the same-model disclosure provably unchanged by a namespace retry** · `plugin-distribution:9` amended in the same commit and re-measured · the `verify.js` header claim true as written.
- **Sizing:** ~8 files. One session. **Do not split** — D4 and D6 share the spawn-wrapper seam and D6's failure mode is what makes D4's guard observable.
- **Pre-registered land obligations:** **S10b-L1 — the eval-baseline shim.** `eval/BASELINE.md` records that `nsAgent` was source-shimmed to bare names to make the v0.5.0 run complete, so *"this run measures the shipped pipeline through project-local agent definitions, not through the installed-plugin registry."* If D6 lands, that deviation is retired — **state it in the land commit** and note the next baseline can drop the shim. **S10b-L2 — INVARIANTS is at 24 B.** Per S10-L3 (resolved at approval) the D6 constraint is **pinned, not appended** — a red-first pin plus a do-not-revert comment at the call site. Escalate to an INVARIANTS entry **only** if the panel judges the constraint real AND not pin-coverable, and then the discharge is the sharding-ladder decision, never an append. **S10b-L3 — ROADMAP has 151 B.** Any routing measured on the MERGE tree; a breach discharges via the sharding ladder.

### Slice 10b — Verify verdict (Stage 7)

RUNNING AS: Opus 5 — all four lenses **and** the gate ran opus; the builder ran the session model. Clean-context **role** independence, never model independence. Round 1 **CHANGES REQUIRED** (12 fixes, 7 numbered deferrals, 10 explicit drops); the applied set was measured end-to-end **before** it was prescribed.

**The engine work is right and materially better than base — measured, not asserted.** A caller could previously make the panel spawn a reviewer per duplicate entry: `Array(200).fill("security")` produced **200 lenses / 203 agent calls**; it now produces **1 / 4**, and 220 entries cap at **11** (the catalog size). The namespace fallback does what it promises without disturbing the same-model disclosure — driven, not pattern-matched: `crossModel {claimed: true}` survives, **zero `:respawn` labels are spent**, and both judges' fallback calls keep `model=opus`.

**What was blocking was what the slice SAID.** In **four newly-authored places** — two engine comments, the CHANGELOG, and the permanent decisions ledger — it stated that the honesty judge's two-failure throw *"has to stay loud."* Driven on the real bytes at HEAD **and at base**: `parallel()` swallows `judgeOutcome`'s throw either way, yielding `verdict: PASS`, `panelDegraded: false`, `honesty: null`, and **zero log lines**. So on a **trust-surface** diff — the only case where the honesty lens convenes at all — the panel can report PASS having never received the honesty review, and `panelDegraded: false` is an **affirmative false statement** about that run.

**The rationale was INVERTED, and the gate proved it by building the construct the spec forbids.** Guarded vs unguarded: outcome byte-identical, agent calls identical — **the only delta is one log line the guard ADDS.** Not guarding buys nothing on the observability axis. The true reason (now in the code) is that `guardFailure`'s message is false for a judge: `coverageGaps` walks `lensReturns` only, so an unrun honesty judge yields no coverage gap and no forced `CHANGES_REQUIRED`.

**The gate overruled TWO lens prescriptions, each by measurement.** (1) reliability's *"land the log line now"* — its own snippet needed a new message builder precisely because `guardFailure`'s wording is false for a judge, which is the evidence you cannot write an honest message before deciding what the event **causes**; and the gate measured it **colliding** with testing's prescribed pin (both applied together = RED). (2) testing's trap-1 pin as written forbade **catching**, but the hazard is **swallowing** — as written it would also have **blocked the future ROADMAP fix**. The corrected pin requires every `catch` arm in that region to rethrow, and is strictly better on all four inputs (clean green · MX1 red · MX2 red · log-and-rethrow green).

**Test strength.** The pin guarding the spec's own *"fatal if ignored"* trap 1 was **vocabulary-only**: the gate built two survivors (a blanket guard written as an in-place `for` loop, and the honesty thunk swallowing alone) and both were **green at HEAD**. The orchestrator re-ran both independently against the fixed tree — **both KILLED** by the corrected effect pin. This is the 10a lesson recurring inside the slice that inherited it; the implementer caught one sibling of the class in its own round 1 and missed this one.

**Also folded in:** the slice bounded the **spawn** reader of `args.dimensions` and left its **fail-loud** reader unbounded in the same file — a 201-line, 15,620-character throw became 2 lines / 98 chars. And `plugin-distribution.md:9` claimed the retry at a caller that cannot reach it (the fallback is in the four engine scripts only; `skills/` carries 65 namespaced spawn mentions and **zero** retry) — the `renderOnly` capability-at-the-caller class, on the doctrine surface a future agent consults.

**An off-by-one worth keeping:** the doc-budget gate compares `measured >= int(cap * 0.9)`, so the safe margin is **+150 B, not 151** and **+15 B, not 16**. The spec's own framing was off by one at exactly the margin that mattered.

##### S10b-L1…L10 discharge (at land, 2026-08-17)

- **S10b-L8 — DISCHARGED, and it was the binding one.** The gate measured ROADMAP could not absorb the four routings: **+631 B needed, 95 B of levers, a 386 B shortfall** requiring real condensation. Discharged **without a shave**: the four routings went into the **existing** `0041 verify-deferred batches` entry as one S10b clause rather than four new bullets (the DRY move the gate's arithmetic had not assumed) · one clause was **genuinely discharged** — the register-sweep entry's *"three tracked test files sat un-indexed"* was fixed during post-S9 housekeeping, so it absorbed to git history · three gate items retold incidents the recent harvests recorded in **standards modules**, so ROADMAP now points instead of restating · the *"first real tag push proves unauthenticated `validate`"* item was **PROMOTED to its home** (`docs/RELEASE_CHECKLIST.md`, unbudgeted) — rung 1 of the escape-valve ladder, a re-home rather than a drop. **Result: 12,449 → 12,580 B with +631 B of routing absorbed; gate exit 0, ZERO WARN** (the slice's AC is stricter than the gate, which returns 0 on a WARN).
- **S10b-L4…L7 — DISCHARGED (written).** D7 (the honesty-judge hole — log it first, then decide **with approval** whether it degrades or blocks, since it moves `finalVerdict` semantics) · the skills' prose spawn directives carry no bare-name retry · the D6 retry re-runs an `isolation: "worktree"` implementer on a post-work throw · a bare retry that also throws drops the first cause. **These are the referent** for the "routed…" pointers R1/R2/R6 ship in engine and ledger copy — had they not been written, those pointers would resolve to nothing, which is verbatim the 0041 S7 §(3) failure.
- **S10b-L9 — DISCHARGED.** `docs/claugentic-decisions/plugin-distribution.md` lands at **12,594 B — 6 B to WARN.** It is effectively at its condensation threshold: S11/S12 must not assume headroom there, and the discharge is the sharding ladder, never a shave.
- **S10b-L1 / L10 — DISCHARGED.** D6 landed, so `eval/BASELINE.md`'s recorded deviation (`nsAgent` source-shimmed to bare names, so *"this run measures … project-local agent definitions, not … the installed-plugin registry"*) is **retired**: the next baseline can drop the shim and measure the shipped agent-resolution path for the first time.
- **S10b-L2 — DISCHARGED as pinned, not appended.** INVARIANTS untouched at **17,976 B / 24 B**. The D6 constraint is covered by pins, driven behaviourally, and carries a do-not-revert comment at the wrapper — per S10-L3's resolution at approval.
- **S10b-L3 — SUPERSEDED by L8** (its premise, that a routing would fit, was measured false).
- **S10b-L5/L6/L7 remain OPEN as ROADMAP items** (written, not built) and carry to plan close with the other routings.
