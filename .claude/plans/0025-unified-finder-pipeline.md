# 0025 — Unified finder→select→plan→build pipeline (+ doctor)

- **Status:** **In progress (2026-06-25)** — ★ audit cell-budget fix + Slices **1 (decision-gated autonomy), 2 (architect-pass TEMPLATE), 3 (Stage-2 sub-process), 4a (finder contract + SELECT), 4b (preemptive PLAN + OFFER-BUILD) LANDED** — the **doc/contract layer (4a+4b) is complete**; finder-pipeline design plan-reviewed (CHANGES folded). Remaining = the **behavioral wiring**: **5 (audit/product SELECT retrofits) · 6 (build-from-pre-made-plan + offer + Stage ordering) · 7 (doctor)**. **`0024` is LANDED.**
- **Resumable from:** **Slice 5** (audit + product SELECT-gate retrofit — depends on 4a ✅ only; wire SELECT-before-render into `skills/audit/SKILL.md` Phase 3 + `skills/product/SKILL.md` gap-mode + the Bugs-as-3rd-source reader; coverage/run-report stays full-scope per review #3). Then 6 (depends on 4b ✅) → 7 (depends on 4b ✅). Slices 5-7 touch the SKILLs/new doctor skill (behavioral). 0026 §C (step 4, roster rewrite) has a precise lockstep edit-map ready (see §C).
- **Blockers:** none (0024 landed). Slices 3 & 7 coordinate with `0026` §C (agent-roster) to avoid double-touching `lens-reviewer`/the Stage-9 signal — sequence noted per-slice.
- **Flags (Slice 1 build — things to review):** (1) retired the word "checkpoint" across `build/SKILL.md`, reframing the watched run as "the watched, decision-gated run" and keeping **build-to-green** as the named *unwatched* rung (the who-watches axis) — chose to keep build-to-green's earned-per-repo unlock contract intact rather than fold it into the new model. (2) `skills/build/SKILL.md` ARCHITECTURE_TREE one-liner runs ~330 chars (over the ~150 working target, under the 450 ceiling) — chose to keep the load-bearing nav signal, matching the entry's pre-existing density. (3) Mapped the three concrete build pauses onto the three STOP classes (spec → (a)/(b) · before-land+irreversible → (c)) per the Stage-3 "same three, refined" sharpening — chose the explicit mapping over a new parallel stop-set.
- **References:** `skills/audit/SKILL.md` · `skills/product/SKILL.md` · `skills/build/SKILL.md` · `engine/audit.js` · `docs/claugentic-WORKFLOW.md` · `.claude/plans/0024-harness-context-and-lifecycle.md` · `.claude/plans/0026-conceptual-spine-and-roster.md` (the extracted spine + the lens-completeness gate that pairs with this plan's audit cell-budget fix)

## Problem

The harness has three "finders" of work, but the lifecycle from *finding* to *doing* leaks effort and bloats the backlog:

1. **No selection gate before elaboration.** `audit` writes **all** surfaced findings into the backlog; the user later has to weed it. Worse, when building, specialists **Spec/research items the user then rejects** (the workflow Specs at Stage 4 *before* Approve at Stage 5) — the user's real pain: *"plans had steps I didn't want; I had to force-complete after specialists had already spec'd."*
2. **No shared scaffold step.** A selected item becomes a backlog line with no groundwork; build re-derives context from scratch.
3. **Finders dead-end.** After an audit/product run the user must manually re-invoke `/build`; the find→do flow isn't continuous.
4. **No harness-health finder.** `doctor` (the harness's own-hygiene checker — distinct from `audit`, which targets *your code* against *standards*) does not exist; the gates (tree, version-sync, doc-budgets, stale plans, init post-conditions, Stage-9 capture) run ad-hoc inside Verify/Land.

## Goals / Non-goals

**Goals**
- One **shared finder pipeline** — `FIND → user multiselect → produce a full plan per selected item → offer build` — reused by `audit`, `doctor`, and `product` gap-mode (DRY).
- The multiselect **is** the scope-selection gate (**item-level**): unselected findings are dropped (no backlog bloat). **Because selection already gates what's wanted, each selected item gets the specialist-produced PLAN preemptively** (problem · approach · decomposition), not a token stub.
- The **deep per-slice SPEC stays just-in-time inside build** (existing Stage-4) — not because items are unwanted (selection settled that) but because per-slice spec is the existing step-pruning gate and is cheaper JIT than deep-speccing every slice upfront. Step-level control = plan-review/approve + 0024 disposition.
- The **initial-plan agent does an architect-level holistic pass** — a required *Architecture & holistic fit* plan section (codebase fit/layering/modules/design-patterns/SOLID · product fit · the quality dimensions to uphold, mapped to `docs/claugentic-standards/` · extensibility/future-proofing) — so architecture is framed **from the start** and guides downstream agents, countering the observed "model-upheld ⇒ agents sometimes skip architecture." Structurally prompted (template forces the section) + `plan-reviewer`-audited; **YAGNI-balanced** (holistic ≠ gold-plated).
- Build `doctor` fully on this pipeline: **diagnose → report → multiselect → treat-on-approval → offer build**; mechanical fixes applied directly on approval (never silently, no new hook), substantive ones planned + offered to build.

**Non-goals**
- **Full upfront per-slice SPEC** of all selected items (the deep Spec is JIT per slice inside build; the PLAN *is* done upfront on selection, the Spec is not).
- **Forced** auto-build (build is *offered* after selection; default is "leave in backlog").
- Engineering→product graduation (rejected in 0024).
- A new always-loaded doc or a new hook (doctor stays user-invoked; honesty posture: only the tree gate is mechanical, now at commit altitude).

## Approach (to be specced)

**The shared finder contract.** A finder produces a candidate list, then:
1. **SELECT** — present the candidates as a **multiselect** (anti-bloat; the user picks what to act on, the rest are dropped — honoring the existing `rejected-findings` / `rejected-proposals` memory so a dropped item stays dropped).
2. **PLAN (preemptive, per selected item)** — a specialist (`Plan`/architect + `plan-reviewer`) produces the item's **full plan file** in `.claude/plans/` (problem · **Architecture & holistic fit** · approach · decomposition · the finding's evidence/`file:line`) **and** a one-line ROADMAP item. The agent does the **architect-level holistic pass** (below); only the **deep per-slice Spec** is deferred to build (which has deeper code context — user-confirmed).
3. **OFFER BUILD** — ask *"build these now, or leave them in the backlog?"* If build: enter the build loop. If not: the plans + backlog lines persist for a later `/build`.
4. **BUILD (JIT spec per slice)** — build works items **one at a time**, each starting from its pre-made plan; per slice the **Spec → Approve → Implement → Verify** runs as build reaches it (existing workflow). Step-level pruning happens at plan-review/approve and 0024 disposition — so no slice is *deep-spec'd* before its turn, and unwanted steps are cut without waste. Build keeps its per-item checkpoints (before code / land / irreversible).

**Commitment — not capture — triggers the plan.** A *committed* item (one you select to do) auto-gets its plan via step 2 (with the architect-pass). A raw *capture* (a bug or idea jotted to the **Bugs/Later** zone) stays a **planless one-liner until you commit it** — so quick capture stays cheap and planning is never front-loaded onto un-prioritized items (the wasted-effort guard). "Adding to the roadmap" = **committing** = the plan trigger; the capture zones are a cheap inbox that doesn't. (Trivial/mechanical items skip even this — `doctor` or a small inline fix just does them, no roadmap, no plan.)

**ONE planning process, every scenario (auto-triggered OR ad-hoc "build feature X").** No weak ad-hoc path where architecture gets skipped. **It expands WORKFLOW Stage 2 (Plan) into a 3-step sub-process, keeping Stage 3 (Review) as the gate "as before":**
- **2a Draft** — Claude's **plan mode** + `Plan` agent → output **structured by the TEMPLATE** (architect-pass = the forcing function).
- **2b Specialist deep-dive + feedback** — a scoped panel (in-scope standards lenses + `architect-reviewer` + `yagni-sentinel`, + `honesty-reviewer` on claims, + `product-designer` if user-facing) review the plan's **design** and give **advisory** feedback (builder-class — they *contribute*, not gate). Reuses the **Verify-panel machinery** (`engine/verify.js` pattern; add a `plan-design-review` mode to `lens-reviewer` beside its diff/audit modes). This **formalizes + standardizes** the diverse-panel-at-Plan the workflow already gestures at (WORKFLOW.md:36, currently contested-only + prose-convened) into a standard Stage-2 sub-step for substantial work.
- **2c Planner incorporates** the feedback → refined plan (loop 2b↔2c as needed).
- → **Stage 3 Review** (`plan-reviewer`, the adversarial **gate** — judge-class) verdicts the refined plan as before → **Stage 5 approve**.

**Same panel, two altitudes (the right-size on "all agents"):** 2b reviews **design** (advisory); Stage 7 Verify reviews **code** (gate) — most lenses contribute at both; security/perf do design-level threat-model/complexity at plan, concrete violations at verify. **Scoped + dial-able** (only the architect-pass's named dimensions; trivial changes keep the lightweight path — not all lenses every plan). **Enforcement = model-upheld + structurally-prompted** (skill flow requires the plan; template forces the sections; 2b panel + Stage-3 gate) — optionally a cheap "plan file exists + required sections non-empty" presence check, but quality stays reviewed.
- **Iteration is the enforcer (loop to a fixed bar).** Quality is upheld by *looping until the gate passes*: **2b↔2c** is **bounded** (1 round default — advisory has no natural stop; the planner decides if another round genuinely adds value, diminishing returns) · **2↔3** loops until `plan-reviewer` **PASS** · **6↔7** loops until the **DoD is green**. Three guardrails make iteration *enforce* rather than spin/cheat: **(1) a fixed, finite bar** (DoD / Stage-3 criteria) so loops terminate — "meet the bar, then stop," never "until perfect" (WORKFLOW.md:135); **(2) a cap → escalate to the user** if a gate won't converge (never loop forever, never fail-open by lowering the bar — `engine/build-item.js` carries the build cap → PARTIAL/escalate); **(3) clean-context adversarial gates** so a PASS is genuine, not the builder rubber-stamping its own rationale. The loop enforces deterministic gates *strictly* and reviewer gates as far as the reviewer's *judgment* — it never turns a model-upheld gate mechanical, just refuses to stop short of it.

**Architect-level holistic pass (the plan's load-bearing addition).** Because architecture is model-upheld, agents sometimes skip it — so the plan TEMPLATE gains a required **Architecture & holistic fit** section the initial-plan (architect) agent must fill: how the item fits the **codebase** (layering · module placement · design patterns · SOLID / separation-of-concerns) · how it fits the **product** · the **quality dimensions to uphold** (maintainability · performance/efficiency · security · reliability · extensibility — each mapped to its `docs/claugentic-standards/` module so the standards frame the plan rather than being discovered late) · **future-proofing**. These are **initial architect thoughts / placeholders**, NOT the deep per-slice Spec (that stays JIT in build) — but they EXIST from the start and **guide every downstream agent** to uphold them. Enforcement = **structural prompt + review**: the template forces the section to exist and `plan-reviewer` audits it is genuinely reasoned (not hand-waved) — you can't mechanically grade architecture, but you can require the section and review it. **YAGNI-balanced** (the existing `yagni-sentinel`): holistic ≠ gold-plated — the section frames what to uphold, not a mandate to build every abstraction now. Plans are ephemeral (deleted at Land), so a richer plan is working scaffolding, never context-bloat. **Surfaces:** `.claude/plans/TEMPLATE.md` (the new section) · the `Plan` agent's mandate · `plan-reviewer`'s audit checklist.

**doctor (the harness-health finder).** Diagnose = run the existing `claugentic-check_architecture_tree.py` + `check_versions_synced.py` + `check_doc_budgets.py` (incl. the new INVARIANTS cap) + scan `.claude/plans/` for landed/cold plans + re-assert init post-conditions (pre-commit wired; managed stamps current; plugin self-ref present in shared mode) + flag recent lands lacking a DECISIONS/INVARIANTS/standards touch (Stage-9 "did the harvest fire?"). **Report** a green/WARN/breach table (default = report-only). **Treat** (on approval): **bounded, reversible, no-architectural-decision fixes are applied directly — no plan needed** (delete a landed/cold plan · re-wire the pre-commit hook · apply an approved **doc-condensation** diff for an over-budget ledger · **tree hygiene** — add a missing entry / drop a stale one / condense an oversized entry). The "just-do-it, no plan" threshold = *bounded + reversible (git history) + no architectural decision*. Anything **substantive** is **not** done here — it's added to the roadmap, which **auto-triggers a plan** (see *Commitment triggers the plan*, below) and is offered to build. Name = `doctor`; treats on approval, never silent, no hook. **Where findings go:** most are **fixed on approval** (no backlog home needed); the report is a **transient snapshot** (regenerates, doesn't accumulate — like audit's overview). A *deferred substantive* finding goes to the **ROADMAP** (kept together, not a separate file) but needs **no new section**: in the harness's **own** repo it's normal **Quality/Feature** work (the harness *is* the product); in an **adopter** repo it's tooling-maintenance → the existing **Later** parking lot with a `harness`/`maintenance` **tag** (distinguishable without a new section). A dedicated *Maintenance* section only if volume ever warrants (YAGNI — doctor fixes most on the spot).

**Retrofits.** `audit` (Phase 3) and `product` gap-mode gain the SELECT step *before* writing the backlog fence (today audit writes the whole fence). `build` gains "start each item from its pre-made plan + JIT per-slice spec" and the post-selection build-now offer. `docs/claugentic-WORKFLOW.md` Stage ordering reflects scope-approval-before-deep-spec.

## Design decisions (resolved 2026-06-25 — leveraging the as-built code seams from steps 1–2)
_The open design questions, now answered against the actual code (`engine/audit.js`, the SKILLs, the 0024 disposition/Bugs model, the step-2 `lensCoverage`/§A) so the slices below are buildable._

- **Multiselect surface — an editable checklist, NOT AskUserQuestion, for the candidate set (Q: scaling past 4).** Render the candidates as an **editable markdown checklist** the user ticks (`- [ ]` per candidate) — scales to N (AskUserQuestion caps at ~4 options). Reuse the existing backlog-rendering form. AskUserQuestion stays for the small/binary moments (the build-now offer, a disposition choice). The checklist's checked items are the SELECT result.
- **SELECT composes with the engine return by FILTER-BEFORE-RENDER (Q: render-whole vs render-then-prune).** `engine/audit.js` already returns the structured post-prune/post-verify findings **and** `renderedBacklog`. The SKILL inserts SELECT **between** "engine returns findings" and "write the fence": present candidates → user ticks → render the fence from the **selected subset only** (unselected are dropped, honoring the `rejected-findings`/`rejected-proposals` memory so a dropped item stays dropped). Minimal engine change — the SKILL filters the findings list, then calls the existing `renderBacklogFence` on the filtered set (the engine need not know about selection; if cleaner, pass an optional `selectedIds` filter to the render seam).
- **Bulk-selection plan-file bound — plan-on-select for build-now, JIT-plan for backlog (Q: many plans upfront).** Items the user picks to **build now** get their full plan on selection (bounded by the build loop working one at a time). Items left in the **backlog** get a one-line fence/ROADMAP entry and are **planned just-in-time as build reaches them** (still architecture-first per item — the plan is produced before that item's slices, not all upfront). So a large multiselect never spawns dozens of ephemeral plan files at once.
- **Bugs feed the SELECT step as a third candidate source (Q: where 0024-S5 Bugs enter).** Triage/SELECT reads candidates from ALL three: the `harness-audit:backlog` fence + the `harness-product:backlog` fence + the durable **Bugs** section (0024 S5, outside the fences). Selecting a Bug **commits** it → triggers its plan (per *"commitment triggers the plan"*). A Bug stays a planless one-liner until selected.
- **doctor's "mechanical treat" bound = bounded ∧ reversible (git history) ∧ no-architectural-decision (Q: what's safe on approval).** The just-do-it-no-plan set: delete a landed/cold plan · re-wire the pre-commit hook (now `.githooks/pre-commit` + `core.hooksPath`, per 0024 S4a) · apply an approved doc-condensation diff for an over-budget ledger · tree hygiene (add/drop/condense an entry). Anything **substantive** (an architectural decision, a non-trivial fix) → ROADMAP → plan → offer-build. Never silent; treated only on approval.
- **Lens-selection is NOT a user multiselect (Q: surface lens selection?).** The step-2 **`lensCoverage` report already answers "did every lens speak?"** and §A's advisory surfacing flags a missed-but-relevant lens — so exposing lens-SELECTION as a user multiselect is YAGNI; the audit selects the relevant subset and reports coverage honestly.
- **Architect-pass / Architecture-fit TEMPLATE section — authored ONCE here (Slice 2), shared with 0026.** It applies to ALL plans immediately (incl. ad-hoc builds and this very plan), so it lands early in step 3 and 0026 §C reuses it (does not re-author it).
- **→ EXTRACTED TO PLAN `0026` — "the conceptual spine" (native plan+implement + agent-roster redesign + the verify-side lens-completeness gate).** The full scope (native generation, the top-down roster redesign, FIRST-PRINCIPLES VALIDATION, the RED-TEAM structural findings, and the lens-completeness gate marked done-in-step-2) now lives in `.claude/plans/0026-conceptual-spine-and-roster.md`. Built LAST (step 4), except its lens-completeness gate which is pulled forward to step 2 (paired with this plan's audit cell-budget fix below).

## Decomposition (step 3 — sliced 2026-06-25; each lands complete in one session, no debt)
**Build order:** Slices 1–2 are independent + foundational (build first); 3 depends on 2; 4 depends on 2(+3); 5–7 depend on 4. Slices 1 & 2 are dogfood-informed and autonomous-tier.

- [x] **Slice 1 — Decision-gated autonomy + async flags.** ✅ LANDED (2026-06-25) — binary ladder → decision-gated model (3 STOP classes incl. class-(c) hard-stop · research-don't-ask · FLAG-reversible-only + surface-at-close · model-upheld); "checkpoint" retired, build-to-green kept as the unwatched axis; `Flags:` Status sub-line. honesty-reviewer **CLEAN**; 158 pytest + 369 node green. Independent; doc/process. Rewrite `docs/claugentic-WORKFLOW.md`'s binary autonomy ladder (`:133`) → the decision-gated model: **STOP** only for (a) a genuine design fork · (b) a spec with a real trade-off · (c) an irreversible/outward action; **RESEARCH-don't-ask** a factual/technical uncertainty (cited); **FLAG (one line + chosen default) + CONTINUE** a non-blocking judgment; **SURFACE all flags at the close.** Wire `skills/build/SKILL.md` (mode handling + the close-out flag summary) + a **Status/Flags block** in `.claude/plans/TEMPLATE.md` (the running flag list). Honesty: model-upheld (the agent judges "is this a real choice?"), bounded by flag-and-surface. _(This session has been dogfooding exactly this — encode the lived behavior.)_
- [x] **Slice 2 — Architect-pass "Architecture & holistic fit" TEMPLATE section.** ✅ LANDED (2026-06-25) — required TEMPLATE section (codebase fit · product fit · quality dimensions→standards modules · future-proofing; YAGNI-guarded skeleton) + WORKFLOW Stage-2 mandate (scoped to substantial work) + plan-reviewer audit-checklist item 8. Model-upheld + structurally-prompted. Fixed a dangling `architecture-styles` module ref (→ real modules). Gates green; lighter self-verify (lean doc skeleton — FLAG). Independent; foundational; **shared with 0026** (author once). Add the required section to `.claude/plans/TEMPLATE.md`: codebase fit (layering · module placement · design patterns · SOLID/SoC) · product fit · the quality dimensions to uphold **each mapped to its `docs/claugentic-standards/` module** · future-proofing — as *initial architect thoughts/placeholders*, NOT the deep per-slice Spec. Add the **Plan-agent mandate** to fill it + the **`plan-reviewer` audit-checklist line** (is the section genuinely reasoned, not hand-waved?). **YAGNI-balanced** — `yagni-sentinel` guards gold-plating (the section frames what to uphold, not a mandate to build every abstraction now). Plans are ephemeral, so richer ≠ context-bloat.
- [x] **Slice 3 — Stage-2 plan sub-process (2a draft / 2b advisory panel / 2c incorporate) — DOC-ONLY.** ✅ LANDED (2026-06-25) — WORKFLOW Stage 2 expanded into 2a draft → 2b advisory deep-dive → 2c incorporate (Stage 3 stays the gate); 2b bound to the same Stage-0 SUBSTANTIAL triggers (trivial → 2a→3 no panel); panel = in-scope standards lenses + `yagni-sentinel` + `honesty-reviewer` (+ `product-designer` if user-facing), ADVISORY/builder-class (contributes, not gate); 2b↔2c bounded 1-round default; PROSE-CONVENED (the `lens-reviewer` plan-design-review engine mode → 0026 §C, NOT here); superseded the "contested-only" Plan note at WORKFLOW *Diverse critics*. Gates green; doc-only (no engine/spawn-id touch). **FLAG:** kept the 2b panel composition as the cross-cutting critics (yagni/honesty/product-designer) + standards lenses — did NOT add `architect-reviewer` as a separate 2b advisory reviewer (it is the Stage-7 gate/synthesizer role; naming it at 2b advisory could blur the contributes-vs-gates line) — chose to keep the kickoff's named set; reversible if a later slice wants the architect lens explicit at Plan. Depends on Slice 2. Expand `docs/claugentic-WORKFLOW.md` Stage 2 into the 3-step sub-process (Stage 3 stays the gate "as before"): **2a** plan-mode draft structured by the TEMPLATE (architect-pass = the forcing function); **2b** a scoped **advisory** panel (builder-class — *contributes*, not gate); **2c** planner incorporates → refined; bounded loop (1 round default). **Review #2 — bind 2b to the SAME Stage-0 SUBSTANTIAL triggers (`WORKFLOW.md:35-36`); trivial plans stay 2a→3 with NO panel** (honors the effort-dial; not every plan). **Review #C — the `lens-reviewer` `plan-design-review` ENGINE mode + its `agent-namespace`/`cross-script` pins MOVE to `0026` §C** (§C already lists that mode + rewrites `lens-reviewer`'s modes + merges its synthesizer — authoring it here then there is a guaranteed double-touch of `engine/verify.js` + the pins). So Slice 3 stays **prose-convened** (zero spawn-id surface); §C wires the engine mode. This is the plan's own "let §C absorb it" escape hatch, chosen explicitly.
- [x] **Slice 4a — Shared finder contract + SELECT-before-render mechanics.** ✅ LANDED (2026-06-25) — DOC-ONLY: new `## The finder pipeline (audit · product · doctor → backlog → build)` section in `docs/claugentic-WORKFLOW.md:165-182` documenting `FIND → SELECT → PLAN → OFFER-BUILD → BUILD` with SELECT fully specified (dismissal-memory-first · transient editable `- [ ]` checklist reusing the rendered item form · checked-subset result · **skip ≠ reject** · filter-before-render `renderBacklogFence({...result, items: selected})` ZERO engine change/no `selectedIds` · **★ full-scope-coverage INVARIANT**). honesty-reviewer **CLEAN** (verified the zero-engine-change claim against `engine/audit.js:1038-1056`); 369 node + 158 pytest + tree/version/doc-budget gates green; no engine/skill/test touched; no tree change. Depends on Slice 2. The reusable **SELECT** step (editable checklist; drops unselected; honors `rejected-findings`/`rejected-proposals` memory) + the documented contract. **Review #3 — filter-before-render needs ZERO engine change:** `renderBacklogFence` already filters `result.items` internally (`audit.js:1387-1404`), so the SKILL filters the candidate list and the existing render reflects the selected subset — **drop the optional `selectedIds`**. **The run-report `lensCoverage`/`verification` MUST keep reflecting the FULL audited scope** (computed over all findings), not the user's filtered subset, or the fence mis-reports "did every lens speak?". Surfaces: a shared section in `docs/claugentic-WORKFLOW.md` + the SELECT helper.
- [x] **Slice 4b — Preemptive PLAN + OFFER-BUILD.** ✅ LANDED (2026-06-25) — DOC-ONLY: expanded the finder-pipeline `PLAN/OFFER-BUILD` into detailed mechanics (`docs/claugentic-WORKFLOW.md:182-194`). **PLAN** = per-committed-item architect-pass (2a→2b→2c→Stage-3 `plan-reviewer` gate) producing the full plan file (reviewed groundwork, NOT a quality guarantee; model-upheld) + a one-line ROADMAP entry; **deep per-slice Spec stays JIT**; **★ plan-volume bound** (build-now one-at-a-time as build works them / backlog one-line entry planned JIT — never dozens at once). **OFFER-BUILD** = AskUserQuestion "build now or leave?", **default = leave, offered never forced** (finder→build continuity, no dead-end). honesty-reviewer **CLEAN** (corroborated against Stage-2/3 wiring, plan-reviewer.md, TEMPLATE, the serial loop); 369 node + 158 pytest + tree/version/doc-budget green; no engine/skill/test touched; no tree change. Depends on Slice 4a + Slice 2. The heaviest design seam + the dependency root for 6/7 (review #1 split — kept separate from 4a so 5 can land on 4a alone).
- [ ] **Slice 5 — `audit` + `product` SELECT-gate retrofits.** Depends on **Slice 4a**. `skills/audit/SKILL.md` Phase 3 + `skills/product/SKILL.md` gap-mode gain the SELECT step **before** writing the backlog fence (filter-before-render; zero engine change per review #3). Wire the **Bugs** section (0024 S5) as a third candidate source. Coverage/run-report stays full-scope (review #3).
- [ ] **Slice 6 — `build` "start from pre-made plan + JIT per-slice spec" + the build-now offer + WORKFLOW Stage ordering.** Depends on **Slice 4b**. `skills/build/SKILL.md`: each item starts from its pre-made plan; per slice the Spec→Approve→Implement→Verify runs JIT; the post-selection build-now offer; `docs/claugentic-WORKFLOW.md` Stage ordering reflects scope-approval-before-deep-spec. No spawn-id change expected — confirm. _(Sequence the WORKFLOW.md-editing slices 1, 3, 6 so the doc stays internally coherent at each land — review.)_
- [ ] **Slice 7 — `doctor` (the harness-health finder).** Depends on **Slice 4b**. A new `/claugentic-dev-harness:doctor` skill on the shared contract: **diagnose (strictly READ-ONLY)** — run the EXISTING gates `claugentic-check_architecture_tree.py` + `check_versions_synced.py` + `check_doc_budgets.py` (the INVARIANTS cap already landed in 0024 S3 — no code change, just run it) + scan `.claude/plans/` for landed/cold plans + re-assert init post-conditions → **report** a green/WARN/breach table (default report-only) → **multiselect** → **treat** (the bounded mechanical set on approval) / substantive → ROADMAP+plan+offer-build. The doc-condensation treat is safe because **the diff is user-approved** (NOT because condensation is decision-free). No new hook, no new always-loaded doc; user-invoked. **Review #C — doctor only REPORTS "Stage-9 harvest didn't fire" (a passive health flag); `0026` §C5's `retrospect-harvester` owns the ACTIVE harvest** (named seam, no double-build — 0025 ships report-only, §C wires the actor).

---

## ★ Audit lens-coverage integrity — a CURRENT BUG in `engine/audit.js` (do EARLY)

> **✅ LANDED (step 2, 2026-06-24).** Built per the Stage-3 review's narrowing: **prong #1** (interleaved round-robin enumeration — dir-outer/module-inner; cap-wins, per-lens floor via the interleave; blindspot strictly last) + **prong #4** (`lensCoverage` report: ran-found / ran-clean / pending) + the **resume-determinism** test (re-enumerate − doneCells === tail, ∀N). Prongs **#2 (auto-size) + #3 (hierarchical synthesis) → ROADMAP** (YAGNI until a real big repo exceeds the synthesis ceiling). 357 node + 158 pytest green; architect-reviewer **PASS**. The verify-side **lens-completeness gate** (0026 §A, its sibling) is the OPEN half — see the build-order note + the gate decision.

**Symptom (observed on 0.2.4, real adopter audit):** a `standard` run over a large repo (8 lenses × 8 dirs = 64 cells) at `maxCellsPerRun: 28` returned `PARTIAL` with **4 of 8 lenses never run** (zero findings) + a 5th half-covered. No lens was truncated mid-run (each that ran ran to full `depthForDial`); the budget dropped **whole lenses**.

**Root cause (verified in code):** `enumerateCells` (audit.js:316-331) orders cells **module-major** (`m0×d0…m0×dN, m1×d0…`); `applyCellBudget` (audit.js:336-341) is a flat prefix `slice(0, N)`. Any `N < total` consumes the first lenses fully and **starves the tail to zero**. No interleaving, no per-lens floor.

**It violates the documented promise.** `skills/audit/SKILL.md:184`: *"all relevant lenses run at every level — depth, never lens-count, is the lever."* Dial = DEPTH (focused/deep/exhaustive + blindspot/yagni at thorough); cell-budget = a SEPARATE cost/context bound. So a `standard` pass SHOULD hear from every lens (at deep depth) — the fix **aligns the engine with the promise**, NOT "make standard like thorough."

**Why the cap exists (the real ceiling):** `maxCellsPerRun` bounds the **synthesis context** — N finder outputs in one dedup/prune pass. "Just run at N=64" only works when the synthesizer can hold 64 outputs; on a big repo it can't (that's why the cap was low). So the fix has TWO prongs: make a LIMITED pass cover all lenses, AND make a FULL pass feasible at scale.

**The fix (ranked):**
1. **Interleave + per-lens floor** — enumerate round-robin across lenses (priority dirs inner) + ≥1 cell per configured lens. A budget-limited pass covers EVERY lens's top dirs (broad-then-deep); starvation structurally impossible.
2. **Auto-size the cap** from `lenses × dirs` (clamped to the synthesis ceiling); default to a **full single pass** (`N ≥ total`, one global dedup) when it fits. Stop hand-picking N (28 was a footgun).
3. **Hierarchical synthesis** (per-lens roll-up → global dedup) — raises the synthesis ceiling so a full pass is feasible on LARGE repos. This is what makes "always all lenses" work at scale (not optional for big repos).
4. **Per-lens finding counts (incl. explicit `CLEAN`) in the run report** — confirm "all lenses spoke" before prioritizing (the user's workflow goal).
5. **(Separate, optional) per-dir fan-out** — one agent per `(lens × dir)` for deeper/uniform reading; a DEPTH/parallelism boost, NOT the coverage fix; bounded by the concurrency cap `min(16, cores−2)` + needs #3.

**Acceptance:** a sub-total budget covers every configured lens ≥1 cell (verify via `done-cells`); a full budget completes all lenses in ONE pass with ONE global dedup; the done/pending cell-key ordering stays a deterministic resume contract after the ordering change; per-lens counts (incl. CLEAN) visible.

**Sibling — same theme, two engines.** This (audit: selected lenses don't get STARVED) pairs with 0026 §A's verify-side **loud advisory + presence-check** (a NAMED lens can't silently no-show — mechanical presence-assertion; the `testing` lens is mandatory on a test-diff; relevant-but-unselected lenses surfaced as a loud, *model-upheld* advisory — NOT a mechanical completeness gate, which was rejected). Together = **"every relevant lens actually runs and reports"** = the harness's core trustworthiness. Treat as one **lens-coverage integrity** theme.

**Ordering impact (RECOMMENDED — confirm):** current bug in the flagship deliverable + self-contained + clear acceptance + tests → **autonomous-tier eligible (like 0024), high-priority, do EARLY** — before the 0025-pipeline / 0026-spine rewrites (both touch `audit.js`/`verify.js` and should build on the fixed cell logic). **New order:** `0024` → **lens-coverage integrity (audit budget + verify completeness) [autonomous, early]** → `0025` → `0026`. Pull 0026's lens-completeness gate forward to pair here.

**Immediate adopter unblock (NOT a harness change; done in the ADOPTER session, not this repo):** re-run that audit at `maxCellsPerRun: 64` (full pass) or resume from the fence — all 8 lenses report now, IF the adopter's synthesis fits 64 outputs (else resume in chunks). The engine fix makes this the reliable default.

---

## Build execution — bootstrapping discipline → EXTRACTED to `0026` §0

The "active tools ≠ source" bootstrapping discipline (build machinery stays on installed v0.2.4; validate engine/agent edits via the test suite, never by live-spawning the half-edited repo-local `engine/*.js`; edit agent+engine+tests in lockstep; republish only at the END) is **governance for the whole multi-plan build** and now lives in `.claude/plans/0026-conceptual-spine-and-roster.md` §0. **Per-plan risk:** `0024` + the lens-coverage **integrity work** (this plan's audit cell-budget fix + `0026` §A's presence-check + test-diff rule) touch no agent-spawn ids → low bootstrapping risk; `0025`-rest / `0026` §C edit agents + engine → full discipline.

---

## Decision-gated autonomy + async flags (refine the autonomy ladder)

**Today:** `WORKFLOW.md:133` has a BINARY ladder — *checkpoint* (3 routine pauses) vs *build-to-green*. Crude: checkpoint over-stops on ceremony; build-to-green under-surfaces (no async signal). The build skill *aspires* to "pause only for the decisions that are yours" but lacks the mechanism.

**The refined model — decision-gated:**
- **STOP (blocking) ONLY for must-decide:** (a) a genuine design fork (multiple valid options); (b) a spec with a real trade-off to accept; (c) an irreversible/outward action (commit-to-shared · republish · delete user content). **Class (c) is a HARD stop the FLAG path can NEVER absorb** — even on low confidence (review sharpening). The flag-and-surface safety net (below) covers only **reversible** judgments.
- **RESEARCH, don't ask** — a FACTUAL/technical uncertainty the agent can resolve from official/trusted sources is resolved by research (cited), **never a user stop**. Stops are for the user's PREFERENCE/JUDGMENT, not facts the agent can look up.
- **FLAG (non-blocking, REVERSIBLE only)** — a should-know (a judgment call · accepted risk · deviation · low-confidence choice) whose cost if wrong is **reversible**: record a one-line flag + the **chosen default** + CONTINUE. Async-reviewable. (A low-confidence *irreversible* call is class (c) → STOP, never FLAG.)
- **SURFACE flags at the close** — the close-out lists all flags ("things to review") so the user reviews async, not mid-run.

**Surfaces:** `docs/claugentic-WORKFLOW.md` (replace the binary ladder with the decision-gated model + flag mechanism) · `skills/build/SKILL.md` (mode handling + the close-out flag summary) · the plan file's **Status block as a `Flags:` sub-line** (the running flag list — kept DISTINCT from the 0024 disposition field, not a parallel status model; review sharpening) + `.claude/plans/TEMPLATE.md`.

**Acceptance:** an autonomous run completes without stopping for anything that isn't a genuine user-action; every non-blocking judgment is flagged with a chosen default + surfaced at close; a factual uncertainty triggers research (cited), not a stop; the three blocking-stop classes always stop.

**Honesty:** model-upheld (the agent JUDGES "is this a real choice?"), not a mechanical gate — the bar is the criteria + examples. A wrong "proceed" is bounded by flag-and-surface (the user sees it at close), so a mis-judgment costs a flagged item to review, not a silent action. **Bootstrapping note:** this run encodes the behavior manually in its kickoff; landing this feature makes it the built-in default for all future runs.

---

## Review  _(filled by plan-reviewer, Stage 3 — after the outline is fleshed out)_

> **Scope of this review:** the **★ Audit lens-coverage integrity** section ONLY (the step-2 audit cell-budget fix). The rest of 0025 (the finder pipeline, doctor, decision-gated autonomy) is still a draft outline and is NOT reviewed here — it needs its own Stage-3 pass once fleshed out. The companion verify-side gate (0026 §A) is reviewed in that file.

- **Verdict (★ audit cell-budget fix only):** `CHANGES REQUIRED`

### Ground truth established (read against the code)
- **Bug CONFIRMED exactly as described.** `enumerateCells` (audit.js:316-331) is module-major (`for module { for dir }`); `applyCellBudget` (audit.js:336-341) is a flat `slice(0, N)`. With `N < total`, the prefix consumes the first modules' cells fully and the tail modules get **zero** — whole-lens starvation, not mid-lens truncation. The `done-cells` resume contract is verbatim cell-key tokens; tests pin module-major order (audit.test.mjs:282-290, 694-723).
- The documented promise it violates is real: SKILL.md:184-185 (*"All relevant lenses run at every level — depth, never lens-count, is the lever"*) and DECISIONS.md:37 (the cap "bounds … the orchestrator's synthesis context"). So the fix genuinely *aligns the engine with the stated contract* — it is a `bug` tag, reproduce-first (a failing test that captures starvation, then the fix).

### Required changes
1. **The resume-ordering contract is the load-bearing risk — make it the acceptance gate, not a footnote.** Interleaving changes the order cells are enumerated, but the contract is `doneCells` is a **set** the next run subtracts (`enumerateCells` line 317: `done = new Set(...)`; line 322 skip-if-done). So the contract survives a reorder **only if** re-enumeration is *deterministic and stable* — the same `(modules, scopeDirs, doneCells, dial)` inputs must always yield the same ordered remainder, and the round-robin must be a pure function of the input order (no set-iteration-order dependence, no sorting that varies). The plan must state this as a hard invariant and the spec must pin it with a test: *enumerate → take N → feed the N back as doneCells → enumerate again → the remainder is exactly the original tail, in the original interleaved order.* Without that test the reorder is unverifiable. (Note: the blindspot pseudo-cell is appended LAST today and tests pin "strictly last" — interleaving must keep blindspot last, or those pins (audit.test.mjs:694-723) break.)
2. **Split prong #1 (interleave + per-lens floor) from prongs #2-#3 (auto-size + hierarchical synthesis) — only #1 is the bug fix; #2-#3 are a separate, larger change and partly YAGNI.** Prong #1 alone closes the reported starvation (a budget-limited pass covers every lens's top dirs) and is a tight, test-guarded, single-session slice. Prongs #2 (auto-size) and #3 (hierarchical synthesis = per-lens roll-up → global dedup) are a **synthesis-architecture change** — they touch the PRUNE phase's single-synthesis seam (audit.js:1192-1207), the `dedupFindings`/`applyPrune`/`mergePriorItems` contract, and the run-report. That is not "fix the prefix-slice bug"; it is "raise the synthesis ceiling," with its own risk surface and its own tests. **Bundling #3 into step 2 fails the session-sized + vertically-complete gate** (it would land a partial synthesis refactor). Recommendation: **step 2 = prong #1 (+ #4 below). Defer #2 + #3 to a ROADMAP item** (auto-sizing is a real ergonomics win but is YAGNI for closing *this* bug; hierarchical synthesis is only needed once a real repo exceeds the single-pass synthesis ceiling — build it when that's measured, not speculatively). Prong #5 (per-dir fan-out) is already flagged optional/separate — keep it out.
3. **Per-lens finding counts incl. CLEAN (prong #4) needs a design decision the plan skips: a lens that ran and found nothing returns `lensVerdict: "CLEAN"` with `findings: []`, but a lens that NEVER RAN (failed batch → cells pending) currently leaves no trace in the result except `pendingCells`.** The "confirm all lenses spoke" report must distinguish *ran-and-clean* from *never-ran* (the same ran-vs-unrun honesty `verify.js` `coverageGaps` already enforces). Specify where the per-lens count is carried in the structured result (a new `lensCoverage` array keyed by module, with state ran-clean | ran-found-N | pending) and pin it with a test. This is small and belongs in step 2 — it is the user's actual workflow goal ("did every lens speak?").
4. **State the per-lens floor's interaction with the cap explicitly.** "≥1 cell per configured lens" can exceed `maxCellsPerRun` when `lenses > maxCellsPerRun`. Decide and document the rule: does the floor *win* (run all lenses' top cell even past the cap, accepting a slightly larger synthesis batch) or does the cap *win* (defer some lenses' floor to resume)? The plan says "starvation structurally impossible" — that only holds if the floor wins, which then makes `maxCellsPerRun` a soft cap. Make the trade explicit; it changes the synthesis-context guarantee the cap exists to protect.

### Sizing / completeness check
- **★ Audit cell-budget fix as currently scoped (prongs #1-#5): SPLIT NEEDED.** Prongs #1 + #4 = step 2 (one session, vertically complete, test-guarded). Prongs #2 + #3 → a separate ROADMAP item (synthesis-ceiling work, built when measured). Prong #5 → already optional, keep deferred. As split, step-2 lands complete with no debt.
- **Bootstrapping CONFIRMED low-risk:** the fix touches `enumerateCells`/`applyCellBudget`/result-assembly helpers only — **no `nsAgent("<role>")` spawn id changes** (verified against audit.js:1119-1135 spawn sites and the `agent-namespace`/`cross-script` pins). Test-suite validation suffices; no mid-step republish. The user's claim holds.

### Harness impact
- No new STANDARD/agent. Add a DECISIONS line recording the cell-enumeration reorder + the resume-stability invariant (and, if prong #4 lands, the `lensCoverage`-in-result contract) — this is a load-bearing invariant (re-enumeration determinism) and a candidate for `docs/claugentic-INVARIANTS.md`, not just DECISIONS.
- The fix is itself a Stage-9 lesson→gate example: the cap-starvation bug was model-upheld coverage that a structural check now makes mechanical — note it as a worked (b)-row at Land.

---

## Finder-pipeline + autonomy design review (Stage 3, 2026-06-25)

> **Scope of THIS review:** the *Design decisions (resolved 2026-06-25)* section, the *Decomposition* Slices 1–7, and the *Decision-gated autonomy* section. The ★ audit cell-budget fix (LANDED, step 2) was reviewed above and is **not** re-reviewed here.

- **Verdict:** `CHANGES REQUIRED` — the design is largely sound and the resolved decisions hold against the code, but three things gate a spec: Slice 4 is over-scoped (must split), Slice 3's `plan-design-review` mode double-touches the exact agents/tests 0026 §C rewrites (sequence or move), and the Stage-2b advisory panel is a YAGNI/honesty risk as written (it can be read as "run the panel on every plan").

### Ground truth established (read against the code)
- **(a) Filter-before-render SELECT seam — VERIFIED SOUND.** `engine/audit.js:1387-1404` returns the structured `items` and `renderedBacklog` separately; `renderBacklogFence(result)` (audit.js:1038-1056) filters `result.items` by tier internally. So the SKILL can filter `result.items` to the selected subset and call `renderBacklogFence({ ...result, items: selected })` with **zero engine change** — the plan's "minimal engine change / optional `selectedIds`" framing is correct, and the "optional `selectedIds`" is genuinely unnecessary (YAGNI — drop it; SKILL-side filter is strictly simpler). **Caveat the spec must handle:** `result.lensCoverage` and `verification`/`refutedCount` are computed over the FULL finding set; if the SKILL filters `items` but passes the unfiltered `lensCoverage`/run-report through `renderBacklogFence`, the rendered fence will claim coverage/counts for findings the user dropped. Decide and pin: SELECT filters the *backlog items* but the *coverage/run-report* must still reflect the full audited scope (honest "we looked at all of this; you chose to act on these") — say so explicitly, or the fence mis-reports.
- **(b) Checklist-not-AskUserQuestion — SOUND.** The candidates already render as tiered markdown lines (`renderTier`); a `- [ ]` checklist reuses that form and scales past AskUserQuestion's ~4-option cap. Workable. The spec must state *where* the checklist is presented (the fence is written to ROADMAP.md; the SELECT checklist is a transient conversational artifact BEFORE the fence write, not a second fence) so it doesn't accidentally persist an unticked checklist into the managed fence.
- **(c) Bugs as a 3rd candidate source — COMPOSES CLEANLY.** `skills/build/SKILL.md:601-604` already models the durable **Bugs** section as a planless one-liner whose *selection later triggers its own plan* — exactly the "commitment triggers the plan" semantics Slice 5 wires. No conflict; this is wiring an existing model into the SELECT reader. Good.
- **(d) doctor's bounded mechanical-treat set — boundary is crisp ENOUGH but one item leaks.** "bounded ∧ reversible (git history) ∧ no-architectural-decision" is a usable rule. *Delete a landed/cold plan*, *re-wire the pre-commit hook*, *tree hygiene add/drop entry* clearly satisfy it. **But "apply an approved doc-condensation diff for an over-budget ledger" is NOT reliably no-architectural-decision** — condensing DECISIONS/ROADMAP is a judgment about *what is still load-bearing* (the WORKFLOW:124 condensation discipline is explicitly model-upheld). It's reversible via git, but it embeds a content decision. Keep it in the "treat" set ONLY because the diff is **user-approved before apply** (the approval IS the decision gate) — but the plan must say the boundary that makes it safe is *user-approval-of-the-specific-diff*, not *no-architectural-decision*. Tighten the wording or doctor will silently condense on a future "auto-treat all" reading.

### Required changes
1. **Split Slice 4 — it bundles a documented contract AND a behavioral helper across all three finders; that is two vertically-complete units, not one.** Slice 4 as written delivers (i) the shared finder-contract *documentation* in WORKFLOW.md, (ii) a *helper the skills invoke*, and the per-selected-item PLAN generation (architect-pass + `plan-reviewer`) — and it is the dependency root for 5/6/7. The PLAN-generation step alone is the heaviest part: it spawns the plan-mode draft + the Stage-2b panel + `plan-reviewer` per selected item, and it is the seam the whole pipeline rests on. Split into **4a — the shared contract + SELECT mechanics** (the documented WORKFLOW section + the checklist→filtered-render wiring, *no plan generation yet*; this is what Slice 5 actually needs to retrofit audit/product's SELECT-before-render) and **4b — the preemptive-PLAN + OFFER-BUILD helper** (commitment→plan-on-select, the build-now/leave-in-backlog offer; depends on 4a + Slice 2 + Slice 3's panel mode). Slice 5 then depends only on **4a** (it only needs SELECT-before-render), and 6/7 depend on **4b**. This unblocks Slice 5 earlier AND makes each unit a clean session. Without the split, Slice 4 risks landing a half-wired pipeline (contract documented, plan-gen stubbed) — the exact half-done state the gate forbids.
2. **Resolve the Slice 3 ⇄ 0026 §C double-touch BEFORE spec'ing Slice 3 — do not land a `plan-design-review` mode here that §C immediately rewrites.** Slice 3 adds a `plan-design-review` mode to `lens-reviewer` and reuses the verify-panel machinery; 0026 §C (§C4) *renames/re-modes `lens-reviewer`* (whole-scope blindspot mode), §C3 *merges `plan-reviewer`+`architect-reviewer`→`synthesizer-gate`* (the Stage-2b panel's synthesizer), and §C's affected-files list (0026:112-119) explicitly edits `engine/verify.js` for "the `lens-reviewer` plan-design-review mode (composes with 0025 Stage-2b)" plus every `agent-namespace`/`cross-script` pin. So the Stage-2b panel's two key agents (`lens-reviewer` and its synthesizer) are **both** being reworked in §C. Landing a full `plan-design-review` engine mode + new pins in Slice 3, then re-touching the same prompt/spawn-id/pins in §C, edits the same surface twice and risks pin churn. **Concrete call:** make Slice 3 **doc-only** (expand WORKFLOW Stage 2 into 2a/2b/2c as *prose process*, prose-convened panel — which is all WORKFLOW:36 currently claims anyway) and **move the `plan-design-review` ENGINE mode + its pins into 0026 §C** (§C already owns the verify.js plan-design seam and the roster rewrite — let it author the mode once, post-rename, against the final agent ids). This removes the lockstep hazard entirely and matches the plan's own escape hatch ("or land the mode minimally here and let §C absorb it") — pick the *doc-only* fork explicitly, don't leave it ambiguous in the spec.
3. **Right-size the Stage-2b advisory panel — it must NOT read as "run the panel on every plan."** WORKFLOW:35 (the effort-dial) and WORKFLOW:36 (diverse critics) already gate fan-out on the *substantial* triggers; an unconditional 2b panel on every plan contradicts both and is the YAGNI risk `yagni-sentinel` exists to catch. The plan gestures at this ("Scoped + dial-able · trivial changes keep the lightweight path") but the Slice 3 box says "expand Stage 2 into the 3-step sub-process" without binding 2b to the same dial. **State in the spec:** 2b fires only on the *same Stage-0 substantial triggers* (shared contract / security boundary / ~8+ files / trust surface); a small/local plan stays 2a→3 with no panel. Otherwise this re-introduces the "run the whole machine on a trivial change" velocity-killer WORKFLOW:35 explicitly warns against.
4. **doctor's diagnose-set (Slice 7) must declare its read-only guarantee and its INVARIANTS-cap dependency explicitly.** Slice 7 runs `check_doc_budgets.py` "(incl. the new INVARIANTS cap)" — but the INVARIANTS file is created *lazily* (WORKFLOW:162, only on the first real invariant) and the cap may not exist yet in `check_doc_budgets.py`. Spec must confirm whether the INVARIANTS cap is in-scope of Slice 7 (then it's a `check_doc_budgets.py` change = a fourth ledger, a real code edit + test, not just "run the existing script") or out-of-scope (drop the parenthetical). As written it smuggles a script change into a "run the existing gates" framing. Also: the diagnose phase must be **strictly read-only** (no mutation before the multiselect/approval) — state that as an acceptance criterion so a future "treat" reading can't bleed into diagnose.
5. **Tighten the doc-condensation treat boundary (per ground-truth (d) above).** State that doc-condensation is in the treat set because the *diff is user-approved before apply* (approval is the decision gate), not because condensation is decision-free — it is not.

### Sizing / completeness check
- **Slice 1 (decision-gated autonomy) — OK.** Doc/process + skill + TEMPLATE Flags block; self-contained; the session is dogfooding it. One caveat (see autonomy notes below): the TEMPLATE "Status/Flags block" must reconcile with the EXISTING `Status:` line (TEMPLATE.md:3) and the 0024 disposition block (TEMPLATE.md:6) — add a *Flags* sub-list, don't fork a parallel status model. Lands complete.
- **Slice 2 (architect-pass TEMPLATE section) — OK, and correctly sequenced early/shared-with-0026.** Adds one TEMPLATE section + the Plan-agent mandate + a `plan-reviewer` checklist line. Vertically complete. Note 0026:117 and 0026:153 both assert "land once, shared with 0026" — keep the single-authorship invariant explicit in the spec so §C does not re-author it.
- **Slice 3 (Stage-2 sub-process) — SPLIT/RESCOPE (req. change 2+3):** land the *doc* process here; move the `plan-design-review` engine mode + pins to 0026 §C; bind 2b to the substantial-trigger dial. As doc-only it lands complete with no pin churn.
- **Slice 4 (shared contract) — SPLIT NEEDED → 4a (contract + SELECT) / 4b (preemptive-PLAN + OFFER-BUILD)** (req. change 1). Each half is then session-sized and vertically complete; Slice 5 re-points to 4a.
- **Slice 5 (audit + product SELECT retrofit) — OK once it depends on 4a.** The filter-before-render seam is verified buildable; the only addition is the coverage/run-report honesty caveat (ground-truth (a)) and wiring Bugs as the 3rd source (composes per (c)).
- **Slice 6 (build from pre-made plan + JIT spec + Stage ordering) — OK, depends on 4b.** Confirm the "no spawn-id change" claim against `engine/build-item.js` at spec time (the plan says "confirm" — make it a spec acceptance line, not a TODO).
- **Slice 7 (doctor) — OK as a slice IF req. change 4 resolves the INVARIANTS-cap scope.** doctor is a new skill on the shared contract reusing existing gate scripts + the SELECT/treat/offer-build flow; that is one coherent vertical. It is NOT too big to split *provided* it does not also have to build the INVARIANTS cap into `check_doc_budgets.py` — if it must, that ledger-cap is a separable prior slice. Resolve in spec.

### 0026 §C coordination (the real risk) — the call
- **Slice 3:** **move the engine mode to §C; keep Slice 3 doc-only** (req. change 2). 0026 §C already lists the `lens-reviewer` plan-design-review mode in its affected-files (0026:115) and rewrites `lens-reviewer`'s modes + the synthesizer it feeds — authoring the mode *here* then *there* is a guaranteed double-touch of `engine/verify.js` + `agent-namespace`/`cross-script` pins. Doc-only Slice 3 has zero spawn-id surface and zero pin risk.
- **Slice 7 ⇄ 0026 §C5 `retrospect-harvester`:** the plan and 0026:156 both flag this. doctor's "did the harvest fire?" check and `retrospect-harvester` are **two readers of the same Stage-9 signal**. Pick one OWNER now: recommend `retrospect-harvester` (0026 §C5) owns the *active* Stage-9 harvest, and **doctor only REPORTS "a recent land lacks a DECISIONS/INVARIANTS/standards touch" as a passive health flag** (it does not harvest). State that boundary in both plans so they don't both try to drive harvesting. Since 0025 builds before 0026 (step 3 vs step 4), Slice 7's doctor should ship the *report-only* signal and 0026 §C5 wires the *actor* — no double-build, just a named seam. Acceptable as sequenced.
- **Net:** with Slice 3 doc-only and Slice 7 report-only-on-the-harvest-signal, the §C overlap is clean. Do NOT pull more of 0025 into §C beyond the `plan-design-review` engine mode.

### Decision-gated autonomy (Slice 1)
- **Sound to replace the binary ladder.** The decision-gated model (STOP only for the 3 must-decide classes · RESEARCH-don't-ask · FLAG+CONTINUE · SURFACE-at-close) is a genuine refinement and is consistent with WORKFLOW:36's "pause only at the decisions that are yours" aspiration and build SKILL.md:31-39's three-pause model. The three blocking-stop classes map cleanly onto the existing three checkpoint pauses (spec / before-land / irreversible) — frame them as the *same* three, refined, not a new parallel set, or the build skill ends up with two stop-models.
- **FLAG/surface integration with 0024 disposition + Status block — workable, but specify the home.** The "running flag list" needs a concrete location. Put it in the plan file's Status block (TEMPLATE.md:3-6) as a **Flags:** sub-line, NOT a new top-level block that competes with `Status:`/`Resumable from:`/`Disposition`. A flag is a *non-blocking judgment to review async* — it is distinct from a 0024 *disposition* (done/defer/reject of an unbuilt item); keep them separate fields with one line each so the close-out can surface flags AND dispositions without overlap.
- **Honesty framing — correct, with one sharpening.** The plan rightly labels this model-upheld ("the agent JUDGES 'is this a real choice?'") and bounds a mis-judgment to "a flagged item to review." Good. **Sharpen:** a wrong FLAG-instead-of-STOP on an *irreversible/outward* action is NOT recoverable by async review (the action already happened) — so the spec must make explicit that the irreversible class (c) is a HARD stop that the FLAG path can never absorb, even on a low-confidence judgment. The flag-and-surface safety net only covers reversible judgments; say so.

### YAGNI check
- **The optional `selectedIds` engine filter — DROP IT.** SKILL-side filtering of `result.items` is strictly simpler and needs no engine change (ground-truth (a)). The plan already says "only if cleaner"; the answer is it isn't.
- **Stage-2b panel on every plan — guarded correctly only if bound to the dial (req. change 3).** As written the Slice 3 box risks reading as unconditional; bind it to the substantial triggers.
- **Architect-pass section — NOT gold-plating, and the yagni-sentinel guard is the right control.** The section is "initial architect thoughts/placeholders," plans are ephemeral (deleted at Land), and the section *frames what to uphold* rather than mandating abstractions. The plan correctly enlists `yagni-sentinel` to keep it from becoming a build-everything mandate. The one real risk is the section becoming checklist-theater (hand-waved to satisfy the template) — the `plan-reviewer` "genuinely reasoned, not hand-waved" audit line (Slice 2) is the right mitigation; keep it.
- **doctor scope — appropriately bounded.** Report-only default, treat-on-approval, no new hook, no new always-loaded doc, "Maintenance section only if volume warrants" (explicit YAGNI). Good restraint; do not expand it.

### Harness impact
- **New agent mode (`lens-reviewer` plan-design-review):** route to 0026 §C (req. change 2) — do not create it in 0025.
- **New skill (`doctor`) + new command (`/claugentic-dev-harness:doctor`):** Slice 7 adds a plugin command — update `plugin.json`/`marketplace.json` command roster + `docs/claugentic-ARCHITECTURE_TREE.md`; the version-sync gate applies (harness-self). Name it in the spec.
- **WORKFLOW.md autonomy-ladder rewrite (Slice 1) + Stage-2 sub-process (Slice 3) + Stage-ordering (Slice 6):** three slices edit WORKFLOW.md; since it is a managed doc that re-init propagates to adopters, sequence them so the doc is coherent at each land (each slice leaves WORKFLOW internally consistent, no "Stage 2 references 2b that doesn't exist yet").
- **DECISIONS lines:** the decision-gated-autonomy model, the SELECT-gate-before-render seam, and the doctor treat-boundary are each non-trivial choices → one dated DECISIONS line each at Land. The "flag-and-surface bounds a mis-judgment EXCEPT on irreversible actions" rule is a candidate INVARIANT.

---

## Spec — Slice 4a (Stage 4, 2026-06-25): Shared finder contract + SELECT-before-render

**What this builds (plain English):** a single documented *finder pipeline* that the three finders (`audit`, `product` gap-mode, `doctor`) all follow — so finding→doing stops leaking effort and bloating the backlog — plus the detailed mechanics for its **SELECT** step (the user picks what to act on *before* anything is written to the backlog). **Doc-only, in `docs/claugentic-WORKFLOW.md`. Zero engine change, zero skill edit** (the per-skill SELECT retrofit into audit/product is Slice 5; the PLAN/OFFER-BUILD wiring is Slice 4b/6). This slice is the DRY single source the later slices point at.

**What "done" means:** WORKFLOW.md carries a coherent *The finder pipeline* section documenting the FIND → SELECT → PLAN → OFFER-BUILD → BUILD contract, with SELECT fully specified (the editable-checklist mechanics, filter-before-render, the skip-vs-reject distinction, and the full-scope-coverage invariant). All deterministic gates green; `honesty-reviewer` CLEAN (no over-claim — SELECT is a model-upheld user action, not a mechanical gate). The doc stays internally consistent (no reference to a Stage/skill behavior that contradicts what exists).

**Trade-offs accepted:** the section documents the *target* unified contract while only SELECT is fully wired this slice (PLAN/OFFER-BUILD/BUILD are described at contract level, their skill wiring lands in 4b/5/6) — consistent with how Stage 2's sub-process was documented as prose (Slice 3) before any engine wiring. WORKFLOW.md is a managed doc → re-init propagates this to adopters (audit + product gap-mode run in adopter repos, so the SELECT gate benefits them).

### File-by-file

**`docs/claugentic-WORKFLOW.md`** — insert a new `## The finder pipeline (audit · product · doctor → backlog → build)` section, placed **immediately before `## Executing an audit backlog item — tag → discipline`** (after the Decision-gated-autonomy section's closing `---`, currently line 163), so the narrative reads finder → SELECT → backlog → build, then how each backlog item's tag selects its discipline. Section contents:

1. **The shared contract — `FIND → SELECT → PLAN → OFFER-BUILD → BUILD`.** One paragraph: three finders surface candidate work — `audit` (engineering, vs standards), `product` gap-mode (vs the spec), `doctor` (harness-health) — and share ONE lifecycle so effort isn't spent elaborating items the user never wanted. Name the five steps; mark which are detailed here (SELECT) vs the continuing contract (PLAN/OFFER-BUILD/BUILD, wired by the build pipeline).

2. **FIND.** The finder runs its sweep and returns a **candidate list** (the engine's structured `items` for audit/product; doctor's diagnose rows). The sweep's coverage / run-report is computed over the **whole** scope (this matters for the SELECT invariant below).

3. **SELECT — the scope gate (fully specified here):**
   - **Read the dismissal memory first.** Before presenting, read the finder's rejected-memory fence (`<!-- harness-audit:rejected-findings -->` for audit, `<!-- product-critic:rejected-proposals -->` for product) and **omit any already-dismissed candidate** — a dropped item stays dropped (this is the existing audit/product behavior, now named as a contract step).
   - **Present remaining candidates as an editable markdown checklist** — `- [ ]` per candidate, **reusing the rendered backlog-item form** (the same `renderItem` shape audit/product already produce) so it scales to N candidates (AskUserQuestion caps at ~4). The checklist is a **transient conversational artifact** presented *before* the fence is written — **never persisted into the managed `harness-audit:backlog` / `harness-product:backlog` fence**. Unchecked by default.
   - **The checked subset is the SELECT result.** Unchecked candidates are **dropped for this run** — not written to the backlog (the anti-bloat gate).
   - **Skip ≠ reject (state the distinction explicitly).** A simple *skip* (left unchecked) is **per-run** — it is NOT recorded in the durable reject-memory and MAY resurface on a later finder run. **Explicit rejection** ("this finding is *wrong*", not "not now") is the stronger, existing action: append one terse line to the finder's rejected-memory fence so it stays dropped across future runs. SELECT offers the user this option but defaults a non-tick to *skip*, not *reject* (rejection is a deliberate user override of model judgment, never inferred from a non-tick).
   - **Render from the selected subset only — zero engine change.** Filter the engine's `result.items` to the ticked subset and render the fence from it: `renderBacklogFence({ ...result, items: selected })`. `renderBacklogFence` already filters `result.items` by tier internally (audit.js:1038-1056), so passing a pre-filtered `items` is all it takes — **no engine edit, no `selectedIds` arg** (dropped per Stage-3 review #3 / YAGNI check).
   - **★ INVARIANT — coverage / run-report stay FULL-SCOPE.** The `lensCoverage` and `verification`/run-report fields handed to the renderer are the **unfiltered, full-audit** values. The fence reports honestly *"we looked at all of this; you chose to act on these"* — never recompute coverage over the selected subset (that would falsely claim "every lens spoke" for only the kept findings). Because the SKILL filters **only** `items` and passes the rest of `result` through unchanged, this is automatic — but **state it as an invariant** so a future edit doesn't also filter `lensCoverage`/`verification`. (This is the verify-side honesty sibling of the audit cell-coverage fix.)

4. **PLAN / OFFER-BUILD / BUILD (the continuing contract — wired later).** Briefly, at contract level: a SELECTed (committed) item gets its full plan preemptively via Stage 2 (the architect-pass) → OFFER-BUILD asks "build now or leave in the backlog?" (default: leave) → BUILD works selected items one at a time, JIT per-slice Spec→Approve→Implement→Verify. **Commitment, not capture, triggers the plan** (selecting = committing = the Stage-2 trigger; a raw Bug/Later one-liner stays planless until selected; trivial/mechanical items skip the backlog entirely and are just done). *(Mechanics + skill wiring land in 4b/5/6 — describe, don't over-specify.)*

5. **Three candidate sources feed SELECT** (named at contract level): the `harness-audit:backlog` candidates, the `harness-product:backlog` candidates, and the durable **Bugs** section (planless one-liners; selecting one commits it). *(The Bugs-as-source reader is wired in Slice 5.)*

**Cross-reference touch-ups (keep the doc coherent, minimal):** if the new section makes any existing singular "the backlog" reference ambiguous, leave the existing copy as-is unless it now reads as wrong — do not refactor neighbouring sections (Slice 5/6 own the audit/product/build wiring). No Stage-table change in this slice (Stage ordering is Slice 6).

### Acceptance criteria
- WORKFLOW.md has the `## The finder pipeline …` section in the stated position; FIND → SELECT → PLAN → OFFER-BUILD → BUILD named; SELECT fully specified with all five mechanics bullets (dismissal-memory-first · editable checklist reusing the rendered item form · checked-subset result · skip-vs-reject distinction · filter-before-render with zero engine change · full-scope-coverage invariant).
- The skip-vs-reject distinction is explicit (a non-tick is per-run skip, NOT durable rejection).
- The full-scope-coverage invariant is stated as an invariant (coverage/run-report reflect the whole audit, not the selected subset).
- "Zero engine change / no `selectedIds`" is stated.
- No skill file, engine file, or test is edited in this slice.
- Deterministic gates green (`node --test tests/workflows/*.test.mjs`, `python -m pytest`, tree-check, version-sync, doc-budgets). honesty-reviewer CLEAN. architecture tree unchanged (no file add/move/remove).

### In-scope standards dimensions (reviewer bar)
- `docs/claugentic-standards/` — **documentation/clarity** + **honesty** (the #1 rule: SELECT is model-upheld; the full-scope-coverage line must not over-claim). Primary reviewer: `honesty-reviewer` (copy/over-claim) + a coherence read. No code lens (doc-only).

### Notes for the implementer (bootstrapping)
- Edit `docs/claugentic-WORKFLOW.md` **in this worktree directly**; do NOT create a sub-worktree, do NOT commit/push (the orchestrator commits after Verify). Touch only WORKFLOW.md. Keep the section **dense** (harness discipline: index, don't duplicate — don't restate renderBacklogFence's body, point at `engine/audit.js`).

---

## Spec — Slice 4b (Stage 4, 2026-06-25): Preemptive PLAN + OFFER-BUILD

**What this builds (plain English):** the **PLAN** and **OFFER-BUILD** steps of the finder pipeline — the reusable mechanics that turn a *committed* (SELECTed) finding into specialist-produced groundwork and then offer to build it, so finders stop dead-ending into a manual `/build`. **Doc-only, in `docs/claugentic-WORKFLOW.md`** — it expands the contract-level `PLAN / OFFER-BUILD / BUILD` paragraph the finder-pipeline section already carries (currently ~`WORKFLOW.md:182`) into detailed, DRY mechanics. **The skill wiring lands later** — Slice 6 wires `build` to consume pre-made plans + present the offer; Slice 7 wires `doctor`. This slice is the single source those point at.

**What "done" means:** the finder-pipeline section's PLAN/OFFER-BUILD content is the detailed reusable procedure (PLAN via the Stage-2 architect-pass + `plan-reviewer`; the plan-volume bound; OFFER-BUILD default-leave + the finder→build continuity). Gates green; `honesty-reviewer` CLEAN (the architect-pass plan is reviewed groundwork, NOT a quality guarantee; build is **offered, never forced**). Doc stays coherent.

**Trade-offs accepted:** documents the target PLAN/OFFER-BUILD contract while the skill wiring is 6/7 (consistent with 4a→5). No skill/engine behavior changes this slice.

### File-by-file

**`docs/claugentic-WORKFLOW.md`** — in the existing `## The finder pipeline …` section, **expand the contract-level `PLAN / OFFER-BUILD / BUILD` paragraph** (the one currently sketching it "wired by the build pipeline") into a structured subsection. Keep BUILD at contract level (Slice 6 owns its detail). Content:

- **PLAN (per committed item — architecture-first, not a stub).** When an item is **committed** (SELECTed to act on), its **full plan file** is produced via **Stage 2** — the architect-pass: 2a plan-mode draft → 2b advisory panel (substantial work) → 2c incorporate → the Stage-3 `plan-reviewer` gate — structured by `TEMPLATE.md`: problem · **Architecture & holistic fit** · approach · decomposition · the finding's evidence (`file:line`), plus a one-line ROADMAP entry. The plan is **specialist-produced groundwork**, so build never re-derives context from scratch.
  - **The deep per-slice SPEC stays JIT** (produced inside build as it reaches each slice — Stage 4): per-slice spec is the step-pruning gate and is cheaper JIT than deep-speccing every slice upfront. "Preemptive PLAN" means the *plan* exists before that item is built (**architecture-first**), NOT that every slice is deep-spec'd upfront.
  - **★ Plan-volume bound (anti-"dozens of plans at once").** **build-now** items are planned **one at a time as build works them** (the build loop is serial → at most one fresh plan file at a time); **backlog** items get only a **one-line fence/ROADMAP entry**, planned **just-in-time when a later `/build` reaches them**. Either way the plan precedes that item's slices (architecture-first per item), never all upfront — a large multiselect never spawns dozens of ephemeral plan files.
- **OFFER-BUILD (the finder→build bridge — fixes the dead-end).** After SELECT the finder run **continues** rather than dead-ending: ask — a small binary moment, so **AskUserQuestion** (not the checklist) — *"build these now, or leave them in the backlog?"* **Default = leave in the backlog** (build is **offered, never forced** — non-goal: forced auto-build).
  - **Build now** → enter the build loop (Slice 6 wires build to consume the pre-made plans, JIT per-slice spec).
  - **Leave** → the selected items persist as backlog lines for a later `/build`; their plans are produced JIT then. The find→do flow is now **continuous** (no manual re-invoke of `/build`).
- **Commitment, not capture, triggers the plan** — keep as the DRY one-liner already in the contract (selecting = committing = the Stage-2 trigger; a raw Bug/Later one-liner stays planless until selected; a trivial/mechanical item skips the backlog and is just done). Do not restate it twice.

**Honesty:** all of PLAN/OFFER-BUILD is **model-upheld** (the orchestrator/skill runs the architect-pass + presents the offer; nothing mechanical forces a plan or a build). The architect-pass plan is **reviewed groundwork (`plan-reviewer`-gated)**, never a guarantee of quality — word it that way.

### Acceptance criteria
- The finder-pipeline section's PLAN/OFFER-BUILD content is the detailed procedure: PLAN via Stage-2 architect-pass + `plan-reviewer`; the **JIT-spec vs preemptive-plan** distinction explicit; the **plan-volume bound** (build-now one-at-a-time / backlog JIT) stated; OFFER-BUILD with **default = leave**, build **offered not forced**, and the finder→build continuity.
- Doc-only; no skill/engine/test edit; no tree change; gates green (`node --test`, `pytest`, tree, version-sync, doc-budgets); `honesty-reviewer` CLEAN.
- The section remains internally coherent (BUILD stays contract-level; no contradiction with 4a's SELECT text or Stage 2's sub-process).

### In-scope standards dimensions (reviewer bar)
- documentation/clarity + **honesty** (architect-pass = reviewed groundwork not a guarantee; build offered-not-forced). Reviewer: `honesty-reviewer` + a coherence read. Doc-only (no code lens).

### Notes for the implementer (bootstrapping)
- Edit **only** `docs/claugentic-WORKFLOW.md` in this worktree; no sub-worktree, no commit/push. Dense, index-don't-duplicate. Reuse 4a's voice; do not duplicate the SELECT mechanics — expand only PLAN/OFFER-BUILD.
