# 0034 — Release tooling: DRY + hardening (one annotated manifest, provable no-op, broken-release-can't-pass guards)

- **Status:** Draft — SCOPE EXPANDED 2026-07-04 (DRY consolidation + release-hardening). The DRY track (Slices 1–3) was re-gated READY-TO-BUILD; the hardening track (Slices 4–9) is **NEW and needs a fresh plan-gate on the expanded plan before build** (see Review → *Expanded-scope note*).
- **Resumable from:** Slice 1 — not started.
- **Blockers:** none for the DRY track. The hardening track is **plan-gate-blocked** pending a fresh gate on the expanded plan.
- **Disposition at close:** completes (deleted, git history keeps it) once every slice is done / deferred (a ROADMAP line) / rejected (a DECISIONS line).
- **Roadmap item:** `docs/claugentic-ROADMAP.md` → *Release/init-contract completeness gate* (the DRY track partially delivers it) + the FINETUNING-INPUTS Track-B design; the hardening track delivers the verified release-pipeline audit's P0/P1/P2 guards (broken release can't pass every gate).
- **References:** `.claude/plans/FINETUNING-INPUTS.md` → Track B + VERIFIED section · `docs/claugentic-INVARIANTS.md` → *The release strips ⇒ init recreates ⇒ nothing dangles* · `scripts/build_release.py` · `scripts/check_shipped_content.py` · `scripts/check_versions_synced.py` · `tests/test_build_release.py` · `docs/RELEASE_CHECKLIST.md` · `.github/workflows/ci.yml` (release branch runs ZERO CI — the hardening premise).

## Problem

**Two problems in the same release tooling — a DRY problem and a hardening problem.**

### Problem A — the ship/strip partition is re-expressed by hand in ~7 places (DRY)

`build_release.py` `DEV_ONLY_FILES/DIRS` is the source of truth; it is then re-hand-partitioned three more ways in `check_shipped_content.py` (`_INIT_CREATES` · `HARNESS_SELF_SCRIPTS` · `_DANGLE_EXCLUDED`), echoed in the `init` managed-set table, and pinned again in `tests/test_build_release.py`. **Adding one dev-only doc that init recreates = 3–5 edits across 3–4 files** (0030 Slice 2 hit exactly this — `_CHARTER.md` needed the seed + the `_INIT_CREATES` entry + the test pin). Nothing cross-checks that `init` can actually (re)create everything the release strips — the release/init contract (`INVARIANTS.md`) is prose-only.

### Problem B — a BROKEN release can pass every gate today (hardening)

A verified release-pipeline audit found the publish path has holes a broken release slips through. The pipeline: `git pull --ff-only main` → `build_release.py --apply` (builds local `release`, strips dev-only, refuses a stale base) → manual `git range-diff` drop-check → manual `git push --force origin release` → marketplace `ref: "release"` (a mutable **BRANCH**, not a tag). The gaps:

- **No version-increase check.** `check_versions_synced.py` only asserts the two manifests AGREE (`plugin.json` ↔ `marketplace.json`) — never that the version moved off the last release. A forgotten bump ships a "new" release adopters see as no update. (Highest-probability silent bad release.)
- **The shipped tree runs ZERO CI.** `ci.yml` runs `on: [main]` only (`push`/`pull_request` → `branches: [main]`); the force-pushed `release` branch — what users actually install — is never CI-validated, and its commit is created with `--no-verify` (`build_release.py:198`). A break introduced by the strip transform is invisible until an adopter hits it.
- **No immutable rollback anchor.** The marketplace tracks a mutable branch; there is no `vX.Y.Z` tag to roll back to. This is precisely how the v0.1.40 distillation was lost (a stale-base rebuild + force-push, no tag to recover from).
- **The drop-check is a manual eyeball.** `git range-diff` (RELEASE_CHECKLIST.md:23-30) is the primary defense against a shipped file silently missing from the release — but it is model-upheld, not computed.
- **The force-push can silently clobber.** `git push --force origin release` overwrites `origin/release` unconditionally, even if it moved out from under the operator since fetch.
- **The stale-base guard misses a direct push.** `_dropped_merges()` (`build_release.py:149-160`) uses `rev-list --merges` — it only sees dropped *merge* commits, so a direct NON-merge push to `main` (which admin-bypass allows — see MEMORY: main branch-protection bypass) is invisible to it.

The hardening track adds guards so each of these becomes a loud, pre-publish failure instead of a post-publish surprise. **The additions naturally reuse the manifest's `classify()`** from the DRY track (the mechanized drop-check is a `classify()` subset assertion), so the two tracks compose.

## Goals / Non-goals

### DRY goals (Track A — the manifest consolidation)

- **Goal:** Make the ship/strip partition **one class-annotated manifest** in `build_release.py` — a `path → recreate-class` dict as the ONLY authored semantics; the three `check_shipped_content.py` hand-lists become **derived** from it. **The class set is SIX** (plan-gate BLOCKER fix — the original five had a taxonomy hole): `init-seed` (init copies a `_X.md` seed → DECISIONS/ROADMAP/CHARTER) · `init-gen` (init generates it → ARCHITECTURE_TREE) · **`recreate-on-demand`** (stripped, NOT init-produced, legitimately non-dangling because created on-demand by a non-init mechanism → **INVARIANTS.md** [workflow lazily creates it, DoD step (f), `WORKFLOW.md:259`] · **PRODUCT.md** [agent-authored per-project by `product-designer`; dev-only, adopters get the pattern not the file — DECISIONS:64] · **PRODUCT_SPEC.md** [user-authored from the shipped `PRODUCT_SPEC_TEMPLATE`]) · `self-gate` (stripped harness-self script) · `config` (machinery, no doc points at it) · `dangle` (stripped AND never recreated). The Slice-1 spec **maps every current `DEV_ONLY_FILES` entry to exactly one class** so the taxonomy is a provable *partition*, not a lossy re-encoding.
- **Goal:** Add the **referential-closure run-gate** (`NEEDS ⊆ HAS`): assert every stripped-adopter-relevant path is producible/recreatable. **Four HAS sources** (one per non-`config`/non-`dangle` class): `init-seed` → its `_X.md` seed ships; `init-gen` → a known init generator output; **`recreate-on-demand` → the class IS its own HAS attestation** (its members are *by design* not init-produced — the closure predicate accepts them via this class, it does NOT hard-require init-producibility for them); `self-gate` → the harness-self script's shipped/stripped status is self-consistent. This **mechanizes the INVARIANT** (`INVARIANTS.md:77-98`, prose-only today). The honesty copy says "producible by init **or the workflow's lazy/templated/agent authoring**" — never "producible by init" flatly (that would over-claim init's scope).
- **Goal:** **Provable no-op** — the shipped file set is **byte-identical before/after** (the load-bearing safety property; asserted by an equality check during migration).
- **Non-goal:** Folding `check_versions_synced.py` in — **it stays a SEPARATE gate** (verified: folding adds dual-manifest JSON parsing = scope creep + risk; the two-manifest agreement check is orthogonal). *(Corrects the dossier, which said "fold version-sync in.")*
- **Non-goal:** A version "third-drift-axis" detector — **REFUTED** (main + release both 0.3.1, in sync; the gate works as designed).
- **Non-goal:** Any adopter-facing change — this is internal harness tooling; adopters never see these scripts (all `DEV_ONLY`/stripped).
- **Non-goal:** Changing what ships. The partition's *membership* + recreate-class stays a human `[J]` judgment (one declared list); only the *re-derivation* is mechanized.

### Release-hardening goals (Track B — a broken release can't pass every gate)

Each guard converts a currently-silent bad outcome into a loud, pre-publish failure. Sized so each lands complete with a test.

- **Goal (P0-1, must-fix) — version-must-INCREASE guard.** Add a release-anchored check: read `plugin.json.version`, compare it to the version currently on `origin/release` (or the latest `vX.Y.Z` git tag), and **REFUSE if not strictly greater** (semver comparison). Lives in `build_release.py --apply` as a precondition (sibling to the existing stale-base refusal), *or* as a checklist-gated script — **decide at Spec** (prefer the `--apply` precondition so it can never be skipped, matching the stale-base guard's placement). Closes the highest-probability silent bad release (forgotten bump → adopters see no update). **This is distinct from `check_versions_synced.py`** (which checks the two manifests *agree*, an orthogonal axis) — do NOT fold the two together (see non-goal).
- **Goal (P0-2, must-fix) — validate the BUILT (stripped) tree before publishing.** Have `build_release.py --apply` run `check_shipped_content.py` + `pytest` (or a defined shipped subset) **against the throwaway built worktree** BEFORE it commits/finishes — so a break introduced by (or surviving) the strip transform fails loud pre-push rather than reaching an adopter. Closes the "release runs zero CI" hole. **Decide the exact validation set at Spec** (full `pytest` may reference dev-only `tests/` that the strip removes → run against the built tree only what remains meaningful there; the safe minimum is `check_shipped_content.py` on the built tree, which is already release-scoped, plus any shipped-subset tests that survive the strip).
- **Goal (P1-1) — tag each release** `vX.Y.Z` on the built `release` commit (`git tag vX.Y.Z release && git push origin vX.Y.Z`) as an **immutable rollback anchor** (today the marketplace tracks a mutable branch — no rollback point; this is how a prior release was lost). The tag **doubles as P0-1's compare anchor**. Delivered as a RELEASE_CHECKLIST step (optionally a `gh release`). **Pointing the marketplace `ref` at the tag instead of the branch is a bigger call — NOTE it as an open question, do NOT do it in this plan.**
- **Goal (P1-2) — `--force-with-lease`.** Change the RELEASE_CHECKLIST's `git push --force origin release` to `git push --force-with-lease origin release` — refuses if `origin/release` moved out from under the operator since fetch, converting a silent clobber into a loud rejection. (Checklist copy only; no script.)
- **Goal (P1-3) — mechanize the drop-check.** Add a **computable** assertion to `build_release.py --apply` (reusing the manifest's `classify()`): the set of paths on `origin/main`-not-`release` must be a **SUBSET of the strip set** — so a *shipped* file accidentally missing from the release fails the build. Turns the manual `range-diff` eyeball into a machine check. (The manual `range-diff` stays in the checklist as defense-in-depth for what a path-set check can't see — commit-level drops.)
- **Goal (P2-2) — broaden the stale-base guard.** `_dropped_merges()` uses `rev-list --merges` (misses a direct non-merge push to `main`, which admin-bypass allows). Broaden to catch **any** commit on `origin/main` not reachable from `HEAD` — `rev-list origin/main --not HEAD` (drop `--merges`), or assert `HEAD` is an ancestor-inclusive match of `origin/main`. Rename/re-doc the function accordingly (`_dropped_merges` → `_dropped_commits` / `_missing_upstream_commits`).

### Hardening non-goals

- **Non-goal:** Folding P0-1 (version-increase) into `check_versions_synced.py`. It **stays a separate gate** — same rationale as Track A's version-sync non-goal: version-*increase* (vs the last release) and version-*agreement* (across the two manifests) are orthogonal axes; P0-1 needs a git/release anchor `check_versions_synced.py` deliberately has no business reading.
- **Non-goal:** Repointing the marketplace `ref` from the `release` branch to the `vX.Y.Z` tag (P1-1's bigger call) — noted as an open question for a future plan; the tag ships as a rollback anchor + compare source now, nothing consumes it yet.
- **Non-goal:** A CHANGELOG. That is separate release hygiene, not this plan.
- **Non-goal:** Running CI *on the release branch* in GitHub Actions (adding `branches: [release]` to `ci.yml`). P0-2 validates the built tree **locally at build time** — pre-push, where the operator can act on a failure — rather than after the force-push. (Revisit only if local validation proves insufficient; YAGNI for now.)

## Approach

Migrate in **safety-netted steps, each green before the next** (the whole point is a provable no-op):
1. **Annotate** — convert `DEV_ONLY_FILES` into a `DEV_ONLY_PATH_CLASSES` dict (`path → class`, the **six** classes above); keep `DEV_ONLY_DIRS` (dirs stay out of the classes — the closure gate reasons over file-level classes only). Derive `is_dev_only()` / `classify()` / `recreate_class()` from the dict. **Map EVERY current `DEV_ONLY_FILES` entry to exactly one class** (the exhaustive partition — see the enumeration below) and **assert the shipped set is byte-identical** vs the pre-change `classify()` (the migration test). *(Enumeration: DECISIONS/ROADMAP/CHARTER→`init-seed` · ARCHITECTURE_TREE→`init-gen` · INVARIANTS/PRODUCT/PRODUCT_SPEC→`recreate-on-demand` · the harness-self scripts→`self-gate` · RELEASE_CHECKLIST→`dangle` · settings.json/CLAUDE.md/pyproject.toml/.gitignore/.gitattributes→`config`. Confirm the live list against `build_release.py` at Spec — the mapping is the partition-completeness check.)*
2. **Derive-alongside** — in `check_shipped_content.py`, compute `_INIT_CREATES` / `HARNESS_SELF_SCRIPTS` / `_DANGLE_EXCLUDED` from the class annotations, **alongside the old hand-lists**, and assert they're equal (the safety net). Keep both until equality holds.
3. **Delete** the three hand-lists once the derived sets prove equal.
4. **Closure check** — add `NEEDS ⊆ HAS` to `check_shipped_content.py`: derive `init_creates`/`recreate_on_demand`/`self_gates`/`dangle_set` from classes; assert every stripped-adopter-relevant path is recreatable via its class's HAS source (`init-seed`→seed ships · `init-gen`→init generator · **`recreate-on-demand`→the class itself is the attestation, NOT init-producibility** · `self-gate`→self-consistent ship/strip). **Honor the `INCLUDE_GLOBS` never-clobber carve-out** (the tree-script's glob assignment is adopter-owned — exclude it from closure exactly as init's body-compare does). May surface a real latent gap — good.
5. **Collapse the test pins** to `classify` + the closure assertion. **Migrate the `test_dangling_set_is_derived_and_excludes_gate_scripts` pin** (`tests/test_check_shipped_content.py`) so the derived dangle set still provably excludes the gate scripts — name it explicitly so Slice 2 lands complete, not with a silently-broken pin.

**`check_versions_synced.py` is untouched** (separate gate). The rename to `release_gate.py` (the dossier's naming) is **optional / deferred** — merging file names is cosmetic and risks churn; the value is the annotated manifest + closure check, which can live in the existing `check_shipped_content.py`. *(Confirm at Spec — prefer keeping the existing filename to minimize the DoD-gate-list / RELEASE_CHECKLIST / tree churn.)*

**Hardening track (Track B) — approach.** The guards land AFTER the DRY track so P1-3's mechanized drop-check can reuse the finalized `classify()`. Each guard is its own slice with its own test. Ordering: the two **script-side must-fixes** (P0-1 version-increase precondition, P0-2 built-tree validation) land first as `--apply` preconditions/postconditions; then the **guard broadeners** (P2-2 stale-base broadening, P1-3 mechanized drop-check) which both touch `build_release.py`'s pre-build assertions; then the **checklist/tag** changes (P1-1 tag, P1-2 `--force-with-lease`) which are RELEASE_CHECKLIST copy + one tagging step. Each script-side guard fails loud with an actionable message (matching the existing stale-base refusal's tone) and is unit-testable by driving `_apply()`'s precondition helpers over fixture repos/refs — extend `tests/test_build_release.py`. **The `[J]`/model-upheld split is preserved:** the mechanical guards (version-increase, built-tree validation, mechanized drop-check, broadened stale-base) fire in-build; the force-push, `range-diff` eyeball, and eval-drift stay model-upheld — the RELEASE_CHECKLIST copy must keep saying so and must not claim the release is now "fully" enforced.

## Architecture & holistic fit

- **Codebase fit:** pure harness-self tooling (`scripts/` + `tests/`), all `DEV_ONLY`/stripped — zero adopter surface. SRP: one authored manifest (the `[J]` membership), derivations are DRY off it. DIP: the derived sets depend on the manifest, not vice-versa.
- **Quality dimensions:** `maintainability-structure` (primary — DRY the 7→1) · `testing` (the migration equality net + closure assertion) · `docs-traceability` (the INVARIANT now mechanized; RELEASE_CHECKLIST/DoD references stay accurate).
- **Honesty:** the closure gate pins `NEEDS ⊆ HAS` **mechanically**; the force-push + eval-drift steps stay `[J]`/model-upheld (a script can't judge them) — the RELEASE_CHECKLIST note must keep saying so. Never claim the gate makes the release "fully" content-enforced.
- **Future-proofing:** the class dict is the natural home for a future new class; the closure check is the natural home for the ROADMAP *completeness gate*'s "forgot-to-register fails loud" behavior — but build only what's asked (no build→init→diff round-trip test now unless trivial).
- **Hardening fit (Track B):** the guards live in the same harness-self tooling (`build_release.py` + `RELEASE_CHECKLIST.md`), zero adopter surface. They **defend the same INVARIANT boundary** the DRY track mechanizes (the release is a correct, complete transform of `main`) — P1-3's mechanized drop-check is a direct consumer of the DRY track's `classify()` (SRP + DIP hold: the guard depends on the manifest, not vice-versa). Each guard is a boundary validation (validate at the build boundary, trust the built tree after) and fails loud — squarely the CLAUDE.md engineering principles. **Honesty is load-bearing here:** the guards reduce, never eliminate, release risk (the force-push + eval-drift stay `[J]`); the RELEASE_CHECKLIST copy already carries the correct "one mechanical defense … reduces, not eliminates" framing (`RELEASE_CHECKLIST.md:30`) — the new guards extend it without ever claiming full enforcement.
- **Quality dimensions (hardening):** `error-handling-resilience` (fail-loud preconditions) · `testing` (each guard unit-tested over fixture refs) · `docs-traceability` + honesty (`RELEASE_CHECKLIST.md` copy, mechanical-vs-`[J]` split).

## Affected files

### DRY track (Slices 1–3)

- `scripts/build_release.py` — `DEV_ONLY_FILES` → `DEV_ONLY_PATH_CLASSES` dict; `is_dev_only`/`classify` read the keys; `DEV_ONLY_DIRS` unchanged. Add a `recreate_class(path)` accessor.
- `scripts/check_shipped_content.py` — derive `_INIT_CREATES`/`HARNESS_SELF_SCRIPTS`/`_DANGLE_EXCLUDED` from `build_release.recreate_class`; delete the hand-lists; add the `NEEDS ⊆ HAS` closure check (honor `INCLUDE_GLOBS` carve-out).
- `tests/test_build_release.py` + `tests/test_check_shipped_content.py` — a migration equality test (shipped set byte-identical); a derived-vs-old equality test (dropped after step 3); collapse the seed pins to `classify` + closure assertions.
- `docs/claugentic-INVARIANTS.md` — update the release/init-contract invariant to note the closure check now mechanizes `NEEDS ⊆ HAS` (was prose-only).
- `docs/RELEASE_CHECKLIST.md` — note the closure gate; keep the `[J]` force-push/eval-drift steps as model-upheld.
- `docs/claugentic-ARCHITECTURE_TREE.md` — update the two script rows if their scope line changes.
- `docs/claugentic-DECISIONS.md` — one dated line (the 7→1 consolidation + closure gate; version-sync kept separate; provable-no-op).

### Hardening track (Slices 4–9)

- `scripts/build_release.py` — **primary hardening surface.** Add: (P0-1) a version-must-increase precondition to `_apply()` (read `plugin.json.version`, compare to the **latest `vX.Y.Z` git tag** via a semver compare helper — NOT `origin/release`, which is never fetched; refuse if not strictly greater; no tag → first release → allow; untagged same-version rebuild allowed); (P0-2) a built-tree validation step run BEFORE the commit that invokes the **dev checkout's** `check_shipped_content.py --root <tmp>` pointed at the throwaway worktree — NOT `pytest` (which vacuously greens on the stripped tree) and NOT the gate run from inside the stripped tree (its tooling is stripped); (P1-3) a mechanized drop-check precondition (**`origin/main`-not-`HEAD`** path set ⊆ strip set, reusing `classify()` — NOT against the `release` ref); (P2-2) broaden `_dropped_merges()` → `_dropped_commits()`/`_missing_upstream_commits()` (drop `--merges`; catch any non-reachable commit). Each fails loud with an actionable message.
- `scripts/check_shipped_content.py` — **(P0-2) add a `--root <path>` argument** so the scan targets an explicitly-given tree (the built worktree) instead of always deriving its root from `__file__` + the current checkout's `git ls-files`. Run the dev copy against `<tmp>` from `_apply()`. (This is the DRY track's same file — the `--root` arg is additive to the closure/derivation work in Slices 2–3.)
- `docs/RELEASE_CHECKLIST.md` — **hardening copy.** (P1-1) add the `git tag vX.Y.Z release && git push origin vX.Y.Z` step (immutable rollback anchor; optionally `gh release`) + note the marketplace-ref-repoint open question; (P1-2) change `git push --force origin release` → `git push --force-with-lease origin release`; document the new mechanical guards (version-increase, built-tree validation, mechanized drop-check, broadened stale-base) and **keep the `[J]`/model-upheld split explicit** — the force-push, `range-diff` eyeball, and eval-drift stay model-upheld; never claim the release is now fully enforced.
- `tests/test_build_release.py` — hardening unit tests: version-increase compare (greater/equal/lesser + missing tag/branch fixtures), built-tree-validation invocation + fail-propagation, mechanized drop-check subset assertion (shipped-file-missing → fail; strip-only diff → pass), broadened stale-base (direct non-merge upstream commit → refuse). Drive `_apply()`'s precondition helpers over fixture repos/refs.
- `docs/claugentic-DECISIONS.md` — one dated line for the hardening track (version-increase + built-tree validation + tag anchor + `--force-with-lease` + mechanized drop-check + broadened stale-base; version-increase kept separate from `check_versions_synced.py`; marketplace-ref-repoint deferred).
- `docs/claugentic-ARCHITECTURE_TREE.md` — refresh the `build_release.py` row (now carries the release-hardening preconditions) + `RELEASE_CHECKLIST.md` row if its scope line changes.
- *(Unchanged, noted for grounding:* `.github/workflows/ci.yml` is **not** touched — P0-2 validates locally at build time, not via a release-branch CI job; see hardening non-goal.*)*

## Risks & mitigations

- **Risk: silently changing what ships.** → **Mitigation:** step-1 migration test asserts the shipped set is byte-identical to pre-change; the whole plan gates on that equality. If it differs, STOP.
- **Risk: the closure check surfaces a real latent gap and fails.** → **Mitigation:** that's a *good* find — fix the wiring (add the missing seed/managed-set entry) or the class annotation; do NOT weaken the check to pass.
- **Risk: the `INCLUDE_GLOBS` carve-out breaks closure** (the tree-script's glob line is adopter-owned). → **Mitigation:** exclude it from the closure NEEDS exactly as `init`'s body-compare does (recorded constraint).
- **Risk: scope creep (folding version-sync / renaming the file).** → **Mitigation:** explicit non-goal; version-sync stays separate; the rename is deferred/optional.

### Hardening risks

- **Risk: a hardening guard blocks a legitimate release** (e.g. version-increase compare mishandles the first-ever tag/branch, or a re-build at the same version during a hotfix). → **Mitigation:** define the missing-anchor behavior at Spec (first release with no `origin/release`/tag → allow, don't refuse); make refusals actionable and overridable only by fixing the cause, never by a silent bypass flag that becomes a footgun.
- **Risk: P0-2 built-tree validation is flaky or false-fails** because `pytest` references dev-only paths the strip removed. → **Mitigation:** decide the shipped-subset validation set at Spec; the safe floor is `check_shipped_content.py` on the built tree (already release-scoped); do NOT run the full dev suite against a tree that no longer contains `tests/`.
- **Risk: P1-3's path-set drop-check gives false confidence** (it catches a missing *shipped file* but not a dropped *commit* whose file also legitimately changed). → **Mitigation:** it is defense-in-depth, NOT a replacement — the manual `range-diff` stays in the checklist; the honesty copy must say the mechanized check is a subset guard, not a total drop guarantee.
- **Risk: over-claiming the release is now "safe."** → **Mitigation:** `honesty-reviewer` on the RELEASE_CHECKLIST copy; the guards reduce, never eliminate, risk — keep the existing "reduces, not eliminates" framing.

## Test strategy

- **Deterministic gates:** `python -m pytest` (+ the new migration/closure/hardening tests), `node --test "tests/workflows/*.test.mjs"`, `python scripts/check_shipped_content.py` (must stay OK — now with the closure check), `python scripts/check_versions_synced.py` (untouched), `python scripts/check_doc_budgets.py`, `scripts/claugentic-check_architecture_tree.py`.
- **The load-bearing check (DRY):** a test/dogfood-run that computes the shipped set from `classify()` before and after the migration and asserts **byte-identical**.
- **Hardening tests (per guard, in `tests/test_build_release.py`):**
  - *P0-1 version-increase* — over fixture manifests + **tags**: greater than latest tag → pass, equal/lesser → refuse, **no tag (first release) → allow**, **untagged same-version rebuild → allow** (only refuse when `plugin.json.version <= latest_tag`). Exercise the semver compare directly.
  - *P0-2 built-tree validation* — **non-vacuous by construction:** the test must prove the scan actually ran against the built tree — `check_shipped_content.py --root <tmp>` PASSES on a good built tree AND FAILS (loud non-zero, no commit) on an injected-bad one, so the test cannot go vacuously green if the validation silently no-ops (mirror the non-hollow-guard pattern at `tests/test_build_release.py:267-276`). Assert the `--root` was pointed at the built worktree.
  - *P1-3 mechanized drop-check* — a strip-only **`origin/main`-not-`HEAD`** diff passes; a shipped-file-missing diff refuses; reuses `classify()` on the diff-path list (asserts the coupling).
  - *P2-2 broadened stale-base* — a direct NON-merge commit on `origin/main` not reachable from HEAD now refuses (the pre-broadening `--merges` form would have missed it); absent `origin/main` still fails loud (unchanged).
- **Checklist-only guards (P1-1 tag, P1-2 `--force-with-lease`):** no unit test — verified by `honesty-reviewer` on the copy + a manual dogfood at the next real release; the RELEASE_CHECKLIST change is reviewed for the correct `[J]`/mechanical split.
- **Reviewer sign-offs:** `maintainability-structure` + `testing` + `docs-traceability` lenses via `synthesizer-gate`; `error-handling-resilience` on the hardening preconditions; `honesty-reviewer` on the RELEASE_CHECKLIST/INVARIANTS copy (mechanical-vs-`[J]` split); `yagni-sentinel` (no version-sync fold, no premature rename, no release-branch CI job, no marketplace-ref repoint).

## Decomposition (slices)

### DRY track (re-gated READY-TO-BUILD)

- [ ] **Slice 1 — Annotate the manifest (SIX classes) + prove the shipped set unchanged.** `DEV_ONLY_FILES`→`DEV_ONLY_PATH_CLASSES` dict in `build_release.py` (the six classes incl. `recreate-on-demand`); `is_dev_only`/`classify`/`recreate_class` off the dict; the **exhaustive partition mapping** (every entry → one class); migration equality test (shipped set byte-identical). No behavior change. **In-scope:** `maintainability-structure`, `testing`.
- [ ] **Slice 2 — Derive-alongside + delete the hand-lists.** In `check_shipped_content.py` derive the three sets from `recreate_class`, assert-equal to the old hand-lists (safety net), then delete the hand-lists once green. **Migrate the `test_dangling_set_is_derived_and_excludes_gate_scripts` pin** explicitly. **In-scope:** `maintainability-structure`, `testing`.
- [ ] **Slice 3 — The referential-closure gate (`NEEDS ⊆ HAS`).** Add the closure assertion over the four HAS sources (`recreate-on-demand` satisfied by-class, NOT by init-producibility; honor `INCLUDE_GLOBS` carve-out); mechanize the INVARIANT; update INVARIANTS + RELEASE_CHECKLIST copy — the honesty wording is **"producible by init _or the workflow's lazy/templated/agent authoring_"**, never "producible by init" flatly (keep `[J]` force-push/eval steps model-upheld). Collapse the test pins. DECISIONS line; tree update. **In-scope:** `maintainability-structure`, `testing`, `docs-traceability`; trust surface → `honesty-reviewer`.

### Hardening track (NEW — needs a fresh plan-gate on the expanded plan before build)

Ordered so the guard broadeners can reuse the DRY track's finalized `classify()`; each slice lands complete with its own test.

> **Tag-anchor ordering (Stage-4 gate fold-in).** Slice 4 (version-must-increase) now anchors on the **latest `vX.Y.Z` git tag** — which Slice 8 (P1-1) establishes as the immutable release anchor. So **Slice 4 depends on Slice 8's tagging discipline existing.** Resolve at Spec by EITHER (a) **moving Slice 8 (tag each release) earlier — before Slice 4** — so the anchor mechanism is in place first, OR (b) keeping the 4→…→8 order with a **documented bootstrap for the very first tagged release**: before any `vX.Y.Z` tag exists the "no tag → first release → ALLOW" branch makes Slice 4 a safe no-op, and the first `--apply` after Slice 8 lands seeds the anchor (tag the current published version once, retroactively, so the guard has something to compare against going forward). **Slice 7 (mechanized drop-check) does NOT depend on Slice 8** — it anchors on `origin/main`-not-`HEAD`, no tag involved — it depends only on Slices 1–3's `classify()`. Prefer (a) if the Spec can reorder cleanly; otherwise (b) with the bootstrap written into the Slice-8 checklist copy.

- [ ] **Slice 4 — P0-1 version-must-increase guard (must-fix).** Add a version-increase precondition to `build_release.py --apply`: read `plugin.json.version`, resolve the last-released version from **the latest `vX.Y.Z` git tag** (`git tag --list 'v*'`, max by semver — NOT `origin/release`, which the pipeline never fetches so it is stale/absent at `--apply` time, and the build is from `HEAD` anyway), semver-compare, **refuse if not strictly greater than the tag.** Anchor semantics: **NO tag at all → first release → ALLOW; a tag exists and `plugin.json.version` is NOT strictly greater → FAIL LOUD.** Because the anchor is the tag (an untagged in-progress version has no anchor above it), a **same-version re-build of an untagged version is permitted** (re-running `--apply` after a failed push does not bump, must not be refused) — you only refuse when `plugin.json.version <= latest_tag`. This cleanly distinguishes "first release" (no tag) from "anchor unfetched" (not a concern once anchored on tags, which ship with the repo). **Depends on Slice 8** (tags become the immutable anchor there); see the tags-bootstrap note under Decomposition. Actionable fail message (sibling to the stale-base refusal). Unit tests over fixture manifests/tags (greater → pass; equal/lesser → refuse; no tag → allow; untagged-same-version rebuild → allow). Keep it **separate** from `check_versions_synced.py`. **In-scope:** `error-handling-resilience`, `testing`.
- [ ] **Slice 5 — P0-2 validate the BUILT (stripped) tree before publishing (must-fix).** **Mechanism REDESIGNED (Stage-4 gate fold-in):** validate the built worktree **FROM THE DEV CHECKOUT**, not from inside the stripped tree. The strip removes `tests/`, `pyproject.toml`, `check_shipped_content.py`, AND `build_release.py` (which `check_shipped_content.py` imports) from the throwaway worktree — so running `pytest` there collects ZERO tests (vacuous green) and `check_shipped_content.py` cannot even import there. **Fix:** give `check_shipped_content.py` a `--root <path>` argument (it currently derives its root from `__file__` and reads the *current* checkout's `git ls-files`; the arg makes it scan an explicitly-given tree instead) and, inside `_apply()` after the strip + before the commit, invoke the **dev checkout's** `check_shipped_content.py --root <tmp>` pointed at the built worktree; a non-zero exit aborts the build loud (no commit). **DROP the "run `pytest` against the built tree" step entirely** — it is vacuous (no `tests/`/config survives the strip) and the dev-tree `pytest` already ran pre-build in the DoD. **Honest scope:** this validates the **SHIPPED STRUCTURE** of the built tree (no stranded tokens / dangling refs / non-ASCII — whatever `check_shipped_content.py` asserts), NOT "the release passes CI." Unit test: a non-vacuous injected-failure (see Test-strategy — must prove the scan actually ran against the built tree and PASSES on a good tree, FAILS on an injected-bad one). **In-scope:** `error-handling-resilience`, `testing`.
- [ ] **Slice 6 — P2-2 broaden the stale-base guard.** Broaden `_dropped_merges()` → `_dropped_commits()`/`_missing_upstream_commits()`: drop `--merges` (use `rev-list origin/main --not HEAD`, or assert HEAD is an ancestor-inclusive match of `origin/main`) so a direct NON-merge push to `main` is caught. Preserve the absent-`origin/main` fail-loud. Rename + re-doc + update call sites/messages. Unit test: direct non-merge upstream commit → refuse (would have slipped the `--merges` form). **In-scope:** `error-handling-resilience`, `testing`.
- [ ] **Slice 7 — P1-3 mechanize the drop-check.** Add a computable precondition to `_apply()` reusing the DRY track's pure `classify()`: the **`origin/main`-not-`HEAD`** path set must be a **subset of the strip set** — NOT a diff against the `release` ref (which the pipeline never fetches and is about to be clobbered/rebuilt from `HEAD`, so it's the wrong anchor). Semantics: does the base I'm about to build from (`HEAD`) omit any shipped path present upstream on `origin/main`? A shipped-path in the `origin/main`-not-`HEAD` diff → refuse (a shipped file would be missing from the release). The manual `range-diff` stays in the checklist as defense-in-depth (honesty copy: this is a **subset guard, not a total drop guarantee** — it catches a missing shipped *file*, not a dropped *commit* whose file also legitimately changed). Unit test: strip-only `origin/main`-not-`HEAD` diff passes; shipped-file-missing diff refuses; asserts the `classify()` coupling (feeding the diff-path list to the pure `classify()`). **Ordered after Slices 1–3** (`classify()` finalized); the drop-check itself needs no tag, so it does NOT depend on Slice 8. **In-scope:** `error-handling-resilience`, `testing`.
- [ ] **Slice 8 — P1-1 tag each release (immutable rollback anchor).** Add a RELEASE_CHECKLIST step: `git tag vX.Y.Z release && git push origin vX.Y.Z` after the force-push (optionally a `gh release`); the tag doubles as Slice 4's compare anchor. **Note the marketplace-ref-repoint as an open question — do NOT repoint now.** Checklist copy + tree/DECISIONS update. **In-scope:** `docs-traceability`; trust surface → `honesty-reviewer`.
- [ ] **Slice 9 — P1-2 `--force-with-lease` + hardening docs/DECISIONS wrap-up.** Change the RELEASE_CHECKLIST push to `git push --force-with-lease origin release`; document the full set of new mechanical guards while **keeping the `[J]`/model-upheld split explicit** (force-push, `range-diff`, eval-drift stay model-upheld — never claim full enforcement). **The mechanical-guard list must state each guard's TRUE scope, not imply totality:** the drop-check is a **subset guard, not a total drop guarantee**; the built-tree validation checks the **shipped STRUCTURE** (`check_shipped_content.py --root` — stranded tokens / dangling refs / non-ASCII), NOT "the release passes CI / the full suite"; the version-increase guard fires only vs the **latest tag** (a same-version overwrite of an *untagged* in-progress build is intentionally allowed — say so). `honesty-reviewer` checks the copy states each guard's scope, not a blanket "the release is now mechanically validated." DECISIONS line for the hardening track; tree refresh. **In-scope:** `docs-traceability`; trust surface → `honesty-reviewer`.

---

## Review  _(synthesizer-gate plan-gate, Stage 3)_

> **Expanded-scope note (2026-07-04) — a FRESH plan-gate is REQUIRED before the hardening track builds.**
> This plan was materially expanded on 2026-07-04: the release-**hardening** track (Slices 4–9: P0-1 version-increase, P0-2 built-tree validation, P1-1 tag anchor, P1-2 `--force-with-lease`, P1-3 mechanized drop-check, P2-2 broadened stale-base) was added alongside the original release-tooling-**DRY** track (Slices 1–3). **The re-gate history below (2026-07-03 CHANGES-REQUIRED → NEEDS-SPEC-FIX-FIRST → 2026-07-04 READY-TO-BUILD) covers ONLY the DRY track (Slices 1–3); it did NOT review the hardening slices.** The DRY track's READY-TO-BUILD verdict still stands and is unaffected. The **hardening track (Slices 4–9) is NOT yet plan-gated** — a fresh `synthesizer-gate` plan-gate pass must review the expanded plan (its hardening goals, the six new slices, their fixture-based test strategy, and the `[J]`/mechanical honesty split in the RELEASE_CHECKLIST copy) before Slice 4 starts. Open items the fresh gate should resolve: (i) the exact P0-2 shipped-subset validation set; (ii) P0-1's placement (`--apply` precondition vs checklist-gated script) and its first-release / hotfix-same-version edge behavior; (iii) confirm P1-3's `classify()` reuse is correctly ordered after Slices 1–3. **Do NOT delete the prior re-gate history below — it remains the DRY track's gate record.**

RUNNING AS: Opus 4.x (same-model as the planner — this is a separate clean-context pass, a reduction of rubber-stamping risk, not a de-correlated oracle).

**Verdict: CHANGES REQUIRED** — the spine is sound (provable-no-op migration, correct non-goals, honest framing), but the **closure-gate taxonomy has a load-bearing hole** that will either hard-fail Slice 3 or force an unsound class annotation. Resolve it in the plan/Slice-3 spec before implementing, not mid-slice.

### What's right (do not re-litigate)
- **No-op safety property is genuinely assertable.** `is_dev_only`/`classify` are pure over `DEV_ONLY_FILES ∪ DEV_ONLY_DIRS` (`build_release.py:80-90`); a `frozenset`→`dict` conversion read via `.keys()` yields the identical membership set, so a before/after equality test over `classify(_tracked_files())` is a direct, load-bearing guard. Step-order (annotate→derive-alongside-and-assert-equal→delete→closure→collapse) is correctly safety-netted — each net is real.
- **The three hand-lists are accurately characterized.** `_INIT_CREATES` (`check_shipped_content.py:103-113`), `HARNESS_SELF_SCRIPTS` (`:119-126`), `_DANGLE_EXCLUDED` (`:131-139`) are exactly the three parallel partitions of `DEV_ONLY_FILES`, consumed only to *subtract* in `dangling_paths()` (`:166-176`). Deriving them from one class annotation is a true DRY win.
- **Non-goals are correct.** Version-sync stays separate (folding would add dual-manifest JSON parsing — real scope creep; corrects the dossier). Third-drift-axis refuted. INCLUDE_GLOBS carve-out is real and must be honored — verified at `skills/init/SKILL.md:210-231` (the tree-script's `INCLUDE_GLOBS` assignment is the one adopter-owned region; init's body-compare excludes it). Closure must exclude it exactly as init does.
- **Honesty framing holds.** Closure pins `NEEDS ⊆ HAS` mechanically; force-push/eval-drift stay `[J]`. `check_shipped_content.py:59-63` already carries the correct "does NOT make the contract fully content-enforced" caveat — the RELEASE_CHECKLIST/INVARIANTS copy must keep that wording. No over-claim in the plan.
- **Does not touch 0029/0030.** Confirmed additive; only `build_release.py` + `check_shipped_content.py` + their tests + the doc rows.

### Required changes (numbered, actionable)

1. **[BLOCKER — taxonomy soundness] The 5 classes + the "producible by init" predicate do NOT cover `INVARIANTS.md`, `PRODUCT.md`, `PRODUCT_SPEC.md`.** All three are in `_INIT_CREATES` today (so Pass A.a correctly treats them as non-dangling), but **`init` does not produce any of them**:
   - `INVARIANTS.md` — created **lazily by the WORKFLOW Definition-of-Done step (f)** (`docs/claugentic-WORKFLOW.md:259` — "Create the file lazily … do not seed it empty"), never by init. It is not in init's managed-set table (`skills/init/SKILL.md:137-145`) and there is **no `_INVARIANTS.md` seed** (only `_CHARTER`/`_DECISIONS`/`_ROADMAP` seeds exist).
   - `PRODUCT.md` / `PRODUCT_SPEC.md` — **user-authored, never managed**; only the `PRODUCT_SPEC_TEMPLATE` ships and is init-managed (`skills/init/SKILL.md:143`). No `_PRODUCT.md`/`_PRODUCT_SPEC.md` seed exists.

   The plan's closure predicate as written (Goal §17 / Slice 3) — "producible by init: `_X.md` seed ships **OR** managed-set table **OR** known generator output" — is satisfiable for `init-seed` (DECISIONS/ROADMAP/CHARTER) and `init-gen` (ARCHITECTURE_TREE), but **fails for these three**. So either (a) they get shoved into `init-seed`/`init-gen` and the closure gate **hard-fails on legitimately-non-dangling files** (this is NOT the "good latent-gap find" the plan anticipates — it's a taxonomy hole, and the only way to green it would be to weaken the check, which the plan rightly forbids), or (b) the taxonomy needs a **distinct class** (e.g. `workflow-lazy` / `user-authored`) whose closure obligation is "producible by a non-init mechanism the workflow owns" — i.e. a **fourth HAS source** the closure predicate must name. **Decide (b) in the plan now** and enumerate all HAS sources; do not leave it to be discovered in Slice 3.

2. **[Correctness] Enumerate the full DEV_ONLY partition in the plan so the classes are provably exhaustive.** Map every current `DEV_ONLY_FILES` entry to its class in the Slice-1 spec (DECISIONS/ROADMAP/CHARTER→init-seed · ARCHITECTURE_TREE→init-gen · INVARIANTS/PRODUCT/PRODUCT_SPEC→the new class from change 1 · 4 scripts→self-gate · RELEASE_CHECKLIST→dangle · settings.json/CLAUDE.md/pyproject.toml/.gitignore/.gitattributes→config). This is the check that the taxonomy is a *partition*, not a lossy re-encoding. `DEV_ONLY_DIRS` stays out of the classes (the plan keeps it — correct; the closure gate reasons about the file-level classes only).

3. **[Slice completeness] Slice 2 must keep `test_dangling_set_is_derived_and_excludes_gate_scripts` green.** `tests/test_check_shipped_content.py:91-100` pins that the derived dangle set excludes the gate scripts. Rewriting `dangling_paths()` to derive from `recreate_class` must preserve (or explicitly migrate) that assertion — name it in the Slice-2 spec so the slice lands complete, not with a silently-broken pin. (The Affected-files list already includes this test file — good; just make the pin migration explicit.)

4. **[Honesty copy — minor] Reclassifying `_INIT_CREATES` changes what the name asserts.** Once change 1 introduces a `workflow-lazy` class, the derived "init-creates" set is narrower than today's `_INIT_CREATES` (which conflates init-created with user/workflow-authored). Ensure the INVARIANTS.md update (Slice 3) describes the closure gate as "every stripped-adopter-relevant path is producible by init **or the workflow's lazy/templated authoring**" — not "producible by init" flatly, which would be a subtle over-claim about init's scope.

### Sizing / completeness check (per slice)
- **Slice 1 (annotate + no-op proof)** — OK. Session-sized, lands complete (pure refactor + equality test, zero behavior change). The equality net is load-bearing and correctly gates the slice.
- **Slice 2 (derive-alongside + delete)** — OK, conditional on change 3. The derive-alongside-then-assert-equal-then-delete sequence is a proper within-slice safety net; keep both lists until the equality test is green in the same slice, then delete — do not split the delete into a later slice (that would leave a dead duplicate = debt).
- **Slice 3 (closure gate + docs)** — **BLOCKED on changes 1, 2, 4.** As written it risks a mid-slice hard-fail on the three workflow-lazy files with no planned resolution. With the taxonomy fixed it is session-sized and lands complete. Trust surface (INVARIANTS/RELEASE_CHECKLIST honesty copy) correctly routes to `honesty-reviewer`.

### Harness impact
- **No new STANDARD/agent/doc-module.** This mechanizes an existing INVARIANT; the INVARIANTS.md + RELEASE_CHECKLIST edits are updates, not new surface. DECISIONS line is warranted (7→1 consolidation + closure gate + version-sync-kept-separate).
- The rename to `release_gate.py` staying **deferred/optional** is the right YAGNI call — the value is the manifest + closure, both of which live fine in `check_shipped_content.py`. Do not let it creep back in.
- **ROADMAP:** the plan partially delivers the *Release/init-contract completeness gate* item; note at Land whether it's fully closed or the ROADMAP line survives (the build→init→diff round-trip is explicitly out of scope here — keep it a ROADMAP pointer, do not build it).

### Re-gate (2026-07-03) — buildability check

RUNNING AS: Opus 4.x (same-model as the planner/prior gate — a separate clean-context pass, a reduction of rubber-stamping risk, not a de-correlated oracle).

**Verdict: NEEDS-SPEC-FIX-FIRST** — the prior CHANGES-REQUIRED blocker is **still open in the plan body**. The Review section correctly diagnosed it, but the *spec text* (Goals §17, Approach step 4, Slice 3) was **not amended** to fold in the fix. As written, an implementer building Slice 3 straight from the Goals/Slices text would hit the exact hard-fail the prior review predicted. One targeted spec edit unblocks the build; the migration net itself is sound.

**1. Is the prior CHANGES-REQUIRED resolved in the plan text? — NO (this is the blocker).**
I confirmed the taxonomy hole against source, and it is real:
- `_INIT_CREATES` (`scripts/check_shipped_content.py:116-126`) holds **7** paths. Four are genuinely init-producible: `DECISIONS`/`ROADMAP`/`CHARTER` (seed copies) + `ARCHITECTURE_TREE` (init generates it). **Three are NOT init-produced:**
  - `docs/claugentic-INVARIANTS.md` — created **lazily by WORKFLOW.md Definition-of-Done step (f)** (`docs/claugentic-WORKFLOW.md:227` — "Create the file lazily … do not seed it empty"). It is **not** in init's managed-set table (`skills/init/SKILL.md:137-145`) and there is **no `_INVARIANTS.md` seed**.
  - `docs/claugentic-PRODUCT.md` / `docs/claugentic-PRODUCT_SPEC.md` — **user-authored, never managed**; only `PRODUCT_SPEC_TEMPLATE` is init-managed (`skills/init/SKILL.md:143`, explicit: "the filled `…-PRODUCT_SPEC.md` is user-owned, never managed"). No `_PRODUCT*.md` seed exists.
- The plan **body** still lists only the 5 classes (`init-seed`/`init-gen`/`self-gate`/`config`/`dangle`, Goals §16) and the 3-way "producible by init" closure predicate (§17, Slice 3 line 69). **No 4th recreate-class was added to the spec.** So Slice 3's closure gate, built as written, either (a) hard-fails on these three legitimately-non-dangling files, or (b) forces them into `init-seed`/`init-gen` — a false annotation the plan itself forbids weakening the check to accommodate. This is a taxonomy hole, **not** the "good latent-gap find" step-4 anticipates.
- **Required spec edit (name it exactly):** add a **4th recreate-class** — call it `workflow-lazy` (or `user-authored`; one class covers all three since their common property is "producible/owned outside init") — and make the closure predicate enumerate a **4th HAS source**: "a path in `workflow-lazy` satisfies closure iff a non-init harness mechanism owns its creation (WORKFLOW step (f) lazy-create, or user authorship of PRODUCT/PRODUCT_SPEC)." Map `INVARIANTS.md` + `PRODUCT.md` + `PRODUCT_SPEC.md` into it. Fold this into **Goals §16-17 and Slice 3** (not just the Review section) before build. Prior review changes 2 (exhaustive partition map) and 4 (INVARIANTS.md copy must say "producible by init **or the workflow's lazy/templated authoring**", not "by init" flatly) ride along and must also land in the spec.

**2. Is the "provable no-op" safety net real? — YES, verified sound.**
- `is_dev_only`/`classify` are pure over `DEV_ONLY_FILES ∪ DEV_ONLY_DIRS` (`build_release.py:80-90`); a `frozenset`→`dict`-keys conversion is a **membership-preserving** change, so the step-1 before/after equality assertion over `classify(_tracked_files())` is a genuine, load-bearing guard. Correct.
- Derive-alongside-then-assert-equal-then-delete (steps 2-3) is a proper within-slice net — each derived set (`_INIT_CREATES`/`HARNESS_SELF_SCRIPTS`/`_DANGLE_EXCLUDED`, `check_shipped_content.py:116-152`) is asserted equal to its hand-list before the hand-list is deleted. Sound.
- `INCLUDE_GLOBS` never-clobber carve-out is real and correctly scoped: the tree-script's `INCLUDE_GLOBS = […]` region is the **one** adopter-owned seam (`skills/init/SKILL.md:210-231`), excluded from init's body-compare. The closure must exclude it exactly as init does — the plan records this (step 4, Risk 3). Honor it verbatim.

**3. Scope discipline — YES, honored, and correctly.**
- `check_versions_synced.py` stays a SEPARATE gate (Non-goal §19, untouched in Affected-files). Verified correct: it enforces `plugin.json` ↔ `marketplace.json` version equality (`WORKFLOW.md:135`) — an orthogonal two-manifest agreement check; folding it in would add dual-manifest JSON parsing = real scope creep. Do not fold.
- The `release_gate.py` rename is deferred/optional (Approach line 33). Correct YAGNI call — the annotated manifest + closure both live fine in `check_shipped_content.py`; the rename is cosmetic churn against DoD-gate-list / RELEASE_CHECKLIST / tree. Keep deferred.

**4. Buildability verdict — NEEDS-SPEC-FIX-FIRST.**
- **Blocking fix (must spec before build):** the 4th `workflow-lazy` recreate-class + 4th HAS source in the closure predicate, folded into **Goals §16-17 and Slice 3** (per §1 above), plus the ride-along exhaustive-partition map and the INVARIANTS.md honesty-copy wording.
- **Non-blocking but do in the same spec pass:** make the Slice-2 rewrite of `dangling_paths()` explicitly preserve `test_dangling_set_is_derived_and_excludes_gate_scripts` (`tests/test_check_shipped_content.py:140-147` — confirmed real; pins RELEASE_CHECKLIST ∈ dangle, gate-scripts/init-created/config ∉ dangle). Name the pin migration in the Slice-2 spec so the slice lands complete.
- **Slices 1 and 2 are buildable now** as written (pure membership-preserving refactor + derive-alongside net); the blocker is contained to **Slice 3**. But since Slice 3's taxonomy is what the class annotations in Slice 1 must anticipate (the 4th class has to exist in `DEV_ONLY_PATH_CLASSES` from Slice 1 for the three files to be annotated correctly), the fix should land in the plan **before Slice 1 starts** so the class set is right the first time — otherwise Slice 3 forces a Slice-1 re-annotation.
- **Build risk: MODERATE, well-contained.** This is executable release tooling — a silent change to the shipped set is the worst outcome — but the plan's step-1 byte-identical equality assertion directly guards exactly that, and the migration is membership-preserving by construction. The residual risk is entirely the taxonomy hole (§1): if it ships unfixed, Slice 3 either hard-fails the build or (worse) gets greened by a false class annotation that mis-asserts init can produce user-authored/lazy files. **Fix the taxonomy in the spec and the risk drops to LOW.** With the 4th-class fix folded into Goals/Slice 3 + Slices 1/2 buildable as-is, this is a clean provable-no-op refactor.

**READY once** the 4th `workflow-lazy` class + 4th HAS source are written into Goals §16-17 and Slice 3 (and the class exists in the Slice-1 `DEV_ONLY_PATH_CLASSES` annotation). Until then: **NEEDS-SPEC-FIX-FIRST.**

### Re-gate (2026-07-04) — six-class fold-in verified

RUNNING AS: Opus 4.x (prose-orchestrated; same model family as the planner/prior gates — a separate clean-context pass, a reduction of rubber-stamping risk, not a de-correlated oracle).

**Verdict: READY-TO-BUILD.** The prior NEEDS-SPEC-FIX-FIRST blocker is **resolved in the plan body.** The taxonomy hole is closed the right way: a **sixth class `recreate-on-demand`** now sits in Goals §16-17, Approach steps 1/4, and Slice 3 (not just the Review section), and the closure predicate accepts its members **by class** — the class IS the HAS attestation — rather than falsely asserting init can produce them. I verified every load-bearing claim against source; all five gate questions pass. Slices 1→2→3 are buildable in order.

**1. Six-class partition is exhaustive + sound — YES.** I mapped every current `DEV_ONLY_FILES` entry (`scripts/build_release.py:44-61`, 16 file paths) to exactly one class; every assignment holds:
- `init-seed` — DECISIONS, ROADMAP. Seeds ship: `docs/claugentic-_DECISIONS.md`, `docs/claugentic-_ROADMAP.md` **exist on disk** (init step 7 copies them, `skills/init/SKILL.md:668-702`).
- `init-gen` — ARCHITECTURE_TREE (init generates the adopter's).
- **`recreate-on-demand` — INVARIANTS, PRODUCT, PRODUCT_SPEC — CONFIRMED correct, they are NOT init-produced:**
  - `INVARIANTS.md`: **not** in init's managed-set table (`skills/init/SKILL.md:137-145`), **no `_INVARIANTS.md` seed exists** (only `_DECISIONS`/`_ROADMAP`/`_CHARTER` do), created lazily by WORKFLOW DoD step (f) — verified at `docs/claugentic-WORKFLOW.md:283` ("Create the file lazily … do not seed it empty"). *(The plan cites this as `WORKFLOW.md:259`/`:227`; the live line is 283 — stale citation, substance correct — see nit A.)*
  - `PRODUCT.md`: agent-authored per-project by `product-designer`, dev-only, adopters get the pattern not the file — verified at `docs/claugentic-DECISIONS.md:65` (plan cites `:64`; live is `:65` — nit A). No `_PRODUCT.md` seed.
  - `PRODUCT_SPEC.md`: user-authored from the shipped TEMPLATE; `skills/init/SKILL.md:143` states explicitly "the filled `docs/claugentic-PRODUCT_SPEC.md` is user-owned, never managed." Only `PRODUCT_SPEC_TEMPLATE` is managed. No `_PRODUCT_SPEC.md` seed.
- `self-gate` — check_versions_synced, check_doc_budgets, check_shipped_content, build_release (4 harness-self scripts).
- `config` — .claude/settings.json, CLAUDE.md, pyproject.toml, .gitignore, .gitattributes.
- `dangle` — RELEASE_CHECKLIST.md.

All 16 map, one class each — a provable partition, not a lossy re-encoding.

**2. Closure predicate is sound — YES.** Goal §17 and Slice 3 both state `recreate-on-demand` "satisfies closure via this class, it does NOT hard-require init-producibility for them," and the honesty copy is "producible by init **or the workflow's lazy/templated/agent authoring**," never "producible by init" flatly. This is exactly the correct fix: the class itself is the fourth HAS source, so Slice 3's closure gate will NOT hard-fail on these three legitimately-non-dangling files, and it does NOT get greened by a false `init-seed`/`init-gen` annotation. The prior review's concern is fully addressed. The `INCLUDE_GLOBS` never-clobber carve-out is real (`skills/init/SKILL.md:210-231` — the one adopter-owned seam) and step 4/Risk 3 correctly require excluding it from closure exactly as init's body-compare does.

**3. Provable-no-op net is real — YES (re-confirmed).** `is_dev_only`/`classify` are pure over `DEV_ONLY_FILES ∪ DEV_ONLY_DIRS` (`build_release.py:80-90`); a `frozenset`→`dict`-keys conversion is membership-preserving, so the step-1 before/after byte-identical equality assertion over `classify(_tracked_files())` is a genuine load-bearing guard. Derive-alongside→assert-equal→delete (steps 2-3) is a proper within-slice net against the three real hand-lists (`check_shipped_content.py:116-152`).

**4. Scope discipline — HELD.** `check_versions_synced.py` stays SEPARATE (Non-goal §19; it enforces the orthogonal `plugin.json`↔`marketplace.json` agreement, `WORKFLOW.md:139` — folding adds dual-manifest parsing = real scope creep). The `release_gate.py` rename stays deferred/optional (Approach line 33) — correct YAGNI.

**5. Slice-2 pin migration — NAMED.** `test_dangling_set_is_derived_and_excludes_gate_scripts` is real (`tests/test_check_shipped_content.py:140-147`; pins RELEASE_CHECKLIST ∈ dangle, gate-scripts/init-created/config ∉ dangle) and is now **explicitly** called out for migration in Approach step 5 and the Slice-2 line (68). The slice will land complete, not with a silently-broken pin.

**Buildable in order — YES:**
- **Slice 1** (annotate the six-class `DEV_ONLY_PATH_CLASSES` dict + byte-identical no-op proof) — session-sized, pure membership-preserving refactor, lands complete. The six-class set is now correct from the start, so Slice 3 will not force a Slice-1 re-annotation (the exact failure mode the prior gate warned about is avoided).
- **Slice 2** (derive-alongside → delete hand-lists + migrate the pin) — session-sized; delete stays in-slice after the equality net is green (no dead-duplicate debt).
- **Slice 3** (closure gate `NEEDS ⊆ HAS` by-class + mechanize INVARIANT + honesty copy) — now unblocked; trust surface correctly routes to `honesty-reviewer`.

**Non-blocking nits to resolve at Spec (do NOT gate the build):**
- **A. Citation drift** — the WORKFLOW/DECISIONS line refs are stale (`WORKFLOW.md:259`/`:227` → live **283**; `DECISIONS:64` → live **65**). Substance is correct; refresh the citations at Spec so the mechanized-INVARIANT provenance points at the right line.
- **B. CHARTER precision** — the plan's Approach-step-1 enumeration (line 27) and Goals §16 fold `_CHARTER.md`/CHARTER into `init-seed`, but `docs/claugentic-CHARTER.md` is **NOT** a current `DEV_ONLY_FILES` entry (the harness keeps no live CHARTER — `skills/init/SKILL.md:701`; the `_CHARTER.md` seed ships for adopters but the harness's own filled CHARTER is absent, so there is nothing to classify). This does not break the partition (an absent path isn't in the manifest to classify), but the Slice-1 spec should map only the **live** `DEV_ONLY_FILES` set (the plan already says "Confirm the live list against `build_release.py` at Spec" — honor that; drop CHARTER from the enumeration or annotate it "seed exists, but no live harness CHARTER in the manifest").

**Bottom line: READY-TO-BUILD.** Fold the two nits into the Spec pass (neither blocks). The six-class fix genuinely resolves the taxonomy hole, the closure predicate is honest, the no-op net is load-bearing, scope is disciplined, and the pin migration is named. Build Slices 1→2→3 in order.

### Hardening-track gate (2026-07-04) — Slices 4–9 (the NEW release-hardening scope)

RUNNING AS: Opus 4.x (prose-orchestrated; same model family as the planner/prior gates — a separate clean-context pass, a reduction of rubber-stamping risk, not a de-correlated oracle). **Scope: the hardening track (Slices 4–9) ONLY.** The DRY track (Slices 1–3) READY-TO-BUILD verdict above is unaffected and not re-litigated.

**Verdict: CHANGES REQUIRED** — the hardening premise is correct, the non-goals are disciplined, the honesty split is honest, and four of the six guards are sound as sliced. But **Slice 5 (P0-2 built-tree validation) has a load-bearing design hole verified against source** — as written, *nothing meaningful runs against the built tree because the strip removes the tests, the pytest config, AND both validation scripts* — and **Slices 4 and 7 have an unresolved anchor-resolution gap** (the compare/subset anchor is `origin/release`, which the pipeline never fetches, and the build is from `HEAD`, not `origin/main`). These will either no-op silently (worst case for a guard: false confidence) or block a legitimate release. Resolve in the plan/spec before building Slices 4/5/7. Slices 6, 8, 9 are READY as sliced.

#### What's right (do not re-litigate)
- **The hardening premise is source-accurate.** `ci.yml` runs `on: push/pull_request → branches: [main]` only (`.github/workflows/ci.yml:6-10`) — the `release` branch genuinely runs zero CI, and `_apply()` commits with `--no-verify` (`build_release.py:198`), so a strip-introduced break is invisible today. `check_versions_synced.py` verifiably checks only manifest *agreement* (`:101`), never increase-vs-last-release. The marketplace tracks a mutable **branch** (`marketplace.json:11` — `"ref": "release"`), no tag. `_dropped_merges()` uses `rev-list --merges` (`build_release.py:159`) — a direct non-merge push is invisible. All six gaps are real.
- **Non-goals hold and are correct.** No version-sync fold (verified orthogonal — `check_versions_synced.py` reads two manifests, has no git/release anchor); no marketplace-ref repoint (correctly deferred as the bigger call — `marketplace.json:11` stays `release`); no CHANGELOG; no release-branch CI job (P0-2 validates locally pre-push — the right altitude, YAGNI on the Actions job). Scope discipline is genuinely held.
- **The `[J]`/mechanical honesty split is preserved and honest.** The plan repeatedly states the mechanical guards fire in-build while the force-push, `range-diff` eyeball, and eval-drift stay `[J]`/model-upheld (Goals, Approach line 72, Architecture line 80, Risk 4), and never claims the release becomes "fully" enforced. `RELEASE_CHECKLIST.md:30` already carries the correct "one mechanical defense … reduces, not eliminates" framing — the new copy extends it. `honesty-reviewer` is correctly routed on Slices 8 and 9. No over-claim.
- **Slice 6 (P2-2) is sound.** `rev-list origin/main --not HEAD` (drop `--merges`) genuinely catches a direct non-merge upstream commit the current `--merges` form misses; the absent-`origin/main` → `None` → fail-loud path (`build_release.py:155-158`) is preserved; the rename + call-site/message update is contained. The existing `TestBaseAncestryGuard` pattern (`tests/test_build_release.py:153-179`, monkeypatched `_git`) extends cleanly to the new-commit fixture. READY.
- **Slice 8 (P1-1 tag) and Slice 9 (P1-2 `--force-with-lease`) are sound** — checklist copy + one tagging step, `honesty-reviewer`-gated, no script logic, no unit test needed (correctly stated). `--force-with-lease` is the right lease-safety upgrade for the manual push. READY.

#### Required changes (numbered, actionable)

1. **[BLOCKER — Slice 5 design hole] "Run `check_shipped_content.py` + tests against the built tree" is NOT executable against the stripped tree as written — the safe floor the plan names is itself stripped.** Verified against `build_release.py`: `_apply()` strips, in the throwaway worktree, ALL of — `tests/` (`DEV_ONLY_DIRS:69`), `pyproject.toml` (the pytest config, `:58`), `check_shipped_content.py` (`:54`), and `build_release.py` (`:55`, which `check_shipped_content.py` imports at `:93`). So in the built worktree:
   - **`pytest` collects zero tests** (no `tests/`, no config) → a **vacuous green**, the exact false-confidence a guard must never give. The plan's own Risk-2 mitigation ("do NOT run the full dev suite against a tree with `tests/` stripped") correctly forbids this — but then names a floor that is also broken:
   - **`check_shipped_content.py` is not present in the built tree AND imports the also-stripped `build_release`** — so "the safe floor = `check_shipped_content.py` on the built tree" (Goals §48, Slice 5, Risk 2) **cannot run there**. It could only run *pointed at* the built tree from the dev checkout (as a `--root <tmp>` invocation), which is a different design than "against the built tree" and needs `check_shipped_content.py` to grow a root/branch argument (it currently derives root from `__file__` and reads the *current* git `ls-files`, `:299-305`) — real added surface, not the "invoke it against tmp" one-liner the plan implies.
   **Decide the actual validation mechanism at Spec and write it into Slice 5** — pick ONE and make it real: (a) run the **dev checkout's** `check_shipped_content.py`/`pytest` *before* the strip (they already validate the shipped subset — but then it's "validate `main` pre-strip," which the existing CI on `main` largely already does, so state what NEW coverage this adds); or (b) validate the **built tree's structure directly** — a `_apply()` post-strip assertion that the expected shipped file set is present/non-empty and the strip removed exactly the strip-set (a genuine built-tree check that needs no stripped tooling); or (c) give `check_shipped_content.py` an explicit `--root`/target-tree parameter and run the dev copy against `tmp`. **Do NOT ship Slice 5 with "run pytest/the gate against the built tree" unqualified** — it silently no-ops or vacuously greens. This is the slice the guard-integrity of the whole track hinges on; get its mechanism concrete before build.

2. **[BLOCKER — Slices 4 & 7 anchor resolution] The compare/subset anchor is `origin/release`, but the pipeline never fetches it, and `_apply()` builds from `HEAD` not `origin/main` — resolve the anchor semantics precisely.** Two coupled gaps:
   - **Slice 4 (version-increase):** the plan says compare `plugin.json.version` to "the version on `origin/release`'s `plugin.json` (or the latest `vX.Y.Z` tag)." But (i) `RELEASE_CHECKLIST.md` fetches `origin/main` (step 1) and never `git fetch origin release` / tags, so `origin/release` may be **stale or absent locally** at `--apply` time → a stale local `origin/release` would compare against an old version and *pass a non-increase*, or an absent ref would hit the first-release path and *skip the check entirely on a normal release*. **Decide the anchor precisely:** prefer the **tag** (`git tag --list 'v*'` max by semver) since Slice 8 makes tags the immutable anchor and they're fetched with the repo, and state whether `--apply` must `git fetch --tags` first (it should, and fail loud if it can't — mirror the `origin/main` absent → fail-loud pattern at `build_release.py:169-174`). The "first release → allow" edge must be distinguished from "anchor unfetched → fail loud" — **an unreachable anchor is NOT a first release** and must not silently allow (that reopens the exact hole).
   - **Slice 7 (mechanized drop-check):** "the set of paths on `origin/main`-not-`release` ⊆ strip set." But `_apply()` **rebuilds `release` from `HEAD`** in the same run — so *which* `release` is the diff against: the stale published `origin/release`, the local `release` about-to-be-overwritten, or the just-built tree? The checklist's existing manual form (`git log origin/main --not release`, `RELEASE_CHECKLIST.md:28`) runs *after* the build against the freshly-built local `release`. For a `--apply` precondition that runs *before/around* the build, the honest computable form is **`origin/main`-not-`HEAD` ⊆ strip-set** (does the base I'm about to build from omit any shipped path present upstream?) — reusing `classify()` on the diff paths — NOT against a `release` ref that either doesn't exist yet or is about to be clobbered. **Name the exact ref pair in the Slice-7 spec** and confirm it composes with the from-`HEAD` build model. (Also confirm the ordering note: Slice 7 depends on Slices 1–3's `classify()` — that dependency is correctly stated and the reuse is genuinely computable *once the ref pair is right*; `classify()` is pure over a path list, so feeding it the diff-path list is sound.)

3. **[Correctness — Slice 4 hotfix/re-build edge] Define the same-version re-build behavior explicitly.** Risk (line 113) names "a re-build at the same version during a hotfix" but the Slice-4 spec only says "refuse if not strictly greater." A legitimate re-run of `--apply` at the *same* version (rebuild the local branch without a bump — e.g. re-running after a failed push) would be **refused** by a strict-greater check. Decide: either (a) the version check fires only on the *publish* step not every `--apply` (but the plan places it in `--apply` deliberately so it can't be skipped — tension), or (b) it compares against the last **published/tagged** version and a local rebuild at the current unreleased version is allowed until that version is tagged (this is why the **tag** anchor from change 2 is cleaner — you only refuse if `plugin.json.version <= latest_tag`, so re-building an untagged in-progress version is fine). Pick (b) and state it, or the guard blocks a normal rebuild-then-retry.

4. **[Slice completeness — Slice 5 test] The injected-failure test must prove a NON-vacuous failure path.** Given change 1, the Slice-5 unit test ("injected built-tree failure propagates non-zero") must assert the validation **actually ran and inspected the built tree** — not merely that `_apply()` returns non-zero. Mirror the `test_control_hook_is_armed` non-hollow-guard pattern already in `tests/test_build_release.py:267-276`: prove the validation would have PASSED on a good built tree AND FAILS on the injected-bad one, so the test can't go vacuously green if the validation silently no-ops (the exact risk change 1 identifies). Name this in the Slice-5 spec.

5. **[Honesty copy — Slice 9] The new mechanical-guard list must state each guard's TRUE strength, not imply totality.** Slice 9 documents "version-increase, built-tree validation, mechanized drop-check, broadened stale-base" as mechanical. Each has a genuine residual the copy must not paper over: the drop-check is a **subset guard, not a total drop guarantee** (the plan says this — keep it); the built-tree validation is only as strong as change-1's resolved mechanism (don't call it "runs the full suite"); the version-increase guard only fires vs the *tagged/published* anchor (so a same-version overwrite of an *untagged* build is intentionally allowed — say so). `honesty-reviewer` on Slice 9 should check the copy states each guard's scope, not a blanket "the release is now mechanically validated."

#### Sizing / completeness check (per slice)
- **Slice 4 (P0-1 version-increase)** — **BLOCKED on changes 2, 3.** Session-sized and lands complete *once the anchor (tag vs stale `origin/release`) and the same-version-rebuild edge are pinned*. The semver-compare helper + fixture tests (greater/equal/lesser/no-anchor) are a clean unit — the blocker is anchor semantics, not size.
- **Slice 5 (P0-2 built-tree validation)** — **BLOCKED on changes 1, 4.** As written it risks a silent no-op / vacuous green (the worst outcome for a guard). With the mechanism made concrete (change 1) and the non-hollow test (change 4) it is session-sized. This is the highest-risk slice — do not build it on the current "run the gate/tests against the built tree" wording.
- **Slice 6 (P2-2 broadened stale-base)** — **OK.** Session-sized, lands complete, test extends the existing `TestBaseAncestryGuard` fixture, rename contained. Ready as sliced.
- **Slice 7 (P1-3 mechanized drop-check)** — **BLOCKED on change 2** (the ref pair). Correctly ordered after Slices 1–3; the `classify()` reuse is genuinely computable once the diff ref pair is `origin/main`-not-`HEAD` (or an explicitly-justified alternative). Session-sized once the anchor is right.
- **Slice 8 (P1-1 tag)** — **OK.** Checklist step + tree/DECISIONS; `honesty-reviewer` on the copy; marketplace-repoint correctly left as a NOTED open question. Ready.
- **Slice 9 (P1-2 `--force-with-lease` + docs wrap-up)** — **OK, conditional on change 5** (honesty copy states per-guard scope). Checklist copy + DECISIONS; no script logic. Ready.

#### Buildability + ordering
- **Track ordering is correct.** The hardening track lands AFTER the DRY track (Slices 1–3) so Slice 7 can reuse the finalized `classify()` — verified sound (`classify()` is pure over a path list, `build_release.py:85-90`, so feeding it a diff-path list is a legitimate reuse). Within the hardening track, Slices 4→5→6→7→8→9 is buildable in order; only 4, 5, 7 carry the blockers above and they're independent of 6/8/9 (which can build as-is).
- **Build risk: MODERATE.** This is executable release tooling — the worst outcome is a guard that blocks a legitimate release (Slice 4's same-version rebuild, change 3) or one that lets a broken release through by silently no-opping (Slice 5's stripped-tree validation, change 1; Slice 7's wrong anchor, change 2). All three are anchor/mechanism-resolution gaps, not sizing problems — **pin them in the Spec and the track drops to LOW risk.** The existing test infrastructure (`TestBaseAncestryGuard` monkeypatch pattern + `TestApplyHookBypass` hermetic-repo fixture) is a solid base the new guard tests extend directly.

#### Harness impact
- **No new STANDARD/agent/doc-module.** The hardening extends existing harness-self tooling (`build_release.py` + `RELEASE_CHECKLIST.md`); the guards are updates to an existing surface, not new plugin surface. Zero adopter impact (all `DEV_ONLY`/stripped) — correctly stated.
- **DECISIONS line warranted** (version-increase + built-tree validation + tag anchor + `--force-with-lease` + mechanized drop-check + broadened stale-base; version-increase kept separate from `check_versions_synced.py`; marketplace-ref repoint deferred) — already listed in Affected files.
- **ROADMAP:** the marketplace-ref-repoint (P1-1's bigger call) is correctly a deferred open question, not built here — keep it a ROADMAP/open-question pointer.

**READY-TO-BUILD once** changes 1–3 (Slices 5, 4, 7 mechanism/anchor) are folded into the plan body + those slice specs, with changes 4–5 riding along in the Slice-5 test and Slice-9 honesty copy. Slices 6, 8, 9 are buildable as sliced now. Until the anchor/mechanism gaps are pinned: **CHANGES REQUIRED.**

**Stage-4 spec fixes folded in (2026-07-04):** (1) Slice 5 built-tree validation redesigned to run `check_shipped_content.py --root <tmp>` from the DEV checkout against the built worktree, pytest-against-built-tree step DROPPED as vacuous; (2) Slice 4 version-increase now anchors on the latest `vX.Y.Z` git tag (no tag → first release → allow; untagged same-version rebuild permitted), not the never-fetched `origin/release`; (3) Slice 7 drop-check now asserts `origin/main`-not-`HEAD` ⊆ strip-set reusing the DRY `classify()`, not a diff against the about-to-be-clobbered `release` ref; (4) Slice-5 test made non-vacuous (proves the scan ran + passes-good/fails-injected-bad); (5) Slice-9 honesty copy now requires stating each mechanical guard's true scope, not implied totality. Slices 4 & 7 ordering/dependency on Slice 8 (tags) documented under Decomposition (move Slice 8 earlier, or bootstrap the first tag). Affected-files (`check_shipped_content.py` `--root` arg) and Test-strategy updated to match.

## Spec  _(per slice, Stage 4)_
