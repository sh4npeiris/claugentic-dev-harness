# 0037 — "Dynamic-workflow harness" grounding + skill-usage observability (from the Claude Code blogs)

- **Status:** Draft (from the 3 Claude Code team blogs the user surfaced 2026-07-04: *getting-started-with-loops* · *a-harness-for-every-task-dynamic-workflows* · *lessons-from-building-skills*). **Blockers:** none for the docs slices; Slice 3 (hook) is behavior-adding → explicit go-ahead only. Additive; does NOT touch 0029/0030's landed work.
- **Disposition at close:** done / deferred (ROADMAP) / rejected (DECISIONS) per slice.
- **References:** the three blogs (first-hand-read, quotes below) · `.claude/plans/FINETUNING-INPUTS.md` → the CORRECTED `/loop`-`/goal` record (2026-07-04) · the harness's own `engine/audit.js` · `engine/build-item.js` · `engine/qa.js` + the Workflow tool · `docs/claugentic-WORKFLOW.md` (roles/patterns) · the skills blog's PreToolUse skill-usage-logging idea + the outstanding stale-eval/BASELINE drift-check.

## Problem

The dynamic-workflows blog defines a **dynamic workflow** as *"Claude writing its own multi-agent harness on the fly … a javascript file with a few special functions that help spawn and coordinate subagents"* — which is **exactly what the harness's `engine/*.js` + the Workflow tool ARE**. Yet the harness (a) had **dismissed** that framing as "debunked" (the 0032 over-refutation — being corrected in the current pass), (b) doesn't **name itself** a dynamic-workflow harness or cite the blog's articulation of **why** multi-agent structure beats single-context Claude, and (c) doesn't leverage two concrete, low-risk ideas the blogs surface. The harness's core patterns are *externally validated* by these blogs — we should ground in them, not ignore them.

**What the blogs give us (first-hand quotes):**
- Dynamic-workflows blog — our patterns, named: **fan-out-and-synthesize** (*"synthesize … a barrier — waits for all fan-out agents, then merges"*), **adversarial verification**, **tournament** (*"N agents … different approaches … judge pairwise"*), **loop-until-done** (*"until a stop condition is met"*), **classify-and-act**, **generate-and-filter**. The failure modes structure prevents: **agentic laziness (stopping early) · self-preferential bias (preferring own results) · goal drift**. Plus **token budgets** (*"use 10k tokens"*) and **pairing with `/loop`+`/goal`** for repeatable flows.
- Loops blog — the four loop **types** (turn/goal/time/proactive) as a valid taxonomy; code-quality-across-iterations (clean codebase · a way to self-verify · reachable docs · a **fresh-context reviewer**) — all things the harness already embodies.
- Skills blog — *"a **PreToolUse hook** that lets us **log skill usage**"* to spot *"skills that are … undertriggering"*; *"the highest-signal content in any skill is the **Gotchas section**"*; the *"don't state the obvious"* anti-pattern.

## Goals / Non-goals

- **Goal (docs — the honest core):** Name the harness a **dynamic-workflow multi-agent harness** and **ground its design** in the blog's three failure-modes (laziness / self-preference / goal-drift) — as the honest *rationale* for the clean-context-independent-reviewer + adversarial-verify architecture. **Grounding, NOT proof** (the blog explains why the shape helps; it doesn't certify the harness is correct). Home: a short WORKFLOW note + one DECISIONS line.
- **Goal (docs — pattern names):** Map the harness's existing orchestration to the blog's named patterns (fan-out-synthesize = audit fan-out / verify panels; adversarial-verify = finding-verifier; tournament = judge-panel/best-of-N; loop-until-done = audit loop-until-dry) so a maintainer sees the correspondence. A note, not new machinery.
- **Goal (Slice 3, behavior-adding — GO-AHEAD ONLY):** A **skill-usage-logging `PreToolUse` hook** — **fail-safe, non-blocking, observability-only** (logs which harness skill fired, never denies) — to measure triggering and catch the **stale-eval / undertriggering** drift. **Distinct from 0035** (which is a *blocking* deny-hook); this one only *observes*. Off-switchable; writes to `${CLAUDE_PLUGIN_DATA}` or a log path.
- **Goal (reconsider):** Reopen the **Gotchas-section** question the audit-verification refuted — the skills blog calls it *"the highest-signal content."* Evaluate whether a dedicated Gotchas section earns its place in the longest skills (vs the current inline+indexed coverage) — a `product`/`docs-traceability` judgment, likely a small doc slice or a ROADMAP note.
- **Non-goal:** Re-architecting the engines. The patterns are ALREADY there; this NAMES + grounds them (YAGNI — don't build a "pattern framework").
- **Non-goal:** Token-budget wiring into `engine/*.js` unless it's a trivial pointer — the Workflow tool already exposes `budget.*`; the engines are invoked by skills, not the raw Workflow tool. Confirm at Spec whether an explicit budget knob is warranted or a YAGNI (likely a ROADMAP note).
- **Non-goal:** Building Slice 3 without explicit go-ahead (any `PreToolUse` hook is behavior-adding; even observability-only fires on every tool call → must be fast + fail-safe + opt-in).

## Architecture & holistic fit

- **Codebase fit:** Slices 1-2 are docs (WORKFLOW note + DECISIONS line + pattern-map). Slice 3 is a new fail-safe hook (bundled via `plugin.json` hooks) + a log sink. Slice 4 (gotchas) is docs. SoC: the grounding names existing structure; the hook adds observability, not control.
- **Quality dimensions:** `docs-traceability` (the grounding/pattern-map is accurate; quotes attributed) · `product-ux` (the gotchas reconsideration). Slice 3 adds `reliability-resilience` (a per-tool hook must be fast + fail-safe) + `security` (a hook is a trust boundary, even observability). Trust surface → `honesty-reviewer`: the grounding must say **the blog validates the design's rationale, NOT that the harness is proven correct**; never claim the failure-modes are "solved," only "structurally mitigated."
- **Honesty invariant:** grounding cites the blog as **external design rationale** (model-upheld), never as a correctness proof. The skill-usage hook **observes**, never enforces — do not let its copy imply enforcement (that's 0035's unbuilt territory).
- **Doc-budget:** the grounding adds to WORKFLOW (not budget-gated) + ONE DECISIONS line — and it lands AFTER the condensation frees headroom (sequence it so DECISIONS stays comfortable; contribute a forward-looking keep-line, NOT a LANDED narrative — per the formalized condensation-prevention rule).

## Affected files (indicative)

- `docs/claugentic-WORKFLOW.md` — the "we are a dynamic-workflow harness + the three failure-modes rationale + the pattern-name map" note (Principles/roles area).
- `docs/claugentic-DECISIONS.md` — ONE forward-looking line (the dynamic-workflow identity + failure-modes grounding; blog-attributed; grounding-not-proof).
- *(Slice 3)* a new hook script + `plugin.json` hooks registration (fail-safe, opt-in, off-switchable) + tests + the log sink.
- *(Slice 4)* the longest skills' SKILL.md (a Gotchas section) OR a ROADMAP note if it doesn't earn it.
- `docs/claugentic-ARCHITECTURE_TREE.md` — rows for any new file (Slice 3).

## Risks & mitigations

- **Risk: grounding over-claims (the blog "proves" the harness works).** → `honesty-reviewer`: grounding = external rationale for the design shape, never a correctness proof; the failure-modes are *mitigated by structure*, not *solved*.
- **Risk: re-importing the "orchestration" confusion.** → keep the crisp split: `/loop`-`/goal` = scheduling/goal primitives; **dynamic workflow = the multi-agent harness (us)**; they pair. (The corrected WORKFLOW:259 + FINETUNING record are the reference.)
- **Risk (Slice 3): a per-tool hook is slow / fails / leaks.** → fast + fail-safe (an error degrades to allow+silent, never blocks a tool); opt-in + off-switch; observability-only (no deny path); a hook that gates is out of scope (that's 0035).
- **Risk: gotchas-section churn re-adds DRY duplication the audit flagged.** → only add where a real failure-mode isn't already captured inline+indexed; `yagni-sentinel` guards.

## Test strategy

- **Slices 1-2 (docs):** `pytest` · `check_shipped_content.py` (WORKFLOW/DECISIONS ship — adopter-aware, no stranded token) · `check_doc_budgets.py` (DECISIONS stays comfortable post-condensation) · tree.
- **Slice 3 (hook):** the hook's fail-safe behavior (probe error ⇒ allow, never block) · latency sanity · off-switch (flag off ≡ current behavior) · the log-sink format · `node`/`pytest` as the hook's language dictates.
- **Reviewer sign-offs:** `docs-traceability` + `product-ux` via `synthesizer-gate`; `honesty-reviewer` on the grounding-not-proof + observe-not-enforce framing; `yagni-sentinel` on Slice 3/4 scope.

## Decomposition (slices) — revised per the Stage-3 gate (CHANGES_REQUIRED folded in)

**Gate resolutions (Stage 4):** (1) WORKFLOW:261 + the DECISIONS dynamic-workflow line already name the identity / pair `/loop`-`/goal` / list the four loop types / map the engines — so the **genuinely-new content is only the three failure-modes rationale + the five *unmapped* pattern names**; **Slice 2 is absorbed into Slice 1** (the pairing already ships). (2) **ROADMAP is at 89.9% (~7 bytes below WARN)** — add NOTHING to ROADMAP without a user-approved condensation first; Slice 4 must avoid a ROADMAP line. (3) **No "lands-after-condensation" dependency** — DECISIONS has ~931 bytes of headroom; the one forward-looking line fits today. (4) **Slice 4 defaults to reaffirming the DECISIONS Gotchas-refutation** (carry the do-not-re-litigate provenance) — a dedicated Gotchas section is added ONLY if a concrete un-captured gotcha is named in a specific longest-skill; otherwise no doc surface changes. Minor: the Slice-3 log sink uses an **established** path (`${CLAUDE_PROJECT_DIR}` or the plugin root), NOT the nonexistent `${CLAUDE_PLUGIN_DATA}` — confirm at Spec.

- [ ] **Slice 1 — Dynamic-workflow identity (first-class) + the three failure-modes grounding + the five-unmapped-pattern map (docs).** Builds on WORKFLOW:261 (already names the engines the dynamic workflow); adds the honest **why** — the blog's three failure-modes (agentic laziness / self-preference / goal-drift) as the rationale for the clean-context-independent-reviewer + adversarial-verify architecture (grounding, NOT proof; "structurally mitigated," never "solved") — and maps the five unmapped patterns (fan-out-synthesize · adversarial-verify · tournament · loop-until-done · classify-and-act/generate-and-filter) to the harness's existing orchestration. **Absorbs Slice 2's pairing pointer.** WORKFLOW note + ONE forward-looking DECISIONS line (no condensation dependency). **In-scope:** `docs-traceability`; trust surface → `honesty-reviewer` (grounding-not-proof).
- [x] **Slice 2 — ABSORBED into Slice 1** (the `/loop`-`/goal` pairing + loop-type taxonomy already ship at WORKFLOW:261 / the DECISIONS dynamic-workflow line — no separate slice; token-budget wiring stays a non-goal, the Workflow tool already exposes `budget.*`).
- [ ] **Slice 3 — Skill-usage-logging `PreToolUse` observability hook (fail-safe, opt-in, off-switch, observe-only). BUILD ON EXPLICIT GO-AHEAD (granted 2026-07-04).** Log sink uses an established path (`${CLAUDE_PROJECT_DIR}`/plugin root), not `${CLAUDE_PLUGIN_DATA}`. **In-scope:** `reliability-resilience`, `security`, `testing`, `docs-traceability`.
- [ ] **Slice 4 — Reaffirm the DECISIONS Gotchas-refutation (default), OR add a Gotchas section ONLY if a concrete un-captured gotcha is named in a specific longest-skill.** No ROADMAP line (budget). **In-scope:** `product-ux`, `docs-traceability`; `yagni-sentinel`.

---

## Review  _(synthesizer-gate plan-gate, Stage 3)_

RUNNING AS: Opus 4.x (prose-orchestrated). Same-model caveat: this gate runs the same model family as the planner — a separate clean-context pass, a reduction of rubber-stamping risk, never model-family-independent verification.

**Verdict: CHANGES REQUIRED.**

The honesty spine is sound and the YAGNI discipline is real — but the plan's Problem statement is partly stale (the identity/`/loop`-`/goal` naming it says is missing is already in the tree), Slice 2 is largely redundant with existing copy, and the doc-budget analysis has a genuine blind spot (ROADMAP is 7 bytes from its WARN). Fix these before Approve.

### What holds (do not re-open)
- **Honesty invariant — PASS.** Grounding-as-rationale-not-proof, "mitigated-by-structure not solved," observe-not-enforce, and the explicit "0035's unbuilt territory" carve-out are stated in Goals, Architecture (`Honesty invariant`), and Risks, with `honesty-reviewer` assigned on every trust-bearing slice. Consistent with DECISIONS → *Honesty positioning* and the *two senses of "independent"* rule. No place lets the blog "prove" the harness.
- **Slice 3 scoping — PASS as gated.** Fail-safe (error ⇒ allow+silent), opt-in, off-switch, observe-only-never-deny, explicitly distinct from 0035's *deny*-hook, and correctly held behind explicit go-ahead. Verified against the repo: `.claude-plugin/plugin.json` today carries only the SessionStart advisor hook — no PreToolUse hook exists yet, so neither 0035 nor 0037-S3 is built. The observe/enforce split is the load-bearing distinction and the plan holds it.
- **Non-goals — PASS.** Correctly forbid re-architecting `engine/*.js` (confirmed present: `audit.js`/`build-item.js`/`qa.js`/`verify.js`), a "pattern framework," and token-budget wiring into the engines. The failure-modes grounding is genuinely new (grep: no `laziness`/`self-preferen`/`goal-drift` anywhere in docs today), so Slice 1's core is additive, not restated.
- **`/loop`-`/goal` framing — matches the corrected record.** Re-verified first-hand against WORKFLOW:261 + DECISIONS:63 + the FINETUNING corrected bullet (not trusting the unreliable "VERIFIED" labels): primitives-that-pair, four real loop types, dynamic-workflow = engine/*.js + Workflow tool. No re-import of the "debunked/orchestration" over-refutation.

### Required changes (numbered, actionable)
1. **Fix the stale Problem framing + de-dup Slice 2 against WORKFLOW:261 (DRY).** The Problem (line 9) claim that the harness "doesn't **name itself** a dynamic-workflow harness or cite the blog's articulation of **why**" is **partly false as written**: WORKFLOW:261 already states *"dynamic workflow: Claude writing its own multi-agent harness on the fly … which is exactly what the harness's engine/*.js + the Workflow tool already ARE,"* names all four loop types, pairs `/loop`-`/goal`, and maps build-to-green≈goal-based + loop-until-dry≈loop-until-done. DECISIONS:63 carries the same. So **Slice 2 (pair-with-`/loop`-`/goal` + the identity naming) is ~90% already-shipped** — folding a fresh note there would duplicate line 261. Rewrite the Problem to name what is *actually* missing (the two genuinely-new things): **(a)** the **three failure-modes** (laziness/self-preference/goal-drift) as the *rationale* for the clean-context-reviewer + adversarial-verify architecture, and **(b)** the **pattern-name map** for the patterns line 261 does NOT yet map — `fan-out-and-synthesize`, `adversarial-verification`, `tournament`, `classify-and-act`, `generate-and-filter` (line 261 already covers loop-until-done + goal-based; do not re-map those). **Resolve Slice 2 to a ROADMAP note or a one-line cross-reference to 261, not a new WORKFLOW block** — the plan's own "likely folds into Slice 1 or a ROADMAP note" instinct is correct; commit to it here rather than deferring the call to Spec, because the redundancy is now demonstrated, not speculative.

2. **Close the ROADMAP doc-budget blind spot (the analysis only covers DECISIONS).** Measured now: `docs/claugentic-ROADMAP.md` = 10793/12000 bytes = **89.9%, i.e. 7 bytes below the 10800-byte WARN threshold.** Slice 2's token-budget ROADMAP pointer, Slice 4's gotchas-fallback ROADMAP note, and the per-plan ROADMAP line the finder pipeline requires will **each trip the ROADMAP WARN and force a condensation pass** that the plan does not budget for. The *Architecture → Doc-budget* bullet and *Test strategy* line discuss only DECISIONS. Add ROADMAP to the doc-budget reasoning: either (a) sequence any ROADMAP add behind a ROADMAP condensation pass, or (b) keep total net ROADMAP additions ≤ 6 bytes (unrealistic — so effectively (a)). State it explicitly so a slice doesn't land red on `check_doc_budgets.py`.

3. **Make the DECISIONS sequencing dependency self-contained or drop it.** Architecture line 31 says the grounding "lands AFTER the condensation frees headroom." Measured: DECISIONS = 53069/60000 = 88.4%, **931 bytes below WARN** — one forward-looking keep-line fits *today* without any prior condensation. Either (a) drop the "lands after condensation" precondition (it's unnecessary for a single ≤931-byte line and creates a dependency on work not in this plan), or (b) if a condensation *is* intended as a gating precursor, name it as an explicit in-plan step with its own slice/disposition rather than an ambient assumption. As written it reads as a dependency on out-of-plan work — a session-sizing/completeness hazard.

4. **Slice 4 — carry the prior 0036 refutation into the plan, or default it to a ROADMAP note.** DECISIONS:92 records the 2026-07-03 authoring-audit outcome: *"skills 'Gotchas' sections (already inline + indexed)"* was one of the **5-of-6 refuted gaps**, marked **do-not-re-litigate**. Slice 4 reopens exactly that. Reopening on the strength of the skills blog's *"highest-signal content"* line is defensible, but the plan must **acknowledge the prior refutation and state what new evidence justifies reopening** (the first-hand blog quote), and should **default to a ROADMAP note** unless the evaluation finds a *concrete* skill where a real gotcha is genuinely uncaptured inline+indexed. Frame Slice 4 as "evaluate → ROADMAP by default; add only on a named, un-captured gotcha," so it can't become gold-plating the 0036 audit already refuted. `yagni-sentinel` is correctly assigned; make the default-to-defer explicit in the slice text.

5. **Slice 3 — flag `${CLAUDE_PLUGIN_DATA}` as an unverified feasibility claim (verify at Spec).** The log-sink target `${CLAUDE_PLUGIN_DATA}` (Goal line 20, Affected-files line 37) is **not used anywhere in the repo**; the established hook env vars are `${CLAUDE_PLUGIN_ROOT}` and `${CLAUDE_PROJECT_DIR}`. Per DECISIONS → *check a claim from the surface that MAKES it*, an unverified env var is a feasibility claim. Not a plan-blocker (Slice 3 is go-ahead-gated and deferred), but add a "confirm the var exists / else use `${CLAUDE_PLUGIN_ROOT}`-relative or a configured log path — verify against the hooks/plugins docs at Spec" caveat so the copy doesn't assert an unverified capability.

### Sizing / completeness check (per slice)
- **Slice 1 (identity + failure-modes + pattern-map, docs) — OK, after scoping.** Session-sized, vertically complete. Once change #1 lands, its genuinely-new content (failure-modes rationale + the 5 unmapped pattern names) is a clean single-session docs slice. Watch the DECISIONS keep-line stays *forward-looking*, never a LANDED narrative (per the condensation-prevention rule) — the plan already says so; hold it.
- **Slice 2 (pair-with-`/loop`-`/goal` + token pointers) — SPLIT/COLLAPSE needed.** Not wrong, but ~redundant with WORKFLOW:261 (change #1). Collapse into a one-line cross-ref in Slice 1 or a ROADMAP note; do not stand it up as a separate WORKFLOW block.
- **Slice 3 (observability hook) — OK as a gated unit, DO NOT BUILD without go-ahead.** Behavior-adding; correctly fenced. Sizeable but single-session (hook + probe + off-switch + tests + tree + log sink). Honor the plan's own "explicit go-ahead only." Resolve change #5 at Spec.
- **Slice 4 (gotchas reconsideration) — OK as an *evaluation* that defaults to ROADMAP.** Complete-as-a-decision only if it defaults to defer (change #4); an "add a Gotchas section to the longest skills" build would risk re-adding the DRY duplication the 0036 audit refuted — keep it evaluation-first.

### Harness impact
- **Slice 1:** one forward-looking DECISIONS line (the *workflow & roles* section, sibling to the existing 0032 `/loop`-`/goal` line at DECISIONS:63) + a WORKFLOW note. No new STANDARD, no new agent, no INVARIANT (the grounding is model-upheld rationale, not a must-hold constraint).
- **Slice 3 (if built):** a new hook script + `plugin.json` PreToolUse registration + tree rows + tests + a log-sink format. This is the first PreToolUse hook the plugin would ship; it **must not** read as enforcement in any adopter-facing copy (0035 owns the deny-hook territory). Also check the SessionStart-advisor portability precedent (DECISIONS:103 — rooted `${CLAUDE_PLUGIN_ROOT}` form, fail-safe exit 0) applies: an observe-only hook may use the fail-safe launcher form; a future deny-hook may not.
- **Slice 4:** no harness impact if it defers to ROADMAP (the likely outcome).

## Spec  _(per slice, Stage 4)_
