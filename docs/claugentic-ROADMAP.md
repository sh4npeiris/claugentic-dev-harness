# Roadmap

The forward backlog for the harness itself. Work runs through `docs/claugentic-WORKFLOW.md` (triage → plan → review → spec → approve → implement → verify → land → retrospect), sliced so each unit lands complete with no tech debt. Tangents land here, never silently into the current change.

> **How this file works.** The two fenced sections below are **regenerated** — `/claugentic-dev-harness:audit` writes the engineering one, `/claugentic-dev-harness:product` gap mode the product one; **never hand-edit inside a fence** (a re-run overwrites it). Everything under *Standing sections* is hand-written and never fence-touched — put your own ideas there. Run **`/claugentic-dev-harness:build`** to work the backlog. This is the harness's OWN roadmap, not a generated audit; an adopter generates theirs by running those two skills against their repo.

---

## Engineering backlog — the work worth doing  _(generated by /claugentic-dev-harness:audit)_

<!-- harness-audit:backlog:start -->
No open items — the harness is feature-complete for v1. (An adopter's backlog is generated here by `/claugentic-dev-harness:audit` when they run it on their repo.)
<!-- harness-audit:backlog:end -->

---

## Product backlog — gaps vs your spec  _(generated by /claugentic-dev-harness:product gap)_

<!-- harness-product:backlog:start -->
No product spec yet for the harness itself — run `/claugentic-dev-harness:product` spec mode to define one, then gap mode regenerates this fence. (An adopter's product backlog is generated here when they run gap mode against their `docs/claugentic-PRODUCT_SPEC.md`.)
<!-- harness-product:backlog:end -->

---

## Standing sections — human/agent-owned (yours · never auto-touched by any fence)

A fence replaces **only** the text between its markers, so a re-audit never wipes anything here. Add a section only on real need (KISS/YAGNI); prefer a type tag on an item over a new section. (Dismissed findings are not hand-written — they live in the audit's **wired** `<!-- harness-audit:rejected-findings -->` fence, read by the audit/build engines to skip a dropped finding.)

### Bugs

Out-of-scope defects spotted mid-work, jotted here as **planless one-liners** so capturing one stays cheap (trivial fixes skip even this). A bug stays a one-liner until you select it to fix — selecting it triggers its own plan (per `0025`), then the normal pipeline.

- **`harness`** — a conflict-free `git merge` commits via `pre-merge-commit`, which nothing wires — **neither chained gate runs on the merge result** (measured, git 2.55; a server-side PR merge runs no local hook; CI's WARNs are exit 0, so a band only the merge trips lands green — 0041 S7 L1). Fix: chain the wrapper from `pre-merge-commit`, both homes, sweeping the sibling "every commit" sites (init SKILL:597/:740/:998 · WORKFLOW:173 · the wrapper spec).

### Later / Ideas

Longer-term tracks and ideas — hand-written, never fence-touched. Deferred harness-maintenance / `doctor` findings ride here with a **`harness`** tag (no separate section). Nothing here is committed work: each item carries the evidence that would pull it in.

- **`harness`** — **Plan 0041 — adopter hardening (ACTIVE, `.claude/plans/0041-adopter-hardening.md`).** S1–S9 landed; **S10** engine fixes · **S11** eval leak · **S12** WORKFLOW weight + the whole-feature closing pass remain. Tracked there: remediating the 3 existing deployments · doctor post-conditioning the DELIVERED gate + seeded caps (today only an N-A gates row) · WORKFLOW is the one large managed doc absent from the caps config, which is why it grew unchecked to ~94 KB.
- **`harness`** — **Gate items — reviewer catches a gate could have made; none built, all model-upheld today.** **(a) Local gate runs must be exit-code-OBSERVED** (0041 S3): `<gate> | tail -1` reports the FILTER's exit — a red gate read green at Land. CI is immune (per-step jobs); the exposure is local. Close: a runner deriving the gate list from `ci.yml`'s gates job (never a third hand-list), one PASS/FAIL line per gate + non-zero aggregate. **(b) Spec/plan file-lists anchor on CONTENT, never line numbers** (0041 S4; second axis S8): an anchor drifted 2 lines before implement-time, and a spec cited a body section existing only in frontmatter. Close: flag `<path>.md:<N>` inside a slice's **Spec** block (exempting Review/verdict blocks — findings are contractually `file:line`) and flag a quoted section name the file lacks; degrade to a named `synthesizer-gate` checklist item, never a silent drop. **(c) An exemption inside a scan is itself scanned** (0041 S6, the same defect twice): `HISTORICAL_RE` exempts lines from both stale-claim scans and is the one vocabulary the scan-vocabulary test skips. Close: a branch-set identity pin derived from the regex source + per-branch fixtures BOTH ways — writing the second fixture (a live denial carrying the same phrase is still caught) is what kills a forward/present-state branch at authoring time. **(d) Release-workflow matrix-leg guard** (0041 S2): the on-main ancestry check and `plugin validate --strict` run on the **Linux leg only**, so dropping `ubuntu-latest` from `gates`' `matrix.os` voids both **with the suite green**. Close: one assertion that the matrix still contains it. **(e) Repo-wide corpus scans enumerate from `git ls-files`, never a disk walk** (0041 S9; incident recorded in `docs/claugentic-standards/testing.md`). Close: one pin flagging a repo-rooted `rglob(`/`os.walk(` under `tests/`+`scripts/` — CI's fresh clone is blind to this class by construction.
- **`harness`** — **0041 verify-deferred batches.** **S2 follow-ons:** dependabot.yml for github-actions · pin `@anthropic-ai/claude-code` in release.yml only if a floating validator breaks a run · optional `concurrency:` group · publish-call-site base-ancestry vs a moving main · the first real tag push proves unauthenticated `claude plugin validate`. **S3 advisor hardening:** git-subprocess `timeout=` **paired with** its `TimeoutExpired` except (never split — a bare timeout re-creates the totality bug) · off-switch short-circuits `derive_state()` · a call-count pin for one-subprocess-per-plan · a lead-plan sorted-order pin. **S4 `_repo_root()` 5-file sweep:** widen the except to `(OSError, subprocess.CalledProcessError)` + `timeout=` across ALL five byte-identical copies in ONE pass (a lone fix breaks the byte-identity that makes the duplication safe) · a `.gitignore` un-ignore (`!.claude/claugentic-*.json`) so the next top-level `.claude/` file isn't double-invisible · run pytest once from a non-root cwd in CI.
- **`harness`** — **Shipped pointers that don't resolve in an adopter (0041 S9 residual).** Two classes S9 scoped out; neither is claimed swept anywhere, so no copy over-claims. `.claude/agents/<role>.md` as a read-this-file instruction in shipped skills/README (8 sites), and bare ledger filenames missing the `docs/` prefix (~50–75, pattern-dependent). **Re-derive by grep before fixing — never work from a stale site list.**
- **`harness`** — **Repo-wide mechanical-vs-run-gate register sweep (0041 S9).** `CLAUDE.md:3` collapses *hook-enforced* into *mechanically enforced*, dropping the deterministic **run-gate** class (DECISIONS → honesty names it, and S9 made it canonical in `honesty-reviewer`). The always-loaded anchor under-describes its own wiring; sweep the siblings in one pass. Same anchor, second class: `:17`'s "a commit-time hook enforces presence" holds only inside `INCLUDE_GLOBS` — three tracked test files sat un-indexed.
- **`harness`** — **Ledger hygiene.** INVARIANTS climbs the sharding ladder at its own WARN (rung 2 may be its answer; DECISIONS → doc-lifecycle has the ladder) · the `deterministic-gates` shard restates the five cap numbers the caps-config owns — a second source of truth with **no owning slice**; fold it into the next doc-lifecycle condensation.
- **`harness`** — **Re-chain a pre-v0.5.2 adopter's wrapper (0041 S7 §3 routing).** S7 makes only the *report* honest (a wrapper with no `run_gate` is never auto-chained, and the one-line remedy is refused by name — pasted in, it disarms the tree gate). The **repair** is unbuilt: a released-shape catalogue, or `init --rewrite-wrapper` (confirm-and-replace).
- **`harness`** — **Restrict pushes to `release` — makes ONE-PUBLISHER mechanical (0041 S2).** INVARIANTS states the branch half is **model-upheld until `release` is branch-protected** (verified live: no protection, no rulesets). A ruleset restricting pushes to the Actions app converts the contract into a platform guarantee. **User-applied GitHub setting** (`docs/RELEASE_CHECKLIST.md` → Branch protection); tracked so it doesn't stay model-upheld by default.
- **`harness`** — **Bootstrap-and-approve self-extension — the living, per-project layer (user-approved 2026-06-29).** The harness studies each project and **PROPOSES** the tailored set it needs (in-scope + domain standards modules · domain specialist agents · project-relevant skills) for the **user to approve**; Stage 9 grows it. Extends two-tier promotion **downward** (generate-for-this-repo). **Autonomous self-deploy out of scope.** Sequenced after the charter (`0030`); the largest unrealized piece of the vision.
- **`harness`** — **Multi-charter / domain-pack (DIAGNOSED, not built).** The kernel is domain-NEUTRAL; a swappable **domain-pack** re-points it at a non-software domain (methodology ⊂ domain-pack; strongest first pack = deep-research/technical-writing; the one hard seam = the software-bound EXECUTION engine, else the `manual` fallback). **Sequencing (YAGNI): engineering charter + the one software pack FIRST.** Seam reserved in the standards README; rationale in DECISIONS.
- **`harness`** — **doctor ledger-coherence check (`[J]`, flag-only — user idea 2026-07-06).** Scan the ledgers (DECISIONS/INVARIANTS/CLAUDE.md) for semantically conflicting entries and FLAG the pair — **detection only; resolution is above doctor's treat-boundary** (never auto-fix). Today's guards are all write-time + model-upheld; this adds a periodic net.
- **`harness`** — **Release/init-contract completeness gate — RESIDUAL only** (core delivered by `0034`). **Deferred, YAGNI-until-a-real-miss:** an agent missing from `plugin.json:agents` has **no gate** (silently won't load) · a dev-only file forgotten from the manifest **ships by default-include** instead of failing loud · the belt-and-suspenders **build→init→diff round-trip** test. Gates WIRING, never SEMANTICS.
- **`harness`** — **Experience-craft ("addractive") — two deferred parts** (core landed v0.3.0 / `0029`; DECISIONS → *Craft is first-class*): **(a)** `runtime-qa` felt/runtime observation — **OPT-IN, never default-on** (capability unproven + context cost); **(b)** the `transition`-state enum slice — engine-touching, frozen-schema; stays deferred.
- **`harness`** — **Small standards fills (JIT, not keystone).** Author `architecture-styles` (reserved), an `error-handling` philosophy module, and a `domain-design`/DDD module **each only when a change pulls it in** — they are *not* the architecture keystone (`maintainability-structure` already cites Clean/Hexagonal/Onion/DDD/Saga/Outbox, and the plan *Architecture & holistic fit* section frames system-shape up front).
- **`harness`** — **Adoption-evidence-gated (`0036`) — author only if a real adopter proves the need:** a consolidated *Common Adoption Gotchas* digest in INVARIANTS (inline+indexed coverage is the deliberate DRY answer) · guidance on wiring an adopter's own design-system/QA MCP (the harness declares and ships none, by design).
- **`harness`** — **0040 Verify banked:** the non-`.md`-in-shard-dir stance. *(The engine `nsAgent()` bare-name fallback moved into plan 0041 S10.)*
- **`harness`** — Stage-2b advisory panel as an `engine/verify.js`-orchestrated fan-out: prose-convened 2b works; engine orchestration only if it proves unwieldy at scale (`0026` §C; the prompt mode landed).
- **`harness`** — Eval residuals, and a stale baseline: `eval/BASELINE.md`'s newest row is v0.5.0 — record one per release.
