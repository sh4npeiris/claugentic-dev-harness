# Roadmap

The forward backlog for the harness itself. Work runs through `docs/claugentic-WORKFLOW.md` (triage → plan → review → spec → approve → implement → verify → land → retrospect), sliced so each unit lands complete with no tech debt. Tangents land here, never silently into the current change.

> **How this file works.** The two fenced sections below are **regenerated** — `/claugentic-dev-harness:audit` writes the engineering one, `/claugentic-dev-harness:product` gap mode the product one; **never hand-edit inside a fence** (a re-run overwrites it). Everything under *Standing sections* is hand-written and never fence-touched — put your own ideas there. Run **`/claugentic-dev-harness:build`** to work the backlog. This is the harness's OWN roadmap, not a generated audit; an adopter generates theirs by running those two skills against their repo.

---

## Engineering backlog — the work worth doing  _(generated by /claugentic-dev-harness:audit)_

<!-- harness-audit:backlog:start -->
No open items — the harness is feature-complete for v1. (An adopter's backlog is generated here by `/claugentic-dev-harness:audit` when they run it on their repo.)
<!-- harness-audit:backlog:end -->

---

## Product backlog — gaps vs your spec  _(generated by /claugentic-dev-harness:product gap)_

<!-- harness-product:backlog:start -->
No open items. Gap mode ran 2026-08-19 against `docs/claugentic-PRODUCT_SPEC.md` and measured 25 gaps; **all 25 are closed** (plan 0043, landed in 0.5.3 — git history holds it). Re-run gap mode to refresh. (An adopter's product backlog is generated here when they run gap mode against their `docs/claugentic-PRODUCT_SPEC.md`.)

_This read the code against the spec — it did not run the app. Runtime checking is the QA workflow._
<!-- harness-product:backlog:end -->

---

## Standing sections — human/agent-owned (yours · never auto-touched by any fence)

A fence replaces **only** the text between its markers, so a re-audit never wipes anything here. Add a section only on real need (KISS/YAGNI); prefer a type tag on an item over a new section. (Dismissed findings are not hand-written — they live in the audit's **wired** `<!-- harness-audit:rejected-findings -->` fence.)

### Known issues

One open, honestly disclosed:

- **`crossModel` keys on the ORCHESTRATOR's family, not the finding-producer's** (`engine/audit.js` folds judge self-reports against `builderFamily` = the session model). A Fable session driving Opus sub-agents prints `crossModel: true` while finder↔judge are both Opus — the relationship rubber-stamping actually guards. Recall/count outputs are unaffected; only the cross-model *claim* over-states. Fix: fold against the lens agents' `RUNNING AS` families, or caveat the claim. Measured case: `eval/BASELINE.md` (2026-08-20).

### Future directions

Considered, evidence-gated — nothing committed. Full detail lives in git history and the eval ledgers; these are the two substantive tracks:

- **`harness`** — **Init split (the natural 0.7.0 headline).** Split `skills/init/SKILL.md` (≈74.7 KB ≈ 40% of the skills corpus) into a lean skill + an on-demand reference — ~45 KB off the context an agent loads whenever init is merely *considered* (context-load, not download payload). Deferred as a structural refactor of the contract-densest command (~7 test-pin sites); sequence: description-parity gate → split → wrapper de-dup. Detail: git history of plan 0044 §5 + `groundwork/skills-merged.md`.
- **`harness`** — **Re-sharpen the build exam so it discriminates at top-tier builder strength.** The 0044 S2b sitting saturated (both the full and cut catalogs scored 10/10 at Opus strength); the deep standards cut landed on detection recall + the pre-registered no-regression rule, not a build-path delta. The calibration allowance permits re-sharpening the brief/traps once (voiding prior runs). Pull-in evidence: a proposed standards cut deeper than 91 KB that needs an output-path gate. Detail: `eval/BUILD_ENTRIES.md` entry 3.
