# Roadmap

The forward backlog for the harness itself. Work runs through `docs/claugentic-WORKFLOW.md` (triage → plan → review → spec → approve → implement → verify → land → retrospect), sliced so each unit lands complete with no tech debt. Tangents land here, never silently into the current change.

> The harness audits *adopter* repos via `/claugentic-dev-harness:audit`, which writes its own `harness-audit:overview` / `harness-audit:backlog` fences into that repo's `docs/claugentic-ROADMAP.md`. This file is the product's own roadmap, not a generated audit — an adopter generates theirs by running the `/claugentic-dev-harness:audit` + `/claugentic-dev-harness:product` gap fences.

> **This is your backlog.** The two fenced sections just below are **regenerated automatically** — `/claugentic-dev-harness:audit` writes the engineering one, `/claugentic-dev-harness:product` gap mode writes the product one; don't hand-edit inside the fences (a re-run overwrites them). Run **`/claugentic-dev-harness:build`** to work them. Put **your own ideas and longer-term tracks** under *Standing tracks & later* at the bottom — that section is hand-written and never touched by any skill.

---

## Engineering backlog — the work worth doing  _(generated by /claugentic-dev-harness:audit · do not edit — re-run to refresh · run /claugentic-dev-harness:build to see it merged with the product backlog)_

<!-- harness-audit:backlog:start -->
No open items — the harness is feature-complete for v1. (An adopter's backlog is generated here by `/claugentic-dev-harness:audit` when they run it on their repo.)
<!-- harness-audit:backlog:end -->

---

## Product backlog — gaps vs your spec  _(generated by /claugentic-dev-harness:product gap · do not edit — re-run to refresh · run /claugentic-dev-harness:build to see it merged with the engineering backlog)_

<!-- harness-product:backlog:start -->
No product spec yet for the harness itself — run `/claugentic-dev-harness:product` spec mode to define one, then gap mode regenerates this fence. (An adopter's product backlog is generated here when they run gap mode against their `docs/claugentic-PRODUCT_SPEC.md`.)
<!-- harness-product:backlog:end -->

---

## Standing sections — human/agent-owned (yours · never auto-touched by any fence)

Everything below lives **outside** the two generated fences above (`harness-audit:backlog` / `harness-product:backlog`), so a re-audit or re-gap — which replace **only** the text between their start/end markers — **never wipes it.** Add a section only on real need (KISS/YAGNI); a lightweight type tag on an item is preferred over spawning a new section. (Dismissed audit findings are NOT a hand-written section here — they live in the audit's own **wired** `<!-- harness-audit:rejected-findings -->` fence, created on first use, which the audit/build engines read to skip a dropped finding.)

### Bugs

Out-of-scope defects spotted mid-work, jotted here as **planless one-liners** so capturing one stays cheap (trivial / mechanical fixes skip even this). A bug stays a one-liner until you select it to fix — selecting it triggers its own plan (per `0025`); it's then triaged / built via the normal pipeline.

- _(none open)_

### Later / Ideas

Longer-term tracks and ideas — hand-written, never fence-touched. Deferred harness-maintenance / `doctor` findings ride here with a **`harness`** tag (no separate section). The harness is **feature-complete — there is no open backlog;** new work, if it arises, is surfaced by an audit (the fences above) or added here by hand.

- **`harness`** — audit synthesis-ceiling: auto-size `maxCellsPerRun` + hierarchical per-lens roll-up for very large repos — only if a real repo exceeds the single-pass ceiling (YAGNI until measured; interleaving already covers every lens in a sane-budget pass).
- **`harness`** — **Ledger sharding (deferred lever, YAGNI).** The condensation pass (WORKFLOW → Definition of Done) is the standing answer to a ledger at its doc-budget cap; splitting an oversized ledger into shards is the large-repo lever, reached for **only if** a ledger can't be condensed below cap — mirrors the tree's "condense now, shard later" stance. Not built; no evidence it's needed.
- **`harness`** — **Release/init-contract completeness gate (generalizes the `plugin.json:agents` pin).** Today `check_shipped_content.py` catches the *symptoms* (a shipped doc citing a stripped-uncreated **path** → hard fail; a stranded agent/skill **namespace** token → hard fail), but its `_INIT_CREATES` allow-list (`:103-112`) and the `test_build_release.py` ship-pins are **hand-maintained**, so *under-wiring* a new user-file isn't fully fail-loud: **(a)** a new agent left out of `plugin.json:agents` has **no gate** (silently won't load); **(b)** a new seed (`_X.md`→`X.md`) whose created twin isn't added to `_INIT_CREATES` false-flags as a dangle or slips; **(c)** a missing ship-pin isn't self-caught. Fix = **one referential-closure run-gate** (CI + DoD, not a hook): assert **NEEDS ⊆ HAS** (HAS = shipped ∪ init-created; NEEDS = everything shipped content references) with **both sides DERIVED from the filesystem + naming conventions** (the `_X.md`→`X.md` convention self-derives init-creates — **no hand-kept `_INIT_CREATES`**); a file matching **no** convention **forces a one-time role declaration** (forgot-to-register fails LOUD). Derive the derivable lists (`_INIT_CREATES` · `KNOWN_MODULES` · agents-list); shrink the irreducibly-*semantic* ones (`DEV_ONLY` judgments · the allow-lists) to conventions + a gate-verified **declared-exceptions** set. Type-agnostic; evolves `check_shipped_content.py` (belt-and-suspenders = a round-trip *build→init→diff* test). **Honest limit — gates WIRING, never SEMANTICS** (seed-content currency / category judgment stay model-upheld). Companion to `0030`'s `_CHARTER.md` (the first new seed). (User's release-robustness question, 2026-06-29.)
- **`harness`** — **Modernize from recent Claude Code updates (2026-06-29; mostly already-embodied — align, don't rebuild).** **(a)** Top tier is now **Fable 5** (`best`) above Opus, for *tasks larger than a single sitting* (build-to-green · deep audits · long research) — add to the WORKFLOW model-tier note as the long-running-work tier (Opus stays the standard default). **(b)** Loops + dynamic-workflows already embodied (build-to-green ≈ goal-loop; build/audit/verify use the Workflow tool) — doc-*alignment* to the official taxonomy, not new capability. **(c)** Skills-taxonomy = a lens for future skills. **(d) `/design-sync`** = the design-system integration now referenced by `0029`. Deferred; blocks nothing.
- **`harness`** — Stage-2b advisory panel as an `engine/verify.js`-orchestrated path (a `lens-reviewer` plan-design fan-out, mirroring the Stage-7 Verify panel): 2b is prose-convened today and works (advisory, builder-class, 1-round). An engine orchestration would only pay off if a prose 2b proves too unwieldy at scale — YAGNI until then (deferred from 0026 §C; the `lens-reviewer` plan-design *prompt* mode still landed, only the engine orchestration is deferred).
- **`harness`** — **Engineering charter (methodology-aware, not -locked)** + its 1st behavior **test-first/TDD** (feature work test-first *where it earns it*, via an **independent test-author**; encourage-now / enforce-when-the-unbuilt-PreToolUse-hook-lands). A durable per-project ways-of-working record (sibling to `docs/claugentic-PRODUCT.md`) that **records** the project's choice, never imposes it — closes the "pick a methodology and stick to it" gap; keep it lean (over-confining risk). Harness already embodies red-first for `bug`/`refactor` + is structurally ATDD, so no conflict. **Planned → `.claude/plans/0030-engineering-charter.md`**; rationale in `docs/claugentic-DECISIONS.md`. (User-approved 2026-06-29.)
- **`harness`** — **Bootstrap-and-approve self-extension — the living, per-project layer.** The harness studies each project and **PROPOSES** the tailored set it specifically needs — in-scope + domain-specific standard modules · domain specialist agents · project-relevant skills — which the **user approves**; Stage 9 grows it. This is what makes the harness *adapt to and elevate each project on its own terms* (paired with the per-project charter + design-language). Extends two-tier promotion **downward** (generate-for-this-repo). **Autonomous self-deploy out of scope** (no-over-claim + user-approved-promotion). Sequenced **after** the charter (0030); the largest unrealized piece of the vision. (User-approved 2026-06-29.)
- **`harness`** — **Small standards fills (JIT, not keystone).** Author `architecture-styles` (still reserved), an `error-handling` philosophy module, and a `domain-design`/DDD module **each only when a change pulls it in** — they are *not* the architecture keystone (`maintainability-structure` already cites Clean/Hexagonal/Onion/DDD/Saga/Outbox, and the plan *Architecture & holistic fit* section now frames system-shape up front). Lower priority than the two items above.
- **`harness`** — **Multi-charter / domain-pack ("other types of work" — DIAGNOSED, not built).** The kernel is domain-NEUTRAL (review engine · honesty engine · 6 reviewer postures · 10-stage spine · ledger trio); a swappable **domain-pack** (catalog + DoD gates + tag→discipline + vocab + 1–2 specialists + execution binding) re-points it at a non-software domain. "Charter" = two altitudes: **methodology ⊂ domain-pack**. Strongest first pack = **deep-research / technical-writing**; the one hard seam = the software-bound EXECUTION engine (`build-item.js`/`qa.js`, else the `manual` fallback). **Sequencing (YAGNI): build the engineering charter + mature the one software pack FIRST.** Reserved seam in `docs/claugentic-standards/README.md`; full rationale in `docs/claugentic-DECISIONS.md`.
- **`harness`** — **Experience-craft elevation ("addractive" — loved, not just correct).** Reframes "done" to also *crafted*. **Landed:** the `product-ux.md` "Aesthetic & motion craft" dimension (v0.2.0; `[D]` floor / `[J]` ceiling — never certifies "beautiful"). **Remaining slices:** `product-designer` craft forcing-function · per-project **design-language** seam (the anti-sameness answer) · attributed **motion baseline** (Kowalski · GSAP · easings.net · impeccable · taste-skill) · DoD/architect-pass craft bar · `runtime-qa` felt-observation kept **opt-in** · README **"addractive"** headline — all ride the **architect-pass rail** (the reusable pattern for first-classing any hard-to-gate concern). **Planned → `.claude/plans/0029-ui-ux-craft.md`**; rationale in `docs/claugentic-DECISIONS.md`.
