# Roadmap

The forward backlog for the harness itself. Work runs through `docs/claugentic-WORKFLOW.md` (triage → plan → review → spec → approve → implement → verify → land → retrospect), sliced so each unit lands complete with no tech debt. Tangents land here, never silently into the current change.

> The harness audits *adopter* repos via `/claugentic-dev-harness:audit`, which writes its own `harness-audit:overview` / `harness-audit:backlog` fences into that repo's `docs/claugentic-ROADMAP.md`. This file is the product's own roadmap, not a generated audit — an adopter generates theirs by running the `/claugentic-dev-harness:audit` + `/claugentic-dev-harness:product` gap fences.

> **This is your backlog.** The two fenced sections just below are **regenerated automatically** — `/claugentic-dev-harness:audit` writes the engineering one, `/claugentic-dev-harness:product` gap mode writes the product one; don't hand-edit inside the fences (a re-run overwrites them). Run **`/claugentic-dev-harness:build`** to work them. Put **your own ideas and longer-term tracks** under *Standing tracks & later* at the bottom — that section is hand-written and never touched by any skill.

---

## Engineering backlog — the work worth doing  _(generated by /claugentic-dev-harness:audit · do not edit — re-run to refresh · run /claugentic-dev-harness:build to see it merged with the product backlog)_

<!-- harness-audit:backlog:start -->
No open items — the harness is feature-complete for v1. (An adopter's backlog is generated here by `/claugentic-dev-harness:audit` when they run it on their repo.)
<!-- harness-audit:backlog:end -->

---

## Product backlog — gaps vs your spec  _(generated by /claugentic-dev-harness:product gap · do not edit — re-run to refresh · run /claugentic-dev-harness:build to see it merged with the engineering backlog)_

<!-- harness-product:backlog:start -->
No product spec yet for the harness itself — run `/claugentic-dev-harness:product` spec mode to define one, then gap mode regenerates this fence. (An adopter's product backlog is generated here when they run gap mode against their `docs/claugentic-PRODUCT_SPEC.md`.)
<!-- harness-product:backlog:end -->

---

## Standing sections — human/agent-owned (yours · never auto-touched by any fence)

Everything below lives **outside** the two generated fences above, which replace **only** the text between their markers — so a re-audit or re-gap **never wipes it.** Add a section only on real need (KISS/YAGNI); prefer a type tag on an item over a new section. (Dismissed findings are not a hand-written section — they live in the audit's **wired** `<!-- harness-audit:rejected-findings -->` fence, read by the audit/build engines to skip a dropped finding.)

### Bugs

Out-of-scope defects spotted mid-work, jotted here as **planless one-liners** so capturing one stays cheap (trivial fixes skip even this). A bug stays a one-liner until you select it to fix — selecting it triggers its own plan (per `0025`), then the normal pipeline.

- _(none open)_

### Later / Ideas

Longer-term tracks and ideas — hand-written, never fence-touched. Deferred harness-maintenance / `doctor` findings ride here with a **`harness`** tag (no separate section). The harness is **feature-complete — there is no open backlog;** new work, if it arises, is surfaced by an audit (the fences above) or added here by hand.

- **`harness`** — Eval residuals: `eval/BASELINE.md`
- **`harness`** — audit synthesis-ceiling: auto-size `maxCellsPerRun` + hierarchical per-lens roll-up for very large repos — only if a real repo exceeds the single-pass ceiling (YAGNI until measured; interleaving already covers every lens in a sane-budget pass).
- **`harness`** — **doctor ledger-coherence check (`[J]`, flag-only — user idea 2026-07-06).** Scan the ledgers (DECISIONS/INVARIANTS/CLAUDE.md) for semantically conflicting entries and FLAG the pair — **detection only; resolution is above doctor's treat-boundary** (never auto-fix). Today's guards are all write-time + model-upheld; this adds a periodic net.
- **`harness`** — **INVARIANTS ledger climbs the sharding ladder at its own WARN** (10.4K/20K — nothing now; rung 2 may be its right answer). DECISIONS took rung 3 (landed 2026-07-30, plan 0040; rationale in the decisions ledger → doc-lifecycle). The tree's "condense now, shard later" stance untouched.
- **`harness`** — **0040 Verify banked (2026-07-30, low):** CWD-coupling (`TestClosurePassD` red from `tests/`, pre-existing; `check_doc_budgets.py` lacks a `_repo_root()` anchor) · index↔`REQUIRED_SHARDS` agreement test · non-`.md`-in-shard-dir stance · engine `nsAgent()` hard-namespaces spawns — the wired path fails without the plugin agent registry; **hit twice now** (0040 Verify went prose-orchestrated; the 2026-07-30 eval needed a one-line bare-name shim to run at all), so a bare-name fallback is the standing fix.
- **`harness`** — **Release/init-contract completeness gate — RESIDUAL only** (core delivered by 0034: the `NEEDS ⊆ HAS` closure gate + manifest-derived hand-lists; see git). **Deferred, YAGNI-until-a-real-miss:** (a) a new **agent** left out of `plugin.json:agents` has **no gate** (silently won't load); (b) a new **dev-only** file forgotten from the manifest **ships by default-include** instead of failing loud; (c) the belt-and-suspenders **build→init→diff round-trip** test. Gates WIRING, never SEMANTICS.
- **`harness`** — Stage-2b advisory panel as an `engine/verify.js`-orchestrated fan-out: prose-convened 2b works; engine orchestration only if it proves unwieldy at scale — YAGNI until then (0026 §C; the prompt mode landed).
- **`harness`** — **Plan 0041 — adopter hardening (ACTIVE 2026-08-12,** `.claude/plans/0041-adopter-hardening.md`): ship the budget/currency machinery to adopters; remediating the 3 existing deployments (re-init + condense in-repo) is follow-on work there, not in 0041.
- **`harness`** — **Bootstrap-and-approve self-extension — the living, per-project layer.** The harness studies each project and **PROPOSES** the tailored set it specifically needs — in-scope + domain-specific standard modules · domain specialist agents · project-relevant skills — which the **user approves**; Stage 9 grows it. This is what makes the harness *adapt to and elevate each project on its own terms* (paired with the per-project charter + design-language). Extends two-tier promotion **downward** (generate-for-this-repo). **Autonomous self-deploy out of scope** (no-over-claim + user-approved-promotion). Sequenced **after** the charter (0030); the largest unrealized piece of the vision. (User-approved 2026-06-29.)
- **`harness`** — **Small standards fills (JIT, not keystone).** Author `architecture-styles` (still reserved), an `error-handling` philosophy module, and a `domain-design`/DDD module **each only when a change pulls it in** — they are *not* the architecture keystone (`maintainability-structure` already cites Clean/Hexagonal/Onion/DDD/Saga/Outbox, and the plan *Architecture & holistic fit* section now frames system-shape up front). Lower priority than the two items above.
- **`harness`** — **Multi-charter / domain-pack ("other types of work" — DIAGNOSED, not built).** The kernel is domain-NEUTRAL (review engine · honesty engine · 6 reviewer postures · 10-stage spine · ledger trio); a swappable **domain-pack** (catalog + DoD gates + tag→discipline + vocab + 1–2 specialists + execution binding) re-points it at a non-software domain. "Charter" = two altitudes: **methodology ⊂ domain-pack**. Strongest first pack = **deep-research / technical-writing**; the one hard seam = the software-bound EXECUTION engine (`build-item.js`/`qa.js`, else the `manual` fallback). **Sequencing (YAGNI): build the engineering charter + mature the one software pack FIRST.** Reserved seam in `docs/claugentic-standards/README.md`; full rationale in `docs/claugentic-DECISIONS.md`.
- **`harness`** — **Common-adoption-gotchas digest in INVARIANTS (gated on real adoption evidence — not now, 0036).** Failure-modes are already covered inline (init 44+, build 16+, audit 6+ guards) and indexed in DECISIONS/INVARIANTS by deliberate DRY design; a consolidated "Common Adoption Gotchas" digest is worth authoring **only if** that inline+indexed coverage ever proves to miss a real adopter blind-spot. YAGNI-deferred candidate, not committed work.
- **`harness`** — **Optional adopter-guidance on wiring a design-system / QA MCP (gated on real adoption evidence — not now, 0036).** The harness **declares no MCP by design** — adopters wire their own (design-system, QA, etc.). A short optional guidance note on *how* an adopter wires one is a candidate **only if** real adopters ask; not a harness-shipped MCP. YAGNI-deferred candidate, not committed work.
- **`harness`** — **Experience-craft elevation ("addractive" — loved, not just correct).** Core **LANDED** (`product-ux.md` "Aesthetic & motion craft" v0.3.0 + the 0029 slices — design-language seam · designer craft forcing-fn · motion baseline · DoD craft bar · README "addractive" · `/design-sync`-generic; on the architect-pass rail; see DECISIONS → "Craft is first-class"). **Two parts deferred:** **(a)** `runtime-qa` felt/runtime observation — **OPT-IN / user-requested, NEVER default-on** (capability unproven + context-cost); **(b)** the `transition`-state enum slice — most-gated, engine-touching, frozen-schema change; stays deferred.
- **`harness`** — **Release-workflow matrix-leg guard (gate item; 0041 S2 Verify R2-b, 2026-08-12).** Two `release.yml` gates run on the **Linux leg only** — the `git merge-base --is-ancestor` on-main check and `claude plugin validate --strict`. If `gates`' `matrix.os` ever loses `ubuntu-latest`, both vanish **with the suite green**. One assertion in `tests/test_release_workflow.py` (`gates`' `matrix.os` contains `ubuntu-latest`) closes it. Reviewer catch → mechanical check; pre-existing exposure for the validate step, surfaced by 0041 S2.
- **`harness`** — **Restrict pushes to `release` — the ruleset that makes ONE-PUBLISHER mechanical (0041 S2, 2026-08-12).** `docs/claugentic-INVARIANTS.md` → *The `release` branch has exactly ONE publisher* states its branch half is **model-upheld until `release` is branch-protected** — verified live: `branches/release/protection` → 404, `rulesets` → `[]`. A ruleset restricting pushes to `release` to the GitHub Actions app converts a documented contract into a platform guarantee. **User-applied GitHub setting** (procedure in `docs/RELEASE_CHECKLIST.md` → Branch protection); tracked here so the model-upheld half doesn't stay model-upheld by default.
