# Agent Development Workflow

> ### 👋 New here? Read this first
> This repo is built and maintained with an **agent-assisted workflow**:
> - **Small/local change?** Just make it — tests must pass, and the architecture-tree hook will prompt you to document any new source file in `docs/claugentic-ARCHITECTURE_TREE.md`.
> - **Substantial change?** (new subsystem, cross-cutting refactor, shared-contract change, or **~8+ files**) — don't free-code. The agent will **pause, ask questions, enter plan mode**, and run the pipeline below: a plan in `.claude/plans/`, an adversarial review, a spec **you approve**, implementation in an isolated branch, verification, then a retrospect that improves this harness.
> - **Your map of the codebase** is `docs/claugentic-ARCHITECTURE_TREE.md` (one line per file) — read it before diving into source. Specialist **roles** the agent delegates to live in `.claude/agents/` and grow over time.
> - **Decisions** → `docs/claugentic-DECISIONS.md`. **Backlog** → `docs/claugentic-ROADMAP.md`.
> - **Build mode** (`skills/build/SKILL.md`) is the orchestration layer that **auto-drives this pipeline over the audit backlog** — pick items or a whole tier and it works them through the stages below, pausing only at the decisions that are yours.

How agents **and** human devs take *substantial* work from idea → landed change while keeping quality high and the harness self-improving. CLAUDE.md links here; this is the source of truth for process.

> **One-line:** Triage → Discuss → Plan → Review the plan → Spec → **Approve** → Implement → Verify → Land → **Retrospect**. Small changes skip to Implement+Verify.

---

## 0. Triage — does this need the full pipeline?

**Full pipeline** for *substantial* work: a new subsystem, a cross-cutting refactor, a change to a shared contract/pattern/standard, a security boundary, or anything touching **roughly 8+ files** (or a pattern documented in CLAUDE.md / STANDARDS).

**Lightweight path** for small/local/mechanical changes: go straight to **Implement → Verify**, still updating `claugentic-ARCHITECTURE_TREE.md` and `claugentic-DECISIONS.md` as needed.

**Triage continuously, not just up front.** A conversation often *grows* into a substantial change. The moment a request is shaping up to cross the bar above (≈8 files, or any qualitative trigger), **stop free-coding**: ask the user clarifying questions — from technical *and* **product-discovery** angles (see Stage 1) — until scope is crystal-clear, then **enter plan mode (Stage 2)** and follow the pipeline — don't keep ad-hoc editing.

When unsure, default to full; the `synthesizer-gate` (Stage 3, plan-gate) confirms the path was right.

**Carry-forward gate (before a new arc).** When a request crosses the *substantial* bar above, before opening its plan, confirm the **prior** arc left nothing un-homed — every finding it deferred or left open now sits in its proper place (the dispositions themselves are defined once in *Plan file lifecycle* below; point there, don't restate them). This is the **proactive** complement to the close-time disposition rule and doctor's retroactive plan-scan: it catches a prior plan that closed imperfectly *before* new work builds on top. **Model-upheld** — findings are prose, so there is no mechanical gate (doctor is the retroactive net). Lightweight-path changes skip it.

**Mirror the ask back before you build (shape-triggered, either path).** When a request arrives as rambling multi-clause prose — several asks chained together, constraints buried mid-sentence, no single "the ask is X" — distill it **inline** (*goal · scope · the constraints you caught · the questions actually asked*) and mirror it back for a yes **before any substantive work**; **never proceed on inferred scope without stating it.** Triggered by input **shape, not size** — so unlike the carry-forward gate above it applies on **either path** — and it is load-bearing before anything irreversible (a history rewrite, a force-push, a large delete). It complements Stage-1's clarify-outward (that asks questions *outward* until scope is crystal-clear; this restates understanding *inward* for a yes) and is the intake-time counterpart of the Stage-4→5 plain-English spec confirm-back (there you mirror the *spec* after planning; here the *raw ask* before it — a misread is caught at turn one, not after work is spent). Ephemeral and inline — no brief template, no persisted artifact. **Model-upheld** — a restatement is prose; no hook can grade it.

---

## Principles (apply at every stage)

- **Slice small, land complete.** Every unit of work must be finishable by **one specialist agent in a single ≤1M-token-context session** and land **vertically complete** — no half-done state, no `TODO`/debt left behind. If it doesn't fit, decompose further *before* implementing. This is a hard gate, not a guideline.
- **No new tech debt.** A landed slice leaves the codebase at least as clean as it found it: tests added, docs/ARCHITECTURE_TREE updated, no dead code, no silenced errors.
- **The harness is living.** Any task may improve STANDARDS / CLAUDE.md / the `.claude/agents/` role library / this workflow. Stage 9 is how that happens; treat harness improvements as first-class output, not a chore.
- **Delegate liberally to preserve orchestrator context.** Use subagents freely and in parallel — **no resource constraints** — so the orchestrator's own context stays lean for synthesis and decisions (fan out reads, reviews, and implementation to specialists). The orchestrator picks whichever role(s) fit from the `.claude/agents/` library; as the library grows it has more specialists to choose from.
- **Effort-dial the machine.** Scale review/verification depth to the change's risk and size, and load only the standards modules the change *touches* (relevance-gating). The dial flips on the **same triggers Stage 0 uses to call work *substantial*** — a **security/trust boundary · a shared contract/pattern/standard · ~8+ files · or any trust/honesty surface** (per the diverse-critics principle below): a small/local change gets a quick **solo `synthesizer-gate`** look; any of those triggers fans out the lenses (and, on a trust surface, the diverse panel — that principle names *who* joins; this names *when* to fan out). Don't run the whole machine on a trivial change — that's how a harness kills the velocity it's meant to protect.
- **Diverse critics on contested or trust-surface changes.** A contested design fork OR a trust/honesty surface (claims, `[D]`/`[J]` labels, proof-vs-attempt wording, a security boundary) triggers the **diverse panel** — `synthesizer-gate` + `yagni-sentinel` + `honesty-reviewer`, **plus `product-designer` when the change is user-facing** (so the review confirms the plan still achieves what the user is trying to achieve, not just that it's technically sound) — at **every gate that change passes** (Plan **and** Verify); else a lone reviewer suffices. At **Verify**, the wired mechanism is `engine/verify.js`: the orchestrator **invokes** it (the invocation is model-upheld — the platform does not auto-fire workflows), and the script then convenes the panel mechanically; if the Workflow tool is unavailable, say so, convene the panel in prose, and tag the run **"prose-orchestrated"** — never claim script guarantees on a prose run. At **Plan**, the panel is the **advisory Stage-2b deep-dive** (*The pipeline* → *Stage 2, the draft → advisory-panel → incorporate sub-process*) — a **standard** step for substantial work (not contested-only), **prose-convened** (the `lens-reviewer` runs in its *plan-design* mode; an engine-orchestration path for the 2b panel is a deferred ROADMAP item — YAGNI for an advisory, 1-round panel), and **builder-class: it CONTRIBUTES, it does not gate** (Stage 3 is the gate).
- **Review is cross-review by separate specialist agents — each on the most capable available model, each clean-context.** The gate/refute roles — `synthesizer-gate` · `honesty-reviewer` · `finding-verifier` — are **distinct specialist agents** (separate roles, separate contexts); each works only from `{claim, file:line, …}` and **never sees the builder's rationale or transcript**, so it cannot rubber-stamp the reasoning. The independence is of **context and role** — a separate agent, a clean contract, a single lens — **not** of model: every agent runs the most capable available model, so blind spots can still correlate, and the harness says so (it does **not** claim model-independent review). **Honest claim:** multiple skeptical, clean-context specialist passes on the most capable model — a reduction of rubber-stamping risk, never a guarantee. *(The engine keeps a `RUNNING AS` / `sameModelTag` note as an honest per-run model-relationship reporter; it no longer engineers cross-model de-correlation.)*
- **Model tiers — by capability, never a pinned version.** The harness picks models by capability *tier* via aliases that auto-resolve to the current model in that tier: **`best`** (the most-capable tier — Fable 5 **where the org has access, auto-falling-back to Opus otherwise**) or **`fable`** for the **hardest / longest, larger-than-one-sitting** work (deep audits · build-to-green over a backlog · long research) · **`opus`** the standard-judgment **default** for anything needing judgment — *including high-volume review* · **`sonnet`** routine / faster · **`haiku`** mechanical / cheapest. **`opusplan`** is a coarse built-in hybrid (opus in plan-mode → sonnet in execution); the harness's per-agent `model:` frontmatter is the **finer** control — both valid, no conflict. **NEVER hardcode Fable or assume its availability** — a user's Fable access is **conditional and can be temporary** (limited/safety-fallback), so prefer `best` (which auto-falls-back to Opus) or an explicit fallback chain, never a bare `fable`. Default to the most capable available tier for judgment; drop to a cheaper tier only for genuinely mechanical work. **Accuracy first, usage-conservation second.** The tier→alias mapping is the single thing to change if a tier is ever renamed (the engine `MODELS` block + the agents' `model:` frontmatter).
- **The platform advisor — an optional accuracy lever for the gate/verify roles (advisor-AWARE, not advisor-required).** The Claude Code platform **advisor** (`/advisor` command · `advisorModel` setting · `--advisor` flag) can pair a **stronger model** with the harness's gate/verify roles at decision points — before committing to a plan · when an error keeps recurring · before declaring done — exactly the harness's gate junctures; subagents inherit it. **State the constraints, don't gloss them:** it is **Anthropic-API-only** (not Bedrock / Vertex / Foundry), **experimental** (version-gated, beta), so it must be **opt-in behind an env / feature flag + a version-floor re-check — never a hard dependency**; adopters off the Anthropic API are unaffected. **Invocation is model-upheld** — the model consults the advisor; nothing mechanical fires it. **The honesty framing is load-bearing:** the advisor is a **reduction of single-model-blind-spot risk, NOT independent verification** (same vendor → blind spots stay correlated; DECISIONS → *the two senses of "independent"* forbids the independence claim). **Not to be confused with** the harness's own SessionStart `scripts/claugentic-session-advisor.py` — a *different thing*, a context / resumption hook, not this decision-point escalation lever.
- **The optional engineering charter** (`docs/claugentic-CHARTER.md`) is a **living per-work-type record** — it records the approach chosen per kind of work (from the methodology toolbox, *Choosing the approach for the work at hand* below) and **applies** it when that kind of work recurs; it **defaults to the toolbox judgment**, so an **absent/empty charter ≡ current behavior**. Optional, model-upheld, revisable-default-not-mandate — nothing in it is required.
- Plus the CLAUDE.md non-negotiables: SOLID > DRY > KISS > YAGNI · fail loudly · validate at boundaries · configurable-over-hardcoded · single source of truth.

---

## Context & handoff

- **The durable memory is the plan file + `claugentic-DECISIONS.md` + `claugentic-ARCHITECTURE_TREE.md`** — keep them current as you go. For long work, a **fresh session resuming from the plan checklist beats a deeply-compacted context**; no manual orchestrator "handover" is needed.
- **Delegate token-heavy work to subagents** to keep the orchestrator lean (the *Delegate liberally* principle above — this is its context payoff).

---

## Roles — a library mapped to functions, not a fixed pair

The orchestrator **selects the role(s) that fit the task** and may spawn several or compose them. It is not locked to a fixed sequence of agents.

**Native Claude capability does the creation.** Drafting a **plan** (plan mode / the built-in `Plan`) and **implementing** a slice are **native** work — the harness *enables* them (the best prompt + the spec contract + the `docs/claugentic-PLAN_TEMPLATE.md` architect-pass forcing function + worktree isolation), it does **not** wrap generation in a vanity "architect" identity. The architectural thinking comes from the TEMPLATE's *Architecture & holistic fit* section + the standards lenses, not an agent label. So the roles map to **functions** — *generate* (native) · *apply a lens* · *critique* · *gate* · *product* — and agents **point at** the standards catalog, never duplicate it (DRY).

Starter library (`.claude/agents/`):
- **`implementer`** — implements an approved spec to standard, in an isolated worktree, landing one slice complete (native implement). Upholds the CLAUDE.md non-negotiables + the in-scope `docs/claugentic-standards/` modules — it **points at** them, never restates them.
- **`synthesizer-gate`** — the integrate→verdict→loop **gate**, one posture at three altitudes / caller-contracts: **plan-gate** (Stage 3 — adversarially critiques a draft plan against the gate criteria; writes the verdict into the plan's *Review* section), **verify-verdict** (Stage 7 — **solo** for a small change, or **synthesizes** the fan-out lenses + critics into one PASS/CHANGES_REQUIRED), and **audit-synthesis** (consolidates an audit's deduped findings into the tiered backlog). One posture (integrate→verdict→loop), three caller contracts.
- **`lens-reviewer`** — applies **one** `docs/claugentic-standards/` module (the "lens"); modes: **verify-diff** (Stage 7), **audit-scope** (the `audit` skill), **whole-scope** (the `thorough` audit's cross-cutting / between-the-modules sweep — its lens is the *whole scope*, red-teaming the risk **no single module-lens owns**: emergent smells, integration gaps, systemic cross-cutting issues; always `exhaustive`), and **plan-design** (the Stage-2b advisory design review). Invoked once per relevant lens in a fan-out.
- **`product-designer`** — the product/UX lens; modes: **discover** (Stage 1, user-facing work — user, job-to-be-done, flows, states, "what good feels like"; applies `docs/claugentic-standards/product-ux.md`, persists to `docs/claugentic-PRODUCT.md`) and **elevate** (spec mode — critiques a draft product spec by method and returns proposals the user decides on).
- **`finding-verifier`** — refutes **one** audit finding against the code: given only the claim + `file:line` (never the finder's rationale), it tries to prove the finding wrong and returns `Verified` / `Refuted` / `Unconfirmed`. The audit's adversarial-verify counterpart to `lens-reviewer`.
- **`yagni-sentinel`** — the anti-over-engineering skeptic; argues a plan/diff is *too much*. The deliberate counterweight to the quality lenses. (Also the audit's `thorough`-only adversarial prune over consolidated findings.)
- **`honesty-reviewer`** — the claims / over-claim lens; refutes **copy** (not code), flagging text that launders model-or-human-upheld judgment into apparent mechanical fact (the verb discipline · `[D]`/`[J]` label integrity · dimension-scoped success claims). Part of the diverse panel at Plan + Verify on trust/honesty surfaces.

Also available without new files: built-in **`Explore`** (fan-out search), **`Plan`** (drafting), and `/code-review` · `/simplify` for diff cleanup. **Add new role files as needs emerge** — the library grows (Stage 9).

### Why the multi-agent shape — the dynamic-workflow grounding

This harness is a **dynamic-workflow multi-agent harness**: its `engine/*.js` + the Workflow tool *are* the "Claude writing its own multi-agent harness on the fly" the Claude Code team's **dynamic-workflows blog** describes (it pairs with the `/loop`-`/goal` scheduling/goal primitives — see the edge-skills note below, don't restate). That blog also names **three failure modes** a single-context agent is prone to, and they are the honest **rationale** for *why* this multi-agent shape — clean-context independent reviewers + adversarial verify — beats one long context. This is **grounding, NOT proof**: the blog explains why the shape helps; it does **not** certify this harness is correct, so each mode is **structurally mitigated**, never "solved"/"prevented":

- **Agentic laziness** (stopping / declaring done early) → mitigated by build-to-green (iterates until the deterministic gates are green or the cap), the DoD gate list, the audit's loop-until-dry, and the "not-green at cap → here's the residual, nothing partial lands" honesty. The **independent Verify gate** catches a premature "done."
- **Self-preferential bias** (trusting one's own output over an independent check) → mitigated by **clean-context independent reviewers**: `finding-verifier` gets only the claim + `file:line`, never the finder's rationale (Roles §); the Verify panel is spawned clean-context (never handed the builder's rationale); a lone reviewer can't bless what the panel rejects.
- **Goal drift** (losing the objective over a long run) → mitigated by the plan file + spec as durable memory (*Context & handoff* — "a fresh session resuming from the plan checklist beats a deeply-compacted context"), the machine-readable acceptance criteria, and the re-confirmed worklist order.

**The pattern-name map** (the blog's named orchestration patterns → what the harness genuinely embodies — accurate, not aspirational):

| Blog pattern | Embodied? | Where |
|---|---|---|
| **fan-out-and-synthesize** | ✓ | the audit's lens fan-out + the Verify panel, synthesized by `synthesizer-gate` (verify-verdict altitude) |
| **adversarial verification** | ✓ | `finding-verifier` (refutes a handed claim without the finder's rationale) + `honesty-reviewer` (refutes copy) |
| **loop-until-done** | ✓ | build-to-green (goal-based) + the audit's loop-until-dry |
| **generate-and-filter** | ✓ | the audit generates candidate findings → `finding-verifier`/prune filters → backlog |
| **classify-and-act** | ~ | partial — the audit-backlog tag→discipline routing (a classification that selects the execution discipline) |
| **tournament** | ✗ | **not embodied** — the pipeline runs no N-solution pairwise-judged tournament; it's available via the Workflow tool's judge-panel pattern if a maintainer wants it, but the pipeline itself does not tournament solutions |

---

## The pipeline

**The ten stages group into four beats — `FRAME → APPROVE → BUILD → CLOSE`** (a one-glance map; no stage is cut or renumbered):

- **FRAME** (0 Triage · 1 Discuss · 2 Plan · 3 Review · 4 Spec) — converge on *what to build and how*.
- **APPROVE** (5) — the user's sign-off; *no code before this*.
- **BUILD** (6 Implement · 7 Verify) — build it and check it.
- **CLOSE** (8 Land · 9 Retrospect) — ship it, harvest the lessons.

| # | Stage | Beat | Owner | Output |
|---|-------|------|-------|--------|
| 0 | **Triage** | FRAME | orchestrator | full vs lightweight path |
| 1 | **Discuss & brainstorm** | FRAME | orchestrator + **user** | learn the endeavour from multiple **angles** before planning — technical scope **+ product-discovery** (who's the user · the job-to-be-done · what success/"delight" looks like · the key flows & their states); these surface the user-story gaps a technical-only intake misses. User-facing work pulls in `product-designer` → `docs/claugentic-PRODUCT.md`. Crystal-clear scope; tangents→ROADMAP, decisions→DECISIONS |
| 2 | **Draft plan** | FRAME | orchestrator / `Plan` (+ advisory panel) | `.claude/plans/NNNN-<slug>.md` (Problem · Approach · **Architecture & holistic fit** · Risks · Test strategy · slices — Review & Spec filled later), **sliced into ≤1-session units**. For *substantial* work the draft **MUST** fill the **Architecture & holistic fit** section (the forcing function — see below) and run the **2a draft → 2b advisory deep-dive → 2c incorporate** sub-process (see below); trivial/lightweight changes keep the lightweight path (2a → Stage 3, no panel) and may give the section a one-liner or skip it |
| 3 | **Review the plan** | FRAME | `synthesizer-gate` (plan-gate altitude; + others as fit) | critique written into the plan's *Review* section; iterate until it passes the gate |
| 4 | **Spec** | FRAME | orchestrator | plan upgraded to implementation-ready spec **per slice**: opens with a short plain-English block (*what this builds · what "done" means for you · what you're accepting — risks/trade-offs*), then file-by-file changes, signatures, test list, acceptance criteria, **+ the in-scope `docs/claugentic-standards/` dimensions & target bar (entry point: `docs/claugentic-ENGINEERING_STANDARDS.md`)** |
| 5 | **Approval gate** | APPROVE | **user** | sign-off on the spec — *no code before this* |
| 6 | **Implement** | BUILD | `implementer` | one slice/session, isolated worktree/branch; upholds CLAUDE.md; updates ARCHITECTURE_TREE inline |
| 7 | **Verify** | BUILD | `synthesizer-gate` (+ lenses) | **effort-dialed** on the same triggers that flip Stage-0 to *substantial* — a security/trust boundary · a shared contract/pattern/standard · ~8+ files · any trust/honesty surface (per the diverse-panel principle): a **small/local** change → `synthesizer-gate` audits solo; any of those triggers → fan out the panel via **`engine/verify.js`** (the orchestrator invokes it — `${CLAUDE_PLUGIN_ROOT}/engine/verify.js` for an adopter, the repo-local `./engine/verify.js` dogfooding this repo; spike-verified read-from-install-path) — one `lens-reviewer` per relevant `docs/claugentic-standards/` module **+** `yagni-sentinel` (**+ `honesty-reviewer`** on a trust surface), then `synthesizer-gate` **synthesizes**, with the judge `model:` (the most-capable tier) and same-model tag computed in code. Workflow tool unavailable → convene in prose and tag the run **"prose-orchestrated."** **All Definition-of-Done gates green** (see below); run **`/simplify`** + **`/code-review`**; confirm spec match. Findings are **dual-layer** (technical + plain-English). |
| 8 | **Land** | CLOSE | orchestrator | conventional commit/PR; remove the completed plan from `.claude/plans/` (git history keeps it); append DECISIONS |
| 9 | **Retrospect & evolve** | CLOSE | orchestrator | harvest learnings into the harness (see below) |

**Stage 3 gate — a plan may not pass review until:** it is correct & sound (SOLID/patterns), each slice is **session-sized and lands complete with no debt**, the right path was chosen, risks + test strategy are stated, the **Architecture & holistic fit** section is genuinely reasoned (for substantial work — see below), and any harness impact (new STANDARD/agent/doc) is noted. The reviewer writes a verdict + required changes into the plan; the orchestrator iterates.

**Stage 2, the architect-pass mandate (architecture framed from the start):** because architecture is **model-upheld** (agents sometimes skip it), the plan `docs/claugentic-PLAN_TEMPLATE.md` carries a required **Architecture & holistic fit** section the Stage-2 draft **must fill for substantial work** — initial architect thoughts on codebase fit (layering · module placement · design patterns · SOLID/SoC) · product fit · the **quality dimensions to uphold, each mapped to its `docs/claugentic-standards/` module** (so the standards frame the plan, not discovered late — this is the forward-pointer the Stage-4 Spec's *in-scope dimensions* refines per slice) · future-proofing. These are **initial framing, NOT the deep per-slice Spec** (that stays JIT in build), and they GUIDE every downstream agent. For **user-facing work**, the *Architecture & holistic fit* section must ALSO name the **felt/visual craft bar** the surface must clear (per `docs/claugentic-PLAN_TEMPLATE.md` — same rail: the template forces it to exist, `synthesizer-gate` audits it's reasoned, `yagni-sentinel` guards over-build; non-user-facing work skips it). **Scoped to substantial work** — trivial/lightweight changes keep the lightweight path and may give the section a one-liner or skip it (honor the effort-dial; no holistic pass on a typo fix). **Enforcement = structural prompt + review, NOT a mechanical gate:** the template forces the section to *exist*; `synthesizer-gate` (plan-gate) audits it is genuinely reasoned (not hand-waved, not gold-plated — `yagni-sentinel` guards the over-build edge). You can't mechanically grade architecture; you can require the section and review it. Plans are ephemeral (deleted at Land), so a richer plan is working scaffolding, never always-loaded context-bloat.

**Stage 2, an optional ADR working-artifact for a weighty design fork (`/architecture`).** For a **genuinely weighty** design fork — a decision with real trade-offs a fresh reader would need reasoning-out — the orchestrator **may consider the bundled `/architecture` skill** to produce a full ADR as a **Stage-2 working artifact** that reasons the trade-off in depth, then **distil ONE `claugentic-DECISIONS.md` line at Land** (the ledger stays lean; the ADR is scaffolding, not a shipped doc). This is a **model-upheld option, never wired behavior** — most forks are reasoned inline in the *Architecture & holistic fit* section, and ADRs are **not** added as always-loaded docs (the ephemeral-plan / lean-ledger discipline holds). Use it only when the *Architecture & holistic fit* prose isn't room enough for the trade-off; otherwise skip it.

**Stage 2, the draft → advisory-panel → incorporate sub-process (for *substantial* work):** Stage 2 is itself three steps — **2a draft · 2b specialist advisory deep-dive · 2c incorporate** — so the diverse-panel-at-Plan (named in *Diverse critics* above) is a **standard** Stage-2 step for substantial work, not an ad-hoc afterthought. **Stage 3 (Review) stays the gate, unchanged.** The three steps:

- **2a — Draft.** The orchestrator drafts in **plan mode** (the built-in `Plan` capability) → a plan file **structured by `docs/claugentic-PLAN_TEMPLATE.md`**, with the **Architecture & holistic fit** section filled (the architect-pass = the forcing function above). This is the planner's first pass.
- **2b — Specialist advisory deep-dive (ADVISORY, builder-class — contributes, does NOT gate).** A **scoped panel** reviews the draft's **design** and returns advisory feedback: the **in-scope `docs/claugentic-standards/` lenses** (relevance-gated — only the dimensions the change touches; each `lens-reviewer` runs in its **plan-design** mode) **+** the cross-cutting critics `yagni-sentinel` (is this too much?) and `honesty-reviewer` (do the plan's claims over-state?) **+ `product-designer` when the change is user-facing** (does the design still achieve what the user is trying to do?). This is **prose-convened** — the orchestrator spawns the panel directly; there is **no engine path** for it (an `engine/verify.js`-orchestrated 2b panel is a **deferred ROADMAP item** — YAGNI for an advisory, builder-class, 1-round panel that works in prose today; build it only if a prose 2b proves too unwieldy). The panel **CONTRIBUTES, it does not gate** — its feedback is input to 2c, not a pass/fail verdict.
- **2c — Incorporate.** The planner folds the panel's feedback into a **refined plan**. The **2b↔2c loop is bounded — 1 round by default**: advisory feedback has no natural stop, so the planner decides whether a second round genuinely adds value (diminishing returns) rather than looping for polish. The refined plan then goes to **Stage 3 Review** (`synthesizer-gate` in its plan-gate altitude, the adversarial **gate**) → Stage 5 Approve.

**Same panel, two altitudes (HONEST framing):** most of these lenses appear again at **Stage 7 Verify** — but there they review **code** and **GATE** (a `CHANGES_REQUIRED` blocks the land), whereas at **2b** they review **design** and only **CONTRIBUTE** (advisory input the planner weighs). 2b is **model-upheld, builder-class** — convening it and weighing its feedback are judgment calls; it does **not** mechanically improve the plan, and Stage 3 remains the only Stage-2/3 gate. **Scoped to substantial work ONLY:** 2b fires on the **same Stage-0 substantial triggers** that gate the diverse panel and the architect-pass — a **security/trust boundary · a shared contract/pattern/standard · ~8+ files · any trust/honesty surface**. **A trivial/lightweight plan stays 2a → Stage 3 with NO advisory panel** (honor the effort-dial — the panel does **not** run on every plan; running the whole machine on a small change is the velocity-killer the *Effort-dial* principle warns against).

**Stage 7, the whole-feature closing pass (last slice of a multi-slice plan):** each slice's Verify checks *that slice's diff* — but cross-slice integration regressions (slices that each looked fine alone yet don't assemble into the feature) fall between those per-slice gates. So on the **last slice of a multi-slice plan**, the Verify pass ALSO confirms the **assembled feature achieves the Stage-1 job-to-be-done**, end-to-end. This is **model-upheld, orchestrator-driven** — `engine/verify.js` is sandboxed and **cannot read `docs/claugentic-PRODUCT.md` / the plan off disk**, so the *orchestrator* passes the Stage-1 job-to-be-done (and "this is the closing whole-feature pass") into the last-slice `synthesizer-gate` invocation's prompt; the engine gains **no new args-contract field** (KISS). Single-slice changes skip it (there's nothing to integrate).

**Stage 7, when the SPEC is what's wrong (the spec-amendment path).** Verify checks the diff against the spec — but sometimes the diff matches the spec faithfully and the **spec line is the defect**. A **spec-conformant diff is never a defense against a lens finding**: the finding stands, and the *spec* is amended. Record it in the plan file as a short **`Spec amendment`** note (*the line as spec'd · the corrected line · the lens and the reason*), fix the diff in the same pass, and restate any acceptance criterion the amendment touched. **Two registers, one rule:** an amendment that only **tightens** what the user already accepted (a narrower permission, a stricter guard, a stronger assertion) rides the normal Verify loop; an amendment that changes **scope, a stated trade-off, or the risk profile the user signed off at Stage 5** goes **back to the user** — Stage 5 approved the plain-English block, so a change to what that block promised is not the reviewer's to make. **Model-upheld** — nothing mechanical tells the two registers apart. *(Provenance: plan 0041 Slice 2 — the spec prescribed workflow-level `permissions: contents: write`, the implementer matched it exactly, and the security lens refuted the spec; amended to workflow-level `contents: read` with `write` on the `publish` job alone. A tightening, so no re-approval.)* **When a spec line is wrong, prefer fixing the MECHANISM over weakening the COPY** — the two are not equal-cost. Tightening the mechanism keeps the Stage-5 promise intact and rides the Verify loop; rewording the promise to match the defect changes what the user approved and goes **back to them**. *(Second use, plan 0041 Slice 3, 2026-08-13: the spec said the new nudges were "truncated honestly if over the cap", and the lenses measured them truncated away at 4 in-flight plans — worst in exactly the cluttered repos the feature targets. Reserving the clause budget **tightened** the guarantee and stayed in the loop; rewording the plain-English "open **any** adopter repo and the session's first line tells you" down to "usually" would have needed re-approval. Fixing the mechanism was both the cheaper and the register-safe path.)* **A third register, and the trap it closes: an acceptance criterion is read for what it PROMISED — never used as a veto on a correctness fix.** When a blocking finding appears to collide with an AC, state the AC's **actual scope** and check whether the fix touches it; do not soften the fix to preserve the AC's letter. *(Plan 0041 Slice 4, 2026-08-13: the AC was "the gate's output is byte-equivalent pre/post migration." The blocking honesty fix changed the summary **only** in the report-only regime, and this repo declares zero report-only entries — verified by rendering the live summary at both commits, byte-identical. The AC survived intact. Read literally it would have preserved a gate that printed "all managed ledgers within budget" over a file at 52× its cap, and a test that pinned that as correct.)*

**Stage 4 → 5, the plain-English layer (the non-engineer's steering wheel):** the spec contract is code-shaped (file-by-file edits, signatures, named dimensions), but approval is about *intent, not code*. So every spec **MUST open with a short plain-English block — *what this builds · what "done" means for you · what you're accepting (risks/trade-offs)*** — and that block is **presented first at the approval gate (Stage 5)**, with the file-by-file detail beneath it. The user approves the intent; the detail is there to verify against, not to decode. *(The plan `docs/claugentic-PLAN_TEMPLATE.md` carries a matching section.)*

**Stage 4, the charter write-trigger (record a non-default methodology choice — defaults-safe):** Stage 4 is where the per-work approach is chosen (the row above names the in-scope dimensions), so it is the timely moment to record it. **IF this spec names a methodology approach that DIFFERS from the harness's default grain for this work-type** — a per-work choice from the toolbox (*Choosing the approach for the work at hand* below) that isn't what the default discipline would apply, e.g. test-first chosen for a `feature` — **RECORD that choice to `docs/claugentic-CHARTER.md`** per the charter's RECORD/GROW behaviors (`work-type → chosen approach + brief rationale`, **update the work-type's entry in place, never append**). **This fires ONLY on a non-default choice: default-grain work writes NOTHING** — an empty charter still ≡ default behavior. It is a **model-upheld prompt, not a gate** — it does not make the charter required, and nothing mechanical enforces it. *(The charter itself carries the behavior contract; this note is only the trigger — see `docs/claugentic-_CHARTER.md`.)*

**Stage 4, scoping the in-scope dimensions (surfaces, not just runtime output):** a dimension is in scope for **every surface the slice WRITES** — code, docstrings, skill/README copy, index and ledger entries, and the plain-English approval block itself — never for its runtime output alone. Narrowing a dimension at spec time narrows what the Verify panel is asked to check, and copy is the surface a slice most often ships un-reviewed. *(Provenance: plan 0041 Slice 5, 2026-08-14 — the spec scoped honesty to "the wrapper's printed claims"; the hardest findings were a bootstrap line written byte-identically into every teammate's always-loaded `CLAUDE.md` that told husky users to run a command which would disable their own project's hooks, plus four un-hedged claims in shipped prose and two index entries. All in-scope surfaces, none in scope as spec'd.)*

**Stage 1, the plain-English open (say why before the questions):** when Discuss begins, tell the user in plain English why you're asking before any code — *"Before any code I'll ask a few questions about who this is for and what 'good' looks like; your answers steer everything downstream."* Then ask. (The *why* of Discuss is the Stage-1 row above — this is just the user-facing opener, not a second rationale.)

**Keep the docs current — it's part of each stage (no hooks needed beyond the tree check):**
- **Implement (6):** update `claugentic-ARCHITECTURE_TREE.md` for any file add/move/remove (also hook-nudged); touch `CLAUDE.md` only for a genuinely new gotcha/command/pattern — *concisely; index, don't duplicate code*.
- **Verify (7) — honesty register:** the report says what the verify **attempted and tagged**, never what it "proved." Deterministic gates passed; reviewer sign-offs are model-upheld judgment, not a guarantee.
- **Land (8):** append a dated one-liner to `claugentic-DECISIONS.md` for non-trivial choices; update `ROADMAP.md` if scope shifted; **run the Stage-9 harvest checklist** (below) before moving on.
- **Land (8) — worktree hygiene:** a slice that **landed, was abandoned, or was escalated** leaves its worktree **clean or disposed** — the **branch is the durable artifact**, so an unchanged worktree is auto-reclaimed and a changed one is either landed (then reclaimed) or intentionally kept for inspection, never silently orphaned. Confirm no stray worktree/branch is left dangling after the disposition (the plan's *Disposition* covers what to do with the work; this covers the *worktree* it lived in).
- **Land (8) — honesty register:** the close-out names **which gate-class passed** — the *deterministic gates* (tests, tree-check, lint/type/security) vs the *reviewer sign-offs* (the audited dimensions) — never a blanket "verified/done."
- **Land (8) — close out for the user:** after the slice lands, say it plainly — *"This one's done. Next: pick another item the same way, run `/claugentic-dev-harness:build` to keep working the backlog, or re-run `/claugentic-dev-harness:audit` for a fresh picture — you're finished when Tier 1 and Tier 2 come back empty."*
- **Retrospect (9):** promote durable learnings into the `docs/claugentic-standards/` modules (entry point: `docs/claugentic-ENGINEERING_STANDARDS.md`) / `CLAUDE.md` / agent files.

---

> ### Adopter note — reading this doc inside YOUR repo (not the harness)
> When the `claugentic-dev-harness` plugin is installed into your project, this WORKFLOW.md is a **managed copy** living in your `docs/`. So a few references below resolve to the **installed plugin**, not to files your repo must contain:
> - **The specialist agents** (`synthesizer-gate`, `lens-reviewer`, `implementer`, `yagni-sentinel`, `honesty-reviewer`, …) **and the `engine/*.js` scripts** referenced throughout live **inside the installed plugin** — agents are resolved at spawn (by their namespaced id), engine scripts are run via the Workflow tool from the install path. They are **NOT** a `.claude/agents/` or `engine/` directory your repo has to provide.
> - **The Definition of Done's *deterministic gates* mean THIS project's OWN gates** — *your* lint, *your* type-check, and *your* test suite — plus the architecture-tree check. The specific commands written below (`python -m pytest`, `python scripts/check_versions_synced.py`, `python scripts/check_doc_budgets.py`) are the **harness's own self-test** for developing the plugin itself — both are **harness-self and irrelevant to an adopter**: version-sync checks the *plugin's* two manifests, and doc-budgets enforces the *harness's own* ledger caps (and is stripped from the release, so the script isn't even present in your repo). **Substitute your project's equivalents.**
> - **The architecture-tree check is wired as a hook by `init` only when the tree-gate is enabled** — a fresh or mature-no-tree repo gets a harness-format tree and the hook; a mature repo that already has its own tree is **asked** (replace it with a harness skeleton → gate on, or keep yours → **gate off, no hook wired**). If you keep your own tree (gate off), there's no hook — run `scripts/claugentic-check_architecture_tree.py` (with `python` / `python3` / `py`) manually at Verify if you ever want a one-off check.
> - **Three managed-file kinds** (governs what ships + what `init` recreates): **full-copy docs** (this WORKFLOW, PLAYBOOK, the standards modules) — source = ship = your copy, refreshed on re-`init`; **repeated-use templates** (`*_TEMPLATE.md` — the plan, product-spec, and standards-module skeletons) — you keep them and copy one per use, also refreshed; **one-time seeds** (`_X.md` → `X.md`) — `init` copies the shipped pristine `_DECISIONS.md`/`_ROADMAP.md`, **stripping the leading underscore, create-if-absent only** (never refreshed — it would clobber the ledger you fill over time). A file the release strips but the workflow needs in any repo must fall into one of these (the release/init contract).

## Definition of Done

A slice is **done** — and may land (Stage 8) — only when **all** hold. Two groups, same bar; they differ in *who* says pass:

**Deterministic gates** (pass/fail, can't be argued around):

> **Run each gate as its own command and read its exit code — never pipe one through `tail`/`head`/`grep`.** A shell pipeline's status is the **last** command's, so `<gate> | tail -1` reports the *filter's* success and a red gate reads green. Trim noise by reading the output, not by piping the gate. *(Provenance: 2026-08-13 — a real failure of the shipped-content scan below was masked exactly this way at Land and caught only by an unrelated re-run before push. A per-gate-step CI job is immune by construction; this bites the local pre-push run.)*

1. **Full test suite green** + any regression/snapshot tests — *your* project's suite. *(This plugin's own suite is `python -m pytest`.)*
2. **Architecture-tree check green** (when the gate is wired) — file-index presence, staleness, and **glob-drift detection**. Run it with whichever interpreter you have (`python` / `python3` / `py`): `scripts/claugentic-check_architecture_tree.py`.
   - **Updating the codebase map (the handled drift case).** When this gate reports *glob drift* — it's watching no files while the repo now contains source (e.g. an `init`'d empty repo that has since grown real code) — surface it to the user in plain English: *"I'm updating your codebase map to match your new code"* — then re-detect the layout and reset `INCLUDE_GLOBS` (init step 5's terminating self-correction). **Scope the claim honestly:** this is the *handled* drift path reading as plain English — a **genuine gate crash still fails loud by design** (CLAUDE.md: never swallow errors); don't promise "no error ever," only that this one case is plain.
3. **Version-sync green** — *harness-self only* (developing this plugin): `python scripts/check_versions_synced.py` enforces `plugin.json` ↔ `marketplace.json` version equality (`plugin.json` is the source of truth). A **run-gate** (like `pytest`), not hook-wired; scope is the two manifest versions only. **Not applicable to adopter repos** — skip it.
4. **Doc-budget green** — *harness-self only* (developing this plugin): every budgeted managed ledger (`CLAUDE.md`, `claugentic-DECISIONS.md`, `claugentic-ROADMAP.md`, `claugentic-INVARIANTS.md` — and, where a ledger has been split, each of its parts under a shared per-part cap) stays within byte budget: `python scripts/check_doc_budgets.py`. **The budget is BYTES** (`len(read_bytes())`), **not characters** — an em-dash/middot-dense ledger reads a few % lower by char-count (`wc -m` / `len(str)`), so an agent judging the % must trust the gate's byte figure, never a char count (a recurring mis-measure). A **run-gate** (like version-sync), not hook-wired; it emits a **WARN at ≥90%** (the cue to condense — merge superseded entries to git history) and fails only on a strict breach. The caps are harness-tuned and the script is **stripped from the release**. **Not applicable to adopter repos** — skip it.
   - **The condensation pass — condense on the trigger (model-upheld).** Two triggers, one procedure: in the **harness's own** repo a **≥90% doc-budget WARN** (from `check_doc_budgets.py`) is the do-it-now signal; in an **adopter's** repo — where that gate is stripped, so no WARN can fire — the reachable trigger is **`/doctor`'s budget advisory** ("DECISIONS at 92% of your configured cap — condense soon", read from `.claude/claugentic-doc-budgets.json`) **or your own periodic review**. Either way it is a **do-it-now signal, not a deferral**: as part of the current slice, run a **condensation pass** on that ledger. It is *judgment, not byte-shaving* — nothing mechanical decides what to cut. **Sort by *settled-ness*, never by *age*:** an old load-bearing constraint STAYS; a landed-yesterday build-history entry goes. (Condensing the *oldest* entries is exactly how a still-live constraint is lost and history repeats — age is not the axis.) **The keep/drop test, per entry:** *will a future agent need this to make a correct decision, or is it settled fact git history already holds?*
     - **Keep** — a still-live constraint (a decision still in force · an open trade-off · a "never do X because Y" a future change could re-break). These are the forward-looking essentials the ledger exists for.
     - **Drop to git history** — anything **superseded, settled, or landed**: an entry a later decision overrode, "how we got here" narrative, a plan already landed. **Git history is the archive** (no `docs/archive`), so the drop is lossless.
     - **Promote, don't drop, a "must-hold"** — if a condensed *decision* ("what we chose") has hardened into a *load-bearing invariant* ("what must stay true or something breaks"), **move it to `claugentic-INVARIANTS.md`** rather than losing it. The pass is the moment you notice that shift.
     - **Merge siblings** — collapse several dated one-liners about one subsystem into a single dense current-state line (the newest truth, not the diff that produced it).
     - **Landed build-records are the PRIMARY target — ABSORB, don't preserve (this reconciles "do-not-re-litigate" with "landed → drop").** A `**00NN — LANDED …**` entry is usually the largest and most-condensable thing in the ledger. Once the plan has landed, its **live constraint already has a permanent home** — a must-hold **promoted to `claugentic-INVARIANTS.md`**, or it simply **lives in the code / managed doc it changed** — and its narrative is git history. So **absorb it:** promote-or-locate the constraint, drop the story, leave **at most a one-line pointer** (often nothing). A landed entry's *"do-not-re-litigate"* protection comes from **that promoted constraint + git history**, **NOT** from keeping the narrative — so *"don't re-litigate"* never licenses *"keep the full LANDED text."* **Never instruct a condensation pass to "preserve the landed entries"** — that inverts this procedure and is the classic underdelivery (a pass then nibbles unrelated *live* rules and shaves ~4% while the settled bulk sits untouched). **Prevention beats cure:** don't write a "here's what I built" narrative into DECISIONS at Land — a landed plan contributes a forward-looking *keep-line* or a *promoted INVARIANT*; the commit message + the changed files are the build record.
     - **Lever order (largest reduction first) + aim for a band, not the edge.** Apply in order: (1) **absorb landed records**; (2) **promote** hardened must-holds to INVARIANTS; (3) **merge siblings**; (4) **trim implementation-locators** (helper/test names, line refs a maintainer re-derives from code); (5) **shard** (last resort — reach for it only via the ladder below, never as a first cut). **Return the ledger comfortably under the WARN (aim ~80%), not just under breach** — a pass that lands at 89.9% re-fires the WARN on the next append and the work repeats; condense once, properly.
     - **The escape-valve ladder — when condensation genuinely can't reach the band (the content is ALL live).** The lever order above is *how to cut*; this ladder is *what to do when cutting is exhausted and honest reduction still can't reach the band without shaving live constraints.* **Do NOT over-cut live rules to fake the band** — climb this ordered ladder instead (each rung tried before the next):
       1. **Promote durable constraints to their proper home.** A must-hold that has hardened into a load-bearing invariant belongs in `claugentic-INVARIANTS.md`; durable structural/domain context belongs in the relevant `CLAUDE.md` harness block — moving it out of the ledger is real reduction, not deletion. **Promoting *into* those homes respects THEIR caps too** (a cascade, not a loop): promote the distilled constraint, never the narrative, and if the target home is *itself* near its cap, that is the signal the constraint set has genuinely grown — go to rung 2 (it is not a licence to relocate bloat, and it never loops back into more cutting of live rules).
       2. **A deliberate, RECORDED cap increase — the honest escape valve.** When the content is genuinely all-live and promotion can't move it, **raise this repo's cap for that file** rather than shaving a live constraint: a one-line edit to the per-repo caps config (`.claude/claugentic-doc-budgets.json` — the JSON map `{ "<relpath>": <max_bytes>, ... }` that `/doctor`'s budget advisory reads; see *Creating the caps config + a recorded cap-bump* below) **PLUS a dated one-line `claugentic-DECISIONS.md` entry recording why the cap grew.** This is a `[J]` **recorded decision, never a mechanical action** — the git history holds the prior cap, the DECISIONS line holds the *why*. Raising a cap without the recorded rationale is the failure this rung forbids: the record is what keeps a cap-bump honest rather than a silent ceiling-raise. *(**One cap source per repo, and it is the same file everywhere:** `.claude/claugentic-doc-budgets.json`. The harness's own caps live there too, so the bump is the same edit — that file's value **plus** the DECISIONS line — in a harness-self repo as in an adopter's, with **one harness-self extra**: this repo also pins its caps byte-exactly in a test (deliberate drift-detection), so a harness-self bump updates that pin in the same commit. The harness-self gate `scripts/check_doc_budgets.py` only *reads* the config — an adopter has no such script, so `/doctor`'s advisory reads it there instead. **Schema:** `skills/doctor/SKILL.md` → *Adopter doc-budget advisory* is its canonical home and today states the flat `{ "<relpath>": <max_bytes> }` form; the glob-by-key and report-only forms are stated in the harness-self gate's module docstring until the two statements converge. Never a second cap list.)*
       3. **Shard the ledger — the LAST resort (built, and climbed once).** Splitting an oversized ledger into a small routing **index at the original path** plus one file per topic, each under its own cap, is a valid *scaling* lever — and the harness has now climbed it on one of its own ledgers, so it is a real procedure rather than a parked idea. It is still **not reached first**: for an **append-style log** (entries supersede, so condensation genuinely reclaims bytes) a recorded cap-bump (rung 2) preserves the single-scannable-record property and is the right answer. Sharding earns its keep on the other content class — a **rule-book** whose entries persist by design, where each condensation pass lands higher than the last: there a cap-bump only defers the problem. Two properties make the split safe: every external reference keeps pointing at the **index path** (so nothing outside has to learn the parts), and the index stays **routing-only** under a tight cap of its own — an index that starts holding entries has quietly become the ledger again. **Adopters are unaffected by default** (one simple ledger file); an adopter who does climb this rung caps each part in their own caps config — one key per part, the procedure `skills/doctor/SKILL.md` owns.
     - **Creating the caps config + a recorded cap-bump (adopter procedure).** The caps config is **per-repo and repo-owned**, and the harness **ships no live copy** — its own config is a tracked dev-only file that never reaches an install, so an adopter creates their own rather than inheriting harness-tuned numbers. (The harness's caps live in *its* copy of this same config; the harness-self gate only reads it. There is never a second cap list in either repo.) To opt in, create `.claude/claugentic-doc-budgets.json` with the schema `{ "<relpath>": <max_bytes>, ... }` (repo-relative path → integer byte cap; an absent key = that file is un-capped/skipped) — e.g. `{ "docs/claugentic-DECISIONS.md": 60000, "CLAUDE.md": 6000 }`. `/doctor`'s budget advisory reads exactly this file and this shape (its reader-contract is defined in `skills/doctor/SKILL.md` → *Adopter doc-budget advisory*; this doc does not redefine it). **A recorded cap-bump (rung 2)** is then a one-line edit raising that file's value **plus** a dated `claugentic-DECISIONS.md` entry naming why the ledger legitimately grew — the two together are the recorded decision.
     - The pass is safe to run inline **because the drop is git-recoverable**; surface the diff before committing if the condensation is large or surprising (a reversible FLAG). No separate task — the trigger (the WARN or `/doctor`'s advisory) is the cue, the pass is the work. **Sharding a ledger stays the ladder's last rung, not a step here** — condense first; split a ledger only when honest condensation genuinely can't reach the band (rung 3 above, with its log-vs-rule-book test).
     - **The executable operator is `/claugentic-dev-harness:condense`.** This procedure ships as a first-class skill that encodes the exact steps above in order (classify-first → absorb-landed → promote-must-holds → merge-siblings → trim-locators, ~80% band). On a **WARN or `/doctor`'s "condense soon" advisory**, the Definition of Done **OFFERs `/condense`** — a WARN/breach is a **ramp to the operator, not a dead-end**. `/condense` is prose-orchestrated with **no cut-decider**: it classifies and proposes a diff, you approve it (the approval is the decision gate), and it applies via `/doctor`'s existing user-approved-diff treat. Running the pass by hand and running `/condense` are the same procedure — the skill just makes the ordering (and the anti-footgun) hard to invert.
5. **Shipped-content scan green** — *harness-self only* (developing this plugin): `python scripts/check_shipped_content.py` scans every SHIPPED file's text for the release/init-contract content breaches — a stranded `claugentic-dev-harness:<token>` namespace literal (a rename leaving a dead agent/skill/command id) or a dangling reference to a stripped-uncreated path. A **run-gate** (like version-sync), not hook-wired; it pins the **exact-literal** cases mechanically (hard exit 1) and emits a **WARN** for the heuristic uncaveated-gate-mention pass (it never hard-fails on the heuristic). The script is **stripped from the release**. **Not applicable to adopter repos** — skip it.
6. **The project's lint / type-check / security gates** green.

**Reviewer sign-offs** (model judgment, not a mechanical gate):
5. **In-scope `docs/claugentic-standards/` dimensions pass** the `synthesizer-gate` audit (incl. SOLID) — solo, or synthesized from `lens-reviewer`s + `yagni-sentinel` — for what this slice touches; `/simplify` + `/code-review` run. For **any user-facing surface**, the craft dimensions (`product-ux.md` → *Aesthetic & motion craft*) are **in-scope** for that audit (effort-dialed — named craft bar reviewed, never certified beautiful).
6. **Runtime QA (dial-gated)** — when the slice's spec carries acceptance criteria (the `docs/claugentic-PRODUCT_SPEC.md` schema) and the repo records a run-the-app command, and the Verify dial has fanned out: the QA workflow (`engine/qa.js`) attempts each criterion's flow in the running app and tags the outcome (screenshots on failure); findings join the Verify synthesis. The script mechanically sequences the flow attempts and never silently skips (a QA run that could not boot the app reports exactly that); invoking it, and the flow observations themselves, are model-upheld.

**Plus:** acceptance criteria met (the spec's checklist) **+ no new tech debt.**

## Decision-gated autonomy (when to stop, when to proceed)

While iterating to the bar above, the agent **proceeds autonomously by default** and **stops only for a decision that is genuinely the user's.** This replaces the old binary *checkpoint (three routine pauses) vs build-to-green* ladder: the question is never "which rung am I on," it is **"is this a real choice the user must make?"** — judged per moment against four crisp rules. (The separate *who-watches-the-run* axis — an unwatched, engine-driven run earned per-repo — survives as **build-to-green**, below; it changes *who is watching*, not *when to stop*.)

**This is MODEL-UPHELD, not a mechanical gate.** The agent *judges* "is this a real choice?" against the criteria + examples below — nothing mechanically forces a stop at the right moment. The cost of a mis-judgment is **bounded by flag-and-surface** (a wrong "proceed" on a **reversible** call becomes a flagged item the user sees at the close — not a silent action), **except** for the irreversible class (c): a wrong "proceed" there is *not* recoverable by later review, so class (c) is a hard stop the flag path can never absorb. The bar is the criteria, not a guarantee.

- **STOP (blocking) — ONLY for a must-decide.** Three classes, and only these:
  - **(a) a genuine design fork** — multiple valid options with no clearly-right answer (a preference/architecture call that's the user's to make). *e.g. "store the worklist in a state file or derive it from the backlog?" — both work; ask.*
  - **(b) a spec with a real trade-off to accept** — the change buys X at the cost of Y and the user must accept the trade. *e.g. "the simplest fix drops support for the legacy format — accept that?"*
  - **(c) an irreversible / outward action** — commit-to-shared (push to a shared remote / `main`), republish/deploy, delete user content, spend, any external side-effect. **This is a HARD stop the FLAG path can NEVER absorb — even on a low-confidence call** (a wrong proceed here can't be undone by async review; the flag safety net covers only reversible judgments). *e.g. "push this to main?" — always ask.*
- **RESEARCH, don't ask — for a FACTUAL / technical uncertainty.** A question the agent can resolve from official / trusted sources is settled by **research (cited)**, never a user stop. Stops are for the user's *preference / judgment*, not for facts the agent can look up. *e.g. "does this API accept a null body?" — read the official docs and cite, don't ask the user.*
- **FLAG (non-blocking) — for a REVERSIBLE judgment.** A should-know (a judgment call · an accepted risk · a deviation from the spec · a low-confidence choice) whose cost-if-wrong is **reversible**: record a **one-line flag + the chosen default** and **CONTINUE** — async-reviewable, never a mid-run stop. *e.g. "named the helper `selectItems` (spec didn't say); FLAG + continued."* (A low-confidence *irreversible* call is **not** a flag — it is class (c) → STOP.)
- **SURFACE all flags at the close.** The close-out lists every flag raised during the run as **"things to review,"** so the user reviews them async *after* the work lands — not interrupted mid-run. (The running list lives in the plan file's Status block as a `Flags:` sub-line — kept **distinct** from the plan's disposition field; the close-out surfaces flags **and** dispositions as two separate concerns.)

**Acceptance:** an autonomous run completes without stopping for anything that isn't a genuine user-action; every reversible judgment is flagged with a chosen default and surfaced at the close; a factual uncertainty triggers cited research, not a stop; the three blocking-stop classes always stop.

**build-to-green (the who-watches axis — earned per-repo).** Running **unwatched** (engine-driven: `engine/build-item.js` runs the implement → gates → verify → fix loop mechanically, returning only at the retained hard stops) is **requestable**, unlocked per-repo only when CI runs the deterministic gates, a test baseline covers the touched code, and the item traces to an approved spec with testable acceptance criteria — the contract, the evidence-stated checks, and the verbatim decline live in `skills/build/SKILL.md` → Mode handling; the engine is built and runs only via the Workflow tool from the installed plugin (the session precondition), so a build-to-green ask declines only when a per-repo condition is unmet or that engine is unavailable. Watched or unwatched, the **decision-gated rules above govern when to stop** (the class-(c) irreversible hard stop is a *return* to the orchestrator in the engine case); build-to-green is a reduction of unwatched-run risk, never a substitute for the unbuilt deterministic trust-gates.

Iterate to meet this **fixed** bar, then **stop** — it terminates because the bar is *finite*, not "is it perfect?". Genuinely separate future work → `ROADMAP.md` (backlog, *not* debt).

---

## The finder pipeline (audit · product · doctor → backlog → build)

The harness has three **finders** that surface candidate work — `audit` (engineering, vs the `docs/claugentic-standards/` modules), `product` gap-mode (vs the spec), and `doctor` (harness-health) — and they share **one** lifecycle: **`FIND → SELECT → PLAN → OFFER-BUILD → BUILD`**. The shared pipeline exists so effort isn't spent elaborating items the user never wanted: nothing is planned, spec'd, or written to the backlog until the user has **chosen** it. **SELECT** (the scope gate), **PLAN**, and **OFFER-BUILD** are detailed below; **BUILD** stays at contract level here — its skill wiring lands with the build pipeline.

**FIND.** The finder runs its sweep and returns a **candidate list** — for `audit` / `product` the engine's structured `items`; for `doctor` its diagnose rows. The sweep's **coverage / run-report is computed over the whole scope** (every lens, every dir), independent of what the user later picks — this matters for the SELECT invariant below.

**SELECT — the scope gate.** This is where the user picks what to act on, *before* anything is written to the backlog. SELECT is a **model-upheld user action**, not a mechanical gate — the skill presents candidates and the user chooses; nothing forces a particular pick.

- **Read the dismissal memory first.** Before presenting candidates, read the finder's rejected-memory fence (`<!-- harness-audit:rejected-findings -->` for audit, `<!-- product-critic:rejected-proposals -->` for product) and **omit any already-dismissed candidate** — a finding the user once called *wrong* stays dropped. (This is the existing audit/product behavior, now named as a contract step.)
- **Present the remaining candidates as an editable markdown checklist** — one `- [ ]` line per candidate, **reusing the rendered backlog-item form** (the same per-item shape audit/product already produce) so it scales to N candidates (AskUserQuestion caps at ~4). The checklist is a **transient conversational artifact** shown *before* the fence is written — **never persisted into the managed `harness-audit:backlog` / `harness-product:backlog` fence**. Items are unchecked by default; the user ticks the ones to act on.
- **The checked subset is the SELECT result.** Unchecked candidates are **dropped for this run** — not written to the backlog. This is the anti-bloat gate: the backlog carries only what the user committed to.
- **Skip ≠ reject.** A non-tick is a **per-run skip** — it is *not* recorded in the durable reject-memory and **may resurface** on a later finder run. **Explicit rejection** ("this finding is *wrong*", not "not now") is the stronger, existing action: append one terse line to the finder's rejected-memory fence so it stays dropped across future runs. SELECT *offers* the user this rejection option, but a non-tick **defaults to skip, never reject** — rejection is a deliberate user override of model judgment, never inferred from a left-unchecked box.
- **Render from the selected subset via the `renderOnly` re-render seam.** The engine is a sandboxed Workflow script — a prose skill **cannot** call its internal `renderBacklogFence` on a filtered subset (un-exported, no render-only CLI entry). So the skill filters `result.items` to the ticked subset and **re-invokes the engine's agent-free `renderOnly` mode** with `{ ...result, items: selected }`; that fast-path re-renders the fence over the selected subset and returns the new `renderedBacklog` (the renderer stays the **single source** of the fence format). Full-scope `lensCoverage` / `verification` pass through **unchanged** (only `items` is narrowed — the invariant below is *why* coverage isn't recomputed). Keep-all / nothing-found skips `renderOnly` and writes the engine's original `renderedBacklog`; an **empty** selection is **never** passed to `renderOnly` (see the invariant). *(This corrects the earlier "zero engine change / no `selectedIds`" copy — mechanically false for a prose skill; the `renderOnly` fast-path lives in `engine/audit.js`.)*
- **★ INVARIANT — coverage / run-report stay full-scope; no empty `renderOnly` over a found run.** The `lensCoverage` and `verification` / run-report fields handed to `renderOnly` remain the **unfiltered, full-audit** values. The fence then reports honestly: *"we looked at all of this; you chose to act on these."* **Never recompute coverage over the selected subset** — that would falsely claim "every lens spoke" about only the kept findings. Because the skill narrows **only** `items` and passes the rest of `result` through unchanged, this holds automatically — but it is stated as an invariant so a future edit doesn't also filter `lensCoverage` / `verification`. **Second clause:** `renderOnly` is **never invoked with an empty `items`** when the full run carried Tier-1/2 findings — an empty re-render emits the engine's terminal "sound" signal, which would be a **false** claim when the run actually found things (handle keep-none conversationally + skip the write). (This is the verify-side honesty sibling of the audit cell-coverage fix.)

**Two SELECTs at two altitudes (don't conflate them).** The **finder's** SELECT (above) gates **that finder's own fresh `result.items`** before its fence is written — it is per-finder, per-run. **Build-TRIAGE** is the separate, later select that assembles the *worklist* from **three candidate sources**: the `harness-audit:backlog` candidates, the `harness-product:backlog` candidates, and the durable **Bugs** section (planless one-liners; selecting one **commits** it). The Bugs-as-source reader is wired at **build-triage** (not the finder SELECT).

**PLAN — preemptive, per committed item (architecture-first, not a stub).** When an item is **committed** (SELECTed to act on), its **full plan file** is produced via **Stage 2** — the architect-pass: **2a** plan-mode draft → **2b** advisory panel (substantial work only) → **2c** incorporate → the **Stage-3 `synthesizer-gate` gate** (plan-gate altitude) — structured by `docs/claugentic-PLAN_TEMPLATE.md`: problem · **Architecture & holistic fit** · approach · decomposition · the finding's evidence (`file:line`), plus a one-line `ROADMAP.md` entry. The plan is **specialist-produced groundwork** so build never re-derives context from scratch — but it is **reviewed groundwork (`synthesizer-gate`-gated at the plan altitude), never a guarantee of quality**; the whole step is **model-upheld** (the skill runs the architect-pass; nothing mechanical forces a plan).

- **The deep per-slice Spec stays just-in-time.** "Preemptive PLAN" means the *plan* exists before that item is built (**architecture-first**), **not** that every slice is deep-spec'd upfront. The deep Spec is produced inside build as it reaches each slice (Stage 4) — per-slice spec is the step-pruning gate and is cheaper JIT than deep-speccing every slice in advance.
- **★ Plan-volume bound (no dozens of plans at once).** **build-now** items are planned **one at a time as build works them** — the build loop is serial, so at most one fresh plan file exists at a time. **backlog** items get only a **one-line fence / `ROADMAP.md` entry**, planned **just-in-time when a later `/build` reaches them**. Either way the plan precedes that item's slices (architecture-first per item), never all upfront — a large multiselect never spawns dozens of ephemeral plan files.

**OFFER-BUILD — the finder→build bridge (no dead-end).** After SELECT the finder run **continues** instead of dead-ending into a manual `/build`: it asks — a small binary moment, so via **AskUserQuestion** (not the checklist) — *"build these now, or leave them in the backlog?"* **Default = leave in the backlog.** Build is **offered, never forced** (forced auto-build is an explicit non-goal); the offer itself is **model-upheld** (the skill presents it).

- **Build now** → enter the **build loop** (it consumes the pre-made plans and runs the JIT per-slice spec — see *BUILD* below).
- **Leave** → the selected items persist as backlog lines for a later `/build`; their plans are produced JIT then. Either way the **find→do flow is continuous** — no manual re-invoke needed.

**BUILD (the continuing contract).** Build works selected items **one at a time**, each starting from its **pre-made plan if it has one** (else planned architecture-first via Stage 2 before its slices), with the **deep per-slice Spec → Approve → Implement → Verify run just-in-time** as build reaches that slice (step-level pruning happens at plan-review / approve, so no slice is deep-spec'd before its turn).

**Scope approval precedes the deep per-slice Spec (the lifecycle ordering).** The finder's **SELECT gates *scope*** — *which items* the user wants — and it happens **before** Stage 4 ever runs: an item is **not deep-spec'd until it has been selected / committed**, so a specialist never deep-specs a step the user will later reject (the original "plans had steps I didn't want" pain). Read the two as separate altitudes: **SELECT gates scope** (which items) · **Stage 4 gates steps** (which slices, JIT) · **Stage 5 approves the spec**. (The Stage table is unchanged — Spec → Approval, no code before Approval; this note only names where the finder's scope-gate sits relative to it.)

**Commitment, not capture, triggers the plan:** selecting an item *is* committing it *is* the Stage-2 trigger — a raw Bug / Later one-liner stays planless until selected, and a trivial / mechanical item skips the backlog entirely and is just done.

---

## Executing an audit backlog item — tag → discipline

`/claugentic-dev-harness:audit` writes a tagged backlog into `docs/claugentic-ROADMAP.md`; there is **no separate refactor command**. Each item runs through the pipeline above, and its **tag selects the discipline** the implementer applies. The tag→discipline mapping is defined here (the canonical home); the rationale is settled — don't re-litigate it.

| Tag | Discipline |
|-----|-----------|
| **`refactor`** | **Characterization-tests-first — a HARD precondition.** A refactor item on untested behavior-bearing code **cannot start until its Tier-1 "establish a test baseline" item is done**; if that baseline is absent, the implementer **stops and asks** rather than touching code. This is **model-upheld by design** — upheld by the implementer + the Verify gate; a mechanical characterization-enforcement `PreToolUse` hook was considered and **declined** (0035, 2026-07-04 — the harness does not force characterization-first), never a coming mechanical gate. **Say this to the user when the pause fires:** *"Before I tidy this code I need to capture what it currently does as a test, or I can't prove I didn't change its behavior — so I'll establish that baseline first."* |
| **`capability-upgrade`** | Migration safety — feature-flag the new path · dual-write / shadow-read · keep a rollback. |
| **`dependency-health`** | Update + verify — bump, run the full suite green, check the changelog for breaking changes. |
| **`bug`** | Reproduce-first — a **failing test that captures the bug**, then the fix that makes it pass. *(Diagnosis front-end — model-upheld option: the harness governs *fixing* a known bug, but has no *investigation* procedure for an **unknown** failure. For a failure whose cause you don't yet know, **consider the bundled `/debug` skill** to reproduce+isolate it first; its output then **enters the pipeline as a `bug` item** and runs reproduce-first from there. `/debug` diagnoses; the harness fixes — it is an option the agent may reach for, never wired harness behavior.)* |
| **`feature`** | The standard pipeline; **test-first-when-chosen** adds an independent-test-author red-first step (**model-upheld by design — mechanical enforcement declined**) — never a precondition that fires by default. |

### Choosing the approach for the work at hand — the methodology toolbox

The harness fits the approach to the **nature of the work at hand**, per work-item — **never one golden hammer**. This **extends** the tag→discipline table above (which already picks per work-TYPE) with a written map of established practices, when each fits, and where the harness stands on each — so the per-work approach-choice is principled, reviewable, and consistent across agents/sessions instead of tacit. It is **MODEL-UPHELD guidance: no code reads this, and there is no mechanical selector** — the agent **consults** it to fit the approach to the work.

*Stance key:* **embodied** = the harness already works this way · **accommodated** = the harness supports it (via a standard / role / pass) without imposing it · **opt-in-with-caveat** = pick-able, but honestly against the harness's grain (see the caveats below).

| Practice | Fits when… | Harness stance |
|----------|-----------|----------------|
| **TDD** | logic-heavy work · well-understood requirements · a `bug` or `refactor` | **embodied** — red-first is already the `bug`/`refactor` discipline; for `feature` work it's an available *when-chosen* option — model-upheld, never a default (the per-work test-author mechanism is wired into the `implementer` — model-upheld via that prompt + the Verify gate; mechanical enforcement declined by design), one tool among many. |
| **ATDD / BDD** | user-facing behavior with acceptance criteria | **structurally embodied** — Stage-4 machine-readable acceptance criteria + Stage-7 runtime-QA-per-criterion + the DoD "acceptance criteria met" gate. |
| **DDD** | complex / rich domain logic | **accommodated** — the `docs/claugentic-standards/` catalog + the architect pass support it; never imposed. |
| **Trunk-based** | continuous integration · small batches | **embodied** — the harness's small-batch / land-slice-complete grain. |
| **XP** | rapid iteration with test-first + refactoring + small releases | **accommodated** — many of its practices are embodied. |
| **Contract-first** | API / integration boundaries | **accommodated** — the `api-and-contracts` standard. |
| **CI/CD-gating** | deterministic checks must pass before land | **embodied** — the DoD deterministic gates. |
| **Spec-by-example** | concrete examples clarify intent | **accommodated** — acceptance criteria + worked examples. |
| **GitFlow** | long-lived release/develop branches are genuinely needed | **opt-in with a caveat** — see below. |
| **Shape Up** | fixed appetite · variable scope · longer cycles | **opt-in with a caveat** — see below. |

**Against-the-grain honesty caveats** (so the harness never silently "supports" a practice its pipeline half-fights):

- **GitFlow** — long-lived branches fight the harness's small-batch / trunk-ish, land-slice-complete grain.
- **Shape Up** — fixed-appetite / variable-scope fights the harness's small-batch / **fixed-quality-bar** grain (the harness fixes the quality bar and varies scope by slicing, not by trading quality for a deadline).

TDD is **one entry among many**, applied **when it fits** — never the flagship, the default, or charter-enforced. The point of this toolbox is the *awareness* (fit the tool to the work), not a mandate for any single practice.

---

## Bundled edge-skills the harness doesn't itself cover — model-upheld pointers

Claude Code ships **bundled default skills** (`/debug`, `/architecture`, `/skill-creator`, `/incident-response`, `/deploy-checklist`, `/loop`, `/goal`, …). The harness governs its own pipeline; a few of these fill **edges the pipeline doesn't reach** (diagnosing an unknown failure · ADR-depth trade-off reasoning · the harness's own skill-dev · post-Land ops). These are **pointers, not wiring** — each reads as *"consider this skill here,"* an option the agent or adopter **may reach for**, never as behavior the harness runs. (The stage-placed pointers above — `/debug` on the `bug`-tag row, `/architecture` at Stage 2 — are the same kind; this section collects the rest.)

- **`/skill-creator` — for the harness's OWN skill development (maintainer pointer).** When developing or revising a harness skill (`init` / `audit` / `build` / `product` / `doctor`), **consider `/skill-creator`** for its description-triggering evals — it exercises whether a skill's description reliably triggers on the right prompts. A maintainer aid for keeping the harness's own skills sharp; not part of an adopter's build loop.
- **`/incident-response` + `/deploy-checklist` — post-Land ops pointers for adopters who ship.** The harness's lifecycle currently **stops at Land** (ship-and-operate is out of its pipeline). An adopter who deploys **may reach for** the bundled `/incident-response` and `/deploy-checklist` skills for the operate side; durable lessons from an incident feed back **into** the pipeline as a `bug` item or a Stage-9 harvest. Pointers only — the harness does not run them.
- **`/loop` + `/goal` — scheduling / goal-persistence primitives (pair them with a workflow).** `/loop` runs a prompt on repeat in **three modes** (fixed-interval · self-paced/dynamic · built-in maintenance); `/goal` is **condition-based persistence** (keep going until a stated condition holds). In the loops-blog taxonomy these are the **time-based** and **goal-based** loop types (with **turn-based** and **proactive** the other two — all four are real, useful classifications). They are scheduling/goal **primitives — not *themselves* the multi-agent orchestration.** That orchestration is a **dynamic workflow**: *Claude writing its own multi-agent harness on the fly* — a JS file that spawns and coordinates subagents — which is exactly what the harness's **`engine/*.js` (audit · build · qa) + the Workflow tool already ARE.** **Pair the two layers:** run a repeatable workflow (triage / research / verify) under `/loop` at an interval, or under `/goal` to a hard completion bar. *(Mapping to what the harness already does: build-to-green ≈ goal-based; the audit's loop-until-dry ≈ the blog's loop-until-done pattern.)*
- **Harness-better SKIP carve-out.** The bundled `system-design`, `tech-debt`, `code-review`, and `testing-strategy` skills are **single-shot and self-graded**, so they are strictly **weaker INSIDE this pipeline** than what already covers those concerns (the architect-pass · `audit` · the Verify `/code-review` + `synthesizer-gate` panel · the `testing.md` standard). **SKIP the plugin variants inside the pipeline** — recorded as a carve-out, not wired. (Honest scope: they are weaker *inside the pipeline*, not universally bad — `system-design`, for one, still fits pre-repo greenfield ideation.)

### Skills-taxonomy map — a discoverability / design-discipline lens

The "how we use skills" blog groups skills into **nine categories** (a taxonomy for what a skill is *for*). Mapping the harness's own skills against it keeps skill-building principled and makes the skill surface discoverable. This is a **short model-upheld note**, not a managed doc and not wired — a lens the maintainer consults, nothing reads it.

- **The harness's own skills** cluster into a few of the nine categories: `init` (**setup / scaffolding**), `audit` + `doctor` (**analysis / diagnostic** — audit finds quality gaps, doctor finds harness-health drift), `build` (**workflow / orchestration** — the pipeline runner), `product` (**discovery / design** — the product-excellence pass).
- **The bundled edge-skills** above fill the categories the harness's own skills don't occupy: `/debug` + `/incident-response` (**diagnostic / operational**), `/architecture` (**design / decision-record**), `/deploy-checklist` (**operational / release**), `/skill-creator` (**meta / skill-authoring**).
- **The gap this map surfaces:** a proposed new harness skill should name **which category** it serves — if it duplicates a category the pipeline already covers well (e.g. a second *diagnostic* that re-does `audit`), that is a signal to point at the bundled skill instead of building. The taxonomy is the natural home for a new harness skill's category when one is added.

---

## 9. The learning loop (how the harness grows)

A **finite harvest checklist run at Land** (model-upheld discipline, not automation — it does not trigger by itself). The orchestrator convenes **`retrospect-harvester`** — the **active** harvester — to sweep the landed change against the six categories and return the concrete edits (or *"nothing durable this slice"*); the orchestrator then applies them (it owns the commit). This is the **active** counterpart to `doctor`'s **passive** *"a recent land lacks a DECISIONS / INVARIANTS / standards touch — did the harvest fire?"* flag: **doctor REPORTS the signal; `retrospect-harvester` DOES the harvest** (one owner per role, no double-build). Sweep these six; for each, **emit the edit** or an explicit *"nothing durable this slice"*:

- **(a)** A convention that recurred across review findings → **promote to STANDARDS / CLAUDE.md**. A promoted lesson **must record the incident that motivated it** (the failure/near-miss it prevents) — so the rule is un-cargo-cultable and safe to delete once its cause is gone.
- **(b)** A manual/lens catch that a gate or checklist **could have made** → **open a gate item on `ROADMAP.md`** (not just a `claugentic-DECISIONS.md` line — a logged decision doesn't become a check by itself). *(Worked example: a manual catch that recurred at Verify — logged as a decision but never enforced — gets opened as a `ROADMAP.md` gate item so it becomes a real check, not just a note.)*
- **(c)** A prompt tweak that sharpened a specialist → **fold into the `.claude/agents/` role file**.
- **(d)** Process friction → **edit this `WORKFLOW.md`**.
- **(e)** Every non-trivial choice → **one dated line in `claugentic-DECISIONS.md`**.
- **(f)** A **load-bearing invariant** this slice established or relied on (a "must stay true or X breaks" constraint whose rationale isn't obvious from the code) → **record it in `docs/claugentic-INVARIANTS.md`** with its why + dated provenance. **Create the file lazily** — only when you have the first genuine invariant to record (do not seed it empty); most slices have none, so the honest emit is usually *"no new invariant this slice."*

**The six categories name the COMMON lessons and their homes — not a closed destination list.** A lesson whose canonical home is elsewhere (the PLAYBOOK · a skill's SKILL.md · a template) routes there — judgment picks the home. **One canonical home per lesson (pointers where needed, never copies)** — scattered duplicates drift into exactly the conflicting-entries the consult-DECISIONS-before-writing guard exists to prevent.

**Harvest edits land in SHIPPED files — write them adopter-aware.** The `.claude/agents/` role files, the standards catalog and this WORKFLOW all ship inside the plugin, so a harvested lesson is bound by the same release/init contract as any other shipped text: **no reference to a dev-only path an adopter's repo will never have**, and no uncaveated harness-self gate mention. Name the *thing* ("the release runbook", "the shipped-content scan"), not the stripped path. *(Provenance: 2026-08-13 — the previous slice's harvest wrote a stripped release-runbook path into an `honesty-reviewer` bar item; the shipped-content gate caught the dangling reference at the next Land. The gate worked; the harvester didn't know.)*

**Promotion is two-tier (manual, user-approved):** a *universal* lesson → stage in `docs/claugentic-standards/CANDIDATES.md`, then promote upstream so every repo gets it on update (in the harness's own repo there is no staging step — the module *is* upstream; edit it directly); a *codebase-specific* lesson stays **local** (`CLAUDE.md` / `claugentic-DECISIONS.md`), never propagated.

---

## Plan file lifecycle

`​.claude/plans/NNNN-<slug>.md` (active, contains Plan + Review + Spec + slice checklist) — one plan per substantial change; the slices inside it are the per-session units. Numbering is sequential (`0001`, `0002`, …).

**Commit the plan file at draft (Stage 2), not only at Land.** A plan left uncommitted-and-untracked is unrecoverable if the session crashes — and once `.claude/plans/` is emptied by a prior Land, git no longer tracks the dir, so an untracked new plan has no safety net. Commit each plan (and each slice) promptly; never leave a durable artifact uncommitted across a long gap. *(Provenance: 2026-06-25, plan 0028 — its draft was lost once exactly this way, then recreated and committed.)*

**A plan completes — and is removed (git history keeps it) — once every remaining item has a disposition, gated *only* on the committed slice, never on deferred/rejected/blocked parts.** At close (or on demand), give each still-unchecked item one of three dispositions:

- **done** — checked off (it landed).
- **defer** — a one-line item on `docs/claugentic-ROADMAP.md`; OR, for a substantial / externally-blocked remainder, **move the remaining items into a NEW plan file** (+ one roadmap line pointing to it). Either way the *current* plan closes now.
- **reject** — recorded as a declined decision (a `docs/claugentic-DECISIONS.md` line so it isn't re-proposed) and dropped.

Then the plan **completes and is deleted.** A plan **never lingers `pending` waiting on an outside event** — when the last pieces are blocked externally, defer-to-new-plan (or reject) and **close now**. (`skills/build/SKILL.md` close-out + `docs/claugentic-PLAN_TEMPLATE.md`'s Disposition note carry the same model.)

### Scope that emerges mid-build — the in-flight split

Work surfacing *while building* is **not** silently absorbed; it splits two ways (the user makes the call at plan-review / approve, or at disposition; the architect-pass frames what is genuinely in-scope):

- **(a) intrinsic to the feature** (genuinely part of *this* plan's requirement) → **fold it into the plan**: account · spec · deliver, and **re-run the steps** (plan → review → spec → build → verify). The decomposition grows because it *is* the feature — that is agile in-scope discovery, not creep.
- **(b) out-of-scope** (a defect, or unrelated work) → does **not** bloat the plan. Record it on `docs/claugentic-ROADMAP.md` (a defect → the **Bugs** section; other work → the relevant backlog) and carry on.

**There is no "tech-debt" parking section — debt is structurally prevented, not parked.** Every slice lands complete (the Definition of Done, no shortcuts) and Verify audits the diff against the in-scope standards *before* it lands — so work that would otherwise become debt is built wholly instead: in-scope discovery → fold and re-run; a too-big slice → re-decompose into complete slices; an emergent design flaw → re-plan + re-review; genuinely out-of-scope work → its own roadmap item + its own complete plan (built wholly later); an external blocker → deferred + tracked. *Deferring scope* (tracked, completed wholly later) is not *creating debt* (a half-done thing left in code — not allowed). This is **model-upheld and reviewer-caught, not a mechanical guarantee** — the process prevents debt structurally; it does not make debt impossible by machine. *Existing* debt in an adopter codebase is the **audit's** domain (Quality findings, tracked-to-fix).
