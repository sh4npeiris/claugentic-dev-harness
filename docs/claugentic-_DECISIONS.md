# Decisions

A **forward-looking maintainer's guide** for THIS repo: the standing decisions a future agent MUST know to safely improve it without re-breaking it or re-litigating settled choices. **Consult before revisiting a past choice.** This is **NOT an append-only history** — condense it periodically when `/doctor` flags this ledger near its configured cap (or on your own periodic review): merge **superseded/settled** entries into git history (which keeps the dropped detail) and **promote a hardened "must-hold" to your invariants ledger** — keep an entry only if a future agent needs it to decide correctly (the condensation pass; see `docs/claugentic-WORKFLOW.md` → Definition of Done). When in doubt, keep a constraint. For a genuinely new decision, **append a dated one-liner at the top** of the relevant section.

_(No decisions recorded yet — append your first as you make a non-trivial, hard-to-reverse choice.)_
