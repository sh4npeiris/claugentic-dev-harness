# The workflow & roles — process, stages & gates

Part of the decisions ledger — index & filing rule: `docs/claugentic-DECISIONS.md`. Do not link this file from outside the index.

- **Bundled edge-skills = model-upheld pointers, never wired** (canonical: WORKFLOW → *Bundled edge-skills*; 0032). Don't-re-break: `/loop`-`/goal` are scheduling/goal PRIMITIVES that **pair** with a dynamic workflow (which is what `engine/*.js` + the Workflow tool ARE), the four loop types (turn/goal/time/proactive) are real, and the harness-better SKIP carve-out (`system-design`/`tech-debt`/`code-review`/`testing-strategy` weaker *inside* the pipeline) holds — the earlier "debunked/orchestration" framing was an over-refutation.
- **Dynamic-workflow multi-agent grounding** (canonical: WORKFLOW → *Why the multi-agent shape*). The multi-agent architecture (clean-context independent reviewers + adversarial verify) is **grounded, NOT proven**, in the dynamic-workflows blog's **three failure modes** (agentic laziness / self-preferential bias / goal drift) — each mode is **structurally mitigated, never "solved."** The pattern-map is **accurate, not aspirational**: fan-out-synthesize · adversarial-verify · loop-until-done · generate-and-filter ✓; classify-and-act ~partial; **tournament ✗ — not embodied**. Don't-re-break: never let the grounding read as a correctness proof; never claim tournament is embodied.
- **Methodology-awareness = model-upheld toolbox, no mechanical selector** (canonical: WORKFLOW → methodology toolbox + tag→discipline; `docs/claugentic-_CHARTER.md` seed; INVARIANTS → *greens-without-editing*; 0030). Don't-re-break: **TDD is one tool among many, never the flagship/default** (the default `feature` build is unchanged); **red-first-when-test-first-is-chosen** is wired in `implementer` + `synthesizer-gate` (model-upheld) but **mechanical enforcement is DECLINED by design** (0035 rejected 2026-07-04 — the harness does not force test-first) — **never claim mechanical enforcement**; the charter is update-not-append, create-if-absent/never-clobber, absent≡current-behavior. **The charter write-trigger fires at Stage 4 on a non-default methodology choice** (defaults-safe — empty charter ≡ default; 0038).
- **Decision-gated autonomy** (canonical in WORKFLOW → *Decision-gated autonomy*). The autonomy question is **"is this a real choice the user must make?"** — **model-upheld** (nothing mechanically forces a stop). **STOP only for three classes** (design fork · spec trade-off · irreversible/outward action) — **class (c) irreversible is a HARD stop the FLAG path can NEVER absorb.** RESEARCH-don't-ask for facts; FLAG-and-continue (reversible only) + SURFACE at close. **build-to-green is the separate *who-watches-the-run* axis** (watched vs unwatched/engine-driven, earned per-repo).
- **Plan disposition + in-flight scope-split + ROADMAP taxonomy** (canonical: WORKFLOW → Plan file lifecycle, mirrored in `skills/build/SKILL.md` + TEMPLATE). A plan **completes and is deleted, gated only on the committed slice**, never lingering `pending` on a blocker. **No tech-debt parking section — debt is structurally prevented** (every slice lands complete; Verify audits the diff pre-land), model-upheld + reviewer-caught, NOT a mechanical guarantee; existing adopter debt is the *audit's* domain. **ROADMAP taxonomy — `Bugs` is the only new durable section**, and it (like Later/Ideas) survives a re-audit **structurally** by sitting outside the fence markers; dismissed findings stay in the wired `rejected-findings` fence (DRY).
- **The Definition of Done lives canonically in WORKFLOW** (deterministic gates vs reviewer sign-offs — the split is the honesty line); CLAUDE.md + the pipeline table point to it. The Verify effort-dial reuses the Stage-0 "substantial" triggers — no scoring formula. **Each group numbers from 1** (0041 S12a): an external *"DoD gate N"* always means a **deterministic** gate — never merge the two lists into one sequence.
- **Stage 9 is a finite harvest checklist run at Land — `retrospect-harvester` is the ACTIVE harvester** (canonical: WORKFLOW → *The learning loop*). Model-upheld, **not automation** (it PROPOSES; the orchestrator applies + commits). The **ACTIVE** counterpart to `doctor`'s PASSIVE "harvest likely skipped" flag — **doctor REPORTS; the harvester DOES** (one owner, no double-build).
- **Stage-9 destinations branch by repo type; the upstream channel is named ONCE (0041 S9, 2026-08-17).** Rows (a)/(c)/(d) name **plugin-side** homes — the standards catalog, `.claude/agents/`, WORKFLOW itself: harness-self edits them directly; **an adopter** reads the role files from the plugin install and has only managed copies of the rest, so a *universal* lesson stages in `docs/claugentic-standards/CANDIDATES.md` → **upstream** (an issue or PR at the plugin repo), a *repo-specific* one goes to an adopter-owned home (CLAUDE.md harness area · DECISIONS · ROADMAP · INVARIANTS). The channel URL is written in prose **exactly once**, in WORKFLOW → *The learning loop*; everywhere else points there. Prose + test pins only, **no new gate** (the enforcement deferral below still holds); pinned by `tests/test_adopter_pointer_integrity.py`.
- **Mechanical enforcement / hooks / triggers DEFERRED to a post-v0.4.0 holistic eval (USER, 2026-07-04 — do-not-re-litigate).** Real-world use shows the harness follows its processes **model-upheld**; that is the chosen posture "for now." Do NOT build or propose a new enforcement hook/trigger/gate before that post-release evaluation. This **generalizes** the two already-declined hooks: **0035's red-first deny-hook** (rejected — the harness does not force test-first) and **0037 S3's skill-usage observability hook** (a `PreToolUse` hook can't observe skill invocations — skills aren't tool calls, nothing to fire on — + YAGNI for a single-maintainer harness where `/skill-creator` triggering-evals already cover under-triggering). Neither is wired; **don't re-propose** either.
- **Scope is tech-agnostic:** no cross-product suite layer (converge-on-plugin already propagates standards — the plugin version is the coordination signal), no stack-specific prescriptions in the harness (those stay a suite's own config).
- **Carry-forward gate (Stage-0, model-upheld `[J]`, plan 0039)** — before a *substantial* new arc opens, Triage confirms the prior arc left no finding un-homed; the proactive complement to the close-time disposition rule + doctor's retroactive scan (catches an imperfectly-closed prior plan before new work builds on it). A **model-upheld process rule, NOT a mechanical gate/hook** — the endorsed model-upheld posture, not the enforcement-hook class deferred above.
- **Mirror-back gate (Stage-0, model-upheld `[J]`, 2026-07-16)** — shape-triggered (rambling multi-clause intake), **either path**: distill the raw ask inline (goal · scope · constraints · questions asked) and mirror it back for a yes before substantive work; never proceed on inferred scope without stating it. Intake-time counterpart of the Stage-4→5 spec confirm-back. *Provenance:* two adopter misreads on lightweight-path work (wrong deliverable; an unrequested history rewrite) + the user hand-rolling "so to recap…" manually. yagni KEEP_AS_ONE_LINE — **no** brief template / persisted artifact / prompt-rewriter, **no capability-gap check at intake** (Stage 2/3 owns that). Not the deferred enforcement-hook class.
- **Eight roadmap items DECLINED as out-of-scope for v1 (2026-08-19).** Do not re-propose without evidence a real project needed one; the list and the per-item reason are the *Deliberate non-goals* section of `docs/claugentic-PRODUCT_SPEC.md`.
- **The backlog is ADJUDICATED against the north star, not worked down (2026-08-19).** Every open item was judged by the product spec's falsifiable test -- *does this supply what a TEAM supplies, or compensate for a capability the model already has?* -- with an independent refuter on each verdict. Of 16: **8 declined, 4 built, 4 split, 1 routed to the user.** Three items' PREMISES were measurably false and died on inspection (a ledger claimed two releases stale that was current · a file claimed *at its band* sitting at 68% · a copy claim called inaccurate that was not) -- **so re-measure an item's premise before working it; a backlog entry ages into a claim.** DECLINE is a first-class outcome and its home is `docs/claugentic-PRODUCT_SPEC.md` -> *Deliberate non-goals*, where the bar for re-proposing is **evidence, not a better argument**. The corollary that governs: a rule written for a weaker model is DEBT once the model outgrows it, so the harness must get **thinner** as models improve.
- **2026-08-20 (0044 S1a) -- the two Verify helper passes are DISCHARGEABLE, never silently skipped.** `/simplify` and `/code-review` are Definition-of-Done reviewer sign-offs; a slice may discharge either with a recorded reason in the plan's `Flags:` line instead of running it. The two reasons that have earned it: an artifact whose **exact bytes a recorded measurement attests** (an auto-apply pass would un-verify it, and a panel lens can do the same review with measurements), and a **helper that died mid-run** (discharged by the angles it completed, each disposition named). Rule text: `docs/claugentic-WORKFLOW.md` -> Definition of Done.
