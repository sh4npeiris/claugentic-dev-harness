# Build mode (`/claugentic-dev-harness:build`)

Part of the decisions ledger — index & filing rule: `docs/claugentic-DECISIONS.md`. Do not link this file from outside the index.

- **A thin orchestration layer** over the WORKFLOW pipeline + the audit's backlog — no new pipeline, agents, or state stores. The plain-English ask is the interface; a single named item is the fast path (no triage).
- **Decision-gated by default** (canonical: `docs/claugentic-WORKFLOW.md` → Decision-gated autonomy): the watched run proceeds autonomously, STOPping only for the three classes; reversible judgments are flagged-and-continued and **surfaced at the close-out**, plus the exception pauses (re-slice, item-failure with *"nothing partial landed"*, re-triage). **Build-to-green is the separate who-watches axis (unwatched, requestable):** eligibility = three unlock conditions as judgment-with-stated-evidence (CI runs the gates · a test baseline · an approved spec with testable criteria) **plus** script-only execution (`engine/build-item.js` — never a prose loop); unmet → a verbatim decline naming what's missing, then a watched run is offered.
- **The engine never lands/pushes/merges — `engine/build-item.js` contains no push/merge/land step by construction; the implementer it spawns DOES branch and commit, so the guarantee is scope (only its own work branch), not abstinence from git.** Every terminal status is a return-to-orchestrator (`green` → the before-land pause is the orchestrator's; `needs-irreversible`; `new-tier12` → re-triage, never folded silently; `not-green` at cap → "here is the residual", nothing partial landed; `blocked`). It iterates implement (`implementer` in a worktree) → deterministic gates (the pass/fail *decision* over reported exit codes is mechanical, never fail-open) → Verify panel → QA (when machine-checkable criteria exist) → fix, until green or cap.
- **The irreversible hard-stop set** (push to a shared remote **including `main`** — this repo admin-bypasses branch protection — deploy, data deletion, spend, external side-effects): stop, name the action + consequence, ask; **never proceed on silence.** **Never invent scope** — a discovered feature goes to the roadmap, not the worklist.
- **The loop:** confirmed ordered worklist → per-item engine → a **scoped re-audit over the touched `(module|dir)` cells** after each landed item (cross-file fallout is the closing full audit's — the harness has no dependency graph and claims none) → **re-triage on any new Tier-1/2** (safety over fatigue) → one `standard` closing audit → the terminal signal.
- **Resume derives, never stores:** the worklist reconstructs from **both backlog fences** (engineering + product) + plan files (`Spec'd` = awaiting a sitting · `Approved` = build when reached · unchecked boxes = in-flight · removed at Land = done); the picked *order* is re-confirmed (a 5-second re-confirm beats a third state store). **Approval is never inferred** — only an explicit `Status: Approved` counts.
- **Batch spec-approval is a requestable ask** (*"spec everything first"*), not a standing question — a passive tip line is the only as-we-go addition. The sitting is **roster-first** (scannable one-line roster; full triads beneath; approve/adjust/drop, drop unjudged) — the defense against relocating decision-fatigue inside the sitting. Interruption-honesty at the ask **and echoed at the sitting close**; a **file-level spec-currency check** before each item re-confirms if landed work touched its named files.
- **A substituted default is LOGGED, never silent** (0041 S10a D5, 2026-08-17). The Verify child's args come from the pure `verifyChildArgs(item, diffRef, builderFamily)` → `{ args, defaulted }`, and the call site logs every `defaulted` entry. The two that mattered were inline ternaries at the `workflow()` call: an item naming no `dimensions` silently got the two-dimension `DEFAULT_VERIFY_DIMENSIONS` panel (a narrowed coverage claim nothing stated), and a **non-boolean** `trustSurface` silently coerced to `false` — the flag that decides whether the trust-surface lens spawns at all. Values are unchanged (`trustSurface` stays fail-closed on a bad value); only the silence is fixed. The default set has ONE named source, region-pinned so a re-inlined literal turns the suite red.
