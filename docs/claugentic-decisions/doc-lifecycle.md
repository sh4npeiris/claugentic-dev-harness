# Doc lifecycle & condensation

Part of the decisions ledger — index & filing rule: `docs/claugentic-DECISIONS.md`. Do not link this file from outside the index.

- **Adopter doc-lifecycle trigger + operator (0038).** **SUPERSEDED in its premise by 0041 S6** (2026-08-14): the gate **SHIPS**, so "replacing the stripped `check_doc_budgets.py` WARN an adopter's repo can't produce" is dead. Restated as it now stands — **two readers of ONE cap source** (`.claude/claugentic-doc-budgets.json`): the **gate** emits the ≥90% WARN wherever its script is **present** and a config opts in — S6 put it in the release PAYLOAD and **S7 delivered it** (`init` copies `scripts/claugentic-check_doc_budgets.py` into the adopter repo, seeds their caps, and chains it into the pre-commit hook), so the trigger is live in any `init`'d repo — and **`/doctor`'s advisory** is the on-demand read of the same file, everywhere (skip-when-absent, `[D]` byte / `[J]` "condense soon" — an advisory, **NOT a gate/hook**), no longer a substitute for an unreachable gate. **`/condense`** is the executable condensation operator, unchanged (classify-first anti-footgun — landed-records are the PRIMARY target, never preserved; no cut-decider; reuses `/doctor`'s user-approved-diff apply-path; **accept-floor = clear the WARN with headroom**, ~80% reached only when absorbable settled bulk exists). The **"NO doc-budget hook" don't-re-break is ALSO superseded**, by the user's 2026-08-12 commit-time decision: the gate **is chained** into the shared pre-commit wrapper as of 0041 S7 (after the tree gate, `|| rc=1` so both run and both report), and the tripwire that forced it is now a positive pin. **Scope, stated:** hook-enforced only where a wrapper is wired — a gate-off repo has none. **Still live:** the shipped condensation trigger must stay honest about what a reading repo can actually produce — a `tests/` scan guards it, inverted in S6 from "no orphaned trigger" to "no stale unreachability claim".
- **Escape-valve ladder + per-repo caps config (0038 S3).** When condensation genuinely can't reach the band (all-live content), the ordered recourse is **(1) promote to home** (INVARIANTS/CLAUDE.md — respecting THEIR caps, a cascade not a loop) → **(2) a RECORDED cap-bump** (raise the file's cap + a dated DECISIONS line — a `[J]` recorded decision, NEVER a mechanical ceiling-raise; git holds the prior cap) → **(3) shard = LAST resort — BUILT and CLIMBED (decided 2026-07-16, landed 2026-07-30, plan 0040)**: the harness sharded its OWN decisions ledger (index + `docs/claugentic-decisions/*.md`); for an adopter it stays the optional last resort (one caps-config key per shard — `skills/doctor/SKILL.md` owns that procedure). **Rung 2 was deliberately SKIPPED here:** a recorded cap-bump is the right answer for a *log* (entries supersede, so condensation reclaims bytes) and a treadmill for a *rule-book* (entries persist by design and the post-condense floor trends upward) — bumping defers, it does not resolve. **The INDEX's own recourse IS rung 2**: a routing table is all-live, cannot itself be sharded, and growth-by-new-shards legitimately raises its cap — different content class, different rung. The principle the rung encodes: **bound what an agent READS per consult, not what the harness KNOWS.** Canonical: WORKFLOW → *The escape-valve ladder*. **DRY — exactly one cap source per repo:** the caps config `.claude/claugentic-doc-budgets.json` (reader-contract owned by `skills/doctor/SKILL.md`) is **per-repo and repo-owned, and the release ships NO live copy** — an adopter never inherits harness-tuned numbers. The harness's own caps live in its **tracked** copy of that same file; the gate only READS it. **Do not delete that file on the strength of the sentence above:** an absent config is a silent no-op exit 0, so removing it disarms the gate everywhere while staying green locally (it is un-ignored in `.gitignore` and pinned tracked by a test).
- **Setting a NEW cap: pick the band, never the measurement (2026-08-12, 0041 Stage-3 R2).** Rung 2 forbids an *unrecorded* cap raise; the same act hides in choosing a **first** cap for a newly-budgeted file. **Cap-at-current-size is a ceiling-raise wearing the other face** — and so is any cap the file already sits at 90-99% of: it lands green and **WARNs on the next append**, making every later edit a forced condensation, which is the ceiling-blessing this ladder exists to prevent. **Rule: the cap is chosen so the file lands at ~80% of it, and the condensation work to get there rides the same slice as the cap.** Declined and do-not-re-propose: capping a file at its measured size "to stop the bleeding." *Worked case:* `docs/claugentic-WORKFLOW.md` measured 83,383 B; relocation alone leaves 73,590 B, so a 70,000 B cap **breaches on landing** and any cap that clears it (≥74 KB) parks the file at permanent WARN — resolved by pairing the 70,000 B cap with a hard ≤ 56,000 B condensation AC in the same slice, never by sizing the cap to the file. **Applies to adopter seeding too**: a seeded cap is subject to this rule, which is precisely why the day-one-over case needs the `reportOnly` grace flag rather than a bigger number.
- **File by FIT; byte availability is not a filing criterion (2026-08-17, 0041 S9 A3).** The index's filing rule (`docs/claugentic-DECISIONS.md`) outranks a shard **named in an approved spec**: S9's spec named `plugin-distribution` for a 837 B entry measured at 12,311 B, so obeying it would have landed 13,148 B ≈ 93.9% and tripped the WARN — whose only legal discharge is a condensation pass. The implementer's deviation to the fitting shard was upheld at Stage 7. The inverse is the same error: when a WARN band makes one shard tempting because it has room, that is **routing by free space**, and the S6 precedent (one entry so routed) is the reason this is written down. Recourse when the fitting shard is genuinely full is the escape-valve ladder above, never a re-home of convenience.
- **INVARIANTS admission — pin it where it bites, or file it (2026-08-17, 0041 S10b-L2).** A constraint a **red-first pin** holds plus a **do-not-revert comment at the call site** is recorded where the next edit meets it; `docs/claugentic-INVARIANTS.md` is for constraints **no pin can hold**. Decided at approval (S10-L3), applied to the D6 trust-surface constraint; INVARIANTS untouched at 17,976 B. **Not a byte rule** — an un-pinnable invariant is filed even at the band (recourse = the ladder above), which is *File by FIT* applied to this ledger. Rule + auditor check: `docs/claugentic-standards/docs-traceability.md` -> *Load-bearing invariant traceability*.
- **`/condense` owns the condensation PROCEDURE; WORKFLOW keeps only the OBLIGATION and the ladder (2026-08-17, 0041 S12a).** WORKFLOW's DoD carried a **10,664 B** condensation section — several passages verbatim from `/condense` (keep/drop buckets · merge-siblings · the landed-records-are-the-primary-target anti-footgun · lever order + ~80% band). **The duplicated 6,556 B was deleted and a 792 B pointer line put back — WORKFLOW 96,022 → 90,237 B, a net −5,785 B**; the balance, 4,087 B, is WORKFLOW's own escape-valve ladder, which STAYS. The pointer keeps the **three** clauses `/condense` does *not* state: a WARN/advisory is a **do-it-now signal, not a deferral** (discharged inside the current slice) · the DoD **OFFERs** the operator · and an inline pass is **reversible**, so a small one rides the slice as a FLAG (where `/condense`'s apply is always approval-gated). This is WORKFLOW's own *one canonical home per lesson (pointers, never copies)* applied to WORKFLOW. **The escape-valve ladder STAYS PUT** — `/condense`, `/doctor` and `init` point at that anchor and `/condense` says in terms it "references the ladder; it does not build it"; relocating it would invert four pointers. **Nothing was merged into `/doctor`** — it already owned the reader-contract, rung 2 already carried the recorded-cap-bump rule, and the deleted bullet's remaining clause (*"an adopter creates their own"* config) was **stale**: `init` step 7b has seeded it create-if-absent since 0041 S7. **Do-not-re-propose:** re-stating the procedure in WORKFLOW "for readers who don't open the skill" — that is the duplication this removed.

## Readiness

The core works on real repos; the harness is feature-complete and **deferred backlog items ship to real use, not speculatively** (YAGNI). The bar for improving it is unchanged — no new tech debt, both gate groups pass, in-scope `docs/claugentic-standards/` dimensions reviewer-signed (DoD canonical in WORKFLOW).
- **Shard existence = the two-direction index↔shards agreement pytest (0041 S4), never a second cap.** Every index-routed shard must exist; every shard file must be index-routed; both directions fail independently. The glob budget entry stays the SOLE cap; `REQUIRED_SHARDS` is deleted.
