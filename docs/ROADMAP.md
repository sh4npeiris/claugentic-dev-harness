# Roadmap

The forward backlog for the harness itself. Work runs through `docs/WORKFLOW.md` (triage → plan → review → spec → approve → implement → verify → land → retrospect), sliced so each unit lands complete with no tech debt. Tangents land here, never silently into the current change.

> The harness audits *adopter* repos via `/claugentic-dev-harness:audit`, which writes its own `harness-audit:overview` / `harness-audit:backlog` fences into that repo's `docs/ROADMAP.md`. This file is the product's own roadmap, not a generated audit — an adopter generates theirs by running the skill.

---

## The two standing tracks — growing the libraries (ongoing · just-in-time)

The harness grows as it meets more codebases. Neither track is ever "done" — that's the right shape: additions are made when real use proves a gap, never speculatively.

| Track | How it grows |
|------|--------------|
| **Grow the role library** (`.claude/agents/`) — add specialist agents as real use surfaces gaps the starter set lacks. | The workflow delegates to specialists; when real work keeps hitting a job no existing role owns, that's the signal to add one (and register it in `plugin.json`, the WORKFLOW roster, and the architecture tree). |
| **Grow the standards catalog** (`docs/standards/`) — author new quality modules and capability modules (Redis, queues, object-storage, …) as real projects pull them in. | A real audit or review that needs a bar the catalog doesn't carry is the signal to author it (conforming to `_TEMPLATE.md`, indexed in the catalog README). The catalog modernizes vibe-coded apps, not just cleans code. |

---

## Backlog — the work worth doing  _(generated by /claugentic-dev-harness:audit · do not edit — re-run to refresh)_

<!-- harness-audit:backlog:start -->
status: COMPLETE · level: standard · done-cells: [security×scripts, security×workflows, security×tests, security×skills, testing×scripts, testing×workflows, testing×tests, testing×skills, maintainability-structure×scripts, maintainability-structure×workflows, maintainability-structure×tests, maintainability-structure×skills, reliability-resilience×scripts, reliability-resilience×workflows, reliability-resilience×tests, reliability-resilience×skills, docs-traceability×scripts, docs-traceability×workflows, docs-traceability×tests, docs-traceability×skills] · pending-cells: [] · date: 2026-06-12

`refactor` = tidy without changing behavior · `capability-upgrade` = add/upgrade a technology · `dependency-health` = update/patch dependencies · `bug` = fix wrong behavior · `feature` = new behavior.
`(checked against the code)` = a separate agent re-read the code and couldn't refute it · `(could not confirm independently — model's assertion)` = still just the model's claim · `(⚠ not yet verified — re-run to confirm)` = budget ran out before checking — a re-check by a different model family than the builder (the cross-model judge; on a same-family run, tagged as such) — a reduction of shared-blind-spot risk, not a mechanical guarantee.

### Tier 1 — critical

- **QA's four pure prompt-builders sit outside the tested block, so none are covered** — `refactor` *(checked against the code)*
  - Technical: `bootPrompt`, `teardownPrompt`, `driverPrompt`, and `verifierPrompt` are pure functions but are defined AFTER the `// --- end helpers ---` marker (qa.js:732), so the extract-and-eval harness in qa.test.mjs (which slices only the `// --- helpers --- … // --- end helpers ---` block) cannot evaluate or test them. The sibling script audit.js places its analogous pure prompt builders (`buildLensPrompt`, `buildCriterionLensPrompt`, `buildBlindspotPrompt`, `buildVerifierPrompt`) INSIDE its helpers block, and audit.test.mjs tests them (10 references). The inconsistent placement is an analysability/testability gap (Separation of concerns: pure logic should be in the testable block) and is the direct cause of the line-805 defect going undetected. (recurs in 2 files: workflows/qa.js:738-856, workflows/qa.js:732). Evidence: Confirmed against the code. qa.js helpers block spans line 35 (`// --- helpers ---`) to line 732 (`// --- end helpers ---`). The four pure prompt builders are all defined AFTER the marker: bootPrompt (qa.js:738), teardownPrompt (qa.js:766), driverPrompt (qa.js:792), verifierPrompt (qa.js:838) — outside the testable block. The test harness loadHelpers() in tests/workflows/qa.test.mjs:40-95 slices ONLY the marked block (src.slice(start, end) at line 49) and its `names` allowlist (qa.test.mjs:50-90) contains none of the four functions, so none can be evaluated or tested. The line-805 broken-string defect — `"STATES SCOPE: ... (${(criterion.states || []).join(', ') || 'none ...'})..."` (qa.js:805), a `${...}` interpolation inside a regular double-quoted string, not a backtick template, so it is emitted literally — lives inside driverPrompt, one of the four invisible functions, which is exactly why it slipped through. The comparative claim also holds: audit.js places its analogous builders buildLensPrompt (audit.js:453), buildCriterionLensPrompt (audit.js:472), buildBlindspotPrompt (audit.js:494), buildVerifierPrompt (audit.js:568) ALL before its `// --- end helpers ---` marker at audit.js:999 (inside the block), and tests/workflows/audit.test.mjs both extracts them (lines 111,114,134,140) and tests them (lines 458,460,777,778,1160,1161) — the 10 references cited. I searched for any alternative test path importing/evaluating the four qa.js prompt functions and found none; the refutation attempt failed..
  - Plain English: Confirmed: bootPrompt/teardownPrompt/driverPrompt/verifierPrompt are defined below the `// --- end helpers ---` marker (qa.js:732), and qa.test.mjs's extract-and-eval harness only evaluates the helpers block — zero test references to any of them. The sibling audit.js places its analogous builders INSIDE its helpers block and tests them, proving the pattern is achievable. This placement is the direct reason the line-805 bug shipped invisibly. Fix: move the four functions (they close over only their params + other helpers — already pure) above the end marker and add their names to the test's extracted-helpers list, then assert each produced prompt carries its load-bearing fields (criterion id/flow/expect/states, bounded readiness count, artifact dir, the 'RUNNING AS' demand) and contains no literal `${`. Two lens findings (testing, maintainability) named this same gap — consolidated here.
  - Impact/effort: High impact (closes the test blind spot that let a real bug through and pins the whole prompt-builder surface), low-to-moderate effort (relocate four functions, extend one name list, write straightforward string assertions).
- **QA driver instruction ships a raw, un-filled-in placeholder instead of the state list** — `bug` *(checked against the code)*
  - Technical: workflows/qa.js:805 — the first concatenation segment of driverPrompt() is a DOUBLE-QUOTED string literal ("STATES SCOPE: ... (${(criterion.states || []).join(', ') || 'none — perform NO state checks'}). ..."), not a template literal. The ${...} is therefore emitted verbatim to the driver agent rather than interpolated. (The rest of the function correctly uses backtick template literals — line 806 onward — and line 814 already renders the same states list correctly via `criterion.states.length ? criterion.states.join(', ') : 'none'`.) The bug is invisible to tests/workflows/qa.test.mjs because driverPrompt lives BELOW the `// --- end helpers ---` marker (line 732) and the extract-and-eval harness only evaluates the helpers block, so no unit test exercises this string. The driver folds its report through verdictFor(report, criterion.states), which already filters to the criterion's requested states (lines 499-502), so a mis-instructed driver that reports an extra/garbled states[] entry is mostly defended downstream — but a driver that MISSES a requested state check because the scope line is unreadable yields a silent not-checked promise, which the reliability lens (explicit failure paths, no silent skip) cares about. (workflows/qa.js:805). Evidence: workflows/qa.js:805 — the first concatenation segment of driverPrompt() is a DOUBLE-QUOTED string literal (first non-space char is codepoint 34, confirmed by byte check), not a template literal: "STATES SCOPE: check ONLY the states listed for THIS criterion (${(criterion.states || []).join(', ') || 'none — perform NO state checks'}). ...". Because it is double-quoted, the ${...} is NOT interpolated. Runtime proof: evaluating the exact pattern with criterion.states=['empty','error'] yields the verbatim text "STATES SCOPE: check ONLY the states listed for THIS criterion (${(criterion.states || []).join(', ') || 'none — perform NO state checks'})." while the immediately-following backtick segment (line 806) correctly interpolates ${criterion.id}. The inconsistency vs the adjacent template literals confirms it is an unintended bug. Corroborating facts: line 814 renders the same list correctly inside a backtick literal (`${criterion.states.length ? criterion.states.join(", ") : "none"}`); driverPrompt is defined at line 792, below the `// --- end helpers ---` marker at line 732, so the helper-only extract-and-eval test harness never exercises this string; verdictFor filters reported states to requested ones at workflows/qa.js:499-502 (`requested.includes(s.state)`), so an extra/garbled states[] entry is dropped downstream but a MISSED requested check (driver cannot read the scope line) becomes a silent not-checked — the reliability/no-silent-skip concern the finding raises. Did not read any exclude-set paths..
  - Plain English: At workflows/qa.js:805 the 'STATES SCOPE' line is a double-quoted string, not a backtick template, so `${(criterion.states || []).join(', ')...}` is sent to the driver agent verbatim instead of the actual states. Confirmed: line 806 onward correctly uses backticks, and line 814 already renders the same states list properly, so this line is both broken AND a redundant duplicate. Effect today is mostly defended downstream (verdictFor at 499-502 filters to requested states), but a driver that misses a requested state check because the scope line is unreadable yields a silent not-checked — exactly the reliability failure-path the standard warns about. Fix: delete the redundant line-805 preamble (simplest, since line 814 already covers it) or fold it into the surrounding backtick template so it interpolates; add a `assert.ok(!prompt.includes('${'))` guard once the builder is testable (see prompt-builder testability item). Three separate lens findings (testing, maintainability, reliability) reported this same defect — consolidated here.
  - Impact/effort: High impact (a load-bearing agent instruction is garbled in production code, can silently mis-target state checks), low effort (delete one redundant line or convert one string to a template).

### Tier 2 — important

- **Top-level orchestration glue in each workflow has no cheap regression net** — `refactor` *(checked against the code)*
  - Technical: Each script's post-`// --- end helpers ---` region is substantial untested control flow: verify.js ~159 lines (L380–539), audit.js ~317 (L999–1316), qa.js ~444 (L732–1176), build-item.js ~388 (L459–847). This region calls the tool primitives (agent/parallel/phase/workflow), so it is genuinely not unit-testable under node — that is a legitimate architectural constraint, not a nit. The concern under testing.md 'Test pyramid' (no untested middle) and 'Failure-path coverage' is that the SEQUENCING and ASSEMBLY decisions in this region (e.g. the one-retry-then-throw judge-respawn path, the parallel→split→fold ordering, the final result-object construction) carry real branch logic that is currently proven only by live end-to-end runs (the eval fixture + dogfood). The pure helpers are the right base; the gap is that the orchestration arms have no cheap regression net, so a wiring regression (calling helpers in the wrong order, mis-threading a retry result) would not turn anything red short of a full live run. (recurs in 4 files: workflows/qa.js:732, workflows/audit.js:999, workflows/build-item.js:459, workflows/verify.js:380). Evidence: The finding is confirmed against the code. (1) Line ranges match exactly: I located `// --- end helpers ---` markers and counted post-marker lines — verify.js (total 539, marker L380, post-region 159), audit.js (total 1316, marker L999, post-region 317), qa.js (total 1176, marker L732, post-region 444), build-item.js (total 847, marker L459, post-region 388) — identical to the claimed ~159/~317/~444/~388 and L380–539 / L999–1316 / L732–1176 / L459–847. (2) The only tests of these scripts are tests/workflows/{verify,audit,qa,build-item}.test.mjs, and each one's header comment + loadHelpers() implementation shows it EXTRACTS only the `// --- helpers ---`…`// --- end helpers ---` block and evaluates it via `new Function`, exercising pure helpers standalone (e.g. verify.test.mjs L1–10, L37–74; build-item.test.mjs L1–10, L32–70). The comments state explicitly the post-helpers region "calls the tool primitives agent()/parallel()/phase()/log()…, which are undefined under node," so "the script can't be imported wholesale." (3) The post-helpers regions carry real sequencing/assembly branch logic: spawnJudge one-retry-then-throw (verify.js L384–399), the parallel→splitPanelResults→dedup→synthesis ordering and final result-object construction (verify.js L457–539), and the foldResidual loop with `await workflow(...)` child calls in build-item.js (L584–787, L668/L718 child workflow calls, L783 residual fold). (4) A search for any primitive mock/stub in tests/ found none touching agent/parallel/phase/workflow — the only stubs are in the unrelated Python check_architecture_tree suite (git/subprocess). So this orchestration glue has no cheap automated regression net and is proven only by live end-to-end runs. I tried to refute and could not..
  - Plain English: The post-helpers control-flow region of each script (verify.js ~159 lines, audit.js ~317, qa.js ~444, build-item.js ~388) calls the tool primitives, so it is legitimately not unit-testable under Node — but the sequencing/assembly decisions inside it (one-retry-then-throw respawn paths, parallel→split→fold ordering, terminal result-object construction) carry real branch logic proven only by live eval/dogfood runs. A wiring regression (helpers called in the wrong order, a mis-threaded retry result) would not turn anything red short of a full live run. Fix: where a sequencing/assembly decision is non-trivial, extract it into a pure helper inside the tested block and unit-test it (the project already does this — audit.js's applyVerdicts→verificationSummary composition test is exactly this move for the one wiring bug that shipped). For the irreducible agent() calls, document that they're covered only by the live path so the coverage claim stays honest. No mocking of primitives — keep pushing decisions down into helpers.
  - Impact/effort: Medium impact (turns silent live-only coverage into a cheap regression net for the run's decision logic), incremental effort (extract-and-test per decision, plus a one-line honesty note for the residual glue).
- **build-item's QA cross-model confirmation flag is set but never read (and the line carries a stranded comment)** — `bug` *(checked against the code)*
  - Technical: `qaConfirmedCrossModel` is declared at build-item.js:580 and assigned `true` at line 741 (when `qaResult.crossModel === 'confirmed'`), but its value is never read anywhere afterward. The run-level claim is computed solely by `crossModelClaim(builderFamily, judgeFamilies)` at line 788, and the QA branch pushes `null` into `judgeFamilies` (line 739) on EVERY path — so even a confirmed cross-model QA fold contributes a non-confirming (`null`) signal and the dead flag tracking the confirmation is never consulted. This is both an unused-variable smell (Fowler: Dead Code) and a latent correctness gap: the engine can never let a QA cross-model confirmation raise the run-level claim. A linter (`no-unused-vars` with read-tracking / ESLint `no-unused-vars`) would flag the write-only variable. (recurs in 2 files: workflows/build-item.js:580, workflows/build-item.js:741). Evidence: Repo-wide grep for `qaConfirmedCrossModel` returns exactly two hits, both in workflows/build-item.js: the declaration at line 580 (`let qaConfirmedCrossModel = false;`) and the sole assignment at line 741 (`qaConfirmedCrossModel = true;`, guarded by `qaResult.crossModel === "confirmed"`). There is no third occurrence anywhere in the codebase — the variable is written but never read (it is not imported, exported, or referenced again). This confirms the Dead Code / write-only-variable half.

Correctness-gap half confirmed: the run-level claim is computed at build-item.js:788 by `crossModelClaim(builderFamily, judgeFamilies)`. The function body (lines 389-400) reads ONLY its two parameters and never references `qaConfirmedCrossModel`. In the QA branch (lines 734-743), `judgeFamilies.push(null)` at line 739 runs unconditionally whenever `qaResult.crossModel` is truthy — including the "confirmed" case — and the `=== "confirmed"` check at line 740 only flips the dead flag. Inside `crossModelClaim`, line 390 filters out `null`/empty entries, so the QA path's `null` contributes nothing toward a `"confirmed"` result. Therefore a genuine QA cross-model confirmation can never raise the run-level claim, and the flag that records it is never consulted. Both cited locations (580, 741) and the technical specifics (null push at 739, claim at 788) are exact..
  - Plain English: Confirmed: qaConfirmedCrossModel is declared at build-item.js:580 and assigned true at 741, but never read anywhere — crossModelClaim at 788 computes the run-level claim solely from judgeFamilies, into which the QA branch pushes null on every path (739). So a genuine confirmed cross-model QA fold can never raise the run-level 'independently re-checked' claim — it is silently discarded, while the flag that records it is dead. Decide the intended semantics and make them representable: either remove the flag entirely if QA is deliberately never allowed to raise the run-level claim (and say so in the comment at 739), or have crossModelClaim actually consume the confirmation. Line 580 also has two `//` comment fragments mashed together, the second describing a different declaration (judgeFamilies) and stranded here — remove it as part of this fix (the separate doubled-comment finding resolves with this one).
  - Impact/effort: Medium impact (a latent correctness gap in the cross-model honesty claim plus a no-unused-vars smell a linter would catch), low effort (one decision then delete-or-thread, clean up the comment on the same line).
- **Per-agent/child-workflow calls have no explicit duration bound (only the readiness probe does)** — `refactor` *(checked against the code)*
  - Technical: Across workflows/qa.js, workflows/audit.js, workflows/verify.js, workflows/build-item.js every await agent(...) / await workflow(...) relies on the platform to eventually resolve or reject; none passes a timeout/deadline option, and there is no per-call wall-clock guard (the scripts deliberately have no clock — only the boot agent's readinessPlan is bounded, qa.js:234-243). The retry contract (spawnVerifier/spawnJudge: one respawn then deferred/throw — qa.js:862-892, verify.js:384-399, audit.js:1005-1036) bounds COUNT but not DURATION. parallel() fan-outs (audit.js:1136, qa.js:1060, verify.js:457) are gated on the slowest thunk; one hung agent blocks the whole barrier. This is right-sized at JUDGMENT, not a clear gap: the orchestrating platform likely enforces a session/agent timeout out-of-band, and the budget primitive (qa budget.agents:40, build budget.agents:60) caps call COUNT. But the timeouts dimension's bar (every blocking call has an explicit bound) is only partially met — the bound is the platform's, not the script's, and that is not stated. (recurs in 8 files: workflows/qa.js:984, workflows/qa.js:1024, workflows/qa.js:1060, workflows/audit.js:1136, workflows/audit.js:1276, workflows/verify.js:457, workflows/build-item.js:668, workflows/build-item.js:718). Evidence: Confirmed against the code. Every cited agent/workflow call passes only agentType/schema/label/phase/model — never a timeout/deadline/abortSignal. Concrete option objects: qa.js:984-989 (boot), :1024-1029 (driver), spawnVerifier qa.js:871-877 & :881-886; audit.js guardedAgent :1104-1111 wrapping agent at :1106, synthesis :1186-1189, sentinel :1237-1241, spawnVerifier :1014-1029; verify.js lens :421-427, yagni :433-437, spawnJudge :392 & :395; build-item.js gates :649-654, verify child workflow :668-677, qa child workflow :718-727. A repo-wide grep for timeout|deadline|abortSignal|AbortController|setTimeout|maxDuration|wall-clock across workflows/ returns ONLY the readiness-probe timeoutSec (readinessPlan, qa.js:234-243) and prose echoes — that is the sole clock bound, exactly as claimed. The try/catch wrappers (qa.js:990 boot, :1030 driver; spawnVerifier qa.js:864-869 / audit.js:1007-1013; guardedAgent audit.js:1104-1110; spawnJudge verify.js:385-391) only convert a thrown rejection to null; they do not bound a call that never settles. Retry contracts (spawnVerifier qa.js:862 / audit.js:1005; spawnJudge verify.js:384) bound respawn COUNT (one retry, fired only AFTER a prior attempt resolves/rejects), not the DURATION of a single hang. parallel() barriers (audit.js:1136/:1276, qa.js:1060, verify.js:457) await the full task array — one never-settling task blocks the barrier with no per-task time cap. All four files declare themselves clock-free in their headers ("NO wall-clock ... orchestrator stamps times AFTER the run"), so a script-level wall-clock guard cannot live here. I searched for the missing per-call timeout/deadline guard in these files and any shared helper they call and did not find it. The finding's own JUDGMENT framing (platform likely enforces a session/agent timeout out-of-band; budget caps COUNT not DURATION) is accurate; the narrow claim is true against the code..
  - Plain English: Across all four workflows every `await agent(...)`/`await workflow(...)` relies on the platform to eventually resolve; only the boot agent's readiness probe is clock-bounded (qa.js:234-243), and the retry contracts bound COUNT (one respawn) not DURATION. A hung reviewer/driver/implementer can stall the whole run; parallel() barriers are gated on the slowest thunk, so one hung agent blocks the fan-out. This is right-sized at judgment, not a clear defect — the platform likely enforces a session/agent timeout out-of-band and the budget primitive caps call count — but the resilience bar (every blocking call has an explicit bound) is only partially met, and the bound being the platform's rather than the script's is not stated. Fix: pass an explicit per-call timeout where the primitive supports one, OR add a one-line note at each script's control flow that per-agent duration is bounded by the platform (script bounds only count + the readiness probe); if no timeout knob exists, record the residual hang risk in DECISIONS.
  - Impact/effort: Medium impact (keeps the resilience claim honest and guards against an indefinite stall), low effort (a documented assumption or a per-call option where available).
- **DoD deterministic-gate list omits the node --test command that runs the workflow-script tests** — `refactor` *(checked against the code)*
  - Technical: docs/WORKFLOW.md -> Definition of Done lists the deterministic gates as `python -m pytest`, `python scripts/check_architecture_tree.py`, and `python scripts/check_versions_synced.py` (WORKFLOW.md:113-116), but omits `node --test tests/workflows/*.test.mjs` — the sole run path for the pure-helper unit tests of all four `workflows/*.js` scripts (tests/workflows/{verify,audit,qa,build-item,cross-script}.test.mjs). That command exists only in `.github/workflows/ci.yml:49` and a DECISIONS.md bullet (2026-06-11). For a repo that dogfoods its own DoD, the local runbook for the in-scope `workflows/` code is therefore incomplete: a contributor executing the documented DoD gate suite never exercises the workflow-script tests. Onboarding/runbook dimension: a new operational/verification procedure (the node test run) lacks a runbook reference in the canonical DoD doc. (recurs in 4 files: docs/WORKFLOW.md:113, docs/WORKFLOW.md:114, docs/WORKFLOW.md:116, .github/workflows/ci.yml:49). Evidence: docs/WORKFLOW.md "Definition of Done" → "Deterministic gates" lists exactly three run commands: `python -m pytest` (WORKFLOW.md:113), `python scripts/check_architecture_tree.py` (WORKFLOW.md:114), `python scripts/check_versions_synced.py` (WORKFLOW.md:116). A repo-scoped grep for `node --test` / `.test.mjs` / `tests/workflows` returns ZERO matches in WORKFLOW.md — the node command is absent from the entire DoD doc.

The node command is the SOLE run path for the workflow-script tests, and it is not subsumed by the listed `python -m pytest` gate:
- 5 test files exist for all four scripts: tests/workflows/{verify,audit,qa,build-item,cross-script}.test.mjs (Glob confirmed); the scripts under test are workflows/{verify,audit,qa,build-item}.js (Glob confirmed).
- tests/workflows/verify.test.mjs:10 states these run via `node --test tests/workflows/` and the CI node-tests job — they are node:test ESM files, not pytest-collectable.
- pyproject.toml [tool.pytest.ini_options] sets testpaths=["tests"] but pytest collects only test_*.py; tests/conftest.py merely adds scripts/ to sys.path and does not invoke node. The two Python tests mentioning "workflows" (test_check_architecture_tree.py, test_product_spec_template.py) reference the directory incidentally (tree/template scanning), not as executors of the .mjs tests. So `python -m pytest` genuinely never runs the workflow-script tests.

The command lives only outside the hand-followed runbook:
- .github/workflows/ci.yml:49 — `run: node --test tests/workflows/*.test.mjs` (machine-run in the node-tests job).
- docs/DECISIONS.md:14 — 2026-06-11 bullet documenting the explicit-glob form.
- The one docs/ROADMAP.md hit (line 41) is a finding's fix-suggestion naming tests/workflows/audit.test.mjs, NOT a documented DoD run command.
- No package.json / Makefile / justfile / tox alias exists at the repo root to provide an alternate documented local run path (Glob confirmed: only pyproject.toml).

I searched for the missing guard (a DoD runbook reference to the node test run) in the canonical DoD doc, in any task-runner alias, and in any pytest bridge — none exists. The guard is genuinely absent, so a contributor executing the documented DoD gate suite by hand never exercises the four workflow scripts' tests; a break there surfaces only later in CI..
  - Plain English: Confirmed: docs/WORKFLOW.md Definition of Done lists pytest, check_architecture_tree.py, check_versions_synced.py, and lint/type/security (lines 113-117) but omits `node --test tests/workflows/*.test.mjs` — the sole run path for the pure-helper unit tests of all four workflows/*.js scripts. That command exists only in .github/workflows/ci.yml:49 and a DECISIONS bullet. For a repo that dogfoods its own DoD, a contributor running the documented gates locally before landing silently skips the entire workflow-script suite, so a break there is caught only later by CI. Fix: add a fourth deterministic-gate line to WORKFLOW.md mirroring ci.yml:49 — the explicit file glob is required (the bare-directory form is broken on Windows Node 22 per the existing CI comment/DECISIONS).
  - Impact/effort: Medium impact (closes a real local-vs-CI gate gap in the canonical runbook for in-scope code), trivial effort (one documentation line).

### Tier 3 — polish

- **Make the operator-config trust assumption for executed commands/URLs explicit at the args boundary** — `refactor` *(checked against the code)*
  - Technical: qa.js threads runConfig.runCommand / runConfig.teardownCommand / runConfig.appUrl into bootPrompt() (workflows/qa.js:748-761), teardownPrompt() (766-784) and driverPrompt() (792-832) which instruct a Bash-capable agent to execute them; build-item.js does the same with repo.gateCommands / repo.runApp via gatesPrompt() (501-511) and the qa child-call (718-727). Boundary validation (parseRunArgs workflows/qa.js:170-219; validateArgs workflows/build-item.js:123-200) checks only SHAPE (non-empty string / array / frozen-criteria schema) — never content; the commands reach a shell unescaped. appUrl is curl-probed with no scheme/host allowlist or private-range guard. Per the security module's 'Injection prevention' and 'SSRF' dimensions the inputs cross a trust boundary, but the boundary is operator-config (CLAUDE.md detected-tooling, orchestrator-passed args) not untrusted user input, so no allowlist/escaping control is warranted at the current threat model — flagging only to keep the boundary classification explicit. (recurs in 5 files: workflows/qa.js:748, workflows/qa.js:766, workflows/qa.js:792, workflows/build-item.js:501, workflows/build-item.js:718). Evidence: Every technical assertion in the finding matches the code; I could not find the absent guard it would take to refute it.

1) Raw command/URL interpolation into Bash-agent prompts:
- workflows/qa.js:750-751 — bootPrompt interpolates `${runConfig.runCommand}` and `${runConfig.appUrl}` verbatim, then (753-755) tells the agent to curl-probe the URL.
- workflows/qa.js:769,780 — teardownPrompt interpolates `${plan.command}` (from teardownCommand) and `${runConfig.appUrl}`.
- workflows/qa.js:807 — driverPrompt interpolates `${runConfig.appUrl}` and instructs curl/Bash use.
- workflows/build-item.js:506 — gatesPrompt interpolates each `repo.gateCommands` string verbatim and tells the agent to run them.
- workflows/build-item.js:722-723 — qa child-call passes `repo.runApp` as runCommand and `item.appUrl || repo.appUrl` as appUrl.

2) Boundary validation is SHAPE-only, never content:
- parseRunArgs (workflows/qa.js:170-219) checks runCommand/appUrl/teardownCommand only for non-empty-string and .trim()s them (175-188, 206-211); no escaping, no URL scheme/host/range check.
- validateArgs (workflows/build-item.js:123-200) checks gateCommands is a non-empty array of non-empty strings (172-176) and runApp is string-or-null (181-183); no content check.

3) No allowlist / escaping / SSRF guard anywhere:
- Grep over both files found no `new URL`, no scheme/host allowlist, no private-range/127.0.0.1/localhost guard, no shell-escape, no child_process/execFile/spawn. The only sanitize/escape code (sanitizeForPath, qa.js:634-649) is for filesystem screenshot paths (`..` parent-dir escape), not for the commands or the URL.

4) Boundary is operator-config, not untrusted user input — consistent with the code: qa.js header (1-19) calls these "the recorded run command"; the script itself "has NO Bash and NO clock" (line 19), so it only threads operator/orchestrator-supplied strings (repo.runApp, repo.gateCommands, appUrl) into agent prompts.

The finding is explicitly a non-defect classification note ("no allowlist/escaping control is warranted at the current threat model"), and that description is an accurate reading of the code. There is no guard to point to that would disprove it..
  - Plain English: qa.js threads runCommand/teardownCommand/appUrl into agent prompts that a Bash-capable agent executes verbatim (748/766/792), and build-item.js does the same with gateCommands/runApp (501/718); boundary validation checks only shape, never content, and appUrl is curl-probed with no scheme/host guard. This is correct at the current threat model — the values are operator-recorded config (CLAUDE.md detected-tooling, orchestrator-passed args), not end-user/attacker input — so no allowlist/escaping control is warranted now. The value is keeping the boundary classification explicit: document the 'config is operator-trusted' assumption at the args boundary so a future change that sources these from a less-trusted origin (multi-tenant orchestrator, untrusted-authored spec) is forced to add a content-level guard rather than silently widen the surface.
  - Impact/effort: Low impact (no present defect; a guard against silent future trust-boundary creep), trivial effort (a documented assumption at the args boundary).
- **The extract-and-eval loadHelpers() harness is copy-pasted across all four workflow test files** — `refactor` *(checked against the code)*
  - Technical: `loadHelpers()` — read file, regex-match the line-anchored `// --- helpers ---` / `// --- end helpers ---` markers, slice the block, and `new Function(\`${block}\n; return { ${names.join(', ')} };\`)` — is duplicated in verify.test.mjs:37, audit.test.mjs:81, qa.test.mjs:40, and build-item.test.mjs:32, with only the per-script `names` list and `SCRIPT_PATH` differing. This is DRY duplication of test infrastructure (Fowler: Duplicate Code). Unlike the cross-script trust-surface helpers — whose copying is forced by the no-imports SANDBOX and is gate-pinned by cross-script.test.mjs — the test files run under Node and CAN import, so the duplication is avoidable. (recurs in 4 files: tests/workflows/verify.test.mjs:37, tests/workflows/audit.test.mjs:81, tests/workflows/qa.test.mjs:40, tests/workflows/build-item.test.mjs:32). Evidence: The near-identical loadHelpers() extraction routine is independently defined in all four cited files: tests/workflows/verify.test.mjs:37, tests/workflows/audit.test.mjs:81, tests/workflows/qa.test.mjs:40, tests/workflows/build-item.test.mjs:32. Each body is the same sequence: readFileSync(SCRIPT_PATH,"utf8"); src.match(/^\/\/ --- helpers ---$/m) and /^\/\/ --- end helpers ---$/m; two assert.ok marker checks (differing only by the embedded script filename in the message); assert.ok(end>start,...); src.slice(start,end); a per-script names[] array; and new Function(`${block}\n; return { ${names.join(', ')} };`). Only SCRIPT_PATH and names differ. No shared module factors this out: Glob tests/**/*helper* returns no files, and Grep finds no `import ... from "...helpers"` / shared extractor anywhere in tests/workflows — every loadHelpers is local (function loadHelpers() at the four cited lines only). The marker convention (^// --- helpers ---$ / ^// --- end helpers ---$) is hardcoded in all four bodies, so a marker-convention change must be edited in four places — exactly the drift the finding describes. The fifth test (cross-script.test.mjs) uses a different regex-extraction pattern for a different purpose and is correctly NOT among the four. The finding's distinguishing point also holds: the sandbox-forced helper copying inside the .js scripts is gate-pinned by cross-script.test.mjs, but these .test.mjs files run under Node and already use import, so the loadHelpers duplication is avoidable test infrastructure and is not itself covered by any drift gate..
  - Plain English: Confirmed: an identical marker-slice-and-eval loadHelpers() (read file, regex-match the line-anchored helpers markers, slice, new Function(...)) is duplicated in verify/audit/qa/build-item .test.mjs, differing only by SCRIPT_PATH and the per-script names list. Unlike the cross-script trust-surface helpers (whose copying is forced by the no-imports sandbox and gate-pinned), the test files run under Node and CAN import, so this DRY violation is avoidable — a change to the extraction convention must be edited in four places and they can drift. Fix: extract into a shared tests/workflows/_helpers-harness.mjs exporting loadHelpers(scriptPath, names) and import it in all four, leaving only SCRIPT_PATH + names per call site.
  - Impact/effort: Low impact (single source of truth for the test-extraction convention; prevents drift), low effort (one shared module, four small edits).
- **Document that nothing is acquired before the QA boot try/finally, so pre-try exits need no teardown** — `refactor` *(checked against the code)*
  - Technical: workflows/qa.js: the teardown-always guarantee lives in the finally at lines 1081-1106, which wraps only the boot+drive region (try opens at line 979). The earlier exits — otherErrors throw (line 905-907), validateCriteria throw (line 914-916), and the runInputErrors early return (line 922-941) — all occur before the try, so teardownPlan never runs for them. This is CORRECT now (no app is started until the boot agent at line 984, inside the try), so there is genuinely nothing to tear down on those paths. Flagging it as a latent fragility, not a live leak: if any future pre-boot step acquires a resource (e.g. a pre-flight port grab, a temp artifact dir created before validation), it would escape the finally. The reliability lens prefers the acquire-and-guard to be structurally co-located. (recurs in 4 files: workflows/qa.js:905, workflows/qa.js:914, workflows/qa.js:922, workflows/qa.js:979). Evidence: All structural facts in the finding check out against workflows/qa.js. The try opens at line 979 and the teardown finally at lines 1081-1106; the first agent that starts anything (the boot agent) is at line 984, inside the try. The three early exits all occur BEFORE the try: otherErrors throw at lines 905-907, validateCriteria throw at lines 914-916, and the runInputErrors early return at lines 922-941. Everything executed before the try is pure in-memory computation — parseArgs (151-160), parseRunArgs (170-219), validateCriteria (415-460), criterionPlan (465-471), readinessPlan (234-243) — none touch the filesystem, spawn a process, or bind a port. A grep across the whole file for mkdir|writeFile|fs.|spawn|exec|createServer|.listen(|net.|child_process|open(|require( returned ZERO code matches (only comments), and the artifactDir at line 947 is assigned as a plain string, never mkdir'd. Line 19's contract comment confirms it: the script 'has NO Bash and NO clock — all file/process/clock work happens inside the agents it spawns.' So the finding is accurate: the early-exit paths genuinely have nothing to tear down today (correct now), and the finding correctly frames this as a latent fragility — acquire-and-guard are not structurally co-located, so a future pre-boot resource acquisition would escape the finally. I could not find any code disproving the assertion (the finally does not cover those exits, and no pre-boot resource is acquired), so the judgment-level claim stands as stated..
  - Plain English: qa.js's teardown-always guarantee lives in the finally (1081-1106) wrapping only the boot+drive region (try opens at 979); the earlier exits (otherErrors throw 905, validateCriteria throw 914, runInputErrors early return 922) occur before the try. This is correct today — no app is started until the boot agent at 984, inside the try, so there is nothing to tear down on those paths. It is a latent fragility, not a live leak: a future pre-boot step that acquires a resource (a pre-flight port grab, a temp artifact dir) would escape the finally. Fix: add a one-line invariant comment at the try ('nothing is acquired before this point, so the pre-try throws/returns need no teardown') so a future edit acquiring a resource pre-boot is forced to confront the finally boundary.
  - Impact/effort: Low impact (no present defect; a guard against a future resource-leak regression), trivial effort (one invariant comment).

**Recommended starting point:** QA's four pure prompt-builders sit outside the tested block, so none are covered.

Re-checked every finding I surfaced against the code (the cross-model judge — by default a different model family than the builder); dropped 1 that couldn't be confirmed — verified 9 · unconfirmed 0 · deferred 0.

*To start anything — a backlog item or a brand-new project — just tell the agent in plain English what you want (e.g. 'Let's do Tier-1 item 1' or 'I want to build X'). It will ask you questions (Discuss), then write a plan and spec for you to approve before any code. For a backlog item, the go-button is **`/claugentic-dev-harness:build`** — point it at one item ('build Tier-1 item 1') and it drives the whole reviewed pipeline for you, pausing only at the spec (before any code) and before anything irreversible.*
<!-- harness-audit:backlog:end -->

## Now

- **Harness v2 — re-platform the choreography (plan `0012`).** The audit pipeline, Verify panel, and build loop become executable Workflow scripts (invoked from the plugin install path); deterministic trust-gates (CI, land-gate, secret-scan, characterization-first) unlock an earned **build-to-green** autonomy ladder; a runtime-verification (QA) workflow drives the real app against acceptance criteria; a product layer (spec rebuild + product-gap audit) answers "is this product what it intended to be." Then: the **AskBase pilot** (spec rebuild with the user → gap audit → build-to-green to release-ready) before rolling across the neeshinc suite. (Decisions: `DECISIONS.md` → Harness v2.)
- **Standards-catalog integrity pass** (from the 2026-06-11 multi-lens review, all verified): bring the four `migrated` modules to the deep-module bar; make dimension-level Confidence labels match per-check tags (security.md has six "deterministic" dimensions containing `[J]` checks); replace the 14 dangling `ENGINEERING_STANDARDS.md §` sources; fix the verifiable citation errors (WCAG 2.4.13 is AAA not AA; AWS REL-9/idempotency; SRE-book ch. 5).
- **README first-screen rewrite + one real artifact** (verified DX findings): short plain sentences for the three commands, hedges stated once in Status, an embedded sample backlog excerpt + init report; document uninstall, update, and add a CHANGELOG.

## Later

- **audit.js housekeeping (from the Slice-4a Verify panel, out of that diff's scope):** remove the orphaned `cellKey` comment stranded at its old location; drop `renderItem`'s unconditional trailing period; consider unifying the LEGEND/VERIFICATION_PHRASE two-copy trust strings (both test-pinned today, so drift is caught); reconcile `dedupFindings`' opposite-direction confidence merge between `verify.js` (upgrades to deterministic) and `audit.js` (weakest wins) — decide one semantic or document why they differ.

- **Bound the tree-gate git subprocess with `timeout=`** and map `TimeoutExpired` onto the existing `RuntimeError` boundary (Slice-2 Verify lens note; local read-only git hang is unlikely — low priority).
- **Pin the workflow-script output schemas to their agent `.md` contracts.** The schemas' required field-sets are test-pinned in-script (Slice 2), but nothing links them to the agent files' prose contracts — a doc-side contract change can still drift silently. Decide one home or add a cross-pin when a second script consumes the same contracts.

- **Silence the pre-existing `pytest-asyncio` deprecation warning** (set `asyncio_default_fixture_loop_scope` in `pyproject.toml`) — surfaced during Slice 1; unrelated to any gate.
- **CI installs only `pytest`** — if the test suite ever gains a third-party dependency, add a requirements file + install step to `.github/workflows/ci.yml` (today's suite is pure-stdlib; noted by the Slice-1 re-verify).

- **Reconcile the `settings.json` tree-check hook *command string* on format change.** Today `init` keys hook-presence only on the `check_architecture_tree.py` substring (idempotency key, step 5b), so a managed copy whose hook *command format* drifts between plugin versions (flags, interpreter convention, quoting) is detected as "already present" and never updated. When a hook-command format change ships, `init` should reconcile the managed hook entry — without clobbering a user-customized command — rather than leaving the old form in place. (Deferred from plan 0010 — out of scope for the version-aware init slice.)
- **Make the managed `check_architecture_tree.py` layout comment adopter-neutral.** The script carries a hardcoded comment — `scripts/check_architecture_tree.py:56` "For THIS repo (claugentic-dev-harness) the only code is the check script itself." — which is **wrong once the file is copied into an adopter repo** (the adopter's code is *not* just the check script). Consider rewording that comment to be adopter-neutral (the `INCLUDE_GLOBS` carve-out already protects the per-repo *knob*, but the prose comment is a managed verbatim line that misleads in every adopter). (Surfaced by the real AskBase refresh.)
- **Warn before a REFRESH when the git-recovery net is empty.** A REFRESH overwrites a genuine managed copy and relies on **git history** as the recovery net (the user's prior version is recoverable from a commit). That net is **empty** when the overwritten file was **uncommitted/user-edited** (the working-tree version isn't in history) or the repo has **no commits at all**. Before such a REFRESH, `init` should **warn and suggest committing first** so the prior content is recoverable. (Deferred from plan 0010 Stage-7 verify — out of scope for the version-aware init slice.)
