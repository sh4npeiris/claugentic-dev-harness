# Roadmap

The forward backlog for the harness itself. Work runs through `docs/WORKFLOW.md` (triage → plan → review → spec → approve → implement → verify → land → retrospect), sliced so each unit lands complete with no tech debt. Tangents land here, never silently into the current change.

> The harness audits *adopter* repos via `/claugentic-dev-harness:audit`, which writes its own `harness-audit:overview` / `harness-audit:backlog` fences into that repo's `docs/ROADMAP.md`. This file is the product's own roadmap, not a generated audit — an adopter generates theirs by running the skill.

---

## The two standing tracks — growing the libraries (ongoing · just-in-time)

The harness grows as it meets more codebases. Neither track is ever "done" — that's the right shape: additions are made when real use proves a gap, never speculatively.

| Track | How it grows |
|------|--------------|
| **Grow the role library** (`.claude/agents/`) — add specialist agents as real use surfaces gaps the starter set lacks. | The workflow delegates to specialists; when real work keeps hitting a job no existing role owns, that's the signal to add one (and register it in `plugin.json`, the WORKFLOW roster, and the architecture tree). |
| **Grow the standards catalog** (`docs/standards/`) — author new quality modules and capability modules (Redis, queues, object-storage, …) as real projects pull them in. | A real audit or review that needs a bar the catalog doesn't carry is the signal to author it (conforming to `_TEMPLATE.md`, indexed in the catalog README). The catalog modernizes vibe-coded apps, not just cleans code. |

---

## Backlog — the work worth doing  _(generated by /claugentic-dev-harness:audit · do not edit — re-run to refresh)_

<!-- harness-audit:backlog:start -->
status: COMPLETE · level: thorough · done-cells: [maintainability-structure×scripts, maintainability-structure×workflows, testing×scripts, testing×workflows, reliability-resilience×scripts, reliability-resilience×workflows, blindspot×(scope)] · pending-cells: [] · date: 2026-06-11

`refactor` = tidy without changing behavior · `capability-upgrade` = add/upgrade a technology · `dependency-health` = update/patch dependencies · `bug` = fix wrong behavior · `feature` = new behavior.
`(checked against the code)` = a separate agent re-read the code and couldn't refute it · `(could not confirm independently — model's assertion)` = still just the model's claim · `(⚠ not yet verified — re-run to confirm)` = budget ran out before checking — a re-check by a different model family than the builder (the cross-model judge; on a same-family run, tagged as such) — a reduction of shared-blind-spot risk, not a mechanical guarantee.

### Tier 1 — critical

- **A single reviewer throwing crashes the whole audit instead of degrading to PARTIAL** — `bug` *(checked against the code)*
  - Technical: In workflows/audit.js the FIND phase fan-out thunks call agent(...) directly with no try/catch (findTasks, lines 857-864; the blind-spot thunk, lines 866-873). They are passed to `await parallel(findTasks)` (line 875). The graceful-degradation logic that follows (lines 879-909: failedCells / PARTIAL / 'its cells go pending') only handles the case where a task RETURNS a falsy/non-array result (`if (!r || !Array.isArray(r.findings))`). It does NOT handle a task that REJECTS. If agent() throws for one batch (the comment at line 819 confirms agent() can throw, not only return null), the unhandled rejection propagates through parallel() and aborts the whole run with no PARTIAL result and no resume cells — the opposite of the documented 'never a silent skip; the run goes PARTIAL' contract. The judge fan-outs are protected (spawnVerifier wraps agent() in try/catch, lines 792-798; spawnVerifier in verify.js likewise), so the gap is specifically the non-judge FIND-phase lens/blindspot tasks. (recurs in 8 files: workflows/audit.js:857, workflows/audit.js:866, workflows/audit.js:875, workflows/audit.js:879, workflows/verify.js:390, workflows/verify.js:402, workflows/verify.js:427, workflows/verify.js:451). Evidence: The asymmetry the finding names is present in the code. FIND-phase fan-out thunks call agent() with NO try/catch: workflows/audit.js:859-866 (findTasks lens thunks → bare `agent(buildLensPrompt(...), {...})` at line 860) and workflows/audit.js:867-876 (blind-spot thunk → bare `agent(buildBlindspotPrompt(...), {...})` at line 869). They are handed to `await parallel(findTasks)` at line 877. The degradation logic that follows only inspects RETURN values, never a rejection: line 885 `if (!r || !Array.isArray(r.findings))` (lens batches → cells go pending) and line 903 `if (!blindspotResult || !Array.isArray(blindspotResult.findings))` (blind-spot pseudo-cell goes pending). Neither branch can fire if the thunk REJECTED rather than returned — `findResults[i]` would never be assigned because `parallel()` would reject first.

The contrast that confirms intent: the judge path DOES guard against a throwing agent(). spawnVerifier wraps it in try/catch at workflows/audit.js:794-800 (`try { return { out: await agent(prompt, opts) }; } catch (e) { return { out: null, err: ... } }`), and the comment at lines 818-822 states "Both attempts failed — mark the finding deferred," explicitly treating a thrown agent() as an anticipated failure mode. The same protected pattern is mirrored in verify.js (spawnJudge wrapping agent, used for the panel/synthesis judge fan-out, panelResults at verify.js:427). A grep for try/catch across audit.js returns only lines 522-526 (parseArgs) and 795-797 (spawnVerifier) — the FIND thunks are unprotected.

The design intent is documented and contradicts the implementation: .claude/plans/0012-harness-v2-executable-choreography.md:214 specifies for FIND that "a batch that errors after one retry sends its cells to pendingCells and logs it (run goes PARTIAL — never a silent skip)." The shipped FIND code has neither a retry nor a try/catch — so a throwing agent() for one batch is not converted into a pending cell.

Residual caveat (why not 100% airtight): parallel() is a sandbox tool primitive, not defined in this repo, so its exact rejection semantics are not pinned by any doc/test I can read. If parallel() were internally allSettled-style and returned a falsy slot for a rejected task, line 885/903's `if (!r ...)` would catch it and the run would degrade gracefully — refuting the finding. But nothing in the repo (plan, skill, docs, tests) specifies such isolation, and the authors' deliberate choice to try/catch agent() inside spawnVerifier — while leaving the FIND thunks bare — is strong internal evidence that an unwrapped rejection is NOT isolated by parallel(). On the weight of the code, the FIND-phase guard the finding says is missing is genuinely absent..
  - Plain English: The FIND-phase fan-out thunks (the lens and blind-spot tasks) call the agent primitive with no try/catch, unlike the verifier path which wraps it. The graceful-degradation code that follows only handles a task that RETURNS nothing — not one that REJECTS. So if one reviewer hits a hard error, the unhandled rejection propagates through parallel() and aborts the entire run with no PARTIAL result and no resume cells, the exact opposite of the documented 'never a silent skip; the run goes PARTIAL' contract. Fix: wrap each FIND-phase thunk's agent call in try/catch that converts a throw into the same null-return the degradation path already handles, so the cells route to pending and the run completes PARTIAL.
  - Impact/effort: high-impact / low-effort
- **Resumed audit runs can silently drop a deferred finding from the backlog** — `bug` *(checked against the code)*
  - Technical: deferredFindings (re-passed on a resume run) are concatenated onto `toVerify` AFTER applySynthesisItems has run (audit.js:990), so they never receive tier/tag/title annotation. toResultItem reads `tier: finding.tier` and `tag: finding.tag` with NO default (audit.js:652-653) — unlike every adjacent field which null-guards to "". renderBacklogFence then filters items into exactly tiers via strict equality `items.filter(it => it.tier === 1|2|3)` (audit.js:769-771), so any kept item whose tier is undefined (or a non-integer parsed from prose) matches no tier section and is omitted with no trace. Meanwhile verificationSummary still counts it (it's in `kept`), so renderRunReport's verified/unconfirmed/deferred counts and the recommended-starting-point logic can disagree with the items actually rendered. validateArgs only checks deferredFindings is an array (audit.js:133-135) with zero element-shape validation, so a deferred finding lacking `tier` passes the boundary that every other structured input is shape-checked at. The SKILL resume contract (skills/audit/SKILL.md:217-219, 331-335) describes deferredFindings only in VERIFY terms and the prior fence persists tier only as a section heading (renderItem emits no tier/findingKey/confidence machine field), so the orchestrator cannot reliably reconstruct a fully-annotated deferred finding. (recurs in 5 files: workflows/audit.js:990, workflows/audit.js:652, workflows/audit.js:769, workflows/audit.js:133, skills/audit/SKILL.md:217). Evidence: The full mechanical chain is present in workflows/audit.js and is NOT guarded anywhere:

- audit.js:934 — `applySynthesisItems(dedupedFindings, synthesis.items)` defaults a missing `tier` to 2, but it runs ONLY on the freshly-surveyed `dedupedFindings`.
- audit.js:840 + 992 — `deferredFindings` are taken straight from input behind only an `Array.isArray` guard (no per-element normalization), then `const toVerify = [...survivors, ...deferredFindings]` concatenates them AFTER synthesis annotation. Deferred findings therefore never receive a defaulted tier.
- audit.js:442-477 (applyVerdicts) — spreads `...finding` and adds only `verification`/`verifierRunningAs`; injects no tier.
- audit.js:654 (toResultItem) — `tier: finding.tier,` has NO null-guard, unlike every adjacent field (titlePlain/claimTechnical/locations/whyPlain/impactEffort/confidence all default, lines 656-661). So a tier-less finding yields `tier: undefined`.
- audit.js:771-773 (renderBacklogFence) — `items.filter(it => it.tier === 1)` / `=== 2` / `=== 3` (strict equality). An item with `tier === undefined` (or a non-integer string) matches no tier section and is omitted from the rendered fence with no trace.
- audit.js:491-495 (verificationSummary) — iterates ALL `kept` findings regardless of tier, so the omitted item IS still counted in verified/unconfirmed/deferred, and renderRunReport (750-763) reports that count. Result: report count can exceed items actually rendered.
- audit.js:138-139 (validateArgs) — only `Array.isArray(args.deferredFindings)`; zero element-shape validation, so a deferred finding lacking `tier` passes the boundary that every other structured input is shape-checked at.
- audit.js:707-721 (renderItem) — emits no machine-readable `tier`/`findingKey`/`confidence` field per item; tier survives only as the section heading (renderTier, 726-730). Combined with SKILL.md:217-219 describing `deferredFindings` only in VERIFY terms, the orchestrator has no reliable per-item tier to reconstruct on resume.

No test exercises the tier-less path: tests/workflows/audit.test.mjs:595 only passes findings that already carry a tier; deferredFindings tests (155, 248-258) only check the array guard. The gap is unguarded by code and untested..
  - Plain English: On a resume run, a finding that was 'not yet verified' last time is concatenated onto the verify list AFTER the step that assigns its tier/tag/title, so it reaches the renderer with no tier. The renderer keeps only items whose tier is exactly 1, 2, or 3, so the deferred finding is omitted with no trace — yet the run report still counts it as verified. The user sees a report claiming N findings checked while fewer than N appear in the backlog, and a previously-flagged issue can vanish. Fix: route deferredFindings through the annotation step (or give toResultItem a conservative tier default and have the renderer place untyped items in a fallback tier instead of dropping them), add element-shape validation for deferredFindings at the args boundary, and add a deferred-finding round-trip test.
  - Impact/effort: high-impact / low-effort

### Tier 2 — important

- **An unknown model family silently degrades to 'same-model' with no signal** — `refactor` *(checked against the code)*
  - Technical: modelFamily (verify.js:94-100, audit.js:412-418) regex-matches one of (fable|opus|sonnet|haiku) from a free-form self-report and returns null otherwise; null conservatively degrades to the same-model tag. This is a deliberate fail-safe (documented), but it means an unrecognized-but-legitimate family (a new model) is indistinguishable from garbage — both silently read as same-model with no log/warning, so the user is told 'same-model' when the truth is 'unknown family'. The standard's 'make invalid states unrepresentable / fail loudly' would prefer a third explicit state (resolved-different | resolved-same | unresolved) surfaced to the caller rather than collapsing unresolved into same. The set of known families is also a magic literal embedded in the regex in two files (couples to the DRY finding). (recurs in 3 files: workflows/verify.js:94-100, workflows/audit.js:412-418, workflows/verify.js:105-112). Evidence: Every checkable assertion in the finding holds against the code. (1) Free-form regex, not a closed set: modelFamily does `report.match(/(fable|opus|sonnet|haiku)/i)` returning the token or null — verify.js:94-100 and audit.js:414-420 (identical; audit.js:411 comments "copied verbatim from verify.js"). (2) null degrades to same-model: sameModelTag does `if (b === null || j === null) return SAME_MODEL_TAG;` — verify.js:108-109, audit.js:425-426. (3) No log/warning anywhere: grep for console.log/warn/error|logger|warn( across all of workflows/ returned ZERO matches, so an unrecognized-but-legitimate new family is indistinguishable from garbage — both silently read as same-model. (4) No third explicit state: grep for unresolved|unknown family|resolved-different|resolved-same returned ZERO matches; the resolution is binary (SAME_MODEL_TAG or null). The user-facing tag (verify.js:33-34: "the judge and the builder are the same model family here") is asserted as fact even when a family was merely unresolvable — the exact trust-claim misstatement described. (5) Duplicated magic literal: the same /(fable|opus|sonnet|haiku)/i appears in both verify.js:98 and audit.js:418. No guard, log, or third state exists that would disprove the finding..
  - Plain English: The reviewer's model family is parsed from a free-form self-report with a regex over a hardcoded family set; anything unmatched returns null and conservatively collapses to the same-model trust tag with no log. That means a legitimate-but-new model family is indistinguishable from garbage — both quietly read as 'same-model', so the user is told 'same-model' when the truth is 'could not resolve the family'. That understates the cross-model trust claim the harness's honesty discipline cares about. Fix: distinguish an explicit 'unresolved' state from 'same' so the run report can say it couldn't resolve the family rather than asserting same-model, and at minimum log when a self-report fails to resolve so the silent degrade is observable. Promote the known-family set to a single named constant.
  - Impact/effort: medium-impact / low-effort
- **The scripts' headline safety guarantees live in untested top-level orchestration** — `refactor` *(checked against the code)*
  - Technical: In both workflows/verify.js and workflows/audit.js, ALL tests target only the block between `// --- helpers ---` and `// --- end helpers ---` (extract-and-eval via `new Function`). The imperative top-level orchestration is entirely untested: verify.js `spawnJudge` (lines 354-369), the panel `parallel()` fan-out + `splitPanelResults` wiring + lensGaps/dedupedFindings assembly + the synthesis spawn + crossModel fold (lines 382-496); audit.js `spawnVerifier` (lines 790-821), the FIND/PRUNE/VERIFY wiring incl. the synthesis null-retry-then-throw (lines 919-930), the `thorough` sentinel null-retry-then-throw (lines 965-986), the blind-spot failed-sweep→pending path (lines 899-909), the failedCells→pendingCells assembly (lines 879-893, 1005-1007), and the doneCells/pendingCells resume math at the run level. The script header comments call these the 'headline guarantee this script makes mechanical' (audit.js:20-22 'exactly ONE finding-verifier per surviving finding') and 'Never a silent partial PASS' (verify.js:219), yet no test drives the orchestration that delivers them. `judgeOutcome`'s two-failures-throw is unit-tested (verify.test.mjs:341) but the `spawnJudge` wrapper that calls it (the actual retry/respawn loop) is not; audit.js has no analogous pure helper for spawnVerifier's retry, so its retry-then-defer contract is wholly untested. (recurs in 8 files: workflows/verify.js:354, workflows/verify.js:382, workflows/audit.js:790, workflows/audit.js:899, workflows/audit.js:919, workflows/audit.js:965, tests/workflows/verify.test.mjs, tests/workflows/audit.test.mjs). Evidence: I tried to refute this by hunting for any test that exercises the top-level orchestration; I could not find one — the finding holds on the code.

Boundary fact: in both scripts the marked extraction block ends before the orchestration. verify.js: `// --- end helpers ---` at workflows/verify.js:350; `spawnJudge` at 354-369; the panel `parallel()` fan-out + `splitPanelResults` wiring + lensGaps/dedupedFindings assembly + synthesis spawn + crossModel fold at 382-496 — all AFTER the marker. audit.js: `// --- end helpers ---` at workflows/audit.js:786; `spawnVerifier` at 792-823; FIND/PRUNE/VERIFY wiring incl. synthesis null-retry-then-throw at 919-932, thorough-sentinel null-retry-then-throw at 967-988, blind-spot failed-sweep→pending at 901-911, failedCells→pendingCells assembly at 881-895 + 1007-1009 — all AFTER the marker.

Test fact: all three test files touch only the helpers block or static source text, never the orchestration. tests/workflows/verify.test.mjs:33-68 and tests/workflows/audit.test.mjs:77-142 implement `loadHelpers()`, which slices `src` strictly between `^// --- helpers ---$` and `^// --- end helpers ---$` and evals ONLY that slice via `new Function`. The test headers themselves state the script "can't be imported wholesale" because the top-level flow "calls the tool primitives agent()/parallel()/phase()/log(), which are undefined under node" (verify.test.mjs:5-9). The third file, tests/workflows/cross-script.test.mjs, is a pure static byte-compare drift pin (readFileSync + regex extract + assert.equal) — it executes nothing.

Wrapper-vs-helper fact: `spawnJudge`/`spawnVerifier` are absent from both tests' `names` arrays and live outside the markers, so they are never loaded; a grep for `spawnJudge|spawnVerifier` across tests returns only comment mentions, no invocations. `judgeOutcome`'s two-failures-throw IS unit-tested (verify.test.mjs:341), but the `spawnJudge` wrapper that drives the real retry/respawn loop is not. audit.js has no pure helper analogous to judgeOutcome for the verifier, so spawnVerifier's retry-then-defer contract (audit.js:808-822) is wholly untested.

Headline-comment fact: the guarantee comments the finding names exist as quoted (audit.js:20-22 "exactly ONE finding-verifier per surviving finding"; verify.js:219 "Never a silent partial PASS"), asserted in prose but not driven by any test of the orchestration that delivers them.

Every specific sub-claim checks out; the `deterministic` confidence label is appropriate because the gap is mechanically visible from the extraction boundary..
  - Plain English: Both scripts test only their small pure 'decision' helpers; the imperative top-level control flow that wires them — spawning reviewers, retrying on failure, routing failed cells to pending, and the synthesis/sentinel null-retry-then-throw seams — is exercised by no test. So the headline promises ('exactly one verifier per finding', 'never a silent partial PASS', 'an unrun review is never treated as clean') are asserted in comments but not proven by effect. Fix: extract the retry/assembly logic into pure, injectable helpers (a verifierOutcome mirroring the already-tested judgeOutcome; a pure assembleRunResult for the failedCells/pendingCells/status math) and inject the agent/parallel primitives so a fake can drive the null-verifier, null-lens-batch, and null-blind-spot branches and assert the degradation effects.
  - Impact/effort: high-impact / medium-effort
- **A real-git, non-ASCII integration test runs on every CI build with no skip guard** — `refactor` *(checked against the code)*
  - Technical: tests/test_check_architecture_tree.py:852 `TestNonAsciiIntegration` is `@pytest.mark.integration` but CI runs bare `python -m pytest` (.github/workflows/ci.yml:28) with no marker selection, so it always runs. It depends on a real `git` binary, real filesystem unicode behavior, and macOS-vs-Windows NFC/NFD normalization (handled, but environment-coupled). The pyproject marker is declared (pyproject.toml:3) but there is no separate gated job, no `git`-availability skip guard, and no repeat/shuffle run to surface latent flakiness. Per the determinism dimension, environment-coupled tests that can fail for a non-code reason (missing/old git, locale) should be quarantined behind a guard or a dedicated job rather than mixed into the fast deterministic suite. (recurs in 3 files: tests/test_check_architecture_tree.py:852, .github/workflows/ci.yml:28, pyproject.toml:3). Evidence: Every factual leg of the claim holds against the code:

1. tests/test_check_architecture_tree.py:852 — `@pytest.mark.integration` decorates `class TestNonAsciiIntegration`. Its single test (lines 854-901) invokes the real git binary directly: `sp.run(["git", *pin, "init", ...], cwd=repo, check=True)` (line 875) plus config/add/commit (876-882), writes a real `scripts/café.py` to disk (880), and asserts on real filesystem unicode behavior with explicit NFC normalization to absorb macOS NFD (lines 896-897). So it is genuinely environment-coupled (git presence/version + locale + FS normalization).

2. .github/workflows/ci.yml:28 — the `test` job runs the whole suite bare: `python -m pytest`. No `-m "not integration"`, no marker selection. Confirmed there is NO `addopts`/`markexpr`/`-m`/deselect anywhere: grep over *.toml and the full tree returned no matches. pyproject.toml:3-4 only *declares* the marker ("integration: exercises a real git repo on disk ..."); declaring a marker does not deselect it — so the integration test always runs in the fast job on every push/PR to main.

3. No git-availability skip guard and no gated separation. Grep for `skipif|shutil.which|pytest.skip|importorskip|pytest_collection_modifyitems|add_marker` across tests/ found NOTHING. tests/conftest.py only inserts scripts/ into sys.path — no collection hook that would skip integration tests in the fast job. The only workflow file is ci.yml (no dedicated integration job). Because the git calls use `check=True` (line 875+), a build agent without git raises FileNotFoundError/CalledProcessError — an ERROR, not a skip, exactly as the finding states.

4. No repeat/shuffle to surface flakiness: the install step (ci.yml:26) installs only `pip pytest` — no pytest-repeat/pytest-randomly.

I actively hunted for each guard the finding says is missing (marker deselection, git-availability skip, dedicated job, repeat/shuffle) and none exist here or upstream. The determinism-dimension concern is accurate as stated..
  - Plain English: One test spins up a real git repo on disk with a non-ASCII filename and runs the real git binary. It is marked integration, but CI runs bare pytest with no marker selection, so it always runs — coupled to git availability and OS unicode normalization, with no repeat/shuffle to surface latent flakiness. On a git-less or locale-odd agent it would error rather than skip, the class of test most likely to cause confusing intermittent red builds. Fix: gate it behind a git-availability skipif and run it in a dedicated 'pytest -m integration' step separate from the fast deterministic unit run, or at minimum add the skip guard so a git-less agent skips rather than errors.
  - Impact/effort: low-impact / low-effort
- **Version-sync gate reports a file read error as 'not valid JSON'** — `bug` *(checked against the code)*
  - Technical: In scripts/check_versions_synced.py both readers catch `(ValueError, OSError)` from `json.loads(path.read_text(...))` and emit a single message: `f"{path} is not valid JSON ({exc}) — fix the manifest."` (lines 46-47 and 65-66). A ValueError (genuine JSON parse failure) and an OSError (read failure after the .exists() check passed — permission denied, file vanished mid-run, is-a-directory) are conflated under the JSON-parse wording. The remedy 'fix the manifest [JSON]' is wrong for the OSError case. The standard's 'error messages identify the cause and suggest a remedy' bar is partially unmet — the exception text (exc) is included so it's recoverable, but the headline misdirects. (check_architecture_tree.py handles this more carefully, with a dedicated UnicodeDecodeError branch and OSError→conservative-False in _is_harness_managed.) (recurs in 2 files: scripts/check_versions_synced.py:46, scripts/check_versions_synced.py:65). Evidence: scripts/check_versions_synced.py:44-47 (`_read_plugin_version`) and 63-66 (`_read_marketplace_version`) each wrap BOTH `path.read_text(encoding="utf-8")` and `json.loads(...)` in one try, then `except (ValueError, OSError) as exc: return (None, f"{path} is not valid JSON ({exc}) — fix the manifest.")`. The `.exists()` guards at lines 42 and 61 run before the try, leaving a real window where `read_text` raises OSError (permission denied, file removed mid-run, is-a-directory). OSError there is genuinely a read failure, not bad JSON, yet it is reported under the identical "is not valid JSON ... fix the manifest" headline — a misdiagnosis of the cause and remedy. json.JSONDecodeError subclasses ValueError, so the ValueError arm is the real JSON case; OSError is conflated with it. The raw `{exc}` IS interpolated, so the message is recoverable but the headline misleads — exactly the partial-miss the finder described. The contrast also checks out: check_architecture_tree.py:160 has a dedicated UnicodeDecodeError branch and lines 187-188 treat OSError distinctly (conservative-False in _is_harness_managed). No upstream/alternative guard distinguishes the two error classes before the user-facing string is built. The gate does still fail loudly (exit 1, no swallow), as the claim concedes — this is a message-precision/diagnosability defect, not a fail-open one..
  - Plain English: Both manifest readers in the version-sync gate catch ValueError and OSError together and emit one message: 'is not valid JSON — fix the manifest'. A genuine parse failure and a read failure (permission denied, file vanished mid-run, is-a-directory) are conflated, so the OSError case points the user at the wrong fix. The gate still fails loud with exit 1 (good) but misdiagnoses the cause. Fix: split the except into ValueError ('is not valid JSON — fix the manifest') and OSError ('could not be read — check file permissions/path') so each failure names its actual cause and remedy.
  - Impact/effort: low-impact / low-effort
- **Sibling pipelines merge duplicate-finding confidence in OPPOSITE directions** — `refactor` *(checked against the code)*
  - Technical: verify.js dedupFindings: `if (merged.confidence !== "deterministic" && finding.confidence === "deterministic") merged.confidence = "deterministic"` (verify.js:168) — STRONGEST-confidence-wins (upgrades to deterministic). audit.js dedupFindings: `if (finding.confidence === "judgment") merged.confidence = "judgment"` with the comment 'WEAKEST confidence wins ... never upgraded' (audit.js:271-275) — opposite policy. DECISIONS.md records the shared 'copied verbatim' block as exactly {MODELS, SAME_MODEL_TAG, sameModelTag, parseArgs} (line 23,26) and records audit's weakest-wins as a deliberate choice (line 25) — but there is NO decision reconciling it against verify's strongest-wins, and the cross-script.test.mjs drift pin covers only the four shared helpers, not dedupFindings. The upgrade-to-deterministic direction in verify.js is the riskier one for a gate (it can over-state the provability of a blocking finding, against the project's [D]/[J] honesty discipline in DECISIONS.md:10). (recurs in 2 files: workflows/verify.js:168, workflows/audit.js:271-275). Evidence: Both cited blocks are present verbatim and are the same conceptual operation (merging duplicate findings across lenses), but apply OPPOSITE confidence-merge policies.

verify.js:168-170 (function dedupFindings, header comment line 143 "prefer `deterministic` confidence"):
  if (merged.confidence !== "deterministic" && finding.confidence === "deterministic") {
    merged.confidence = "deterministic";
  }
=> STRONGEST-wins: a duplicate upgrades the merged finding toward `deterministic`.

audit.js:273-277 (function dedupFindings, header comment line 234 "WEAKEST confidence wins ... never upgraded"):
  // Weakest confidence wins: a `judgment` member downgrades the merged finding; once
  // judgment, it never upgrades back to deterministic.
  if (finding.confidence === "judgment") {
    merged.confidence = "judgment";
  }
=> WEAKEST-wins: a duplicate downgrades the merged finding toward `judgment`.

Both scripts use the identical two-value confidence enum ["deterministic","judgment"] (verify.js:275, audit.js:549), so for the same input (one `deterministic` + one `judgment` duplicate) verify yields `deterministic` and audit yields `judgment` — opposite outcomes. No upstream reconciliation or normalization collapses the difference.

Provenance claims also confirmed:
- docs/DECISIONS.md:25 records the copied-verbatim shared block as exactly MODELS/SAME_MODEL_TAG/sameModelTag/parseArgs — dedupFindings is NOT in the shared block; audit's weakest-wins is recorded (DECISIONS.md:24) but no decision reconciles it against verify's strongest-wins.
- tests/workflows/cross-script.test.mjs:28-39 pins drift on only MODELS, SAME_MODEL_TAG, modelFamily, sameModelTag, parseArgs — dedupFindings is unguarded by the drift pin.
- The [D]/[J] honesty discipline the finding cites is at docs/DECISIONS.md:10. The verify upgrade-to-deterministic direction is the riskier one for a gate, consistent with that discipline..
  - Plain English: When two reviewers flag the same thing with different confidence, the Verify script upgrades the merged result to 'deterministic' (strongest-wins) while the Audit script downgrades it to 'judgment' (weakest-wins). They look like the same dedup step and share a copied block, but disagree on direction — so the same situation yields a 'provable' label in one pipeline and a 'judgment' label in the other. Verify's upgrade-to-deterministic is the riskier one for a gate: it can over-state the provability of a BLOCKING finding, against the project's [D]/[J] honesty discipline. Fix: pick one canonical direction (weakest-wins is the honesty-aligned conservative choice) and apply it in both, or record in DECISIONS.md why they must differ; fold the rule into the pinned block so future drift is caught.
  - Impact/effort: medium-impact / low-effort

### Tier 3 — polish

- **The audit script's main run logic is one ~200-line inline top-level sequence** — `refactor` *(checked against the code)*
  - Technical: workflows/audit.js top-level control flow (audit.js:826-1026) inlines FIND orchestration, the failed-cell bookkeeping (audit.js:879-909), the synthesis retry-or-throw seam (audit.js:915-930), the baseline-injection block (audit.js:937-958), the thorough-only sentinel pass (audit.js:965-986), and the doneCells/pendingCells assembly (audit.js:1005-1021) in one async body. The pure helpers are well-factored, but the *orchestration* between them is not decomposed — unlike verify.js which is shorter. The Workflow-sandbox 'no module wrapper' constraint forces top-level code, but stage orchestration could still be extracted into named async functions (spawnFindPhase, runPrunePhases) called from a thin top-level, improving analysability and testability of the seams. (recurs in 3 files: workflows/audit.js:826-1026, workflows/audit.js:879-909, workflows/audit.js:937-958). Evidence: The cited structure is confirmed by reading workflows/audit.js:828-1028. The top-level control flow is one uninterrupted async sequence (~200 lines) that inlines every stage with no orchestration wrappers:
- Enumeration/budgeting: audit.js:845-849
- FIND fan-out (parallel lens + blindspot tasks): audit.js:858-877
- Failed-cell bookkeeping (the null-batch loop + blindspot last-element index arithmetic): audit.js:881-911 (matches cited 879-909)
- PRUNE synthesis retry-or-throw seam: audit.js:917-932
- Baseline-injection block: audit.js:939-960 (matches cited 937-958)
- Thorough-only sentinel pass: audit.js:967-988
- VERIFY fan-out: audit.js:994-999
- doneCells/pendingCells assembly: audit.js:1007-1021

Refute attempt failed: I searched for the decomposition the finding says is missing. `Grep` for `spawnFindPhase|runPrunePhases|async function` in audit.js returns only `async function spawnVerifier` (audit.js:792) — a per-task verifier primitive, not a phase orchestrator. No named async functions wrap the FIND or PRUNE orchestration; the seams are inline. The pure helpers (audit.js:1-823) are all well-factored named functions, exactly as the finding concedes — it faults the *orchestration*, not the helpers.

The comparison anchor checks out: verify.js top-level flow is ~125 lines (verify.js:371-496, total file 496 lines vs audit.js 1028) with 2 phases, and it extracts its result-splitting/index-arithmetic seam into a named, tested helper `splitPanelResults` (verify.js:430, comment 'pins the index arithmetic (tested)') plus `panelRoster`/`coverageGaps`/`crossModelOutcome`. audit.js does the analogous splitting inline (e.g. the blindspot-is-last-element arithmetic at audit.js:902, the failed-cell loop at 883-911). So the finding's specific contrast — 'unlike verify.js which is shorter' and 'stage orchestration could still be extracted into named async functions' — is grounded in the actual code, and the seams are demonstrably extractable within the Workflow no-module-wrapper constraint (verify.js already does it). This is a judgment-tagged maintainability finding; its factual substrate is fully verified and I could not prove it wrong..
  - Plain English: The audit top-level control flow inlines enumeration, budgeting, fan-out, two prune passes, verification, and result assembly in one async body. The pure helpers are well-factored but the orchestration between them is not decomposed, so every future change to any one stage touches this single block — the shotgun-surgery/divergent-change risk the standard warns about. It is readable today, which is why this is polish not a blocker. Fix: extract each phase's orchestration into a named async function (runFindPhase, runPrunePhases, assembleResult) and reduce the top-level body to a short pipeline of those calls, isolating each stage's change-surface. This also unlocks the orchestration-seam testing finding.
  - Impact/effort: low-impact / medium-effort

**Recommended starting point:** A single reviewer throwing crashes the whole audit instead of degrading to PARTIAL.

Re-checked every finding I surfaced against the code (the cross-model judge — by default a different model family than the builder); dropped 2 that couldn't be confirmed — verified 8 · unconfirmed 0 · deferred 0.

*To start anything — a backlog item or a brand-new project — just tell the agent in plain English what you want (e.g. 'Let's do Tier-1 item 1' or 'I want to build X'). It will ask you questions (Discuss), then write a plan and spec for you to approve before any code. For a backlog item, the go-button is **`/claugentic-dev-harness:build`** — point it at one item ('build Tier-1 item 1') and it drives the whole reviewed pipeline for you, pausing only at the spec (before any code) and before anything irreversible.*
<!-- harness-audit:backlog:end -->

## Now

- **Harness v2 — re-platform the choreography (plan `0012`).** The audit pipeline, Verify panel, and build loop become executable Workflow scripts (invoked from the plugin install path); deterministic trust-gates (CI, land-gate, secret-scan, characterization-first) unlock an earned **build-to-green** autonomy ladder; a runtime-verification (QA) workflow drives the real app against acceptance criteria; a product layer (spec rebuild + product-gap audit) answers "is this product what it intended to be." Then: the **AskBase pilot** (spec rebuild with the user → gap audit → build-to-green to release-ready) before rolling across the neeshinc suite. (Decisions: `DECISIONS.md` → Harness v2.)
- **Standards-catalog integrity pass** (from the 2026-06-11 multi-lens review, all verified): bring the four `migrated` modules to the deep-module bar; make dimension-level Confidence labels match per-check tags (security.md has six "deterministic" dimensions containing `[J]` checks); replace the 14 dangling `ENGINEERING_STANDARDS.md §` sources; fix the verifiable citation errors (WCAG 2.4.13 is AAA not AA; AWS REL-9/idempotency; SRE-book ch. 5).
- **README first-screen rewrite + one real artifact** (verified DX findings): short plain sentences for the three commands, hedges stated once in Status, an embedded sample backlog excerpt + init report; document uninstall, update, and add a CHANGELOG.

## Later

- **Bound the tree-gate git subprocess with `timeout=`** and map `TimeoutExpired` onto the existing `RuntimeError` boundary (Slice-2 Verify lens note; local read-only git hang is unlikely — low priority).
- **Pin the workflow-script output schemas to their agent `.md` contracts.** The schemas' required field-sets are test-pinned in-script (Slice 2), but nothing links them to the agent files' prose contracts — a doc-side contract change can still drift silently. Decide one home or add a cross-pin when a second script consumes the same contracts.

- **Silence the pre-existing `pytest-asyncio` deprecation warning** (set `asyncio_default_fixture_loop_scope` in `pyproject.toml`) — surfaced during Slice 1; unrelated to any gate.
- **CI installs only `pytest`** — if the test suite ever gains a third-party dependency, add a requirements file + install step to `.github/workflows/ci.yml` (today's suite is pure-stdlib; noted by the Slice-1 re-verify).

- **Reconcile the `settings.json` tree-check hook *command string* on format change.** Today `init` keys hook-presence only on the `check_architecture_tree.py` substring (idempotency key, step 5b), so a managed copy whose hook *command format* drifts between plugin versions (flags, interpreter convention, quoting) is detected as "already present" and never updated. When a hook-command format change ships, `init` should reconcile the managed hook entry — without clobbering a user-customized command — rather than leaving the old form in place. (Deferred from plan 0010 — out of scope for the version-aware init slice.)
- **Make the managed `check_architecture_tree.py` layout comment adopter-neutral.** The script carries a hardcoded comment — `scripts/check_architecture_tree.py:56` "For THIS repo (claugentic-dev-harness) the only code is the check script itself." — which is **wrong once the file is copied into an adopter repo** (the adopter's code is *not* just the check script). Consider rewording that comment to be adopter-neutral (the `INCLUDE_GLOBS` carve-out already protects the per-repo *knob*, but the prose comment is a managed verbatim line that misleads in every adopter). (Surfaced by the real AskBase refresh.)
- **Warn before a REFRESH when the git-recovery net is empty.** A REFRESH overwrites a genuine managed copy and relies on **git history** as the recovery net (the user's prior version is recoverable from a commit). That net is **empty** when the overwritten file was **uncommitted/user-edited** (the working-tree version isn't in history) or the repo has **no commits at all**. Before such a REFRESH, `init` should **warn and suggest committing first** so the prior content is recoverable. (Deferred from plan 0010 Stage-7 verify — out of scope for the version-aware init slice.)
