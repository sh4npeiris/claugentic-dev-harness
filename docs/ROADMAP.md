# Roadmap

The forward backlog for the harness itself. Work runs through `docs/WORKFLOW.md` (triage → plan → review → spec → approve → implement → verify → land → retrospect), sliced so each unit lands complete with no tech debt. Tangents land here, never silently into the current change.

> The harness audits *adopter* repos via `/claugentic-dev-harness:audit`, which writes its own `harness-audit:overview` / `harness-audit:backlog` fences into that repo's `docs/ROADMAP.md`. This file is the product's own roadmap, not a generated audit — an adopter generates theirs by running the skill.

---

## The two standing tracks — growing the libraries (ongoing · just-in-time)

The harness grows as it meets more codebases. Neither track is ever "done" — that's the right shape: additions are made when real use proves a gap, never speculatively.

| Track | How it grows |
|------|--------------|
| **Grow the role library** (`.claude/agents/`) — add specialist agents as real use surfaces gaps the starter set lacks. | The workflow delegates to specialists; when real work keeps hitting a job no existing role owns, that's the signal to add one (and register it in `plugin.json`, the WORKFLOW roster, and the architecture tree). |
| **Grow the standards catalog** (`docs/standards/`) — author new quality modules and capability modules (Redis, queues, object-storage, …) as real projects pull them in. | A real audit or review that needs a bar the catalog doesn't carry is the signal to author it (conforming to `_TEMPLATE.md`, indexed in the catalog README). The catalog modernizes vibe-coded apps, not just cleans code. |

---

## Backlog — the work worth doing  _(generated by /claugentic-dev-harness:audit · do not edit — re-run to refresh)_

<!-- harness-audit:backlog:start -->
status: COMPLETE · level: gap · done-cells: [PS-1, PS-2, PS-3, PS-4] · pending-cells: [] · date: 2026-06-12

`refactor` = tidy without changing behavior · `capability-upgrade` = add/upgrade a technology · `dependency-health` = update/patch dependencies · `bug` = fix wrong behavior · `feature` = new behavior.
`(checked against the code)` = a separate agent re-read the code and couldn't refute it · `(could not confirm independently — model's assertion)` = still just the model's claim · `(⚠ not yet verified — re-run to confirm)` = budget ran out before checking — a re-check by a different model family than the builder (the cross-model judge; on a same-family run, tagged as such) — a reduction of shared-blind-spot risk, not a mechanical guarantee.

### Tier 1 — critical

- **Re-running an unfinished audit silently loses findings the first pass already confirmed** — `bug` *(checked against the code)*
  - Technical: workflows/audit.js builds result.items = kept.map(toResultItem), where kept derives only from toVerify = [...survivors, ...normalizedDeferred] (audit.js:1247, :1276). survivors come only from THIS run's runCells (the pending cells the resume swept). normalizedDeferred comes only from input.deferredFindings, which the SKILL passes as the prior-run items tagged 'deferred' ONLY (skills/audit/SKILL.md:217-219: findings with a resolved verdict 'are not re-passed — their verdicts persist in the fence'). renderBacklogFence(result) regenerates the ENTIRE fence body from result.items (audit.js:960-976), and SKILL Phase 3 writes it by 'replace only the content inside the fence' (skills/audit/SKILL.md:322). There is no merge step that carries prior verified/unconfirmed findings from already-done cells forward. Net: on a PARTIAL resume, every finding from the first pass's done cells that was Verified or Unconfirmed is dropped from the regenerated fence. This contradicts the spec promise (PS-2 expect 'a re-run resumes from') and the explicit DECISIONS/SKILL claim that resolved verdicts 'persist in the fence' (docs/DECISIONS.md:26, skills/audit/SKILL.md:218-219). (recurs in 5 files: C:\Users\shan.peiris\source\repos\claugentic-dev-harness\workflows\audit.js:1247, C:\Users\shan.peiris\source\repos\claugentic-dev-harness\workflows\audit.js:1276, C:\Users\shan.peiris\source\repos\claugentic-dev-harness\workflows\audit.js:960, C:\Users\shan.peiris\source\repos\claugentic-dev-harness\skills\audit\SKILL.md:217, C:\Users\shan.peiris\source\repos\claugentic-dev-harness\skills\audit\SKILL.md:322). Evidence: The carry-forward/merge step that would make resolved verdicts "persist in the fence" does not exist. Traced the full chain:

1. workflows/audit.js:1276 — `const items = kept.map(toResultItem)`. The fence's items come ONLY from `kept`.
2. workflows/audit.js:1247 — `const toVerify = [...survivors, ...normalizedDeferred]`; `kept` is derived from `toVerify` (audit.js:1256 `applyVerdicts(toVerify, verifyResults)`). So `result.items` cannot contain anything not in `toVerify`.
3. `survivors` come only from THIS run's FIND→PRUNE over `runCells` (audit.js:1182 `applyPrune(annotated, synthesis.cuts)`, where `annotated`/`dedupedFindings` derive from `batches` built only from `runCells` at audit.js:1063-1064 / 1120-1136). `runCells` is the pending cells the resume swept (audit.js:1058-1062).
4. `normalizedDeferred` comes only from `input.deferredFindings` (audit.js:1242), and per SKILL.md:217-219 and docs/DECISIONS.md:26 only `deferred`/`⚠ not yet verified` findings are passed there — "findings carrying a *resolved* verdict tag ... are **not** re-passed."
5. The input contract has NO field for prior resolved items: validateSharedArgs (audit.js:224-229) accepts only `doneCells` and `deferredFindings`; the run reads only `doneCellsIn` and `deferredFindings` (audit.js:1033-1034). There is no `priorItems`/`verifiedFindings`/merge anywhere (grep for merge/carry-forward/priorItems across the repo found none).
6. renderBacklogFence (audit.js:960-976) rebuilds the ENTIRE inner fence body — its tiers are `items.filter(it => it.tier === N)` over `result.items` only (audit.js:962-970). The SKILL writes it by replacing the whole inner fence body (SKILL.md:305-306 "Write that string between the markers"; SKILL.md:322 "replace only the content inside the fence"). The "outside is human-owned" preservation (SKILL.md:322-326) protects human roadmap items and `## Later` OUTSIDE the fence — it does NOT preserve prior generated items inside it.

Net: on a PARTIAL resume, every Verified/Unconfirmed finding from already-done cells is absent from `result.items` and is wiped by the wholesale fence-body replacement. This contradicts the explicit promise that resolved verdicts "persist in the fence" (skills/audit/SKILL.md:219, docs/DECISIONS.md:26) and the PS-2 resume guarantee..
  - Plain English: When an audit is too big to finish in one pass and you re-run it to continue, the findings the first pass already verified vanish from the backlog. The re-run rebuilds the entire backlog from scratch using only what it could check THIS time (the pending cells it swept) plus the items explicitly tagged 'not yet verified' — so a finding the first pass confirmed and tagged 'checked against the code' disappears after the second pass. This is worse than a quiet bug: the harness's own docs (docs/DECISIONS.md:26) and the audit skill (skills/audit/SKILL.md:218-219) both PROMISE these resolved findings 'persist in the fence,' and the backlog file is the one place users are told to trust as the running record. The mechanism that would honor that promise does not exist. In workflows/audit.js:1276 the rendered fence is built from result.items = kept.map(...), and kept derives only from this run's survivors plus the carried-over deferred findings; renderBacklogFence (audit.js:960-976) full-regenerates the fence body, and the skill writes it by replacing everything inside the fence (skills/audit/SKILL.md:322) — there is no step that carries prior-pass resolved items from already-done cells forward. Fix: on resume, parse the existing fence's resolved items and pass them back to the script (e.g. a resolvedFindings arg) to merge into result.items WITHOUT re-verifying, OR merge the prior fence's resolved-tier items at write time instead of full-replace; add a regression test in tests/workflows/audit.test.mjs asserting a resume's rendered fence still contains a done-cell finding that was Verified on the prior pass.
  - Impact/effort: High impact (data loss in the trusted backlog + a broken documented contract), medium effort (resume merge step + one regression test; the resolved items already exist in the fence to parse)

### Tier 2 — important

- **Init's plain-English headline claims it refreshed files even when it changed nothing** — `bug` *(checked against the code)*
  - Technical: PS-1 expect 'the report headline branches on whether anything was refreshed — never a false claim' is only half-delivered. The headline branch keys on the Refreshed group (step 9, line 408). The empty-Refreshed branch text (lines 409-412) asserts 'only my own managed files were refreshed to the installed version.' But the empty-Refreshed branch is exactly the true-no-op case where zero files were refreshed (and also the absent-only/Created case where managed files were CREATED, not refreshed). The assertion is therefore false in the true-no-op path and inaccurate in the create-only path. This directly contradicts line 446's own true-no-op copy ('already at the installed version — nothing to refresh'). The spec's no-false-clobber direction IS covered (the non-empty branch correctly appends the honest caveat and never asserts 'I did NOT overwrite your own files' when a managed file was refreshed), but the inverse — a false *refresh* assertion in the headline — slips through. (recurs in 2 files: C:\Users\shan.peiris\source\repos\claugentic-dev-harness\skills\init\SKILL.md:409-412, C:\Users\shan.peiris\source\repos\claugentic-dev-harness\skills\init\SKILL.md:446-448). Evidence: The empty-Refreshed headline branch (skills/init/SKILL.md:409-412) ends with the unhedged reassurance: "...only my own managed files were refreshed to the installed version." By the skill's own definitions, the empty-Refreshed group IS the true-no-op case — SKILL.md:12 states a true no-op "creates nothing, refreshes nothing, and reports everything as already current," and SKILL.md:446-447 instructs that same run to report "already at the installed version — nothing to refresh." So the headline's literal "were refreshed" asserts an action that did not occur on a no-op run, and is in direct tension with the line-446 body copy ("nothing to refresh") for the very same run. The empty-Refreshed branch also covers the create-only path (files in the Created group per SKILL.md:434-435 are created, not refreshed), where "were refreshed" is likewise inaccurate. I hunted for an upstream guard or a no-op-specific headline variant that would suppress this wording — none exists; the only refinement is the two-way branch on the Refreshed group (SKILL.md:408), and the empty branch carries this fixed "were refreshed" text. So the core code-level defect is confirmed.

One correction to the finding's framing: it paraphrases criterion PS-1 as "never a false claim" of any kind. The actual spec text (docs/PRODUCT_SPEC.md:89) is narrower — "the report headline branches on whether anything was refreshed — never a false 'nothing overwritten' claim." PS-1's no-false-claim obligation is scoped to the OVERWRITE assertion, which the headline does honor (the finding concedes this). The false-REFRESH wording is a real inaccuracy in the delivered text and a self-contradiction with line 446, but it is not, strictly, the thing PS-1's literal words forbid. The defect is genuine; the PS-1 citation is slightly overstated..
  - Plain English: When you re-run init and nothing actually changed (a true no-op at the same version), the reassurance headline still tells you 'only my own managed files were refreshed to the installed version' — but in that case nothing was refreshed at all, and a few lines later init's own no-op message correctly says 'nothing to refresh' (skills/init/SKILL.md:446). The headline branch keys on the Refreshed group being empty (lines 409-412), but empty-Refreshed is exactly the true-no-op case (and also the first-run case where managed files were CREATED, not refreshed) — so the assertion is false in the no-op path and inaccurate in the create-only path, and contradicts the report's own no-op line. For a harness whose entire positioning is honesty about what it did versus didn't do, a headline that asserts an action that never happened is a real defect, not cosmetic. Fix: split the empty-Refreshed headline into its two real sub-cases — true no-op (Refreshed empty AND Created empty) reads 'everything is already at the installed version; I changed nothing'; created-only (Refreshed empty, Created non-empty) reads 'I added a code map, a quality checklist, and a safety check; I did NOT change or overwrite any of your own files' — and drop the unconditional '...were refreshed to the installed version' clause that can't be true when Refreshed is empty.
  - Impact/effort: Medium impact (user-facing honesty claim that's false in the most common re-run path), low effort (copy-only edit to one headline branch, no logic change)
- **Build skill's summary line still says the build-to-green engine isn't installed — it now is** — `bug` *(checked against the code)*
  - Technical: PS-4 expect[0] requires the decline to name 'exactly the unmet unlock conditions with the evidence checked.' The SKILL body (skills/build/SKILL.md:58-76) correctly enumerates four conditions, each with a matching fixed decline line whose engine line (76) is conditional ('the Workflow tool isn't available... | workflows/build-item.js isn't in the installed plugin') — current and correct. But the frontmatter description (skills/build/SKILL.md:2) hardcodes the absolute stale claim 'the engine is installed (it isn't yet, so today it declines).' The engine workflows/build-item.js now exists and the body invokes it via ${CLAUDE_PLUGIN_ROOT}/workflows/build-item.js (lines 54, 64, 80). The description thus contradicts the body: it asserts a blanket always-decline-because-engine-missing, which (a) is factually stale and (b) risks the decline naming an engine-missing condition that is no longer unmet, violating the 'exactly the unmet conditions' promise. (skills/build/SKILL.md:2). Evidence: skills/build/SKILL.md:2 (frontmatter description) hardcodes an absolute, unconditional claim: "...unwatched build-to-green is requestable but runs only where the repo has earned it (CI running the gates, a test baseline, a testable approved spec) and the engine is installed (it isn't yet, so today it declines); otherwise it declines naming exactly what's missing and offers checkpoint." The parenthetical "(it isn't yet, so today it declines)" asserts the engine is categorically not installed and the system therefore always declines.

That premise is now false: workflows/build-item.js EXISTS (Glob hit; 847 lines) and is a real executable engine — its own header reads "workflows/build-item.js — the build-to-green inner loop as an executable Workflow script... The engine the 5a autonomy ladder unlocks." The SKILL body invokes it: skills/build/SKILL.md:54 ("run the item through the engine script (${CLAUDE_PLUGIN_ROOT}/workflows/build-item.js via the Workflow tool)"), :64, and :80 ("invoke the engine (${CLAUDE_PLUGIN_ROOT}/workflows/build-item.js").

The body treats engine-presence correctly as a CONDITIONAL decline slot — skills/build/SKILL.md:76: "the engine itself — <the Workflow tool isn't available in this session | workflows/build-item.js isn't in the installed plugin>; build-to-green never runs as prose." That slot fires only when the engine is actually absent. So line 2's blanket "it isn't yet" directly contradicts the body's conditional line 76.

Against PS-4 (expect[0]: the decline must name "exactly the unmet unlock conditions with the evidence checked"), the stale line 2 promise asserts a perpetual engine-missing decline for a condition that is no longer unmet — both (a) factually stale and (b) contradicting the body's per-condition contract. No disproving evidence exists: line 2 is unconditional (not a fillable slot) and the engine file is present, so the finding cannot be refuted..
  - Plain English: The build skill's one-line frontmatter description (skills/build/SKILL.md:2 — the summary shown to the user before anything runs) still says the build-to-green engine 'is installed (it isn't yet, so today it declines).' That is now false: workflows/build-item.js exists and the skill body invokes it (lines 54, 64, 80). The body's decline logic is correct and current — it enumerates four real unlock conditions (CI, test baseline, approved testable spec, Workflow-tool availability) each with its own conditional decline line — but the stale frontmatter asserts a blanket 'always declines because the engine is missing,' which both misinforms the user and risks the decline naming an engine-missing reason that is no longer unmet, violating the spec's 'name exactly the unmet conditions' promise. Fix: drop the stale '(it isn't yet, so today it declines)' parenthetical from line 2 and replace it with the current truth — the engine is wired, and build-to-green declines only where the repo hasn't earned the unlock conditions or the Workflow tool is unavailable this session — matching the conditional logic already in the body (lines 64, 76).
  - Impact/effort: Medium impact (stale user-facing claim that can drive a wrong-reason decline), low effort (one-line frontmatter copy fix, body already correct)

### Tier 3 — polish

_(empty)_

**Recommended starting point:** Re-running an unfinished audit silently loses findings the first pass already confirmed.

Re-checked every finding I surfaced against the code (the cross-model judge — by default a different model family than the builder); dropped 0 that couldn't be confirmed — verified 3 · unconfirmed 0 · deferred 0.

*To start anything — a backlog item or a brand-new project — just tell the agent in plain English what you want (e.g. 'Let's do Tier-1 item 1' or 'I want to build X'). It will ask you questions (Discuss), then write a plan and spec for you to approve before any code. For a backlog item, the go-button is **`/claugentic-dev-harness:build`** — point it at one item ('build Tier-1 item 1') and it drives the whole reviewed pipeline for you, pausing only at the spec (before any code) and before anything irreversible.*
<!-- harness-audit:backlog:end -->

## Now

- **Harness v2 — re-platform the choreography (plan `0012`).** The audit pipeline, Verify panel, and build loop become executable Workflow scripts (invoked from the plugin install path); deterministic trust-gates (CI, land-gate, secret-scan, characterization-first) unlock an earned **build-to-green** autonomy ladder; a runtime-verification (QA) workflow drives the real app against acceptance criteria; a product layer (spec rebuild + product-gap audit) answers "is this product what it intended to be." Then: the **AskBase pilot** (spec rebuild with the user → gap audit → build-to-green to release-ready) before rolling across the neeshinc suite. (Decisions: `DECISIONS.md` → Harness v2.)
- **Standards-catalog integrity pass** (from the 2026-06-11 multi-lens review, all verified): bring the four `migrated` modules to the deep-module bar; make dimension-level Confidence labels match per-check tags (security.md has six "deterministic" dimensions containing `[J]` checks); replace the 14 dangling `ENGINEERING_STANDARDS.md §` sources; fix the verifiable citation errors (WCAG 2.4.13 is AAA not AA; AWS REL-9/idempotency; SRE-book ch. 5).
- **README first-screen rewrite + one real artifact** (verified DX findings): short plain sentences for the three commands, hedges stated once in Status, an embedded sample backlog excerpt + init report; document uninstall, update, and add a CHANGELOG.

## Later

- **audit.js housekeeping (from the Slice-4a Verify panel, out of that diff's scope):** remove the orphaned `cellKey` comment stranded at its old location; drop `renderItem`'s unconditional trailing period; consider unifying the LEGEND/VERIFICATION_PHRASE two-copy trust strings (both test-pinned today, so drift is caught); reconcile `dedupFindings`' opposite-direction confidence merge between `verify.js` (upgrades to deterministic) and `audit.js` (weakest wins) — decide one semantic or document why they differ.

- **Bound the tree-gate git subprocess with `timeout=`** and map `TimeoutExpired` onto the existing `RuntimeError` boundary (Slice-2 Verify lens note; local read-only git hang is unlikely — low priority).
- **Pin the workflow-script output schemas to their agent `.md` contracts.** The schemas' required field-sets are test-pinned in-script (Slice 2), but nothing links them to the agent files' prose contracts — a doc-side contract change can still drift silently. Decide one home or add a cross-pin when a second script consumes the same contracts.

- **Silence the pre-existing `pytest-asyncio` deprecation warning** (set `asyncio_default_fixture_loop_scope` in `pyproject.toml`) — surfaced during Slice 1; unrelated to any gate.
- **CI installs only `pytest`** — if the test suite ever gains a third-party dependency, add a requirements file + install step to `.github/workflows/ci.yml` (today's suite is pure-stdlib; noted by the Slice-1 re-verify).

- **Reconcile the `settings.json` tree-check hook *command string* on format change.** Today `init` keys hook-presence only on the `check_architecture_tree.py` substring (idempotency key, step 5b), so a managed copy whose hook *command format* drifts between plugin versions (flags, interpreter convention, quoting) is detected as "already present" and never updated. When a hook-command format change ships, `init` should reconcile the managed hook entry — without clobbering a user-customized command — rather than leaving the old form in place. (Deferred from plan 0010 — out of scope for the version-aware init slice.)
- **Make the managed `check_architecture_tree.py` layout comment adopter-neutral.** The script carries a hardcoded comment — `scripts/check_architecture_tree.py:56` "For THIS repo (claugentic-dev-harness) the only code is the check script itself." — which is **wrong once the file is copied into an adopter repo** (the adopter's code is *not* just the check script). Consider rewording that comment to be adopter-neutral (the `INCLUDE_GLOBS` carve-out already protects the per-repo *knob*, but the prose comment is a managed verbatim line that misleads in every adopter). (Surfaced by the real AskBase refresh.)
- **Warn before a REFRESH when the git-recovery net is empty.** A REFRESH overwrites a genuine managed copy and relies on **git history** as the recovery net (the user's prior version is recoverable from a commit). That net is **empty** when the overwritten file was **uncommitted/user-edited** (the working-tree version isn't in history) or the repo has **no commits at all**. Before such a REFRESH, `init` should **warn and suggest committing first** so the prior content is recoverable. (Deferred from plan 0010 Stage-7 verify — out of scope for the version-aware init slice.)
